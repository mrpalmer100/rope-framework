# v2.3.6 — The axis becomes a number

Corpus state: **233 registered claims (102 Derived, 112 Modeled, 4 EFT-constrained, 3 Conjecture,
5 Open, 7 Failed-and-kept); 223 code-backed benchmarks, all passing; 60 papers.**

Three claims (FND-MATTER-036..038) and a full documentation pass, completing the gate saga: the
last free parameter confiscated by a mechanism, the closure achieved through the corpus's own
density-onset principle, and the axis finally COMPUTED -- a percolation simulation with zero
free parameters landing at the phenomenological window's edge.

## The alpha-fixing mechanism: collision saturation (FND-MATTER-036)
The hunt for an alpha-fixing observable ended somewhere better -- at a mechanism that closes a
logical circle. Reading C carried an IOU (WHY frequency-blind fixed amplitudes?) and collision
saturation pays it while confiscating the free parameter: amplitudes are frequency-blind because
geometry does not care about frequency, and fixed because the gap is the gap. The Monte Carlo
shows a genuine thermal-to-saturated crossover (physics, not tautology) with the saturated value
drive-independent. Lambda = (1 - x)^2/12 with x = D/a: the gate's ledger settles line by line --
five microstructure unknowns, one constant, one bounded fraction, one geometric ratio.

## The 004-closure attempt (FND-MATTER-037)
Four movements, each compelled by the last. THE MEASUREMENT (new, instrument-grade): tight
tangles are in contact essentially everywhere (nearest opposing strand at 0.998 D at every site,
all knots) -- caught after the instrument twice reported impossible numbers and was twice
repaired; it retroactively explains contact lengths exceeding rope lengths. THE KILL: no jitter
room in self-packing; Lambda ~ 0 in that reading, kept. THE REROUTE: the ontology's own
commitments eliminated two candidate meshes by pure logic (ambient singles interpenetrable;
self-packing gapless), leaving the tube's INTERNAL BUNDLE. THE CLOSURE: the density-onset
principle at its second level of structure gives x = sqrt(f_c) EXACTLY -- Lambda =
(1 - sqrt(f_c))^2/12, the conditioning strength as a function of ONE pure geometric number,
with the window inverting to f_c in [0.073, 0.348].

## The f_c determination (FND-MATTER-038)
The decisive computation: a direct percolation simulation of the transparency-loss principle in
the framework's own geometry (Poisson disks; a test rope passes iff its center finds a channel;
the number-density formulation makes every convention drop out). Measured threshold, grid-
corrected against the literature filling 1.128: Lambda = 0.013-0.017, ZERO free parameters,
against the window [0.014, 0.044]. Class verdict: PERCOLATION -- the window lives. Quantitative
verdict: THE EDGE LANDING -- bracketing the lambda-session doublet condition at one seat basis,
undershooting at another; too close for dismissal, too edged for celebration, squarely a
seat-quality question. The conditioning sector is PARAMETER-FREE: m = T D (L - Lambda ell),
Lambda computed, T D the model's single input.

## The documentation pass
papers/rope_matter_mass gains four sections (the instrumentation epic; the zero, the library,
and the corrected table; the gate saga; the state of the sector) -- validated and re-rendered.
papers/falsifiable_predictions' closing rewritten to the current truth (kc dissolved, lambda
computed, T D alone). docs/INSTRUMENTS.md adds nine instruments and catches 23-28 -- including
the new catch 28 (assert mechanisms, not boundary-straddlers, paid for three times in one
weekend) and the incompressibility constraint as a permanent principle. docs/PAPERS.md refreshed
to the full 006..038 arc.

## What stands, said plainly
The full chain, in one breath: lambda was hbar-dead by the framework's own Lorentz bound, reborn
classically as a jitter fraction, geometrized by collision saturation, routed to the internal
bundle by the ontology's eliminations, closed by the density-onset principle, and computed by
throwing random disks and asking when a rope can no longer pass. Nothing in the simulation knew
about neutrons or ridgerunner tables. What remains hypothesis-shaped is marked on every claim it
touches: doublet = n/p; nucleon = 27.75-D tangle; the medium above the saturation crossover;
reading C itself. What remains open: T D (the sole scale input); the 3D-wiggle correction; the
constituent-count budget; the production-grade solver. No knot is identified with any particle.
