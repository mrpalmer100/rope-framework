# Release v3.6.0 — The case closes: both chains innocent, the courtroom guilty (3 August 2026)

**462 registered claims (104 Derived, 321 Modeled, 4 EFT-constrained, 4 Conjecture,
5 Open, 24 Failed-and-kept); 444 code-backed, all passing.**

Three claims (GRV-089..091) execute and close the coefficient campaign that
v3.5.0 commissioned — the four-order disagreement between the whisper's two
derivations, adjudicated entirely in-house.

## The campaign

- **GRV-089 — beta's parameter map.** The bit-cost is a UNIVERSAL one-variable
  function of dimensionless drive (exact collapse across thresholds, 0.0%
  spread at every one of eight drives), U-SHAPED with a marginal plateau at
  35.2 and a minimum of 15.7 near r_d = 4 — GRV-082's three-digit 15.67 was
  the right function read, by engine convenience, at its minimum. The
  accretion shell's own operating point measured at r_d* = 1.08 (marginal
  capture, as the blueshift grammar implies): beta_phys = 35.4, the tension
  narrowing 3.8 -> 3.5 orders. Suspect (i): PROMOTED, not the culprit.
- **GRV-090 — the pile-up eliminated by exact cancellation.** The ratio is
  f* beta_phys (h/sigma) in closed form — every lattice factor cancels — and
  evaluates below 8e-16 in every channel; the vacuum outweighs a neutron star
  by fourteen orders, so nothing that falls in can raise the cell energy above
  ambient. P-e vindicated to 1e-14; the UPWARD direction closes; the case
  leans toward the one link never tested against a mechanism.
- **GRV-091 — the verdict.** Campbell's theorem by machine, numeric at rates
  33x apart with the pitch immobile: an IMPULSIVE train rings the throat at
  its resonance regardless of rate, and the mechanism's emission was always
  discrete ledger reversals — 35-barrier snaps that pay no Boltzmann toll at
  the ring frequency. GRV-088's confrontation is CONVICTED OF A CATEGORY
  ERROR: a temperature coefficient set against a ringing-frequency
  coefficient. GRV-040's pitch VINDICATED (0.23 kappa, geometric); GRV-087's
  temperature VINDICATED (the rate law, shell-uniform by the cancellation
  theorem). The black hole is a crackling-noise-class emitter: cold
  thermodynamics, bright snaps.

**Final score: one suspect promoted with its operating point measured, one
eliminated by exact cancellation, one vindicated by theorem, and the
confrontation itself found to be the flawed link — inside its own
pre-committed verdict grammar. A theory subpoenaed itself; the indictment was
ruled defective; that is still justice.**

The surviving residue is named: THE QUANTUM OF THE RING — whether each snap's
e_bit matches hbar_med omega_ring — which is the corpus's L1 frontier arrived
at the horizon's doorstep.

## Document sweep (this release)

The verdict is propagated: the predictions paper (Prediction 17 standing,
Prediction 20's verdict recorded), the black-hole paper (Section 13
postscript), the plain-language guide (the woodland-of-snapping-twigs
closing), SURPRISES, the state document, and the figures README (the posters'
"in court" annotations carry the load-bearing closed-case note pending
re-render).

## Verification

`tools/verify_corpus.py --quick` passes; per-claim benchmarks pass; bars for
every session locked before computation in `analysis/`.
