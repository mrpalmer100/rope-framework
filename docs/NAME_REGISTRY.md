# NAME REGISTRY -- canonical symbols and the collisions they retire

Established 2026-08-13 (v3.24.5, the T4 ride-along session),
discharging the kappa/w/g rename sweep queued since PRED-003-ETA.
PURPOSE: prevent the next FND-066-class collision (a claim reading
"the SAME unregistered width w" when two registered objects wore the
name). CONVENTION: registered claim text is HISTORY and is NOT
rewritten by this sweep; the registry governs all NEW text (claims,
bars, results, docs, papers). When an older claim's bare symbol is
quoted, the quoting text must attach the canonical name in brackets.

## THE KAPPA FAMILY (four live objects, one label)

- kappa_lock -- the LOCKING MODULUS [J/m^2] (the elastic object of
  the locking sector). Bare "kappa" in locking-sector claims means
  this.
- kappa_grav -- the gravity sector's SURFACE GRAVITY [1/s or m/s^2
  per convention of the quoting claim]. Never write bare kappa in a
  gravity-sector context again.
- kappa_C -- ELEC-021's COULOMB COEFFICIENT [J m]. The electric
  sector's object.
- kappa_pack -- the PACKING/FLOOR PARAMETER of the bundle hierarchy
  (dimensionless; the kappa50 / kappa250 floors are its two
  registered values and keep their compact labels). This is the most
  used variant (80+ appearances) and is already unambiguous; no
  change.
- Derived/local variants in registered text (kappa_0, kappa_rel,
  kappa_c, kappa_fund, kappa_tw, ...) belong to their defining claims
  and are read there; none collides with the four above.

## THE g PAIR (two live objects, one letter)

- g_contrast -- the CROSSING CONTRAST, FIXED g = [0.395, 0.460] by
  GRANT-THRESHOLD (FND-080). When a claim says "the grant's g" this
  is it.
- g_C4 -- the MESOSCOPIC RATIO (~83-108, unexplained, frozen per the
  charter). Never bare-g this object.
- (g = 2 shell weighting of SHIN6 is an instrument calibration
  constant local to that commission's engine; quote it only as "the
  SHIN6 shell weighting".)

## THE w PAIR (two live objects, one letter -- the FND-066 collision)

- w_mesh -- the VACUUM-MESH STRAND width (EM-RECON-018 lineage;
  w_mesh/a = 0.6272 under the corrected areal convention,
  EM-RECON-030). ALWAYS quote with its a (FND-066's standing rule).
- w_const -- the FLUX-TUBE CONSTITUENT width (QGATE-004 demanded,
  ELEC-050 measured from the lattice band). Distinct object; identity
  with w_mesh is the one-medium question, CONVICTED (OM-S), so the
  two may never be silently identified.

## THE n FAMILY (for completeness; no collision, but adjacent)

- n_b -- strands per tube CONSTITUENT (bundle count). STATUS AFTER
  FND-096: OPEN; neither [63, 73] (withdrawn) nor [14, 21]
  (displayed) may be quoted as standing.
- n_t -- strands (constituents) per FLUX TUBE (the tube census,
  [47, 198], NUCQ-003 lineage).
- m_b / m_eff -- ENGAGED-PAIR contact multiplicity, PAIR CURRENCY per
  FND-094; the measured currency correction is r_inf = m_eff/n =
  3.6-4.9 (FND-095).

## TRIPWIRE (new, enforced from v3.24.5)

New files and claims use canonical names. A bare kappa, g, or w
outside its defining claim's local context is a review flag. The
quoting-with-brackets convention applies to all citations of
pre-v3.24.5 text.

---

# APPENDIX -- THE NUCQ-002 COINCIDENCE: STATUS AUDIT (same session)

The desk item read: "the two T0 routes agreed at the Lorentz-bound a
because one-medium was implicitly kappa = 1 there; post-conviction
the agreement is unexplained; any resolution routes through n_b."

AUDIT VERDICT: THE ITEM'S FRAMING IS STALE. The coincidence no longer
exists in the form the desk item preserves:

1. ELEC-050/ELEC-052 (2026-07-31) already dissolved the celebrated
   agreement (1692 vs 1700 N) into a MEASURED 28 PERCENT TENSION:
   the modern lattice mass-density radius 0.407(14) fm gives
   n_t = 156 and T0 = 1201 J/m against the Sigma-route 1700, with
   the strand-count-follows-energy-density identification registered
   as the load-bearing assumption at the scale-chain fork. An
   explained disagreement replaced an unexplained agreement; the
   "coincidence" is a quantified tension with a named fork.
2. The resolution route through the bundle count is now GOVERNED BY
   FND-096: n_b is held open pending non-engine data. The same
   external data that adjudicates Chain A (lattice constituent
   substructure, the penetration-barrier spectrum, any independent
   determination) is what this item waits on. No session can advance
   it today without violating the holding.

DISPOSITION: the desk item is RETIRED AS STALE and REPLACED by its
current form: "the T0 fork (ELEC-052's 28 percent tension; the
energy-density identification load-bearing) -- waits on the same
non-engine data as Chain A (FND-096)." Nothing computable is owed;
the tripwires against quoting the old two-route agreement as standing
carry (they have since ELEC-050).

## ADDENDUM (2026-08-13, v3.24.10): k/T0 and the c4 family

- k/T0 = 2 -- canonically the COARSE-EFFECTIVE medium stretch ratio
  (derived by exclusion, EM-RECON-032). The fine-level ratio is
  k_f/T0_f = 8.09 (stiff-helix lower bound 4.05, falsifier armed).
- c4 (lowercase) -- the QUARTIC EXTENSIBILITY COEFFICIENT,
  c4 = (k - T0)/8 = T0/8 at the coarse-effective reading. DISTINCT
  from C4 (SCALE-001's frozen class) and g_C4 (the mesoscopic
  ratio); the three-way collision flagged in the 2026-08-13 board
  scan is hereby disarmed. Bare "c4/C4" outside a defining claim is
  a review flag.

## ADDENDUM (2026-08-13, v3.25.10): THE HIERARCHY COUNTS (SHIN8 ruling, EM-RECON-033)

Two bundle counts at DIFFERENT hierarchy levels were at risk of one
symbol; ruled DISTINCT:

- n_rs -- ROPE STRAND COUNT: how many VACUUM STRANDS compose a
  matter rope (FND-MATTER / NUCQ sector usage). Level: rope of
  strands.
- n_sub -- SUB-STRAND COUNT: how many SUB-STRANDS compose a vacuum
  strand (FND-087, GRANT-SUBSTRUCTURE-TIGHT; underived). Level:
  strand of sub-strands. Unchanged.

New text uses these canonical names; old text quoting a bare strand
count attaches the canonical name in brackets per the standing
tripwire. A formula's scale class is henceforth readable from its
symbol.

## ADDENDUM (2026-08-14, v3.26.4): THE NUN COLLISION (commission names)

Two commissions carry the name NUN, registered five days apart in
different sectors: the GRV matter-to-rotation provenance audit
(GRV-103) and the FND k-string continuum confrontation (FND-101,
with descendants NUN2..NUN6). The collision arose from a truncated
uniqueness check and is retired by qualification, not rename
(registered claim text is never edited retroactively): canonical
handles are **NUN-GRV** (GRV-103) and **NUN-FND** (FND-101 arc). New
text uses the qualified handles; old text quoting a bare "Commission
NUN" attaches the qualifier in brackets per the standing convention.
Process fix: commission-name uniqueness checks must grep the FULL
sorted list, not its tail.

- COMMISSION NUN-GRV2 (2026-08-14): the beta_J pin audit, continuation of the NUN-GRV gravitomagnetic arc. Uniqueness checked against the full-registry commission-name sweep.
- COMMISSION NUN-GRV3 (2026-08-14): the shift-map extension audit, the sector's gating item per GRV-105. Uniqueness checked against the full-registry sweep.
- COMMISSION NUN-GRV4 (2026-08-14): the FND-REL-002 scope audit, the author's challenge to GRV-106's upstream premise, motivation disclosed in the bars. Uniqueness checked.
- COMMISSION NUN-GRV5 (2026-08-14): the shift-structure candidate inventory (closed C1-C6). Product: GRANT-CANDIDATE-SHIFT-C3. Uniqueness checked.
- COMMISSION NUN-GRV6 (2026-08-14): the lambda-bound audit (C3 vs registered EM optics), pre-grant pricing. Uniqueness checked.

## ADDENDUM (2026-08-15, v3.26.20): THE CHROMO-PROFILE OBJECT (NUN7 adoption, FND-111)

- CHROMO-PROFILE-CLEM -- the EXTERNAL-ADOPTED SU(3) flux-tube
  transverse profile: E_l(x_t) = (phi/2pi)(mu^2/alpha)
  K0[(mu^2 x_t^2 + alpha^2)^(1/2)]/K1[alpha], with quoted parameters
  mu/sqrt(sigma) = 2.684(97) and kappa_GL = 0.178(21)
  (Cea-Cosmai-Cuteri-Papa, PRD 89, 094505 (2014)). This is the
  FLUX-TUBE-PROFILE object, cross-sector named per FND-066: distinct
  from the strand diameter w (weave geometry), distinct from
  ELEC-050's flux-tube WIDTH number (which it must remain consistent
  with, gate exercised at FND-111), and NOT identified with any
  strand-level object. Its lengths hang on the lattice sqrt(sigma)
  convention, not on the M-point ladder (FND-110).
- kappa_GL -- the Ginzburg-Landau parameter of the adopted profile.
  Named with its suffix to keep it distinct from the corpus's kappa
  (locking strength) and kappa_pack: three different objects, three
  names, per the standing one-symbol-one-object rule.


## Addendum (2026-08-16, v3.26.35, EM-RECON-040 -- QOF2)

- **tau0_lock** (twist rate, rad/length; EM-RECON-012 lineage): the
  equilibrium twist of the wound conduction strand. Pinned
  dimensionlessly: r tau0_lock = 1/sqrt(gamma - 1), bracket
  [1/sqrt(3), 1] on the registered gamma ~2-4.
- **tau0_trav** (duration; ELEC-047 lineage, RETIRED hbar sector):
  the causal traversal time, c tau0_trav = 1.95 w. A DIFFERENT
  QUANTITY. Any numeric import of tau0_trav into lock-sector
  formulas is a registered failure mode (misassigned ontology
  class, FND-MATTER-047 pattern).


## Addendum (2026-08-16, v3.26.59, FND-115 -- SHIN8)

- **f_{c,f}** (fine volume coverage): the volume fraction of a coarse
  cell occupied by the n_sub wound sub-strands. Bracketed
  <= [2.15%, 6.08%] at the registered ceilings; scales as a_f^2.
  Distinct from any EM crossing/contact fraction.
