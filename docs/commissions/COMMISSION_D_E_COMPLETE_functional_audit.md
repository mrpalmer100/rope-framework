# COMMISSION D-E-COMPLETE: IS THE W ENERGY FUNCTIONAL THE WHOLE FUNCTIONAL?
# (chartered 2026-08-06, NAMED-NOT-OPENED for a later deliberate go-decision.
# A candidate source for the +178.8 ppm alpha residual that SURVIVES Z's A2
# interlock objection -- because it is functional COMPLETENESS, not reweighting.
# The bars are written to run BACKWARD from structure, never toward the target,
# because this is exactly the terrain where the -15 ppm artifact was born.)

## THE OBJECTION THIS COMMISSION MUST RESPECT (Z's A2, registered, verified)
Under F7's all-quadratic reading, the dressing enters R* through mu -- a MASS,
an energy-type quantity -- so the mass dressing IS the energy dressing, which is
why the blind ENERGY D_E was identified from inside the chain as the right
object. The interlock is explicit and load-bearing: Brick 4's quadratic action,
Z's quadratic coupling, and D_E's energy character are one structure. A
charge-weighted (linear-functional) REWEIGHTING of D_E -- the MOMENT-REFINE move
-- would break that interlock and is FORBIDDEN here: it is the correct
refinement for the moment observable, NOT for mu. This commission does NOT
reweight D_E. Any drift toward a linear/charge functional for the mu-slot is
out of scope and violates the registered reading.

## THE CHANNEL THAT SURVIVES (functional completeness, not reweighting)
W's solver computes the ENERGY of the committed profile under the committed
elastic-plus-rotational functional:
    D_E = (E_el - LOG + E_rot)/E_rot,
    E_el = int [sqrt(1+g^2)-1 + (k/2)(...)^2] 2 pi r dr   (elastic, quadratic)
    LOG  = int (f^2/2r^2) 2 pi r dr                        (log subtraction)
    E_rot = (1/2) Omega J_T                                (rotational)
But R's electron is a TETHERED TERMINUS -- the endpoint of the screw Goldstone's
twist line, tethered by strands, with spin = the tether Z_2 (R Phase 1,
registered). The terminated winding's OWN coupling energy at the core -- the
energy the tether/terminus structure carries -- is physics the W functional does
not contain. IF the mode carries energy in a REGISTERED channel the committed
functional omits, the true energy dressing differs from 1.1051029 for a PHYSICAL
reason, not a dial. This is an ENERGY-type correction to an ENERGY functional --
it stays inside the quadratic interlock, so A2 does not forbid it. It is the
"the number was slightly incomplete, not the framework" hypothesis.
NOTE (why the existing theorems do not close this): r_min stability and the
scale-invariance theorem (Z Brick 5) both VARIED the existing functional; they
never asked whether it is the WHOLE functional. Completeness is an untested axis.

## THE BARS -- BACKWARD FROM STRUCTURE, NEVER TOWARD THE TARGET
This is the -15 ppm terrain. The bars run the derivation the ONLY safe way:
- PHASE A (STRUCTURAL AUDIT, alpha and 179 ppm OUT OF THE ROOM): audit the
  REGISTERED electron structure (R Phase 1's tethered terminus, S, the tether
  Z_2, the winding core) against the W functional's terms. Determine from
  MECHANICS ALONE: (1) is there an energy channel the committed functional
  omits? (2) what is its FORM (the integrand, from the registered structure)?
  (3) what SIGN must it carry (does including a previously-omitted positive-
  definite energy raise or lower D_E)? The target value 1.10491 and the shift
  168 ppm MUST NOT appear anywhere in Phase A. The audit either identifies an
  omitted channel with a derived form and sign, or concludes the functional is
  complete. BOTH are registrable Phase-A outcomes.
- PHASE B (SEALED COMPUTE): if Phase A identifies an omitted channel, compute
  its contribution to the energy on the committed profile BLIND -- the corrected
  D_E' is an OUTPUT. No comparison to 1.10491, to alpha, or to 179 ppm during
  the compute. The sign predicted in Phase A is committed before the magnitude
  is known.
- PHASE C (CONFRONTATION, LAST): only after D_E' is computed and its sign
  matches or contradicts the Phase-A prediction, form 4 pi^3 D_E' and confront
  1/alpha. Grade against the pre-committed outcomes below.
- PRE-COMMITTED WILLINGNESS (stated before Phase A): the answer may be "the
  functional is COMPLETE" (no omitted channel -- the residual is not here), or
  the correction may land with the WRONG SIGN and WORSEN the residual toward
  ~350 ppm. The wrong-sign outcome is NOT a failure to bury -- it is a real
  result about where the wall sits, and it is registered at full volume.
- NO TARGET-HUNTING: the commission is FORBIDDEN from "looking for 168 ppm of
  missing energy." A channel is admitted ONLY if the structural audit forces it
  from registered mechanics with alpha out of the room. If Phase A finds no
  forced channel, the commission STOPS at "functional complete" -- it does not
  then relax the audit to manufacture one.

## Registrable outcomes (all acceptable, all terminal)
1. RESIDUAL EXPLAINED (the summit): Phase A identifies a forced omitted energy
   channel with a derived form and sign; Phase B's blind D_E' shifts the chain
   to close the +178.8 ppm (correct sign, right magnitude within grade); a
   SECOND PREDICTION (the same channel's effect on g=2 or 13.6 eV, or the
   mass-ratio analog) confirms it. The residual is derived; alpha follows. The
   W functional was incomplete for a named physical reason.
2. LEAD (channel found, magnitude close, second prediction pending): registered
   with its derived form/sign and the next test named. Not graduated, not dead.
3. FUNCTIONAL COMPLETE (no forced omitted channel): Phase A concludes from
   structure that the W functional represents all registered energy channels of
   the tethered terminus. The residual is NOT a completeness gap -- it points
   harder toward the radiative wall. Registered; STOP.
4. WRONG SIGN / WORSENS (a forced channel exists but its correction has the
   wrong sign or overshoots): the residual is not this channel, and the miss
   LOCATES the wall more precisely (the classical energy content is now bounded
   from the other side). A real result about where the classical chain ends.
   Registered at full volume; STOP.

## WHAT THIS PROTECTS
The standing result is untouched regardless of outcome: 1/alpha = 4 pi^3 D_E =
137.060504, every factor derived, three independent observables consistent,
g=2 a free byproduct. This commission asks ONE structural question -- is W's
energy functional complete for the tethered terminus? -- and any of the four
outcomes leaves the summit intact while either deriving the residual, closing a
candidate, or locating the wall. It does NOT hold the standing result hostage.

## WHY THIS IS BETTER-MOTIVATED THAN V-A WAS
V-A tested a boundary term whose coefficient the committed profile made tiny --
a defensible but somewhat arbitrary realization of "the O(Omega) term." This
commission names a CONCRETE REGISTERED STRUCTURE (R's tethered terminus, the
winding core, the tether Z_2) that the committed W functional DEMONSTRABLY does
not represent -- E_el + E_rot - LOG contains no terminus-core coupling term. The
question "does that omitted structure carry energy" is sharper and more
physically grounded than the boundary-term guess. It is the strongest surviving
classical candidate for the residual.

## Depends on
Commission W (the committed functional and D_E = 1.1051029 -- the object whose
completeness is audited), Commission R Phase 1 (the tethered terminus, the
winding core, the structure the audit compares against), Commission S and the
tether Z_2 (registered spin/terminus structure), Commission Z A2 (the interlock
this commission must respect -- energy-type only, no reweighting) and Z Brick 5
(the scale-invariance theorem that varied the functional but not its
completeness). MOMENT-REFINE is the DISTINCT charge-reweighting refinement for
the moment observable -- explicitly NOT this commission.

## NOTE FOR THE FUTURE SELF WHO OPENS THIS
Run Phase A with alpha and 179 ppm genuinely out of the room -- if you catch
yourself reasoning "what channel would give 168 ppm," STOP and reset, that is
the -15 ppm failure mode. The audit must force a channel from the terminus
structure alone, predict its sign before its magnitude, and confront only at the
end. The most likely honest outcome is 3 (complete) or 4 (wrong sign), and both
are worth having -- outcome 3 hands the residual to the wall, outcome 4 bounds
the wall from the other side. Outcome 1 is possible but must clear a second
prediction before it is called a derivation of alpha. One audit, one sealed
compute, one confrontation. Accept what it says.

## ---- OUTCOME (2026-08-06): PHASE 1 AUDIT CLEAN, outcome 3 (functional complete) ----
Phase A structural audit returned CLEAN on all four items, with alpha and 179 ppm
held out of the room throughout. A1: the tether set is the deformation field
itself (not a separate channel). A2 (flagged most likely OMITTED): the LOG
subtraction's scope is EXHIBITED -- it removes exactly the linearized winding
self-energy (charge-sector divergence), and the kept inner energy vanishes with
r_min on the actual solutions (matches Z Brick 3's continuum check). [The A2
continuum isolation is verified in de_complete_phase1_audit.py; independent
in-package re-run pending that script's upload -- a crude proxy window did not
reproduce it, the correct r_min-adjacent-shell isolation is script-internal.]
A3: the Z_2 class is a label, not an energy channel. A4: J's psi=0 exactness
covers the terminus's driven rotation. VERDICT: the committed W functional is
the whole functional at current registration; D_E=1.1051029 stands; Phase 2 had
nothing sealed to compute; the "number slightly incomplete" hypothesis is closed.
Phase 3 (exploration): the residual's only surviving mechanically-motivated lead
is the RADIATIVE class (stamped LEAD-RAD) -- back-reaction the static functional
cannot contain, with its second prediction pre-installed (alpha residual + g=2
gap from one mechanism). Standing result untouched. Script:
explorations/de_complete_phase1_audit.py.
