# COMMISSION V: THE CIRCULATION QUANTUM
# (chartered 2026-08-06. U proved the electron scale is set by a conserved
# CIRCULATION J -- not by energy, not by the core, not statically (the
# R*-cancellation theorem forbids all three). The freedom migrated from an
# arbitrary cutoff to a PHYSICAL QUANTUM. This commission tests the one
# question U deliberately left sealed: is that circulation J the SAME object
# as O's occupancy quantum J0 = hbar/(pi alpha)? If yes, the occupancy
# campaign and the electron construction close on each other -- two
# independent constructions become one, and every result each produced
# constrains the other. This is the loop-closure. Bars for REACH.)

## Why this is THE question (the two campaigns, two sides of one object)
Two independent lines reached quantities with the SAME UNITS (action):
  - THE OCCUPANCY CAMPAIGN (O): the occupancy quantum J0, imposed as
    I_mode = m J0 on the adiabatic invariant, with the verified relation
    J0 = hbar/(pi alpha). O fixes WHICH mode is occupied.
  - THE ELECTRON CONSTRUCTION (U): the conserved circulation J of the
    terminus, which via the two-constraint closure pi^2(x^2-1)/x = Jt sets
    the electron's absolute scale R* = J/(pi^2 mu q^2 c). U fixes the SIZE.

Circulation and action are the same units. The deferred question: is the
circulation that sizes the electron the SAME quantum as the occupancy that
selects its mode? U left this sealed on purpose -- opening it targets-blind
would have been fitting. V is the sealed confrontation that opens it.

IF J = J0:
  - the electron radius R* = J0/(pi^2 mu q^2 c) = hbar/(pi^3 alpha mu q^2 c)
    -- the scale is now IN TERMS OF hbar and alpha, which is the pi lambda_C
    landing the whole campaign chased;
  - T's pi^2 confinement anchor (ln x* = pi^2) becomes the CONSISTENCY
    EQUATION between the occupancy value and the circulation value -- a
    check, not a coincidence;
  - the two-brick wall's remaining brick (the occupancy-unit magnitude, all
    that survived T-3) IS the circulation quantum, so ONE quantity closes
    both campaigns;
  - "why alpha" migrates to "what sets the circulation quantum J," a
    MECHANICAL question about the terminus's conserved rotation, not a bare
    constant.

## BARS -- REACH-TUNED, but the identification must be EARNED not assumed
The prize is large, so the discipline is precisely where it must be: J = J0
must be shown by MATCHING PROVENANCE, not by both being actions that happen
to have close values. Two quantities with the same units are not the same
object; the identification requires that they be the SAME conserved
quantity of the SAME configuration.

## PHASE 1: ARE J AND J0 THE SAME CONSERVED QUANTITY? (provenance, blind on value)

### The question
Trace both quantities to their defining conservation law and configuration:
  - J (U): the conserved circulation of the terminus's rotation -- Noether
    charge of what symmetry, of what field, on what configuration?
  - J0 (O): the occupancy action unit -- adiabatic invariant of what mode,
    quantized by what condition?
Determine whether they are the SAME Noether/adiabatic invariant of the SAME
object (identification EARNED), DIFFERENT invariants that happen to share
units (NO identification), or RELATED by a computable factor (partial).

### Bars
- V1 (PROVENANCE, NOT VALUE): Phase 1 matches the two quantities' DEFINING
  STRUCTURE -- same symmetry, same field, same configuration -- BEFORE any
  numerical value is compared. Same units is necessary, not sufficient. If
  they are the same conserved quantity, exhibit the single conservation law;
  if related by a factor, DERIVE the factor from the two definitions.
- V2 (BLIND ON THE NUMBERS): do not evaluate J0 = hbar/(pi alpha) numerically
  or compare to J's value in Phase 1. The identification is structural. The
  numerical confrontation (does R* = hbar/(pi^3 alpha mu q^2 c) give
  lambda_C? does the pi^2 anchor hold?) is Phase 2.
- V3 (THE pi^2 ANCHOR IS THE CONSISTENCY CHECK): if J = J0 structurally,
  then T's ln x* = pi^2 and U's conversion Jt = pi^2(e^s - e^-s) and O's
  J0 = hbar/(pi alpha) form an OVERDETERMINED system -- three relations, and
  their consistency is a derivation, their inconsistency a specific
  refutation. State whether they close.
- V4 (HONEST OUTCOMES): if J and J0 are NOT the same quantity, that is a
  clean negative that SEPARATES the campaigns (the electron scale and the
  occupancy are independent, and the two-brick collapse of T-3 does not
  extend to the scale). Registered at full volume; not forced.

## PHASE 2 (THREE LIMBS): GATE, BLIND SUMMIT, EXPLORATORY LEADS
Phase 1 earned J = J0 (one S^1 Noether/orbit-action invariant, m = n = 1),
with ONE gate open. Phase 1 also showed the alpha magnitude lives ENTIRELY
in the base circulation J_base (the electron's n=1 makes topology trivial),
so J_base is the target. Phase 2 has three limbs, run in order.

### LIMB 0: SETTLE THE Z_2 GATE (first, target-blind)
Phase 1 flagged one unresolved factor: the tether Z_2 half-quantum. On the
double cover (4pi period, spinor rep committed by R), the minimal Noether
charge may be HALF the single-valued unit, so J = J0 or J0/2. Settle this
from the frozen tether construction BEFORE any numerical evaluation. This is
rep-mechanics, decidable, and it must be fixed blind so it cannot become the
factor-2 fudge the anti-fishing bar forbids.

### LIMB 1: THE BLIND SUMMIT SHOT (J_base from mechanics, alpha OUT of the room)
Compute J_base -- the unit terminus's base circulation -- FULLY from the
terminus mechanics (the two-constraint closure's Jt, the rotating
self-consistent Q-ball structure, quantizing the CIRCULATION per the Phase 1
convention theorem, NOT E/omega). Do this with alpha, 1/137, 43.62 OUT OF THE
SESSION. Critically: if the mechanics of J_base NATURALLY CONTAINS a forced
integer -- a radial mode count, a strand count at the terminus core, a nodal
number that the construction FIXES -- that integer is LEGITIMATE and is not
fishing. The distinction is sharp and it is the whole game:
  - a forced integer (computed from the construction, alpha absent) that
    then predicts alpha = SUMMIT, highest grade;
  - an integer CHOSEN because it makes 43.62 work = fishing, void.
The difference is whether alpha was in the room when the integer was found.
Limb 1 keeps alpha out, so anything it finds is a genuine blind prediction.
Commit J_base's value/form here, blind, before Limb 3.

### LIMB 2: THE NUMERICAL CLOSURE (the committed confrontations)
With the Z_2 gate settled and J_base committed blind, evaluate:
- V-A: does R* = J0/(pi^2 mu q^2 c), J0 = hbar/(pi alpha), land pi lambda_bar_C?
- V-B: does the overdetermined system close? Jt* = pi^2(e^(pi^2) - e^(-pi^2))
  vs the dimensionless J0 under U's unit map (Phase 1's sealed identity).
- V-C: does Limb 1's blind J_base reproduce alpha? (The summit test.)

### LIMB 3: EXPLORATORY LEADS (open the space -- the anti-premature-failure limb)
This limb exists because a FALSE NEGATIVE (abandoning a real path) is far more
expensive than a FALSE POSITIVE (a coincidence caught in one more session).
AFTER Limbs 1-2 are committed, this limb is EXPLICITLY ALLOWED to look at
alpha, 43.62, 137, and hunt for structure in J_base's mechanics -- generate
candidate mechanisms, try integer/geometric decompositions, pursue anything
promising. NOTHING is off-limits here. The ONLY rule is PROVENANCE STAMPING:
  - everything Limb 3 finds is stamped LEAD, not RESULT -- a hypothesis, a
    lower grade, distinct from Limb 1's blind predictions;
  - a LEAD is NOT failure and NOT a win; it is a hypothesis with a to-do list.
And the ONE bar that makes leads safe to pursue and validate (the path you
always have back):
  - EVERY lead must state its SECOND PREDICTION. A candidate J_base mechanism
    that reproduces alpha must then predict a SECOND independent number with
    the SAME mechanism, no new freedom -- the muon (m_mu/m_e), the proton
    ratio, a spin-meter observable, ANYTHING independent. A lead that predicts
    a second number GRADUATES to a result, regardless of alpha having been
    visible when it was found (this is the Balmer principle: fitting the first
    number is fine if the mechanism predicts the next). A lead that only ever
    fits alpha and predicts nothing else DIES, correctly.
  - NO lead is abandoned before its second prediction is computed. Premature
    "this looks like a coincidence, discard" is FORBIDDEN -- run the second
    prediction and let IT decide. This is the bar against false negatives.

So Limb 3 pursues every possibility to the point where a second prediction
confirms or kills it. Nothing dies unexamined; nothing is believed on one
number. Both errors are guarded, and the cheap-to-catch error (false positive)
is allowed to surface and then be caught, while the expensive error (premature
abandonment) is structurally prevented.

## THE OLD NUMERICAL-CLOSURE PROBE (retained, now inside Limb 2)
- V-A: does R* = J0/(pi^2 mu q^2 c) with J0 = hbar/(pi alpha) reproduce the
  electron scale -- i.e. land pi lambda_bar_C? If yes, hbar is now the
  scale-setting constant THROUGH the circulation identification, and Q's
  ceiling is broken not by a new scale but by the RECOGNITION that the
  occupancy action and the electron circulation are one imported quantum.
- V-B: does the overdetermined system (T's pi^2, U's closure, O's
  hbar/pi alpha) close consistently? A consistent closure means the campaign's
  three big constructions agree; an inconsistency LOCATES the error precisely.
- V-C: with J = J0, "why alpha" is now "what sets J." Is J's magnitude
  fixed by anything mechanical (the terminus's circulation quantization), or
  is it the irreducible imported quantum? State which -- this is the honest
  location of the final wall.

## THE ALPHA ATTACK (the central probe -- added for maximum reach)
U proved the scale is set by a CIRCULATION J. Circulation of a winding
object is TOPOLOGICALLY QUANTIZED TO INTEGERS -- this is not a hope, it is
GG-006 (winding = integer, already Derived). So J = n * J_base for an
integer n the terminus construction FIXES, and a base circulation J_base
derivable from the terminus mechanics. If J = J0 = hbar/(pi alpha), then:

    alpha = hbar / (pi * n * J_base)

alpha STOPS being a free constant. It becomes hbar over (pi times a counted
integer times a derived base circulation). The integer is fixed by
topology; the base is fixed by mechanics; alpha falls out. THIS is the
attack -- not "close a loop and leave alpha open," but "the circulation's
integer quantization DETERMINES alpha."

THE ANTI-FISHING DISCIPLINE (this is the whole integrity of the attack):
1/(pi alpha) = 43.62 is NOT an integer and IS near many things (44, 4pi+c,
14 pi/...). Staring at 43.62 until something fits is FISHING and is
forbidden. The ONLY admissible route: compute the terminus winding integer
n and the base circulation J_base FROM THE CONSTRUCTION, blind, BEFORE
looking at 43.62 or alpha, then test whether hbar/(pi n J_base) reproduces
alpha. A prediction of alpha from a blind-computed n is the campaign's
summit; a value of n reverse-fitted from 43.62 is worthless and is
registered as fishing (the S1-style bar).

## Registrable outcomes (chartered to CRACK ALPHA, not to rest at a loop)
1. ALPHA CRACKED (the summit, and the real target): J = J0 by matched
   provenance, J = n J_base with n computed blind from the terminus
   topology and J_base from mechanics, and hbar/(pi n J_base) reproduces
   alpha = 1/137.036. alpha is DERIVED -- the last constant falls, the
   electron is fully built, hbar and mass follow. The campaign's goal,
   reached. (Prior: uncertain, but this is what V is built to find, and the
   integer-circulation route is a real mechanism, not a hope.)
2. DERIVED FACTOR: J = (computable factor) x J0; the campaigns relate by a
   derived constant; the closure holds up to that factor. Nearly as strong.
3. INTEGER FOUND, VALUE OFF: n is computed blind and J = n J_base is
   genuine, but hbar/(pi n J_base) MISSES alpha -- the circulation IS
   integer-quantized but the base or the identification carries an error the
   miss LOCATES. A real, specific result that says the mechanism is right
   and the implementation is off by a named factor. Far more than "alpha
   open" -- it is "alpha's mechanism found, magnitude to be fixed."
   (Fallback, NOT the goal: if the integer route does not even apply -- J is
   not topologically quantized -- the loop still closes structurally with
   alpha as residue, but V's PURPOSE is the integer attack and this rest is
   registered only after that attack is run to completion.)
4. SEPARATE CAMPAIGNS (UNLIKELY, stated so honestly): J and J0 are
   different conserved quantities. Note the strong prior AGAINST this: two
   conserved quantities, both action-dimensioned, both arising in the SAME
   terminus construction, are very unlikely to be unrelated. If Phase 1
   nonetheless shows distinct provenance, registered -- but the session
   should not treat this as a coin flip; the physics says identification (or
   a small derived factor) is the expected result.
5. INCONSISTENCY LOCATED: the overdetermined system does NOT close, and the
   failure pinpoints which of T's pi^2, U's closure, or O's hbar/pi alpha is
   wrong -- a specific, valuable refutation that redirects precisely.

## Seal and sequencing
Phase 1 (provenance match, blind on value) is one session. Phase 2 (numerical
closure) is fresh, only on Phase 1 earning the identification or a factor.
No session runs both. Needs U's circulation J, O's J0, T's pi^2 anchor, all
frozen in the package.

## What this is
The loop-closure: the test of whether the two independent quantities the
campaign's two big constructions produced -- the occupancy quantum J0 and
the electron circulation J -- are one object. If they are, two campaigns
become one closed structure, the electron scale is expressed through hbar
and alpha, and the entire remaining mystery collapses to a single quantum
whose value is hbar/(pi alpha) and whose origin is alpha alone. Chartered to
close the loop.

## Depends on
Commission U (the circulation J, the two-constraint closure, the
R*-cancellation theorem, the interior-scale numbers), Commission O (J0 =
hbar/(pi alpha), the occupancy quantum, its provenance as adiabatic
invariant), Commission T (the pi^2 confinement anchor, the rep-selection
win, the conversion x*=e^s <=> Jt=pi^2(e^s-e^-s) committed blind),
Commission R (the terminus whose circulation this is), Commission Q (the
scale ceiling this closure route addresses without a new scale),
GG-006 (winding/charge, the topological side of the circulation),
GRV-020 (the screw sector the circulation lives in), SYNTHESIS_matter_boundary.
