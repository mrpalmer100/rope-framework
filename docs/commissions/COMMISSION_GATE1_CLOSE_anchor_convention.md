# COMMISSION GATE1-CLOSE: THE ANCHOR CONVENTION FROM THE REGISTERED FREEZE
# (chartered 2026-08-06, Mark's go-decision, Option A. Gate 1 conditionally
# passed but could not close because O Phase 2's actual a0 calibration artifact
# is not in the package -- the session was deleted. The claim this commission
# tests: the recording convention (rectified-mean <|cos chi|> vs smooth-
# quadratic <cos^2 chi>) was NOT a free choice that lived only in the deleted
# artifact -- it is FORCED by the registered freeze I_mode = E/omega = m J0,
# which survives. If so, Gate 1 closes on surviving material; no session
# re-creation needed. If the convention (or its accompanying 1/2) cannot be
# pinned without the deleted artifact, that is registered and Option B -- a full
# calibration rebuild -- becomes necessary.)

## What survives (the registered facts this builds on -- verified in package)
From the O charter and SYNC_STATE (O Phase 1 + Phase 2 CLOSED):
1. THE FREEZE: I_mode = E/omega = m J0 -- Bohr-Sommerfeld quantization of the
   classical adiabatic invariant (ACTION) the medium already distinguishes.
   This is the E/omega object Gate 1's discriminator depends on. REGISTERED.
2. THE VALUE: J0 = hbar/(pi alpha), nine digits verified (O Phase 2 POSITIVE).
   REGISTERED.
3. THE CONFINEMENT: J0 = hbar iff the electron mode confines at R* = pi lambda_C
   (pi x reduced Compton wavelength), verified algebraic. REGISTERED.
4. THE WAVEFORM: the committed configuration is a two-component pure harmonic
   (Z's A1, verified: zero 4th-harmonic content in cos chi). REGISTERED.
5. THE OFFSET (the 1/2): O6b -- the zero-point offset m J0 vs (m+1/2) J0 is the
   node-structure / mode-boundary-condition quantity, chartered to be committed
   FROM MECHANICS (the same physics that gives the half-integer in a real
   cavity), not fitted. This is exactly Gate 1's flagged "1/2 origin" weakness.
   REGISTERED as a named seal point.
WHAT IS MISSING (the deleted line): which sampling convention the a0 calibration
used at the decisive step -- <|cos chi|> or <cos^2 chi>.

## THE HYPOTHESIS (sharp, falsifiable)
The convention is FORCED, not chosen: for the registered frozen invariant
I_mode = E/omega evaluated on the committed two-component harmonic, exactly ONE
sampling convention reproduces the verified J0 = hbar/(pi alpha). Reconstruct
the a0 calibration from the registered freeze and waveform, compute J0 under
BOTH conventions, and read which one lands.

## THE TEST (blind, pre-stated criterion -- fixed BEFORE the computation)
- Reconstruct I_mode = E/omega = m J0 on the committed two-component harmonic
  at the confinement R* = pi lambda_C (all registered).
- Compute the anchor circulation / a0 calibration TWO ways:
    (Q) smooth-quadratic recording: the cycle uses <cos^2 chi> = 1/2.
    (R) rectified-mean recording:   the cycle uses <|cos chi|> = 2/pi.
- Form J0 under each and compare to the registered J0 = hbar/(pi alpha):
    CRITERION (pre-stated, from Gate 1): recording (R) reproduces J0 on the
    nose; recording (Q) gives m_e c a0/4 (a factor pi/4 = kappa off), which
    does NOT reproduce J0. So:
      (R) reproduces J0  -> GATE 1 CLOSES, kappa = pi/4 DERIVED, 4 pi^3 stands.
      (Q) reproduces J0  -> F7 FALSIFIED, 4 pi^3 DIES, the -21% pi^4 wall returns.
      NEITHER / both      -> the convention is NOT forced by the freeze; the
                            deleted artifact is genuinely load-bearing; Option B
                            (full calibration rebuild) is necessary. Registered.

## THE 1/2 SUB-TARGET (the named weakness, addressed not assumed)
Gate 1 flagged that the 1/2 accompanying the rectified mean needs its origin
exhibited, not assumed. This commission MUST exhibit the 1/2 from O6b's
node-structure / mode-boundary-condition mechanics (the half-integer of a
ground-state cavity mode), the same physics the O charter chartered for the
offset -- BEFORE using it in the J0 reconstruction. If the 1/2 falls out of the
mode boundary condition on the committed waveform, the gate closes cleanly. If
the 1/2 must be inserted by hand, the closure is PARTIAL and the 1/2 remains an
imported piece -- registered honestly, not hidden.

## THE DISCIPLINE
- BLIND / PRE-STATED: the pass/fail criterion above is fixed before the
  computation runs. J0 = hbar/(pi alpha) is already verified and frozen; this
  commission computes an INDEPENDENT reconstruction and checks which convention
  reproduces it. No tuning toward the answer.
- NO NEW FREEDOM: everything (freeze, value, confinement, waveform, offset
  charter) is registered. This commission introduces NOTHING new -- it only
  determines which of two conventions is consistent with the frozen invariant.
  If it needs a new input, that itself is the finding (Option B required).
- THE HONEST NEGATIVE IS A REAL OUTCOME: "the convention is not forced by the
  freeze" is registrable and important -- it would mean Gate 1 genuinely cannot
  close without the deleted artifact, sharpening exactly what the rebuild must
  produce.

## SCOPE CAP -- HARD
- This commission is the CONVENTION RECONSTRUCTION and the 1/2 origin, singular.
- It does NOT re-open the five-bottleneck unification (O7 settled that: negative).
- It does NOT touch Gate 2 (charge functional) or Gate 3 (13.6 eV).
- One commission. If it closes Gate 1, the alpha prefactor 4 pi^3 becomes
  derived-pending-nothing; if it cannot, Option B is named for a fresh
  go-decision.

## READY TO RUN
All inputs are registered in the package (O charter, SYNC_STATE O Phase 1/2,
Z's A1 waveform result, the committed configuration via w_dressing_phase1c.py
and the R/T/U/V chain). The reconstruction is a bounded computation on committed
material -- no deleted session required unless the test's NEITHER/both branch
fires.

## Registrable outcomes
1. GATE 1 CLOSED: recording (R) reproduces J0, AND the 1/2 falls out of the
   node structure. kappa = pi/4 DERIVED from the registered freeze; the 4 pi^3
   prefactor is derived-pending-nothing; the alpha chain stands at 1/alpha =
   4 pi^3 D_E, +178.8 ppm, with the prefactor no longer imported. The deleted
   artifact is not needed -- its one line was forced by surviving structure.
2. GATE 1 CLOSED-PARTIAL: (R) reproduces J0 but the 1/2 must be assumed. kappa
   derived up to the offset; the 1/2 is the single remaining imported piece,
   named. Still a major tightening.
3. F7 FALSIFIED: recording (Q) reproduces J0. The 4 pi^3 framing DIES; the
   chain reverts and LEAD-2's pi^4 21% wall returns. Clean, decisive negative --
   the chain's most dangerous gate fails, honestly.
4. NOT FORCED (Option B required): neither convention cleanly reproduces J0 from
   the reconstruction, so the deleted artifact is genuinely load-bearing.
   Registered; the full calibration rebuild is chartered next.

## Depends on
Commission O (the freeze I_mode=E/omega=m J0, J0=hbar/pi alpha, R*=pi lambda_C,
the O6b offset charter -- all registered in SYNC_STATE / the O charter),
Commission Z (A1: the pure-harmonic waveform), the committed configuration
(w_dressing_phase1c.py, the R/T/U/V chain), Gate 1's pre-stated read criterion.
Names Option B (full a0 calibration rebuild) as the fallback if outcome 4.
