# COMMISSION LAMBDA — THE B-SECTOR RECONCILIATION
## Charter (2026-08-09). Bars locked at chartering, before any computation.

## The question
EM-RECON-026's Lorentz split produced B' = -rho kappa_0 zhat as the
defect-motion half of one Magnus formula, and EM-RECON-027 wrote the target
identity (mu_0-class 1/(eps0 c^2)). But three things stand unreconciled:
(1) THE ETHER-DRAG WORRY: a constant B' implies F = q v_d x B' on ANY charge
    moving through the still medium — an ether-drag force that observed free
    motion forbids. This must CANCEL from registered structure or the split
    dies here, registered at full volume.
(2) THE RADIATION B: the real magnetic field of a wave is |B| = |E|/c, not a
    constant — where is it in the derived structure?
(3) THE REGISTERED SECTOR: EM-009 (screw-sense circulation, type-and-sign)
    and EM-012 (current-current force, magnitude and sign, swinging-rope
    route) already exist. The new structure must LAND ON them, not beside
    them.

## Registered inputs (the ONLY allowed foundations)
- EM-RECON-025/026/027: the carrier, E = rho kappa_0 (v x zhat), and
  kappa_0^2 eps0 rho = 1.
- The carrier wave equation: the acoustic branch, speed c = sqrt(T0/mu)
  (EM-RECON-025, exact).
- Faraday structure: B as E's curl partner — the registered commitment
  (EM-RECON-019's trace: the split and the curl partner ARE committed).
- EM-009/012 as CONFRONTATION targets.
- Geometry: the circulation vector of a winding lies along the winding's OWN
  line direction (Gamma_vec = q kappa_0 khat, from the Magnus derivation).

## Pre-committed bars (LOCKED)
- L1 (provenance): only the inputs above; nothing imported from Maxwell that
  has not been derived or registered.
- L2 (the drag must cancel by STRUCTURE): the resolution of worry (1) must be
  a computed identity (not an argument): the self-force on a CLOSED winding
  from the constant-B' term must vanish by the geometry of closure, shown as
  a line integral. If it does not vanish, outcome 4 fires.
- L3 (the mu_0 identity must be FORCED): define B by Faraday from the derived
  E; the pair (E, B) must then satisfy the second curl equation with
  1/c^2 = eps0 mu_0 as a CONSEQUENCE of the carrier wave equation — not as a
  definition. |B| = |E|/c must emerge, not be imposed.
- L4 (landing on the registered sector): the Faraday-defined B's circulation
  around a current must match EM-009's screw-sense sign, and the
  current-current force implied by the established field equations must match
  EM-012's registered magnitude-and-sign result. Misses registered.
- L5 (scope honesty): whatever remains underived (e.g. full covariance) is
  named, not glossed.

## Outcome space (pre-enumerated)
1. All three reconciled: drag cancels by closure; mu_0 = 1/(eps0 c^2) forced;
   lands on EM-009/012. The EM arc closes with no remaining named debt.
2. Reconciled with a registered amendment to a prior claim.
3. Partial: an item survives as a named residual obligation.
4. The drag does not cancel: the Lorentz split is killed at full volume and
   EM-RECON-026 is amended accordingly.

BARS LOCKED. Computation begins only after this file is saved.
