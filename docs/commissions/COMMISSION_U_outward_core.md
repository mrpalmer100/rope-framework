# COMMISSION U: THE OUTWARD CORE
# (chartered 2026-08-06. NOT a hunt -- a CONSTRUCTION with a NAMED term.
# T proved all three walls -- absolute scale, alpha, rep selection -- reduce
# to ONE missing object: an outward-acting, non-marginal energy term for the
# terminus, and proved the coupled system generically acquires a fixed point
# the moment it exists (scale fixed, a0 admitted, alpha computable, rep
# selected -- all together). The corpus ALREADY BUILT that term, for a
# different reason: the EXTENSIBILITY QUARTIC CORE, c4 = (k-T0)/8, outward
# when k>T0, non-marginal (carries a scale). R and T never coupled it to the
# terminus because they worked in the pure screw/Goldstone sector, which is
# marginal by the masslessness theorem. This commission couples it. Bars set
# for REACH -- chartered to WIN.)

## The term T needs, already in the registry
T's specification: outward-acting (resists confinement, so the luminal
ceiling balances it at a real radius instead of running parallel) AND
non-marginal (p != 1, introduces a scale, not a log).

EM-RECON-009's quartic core is EXACTLY this:
  - c4 = (k - T0)/8, from finite strand extensibility (k = stretch modulus);
  - OUTWARD/repulsive iff k > T0 (registry: "a repulsive core exists iff
    k > T0"; pure tension k=0 is nonlinearly UNSTABLE, so k>T0 is required
    for matter to exist at all -- a necessity argument, not an insertion);
  - NON-MARGINAL: it is a quartic (g^4) term with a coefficient carrying a
    scale, not the scale-free log the screw sector gives;
  - already does this JOB elsewhere: it fixes chemical bond length, nuclear
    spacing, and the ferromagnetic magnitude -- three absolute scales from
    one coefficient. It is the corpus's registered scale-fixing term.

R and T found the terminus marginal because they built it from the screw
Goldstone sector ALONE (gapless, log, scale-free). The extensibility sector
was never coupled in. U couples it. This is the one construction that has
T's proven-sufficient ingredient and has never been run.

## THE k/T0 DISPUTE -- carried explicitly, and possibly ADJUDICATED
FND-021 registered an EIGHT-ORDER dispute in k/T0: EM-RECON-009 + the
chemical/nuclear spacings use k/T0 = 2; QB-008 (Bell timing) requires
k/T0 >= 1.9e8. Both registered, neither citing the other, everything
downstream inherits it. The electron scale U computes INHERITS this dispute,
so U computes on BOTH branches. The prize within the prize: if the electron
confinement scale is sensible on one branch and absurd on the other, U
ADJUDICATES the dispute FND-021 left open -- the electron becomes the object
that decides k/T0. Carry both branches; let the electron rule.

## BARS -- SET FOR REACH (same framing as T)
Generous, aimed at finding the positive:
  - the quartic core IS an admissible outward term; USE IT (it is registered,
    necessity-argued, and exactly T's specification).
  - a0 MAY emerge as a second scale of the coupled system; TRY IT (T proved
    the outward term admits a second orbital scale).
  - solve scale + rep + a0 as ONE coupled fixed point (T proved they fall
    together); COUPLE THEM.
  - a scale from the quartic core is NOT circular -- k is a MECHANICAL
    primitive (stretch modulus), not a re-spent calibration of m_e or hbar.
    Q's ceiling does not apply to a scale set by k. State provenance; do not
    over-apply Q.
  - carry BOTH k/T0 branches; a result absurd on one branch and sensible on
    the other is an ADJUDICATION, not a failure.

## THE PRE-REGISTERED TARGET T HANDED U (added 2026-08-06 after T executed)
T executed and won the rep brick (T-3), and in doing so handed U a SHARP,
CLEAN target that U must confront -- this is not a vague "find a scale," it
is a specific number to hit:

  THE pi^2 ANCHOR: T proved the pi lambda_bar_C landing (the condition that
  makes J0 = hbar) requires ln x* = pi^2 = 9.8696 EXACTLY (verified nine
  digits, x* = e^(pi^2) = 19333.7). This is startlingly clean -- a
  self-consistent condition landing on e^(pi^2) rather than a messy number
  is the framework saying the confinement transmutation exponent IS pi^2 and
  the profile producing it is right. The confinement side is SOLVED; what T
  lacked was the second condition to PIN x* there (its fixed point was a
  one-parameter family).

  THE TWO-EXPONENT TENSION (U's central question): T's alpha limb wants
  ln x* ~ 3.78-4.92, a factor ~2.27 below pi^2. A single x* cannot serve
  both. Either (a) U's outward core drives x* to e^(pi^2), making the pi
  lambda_C landing EXACT and RE-CONFRONTING the alpha exponent against pi^2
  instead of 4.3 -- which could close BOTH limbs at once; or (b) the scale
  exponent (pi^2) and the alpha exponent (~4.3) are genuinely TWO DIFFERENT
  transmutation ratios the construction conflated, and U's job is to
  identify the SECOND condition relating them -- which is exactly the
  condition that collapses T's one-parameter family and fixes the absolute
  scale.

  U's OUTWARD CORE IS THE CANDIDATE SECOND CONDITION. T's family was
  one-parameter because only ONE stationarity condition acted; the quartic
  core adds a SECOND (outward balance), which generically pins the free
  parameter. The pre-registered test: does the core condition land x* at
  e^(pi^2)? If yes, hbar is determined at the clean pi^2 value AND the alpha
  exponent is recomputed against pi^2. This is the sharpest, most specific
  target the campaign has ever handed a commission.

## PHASE 1: COUPLE THE CORE, SOLVE THE FIXED POINT (targets-blind)

### The construction
Rebuild T's self-consistent terminus WITH the extensibility quartic core
(EM-RECON-009) included in the energy functional:
  - the screw/Goldstone sector gives the marginal (log) self-energy and the
    winding/charge (as before);
  - the quartic core c4 = (k-T0)/8 adds the OUTWARD, non-marginal term
    (k > T0 required, per EM-RECON-009's necessity argument);
  - stationarity + luminal ceiling now balance an OUTWARD term against
    inward confinement -> the parallel-constraint sqrt(2) mismatch T found
    is BROKEN because the outward term changes the p of the balance.
  - solve the coupled fixed point on both k/T0 branches.

### The three probes (T's three walls, now with the term that should close them)
- U-1 (ABSOLUTE SCALE, TARGETED AT pi^2): does the core-coupled fixed point
  fix an ABSOLUTE x* (collapsing T's one-parameter family), and does it land
  at x* = e^(pi^2) = 19333.7 -- the value T proved the pi lambda_C landing
  requires? Compute x* on both k/T0 branches. The core adds the SECOND
  stationarity condition T's system lacked; whether it pins x* at e^(pi^2)
  is THE test. If x* = e^(pi^2) on a physical branch: hbar DETERMINED at the
  clean pi^2 value, Q's ceiling BROKEN by the outward core. Compute BLIND
  (before loading pi^2 as a target -- the confinement condition should
  produce it, not be told it).
- U-1b (THE TWO-EXPONENT RESOLUTION): with x* pinned, RE-CONFRONT the alpha
  exponent. T's alpha limb wanted ln x* ~ 4.3 but that was under the free
  family; with x* pinned at e^(pi^2) by the core, recompute the alpha
  coupling. Do the scale exponent and the alpha exponent RECONCILE (one
  quantity, and the earlier 4.3 was an artifact of the unpinned family), or
  are they genuinely TWO (and the core pins scale while alpha needs a third
  condition)? Either answer is a first-class result: reconciliation is the
  summit; two-exponent is the sharpest possible location of what remains.
- U-2 (a0 AS SECOND SCALE -> alpha BLIND): does the core-coupled geometry
  admit a SECOND scale (the atomic/orbital a0) distinct from R*? If R* and
  a0 are both fixed by the coupled system, alpha = R*/(pi a0) is COMPUTED.
  Commit it BLIND (before loading 1/137). This is the campaign's prize: a
  computed alpha.
- U-3 (REP SELECTION): with a genuine fixed point (unlike T), does
  stationarity now SELECT the double-valued rep (the config is stationary
  only in the spinor rep)? T proved rep selection rides on the fixed point
  existing; U has the fixed point.

### Bars (reach-tuned)
- U1 (THE CORE IS THE METHOD): the deliverable is the core-coupled fixed
  point, solved, both branches. Not the pure-screw terminus (T did that and
  found it marginal). The extensibility quartic MUST be in the functional.
- U2 (VALIDATION): validate the augmented solver on a known quartic-
  stabilized soliton/core problem with an absolute scale (the core-fixing
  problem EM-RECON-009 already solves for nuclear/chemical spacing is the
  natural control -- reproduce one of those absolute scales first).
- U3 (TARGETS BLIND, STAGE 1): compute R*, omega*, a0-candidate, and the
  alpha-ratio from mechanics on both branches BEFORE loading m_e, hbar,
  1/137, lambda_C. The blind alpha-ratio is the prize and must be committed
  before seeing 137.
- U4 (BOTH BRANCHES, LET THE ELECTRON ADJUDICATE): k/T0 = 2 AND k/T0 = 1.9e8
  both computed. If one branch gives a sensible electron and the other
  absurd, register the ADJUDICATION of FND-021's dispute as a first-class
  result.
- U5 (HONEST CIRCULARITY): a scale from k is mechanical, not circular (U4
  framing). But if the fixed point secretly requires m_e or hbar's value to
  close, THAT is circular and is flagged. Distinguish honestly.
- U6 (NEGATIVE KEPT, NOT THE GOAL): if the core-coupled system STILL gives
  only a ratio (U-1 null on both branches), that is a deep and surprising
  result -- it would mean even T's proven-sufficient term does not close the
  system, and the "missing object" is subtler than a non-marginal outward
  term. Registered, but the commission's PURPOSE is the positive; run all
  three probes on both branches to completion before any null is called.

## PHASE 2 (SEALED): CONFRONT
- if U-1 fixed an absolute R* >> a: does R* = pi lambda_bar_C hold with
  omega* DERIVED from k (not m_e-anchored)? -> hbar DETERMINED.
- if U-2 gave an alpha-ratio: does it match 1/137.036? (within a few
  percent = landmark; exact = historic.)
- if U-3 selected the rep: spinor match / (4,7) consistency.
- k/T0 adjudication: which branch gave the physical electron?

## Registrable outcomes (chartered to WIN)
1. SUMMIT: the core-coupled fixed point fixes an absolute hierarchy scale
   (U-1), a0 emerges giving alpha near 1/137 blind (U-2), rep selected
   (U-3), on the physical k/T0 branch. hbar determined, alpha derived, mass
   computed, k/T0 adjudicated. The campaign's goal, reached.
2. SCALE + ADJUDICATION: U-1 fixes an absolute scale on one branch, ruling
   the k/T0 dispute; hbar determined even if alpha stays open. Landmark.
3. ALPHA CRACKED: U-2 gives alpha near 1/137 blind. Historic regardless of
   the rest.
4. REP FOLDED: U-3 selects the rep on the fixed point. Two bricks -> one.
5. DEEP NULL: even the quartic core gives only a ratio on both branches --
   T's proven-sufficient term fails to close the system, so the missing
   object is subtler than "non-marginal outward term." A surprising,
   important negative that would redirect the hunt (registered, not the
   goal, reached only after both branches run to completion).

## Seal and sequencing
Phase 1 (couple core, solve both branches, blind predictions) -- may span
sessions (nonlinear, two branches); expected and permitted. Phase 2 fresh.
Stage 1 targets-blind per U3.

## What this is
The construction with T's PROVEN-SUFFICIENT ingredient, using a term the
corpus ALREADY BUILT and NEVER coupled to the electron, carrying the k/T0
dispute so the electron can adjudicate it, chartered to WIN. If it closes,
the campaign's whole objective -- hbar, alpha, mass -- lands together, as T
proved it must. This is the maximum-chance path, and its ingredient is not
hoped-for but registered.

## Depends on
Commission T (the proof that ONE outward non-marginal term closes all three
walls, and the sqrt(2) parallelism it breaks), Commission R (the frozen
terminus), Commission O (J0, pi alpha decomposition), Commission Q (the
survey U5 refuses to over-apply to a k-set scale), EM-RECON-009 (THE
QUARTIC CORE, c4=(k-T0)/8, the outward non-marginal term), EM-RECON-013
(the k_eff/coverage protector), FND-021 (the k/T0 eight-order dispute U
adjudicates), QB-008 (the high-k branch), FND-MATTER-001/002/004 (a0 and
star geometry), GG-006, GRV-020, SYNTHESIS_matter_boundary.
