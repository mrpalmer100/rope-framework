# COMMISSION V-A-NEXT-ORDER: THE BOUNDARY O(Omega) TERM
# (chartered 2026-08-06, NAMED-NOT-OPENED, for a later deliberate go-decision.
# The alpha chain 1/alpha = 4 pi^3 D_E stands with EVERY FACTOR DERIVED and one
# open number: the +178.8 ppm residual. Z Brick 5 exhausted the in-package
# residual ladder and proved a scale-invariance theorem (no continuous dials),
# leaving exactly ONE named candidate source not computable in-package: a
# boundary O(Omega) term at the V-A landing formula, where Omega x* = pi EXACTLY
# makes naive power-counting degenerate so such a term is dropped unless computed
# explicitly. This commission is the single cheap, decisive, falsifiable shot at
# converting "alpha reduced" into "alpha derived" -- OR at cleanly locating the
# residual as the quantum/radiative wall. It is deliberately bounded: one
# computation, a pre-committed bar, a genuine willingness to accept the wall.)

## THE STAKES, STATED HONESTLY UP FRONT
This is the commission that could derive the fine-structure constant, or could
show the residual is the same radiative physics as the g=2 anomaly (the
framework's one fence). BOTH outcomes are wins IF the discipline holds. The
failure mode to guard against is turning this into a SCAN for explanations of
178.8 ppm -- that is the numerology trap the whole arc was disciplined against
(the -15 ppm artifact, the nulled Omega lead, the premature pi^4 lock all came
from it). This commission runs ONCE, against a bar set BEFORE the computation,
and STOPS -- whether it lands, misses, or points at the wall.

## THE TARGET (sharp, grounded in registered numbers)
The residual is +178.8 +/- 0.4 ppm: the chain's 1/alpha = 137.060504 sits ABOVE
measured 137.035999, so a closing correction must REDUCE 4 pi^3 D_E by 178.8
ppm. Registered facts (verified):
  - x* = e^(pi^2) = 19333.7 (T's confinement anchor, ln x* = pi^2 exact)
  - Omega = pi/x* = 1.624932e-4 (the rotation rate)
  - Omega x* = pi EXACTLY -- the degeneracy condition
  - closure requires delta(4 pi^3 D_E)/(4 pi^3 D_E) = -178.8 ppm, i.e. a
    boundary term of the form c * Omega with c = -1.1004 (an O(1) coefficient).
So the test is SHARP: does the V-A landing formula carry a boundary O(Omega)
term whose DERIVED coefficient is -1.10 (within a pre-committed grade)?

## WHY THIS IS THE DISTINCT ROUTE (not the nulled channel)
Z Brick 5 already NULLED the BULK rate channel: d ln D_E/d ln Omega = -0.19,
requiring an unsupported coefficient 5.78. That is the response of D_E to a
change in Omega -- a bulk quantity. The V-A term is DIFFERENT: it is a BOUNDARY
term in the landing formula (the R* = pi lambda_C closure that converts J0 to the
confinement scale), present because Omega x* = pi makes the naive expansion
degenerate at the boundary. The bulk rate being nulled does NOT bound the
boundary term -- they are different objects. This commission computes the
boundary term the power-counting drops, which the in-package machinery never
reached.

## THE BARS -- STRICT ON PROVENANCE, PRE-COMMITTED (the anti-fishing discipline)
- BLIND / DERIVED-FIRST: compute the O(Omega) boundary coefficient c from the
  landing formula's mechanics with alpha OUT of the room. c is an OUTPUT; -1.10
  is the target it is compared to AFTER derivation, never fitted toward.
- THE BAR (pre-committed, before computation): the derived c must land within
  +/- 20% of -1.10 AND carry the correct sign (negative, reducing the chain).
  A coefficient outside that band, or of the wrong sign, is a MISS -- registered,
  not massaged.
- SECOND PREDICTION REQUIRED (the arbiter, no exceptions): a c that lands is a
  LEAD until it predicts something INDEPENDENT with no new freedom. The natural
  second prediction: the SAME boundary term must appear in the g=2 moment or the
  13.6 eV binding at its own predicted size, OR the muon/proton-ratio analog
  must carry the term with the mass-scaling the construction forces. A c that
  lands AND whose second prediction holds GRADUATES to a derivation of alpha.
  A c that lands but whose second prediction fails is a COINCIDENCE, killed.
- THE WALL IS AN ACCEPTABLE ANSWER: if the boundary term is not a clean
  classical O(Omega) term -- if it requires radiative back-reaction (the same
  physics the g=2 residual localizes on the Schwinger term) -- that is
  registered as "the residual is the quantum fence," a CLEAN, PUBLISHABLE
  boundary, NOT a failure. The framework does not fake the quantum sector.
- NO SCAN: exactly ONE object is computed (the V-A boundary O(Omega) term). If
  it misses, the commission does NOT then hunt other candidates -- it registers
  the miss and STOPS. Other candidates (if any survive) are separate future
  go-decisions.

## PHASE STRUCTURE (one session)
- PRIMARY: expand the V-A landing formula (R* = pi lambda_C closure, converting
  J0 = hbar/pi alpha to the confinement scale) to next order in Omega at the
  boundary, using the Omega x* = pi degeneracy explicitly (the term naive
  power-counting drops). Extract the coefficient c of the O(Omega) correction to
  4 pi^3 D_E.
- GRADE (blind): compare c to -1.10 +/- 20%, correct sign required.
- IF IT LANDS: run the second prediction (same term in g=2 / 13.6 eV / a mass-
  ratio analog). GRADUATE only if it holds.
- IF IT MISSES or REQUIRES RADIATIVE PHYSICS: register the honest outcome
  (residual not this boundary term / residual is the quantum wall) and STOP.

## Registrable outcomes (all acceptable, all terminal)
1. ALPHA DERIVED (the summit): c lands within the bar, correct sign, AND the
   second prediction holds. 1/alpha = 4 pi^3 D_E (1 + c Omega) closes to <ppm
   with every factor derived and the boundary term confirmed on an independent
   observable. The fine-structure constant is derived. (Prior: uncertain -- but
   this is the single cleanest shot, on a sharp target, with a real arbiter.)
2. LEAD (c lands, second prediction partial/pending): registered as a live lead
   with its exact coefficient and its next test named. NOT graduated, NOT dead.
3. MISS (c outside the bar or wrong sign): the residual is NOT this boundary
   term. Registered; the candidate list shortens sharply (the ladder was already
   exhausted, so a miss here points hard toward the wall). STOP.
4. THE WALL (the term requires radiative back-reaction): the residual is the
   quantum/radiative fence, the SAME physics as the g=2 Schwinger residual. A
   clean boundary: "alpha reduced to one residual that is radiative, not
   classically derivable." Publishable as the framework's honest endpoint. STOP.

## WHAT THIS PROTECTS (the standing result, whatever happens)
Regardless of outcome, the current result STANDS: every factor of
1/alpha = 4 pi^3 D_E derived, three independent observables (J0, g=2, 13.6 eV)
consistent, g=2 a free byproduct. This commission decides whether the LAST
residual falls or is the wall -- it does NOT hold the existing result hostage.
Outcome 3 or 4 leaves the corpus exactly where it is now, with one candidate
honestly closed.

## Depends on
Commission V (V-A landing formula, R* = pi lambda_C, J0 = hbar/pi alpha),
Commission T (x* = e^(pi^2), ln x* = pi^2, the Omega x* = pi degeneracy),
Commission W (D_E = 1.1051029), Commission Z Brick 5 (the nulled bulk rate
channel -- this is the DISTINCT boundary route; the scale-invariance theorem;
the residual ladder exhaustion that makes this the last candidate), Gate 1/2/3
(every other factor derived, so this residual is the sole open number). The
g=2 moment (Gate 2b) and 13.6 eV (Gate 3) are the second-prediction homes.

## NOTE FOR THE FUTURE SELF WHO OPENS THIS
The coefficient target -1.10 is suspiciously close to the LEAD-Omega observation
(residual = Omega x D_E to 0.4%). That closeness is WHY this needs a real
derivation, not a fit: the same numerical proximity that makes it attractive is
the numerology trap. Derive c from the boundary mechanics with alpha out of the
room, grade against the pre-committed bar, demand the second prediction. If it
graduates honestly, it is the fine-structure constant. If it does not, the wall
is the honest and publishable answer, and the reduced-not-derived chain is a
real result on its own. Run it once. Accept what it says.

## ---- OUTCOME (2026-08-06): MISS, charter outcome 3 ----
RUN ONCE, NO SCAN, stopped. The chartered object was realized as the finite-
strain correction to the landing condition's wave speed at the committed
profile's boundary. The committed solution's boundary strain is tiny
(g^2(x*) ~ 4-5e-9), giving a wave-speed shift of +~5e-9: coefficient effectively
ZERO, POSITIVE sign, versus the required -1.10 +/- 20% negative. Outside the
band and wrong sign -> MISS, registered, stopped (no further candidates hunted).
INFORMATIVE per the charter: the residual ladder was already exhausted, this was
the last named classical candidate, and its clean failure points toward outcome
4 (the residual as the radiative fence). But outcome 4 is NOT claimed -- a miss
on one candidate is not a positive identification of the wall. CAVEAT: the
finite-strain landing correction was the chosen realization of "the boundary
O(Omega) term"; a different, more fundamental boundary expansion of the V-A
formula would be a NEW commission with its own go-decision, not a continuation.
Script: explorations/va_next_order_boundary.py. The standing result is untouched.
