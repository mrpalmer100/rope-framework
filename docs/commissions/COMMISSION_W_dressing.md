# COMMISSION W: THE DRESSING
# (chartered 2026-08-06. The decisive test of the campaign's live alpha lead.
# V Phase 2 migrated the wall: alpha = 1/(pi^4 x D) where D = 1.40681 is a
# DRESSING factor, and LEAD-2 read D as the electron's mass renormalization
# mu/m_e, with a SECOND PREDICTION that already landed -- R_core = pi lambda_C
# /e^(pi^2) = 6.275e-17 m hits the registered M-point mesh (6.0e-17) at +4.6%,
# with NO mesh input. D is a mechanical property U's solver can compute with
# no target in the room. W computes it. If the blind D = 1.40681 and its
# second prediction holds, alpha is DERIVED: alpha = 1/(pi^4 x [computed D]).
# Discipline: LOOK HARD, keep candidates open, second-prediction rules them
# in or out -- we can rule them out cheaply later, so we do not kill early.)

## The wall's current location (what V handed W)
V Phase 2 confrontation, verified this session:
  - the electron scale from J0 = hbar/(pi alpha) lands at EXACTLY
    1/(pi^4 alpha) x pi lambda_bar_C -- so an exact closure needs
    1/alpha = pi^4 x D, and pi^4 = 97.409 is clean geometry (four
    construction factors), leaving the ENTIRE residue in D = 1.40681.
  - LEAD-2: D read as the electron dressing mu/m_e; second prediction
    R_core = pi lambda_bar_C / e^(pi^2) = 6.275e-17 m hits the M-point mesh
    (6.0e-17 m, independently registered, NO mesh input) at +4.6%. A
    genuine second prediction, conditionally graduated (mesh downstream of
    reopened 049).
So D is the last number. If D is a computable mechanical dressing, alpha
falls: alpha = 1/(pi^4 D).

## THE MODIFIED DISCIPLINE (look hard, kill cheap, never kill early)
The false-negative asymmetry governs W: abandoning a real path is far more
expensive than a coincidence caught later. Therefore W is chartered to LOOK
HARD and hold MULTIPLE candidates open, with the second-prediction rule --
not premature skepticism -- as the sole arbiter:
  - W computes D BLIND from U's solver (alpha, 137, 1.40681 out of the room).
  - Then W compares its blind D against a MENU of readings AND closed forms
    (below), generating as many candidate interpretations as it can.
  - EVERY candidate that matches D is kept as a LEAD and pursued to its
    SECOND PREDICTION. None is discarded for "looking like a coincidence."
  - A lead graduates to a result iff its second prediction (independent,
    zero new freedom) holds. A lead dies iff its second prediction fails.
    Nothing dies unexamined; nothing is believed on one number.
  - We can rule candidates out cheaply later (one confrontation each), so
    the bias is toward KEEPING and TESTING, not toward pre-emptive refusal.

## PHASE 1: COMPUTE D BLIND (alpha out of the room)

### The primary computation
From U's k-branch fixed point (the working-zip solver, v3.11+), compute the
DRESSING RATIO of the terminus: the ratio of the bare terminus mass/energy
to the physical (dressed, self-energy-included) electron mass, mu/m_e. This
is a mechanical property of the self-consistent rotating terminus -- the
back-reaction correction U's solver already carries. Compute it with alpha,
137, and 1.40681 OUT OF THE SESSION. Commit the raw number.

### The menu W compares against AFTER committing its blind number
(This menu is provided so a near-miss is not discarded -- W checks whether
its blind D matches any of these, then pursues matches to second prediction.
The menu is NOT a fishing license: W commits its computed D first, then
consults the menu.)
  - the LEAD-2 reading: D = mu/m_e (dressing), second prediction = R_core
    hits the mesh (already +4.6%; W's blind D sharpens or breaks it);
  - closed forms near 1.40681, for cross-reference: pi^2/7 = 1.40994
    (+0.22%), 45/32 = 1.40625 (-0.04%), sqrt(2) = 1.41421 (+0.53%), and
    any W's own number suggests. A closed-form match is itself a LEAD
    requiring its own second prediction (e.g. if D = pi^2/7 exactly, what
    else does 7 predict?).
  - the J0/2 gate sensitivity (V Limb 0's reopening row): if the gate
    reopens, D shifts by 2 -- W carries both.

### Bars (reach-tuned, second-prediction as arbiter)
- W1 (BLIND FIRST): the solver D is computed and committed before the menu
  or alpha is consulted. This keeps a match meaningful.
- W2 (LOOK HARD, KEEP CANDIDATES): W generates as many readings of its D as
  it can (dressing, closed form, gate-shifted, a ratio of registered
  quantities) and keeps ALL that match within solver grade. None discarded
  for implausibility.
- W3 (SECOND PREDICTION IS THE ARBITER): each kept candidate must state and
  compute a second, independent, zero-freedom prediction. Graduation on a
  held second prediction; death on a failed one; LEAD status while pending.
  No candidate killed before its second prediction runs (the anti-false-
  negative bar).
- W4 (SOLVER GRADE HONEST): U's solver has a grade; D's uncertainty is
  stated, so "+4.6%" on the mesh is assessed against BOTH the solver grade
  AND the reopened-049 mesh uncertainty, not called a hit or a miss
  prematurely.
- W5 (THE k/T0 BRANCH): D is computed on both k/T0 branches (FND-021
  dispute); if one branch gives D = 1.40681 and the other absurd, that
  branch is the physical one AND the electron adjudicates FND-021 -- carry
  both.

## PHASE 2 (SEALED): CONFRONT alpha AND THE SECOND PREDICTIONS
- W-A: does the blind D = 1.40681 (within solver grade)? If yes, alpha =
  1/(pi^4 D) is DERIVED -- the campaign's summit. The last constant falls.
- W-B: does LEAD-2's R_core = pi lambda_C/e^(pi^2) hit the mesh under the
  blind D and the resolved 049 status?
- W-C: for every graduated candidate, does its second prediction hold, and
  do any TWO candidates agree (a triangulation that would be decisive)?

## Registrable outcomes (chartered to close alpha, cheap to prune)
1. ALPHA DERIVED: blind D = 1.40681 within grade, second prediction (mesh)
   holds. alpha = 1/(pi^4 D) with D mechanical. The last constant falls;
   hbar, alpha, mass all close. THE SUMMIT.
2. D CLOSE, SECOND PREDICTION HOLDS: D near 1.40681 within an assessed
   grade and the mesh prediction holds -- alpha derived to that grade,
   with the residual a stated solver/anchor uncertainty, not a mystery.
3. D CLOSE, CLOSED FORM: blind D matches a clean closed form (pi^2/7,
   45/32, ...) that ALSO predicts a second number -- a different but real
   route to alpha; pursued.
4. D COMPUTED, LEAD-2 KILLED: blind D is a definite mechanical number but
   NOT 1.40681 -- LEAD-2 dies honestly (its second prediction was the mesh;
   if D is wrong the mesh hit was coincidence), and the dressing is a
   different open number. Clean, cheap kill; the wall stays at D but D is
   now known and LEAD-2 is off the table.
5. D UNDERDETERMINED: U's solver cannot fix D absolutely (the same
   circulation-not-energy issue) -- then D itself needs the next construction,
   and the wall migrates once more, precisely located. Registered, and the
   next target named.

## Seal and sequencing
Phase 1 (blind D on both branches, menu consulted after commit, candidates
kept) -- needs the v3.11+ WORKING-ZIP SOLVER (U's k-branch fixed point),
NOT this base package. Phase 2 (confront alpha + second predictions) fresh.
The two committed one-line confrontations V handed forward are W's starting
confrontations.

## MERGE NOTE
W requires the live registry / working-zip solver (v3.11+), and the V
Phase 1 provenance benchmark has a sympy diff bug to REPAIR before W's
provenance chain is machine-clean. W is the first commission that CANNOT
run on the base package -- it needs the session with the real solver.

## What this is
The decisive test of the campaign's live alpha lead: compute the dressing D
blind from the solver, and either alpha = 1/(pi^4 D) closes (summit) or
LEAD-2 dies cheaply and the wall stays at a now-known D. Chartered to LOOK
HARD, keep every candidate, and let second predictions -- not premature
skepticism -- rule them in or out. The path V kept alive, tested to
resolution.

## Depends on
Commission V (D = 1/(pi^4 alpha), LEAD-2, the mesh second prediction, the
committed confrontations), Commission U (the k-branch solver that computes
D, the two-constraint closure), Commission T (the e^(pi^2) anchor in
R_core), Commission O (J0 = hbar/(pi alpha)), FND-MATTER-044/049 (the mesh
and its reopened status, for W-B), FND-021 (the k/T0 branch W adjudicates),
SYNTHESIS_matter_boundary. REQUIRES the v3.11+ working-zip solver.
