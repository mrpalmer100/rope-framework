# COMMISSION N: NONLINEAR SELECTION
# (chartered 2026-08-05. The direct successor to Commission M and the
# first move in pursuing hbar. M proved the LINEAR star-mode spectrum is
# scale-free, hbar-free, and STRUCTURELESS -- dense, every integer
# present, no mode selected as the proton -- and pinned hbar as
# load-bearing at the SELECTION/occupancy step. This commission asks the
# one question that could push that verdict back a step WITHOUT importing
# hbar: does the medium's own NONLINEARITY select discrete stable
# structures where the linear spectrum could not?)

## Why this is the right first move on the hbar question
M's verdict -- "hbar is load-bearing at the selection step" -- was
proven for the LINEAR spectrum. But the rope medium is NOT linear: it
carries the quartic core (EM-RECON-008, c4 = (k-T0)/8), the twist-stretch
lock (EM-RECON-012), and the contact law (EM-RECON-013). Nonlinear wave
media SELECT: a nonlinear field equation admits soliton / breather
solutions at DISCRETE amplitudes and energies even when its linearized
spectrum is continuous or dense. The occupancy that the linear star-mode
spectrum lacks may be supplied by nonlinearity the corpus ALREADY OWNS --
no hbar, no imported quantum input. If so, the proton is a nonlinear
localized mode at a selected amplitude, and m_p/m_e is a ratio of
nonlinear mode energies, still scale-free and still hbar-free.

This is the cheapest, most native shot at the wall, and M practically
wrote its charter. It has two honest outcomes and both are valuable: it
finds the classical door (nonlinear selection lands structure), or it
confirms the wall is real by showing even the medium's full nonlinearity
does not select the proton -- which SHARPENS M5 from "the linear spectrum
cannot select" to "the classical medium cannot select, nonlinearity
included," a materially stronger boundary statement.

## THE hbar DISCIPLINE, inherited from M and stated first
This commission MAY NOT import hbar or an absolute occupancy scale to
manufacture selection -- that would fake the exact step M located. The
entire point is to test whether selection arises from the CLASSICAL
nonlinear dynamics alone. Any selection found must be exhibited as a
property of the nonlinear field equations with their REGISTERED
coefficients (k, T0, lambda, mu, the contact law), and any ratio must be
a ratio of classical energies with absolute scale cancelling as it did in
M. If selection is found ONLY by importing a quantum input, that is not a
door -- it is M's verdict re-confirmed, and it is registered as such.

## PHASE 1: DOES NONLINEARITY SELECT? (compute the nonlinear spectrum;
## hbar-blind, target-blind)

### The question
Solve for the localized nonlinear stationary solutions (solitons,
breathers, or amplitude-selected standing modes) of the strand medium in
the star-convergence geometry (FND-MATTER-002/004), using the REGISTERED
nonlinear energetics (the quartic core EM-RECON-008; the twist-stretch
lock EM-RECON-012; the contact law EM-RECON-013). Report:
  (i)   whether discrete stable localized solutions exist at all, and if
        so, at what discrete amplitudes/energies (the selection the
        linear spectrum lacked);
  (ii)  the spectrum of their energy RATIOS (dimensionless, scale-free,
        the M1 cancellation carried forward and re-verified for the
        nonlinear case);
  (iii) their stability (a selected solution that is dynamically unstable
        does not select matter -- linear stability analysis of each
        branch is mandatory, not optional).

### Bars
- N1 (NONLINEAR SELECTION IS THE DELIVERABLE): the primary output is
  whether the nonlinear problem has a DISCRETE set of stable localized
  solutions, versus a continuous family (which would fail to select just
  as the linear spectrum did). "Discrete and stable" is the door;
  "continuous family" or "all unstable" is the wall confirmed.
- N2 (REGISTERED COEFFICIENTS ONLY): every nonlinear coefficient comes
  from a registered claim (EM-RECON-008/012/013), not chosen. Where a
  coefficient is disputed (the k/T0 branches; the dE_zp corrected-map
  caveat) both branches are carried. Nothing is tuned to produce
  selection.
- N3 (VALIDATION): the nonlinear solver is validated on a known
  soliton-bearing equation with an analytic discrete spectrum (e.g. the
  1D phi-4 kink or the NLS soliton, whose selected energies are
  textbook) before the star geometry is trusted -- the M3 / FND-MATTER-006
  control discipline. Control result and error reported.
- N4 (RATIOS SCALE-FREE): energy ratios are exhibited to carry no
  absolute scale (the M1 identity re-checked for the nonlinear energy
  functional; if the nonlinearity BREAKS the scale cancellation -- which
  is possible, since a quartic term can introduce a scale -- that is a
  CRITICAL finding and is reported at full volume, because it would mean
  the nonlinear ratio is NOT hbar-free and the door is narrower than
  hoped).
- N5 (STABILITY MANDATORY): each discrete solution's linear stability is
  computed. An unstable branch is not a selection. This bar exists
  because nonlinear problems routinely produce discrete solutions that
  are saddles, not minima, and calling a saddle "the proton" would be the
  commission's characteristic failure mode.

## PHASE 2 (SEALED): THE CONFRONTATION
Targets loaded only after Phase 1's nonlinear spectrum commits.

- N-A (THE RATIO): among the discrete STABLE nonlinear solutions, is
  there one whose energy ratio to the lightest (electron-candidate)
  stable solution is near 1836? Under F4's bars (3x whisper, 25 percent
  promotes). CRITICAL DISTINCTION FROM M-A: here the modes are DISCRETE
  and SELECTED by the nonlinear dynamics, so a landing is NOT
  look-elsewhere noise the way M-A's dense-spectrum near-hit was -- IF the
  discrete set is sparse. The session must report the DENSITY of the
  discrete stable set: a sparse set landing near 1836 is a genuine hit; a
  dense set is M-A's problem again and is registered as noise under M6.
- N-B (THE SEQUENCE): do the discrete stable solutions' occupancy
  closures reproduce the magic numbers, and critically, do they now reach
  PAST 28 (where the linear/central-well spectrum failed on the missing
  spin-orbit)? Nonlinearity coupling the modes could supply the
  level-reordering that spin-orbit supplies in nuclear physics -- if the
  nonlinear closures reach 28, 50, 82, that would be a MAJOR result
  (nonlinearity as the classical origin of spin-orbit structure). If they
  stall at 20 like the linear case, that is registered too: nonlinearity
  does not supply the spin gap either.

### Phase 2 bars
- N6 (SPARSITY IS THE EVIDENCE): the strength of any N-A hit is set by
  the density of the discrete stable set. Sparse-and-selected is a hit;
  dense is noise. The session commits the sparsity readout BEFORE the
  target, so 1836's proximity cannot be read into a dense set post hoc.
- N7 (THE hbar VERDICT, mandatory, inherited from M5): the session states
  explicitly whether nonlinear selection is achieved on classical
  coefficients ALONE (the door -- M5's verdict pushed back a step, the
  proton potentially a classical nonlinear structure), or whether
  selection still required an imported scale (the wall confirmed and
  hardened). Whichever way it falls is registered at full volume.

## Registrable outcomes (all acceptable)
1. CLASSICAL DOOR: discrete, sparse, stable nonlinear solutions exist,
   scale-free ratios, and one lands near 1836 selected by the dynamics.
   The proton is a classical nonlinear structure, m_p/m_e is a nonlinear
   energy ratio, and M5's "hbar at the selection step" is PUSHED BACK --
   nonlinearity supplies the occupancy classically. The matter sector's
   largest positive result; F1-F4 and M reframed from "blocked" to "the
   proton is a soliton-class object."
2. SEQUENCE BREAKTHROUGH: the nonlinear closures reach past 28 (50, 82,
   ...), showing nonlinearity is the classical origin of the spin-orbit
   structure the linear spectrum lacked -- even if the 1836 ratio does not
   land. A major nuclear-sector result independent of the proton.
3. SELECTION WITHOUT THE RATIO: discrete stable solutions exist (the
   medium DOES select classically) but none lands near 1836 -- the proton
   is not among the low-lying nonlinear structures, but the SELECTION
   mechanism is classical after all, narrowing M5 (occupancy is classical;
   the proton specifically sits higher or elsewhere).
4. SCALE BROKEN: nonlinearity selects but BREAKS the scale-cancellation
   (N4), so the selected ratios carry an absolute scale -- selection is
   classical but the ratio is NOT hbar-free, a partial door with a named
   limit.
5. WALL CONFIRMED AND HARDENED: no discrete stable selection (continuous
   family or all-unstable), OR selection only under an imported scale.
   M5's verdict stands and STRENGTHENS: the classical medium cannot select
   heavy matter even with its full nonlinearity, and hbar is load-bearing
   at the occupancy step for the medium as a whole, not merely its linear
   part. The cleanest possible statement of the matter-sector boundary,
   and it closes the classical-door question that N was chartered to open.

## Seal and sequencing
Phase 1 (nonlinear spectrum + stability, hbar-blind, target-blind) is one
session, self-sealed. Phase 2 (targets loaded, confrontations) is a fresh
session. No session runs both phases. If Phase 1 finds NO discrete
solutions, Phase 2 is a one-line closure (outcome 5) and the wall is
confirmed without needing the targets.

## Stopping rule
One geometry, one nonlinear energy functional (the registered one), one
spectrum, stability analysis mandatory, two confrontations, one hbar
verdict, one registration. No coefficient tuning, no geometry scanning. A
negative prunes and is kept; outcome 5 is a positive boundary result even
as a negative.

## What this commission is and is not
IS: the test of whether the wall M located has a CLASSICAL DOOR -- whether
the medium's own nonlinearity supplies the occupancy selection the linear
spectrum lacked, pursuing hbar by first asking whether hbar is even needed.
IS NOT: an import of hbar (N2/N7-barred), a fit (N2/N6-barred), or a claim
about heavy generations (PM-005 scope holds). If it fails, the NEXT hbar
route is the QUANTUM_LEDGER unification (one normalization serving the
four bottleneck inputs), which is a SEPARATE, heavier commission that
imports and tests an absolute quantum input -- chartered only if N
confirms the wall.

## Depends on
Commission M (the linear spectrum and the M5 verdict this extends),
EM-RECON-008 (the quartic core), EM-RECON-012 (the twist-stretch lock),
EM-RECON-013 (the contact law), FND-MATTER-002/004 (the star geometry),
FND-MATTER-001 (the Open atomic-scale boundary this bears on),
NUC-007/008/009 (the magic-number / spin-orbit structure N-B tests),
F1/F4 (the ratio bars and scale-cancellation), the QUANTUM_LEDGER (the
four-input bottleneck N's outcome speaks to), PM-004/005 (the
excitation-spectrum negative and ordinary-matter scope), and the
SYNTHESIS_matter_boundary.md located wall.
