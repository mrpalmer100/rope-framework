# COMMISSION Z ADDENDUM (BRICK 5): LEAD-Omega NOT GRADUATED; THE
# SCALE-INVARIANCE THEOREM REGISTERED (2026-08-06)
# Scripts: z5_omega_expansion.py, z5b_xstar_family.py. Blind order held:
# both response coefficients committed (with step-size and curvature
# quality checks) before the LEAD-Omega requirement entered.

## CHANNEL 1 -- THE RATE RESPONSE: COMMITTED, DOES NOT GRADUATE
d ln D_E / d ln Omega = -0.19025 (central differences at 1 and 2 percent
agree to 6e-4; curvature 0.57; lam/Omega = 0.671737 at the committed
point, drifting -0.34 per unit ln Omega). A genuine O(Omega) rate shift
s = k Omega would need k = 5.78 to close alpha. No registered structure
supplies 5.78; the two-rate O(1) shifts (lam vs Omega) are EXCLUDED
outright -- they would move D at percent scale. The rate channel does
not graduate LEAD-Omega.

## CHANNEL 2 -- THE CLOSURE FAMILY: AN EXACT NULL, AND A THEOREM
Along the full committed closure (Omega = pi/x*, J_T = pi^2(x*^2-1)/x*),
D_E is INVARIANT to all seven computed digits under +/-1 percent
variations of x*:
    d ln D_E / d ln x* = 0   (exact within solver precision)
This is a derived SCALE-INVARIANCE THEOREM: the dressing is a pure
number of the construction, independent of the anchor's value. Two
consequences, one negative and one structural:
  - The anchor channel is CLOSED with zero lever arm: no correction to
    T's ln x* = pi^2 -- of any size -- can move the chain's value. The
    required shift is formally -5084 (absurd), i.e. the channel simply
    does not couple.
  - THE RIGIDITY RESULT: combined with the branch adjudication (Brick
    3), the r_min stability, and kappa's exactness, the chain's number
    4 pi^3 D_E is now shown to depend on exactly ONE input in the
    entire committed machinery: the DISCRETE branch choice k/T0. Every
    continuous knob has been varied and the number does not move. The
    +178.8 ppm residual is pinned against a construction with no dials.

## LEAD-Omega STATUS
Every derived channel the package owns returns a null or an unexplained
coefficient. The Omega x D_E observation (0.4 percent) stays displayed
and refused. Its remaining possible homes, named for merge: the V-A
landing-formula derivation (does R* = J0/(pi^2 mu q^2 c) carry an
O(Omega) term at next order? -- not derivable from this package), and
the boundary scale degeneracy registered in Brick 5's run: Omega x* =
pi EXACTLY, so the rotation scale and the outer boundary coincide, and
naive O(Omega) power counting is degenerate there -- next-order
boundary terms are the one correction class the in-package machinery
cannot rule on.

## THE ARC AT REST
    1/alpha = 4 pi^3 D_E = 137.060504,  +178.8 +/- 0.4 ppm
  One discrete input (adjudicated). Zero continuous dials (theorem).
  One exact conversion (kappa = pi/4, reading identified). One refused
  lead (Omega x D_E) with its homes named. Three external gates: the
  anchor verification, the 13.6 eV second prediction, and the V-A
  next-order derivation. All merge items -- this package is exhausted,
  honestly and completely.
