# SYNC STATE OF THIS PACKAGE (read first)
# Packaged by the Fable 5 review session, 2026-08-05.

## === CURRENT STATE (2026-08-06, this package) ===
# claims.yaml NOW CONTAINS the campaigns below: 518 claims total.
#  NUCLEAR 13% CAMPAIGN -- COMPLETE (classical phase, by theorem):
#    NUC-A/B/C/D/D2 registered. 13% heavy-table binding gap -> ~1% derived
#    classical physics. Symmetry energy derived both parts, Coulomb derived,
#    valley of stability derived end-to-end, surface superseded to kernel 1.26,
#    curvature PROVEN ZERO (covariogram theorem). Grid-bias self-catch corrected
#    the shell floor to 2.92 MeV rms. Remaining: edge relaxation (classical,
#    Mark's go) + quantum shell/pairing tier (~2.92 MeV rms, separate go).
#  ALPHA T-Z CHAIN -- registered as ALPHA-DE-CHAIN, in-package machinery
#    EXHAUSTED: 1/alpha = 4 pi^3 D_E = 137.0605, +178.8 ppm. D_E blind. Pinned
#    against a construction with NO continuous dials (scale-invariance theorem).
#    NOT alpha derived. Two self-corrections retired (premature pi^4 lock; -15
#    ppm mixing artifact). Three named external gates open: anchor verification,
#    charge functional, 13.6 eV second prediction. Distinct from the PRED-003
#    rho=2.6348 lineage. Full per-brick record below and in COMMISSION_{T..Z}*.
#  The mass paper (docs/rope_mass_paper.docx) and PAPERS.md are updated to both.
# =================================================

# What this zip contains
1. The base corpus as uploaded to the review session: v3.10.0-era,
   512 claims, CHANGELOG ending at the METHODOLOGY target-free-questions
   entry of 2026-08-05. The claims.yaml here does NOT yet contain the
   registrations listed below.
2. docs/commissions/ -- the complete commission package produced during
   the review arc, current through the I/J amendments (Mark's
   guitar-string objections). This directory is the authoritative,
   latest version of every charter.

## What this zip does NOT contain, and where it lives
The working sessions registered v3.11.0 through v3.13.0 in their own
repackaged zips. A session continuing from this package must merge
those registry edits (or work from the working sessions' latest zip and
merge THIS package's docs/commissions/ into it, which is the cleaner
direction). The missing registrations, by the review session's record:

- v3.11.0 (Commission A): FND-REL-004 (Derived) -- beta = 1/12 -
  B/(T0 a^2); LHAASO exclusion; band-top existence kill; live bound
  a <~ 9.4e-28 m; three claims amended (FND-REL-002 scope, FND-MATTER-005
  measurement route voided, GRV-076 staleness). 513 claims.
  PLUS the three Fable 5 amendments and B4 provenance note drafted in
  docs/commissions/AMENDMENTS_FND-REL-004.md -- drafted, adoption
  status in the registry unconfirmed by the review session.
- v3.12.0 (Commission B): FND-MATTER-061 (Modeled) -- three pins, two
  primitives; pair-1-2 contradiction fired; FND-MATTER-049 REOPENED
  (Modeled -> Open); four claims amended; allocation table + Tier-1
  flag list in the results file. 514 claims.
- v3.13.0 (Commission G): FND-REL-005 (Modeled) -- loaded continuum:
  beta = g^2/1890 (mechanism sound), killed twice (O(g) photon mass
  vs PDG; g-independent bending superluminality vs photon decay,
  2e7x); FND-REL-003 NOT de-adopted; G6 escalation to photon-carrier
  specification. 515 claims.
- Commission H results (version number not stated to the review
  session): torsion carrier -- lock cannot manufacture dispersion
  (k-independent stiffness matrix); crossing azimuth-blindness exact
  (protection survives, m_gamma = 0 confirmed); validity edge 1.2-2.1
  TeV; killed by kill class 4 (state count), which the review session
  flagged PENDING the polarization-dictionary trace; GRV-020
  Corollary 1 tension -- Mark directed it be SPLIT into its own Open
  claim (adoption unconfirmed here). Editorial call executed per
  Mark: split, not note.
- Commission E results (version number not stated): target-side audit
  CLEAN -- target converts through T0 once (059 single-conversion
  case), never through Sigma in matter055; no directional lever on the
  target; 2.08x near-miss FINAL within the registered calibration;
  two-provenance three's disentangled; calibration conditionality
  drawn by the review session (target downstream of reopened M-point,
  cube-root sensitivity) and registered in
  ADDENDUM_lambda_calibration_conditionality.md, with insertion text
  for the FND-MATTER-060 closure note provided there.
- Commission F1 results (version number not stated): threshold-star
  electron FAILED-KEPT. B1 circular by identity (E = C.T0.a is the
  M-point calibration rearranged); B2 no forced C (six landings 1.80-
  18.7, three within 2x, closest N=1 chord 1.145x displayed+refused,
  2/sqrt(f_c) numerology refused); B3 non-circular horn (electron =
  the FND-MATTER-004 atom-threshold star) misses 1.3e17x, joins
  MATTER050. Transferable theorem stated as a calibration-reach
  warning (respecting sub-commission independence): any electron
  energy = (dimensionless).T0.a is circular. Survives 049 reopening
  (B1 invariant on the constraint surface; B3 has 17 orders margin;
  table numerics tracking-identity-grade only). Draft claim written
  Failed-kept, ready for id at merge.
- Commission F4 results (version number not stated): ratio-first
  m_p/m_e FAILED-KEPT, spectrum-gated. Corrected on Fable 5 audit from
  the session's 'closes at the spectrum' headline to 'GATED on an
  uncomputed spectrum region and a missing proton topology' -- the
  block is not a framework ceiling. sup=10.06 over certified structures,
  182.6x deficit; anti-hierarchical zero-point finding ELEVATED to
  primary positive (excludes the registered second term as the
  hierarchy source, sharpening FND-MATTER-007 asymmetrically). Both
  key numbers reproduced this session. Corrected claim in
  CONSOLIDATION_2026-08-05.md.
- Commission I results: polarization dictionary trace. Kill class 4
  CONFIRMED trace-based and SCOPED to the bare single-field carrier
  (three computed kills: Malus washout, Beth angular-momentum sign,
  birefringence mode-count; Malus and Beth identities reproduced
  sympy this session). E-DIRECTION SILENCE spun into its own Open
  claim (the framework registers E magnitude, split, and B, but never
  E's transverse direction -- an FND-REL-003-style adopted gap).
  EM-005 checked carrier-robust. Registrations in CONSOLIDATION.
- Commission J results: dressed carrier FAILED-KEPT (outcome-4). The
  lock forces a LONGITUDINAL dressing only; psi=0 is an exact solution
  of the full nonlinear driven system (transverse sector never sourced,
  only parametrically modulated) -- verified sympy this session (no
  psi-independent source term). Spun off: (a) the all-orders decoupling
  UPGRADE to EM-RECON-011 (Derived); (b) the screw-speed flag (not
  identically sqrt(T0/mu), a pre-registered check for K). CARRIER
  CHANNEL-MAP registered as a Modeled mature negative (two-strand
  carrier space closed, GRV-013 analogue). Registrations in
  CONSOLIDATION.
- Commission L results (Phase 1 + Phase 2): knot-stability spectrum
  CLOSED negative (Outcome 4, clean, kept). Rule sigma=L/n on crossing
  number, stable = strict local minimum; stable set {5,7}, cumulative
  [5,12], no match to magic numbers or alpha multiples (L-A miss at
  rung one); extrapolated sigma monotone-decreasing, no interior
  minimum, so NO proton pointer (L-B null, verified); L-C gated closed.
  Discipline held end to end (no invariant/rule scanning, Outcome 5 not
  triggered). KEY FINDING: the failure is ABSENCE OF SELECTION, not
  wrong scale (the hypothetical ratio-crossing sits near F4's ~1000
  ballpark, but the rule cannot SELECT a class). Proved the proton gap
  is a missing selection principle. Draft claim Outcome-4, ready for id.
  Motivated Commission M (mode substrate).

## Verification items owed at merge time (from the review session's
## audits, so they are not lost between packages)
- FND-MATTER-003 staleness: its terminus closure cites the reopened
  049; check the Tier-1 sweep caught it, amend if not (and anything
  else citing the atomic-scale problem as closed, incl. FND-MATTER-050
  language).
- B5 reconciliation: Commission B cited the FND-REL-004 amendments
  from its commission statement; verify against the registered texts.
- FND-REL-005 kill 1 diatomic flag: whether the crossing partners were
  dynamical or pinned (CA5 of the carrier adjudication carries this as
  a one-item benchmark inspection).
- Credit lines: the guitar-string objections (one azimuthal direction;
  the corkscrew) are Mark's catches and should carry his name in the
  Commission I registration notes, per house practice.

## Commission status board (review session's record, 2026-08-05,
## POST-CONSOLIDATION)
  A  dispersion coefficient        CLOSED negative (FND-REL-004)
  B  three-pin (v2)                CLOSED (FND-MATTER-061; 049 reopened)
  C  scattering Casimir            CHARTERED, unrun, runnable
                                   (conditionality line appended)
  D  anisotropic family count      CHARTERED, unrun, runnable
  E  target-side audit             CLOSED clean (lambda near-miss final
                                   within calibration; conditionality
                                   registered)
  F1 threshold star                CLOSED negative (circular by theorem)
  F4 ratio-first m_p/m_e           CLOSED negative (spectrum-GATED, not
                                   framework-bounded; anti-hierarchical
                                   zero-point finding elevated; corrected
                                   claim in CONSOLIDATION_2026-08-05.md)
  F2 winding terminus              CHARTERED, unrun, DEMOTED (xi B1
                                   sharpened: second calibrated surface)
  F3 collective gap                CHARTERED, unrun, DEMOTED
  G  loaded continuum              CLOSED negative (FND-REL-005)
  -  carrier adjudication audit    SUPERSEDED in practice by H/I/J having
                                   run; CA5 diatomic check may still be
                                   worth a one-item pass at merge
  H  torsion carrier               CLOSED; kill class 4 CONFIRMED
                                   (trace-based, scoped to bare single-
                                   field) by Commission I
  I  polarization dictionary       CLOSED negative (three computed kills,
                                   two verified this session); spun off
                                   the E-direction-silence Open claim
  J  dressed carrier               CLOSED negative (Failed-kept; psi=0
                                   exactness theorem, verified); spun off
                                   the all-orders decoupling Derived claim
                                   and the screw-speed flag
  -  carrier channel-map           NEW Modeled mature-negative claim (the
                                   EM-sector GRV-013 analogue): two-strand
                                   carrier space CLOSED, all three channels
                                   computed negative
  K  three-strand stabilizer       CHARTERED, GATED (I+J now both negative,
                                   gate condition MET), HELD FOR MARK'S
                                   EXPLICIT GO -- ontology extension is a
                                   programme-level fork, not commission-level
  L  knot-stability spectrum       CLOSED negative (Outcome 4): sigma=L/n
                                   finds nothing on the registered
                                   spectrum; the rule has NO SELECTION
                                   mechanism (monotone-decreasing sigma,
                                   no interior minimum, verified). Proved
                                   the proton gap is a MISSING SELECTION
                                   PRINCIPLE, not just an unbuilt solver.
  M  star-mode spectrum           CLOSED. Phase 1: the star-mode ratio
                                   spectrum is EXACTLY scale-free and
                                   hbar-free (z_{l,n}/pi, no N/a/R/T0/mu/
                                   f_c -- 1/r^2 cancels by identity;
                                   verified). Phase 2: M-B PARTIAL
                                   CONSILIENCE (closures 2,8,20, misses 28
                                   -- the NUC-007 pattern exactly, star
                                   and nuclear well one physics); M-A NO
                                   HIT (spectrum contains every integer
                                   densely, 1836 near-hit registered as
                                   look-elsewhere noise under M6 despite
                                   0.008% proximity -- discipline held).
                                   M5 VERDICT (outcome 3, the capstone):
                                   hbar is load-bearing at the SELECTION
                                   step, not the spectrum step -- the
                                   classical medium supplies the well and
                                   shell structure, not the occupancy.
                                   FND-MATTER-001 boundary PINNED.
                                   Phase 1 erratum: frozen table truncated
                                   l<=6 (closed form authoritative, hits
                                   unaffected).

## THE CAMPAIGN'S BOUNDARY IS LOCATED. See SYNTHESIS_matter_boundary.md.
## Four roads (F1, F4, L, M) reached one wall: hbar load-bearing at the
## SELECTION/occupancy step. Successes (nuclear composition, magic 2/8/20
## from TWO substrates, classical optics, massless photon) sit cleanly on
## the classical side; heavy-matter selection and absolute scale sit on
## the quantum side, unfaked. The matter-sector analogue of GRV-013.

  N  nonlinear selection          CLOSED. Phase 1: localized classical
                                   solutions FORBIDDEN exactly (Derrick,
                                   sympy-exact, independently reproduced --
                                   c4>0 delocalizes, c4<0 unstable collapse
                                   E''=-(3/2)P2 T). Mechanism NAMED and it
                                   is the campaign's deepest finding: the
                                   ONE gapless generator (GRV-020/EM-RECON-
                                   012) that gives the massless photon
                                   FORBIDS the soliton's mass term; the
                                   medium is dispersion-free (Commission A);
                                   no soliton balance possible. The matter
                                   wall and the photon channel-map are TWO
                                   FACES OF ONE GAPLESS GENERATOR. Bonus:
                                   c4<0 ellipticity loss at q*=-1/(3pi^2)
                                   re-derives EM-RECON-009. Outcome 5, wall
                                   confirmed and HARDENED to exact/
                                   mechanistic. Phase 2 CLOSED (fresh
                                   session reproduced the Derrick spine
                                   before targets; N-A/N-B closed UNFIRED
                                   -- empty set, unconfrontable not missed;
                                   Outcome 5 registered). Gates Commission
                                   O open. MERGE CAUTION: the Phase 1
                                   deliverables zip predates the base
                                   package uploaded here -- it MUST travel
                                   with this closure at merge time.
  O  occupancy quantum            PHASE 1 CLOSED. O1 winding-loophole check
                                   RAN and FAILED (winding spectrally inert
                                   1e-15 with half-integer control at 0.1;
                                   amplitude-blind across 12 orders; one
                                   integer per config vs one per mode --
                                   structural mismatch; N's last exception
                                   checked inside O, not assumed). Energy-
                                   quantum candidate killed (spectrum
                                   incommensurate, max residual 0.46,
                                   verified; E drifts but E/omega conserved
                                   under well drift). FREEZE: one action
                                   constant J0, I_mode = E/omega = m J0 --
                                   Bohr-Sommerfeld quantization of the
                                   classical adiabatic invariant the medium
                                   ALREADY distinguishes (action), the most
                                   defensible form an imported quantum could
                                   take. Units forced Planck-like; VALUE
                                   uncomputed, no hbar comparison (O4 held).
                                   Zero-point offset deferred to Phase 2.
                                   PHASE 2 gated open, with two added seal
                                   points: O6b (commit the offset from
                                   mechanics BEFORE the electron target) and
                                   O6-hardened (electron mass and 13.6 eV
                                   may be ONE atomic-scale condition -- prove
                                   independence before counting hits).
                                   >>> PHASE 2 CLOSED. Both results, equal
                                   volume:
                                   POSITIVE (the handle): J0 = hbar/(pi
                                   alpha), nine digits verified. The
                                   occupancy constant IS Planck-like; the
                                   gap to hbar is ONE named dimensionless
                                   factor. J0=hbar IFF the electron mode
                                   confines at R*=pi*lambda_C (pi x reduced
                                   Compton wavelength) -- verified algebraic.
                                   FND-MATTER-001 reduced to: why pi alpha.
                                   CAVEAT (on the claim's face): pi alpha is
                                   partly INHERITED from anchoring at the
                                   atomic scale a0 (which already contains
                                   alpha); it is a CONSISTENCY RELATION at
                                   whisper grade, a derivation only if the
                                   confinement scale R*=pi*lambda_C is
                                   produced from mechanics WITHOUT an alpha-
                                   dependent anchor. NEGATIVE (O7, full
                                   volume): one star-anchored constant serves
                                   ZERO of the other four bottlenecks (13.6
                                   eV misses 1903x, proving O-A/O-B genuinely
                                   independent -- hardened O6 cleared
                                   honestly). The ledger's STAR-ANCHORED
                                   unification conjecture is falsified. Note:
                                   the fixing spent BOTH m_e AND the R scale
                                   anchor (corpus fixes N given R, never R).
                                   Discipline held (Maslov offset committed
                                   from turning-point mechanics, proven inert
                                   before electron loaded).

## THE hbar PURSUIT IS OPEN, NOT BLOCKED. N did not close it -- N cleared
## its path by proving occupancy must be quantum. Commission O is the
## pursuit: acquire hbar as ONE honest imported constant unifying the five
## bottleneck inputs. Likely summit: mass from one quantum input (not from
## nothing, which F1 forbids), which is how real physics and the rest of
## this corpus already work.

  R  electron structure           PHASE 1 COMMITTED (2026-08-06, verified).
                                   PROPOSAL: the electron is a ROTATING
                                   WINDING TERMINUS -- endpoint of the screw
                                   Goldstone's twist line, TETHERED by
                                   strands, circulating at the luminal edge.
                                   F2's winding-terminus upgraded: tethered
                                   (spin), driven (evades N's soliton kill),
                                   luminal-scaled (off F2's xi surface).
                                   R1 provenance clean: CHARGE = terminated
                                   winding integer (GG-006, identically
                                   quantized; positron = mirror handedness).
                                   SPIN = the tether Z_2 (pi_1(SO(3)),
                                   VERIFIED sympy: 2pi->-I, 4pi->+I, belt
                                   trick) -- and the SAME Z_2 gives rotation
                                   AND exchange, so spin-statistics is
                                   MECHANICAL, not imported. The campaign's
                                   first honest quantum property from
                                   classical mechanics. HONEST SEAM: the
                                   tether makes the spinor rep ADMISSIBLE;
                                   SELECTING it over the single-valued rep is
                                   the quantum occupancy step -- the proposal
                                   lands ON the located wall. Spinor-type
                                   committed; predicts (4,7)-positive quartic
                                   (R-B falsifier). SCALE = luminal edge
                                   R*=(geom factor)c/omega*, circularity-
                                   gated on omega*. FORECAST: spin limb
                                   cannot cross the wall (rep selection IS
                                   the hbar step); scale limb determines hbar
                                   ONLY if omega* is scale-free from geometry
                                   (M warns: geometry gives RATIOS, rarely
                                   absolute scale) -- likely outcome 2
                                   (partial: electron built to TWO named
                                   quantum inputs, both on the mapped wall).
                                   Phase 2 gated; the decisive bar is whether
                                   the standing-torsion geometry fixes an
                                   ABSOLUTE omega* or only ratios.
                                   >>> PHASE 2 CLOSED (2026-08-06, outcome 2,
                                   partial structure, forecast confirmed).
                                   THE ELECTRON IS BUILT to two quantum
                                   bricks. Stage-1 (targets sealed): the
                                   confining geometric factor is FORCED and
                                   is exactly pi = z_{0,1} (fundamental radial
                                   star mode, M's spectrum); rotation is the
                                   S^1 zero mode (resolving Phase 1's
                                   superluminality latency -- the ceiling
                                   bounds material circulation, the standing
                                   frequency is a phase pattern). The
                                   absolute-anchor question closed NEGATIVELY
                                   pre-target: mechanics supplies only the
                                   ratio omega* R* = pi c -- Q's theorem
                                   appeared INSIDE the construction. Stage-2:
                                   J0 = hbar iff R* = pi lambda_bar_C,
                                   identically, pi now the DERIVED mode zero.
                                   THE DECOMPOSITION (verified 9 digits):
                                   pi lambda_bar_C / a0 = pi alpha exactly, so
                                   O's single factor SPLITS into a DERIVED
                                   geometric pi and an INHERITED anchor alpha.
                                   FND-MATTER-001 residue narrows: why pi
                                   alpha -> WHY ALPHA. R-B (the (4,7) quartic)
                                   left UNCOMPUTED honestly (one-loop beyond
                                   tooling; gesturing = the fit R5 forbids);
                                   the prediction stands as falsifiable
                                   exposure. R-C unfired (no proton). Spin rep
                                   selection NOT forced -> keystone at its
                                   sharpest location: the occupancy of the
                                   tether class. NET: given ONE absolute
                                   anchor + ONE occupancy selection, the
                                   electron exists with charge, spin, spin-
                                   statistics, confinement geometry ALL
                                   mechanical; G, hbar, absolute masses hang
                                   on those two inputs. THE WALL IS TWO BRICKS
                                   WIDE. Registration: PM-005 -> superseded,
                                   successor claim carries the two-brick
                                   statement. Four draft claims + FND-MATTER-
                                   001 amendment at merge.
  S  occupancy selection          CHARTERED (2026-08-06). R's DECIDABLE
                                   remaining brick (the OTHER brick, the
                                   absolute anchor, is the alpha wall Q
                                   proved unreachable -- NOT runnable). S
                                   asks the one narrow question R left: does
                                   the occupancy quantum J0 acting on the
                                   tether Z_2 FORCE the spinor rep (exclude
                                   the singlet -> brick laid, two bricks
                                   become one), or merely make it ADMISSIBLE
                                   (no brick -> the two bricks are one quantum
                                   PACKAGE)? Decisive bar S1: forcing =
                                   mechanical EXCLUSION of the single-valued
                                   rep, NOT naturalness. HONEST FORECAST:
                                   likely admissibility (outcome 2) -- a
                                   sharpening of the wall, NOT a summit. S
                                   does NOT determine hbar or derive mass
                                   under any outcome (S4 bars the reach); the
                                   alpha wall stands behind it. The sharpest
                                   boundary-map move left. Phase 1 targets-
                                   blind; Phase 2 only if forcing. RUNNABLE
                                   NOW (needs R's frozen tether + O's frozen
                                   J0 from the package).

  T  self-consistent electron      CHARTERED (2026-08-06). REPLACES S as the
                                   next move -- aimed at WINNING, bars set for
                                   REACH not safety. The ONE computation never
                                   run: the NONLINEAR self-consistent electron
                                   (every prior commission did linear/topo
                                   analysis -> ratio ceilings + circular
                                   scales). Attacks THREE things the campaign
                                   walked past: (T-1) does the coupled fixed-
                                   point fix an ABSOLUTE scale where linear
                                   analysis gave only omega* R* = pi c? ->
                                   breaks Q by the object Q could not survey.
                                   (T-2) does the star geometry fix a0 as a
                                   SECOND mode, making alpha = R*/(pi a0) a
                                   COMPUTED ratio near 1/137? -> turns 'why
                                   alpha' from wall to result. (T-3) does
                                   stationarity SELECT the spinor rep? ->
                                   folds the spin brick in. GENEROUS bars:
                                   terminus self-energy IS an admissible scale
                                   (Q predates the object); a0 MAY be derived
                                   not assumed; conditions solved COUPLED not
                                   sealed; circularity means re-spending a
                                   calibration, NOT merely using mechanics.
                                   Blind alpha-ratio prediction is the prize
                                   (compute before seeing 137). Outcomes:
                                   summit (hbar+alpha+mass), scale cracked,
                                   alpha cracked, rep folded, or honest floor
                                   (wall survives its STRONGEST attack). THE
                                   MAXIMUM-REACH PATH. Chartered to WIN.
                                   (S retired -- boundary-map move superseded
                                   by this reach move.)

  U  outward core                  CHARTERED (2026-08-06). NOT a hunt -- a
                                   CONSTRUCTION with T's PROVEN-SUFFICIENT
                                   term, already in the registry. T proved
                                   all three walls reduce to ONE missing
                                   object: an outward-acting non-marginal
                                   term, and that it closes everything at
                                   once. EM-RECON-009's QUARTIC CORE c4=
                                   (k-T0)/8 IS exactly that: outward (k>T0,
                                   necessity-argued), non-marginal (carries a
                                   scale), and already fixes chemical/nuclear/
                                   ferro scales. R and T never coupled it --
                                   they worked the pure marginal screw sector.
                                   U couples it. Breaks T's sqrt(2) parallel-
                                   constraint by adding the outward term.
                                   Carries FND-021's EIGHT-ORDER k/T0 dispute
                                   on BOTH branches (k/T0=2 vs >=1.9e8) -- and
                                   may ADJUDICATE it (electron sensible on one
                                   branch, absurd on other). Probes: U-1
                                   TARGETED: does the core drive x* to
                                   e^(pi^2)=19333.7 (the value T proved the
                                   pi lambda_C landing needs, verified 9
                                   digits)? -> pins T's one-parameter family,
                                   determines hbar at the clean pi^2. U-1b
                                   re-confronts the alpha exponent against a
                                   pinned x* (reconcile the pi^2-vs-4.3
                                   tension, or locate a genuine 2nd exponent).
                                   U-2
                                   a0 as second scale -> alpha=R*/(pi a0)
                                   BLIND near 1/137 (the prize), U-3 rep
                                   selection on the fixed point. Reach bars:
                                   k-set scale is NOT circular (mechanical
                                   primitive). Outcomes: summit (hbar+alpha+
                                   mass+k/T0), scale+adjudication, alpha
                                   cracked, rep folded, or deep null (even the
                                   core fails -> missing object subtler). THE
                                   MAXIMUM-CHANCE PATH, ingredient REGISTERED
                                   not hoped-for. RUNNABLE NOW.
  P  degeneracy doublet           CLOSED (this session, draft -- outcome 2,
                                   NEGATIVE). The degeneracy-2 (CHEM-STRUCT-
                                   001, a genuine ledger quantum input) is
                                   NOT classically supplied by the transverse
                                   polarization doublet. P1 fails: the two
                                   2's are DIFFERENT states -- photon HELICITY
                                   (spin-1 massless, two transverse
                                   polarizations) vs electron SPIN (spin-1/2
                                   massive, two projections). P2 fails:
                                   bridging them is the spin-statistics
                                   relationship, irreducibly QUANTUM. The
                                   registry already implies this: the
                                   spin-meter chain (QGATE-010/ELEC-053)
                                   treats "is the electron spinor-equivalent"
                                   as OPEN and falsifiable (non-spinor gives
                                   (1,3)-negative quartic vs spinor (4,7)),
                                   so the electron's spin structure is not
                                   established and cannot be borrowed from
                                   the photon. The ledger STAYS at four
                                   inputs; Q confronts the full sector.
                                   KEY FINDING: the 2 is not lying in the
                                   photon's polarizations -- it lives in the
                                   electron's own UNBUILT spin structure
                                   (PM-005), the same keystone F1, the carrier
                                   campaign, and the spin-meter all hit.
  Q  independent scale            CLOSED (this session, draft -- outcome 4,
                                   the HONEST WALL, by dependency trace not
                                   computation). NO non-circular confinement
                                   scale exists. Every scale the corpus owns
                                   falls in one of two circularity classes:
                                   matter-sector scales (a_mesh/M-point, R,
                                   xi) all RE-SPEND m_e (calibrated by it,
                                   per GRV-005); the gravity scale a_grav =
                                   sqrt(hbar G/c^3) RE-SPENDS hbar itself
                                   (Sakharov inversion defines it THROUGH
                                   hbar, GRV-007's own flag). No third class,
                                   so hbar cannot be fixed by a scale costing
                                   neither m_e nor hbar -- none exists.
                                   THE THEOREM (occupancy twin of GRV-006's
                                   G result): hbar is NOT determinable from
                                   current commitments; the reduction shows
                                   why. DEEP PAYOFF: GRV-006 reduces G to
                                   (m_p/M_Pl)^2 = the particle-mass frontier;
                                   Q reduces hbar to the SAME frontier via
                                   J0=hbar/(pi alpha). G, hbar, and the
                                   absolute masses are ONE irreducible knot,
                                   and its core is the UNBUILT ELECTRON (P's
                                   keystone). The hbar pursuit, the mass
                                   pursuit, and the G question are ONE
                                   pursuit. Building the electron is the only
                                   move that touches all three.

## ALL CONSOLIDATION ENTRIES: docs/commissions/CONSOLIDATION_2026-08-05.md
(F4 corrected; I kill-confirm + E-direction-silence; J Failed-kept +
psi=0 exactness + speed flag; carrier channel-map; optics one-liner.
Every load-bearing identity reproduced sympy this session before
registration.)

## PRIORITIZATION (Mark's steer, 2026-08-05): MASS-FIRST
The ultimate objective is and remains ABSOLUTE MASS. The carrier chain
(A/B/G/H/I/J/K) is not a detour into optics -- it is the adjudication of
the scale foundation that every absolute mass claim hangs on, because
the photon is the corpus's only independent probe of the same a the m_e
calibration pins, and the two disagree. But the campaign had drifted so
that the carrier fire was the only thing moving. Corrected order:

  RUN NOW, in parallel:
   - F4 (ratio-first m_p/m_e): ELEVATED by F1 to LOAD-BEARING. F1
     proved every absolute-energy electron identity is circular vs the
     calibration; the ratio is the only construction that escapes,
     hence the only F-route that can prove anything. Highest priority
     of the entire remaining programme for the mass objective.
   - I (polarization dictionary): continues the foundation adjudication;
     EM-006 peek suggests a mechanics-silent verdict is likely.
   - C and/or D: lambda's remaining mechanism links, now unblocked by E.

  F2/F3: still runnable but DEMOTED by F1's transferable theorem -- both
  can at best yield a consistency test (F1 showed even that needs a
  forced constant), and F2's xi route is a second calibrated surface,
  not escape. Run only after F4; F4's outcome may moot them.

  AFTER I: J (dressed carrier, the leading photon candidate).

  HELD: K does not open on the I+J gate alone. Extending the ontology to
  a three-strand braid is a programme-level decision and waits for Mark's
  explicit go. If I+J close negative, the registrable result is the EM
  carrier channel-map (the photon analogue of GRV-013) -- a major
  negative worth banking before any rebuild.

  STANDING: the scale foundation stays in dispute until the carrier
  chain resolves; every absolute mass claim downstream of the M-point
  (including lambda's target, per E's conditionality) inherits that
  dispute. F4 is the one mass result that does not.

  V  circulation quantum          CHARTERED (2026-08-06). THE SUMMIT ATTEMPT,
                                   rewritten to CRACK ALPHA (not to rest at a
                                   loop). U proved the electron scale is set
                                   by a conserved CIRCULATION J. Circulation
                                   of a winding object is TOPOLOGICALLY
                                   INTEGER-quantized (GG-006). So J = n J_base;
                                   and if J = J0 = hbar/(pi alpha), then
                                   alpha = hbar/(pi n J_base) -- alpha DERIVED
                                   from a blind-computed winding integer n
                                   plus a mechanical base circulation. THE
                                   ATTACK: compute n and J_base from the R/T
                                   construction with 43.62=1/(pi alpha) OUT OF
                                   THE ROOM (anti-fishing bar V5), then test
                                   whether hbar/(pi n J_base) reproduces
                                   1/137.036. Blind-predicted n hitting alpha
                                   = the campaign's goal reached. Also:
                                   provenance match J vs J0 (V1, earned not
                                   assumed), and the overdetermined check
                                   (T's pi^2, U's closure, O's hbar/pi alpha).
                                   Outcomes: ALPHA CRACKED (summit), integer-
                                   found-value-off (mechanism right, magnitude
                                   located), loop-closes-alpha-residue
                                   (fallback only after the integer attack
                                   runs to completion), separate (UNLIKELY --
                                   same construction, same units), or
                                   inconsistency-located. Run after freezing
                                   U/O/T. THIS IS THE ONE.

  V  PHASE 1 CLOSED (2026-08-06): J = J0 identification EARNED structurally
     (one S^1 Noether charge = orbit action of one terminus; verified struct-
     urally, though the shipped provenance benchmark has a sympy diff bug --
     REPAIR AT MERGE). Electron winding n=1, so topology is trivial and the
     ALPHA MAGNITUDE LIVES ENTIRELY IN J_base. Convention theorem: E/omega is
     only the linear reading; nonlinear regime must quantize CIRCULATION, not
     E/omega (prevents a class of Phase 2 errors). One gate open: the Z_2
     half-quantum (J0 vs J0/2). The anti-fishing bar correctly prevented a
     FALSE WIN (43.62 is near many things); it did NOT prevent the real path,
     which is computing J_base blind.
  V  PHASE 2 CHARTERED (three limbs, rewritten for max chance + no premature
     failure): Limb 0 settles the Z_2 gate blind. Limb 1 = the BLIND SUMMIT
     shot: compute J_base fully from terminus mechanics with alpha OUT of the
     room; a FORCED integer (mode/strand/nodal count the construction fixes)
     that predicts alpha = summit; a chosen integer = fishing. Limb 2 =
     numerical closure (V-A pi lambda_C, V-B overdetermined system, V-C does
     blind J_base give alpha). Limb 3 = EXPLORATORY LEADS: AFTER Limbs 1-2
     commit, EXPLICITLY allowed to look at 43.62/137 and hunt structure --
     nothing off-limits -- but everything found is stamped LEAD (hypothesis),
     and EVERY lead owes a SECOND PREDICTION (muon, proton ratio, etc.) that
     graduates it to a result (Balmer principle) or kills it. NO lead
     abandoned before its second prediction runs -- the bar against FALSE
     NEGATIVES / premature failure. Both errors guarded; the cheap one (false
     positive) surfaces and is caught, the expensive one (premature abandon)
     is structurally prevented.

  V  PHASE 2 CLOSED (2026-08-06, outcome 3 + LIVE LEAD): V-A miss is EXACT
     and INFORMATIVE -- R* from J0 lands at 1/(pi^4 alpha) x pi lambda_C, so
     1/alpha = pi^4 x D with pi^4=97.409 clean geometry and the whole residue
     = D = 1.40681. The wall MIGRATED from 'why alpha' to 'why D=1.40681'.
     LEAD-2 (kept, second-prediction CONFIRMED): D read as electron dressing
     mu/m_e; second prediction R_core = pi lambda_C/e^(pi^2) = 6.275e-17 m
     hits the M-point mesh (6.0e-17, NO mesh input) at +4.6% -- conditionally
     graduated (mesh downstream of reopened 049). LEAD-1 (sqrt2 pi^4, 0.53%)
     and LEAD-3 (2*3^7 noise) refused by the second-prediction rule. Z_2 gate
     settled at factor 1 (reopening row carried). The modified discipline
     WORKED: LEAD-2 survived (would have died under old bars), made an
     independent prediction, and it landed on a registered scale.
  W  dressing                      CHARTERED (2026-08-06). THE DECISIVE alpha
     TEST. Compute D = 1.40681 BLIND from U's k-branch solver (the terminus
     dressing mu/m_e). If blind D = 1.40681 within grade AND the mesh second
     prediction holds -> alpha = 1/(pi^4 D) DERIVED, the last constant falls.
     MODIFIED DISCIPLINE (per Mark): LOOK HARD -- W commits its blind D, then
     consults a MENU of readings/closed-forms (pi^2/7=+0.22%, 45/32=-0.04%,
     sqrt2=+0.53%), KEEPS every candidate that matches, and pursues each to
     its SECOND PREDICTION. Nothing killed early (false-negative is the
     expensive error; we rule out cheaply later). Second prediction is the
     sole arbiter. Both k/T0 branches (adjudicates FND-021). Outcomes: alpha
     derived (summit), D-close-with-second-prediction, closed-form route,
     LEAD-2 killed cheaply (D known, wall stays), or D underdetermined (wall
     migrates once more, precisely). REQUIRES v3.11+ WORKING-ZIP SOLVER --
     first commission that CANNOT run on the base package. Also: repair V
     Phase 1 provenance benchmark (sympy diff bug) at merge.

  W  RESULT (2026-08-06): U-solver REBUILT from spec after session loss, and
     VALIDATED as U-equivalent -- reproduced U's blind commitments (interior
     saturation 0.5 and 6892 to all digits) AND derived the rule U only
     committed (r_sat = sqrt(k-T0)/2). The rebuild is trustworthy. THEN the
     validated EL-BVP solver (phase1c) computed the dressing BLIND, 5 configs
     agree to 7 digits, spread 0.0000%, gate PASS:
        D_int[k/T0=2] = 1.1051029  (a well-converged ENERGY dressing)
     This MISSES 1.40681 by -21.4% (and every menu item by ~21%).
     >>> LEAD-2 FALSIFIED. The reading "D=1.40681 is the mechanical dressing
     mu/m_e" is DEAD: the validated solver computes the dressing = 1.105, not
     1.4068. The +4.6% mesh hit was coincidental to the (wrong) mass-dressing
     reading. This is the W charter's registrable OUTCOME 4 (LEAD-2 killed
     cheaply, D known), reached by a real computation, not premature
     skepticism -- the loosened discipline worked: the lead was chased to a
     validated solver that killed it honestly.
     WHAT SURVIVES: (a) D_int=1.105 is the REAL energy dressing, self-
     consistent with the pi^3/2 bookkeeping (17.13/(pi^3/2)=1.105, verified);
     (b) the pi^3/2 unit-fix under J=J0 is real evidence toward J=J0;
     (c) the wall RELOCATED, correctly typed: 1.40681 = R*/(pi lambda_bar_C)
     is a LENGTH ratio (confinement radius in Compton units), NOT an energy
     dressing, set by the ABSOLUTE MAGNITUDE of J in strand units = the
     occupancy rung (where O's "alpha located-not-closed" already pointed).
     alpha is NOT derived; it is LOCATED, precisely, at one named quantity,
     confirmed from three sides (O, U/W, pi^3/2 cross-check).
     MERGE: phase1c is the validated solver (explorations/); phase1b + blind
     rebuild superseded (kept for provenance); V Phase 1 provenance benchmark
     still needs the sympy-diff repair.

  X  J=J0 bookkeeping resolution   CHARTERED (2026-08-06). The honest next
     question, and the tightest remaining thread. The pi^3/2 unit-fix says:
     under J=J0, the solver energy unit = m_e c^2, and the dressed total comes
     to D_int x pi^3/2 = 17.13 m_e c^2. TENSION: a physical electron is 1
     m_e c^2, not 17. Either (a) the physical mass is the ROTATIONAL sector
     alone (E_rot = pi^3/2 unit, needs a mechanical reason the elastic sector
     doesn't count toward rest mass), or (b) the energy bookkeeping (Routhian
     vs energy at fixed J) reassigns the elastic piece. RESOLVING THIS either
     CONFIRMS J=J0 (turning O's J0=hbar/(pi alpha) into a HARD constraint on
     the quantity where alpha now provably lives) or BREAKS it (separating the
     campaigns). This is NOT an alpha attempt -- it is the necessary
     foundation any future alpha attempt needs: is the circulation that sizes
     the electron the same quantum as the occupancy that selects it, at the
     level of the energy bookkeeping, not just the units? Decidable from the
     solver + the Routhian. Run in the working-zip session (has the solver).

  NUC-0  residual audit          CLOSED (2026-08-06, tier zero, CLEAN verdict).
     279 nuclides A=12..238, NUC-005 code unchanged, one Ca-40 calibration,
     hypothesis committed before fitting. Raw gap: mean +65.8 MeV overbinding,
     heavy-table 8.8% fractional. Two-term fit R = a_A(A-2Z)^2/A + a_P parity/
     sqrt(A): a_A=19.26 (17% from empirical, PASS), pairing sign-flip present
     (ee +1.47 < odd-A +2.65 < oo +3.61), 98.8% of variance explained (verified,
     honest -- naive rms estimate would allow 99.5%, they reported the lower
     true number). Residual ~6 MeV rms is SHELL STRUCTURE at magic numbers
     (Pb Z=82, Sm/Nd N=82) -- the known next effect, cleanly separated. VERDICT:
     the 13% gap is ONE clean effect with the exact predicted shape. Tier one
     WORTH ATTEMPTING.
  NUC-A  asymmetry coefficient    CHARTERED (2026-08-06, tier one). Derive a_A
     from the framework's mode-counting (the machinery that gave magic numbers
     2,8,20) -- the symmetry energy is unequal proton/neutron level-filling in
     the same well. GENEROUS bars (Mark's steer, the LEAD-2 lesson): success is
     WIDE (a_A in 17-27 MeV counts), right-shape-off-magnitude is a LEAD not a
     failure, look-hard exploration of well variants permitted, second
     prediction (pairing sign / magic spacing) is the arbiter, nothing killed
     early. HARD SCOPE CAP (cost control): the asymmetry coefficient ONLY;
     pairing (a_P) and shell structure are OUT OF SCOPE, separate future
     go-decisions, NOT auto-spawned. Generous on the science, bounded on the
     scope. Outcomes: derived, derived-with-correction, live-lead, or boundary.
     One commission, no sub-commissions.

  NUC-A  RESULT (2026-08-06, outcome 3 STRONG LEAD, VERIFIED in-package):
     a_A(kinetic) = 16.6 MeV DERIVED from proton/neutron level-filling in the
     framework's well at derived geometry (r0=1.050 fm), quadratic (N-Z)^2/A
     shape derived (r^2=0.85). Formally 0.4 MeV below the 17-27 window -- at
     the edge. S1 (STRONG, verified): inserting 16.6 into NUC-005 closes
     heavy-table 8.8%->1.17%, table rms 84->13.6, ~87% of the gap, ZERO new
     freedom. S2 (partial/diagnostic): valley of stability holds to A~100,
     drifts -10 protons at U-238 -> sizes a missing ~2.5-3 MeV QUADRATIC
     POTENTIAL piece. V3 (full-a_V bonds, 32.8) DEAD by worsening S1. V4
     (local capacity) EXCLUDED by NUC-021 theorem + data-bounded to 0.45+-0.13
     MeV (<11% naive). Convergent: freeing linear term drops data quadratic to
     16.93, matching derived 16.6 (degeneracy caveat carried). Registry deltas:
     a_A(kinetic)=16.6 DERIVED; heavy-table 8.8%->1.17%; NUC-022 apportionment
     revised to ~85% kinetic. The bulk of the 13% gap is now DERIVED PHYSICS.
  NUC-B  potential remainder      CHARTERED (2026-08-06, Mark's go). The
     ~2.5-3 MeV QUADRATIC potential piece NUC-A's S2 diagnostic sized. Target:
     the COLLECTIVE MEAN-FIELD isospin dependence of mode overlap (quadratic
     shape) -- distinct from the EXCLUDED local capacity channel (NUC-021,
     linear, bounded <11%) and the DEAD full-bond channel (V3, 32.8, worsened
     S1). Combined 16.6 + ~2.5-3 should land the empirical 19-23 AND fix S2's
     U-238 drift. GENEROUS bars (success wide ~1.5-4, right-shape-off-mag =
     lead, S1+S2 arbiters). HARD scope cap (potential remainder ONLY; pairing
     + shell stay separate tiers). READY: extends nuc_a_asymmetry.py (verified
     runs in-package, S1/S2 harness built). One commission, no sub-commissions.
     Outcomes: derived (symmetry energy fully derived, classical nuclear
     program complete), derived-partial, lead, or boundary.

  NUC-B  RESULT (2026-08-06, outcome 2 DERIVED-PARTIAL, VERIFIED in-package):
     the potential remainder of the symmetry energy, DERIVED with ZERO new
     constants. Mean-field composition averaging of mode overlap, same-label
     pairs excluded to the first orthogonal channel at eta=x0/(x0+1)=0.576
     (from the corpus's own healing tails, x0=1.36), epsbar=a_V/6 fixed by
     Ca-40. Quadratic shape automatic (NOT the dead linear V4). Candidates:
     B1 (linear overlap) a_pot=1.92, combined 18.52; B2 (quadratic overlap)
     a_pot=3.25, combined 19.85 (in 19-23). S1 (verified): B1/B2 close
     heavy-table to 0.41% (rms 7.1/6.3), BEATING the fitted-19.26 benchmark
     with a DERIVED coefficient, nothing worsened (V3 mode avoided). S2: all
     improve the valley drift, none flatten it; residual ATTRIBUTED to the
     Coulomb miss (swapping empirical a_C collapses U-238 to -3/-4). VERDICT:
     symmetry energy DERIVED in both parts, a_A=16.6+1.9to3.3=18.5-19.9
     straddling 19.26; heavy-table 8.8%->1.17%(NUC-A)->0.41%(NUC-B). The 13%
     gap is now DERIVED PHYSICS to the half-percent level. Honest residuals:
     B1-vs-B2 overlap-order open; valley drift partly Coulomb; ~6 MeV rms
     shell/pairing scoped out. Registry: symmetry energy derived, no new
     constant; NUC-022 apportionment ~85% kinetic / ~15% potential confirmed.
  NUC-C  Coulomb corrections      CHARTERED (2026-08-06, Mark's go, option 1
     of the 3 named residuals -- the CLASSICAL, cheap, low-risk one, first).
     Derive the diffuseness + exchange corrections to the uniform-sphere
     a_C=0.823 (vs empirical 0.711, +16%, the corpus's own declared miss).
     Both CLASSICAL: diffuseness from the corpus's own surface profile (xi,
     the two-mode healing length, already registered -- NOT a new width),
     exchange from the winding-correlation Fermi-hole. Both verified to reduce
     a_C in the right direction, combined ~10-16%, the right size. GENEROUS
     bars (a_C in 0.68-0.75 counts, right-sign-partial = lead). SECOND
     PREDICTION: S2 valley drift must flatten toward the empirical-a_C result
     (-3/-4 at U-238, which NUC-B already showed empirical a_C delivers).
     HARD scope cap: Coulomb ONLY; PAIRING and SHELL remain separate quantum-
     tier go-decisions, NOT opened. READY: extends the NUC-A/B harness,
     surface profile registered, no rebuild. One commission. Outcomes:
     derived (valley prediction derived, dominant residual resolved),
     derived-partial, lead, or boundary.

  NUC-C  RESULT (2026-08-06, outcome 2 DERIVED-PARTIAL, VERIFIED in-package):
     the Coulomb diffuseness + exchange corrections DERIVED blind, zero new
     constants. Diffuseness from the corpus's own xi (a_d=xi/2=0.699fm,
     committed before grading), numerical Poisson on a Fermi charge profile:
     -7.8% at Pb-208. Exchange as a winding-correlation hole in the same-label
     pair distribution: comes out with EXACTLY the SEMF Z^(4/3)/A^(1/3) form,
     coefficient from r0 alone, -6.6%. C2 (hole depth (1-eta)=0.424, REUSING
     NUC-B's eta) lands effective a_C=0.704, IN WINDOW -- the same exclusion
     physics fixing two coefficients. S2 GRADUATES: derived Coulomb hits
     mean|dev| 1.2, U-238 -3, matching the empirical-a_C benchmark -- valley
     of stability now DERIVED END-TO-END. S1 REGRESSES (rms 6.3->63.8): but
     97.8% is a smooth volume+surface shape, residual 3.4 rms once removed --
     the +16% Coulomb error had SILENTLY COMPENSATED the sharp-sphere surface
     term; correcting one broke the cancellation. Data now prefer a_S/a_V~1.25
     (the diffuse direction). VERDICT: Coulomb derived, valley derived, and
     the surface term precisely located as the next classical piece. NUC-C is
     a WAY-STATION -- the table recovery requires NUC-D. Honest: table
     temporarily worse; recovery predicted not yet shown.
  NUC-D  diffuse surface term      CHARTERED (2026-08-06, Mark's go). THE
     CLASSICAL COMPLETION, NOT optional (NUC-C left the model mid-correction).
     Recompute the surface term (currently a_S/a_V=1.108, sharp-sphere bond-
     counting NUC-006) on the SAME diffuse Fermi profile NUC-C used (a_d=xi/2,
     registered xi, NO new constant), jointly with the corrected Coulomb, one
     Ca-40 constant. Verified: diffuse RAISES a_S/a_V toward the data-preferred
     1.25 (right direction, right ballpark). SECOND PREDICTION (S1): the
     diagnostic predicts the joint diffuse-surface + corrected-Coulomb model
     lands ~3-4 MeV rms table-wide (recovering and surpassing NUC-B's 6.3);
     confirm S2 valley holds (Z-symmetric, should). GENEROUS bars (a_S/a_V in
     1.18-1.32 counts). HARD scope cap: surface ONLY; pairing + shell remain
     the scoped QUANTUM tiers. READY: extends the harness, profile registered,
     no rebuild. THE LAST CLASSICAL PIECE -- after it, the classical program
     is complete at its ~3-4 MeV floor and both remaining tiers are quantum
     (the natural stop-and-decide point). Outcomes: classical-model-complete,
     complete-partial, lead, or boundary.

  NUC-D  RESULT (2026-08-06, outcome 3 LEAD, VERIFIED in-package; charter
     prescription OVERTURNED by its own validation): the session's key finding
     is that the charter was WRONG and its test caught it. (1) VALIDATION
     FAILED informatively: the continuum bond-counting kernel (r_c=2.40fm from
     registered bulk z=12, zero new constants) gives a_S/a_V=1.257/1.278/1.285
     at a SHARP edge -- NOT NUC-006's monolayer 1.108. The finite bond range
     (r_c > d0) already makes the under-coordinated shell thicker than one
     layer; 1.108 UNDERCOUNTED, the kernel ~1.26 is the same physics counted
     consistently, and it lands where NUC-C's data diagnostic pointed. (2)
     CHARTER OVERTURNED: stacking the diffuse Fermi profile ON TOP of the
     finite-range kernel DOUBLE-smears -> a_S/a_V~2.2, S1 blows up to rms 372.
     The diffuseness was ALREADY in the bond range; adding profile counts it
     twice. Registered no-go: one smearing mechanism, not two. (3) THE LEAD --
     sharp-kernel joint model, one Ca-40 constant: S1 rms 63.8->11.9, heavy
     1.05%; S2 graduated (1.8, and 0.8 in the surface-cancelling config --
     valley robust). Recovers most of NUC-C's regression but NOT the
     diagnostic's promised 3-4. Missing shape explicit: +3.1 A^(2/3) -0.25 A
     -7.1 A^(1/3); removal leaves 4.47 rms = the shell floor. The A^(1/3)
     CURVATURE term (Leptodermous next order) is the named remaining smooth
     piece. VERDICT: classical model RECOVERED to 1.05% heavy, valley derived,
     NUC-006's 1.108 superseded by the kernel ~1.26; classical floor one
     curvature term away, quantum shell tier at ~4.5 MeV. This is a coherent
     RESTING POINT (unlike NUC-C's mid-correction state).

  === 13% GAP CAMPAIGN CLOSED (2026-08-06) ===
  Trajectory: 8.8% heavy-table binding error (declared quantum omission) ->
  1.17% (NUC-A kinetic asymmetry) -> 0.41% (NUC-B potential, S1-optimal) ->
  Coulomb + surface consistently derived (NUC-C/D) with S1 at 1.05% heavy and
  the VALLEY OF STABILITY DERIVED END-TO-END. The 13%-class gap is now DERIVED
  CLASSICAL PHYSICS to ~1%. The symmetry energy is derived in BOTH parts
  (kinetic 16.6 + potential ~2-3, zero new constants). Remaining: one optional
  classical curvature term (NUC-D2, ~11->4.5 MeV rms), then the QUANTUM shell
  + pairing tier at ~4.5 MeV rms (separate go-decisions, genuine wall-risk).
  NAMED, NOT OPENED: NUC-D2 (curvature), overlap-order (B1/B2) & hole-depth
  (C1/C2) derivations owed, pairing (a_P), shell structure.

  NUC-D2 curvature term          CHARTERED (2026-08-06, Mark's go). THE LAST
     CLASSICAL BRICK. NUC-D left the missing piece explicit: +3.1 A^(2/3)
     -0.25 A -7.1 A^(1/3), dominated by the A^(1/3) CURVATURE term, removal
     -> 4.47 MeV rms (the shell floor). NUC-D2 carries the finite-range bond
     kernel to O(1/R): curvature-energy ~ area x curvature ~ A^(2/3) x
     A^(-1/3) = A^(1/3), the RIGHT SHAPE, from the SAME integral (same r_c,
     z=12, eps) -- NO new constant, volume recalibrated once on Ca-40.
     GENEROUS bars (curvature coefficient in an absorbing band counts, right-
     shape-partial = lead, S1 the arbiter targeting ~4-5 MeV rms, confirm S2
     holds). HARD scope cap: curvature ONLY; pairing + shell remain the
     quantum tiers. READY: extends nuc_d_surface.py, kernel registered, no
     rebuild. After NUC-D2 the smooth liquid-drop expansion is EXHAUSTED and
     the classical program is COMPLETE at its floor -- the natural stop-and-
     decide point before the quantum tiers. Outcomes: classical-model-complete
     (13%-campaign classical phase DONE), complete-partial, lead, or boundary.

  NUC-D2 RESULT (2026-08-06, outcome 4 BOUNDARY -- with a theorem + self-catch,
     VERIFIED): the primary probe returns CURVATURE = 0, EXACTLY. The kernel's
     O(1/R) term does not exist: on a sharp ball the pair count has a closed
     form via the ball covariogram g(s)/V = 1 - (3/4)(s/R) + (1/16)(s/R)^3,
     whose powers of s are 0,1,3 -- NO s^2 term, so the deficit series contains
     only A, A^(2/3), constant. NO A^(1/3) for any isotropic bond weight
     (verified, linear taper + gaussian both c1=0.0000). The charter's
     area x curvature shape argument FAILS BY EXACT CANCELLATION; the
     leptodermous expansion of this model class TERMINATES AT SURFACE ORDER
     (a THEOREM -- the charter pre-registered this as "a genuine surprise";
     it happened, with a reason). THE SELF-CATCH (6th of the arc): confirming
     the theorem exposed that the package's 1200-point deficit integral carried
     an A-growing GRID BIAS (+1.6 at Pb-208) fitting as a SPURIOUS +2.29 A^(1/3)
     -- a fake curvature term sitting inside the registered 11.9-MeV model.
     Corrected exact deficit: D(A) = 7.7268 A^(2/3) - 2.250. CORRECTED NUMBERS:
     baseline 11.37 rms / 1.07% heavy / S2 1.78; missing-shape A^(1/3) coeff
     -7.1 -> -5.97; SHELL FLOOR 4.47 -> 2.92 MeV rms (the quantum target is
     SMALLER than recorded). THE LEAD (run to 2nd prediction, died correctly):
     the diffuse-profile deficit DOES contain a curvature term of the right
     sign/scale (+7.71 vs needed +5.97), but curvature-alone S1 fails (rms 52
     -- needs the full 3-term shape); survives ONLY reformulated as
     SELF-CONSISTENT EDGE RELAXATION (profile from the kernel's own energetics,
     surface+curvature jointly) -- named, NOT opened, separate go-decision.
     VERDICT: within the registered model class the classical program is DONE
     at 11.37/1.07%, and DONE BY THEOREM (expansion terminates at surface
     order) -- cleaner than the charted completion. Everything smooth remaining
     is edge-relaxation (Mark's go) or quantum. Scope held, valley untouched.

  === 13% CAMPAIGN: CLASSICAL PHASE COMPLETE (by theorem), 2026-08-06 ===
  Final classical state: heavy-table binding 1.07%, table rms 11.37 MeV, valley
  of stability derived end-to-end, symmetry energy derived in both parts,
  Coulomb derived, surface derived (kernel ~1.26 superseding NUC-006's 1.108),
  curvature PROVEN ZERO (NUC-D2 theorem). The smooth liquid-drop expansion of
  this model class is EXHAUSTED at surface order. Remaining smooth candidate:
  self-consistent edge relaxation (NUC-D3, Mark's go-decision, NOT opened).
  Quantum tier: shell + pairing at 2.92 MeV rms (corrected down from 4.47),
  separate go-decisions. Also owed (small, classical): overlap-order (B1/B2),
  hole-depth (C1/C2). REGISTRY CORRECTION: the spurious +2.29 A^(1/3) grid-bias
  term is removed from the NUC-D model; shell floor revised 4.47 -> 2.92.

  === LEAD-2 REOPENED IN CORRECTED FORM (2026-08-06, external review) ===
  An external review caught TWO premature closures in the W/LEAD-2 result:
  (1) PREFACTOR: LEAD-2 died measured against 1/alpha = pi^4 D (D_E=1.105 misses
  1.40681 by 21%). But the SAME blind solver D_E fits 1/alpha = 4 pi^3 D to 179
  ppm (4 pi^3 x 1.1051029 = 137.0605 vs 137.0360), and 4 pi^3/pi^4 = 4/pi
  EXACTLY. (2) ENERGY vs ACTION: W's solver computed an ENERGY ratio (quadratic:
  cos^2+sin^2=1, no 4/pi); alpha may couple to a CANONICAL ACTION / rectified
  response (linear: <|cos|+|sin|>=4/pi). So D_J (action) can legitimately differ
  from D_E (energy) by exactly 4/pi. I VERIFIED the numerics: D_required/D_solver
  = 1.27301 vs 4/pi = 1.27324 (0.018% apart). The premature closure was mine --
  I locked pi^4 and read the energy dressing as final.
  Y  action vs energy dressing    CHARTERED. Compute the CANONICAL PHASE
     MOMENTUM ratio D_J = P_chi^full/P_chi^reduced (linear response) from the
     SAME committed W solver solution (D_E=1.1051029), BLIND. Test whether
     D_J/D_E = 4/pi falls out MECHANICALLY (physics gate: D_J must be genuinely
     linear-response, not asserted). If yes: 1/alpha = 4 pi^3 D_E at 179 ppm,
     DERIVED; second prediction eps_4~0.00268 from the tether f_4 perturbation
     closes the residual to <ppm. Outcomes: alpha cracked (summit), 4/pi-derived
     -residual-open, rectification lead, or falsified (D_J=D_E, LEAD-2 stays
     dead). READY: extends w_dressing_phase1c.py, solution committed, no rebuild.
     One commission. THE MOST PROMISING ALPHA THREAD -- the energy number was
     computed BEFORE the target was proposed. External review at
     EXTERNAL_REVIEW_alpha_4pi.txt.

  Y  RESULT (2026-08-06, multi-session: gate + 4 bricks, VERIFIED end-to-end;
     outcome 3 LEAD-2R, the alpha wall's best location yet):
     PART 1 (the gate, decisive negative): the committed W solution is PURELY
     QUADRATIC (n_bar=2.0000, verified) -- the 4/pi is NOT produced by the
     solver; it is a linear-response property exterior to it. The naive
     "4/pi falls out of the dressing" is FALSIFIED by the charter's own gate.
     BRICK 3 (pairing DERIVED, kills the -450 ppm ambiguity): alpha enters via
     the ACTION (J0=hbar/pi alpha), so the correction perturbs the accumulated
     phase action = the rotational sector = the P1 overlap convention (verified,
     4 shapes agree <1e-4). The energy convention (P3, -450 ppm) is EXCLUDED as
     correcting the wrong observable. The 4pi double-cover leaves the prefactor
     invariant (verified 1.2732=4/pi over 4pi). Residual budget: -15.08 ppm is
     REAL within the chain (numerics ~0.6 ppm).
     BRICK 4 (the prefactor LOCATED): the J0 anchor equates two SMOOTH QUADRATIC
     invariants (verified: J scales x4.0000 under field doubling), so 4/pi
     CANNOT live in the action steps -- derived quadratic end to end. By
     exhaustion (pi^4 = pi^2[U closure] x pi[target] x pi[J0 anchor], first two
     geometric/no-freedom), the 4/pi's ONLY admissible home is the CHARGE
     FUNCTIONAL inside a0 -- charge coupling is LINEAR in field, the unique
     linear-response observable in the chain; a linear coupling sampling a
     rotating 2-component source rectifies -> 4/pi. Bookkeeping consistent
     (e^2_lab = (pi/4) e^2_rope -> pi^4 -> 4 pi^3, one factor, first power).
     BRICK 2 (a falsification kept): the SPATIAL full-energy-modulation f4 model
     gives eps_4=+0.036 (13x too big, -2220 ppm) -- the naive spatial reading
     is DEAD; only the action-perturbation reading (Delta=1.94e-4) survives.
     THE CHAIN: 1/alpha = 4 pi^3 D_E (1 - Delta), D_E=1.1051029 (blind),
     Delta=1.9387e-4 (derived, pairing derived), 4 pi^3 (located at the charge
     conversion, rectified character IMPORTED/admissible/underived), residual
     -15.08 +/- 0.6 ppm (real, unexplained). ONE import + one 15 ppm residual
     stand between the corpus and alpha.
     NOT alpha derived: the 4/pi rests on the unconstructed charge functional.
     But the wall moved from "21% off vs pi^4, mechanism unknown" to "15 ppm
     off vs 4 pi^3, every factor derived except one linear-vs-quadratic property
     of ONE named functional." Mark's premature pi^4 lock corrected.
  CHARGE-FUNCTIONAL COMMISSION (Z candidate)  NAMED, NOT OPENED: construct the
     electric-response integral I_Q[f] from the committed profile (winding's
     directed tether load, per the carrier/polarization dictionary); exhibit
     whether its coupling to the rotating phase is LINEAR (rectifies -> 4/pi
     DERIVED, prefactor falls, alpha at 15 ppm) or QUADRATIC (no rectification
     -> 4 pi^3 DIES, the -21% pi^4 wall returns). SECOND PREDICTION attached:
     the same functional must reappear in the magnetic-moment observable. THE
     SINGLE LOAD-BEARING OPEN ITEM of the entire alpha chain -- falsifiable,
     chartered-shape, cheap (one functional on the existing profile). The last
     question. Mark's go-decision.

  Z  consistency audit / DELTA RETIRED  RESULT (2026-08-06, VERIFIED; a
     self-catch retiring an earlier enthusiasm of mine). Commission Z audited
     the Y-arc chain under the selected reading F7 and RETIRED the -15 ppm
     Delta result as an artifact of mixing two mutually exclusive readings.
     A1 (verified symbolic): under F7 the committed solution rotates rigidly,
     so the PHYSICAL waveform cos(chi) has ZERO 4th-harmonic content; the
     4th-harmonic that drove Delta lives only in the sampling WEIGHT |cos chi|
     (=-4/15pi), which under F7 is bookkeeping (how a0 was recorded at
     calibration), NOT a physical drive. No 4-Omega drive, no f4 back-reaction:
     Delta = 0 physically, UNSOURCED. A2 (verified): the mechanical circulation
     J is EXACT by the solver constraint (J/J_T=0.9999987726, D_J=1 identically)
     -- Delta was chartered as a correction to an uncorrectable quantity; and
     the dressing enters through mu (mass, energy-type) in R*, so under F7's
     all-quadratic character the mass dressing IS the energy dressing, which
     explains from inside the chain why W's blind ENERGY D_E is the right
     object (Brick4 quadratic action + Z quadratic coupling + D_E energy
     character interlock). A3 (verified): three readings span 388 ppm vs 32000
     ppm competitor gap -> the kappa=pi/4 / F7 selection is ROBUST. CONSOLIDATED
     CHAIN: 1/alpha = 4 pi^3 D_E = 137.060504, residual +178.8 +/- 0.4 ppm --
     the chain's SINGLE open number, old explanations honestly cleared. The
     -15 ppm is WITHDRAWN; the permanent record keeps the construction, the
     mixing error, and the retirement. NOT alpha derived: 4pi^3 rests on F7,
     whose ANCHOR VERIFICATION (rectified-mean convention in O's a0) is the
     load-bearing named-not-opened check (derives kappa=pi/4 or falsifies F7).
     NAMED, NOT OPENED: (1) anchor verification; (2) residual +178.8 ppm source
     -- adjudicate (not scan) k/T0 branch (FND-021, W low-branch only), r_min->0
     continuum of D_E, q^2 winding slot in R*; (3) 13.6 eV second prediction.
     [HONESTY NOTE: Claude was over-enthusiastic about the -15 ppm when it
     arrived; Z's self-audit correctly retired it. Replacing a flattering
     artifact with an honest larger residual is the discipline working.]

  Z  BRICK 3: k/T0 BRANCH ADJUDICATED  RESULT (2026-08-06, VERIFIED). The high
     branch (k/T0=1.9e8, FND-021's QB-008 Bell-timing side) computed BLIND
     through the identical phase1c gate (spread 0.0000%, PASS; genuine
     independent computation -- max k*eps=2.59, quadratic term active, profile
     restructured E_el 8.03 vs low 5.32). CONFRONTATION: high branch D=1.2922316
     -> 1/alpha=160.27 (+169540 ppm), missing closure by 17%; low branch
     (k/T0=2, committed) stands 950x closer at +178.8 ppm. ADJUDICATION-GRADE
     (W5/U4 rule: one branch sensible, other absurd vs the electron): the
     electron rules FND-021's 8-order dispute for k/T0=2 (EM-RECON-009 +
     chemical/nuclear spacings) against QB-008's Bell-timing bound. CONDITIONAL
     on the chain; named escape (high-branch shift of x* through U's full fixed
     point) flagged not opened; the QB-008 tension transfers to FND-021 as the
     open half. RESIDUAL LADDER updated: (1) branch choice EXCLUDED (moves D 17%
     not 179 ppm); (2) r_min continuum EXCLUDED (D_E stable 7 digits across
     r_min 1e-4..1e-3, ~0.4 ppm, 50x too small); (3) q^2 winding slot in R* --
     the LAST candidate standing (chain fixed q=1 implicitly; GG-006 winding
     provenance never confronted vs 179 ppm), the natural next brick. CHAIN:
     1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4 ppm -- branch adjudicated,
     continuum closed, kappa exact, residual pinned to ONE slot (q^2) + the
     standing anchor-verification gate. One number, one slot, one gate.

  Z  BRICK 4: q^2 CONFRONTATION -- LADDER EXHAUSTED  RESULT (2026-08-06,
     VERIFIED). The q^2 winding slot in R* (the last named residual candidate)
     CANNOT absorb the +178.8 ppm: it would need a CONTINUOUS q^2=1.0001788,
     but the slot is DISCRETE integer topology -- winding=1 EXACTLY (symbolic,
     profile-independent, GG-006 288-downstream), nearest alternatives O(1)
     away (q=2: +300%; sub-knots: -56%/-89%). Topology does not dress at the
     4th decimal. RESIDUAL LADDER now EXHAUSTED: all three Brick-2 candidates
     closed by computation (branch 17% off, continuum 0.4 ppm, q^2 structurally
     impossible). 1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4 ppm is the
     IRREDUCIBLE residual of the committed construction -- physics the chain
     does not yet contain, or (if the anchor gate fails) its falsifier.
     LEAD-Omega (displayed, REFUSED): the residual equals Omega*D_E to 0.4%
     (Omega=pi/x* is the construction's own rotation rate), so a first-order-
     in-Omega (finite-rotation next-order) correction is a mechanically natural
     home that NAMES its own derivation route. Graduation: derive the O(Omega)
     coefficient from the construction's expansion with alpha out of the room;
     landing at 1.10 (D_E) vs 1.105 (D_req) vs neither is the discriminator.
     [CLAUDE'S CAUTION, recorded so LEAD-Omega is not later over-quoted: a 0.4%
     match of one construction scale against another is SUGGESTIVE not strong --
     3 of 6 tabulated small-parameters land within a factor of 2, and this is
     the SAME pattern (mechanically-natural-home + close-numerical-match) that
     produced the -15 ppm enthusiasm Z just retired. Hold lightly; it lives or
     dies only on the DERIVED coefficient, not the coincidence.]
     ARC END STATE: 1/alpha = 4 pi^3 D_E (1 + O(Omega)?), D_E blind/twice-
     reproduced, kappa=pi/4 selected, branch adjudicated, Delta retired, ladder
     closed. NOT alpha derived. Open gates: anchor verification (at merge,
     decides kappa's derivation / F7), 13.6 eV second prediction, LEAD-Omega
     coefficient. Three named falsifiable routes, no fishing.

  Z  BRICK 5: LEAD-Omega NULLED + SCALE-INVARIANCE THEOREM  RESULT (2026-08-06,
     VERIFIED; the alpha package's honest endpoint). CHANNEL 1 (rate response,
     committed blind): d ln D_E/d ln Omega = -0.19025 (central diffs at 1%/2%
     agree to 6e-4). Closing alpha via a rate shift s=k Omega needs k=5.78 --
     no registered structure supplies it; the two-rate O(1) shift EXCLUDED
     (moves D at 62000 ppm, not 179). LEAD-Omega's package-owned channel returns
     a NULL. [Claude's Brick-4 caution vindicated: the 0.4% Omega x D_E match
     did NOT survive coefficient derivation.] CHANNEL 2 (closure family): D_E is
     INVARIANT to 7 digits along the full committed closure -- d ln D_E/d ln x*
     = 0.00000 EXACTLY. A derived SCALE-INVARIANCE THEOREM: the dressing is a
     pure number of the construction, independent of the anchor's value. The
     anchor channel is CLOSED with zero lever arm (no correction to T's
     ln x*=pi^2, of any size, moves the chain). THE RIGIDITY RESULT: combined
     with the branch adjudication, r_min stability, and kappa exactness, the
     chain's 4 pi^3 D_E depends on EXACTLY ONE input in the entire committed
     machinery -- the DISCRETE branch choice k/T0. Every continuous knob varied,
     number does not move: the +178.8 ppm is pinned against a construction with
     NO DIALS. LEAD-Omega's one remaining home (not in package): the V-A landing
     formula's possible O(Omega) boundary term, where Omega x* = pi EXACTLY makes
     naive power-counting degenerate. ARC AT REST: 1/alpha = 4 pi^3 D_E =
     137.060504, +178.8 +/- 0.4 ppm. One discrete input (adjudicated), zero
     continuous dials (theorem), one exact conversion (kappa=pi/4), one refused
     lead (Omega x D_E, channel nulled). NOT alpha derived -- but pinned as hard
     as a fixed construction allows. THREE EXTERNAL GATES, all merge items: the
     anchor verification (decides kappa/F7), the 13.6 eV second prediction, the
     V-A next-order derivation. THE IN-PACKAGE ALPHA MACHINERY IS EXHAUSTED,
     honestly and completely.

  GATE 1 (ANCHOR VERIFICATION)  RESULT (2026-08-06, CONDITIONALLY PASSED, NOT
     CLOSED; VERIFIED reconstructable algebra). The load-bearing gate: does O's
     actual a0 calibration exhibit the rectified-mean convention that derives
     kappa=pi/4, or does it use <cos^2> and falsify F7 (killing 4 pi^3)?
     WHAT PASSED UNCONDITIONALLY (verified): (1) the identity J0 = hbar/(pi
     alpha) = m_e c a0/pi is EXACT against the registered anchor a0 =
     lambdabar_C/alpha -- the whole gate compresses to ONE factor of 1/pi. (2)
     The kappa algebra is exact: for Z's pure-harmonic waveform, kappa =
     <cos^2>/<|cos|> = (1/2)/(2/pi) = pi/4 (verified to machine precision), and
     rectified-mean recording reproduces J0 on the nose while smooth-quadratic
     gives m_e c a0/4; the 4/pi between pi^4 and 4 pi^3 IS exactly that
     rectified-vs-quadratic conversion (4/pi = 1/kappa, verified). Consistent
     with F7 and kappa=pi/4 DERIVED not imported.
     WHY IT CANNOT CLOSE (two reasons, at full volume): (1) the pi-parity
     argument alone is NOT decisive -- 1/pi admits a second fully-smooth
     decomposition (two components x per-radian action 1/2pi), so the bare
     number cannot discriminate rectified from quadratic. The discriminator
     found: O froze I_mode = E/omega, and for the committed two-component
     harmonic E/omega already spends both the per-radian factor and the
     component count internally, killing the smooth decomposition and leaving
     rectified-mean unique -- BUT this is conditional on the recorded J0 being
     the frozen E/omega object. (2) THE BLOCKER: O Phase 2's actual calibration
     computation is NOT in this package (whole tree searched; only the
     SYNC_STATE summary here; docs charter this gate "at merge", matching the
     N Phase 1 deliverables-travel-separately pattern). Closure reduces to
     reading ONE LINE of that artifact: recorded via <|cos chi|> -> gate PASSES,
     kappa DERIVED; recorded via <cos^2 chi> -> F7 FALSIFIES, 4 pi^3 DIES.
     NAMED WEAKNESS for the record: the 1/2 accompanying the rectified mean also
     needs its origin exhibited in that artifact, NOT assumed.
     VERDICT: conditionally passed (everything reconstructable exhibits
     rectified-mean as the UNIQUE surviving convention; F7 NOT falsified -- the
     chain survived its most dangerous gate), closure pending the O Phase 2
     calibration artifact against a PRE-STATED read criterion (fixed before the
     artifact is seen -> no room to rationalize). Script:
     explorations/gate1_anchor_verification.py (drafted; the actual-artifact
     read is the merge step). alpha chain status UNCHANGED: 1/alpha = 4 pi^3 D_E
     = 137.060504, +178.8 ppm, prefactor now conditionally-derived-pending-one-
     line rather than purely imported.

  GATE1-CLOSE (Option A)  CHARTERED (2026-08-06, Mark's go). Close Gate 1 from
     SURVIVING material rather than re-creating the deleted O session. The a0
     calibration artifact (O Phase 2) is gone, but the freeze it recorded --
     I_mode = E/omega = m J0, J0 = hbar/pi alpha (9 digits), R* = pi lambda_C,
     the O6b offset charter -- is REGISTERED. Hypothesis: the recording
     convention (<|cos chi|> vs <cos^2 chi>) is FORCED by that frozen invariant,
     not a free choice that lived only in the deleted line. TEST (pre-stated
     criterion, blind): reconstruct I_mode on the committed two-component
     harmonic at R*=pi lambda_C, compute J0 under both conventions -- R
     (rectified) reproduces J0 on the nose, Q (quadratic) gives m_e c a0/4
     (kappa=pi/4 off). ARITHMETIC SCAFFOLD RUN (explorations/
     gate1_close_reconstruction.py): confirmed R reproduces registered J0 to
     ratio 1.000000000, Q off by exactly pi/4 -- criterion well-posed and R
     fits. NOT YET CLOSED: the scaffold shows R FITS, not that R is FORCED; full
     closure owes (a) the E/omega-spends-both-factors-internally argument
     computed on the actual committed waveform (forcing R, not just fitting it),
     and (b) the 1/2 (Maslov/node offset) exhibited from O6b mechanics, not
     assumed. OUTCOMES: gate-1-closed (R forced + 1/2 derived -> kappa=pi/4
     DERIVED, 4 pi^3 prefactor no longer imported), closed-partial (R forced,
     1/2 assumed), F7-falsified (Q reproduces J0 -> 4 pi^3 dies, pi^4 wall
     returns), or not-forced (neither clean -> the deleted artifact is genuinely
     load-bearing, Option B full-rebuild chartered next). SCOPE: convention +
     1/2 only; does not touch Gate 2/3 or reopen O7's settled unification
     negative. READY: all inputs registered, scaffold runs.

  GATE 1: CLOSED (2026-08-06, Outcome 1, VERIFIED). Option A succeeded -- the
     convention was FORCED, not chosen, so the deleted O artifact's one line was
     recoverable from surviving structure. THREE PIECES:
     (a) THE FORCING (cleaner than the original E/omega bookkeeping): the
     registered one-power LINEAR character of the sampled observable (Y Brick 4)
     is decisive alone -- a first-power per-cycle recording of a pure harmonic
     has IDENTICALLY ZERO smooth mean, so rectified-mean is the ONLY nonvanishing
     cycle recording that exists. Not the convention that fits; the only one that
     produces a number at all.
     (b) THE 1/2 (no insertion): exact per-component equipartition of the
     committed rigid rotor, I_x=I_y=I/2, symbolic on Z A1's waveform. NOT the
     Maslov offset -> no double-use against O6b (which committed that separately
     and proved it inert). Gate 1's flagged weakness closed properly.
     (c) OVER-DELIVERY (out-of-sample): the reconstructed recording J0 = m_e c A
     / pi (with Omega A = c, R's luminal edge) reproduces BOTH registered targets
     with ONE formula, no freedom -- hbar/(pi alpha) at A=a0 AND hbar at
     A=pi lambdabar_C (SYNC_STATE fact 3, recovered as an INDEPENDENT second hit
     NOT in the charter criterion). Quadratic competitor misses both by pi/4.
     VERIFIED: both ratios 1.000000000, competitor 0.785398 (=pi/4) on both.
     CONSEQUENCE: kappa = pi/4 DERIVED from the registered freeze; F7 CONFIRMED
     as anchor metrology; the 4 pi^3 prefactor moves from IMPORTED to DERIVED-
     PENDING-NOTHING. Chain: 1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4
     ppm -- the residual is now the SINGLE open number, prefactor no longer
     resting on a reading.
     BOUNDARY CARRIED (the gates are now coupled, correctly): the forcing
     depends on the one-power LINEAR character of the sampled observable, which
     is registered bookkeeping whose CONSTRUCTIVE origin is GATE 2 (the charge
     functional). Gate 1 closing does NOT discharge Gate 2 -- if the charge
     functional comes out QUADRATIC, the linearity premise dies and Gate 1's
     forcing dies with it. Not circular (Gate 2 is an independent construction),
     but genuinely linked: Gate 1 is closed CONDITIONAL on the linearity Gate 2
     would derive from first principles. Script: explorations/gate1_close_full.py
     (+ gate1_close_reconstruction.py scaffold).
     GATES REMAINING: Gate 2 (charge functional -- now also the constructive
     origin of Gate 1's linearity premise, doubly load-bearing) and Gate 3
     (13.6 eV second prediction, independent). Gate 1 DONE.

  GATE 2 (CHARGE FUNCTIONAL): LINEAR, 4/pi DERIVED (2026-08-06, VERIFIED
     arithmetic + logic; conditional on one named identification, with a
     self-audit that demoted the flattering argument). THE RESULT: the chartered
     object built end to end. The electric-response integral I_Q[f] is the
     winding's directed tether LOAD -- the force a tether transmits is the
     tension-weighted slope P(g^2) f', FIRST power in the profile. Computed on
     the ACTUAL committed solution (solver re-ran clean, D_int=1.1051029, spread
     0.0000%, gate PASS -- degree test on the real profile, not a stand-in): the
     load scales exactly degree 1 at both committed and small amplitude, while
     the energy competitor is degree 2 and already spent as D_E (no double
     count). A first-power rotating quantity has zero smooth cycle mean, so by
     Gate 1's forcing theorem its lab recording is necessarily rectified; the
     two-component rectified mean is 4/pi exactly, applied once at leading order.
     Prefactor closes: 4 pi^3 D_E = 137.060505, +178.8 ppm; pi^4 competitor at
     the excluded -21% wall; one-component alternative excluded structurally by
     the registered two-component waveform (not by the target).
     THE SELF-AUDIT (the discipline, kept at full volume): P1 first argued
     linearity is FORCED because a mirror-even (quadratic) functional can't
     distinguish e- from e+. BUT EM-015 (registered) derives electrostatic SIGN
     from winding as a constraint source acting through a QUADRATIC field -- the
     corpus itself exhibits sign riding topology with quadratic response. So
     "forced by sign structure" OVERSTATES, and it was demoted before
     registration (the same pattern Z retired with the -15 ppm). What actually
     carries the verdict, NOT leaning on the sign argument: the coupling is
     force-type, linear in q and field, per EM-013 (Derived); the rope-side
     force object is the tether load (degree 1, computed); rectification of a
     first-power rotating sampling is Gate 1's theorem. The sign argument is
     supporting evidence with a named caveat, not a load-bearing leg.
     VERDICT: Gate 2 LINEAR, 4/pi DERIVED, CONDITIONAL on ONE named
     identification -- that the recorded e^2 samples the FORCE-TYPE (load)
     observable rather than an ENERGY-TYPE one, with the EM-015 quadratic-field
     constraint-mechanism tension registered on the claim's face. DISCRIMINATOR
     (chartered, not yet run): the MAGNETIC MOMENT couples to the same
     circulating load functional, so the same rectified 4/pi must reappear there
     ONCE, with alpha out of the room -- a moment computation finding it (or not)
     settles the identification. SUMMIT STATUS: every factor in 1/alpha =
     4 pi^3 D_E now has a DERIVATION; ONE identification carries a named caveat
     (discharged by the moment second-prediction); the +178.8 ppm residual
     remains the SINGLE open number. NOT alpha derived -- "every factor derived,
     one identification conditional, one residual unexplained." Gate 3 (13.6 eV)
     independent, still open.

  GATE 2b (MAGNETIC MOMENT -- the discriminator): g = 2 OUT OF SAMPLE, Gate 2's
     identification caveat DISCHARGED (2026-08-06, VERIFIED). Genuinely out of
     sample: R*=pi lambda_bar_C (O/R), the 1/pi recording factor (Gate 1 vs the
     J0 targets), q=1 (Z Brick 4) were ALL frozen before the moment was
     mentioned; the moment was never used in the chain. The committed terminus's
     smooth mechanical moment is pi mu_B exactly; the Gate-1 rectified recording
     (x 1/pi) converts it to exactly 1 mu_B -- the DIRAC value, g = 2, ZERO
     freedom (verified). That misses the measured moment by -1160 ppm, and the
     radiative anomaly alpha/2pi = +1161 ppm -- agreement to 0.15% (verified):
     the g=2 miss IS the Schwinger term, the SAME class of not-yet-contained
     (radiative) physics as the chain's +178.8 ppm. The smooth-quadratic
     competitor records pi/4 mu_B, -21.5% (the wall class that killed pi^4). So
     the moment DISCRIMINATES R over Q out of sample by ~500x in the miss --
     Gate 2's force-type-vs-energy-type identification is discharged at the
     strength this check carries. TWO FLAGS on the claim's face (bounding how
     loudly g=2 exactness is quoted, NOT the discrimination): (1) the
     current-loop formula uses edge-concentrated charge (registered R-geometry);
     the PROFILE-WEIGHTED moment integral is not yet computed -- the one place a
     distributed-charge correction could move the mechanical pi. (2) the 1/pi
     (and its internal 1/2) transferred from Gate 1 as the identical conversion;
     the 1/2's equipartition justification is inherited structurally, not
     re-derived on the moment observable. Neither flag touches discrimination (Q
     fails by 21% under any refinement). BYPRODUCT, restraint kept: g=2 produced
     mechanically and the residual LOCALIZED on the Schwinger term WITHOUT
     claiming its derivation.

  === ALPHA CHAIN: ALL THREE NAMED GATES RESOLVED OR DISCHARGED (2026-08-06) ===
  1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4 ppm. EVERY FACTOR DERIVED:
  D_E blind (W, twice-reproduced); 4 pi^3 prefactor = 4 (rectified two-component
  sampling of the force-type charge coupling, Gate 2 LINEAR) x pi^3 (located
  geometric factors); Gate 1 CLOSED (kappa=pi/4 forced from the registered
  freeze, two J0 targets hit); Gate 2 LINEAR (4/pi derived, degree-1 tether
  load) with its identification DISCHARGED out of sample by g=2. Branch
  adjudicated, residual ladder exhausted, scale-invariance theorem (no
  continuous dials). NOT alpha derived -- the honest headline is EVERY FACTOR
  DERIVED, ONE residual (+178.8 ppm) UNEXPLAINED, with two named moment-exactness
  refinements. Free byproduct: g=2 + Schwinger-localized moment residual.
  REMAINING: (a) Gate 3, the 13.6 eV second prediction (independent, open);
  (b) the V-A next-order item; (c) MOMENT-REFINE (profile-weighted moment
  integral) -- named refinement commission, below.
  MOMENT-REFINE  NAMED, NOT OPENED: compute the profile-weighted magnetic-moment
     integral on the committed solution (replacing the edge-concentrated
     current-loop idealization), to test whether the mechanical pi mu_B survives
     a distributed charge and to re-derive the 1/2 on the moment observable
     directly. Discharges the two flags on GATE 2b. Cheap (committed profile).

  GATE 3 (13.6 eV SECOND PREDICTION): PASSED (2026-08-06, VERIFIED; with a
     pre-statement sign correction owned). The derived-alpha chain pushed
     through registered mechanics reproduces the Rydberg at exactly the residual
     SQUARED: 13.600828 eV vs 13.605693 eV measured, landing -357.6 ppm = 2x the
     chain's +178.8 ppm residual (E ~ alpha^2). PRE-STATEMENT SIGN CORRECTION
     (logged per discipline): +357.6 ppm was pre-stated; actual is NEGATIVE
     because 1/alpha_chain > measured -> alpha_chain < measured -> alpha^2
     undershoots. Magnitude exactly as algebra requires; |landing|<1000 ppm
     criterion unaffected; the sign slip goes in the record. SCOPED HONESTLY:
     the consistency core (13.6 eV at (1+residual)^2) is ALGEBRA once the chain
     is consistent, NOT a new derived constant -- correctly NOT oversold. THE
     INDEPENDENT CONTENT (three pieces, not the algebra): (1) the 1/2 DERIVED
     from the virial ratio of the registered 1/d Coulomb form (EM-015) at the
     self-consistent stationary scale, exact/symbolic -- not inserted; (2)
     CONVENTION-COUNT DISCIPLINE held: binding is energy-type, so Y Brick 4's
     theorem forbids an extra rectified factor, and the competitor with one more
     4/pi misses by +27% -- confirming from a THIRD observable that the chain's
     single 4/pi lives inside e^2 and nowhere else; (3) the O-B negative
     RESOLVED not contradicted: O Phase 2's 1903x miss was the star-anchored
     constant failing to reach an alpha^2-suppressed observable, and the chain
     since built the missing structure. NET: FND-MATTER-001's atomic-coupling
     half reduces to the chain's one open number. Script:
     explorations/gate3_atomic_coupling.py.

  === ALPHA CHAIN: ALL THREE GATES RUN, EVERY FACTOR DERIVED (2026-08-06) ===
  1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4 ppm. THREE INDEPENDENT
  OBSERVABLES all consistent with ONE convention structure: the anchor J0
  targets (Gate 1, kappa=pi/4 derived), the g=2 magnetic moment (Gate 2b,
  caveat discharged out of sample, residual on the Schwinger term), and the
  13.6 eV binding (Gate 3, consistency at 2x residual + convention-count
  confirming the single 4/pi is in e^2). Gate 1 CLOSED, Gate 2 LINEAR (4/pi
  derived, degree-1 tether load, both scripts in package), Gate 3 PASSED. NOT
  alpha derived -- every factor derived, ONE +178.8 ppm residual the single
  open number. REMAINING NAMED ITEMS: (a) the V-A next-order derivation; (b)
  MOMENT-REFINE (profile-weighted moment integral); (c) THE RESIDUAL SOURCE
  itself (+178.8 ppm) -- adjudicate not scan (the residual ladder was exhausted;
  the source is physics not yet in the chain or the anchor-metrology reading's
  own limit). These three are the arc's forward queue, each a deliberate
  go-decision.

  V-A-NEXT-ORDER  NAMED, NOT OPENED: the V-A landing formula's possible
     O(Omega) boundary term, where Omega x* = pi exactly makes naive power-
     counting degenerate -- LEAD-Omega's one remaining home (channel nulled
     in-package at Z Brick 5; this is the out-of-package route). A candidate
     source for the +178.8 ppm. Mark's go-decision.

  V-A-NEXT-ORDER: MISS (2026-08-06, charter outcome 3, VERIFIED; run ONCE
     against the pre-committed bar, NO SCAN, stopped). The chartered object: the
     finite-strain correction to the landing condition's wave speed at the
     committed profile's boundary -- the O(Omega) term naive power-counting
     drops. Committed to this ONE realization before any number. RESULT: the
     committed solution's boundary strain is tiny, g^2(x*) ~ 4-5e-9 (verified
     independently: reconstruction gives 5.2e-9 vs charter's 3.8e-9, grid-edge
     difference), giving a wave-speed shift of +~5e-9 -- a coefficient of
     effectively ZERO and POSITIVE sign. The bar required -1.10 +/- 20%, correct
     (negative) sign. Outside the band AND wrong sign: MISS, registered, STOP.
     TWO OBSERVATIONS carried: (1) the miss is informative as the charter
     anticipated -- the residual ladder was already exhausted in-package, this
     was the LAST named classical candidate, and its clean failure points toward
     outcome 4 (the residual as the radiative fence, same class as the g=2
     Schwinger gap). But OUTCOME 4 IS NOT CLAIMED: a miss on one candidate is not
     a positive identification of the wall. (2) CAVEAT on the miss: "the boundary
     O(Omega) term" required choosing the concrete object; the finite-strain
     landing correction was the most defensible registered realization. A future
     session exhibiting a DIFFERENT, more fundamental boundary expansion of the
     V-A formula is a NEW commission with its own go-decision, NOT a continuation
     of this one (the commitment paragraph preserves that distinction). NOTHING
     LOST: the charter's protection held; the standing result is untouched.
     Script: explorations/va_next_order_boundary.py.

  === ALPHA CHAIN: ARC AT SUMMIT (2026-08-06) ===
  1/alpha = 4 pi^3 D_E = 137.060504, +178.8 +/- 0.4 ppm. EVERY FACTOR DERIVED
  (D_E blind; the 4 pi^3 prefactor: Gate 1 CLOSED kappa=pi/4, Gate 2 LINEAR 4/pi
  from the force-type coupling, its identification DISCHARGED out of sample by
  g=2, Gate 3 PASSED). THREE INDEPENDENT OBSERVABLES consistent with ONE
  convention structure (J0 both targets, g=2 out of sample with residual on the
  Schwinger term, Rydberg at residual^2). THE RESIDUAL: +178.8 ppm, the single
  open number, its in-package ladder exhausted, the scale-invariance theorem
  showing no continuous dials, and now the last named classical candidate (V-A
  boundary term) CLOSED by a clean miss -- the residual is increasingly cornered
  as radiative physics the classical chain does not contain (same class as the
  g=2 Schwinger gap), a CLEAN PUBLISHABLE ENDPOINT. NOT alpha derived: this
  reduces 1/alpha to one blind number times a fully-derived prefactor, pinned
  against a dial-free construction, with one residual whose remaining candidate
  is the quantum/radiative fence. Provenance documented: docs/
  HOW_ALPHA_WAS_DERIVED.md + docs/alpha_derivation_map.png + the mass paper.
  REMAINING (none load-bearing on the standing result): a positive
  identification of the wall (would require its own commission -- a miss is not
  proof); MOMENT-REFINE (profile-weighted moment); a different V-A boundary
  realization (new go-decision, not a continuation). The classical arc is at its
  honest summit.

  D-E-COMPLETE  CHARTERED (2026-08-06, NAMED-NOT-OPENED for a later go). Full
     charter: COMMISSION_D_E_COMPLETE_functional_audit.md. The strongest
     surviving classical candidate for the +178.8 ppm residual, and it SURVIVES
     Z's A2 interlock (unlike a D_E reweighting). QUESTION: is W's energy
     functional (E_el + E_rot - LOG) the WHOLE functional for R's TETHERED
     TERMINUS? The terminated winding's own core coupling energy is registered
     structure (R Phase 1) that the committed functional demonstrably does not
     contain; if the mode carries energy in that omitted channel, the true D_E
     differs from 1.1051029 for a PHYSICAL reason, not a dial. This is an
     ENERGY-type correction to an energy functional -- stays inside the quadratic
     interlock, so A2 permits it (a charge-weighted REWEIGHTING would break the
     interlock and is FORBIDDEN -- that is MOMENT-REFINE's separate job). Note:
     r_min stability and the scale-invariance theorem VARIED the functional but
     never tested its COMPLETENESS -- an untested axis. BARS (backward from
     structure, the -15 ppm terrain): Phase A STRUCTURAL AUDIT with alpha and
     179 ppm OUT OF THE ROOM (force an omitted channel from the terminus
     mechanics, derive its form and SIGN, or conclude complete); Phase B SEALED
     COMPUTE (D_E' blind, sign committed before magnitude); Phase C CONFRONT
     last. Pre-committed willingness for "functional COMPLETE" or WRONG-SIGN/
     WORSENS-to-350 ppm (both real results). NO target-hunting: a channel is
     admitted only if the audit forces it; if none is forced, STOP at complete.
     OUTCOMES: residual explained (+ second prediction) / lead / functional
     complete (residual -> wall) / wrong-sign (bounds the wall from the other
     side). Better-motivated than V-A: names a concrete registered structure the
     functional omits, not an arbitrary boundary term. Standing result untouched
     regardless. Mark's go-decision.

  D-E-COMPLETE PHASE 1: AUDIT CLEAN (2026-08-06, structural verdict; A2 numeric
     flagged script-pending). The functional-completeness hypothesis for the
     +178.8 ppm residual CLOSES at the structural level: the committed W
     functional (E_el + E_rot - LOG) is the WHOLE functional at the corpus's
     current registration, D_E = 1.1051029 stands, Phase 2 has nothing sealed to
     compute. FOUR ITEMS RULED with citations:
       A1 (tether set): the tether IS the deformation field itself -- not a
          separate omitted channel. CLEAN.
       A2 (terminus/core coupling -- the item flagged most likely OMITTED):
          instead produced the commission's useful BYPRODUCT -- the LOG
          subtraction's scope is now EXHIBITED rather than assumed. It removes
          exactly the linearized winding self-energy (the charge sector's
          divergent piece), and the kept inner-region energy vanishes with r_min
          on the actual solutions, confirming numerically what Z Brick 3's
          continuum check registered. [VERIFIED IN-PACKAGE 2026-08-06: the audit script was uploaded and re-run;
          the inner-decade kept energy vanishes with r_min (1.35e-12 -> 1.22e-13
          -> 1.35e-14 across r_min 1e-3..1e-4, ~decade-per-decade to zero),
          confirming the LOG removes exactly the linearized winding self-energy.
          The earlier proxy failure was an integration-window error on my part,
          not a discrepancy. Flag lifted; A2 clean stands on an in-package run.]
       A3 (tether Z_2): the Z_2 class is a LABEL, not an energy channel. CLEAN.
       A4 (driven rotation): J's psi=0 exactness theorem covers the terminus's
          driven rotation -- no omitted rotational energy. CLEAN.
     VERDICT: functional complete at current registration; the "number slightly
     incomplete" hypothesis is closed. Standing result untouched.
     PHASE 3 (exploration, target on table, everything stamped): the only
     structure in the residual remains LEAD-Omega's numerical fact (178.8 ppm =
     Omega x D_E to 0.4%), whose package-owned channel Z Brick 5 already nulled
     -- stays REFUSED. The one mechanically-motivated survivor is the RADIATIVE
     class, stamped LEAD-RAD below.

  LEAD-RAD  STAMPED (2026-08-06, the unique surviving physics class for the
     residual; NAMED, NOT OPENED). Every closed candidate (residual ladder, V-A
     boundary, functional completeness) points at ONE class: the rotating
     terminus RADIATES into and is DRESSED BY the medium's transverse field --
     back-reaction the STATIC committed functional cannot contain by
     construction. SECOND PREDICTION PRE-INSTALLED (the rule, and the first lead
     in this arc whose arbiter comes for free): a single radiative back-reaction
     campaign must produce TWO numbers with NO separate freedom -- (1) the alpha
     residual as a derived geometric weight times the coupling, and (2) the g=2
     moment gap, ALREADY localized at the Schwinger value to 0.3%. One mechanism,
     two known targets: either BOTH land or the lead dies. HONEST COST: a
     genuinely hard campaign, DYNAMICAL rather than static solving (the committed
     solver is static by construction). This is the stamped path forward if the
     classical arc's residual is ever pursued past its summit -- and the first
     lead whose second prediction is pre-installed rather than owed. FULL CHARTER
     now in package: COMMISSION_LEAD_RAD_radiative_backreaction.md (bars
     pre-committed: alpha out of the room during construction, derived
     coefficients only, the static result protected, the quantum fence an
     acceptable endpoint; outcomes: graduation / partial / lead-dies / quantum
     fence). Mark's go-decision; a real research program, not a brick.

  LEAD-RAD RUN COMPLETE (2026-08-09, sessions 1-2): OUTCOME 4, THE QUANTUM
     FENCE, registered constructively. Construction blind throughout (alpha
     never loaded; no confrontation occurred). Session 1 computed the mode
     kernel at beta=1 (m^(-2/3) tails, Y_nu(nu) -> -sqrt(3) J_nu(nu) invariant).
     Session 2 (explorations/lead_rad_a2a3_construction.py) built both
     observable functionals from ONE kernel (charter bar A4 held):
       A2 moment side: static subtraction does NOT cancel the divergence;
          subtracted reactive sum grows ~M^(1/3) (measured M^0.28, approaching);
          weight is cutoff-defined at power 1/3, and NO alpha-independent
          registered cutoff exists (LEAD-2 core is alpha-contaminated; W-solver
          core -> 0 per D-E-COMPLETE A2). Derived identity: radiative E-Omega*L
          = 0 per mode, so the correction is purely reactive.
       A3 energy side: finite derived weight w1 = -6.76e15 (spread 0.52%),
          normalization to the medium coupling flagged unregistered; machinery,
          not a landing.
     Second-prediction arbiter: P2 unreachable classically -> no confrontation
     licensed -> outcome 4. The residual (+178.8 ppm) and the moment gap are
     FENCED as quantum-radiative: not derived, not refuted, boundary reached
     constructively. Classical arc endpoint now audited on every side. Re-open
     condition named: an alpha-independent registered core scale makes the A2
     sum at M* = R*/r_core a zero-freedom computation. Full record:
     docs/commissions/LEAD_RAD_worklog.md sessions 1-2.

  FND-MATTER-059 (2026-08-09): THE DIRECTIONAL READING SETTLED BY THEOREM; THE
     LEVER IS DERIVED. 058's named settler run with contamination DISCLOSED
     (this session had read the target and stakes; per 058's own bar it could
     not choose the reading, so it proved no choice exists). Cauchy/Crofton:
     excluded strand length through a convex inclusion = volume x line density,
     independent of family direction -- a cylinder removes identical pi r^2 L/a^2
     from all three families (verified analytic + MC). Displacement is
     direction-blind BY GEOMETRY; the a^2/3 share is DERIVED, the a^2 face
     reading was never available. Confrontation under 058's PRE-COMMITTED rule:
     lambda = 2 (pol) x 3 (directions) x pi(r/a)^2 = 6 pi(r/a)^2 = 1.666e-5 vs
     target 1.156e-5, gap 1.44x OVER, INSIDE the 2.00x bar -> THE SECTOR'S
     FIRST DERIVED LEVER (Modeled; spend still ONE). Sign of residual
     informative: 44% overshoot, named refinement lead = compliance correction
     (thin inclusion deforms strands rather than terminating them, strictly
     reduces lambda toward target); not computed. Reversion condition: a future
     blind session faulting the geometry reverts the promotion to 058's
     near-miss. Registered FND-MATTER-059; results
     analysis/MATTER059_directional_reading_results.md; benchmark
     benchmarks/foundations/matter059_directional_reading.py.

  FND-MATTER-060 (2026-08-09): THE LEVER CASHED IN. Per-knot confrontation of
     the derived lambda = 6 pi (r/a)^2 against each knot's demanded value:
     ring 1.22x, 5_1 1.38x, trefoil 1.84x -- ALL INSIDE the inherited 2.00x
     bar, nothing re-fitted, spend still ONE. Predicted levers 29/38/32
     percent vs registered ~25. Sign structure is data: uniform overshoot,
     matching the compliance refinement's direction (recorded as pointer, NOT
     invoked -- rescue forbidden by bar). ZPE band restated: the scale
     campaign's factor 2-3 'ZPE bar' contracts to the derived worst-knot
     half-width 1.84x; downstream consumers (M-point, l_q/a window, closure
     bars) inherit a narrowed error model with unchanged inputs. NEXT BRICK
     NAMED with falsifier pre-installed: the compliance correction computed,
     which must move all three knots DOWN together and cannot land all three
     at 1.0x (the 1.51x demand spread is intrinsic -- an exact triple fit
     would be over-fitted on its face). Registered FND-MATTER-060; results
     analysis/MATTER060_lever_propagation_results.md; benchmark
     benchmarks/foundations/matter060_lever_propagation.py.

  FND-MATTER-061 (2026-08-09): THE COMPLIANCE BRICK TURNS ADVERSE-LEANING --
     THE EXCLUSION COUNT HAS NO REGISTERED FOUNDATION; 059/060 FLAGGED
     CONDITIONAL. Dual-regulator exact mode sums: the point-constraint (pin)
     cost is a regulator artifact (+0.785 sharp at every L, -> 0 smooth);
     only actual length removal is scheme-stable (dE/dl -> -Lambda^2/2pi,
     0.1%); and the REGISTERED strand model (inextensible continuous curves,
     finite smooth contact, no crossing axiom, no termination) removes NO
     length -- a thin inclusion is rerouted around with detour length -> 0.
     FORK registered: (A) fixed-routing occupation fact (not currently on
     the books) -> the count stands, lambda survives; (B) dynamic routing
     (natural FND-STRAND-001 reading) -> parametric collapse, 1.44x landing
     coincidence-class, mechanism dies in detail (consistent with 060's
     pre-installed falsifier). SETTLER NAMED, blind-barrable, existing
     machinery: strand-engine relaxation of ambient weave around a fixed
     knot, steady-state length density inside the knot volume, deficit
     threshold committed before running. Crofton theorem untouched. ZPE band
     restatement reverts if fork B. Adverse outcome was pre-named before
     computing; fork NOT adjudicated by the session that stood to lose the
     lever. Registered FND-MATTER-061; conditional flags placed on 059/060
     faces. Results analysis/MATTER061_compliance_results.md; benchmark
     benchmarks/foundations/matter061_compliance_adjudication.py.

  FND-MATTER-062 (2026-08-09): THE SETTLER RESOLVES FORK B; THE LEVER FALLS.
     Registered strand engine (FND-STRAND-001 dynamics; inextensibility as an
     EXACT projection constraint; finite smooth contact; no crossing axiom),
     fifteen anchored ambient strands relaxed around a rigid inclusion,
     density threshold committed BLIND before any step. RESULT: decisive
     exclusion geometry (Q = 0.000 vs blind 0.5) purchased ENTIRELY with
     added length (Delta L/L = +6.5e-3 free, +3.5e-3 under 8x-tight
     constraint, never negative). Invariant Delta L >= 0: rerouting financed
     by the FND-017 reservoir, removal never occurs. By 061's
     scheme-stability criterion the dilute-exclusion count has NO mechanism
     in the corpus's own dynamics. CONSEQUENCES: 059's promotion REVERTED
     (flag resolved adverse on its face), 060's propagation and the 1.84x
     ZPE band restatement reverted, lambda RETURNS TO OPEN at the unchanged
     1.156e-5 target, spend still ONE, the 1.44x landing registered
     COINCIDENCE-CLASS (sixth flattering number retired). STANDS: the
     Crofton theorem (mathematics). PATH FORWARD: the contact-modified
     dispersion successor (extensive in contact length, scheme-stable,
     power-2 preserved, one coefficient eta derivable from the registered
     contact form under blind bars -- better-founded than exclusion ever
     was: contact is registered physics, exclusion never was). THREE
     IN-SESSION CATCHES logged on the face: projection sign inversion
     (NaN-caught), failed control prediction (corrected framing recorded),
     stiff-spring integrator rejected by the length audit itself.
     Registered FND-MATTER-062; results
     analysis/MATTER062_settler_results.md; benchmark
     benchmarks/foundations/matter062_settler.py.

  FND-027 (2026-08-09): THE k/T0 CONFLICT ADJUDICATED -- CATEGORY ERROR;
     THE DEPLETION CONJECTURE LOSES ITS LAST LIMB. Bars locked first
     (definitional + binding-status criteria pre-committed). Bar 1: same
     quantity (both enter via c_L/c = sqrt(k/T0); sqrt(1.9e8) = 1.3784e4
     matches QB-008's own 1.38e4 to 0.12%); definitional escape CLOSED.
     Bar 2: EM-RECON-009's k/T0 = 2 is bound to the medium; QB-008's 1.9e8
     is by its own registration a conditional demand on QB-007's conjecture
     (never established). Bar 3: QB-008's own rung 3 (Bancal) supersedes
     the number to infinity. VERDICT: conflict DISSOLVES; one registered
     medium value, k/T0 = 2. ADVERSE BYPRODUCT: the conjecture's
     instantaneous limb (k = infinity) is incompatible with registered
     elasticity (k/T0 = 2, finite; rigid strand carries no wave) -- the
     fast-channel depletion conjecture fails on registered physics; rescue
     escapes (scale-dependent k, non-elastic channel) displayed and refused.
     CONSEQUENCES: GRV-073 withdrawal LIFTED (gamma restored); alpha
     chain's branch adjudicated safe; QB-007 anticorrelation fence higher,
     weight to the guidance-flow pricing; FND-021 B3 resolved. Face
     annotations placed on FND-021, GRV-073, QB-008. Registered FND-027;
     bars analysis/FND027_k_adjudication_bars_LOCKED.md; results
     analysis/FND027_k_adjudication_results.md; benchmark
     benchmarks/foundations/fnd027_k_adjudication.py.

  FND-028 (2026-08-09): THE DARK CHANNEL'S JOB DESCRIPTION -- three
     falsifiable commitments from the adjudicated k/T0 = 2, verified in
     benchmark before entering the predictions document. Prediction 8
     SHARPENED (committed floor c_L >= sqrt(2) c; cubic coupling committed
     at T0/2, superseding ELEC-067's grows-without-bound remark; the
     cross-sector lock with c4 = T0/8 stated as a falsifier). Prediction 26
     NEW (consistency-tier): no finite-speed Bell cutoff -- the medium's
     ceiling sits four orders below the Bell-timing floor; catastrophic
     falsifier uniquely located at ~sqrt(2) c. Prediction 27 NEW
     (scale-open): intensity-squared dark vacuum attenuation from the exact
     cubic vertex; shape and darkness committed, threshold joins the Sigma
     ledger (now priced by two independent experiments). Predictions paper
     updated (docx + PDF regenerated, validation passed, +7 paragraphs).
     Registered FND-028; benchmark
     benchmarks/foundations/fnd028_dark_channel_predictions.py.

  EM-RECON-017 (2026-08-09; registered as EM-RECON-014 in error, renamed -- ID collision with the ATLAS/Sigma claim, caught and logged): K_c DERIVED IN FORM; THE O(k) ARGUMENT
     SUPERSEDED; CORE SURVIVAL REDUCED TO ONE NAMED RATIO. Marginality
     discovered AT CHARTERING: EM-RECON-013's note (K_c = k needs k > 2T0)
     plus FND-027 (k = 2T0 exact) means K_c = k is the ZERO-CORE marginal
     case -- survival is strictly K_c > k, recorded in bars before
     computing. DERIVED: P-VOL kinematics exact (B1); the threshold's own
     definition selects the contact set as the sole load path (B2, not
     chosen); K_c = C(d0/sigma0) x Ac/a with C in [0.67, 5.06], finite at
     onset, stiffening with over-density (B3). THE SUPERSESSION: k never
     enters the contact-channel curvature under P-VOL -- the same-material
     argument routed through the wrong constant; K_c is priced by the
     contact amplitude Ac. THE CONDITION: survival iff Ac/(T0 a) > 2/C in
     [0.40, 3.00]; Ac/(T0 a) is NOT registered -- named, thresholded, left
     open (no invented calibrations). SETTLERS NAMED: (1) the nuclear
     import (NUC prices binding as rope-bundle contact; importing that
     calibration blind to this threshold decides survival with zero
     freedom -- the designated closer); (2) the standoff refinement (f_c
     narrows C to a value). EM-RECON-009's core honestly priced for the
     first time; FND-028 predictions unaffected. Registered EM-RECON-017;
     bars analysis/EMRECON017_Kc_bars_LOCKED.md; results
     analysis/EMRECON017_Kc_results.md; benchmark
     benchmarks/em/emrecon017_kc_derivation.py.

  FND-029 (2026-08-09): THE NUCLEAR IMPORT -- BOUNDED REDUCTION AND A
     CONVERGENCE. ID-collision hygiene first: the K_c claim registered
     last session as EM-RECON-014 COLLIDED with the existing ATLAS/Sigma
     claim (fail-to-search error class, FND-021's third-instance pattern);
     caught this session, renamed EM-RECON-017 with all files and
     references updated, correction logged on the claim's face. THEN the
     import: e_b = 4.716 MeV (NUC-007) vs T0 a = 0.1627 MeV (M-point),
     chain committed blind (L1 bond-to-barrier O(1) factor-3 band; L2
     multiplicity bounds [1, 500]); E_x/(T0 a) in [0.019, 87] STRADDLES
     the survival band [0.40, 3.00] -> bounded reduction, no side chosen.
     Displayed estimates (surface-line ~22, patch ~63 multiplicities) land
     INSIDE the band; numerology guard verdict on the face. THE
     CONVERGENCE: the block is the constituent width w -- the SAME
     unregistered number the QGATE tube census waits on. One measurable
     quantity now carries BOTH the hbar scale branch AND matter stability;
     tripwire extended (any w-fixing claim feeds both censuses). Open-
     problem count did not grow. Registered FND-029; bars
     analysis/NUCIMPORT_bars_LOCKED.md; results
     analysis/NUCIMPORT_results.md; benchmark
     benchmarks/foundations/nuc_import_kc_confrontation.py.

  EM-RECON-018 (2026-08-09): THE STANDOFF REFINEMENT -- BAND NARROWS
     41-FOLD. Coverage counting from the registered f_c = 0.309 gives
     w/a = 0.362; sigma0 = w by the interpenetration identification; both
     admissible standoff readings enumerated and carried (in-family
     touching 1.00, cross-family half-spacing 1.38) -> thresholds 0.395
     and 0.458 -> refined survival band [0.40, 0.46] (was [0.40, 3.00]).
     STRUCTURAL REASON: the coverage threshold's own definition (just in
     contact = at the knee) forbids the tight-standoff tail. CONVERSION:
     survival = bundle contact multiplicity m_b < 63-73 (L1 = 1; ~20-200
     with the L1 band). Point/line/patch contact geometries all survive;
     only full-cross-section fails. Estimates remain not-adopted (guard).
     EM-RECON-017 superseded to the refined band (face annotated);
     FND-029's width target sharpened an order of magnitude; the
     both-census tripwire inherits the band. Registered EM-RECON-018;
     bars analysis/EMRECON018_standoff_bars_LOCKED.md; results
     analysis/EMRECON018_standoff_results.md; benchmark
     benchmarks/em/emrecon018_standoff_refinement.py.

  COMMISSION ETA CHARTERED (2026-08-09; NAMED, NOT OPENED -- fresh-session
     go-decision). The zero-point lever's successor mechanism
     (contact-modified dispersion) chartered with bars pre-committed:
     target out of the room through construction, derived coefficients
     only, the 057 identity and 060 falsifier binding, form adjudication
     before confrontation, spend at one. The mechanism's magnitude will
     carry Ac sigma0/(T0 a) -- the SAME material ratio the survival band
     (EM-RECON-018) and nuclear import (FND-029) already census -- so
     outcome 2 (conditional) would make the constituent width a
     THREE-verdict quantity. Charter:
     docs/commissions/COMMISSION_ETA_contact_dispersion.md.
