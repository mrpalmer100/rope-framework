# COMMISSION IOTA — THE CIRCULATION QUANTUM kappa_0
## Charter (2026-08-09). Bars locked at chartering, before any computation.

## The question
EM-RECON-026 identified E = rho kappa_0 (v x zhat) with kappa_0 the per-winding
circulation quantum — the last unnormalized constant in the new electromagnetic
triad. Commission IOTA asks: **is kappa_0 fixed by registered commitments — and
if its value reduces to a registered-but-unpinned constant, what exactly is the
reduction and what numerical bound does the registry already imply?**

## Registered inputs (the ONLY allowed foundations)
- EM-RECON-014: the field<->strain energy bridge, ONE constant SIGMA = T0 n_L,
  with the registered bound SIGMA >= ~4e24-1.5e25 J/m^3 (Schwinger-form;
  ATLAS equality superseded by QGATE-007 — the BOUND is what is used).
- EM-RECON-025: the carrier (medium density rho, wave speed c = sqrt(T0/mu)).
- EM-RECON-026: E = rho kappa_0 (v x zhat); the static field of a winding.
- EM-015 / EM-RECON-026: the static force law (confrontation target).

## Pre-committed bars (LOCKED)
- I1 (provenance): only the inputs above; no constant is invented.
- I2 (two independent routes, both computed, must AGREE with no tuning):
  Route A — ENERGY: the wave's EM energy density (eps0 E^2 contributions)
  must equal its mechanical energy density under the registered bridge.
  Route B — STATIC/GAUSS: the winding's static E must satisfy the 2D Gauss
  normalization consistent with the registered force law.
  The commission PASSES only if A and B fix the SAME kappa_0 identity.
- I3 (value honesty): if the numerical value reduces to an unpinned registered
  constant, the reduction is the registered outcome — form exact, value gated,
  and the registry's existing BOUND on that constant is propagated to a bound
  on kappa_0. No number is invented to fill the gap.
- I4 (dimension audit): the full chain E = rho kappa_0 v, Gamma = q kappa_0,
  F = q E is audited end-to-end in SI dimensions; any unit-carrying step in
  the winding=charge map is stated explicitly.

## Outcome space (pre-enumerated)
1. kappa_0 fixed in closed form; routes agree; value computed.
2. Form fixed; value reduces to a named registered constant with a propagated
   bound (the expected outcome given SIGMA's status).
3. Routes disagree — registered contradiction between the bridge (014) and
   the Magnus identification (026), at full volume.

BARS LOCKED. Computation begins only after this file is saved.
