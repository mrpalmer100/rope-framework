# COMMISSION Y ADDENDUM (BRICK 3): THE PAIRING DERIVED, THE PREFACTOR
# LOCATED (2026-08-06)
# Go-decision follow-on to Bricks 1-2. Script: y3_pairing_derivation.py
# (checks C1-C4, all pass/complete). Corpus docs consulted: SYNC_STATE
# (V Phase 2 decomposition), COMMISSIONS_P_and_Q (J0 anchor provenance),
# DETECTOR_KINETICS (click law), COMMISSION_V_PHASE1_results (J = J0
# identification).

## RESULT 1 -- THE PAIRING IS DERIVED, NOT CHOSEN (C1, verified)
V Phase 1 identified the chain's observable: J = J0 is the Noether charge
and orbit ACTION of the S^1 phase, and alpha enters the chain through
J0 = hbar/(pi alpha) -- through the action quantum, never through the
mass. Therefore the correction Delta perturbs the ACCUMULATED PHASE
ACTION, whose on-shell density is the rotational sector
(Omega^2 - 2 lam Omega) f^2. First order in a profile perturbation, the
fractional action change is exactly the rotational-weight overlap -- the
P1 pairing. Verified numerically against direct evaluation of the
perturbed action functional (four independent perturbation shapes, all
agree to <1e-4 relative). The energy pairing (P3, -450 ppm) is thereby
EXCLUDED: it corrects a mass observable the chain does not use. The
pairing ambiguity flagged in the Y results is RESOLVED by derivation:
    1/alpha = 4 pi^3 D_E (1 - Delta),  Delta = 1.9387e-4  ->  -15.08 ppm.

## RESULT 2 -- THE -15 ppm IS REAL WITHIN THE CHAIN (C3)
Numerics budget: D_E spread 0.36 ppm, Delta mesh spread 0.02 ppm,
harmonic tail m>6 bounded <0.20 ppm. Total ~0.6 ppm against a 15.08 ppm
residual. The residual is physical to the chain, not numerical noise.
Menu confrontation (C4, display only, per corpus discipline): closest
closed form is alpha^2/pi at ratio 0.89 (11% off) -- noted, NOT adopted,
second prediction owed before it may even be called a lead.

## RESULT 3 -- THE 4pi DOUBLE-COVER CANNOT ALTER THE PREFACTOR (C2)
Rectified/smooth ratio over the 4pi closure equals the 2pi value,
1.2732395 = 4/pi, computed. The Z_2/double-cover worry named in the Y
charter is CLOSED for the prefactor question.

## RESULT 4 -- THE PREFACTOR AUDIT: WHERE THE 4-vs-pi CHOICE LIVES
The pi^4 decomposes over the chain as pi^2 x pi x pi:
  (a) pi^2 in R* = J/(pi^2 mu q^2 c): U's two-constraint closure geometry,
      anchored by T's ln x* = pi^2. Geometric. NOT an angular cycle
      integral of any coupling. No 4-vs-pi freedom.
  (b) pi in the pi lambda_bar_C target scale: target-side, audited clean
      by Commission E (single conversion through T0). No freedom.
  (c) pi in J0 = hbar/(pi alpha): Q's registered finding -- this factor
      was IMPORTED through the alpha-dependent anchor a0, not derived.
      This is the unique factor that is a rope-to-laboratory conversion:
      the point where the mechanical circulation (smooth quadratic
      invariant of the rotating phase, angular integral pi) is equated
      with the laboratory action quantum. IF the laboratory accumulation
      is a rectified linear sampling of the phase components (angular
      integral 4), factor (c) is a 4, and the prefactor is 4 pi^3.
The detector sector does NOT decide it: the registered click law is an
energy-threshold (quadratic) coupling, but the anchor path runs through
a0, not through the detector, so no registered claim forces OR blocks
the rectified reading. STATUS: the 4 pi^3 hypothesis is ADMISSIBLE and
sharply located (one factor, one conversion, one question: is the lab
action a rectified linear functional of the rotating phase?), but still
an import. It is now the SINGLE remaining underived element of the
entire alpha chain.

## THE CHAIN AS IT NOW STANDS
    1/alpha = 4 pi^3 D_E (1 - Delta)
  D_E = 1.1051029     computed blind (W), reproduced twice
  Delta = 1.9387e-4   derived (rectified harmonics x f4 response),
                      pairing derived (this brick), numerics tight
  4 pi^3              located at the J0 anchor conversion; rectified
                      character IMPORTED, admissible, underived
  residual            -15.08 +/- 0.6 ppm, real, unexplained
One import and one 15 ppm residual stand between the corpus and alpha.

## NAMED FOR GO-DECISION (NOT opened)
  (i) The anchor-conversion derivation: derive the angular character of
      the J0 = hbar/(pi alpha) conversion from the corpus's own
      quantization step (Q's independent-scale program is the natural
      home; the question is now sharp enough to pose there).
  (ii) The -15 ppm: hold as the chain's registered residual. The
      alpha^2/pi observation stays on the menu unadopted until a second
      prediction is named for it.
