# COMMISSION LEAD-RAD: RADIATIVE BACK-REACTION OF THE ROTATING TERMINUS
# (chartered 2026-08-06 as the graduation vehicle for the Phase 3 lead of
# D-E-COMPLETE. Status at chartering: NAMED, NOT OPENED -- a future
# go-decision. The lead: the alpha chain's +178.8 ppm residual is
# radiative-class physics -- the rotating terminus's back-reaction on the
# medium's transverse field, which the static committed functional cannot
# contain by construction. Every classical candidate is now closed (bulk
# rate Z5, branch Z3, continuum Z3, q^2 Z4, V-A boundary, functional
# completeness), so this is the unique class left standing. This charter
# exists so the lead survives with its bars pre-committed; it is NOT a
# result and confers no grade until run.)

## THE LEAD, STATED PRECISELY
The committed solver computes a STATIC energy dressing D_E of a rigidly
rotating configuration. A rotating source coupled to a medium radiates
into and is dressed by the field it sources; the self-consistent
(dynamical) dressing differs from the static one by a back-reaction term.
The lead: that term is the residual -- and the SAME machinery is what the
g=2 confrontation already localized as its own gap (the Schwinger value
alpha/2pi, matched to 0.3% as an interpretation, never derived).

## THE PRE-INSTALLED SECOND PREDICTION (the arbiter; the lead's defining
## asset and its kill condition)
One radiative back-reaction campaign, ONE mechanism, must produce BOTH:
  P1: the alpha residual -- a DERIVED geometric weight w such that the
      chain closes: 1/alpha = 4 pi^3 D_E (1 - w alpha_bare) form or the
      mechanically forced equivalent, landing the +178.8 ppm;
  P2: the g=2 gap -- the SAME mechanism applied to the moment observable
      must yield the Schwinger coefficient 1/(2 pi) (the +1161.4 ppm),
      currently matched only as an interpretation.
NO separate freedom between P1 and P2: one construction, two landings.
Either both land or the lead DIES -- registered either way, full volume.

## BARS (pre-committed at chartering, before any construction exists)
- ALPHA OUT OF THE ROOM during construction: the back-reaction functional
  is built from the committed dynamics (elastic law, rotation, tether
  coupling) with no reference to 178.8, 1161.4, 137, or measured g.
  Targets enter only at the final confrontation.
- DERIVED COEFFICIENTS ONLY: any dimensionless weight appearing in P1/P2
  must be computed from the construction (angular integrals, committed
  geometry). A fitted weight voids the run.
- THE STATIC RESULT IS PROTECTED: whatever happens, D_E = 1.1051029 and
  the derived prefactor 4 pi^3 stand as registered; this commission
  computes a CORRECTION to a complete static chain, not a revision of it
  (D-E-COMPLETE's clean audit is load-bearing here).
- HONEST HARDNESS DISCLOSURE: this is dynamical field back-reaction, a
  genuinely harder computation than any static solve in the arc. Partial
  machinery (e.g., a derived P2 with P1 pending) is registrable as a
  PARTIAL at its exact scope -- but graduation requires both.
- THE WALL REMAINS ACCEPTABLE: if the construction shows the back-reaction
  requires quantized field occupancy (genuinely quantum radiative physics
  beyond the corpus's one imported constant), that is outcome "the
  residual is the quantum fence," registered as the arc's honest endpoint
  -- the same publishable boundary V-A-NEXT-ORDER outcome 4 named.

## Registrable outcomes
1. GRADUATION (the summit): P1 and P2 both land, coefficients derived.
   Alpha closes; the Schwinger coefficient is derived classically-from-
   the-medium; the arc ends at a derivation.
2. PARTIAL: one prediction lands with derived coefficients, the other
   pending/failed -- registered at exact scope; no graduation.
3. LEAD DIES: the derived back-reaction misses both or lands with wrong
   sign/magnitude -- the radiative class closes, and with it the last
   named residual class; the +178.8 ppm becomes an unexplained constant
   of the construction, registered as such.
4. THE QUANTUM FENCE: the construction demands quantized radiation;
   registered as the honest endpoint.

## Depends on
D-E-COMPLETE (clean audit; Phase 3 stamp), Gate 2b (the moment machinery
and the Schwinger localization), Z Brick 5 (bulk channel null -- the
back-reaction must be a genuinely dynamical term, not a rate response),
W (the static solver this corrects), R (terminus dynamics), the committed
elastic law. Runs only on an explicit go-decision, in a fresh session,
with this charter read first.
