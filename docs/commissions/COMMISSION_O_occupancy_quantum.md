# COMMISSION O: THE OCCUPANCY QUANTUM
# (chartered 2026-08-05. THE hbar PURSUIT IN EARNEST. Gated on Commission
# N's registered closure -- N proved classical selection is FORBIDDEN (the
# gapless theorem forbids the soliton's mass term; the medium is
# dispersion-free; Derrick makes it exact), so occupancy quantization is a
# genuine, irreducible quantum input. This commission does not ask WHETHER
# hbar is needed -- N settled that yes. It asks whether ONE occupancy
# input, honestly imported, unifies the framework's quantum bottleneck.)

## Why this is now the right move (N cleared its path)
Before N, importing hbar competed with the hope that nonlinearity would
supply occupancy classically. N killed that hope the strong way, so
importing an occupancy quantum is no longer a failure of nerve -- it is
the demonstrated correct move at a NAMED step for a PROVEN reason. The
framework's honest pattern for irreducible constants is: measure ONE,
derive the rest (vacuum tension from ATLAS; mesh scale from the M-point).
This commission tests whether occupancy quantization is that one input
for the quantum sector -- whether a single normalization, imported once,
unifies what the QUANTUM_LEDGER identified as four separate bottleneck
inputs, now five with occupancy.

## The QUANTUM_LEDGER starting point
The ledger (docs/QUANTUM_LEDGER.md) found: 92 claims mention quantum, 11
genuinely depend on it, bottlenecking through FOUR inputs -- (1) the
electron mass, (2) spin/degeneracy, (3) the 13.6 eV atomic coupling, (4)
the induced-gravity normalization -- and conjectured that ONE
normalization might serve all four. Commission M added the fifth and
sharpest: (5) OCCUPANCY -- the designation of which mode is occupied and
stable, the step N proved classically forbidden. This commission tests
the ledger's unification conjecture with occupancy as the anchor, because
occupancy is the one input whose necessity is now PROVEN rather than
assumed.

## THE DISCIPLINE (this commission IMPORTS a quantum input -- the bars
## govern how honestly)
Unlike every prior commission in this arc, O is ALLOWED to introduce an
absolute quantum constant. The integrity is not in avoiding it (N proved
it unavoidable) but in importing exactly ONE and testing whether it
serves all five bottlenecks WITHOUT per-sector retuning. A separate
constant for each input is not unification -- it is five free parameters
and no result. The prize is ONE constant, five consequences.

## PHASE 1: WHAT IS THE MINIMAL OCCUPANCY INPUT? (define it; do not yet
## confront the five)

### The question
State the minimal additional structure that designates a star-mode (or
its nonlinear-family amplitude, N's family coordinate A) as OCCUPIED and
stable. Candidate forms, to be adjudicated on mechanics, not fit:
  - an energy cost per quantum (an action scale: E = (integer) x epsilon,
    epsilon the imported constant) selecting integer occupation;
  - an amplitude quantization (A restricted to a discrete set by a
    single imposed action/circulation quantum) -- N showed A is otherwise
    a free family coordinate, so ONE quantization condition on A collapses
    the family to a spectrum;
  - a topological occupancy (occupied modes are exactly those compatible
    with integer winding, GG-006) -- the one candidate that might be
    classical after all, and so must be checked FIRST (if occupancy is
    winding-compatibility, hbar is NOT needed and N's verdict has a loophole).

### Bars
- O1 (WINDING CHECK FIRST, target-free): before importing any constant,
  test whether integer-winding compatibility (GG-006, already classical
  and already integer-quantized) selects the occupied modes. This is the
  one route that could avoid the import entirely, it is wrong-wherever-it-
  lands, and it is asked first per the METHODOLOGY rule. If winding
  selects, O becomes a classical commission and N's verdict gets its
  loophole registered. Expected to fail (winding is a phase index, not an
  occupation number), but the check is mandatory and cheap.
- O2 (ONE CONSTANT): the imported occupancy input is a SINGLE constant
  with a SINGLE value, declared before any of the five bottlenecks is
  confronted. Not one-per-sector.
- O3 (MINIMALITY): the input is the minimal structure that produces
  discrete occupation -- the session justifies that nothing smaller works,
  and reports if MORE than one constant proves necessary (which would be
  a partial/negative result: the sector does not unify under one input).
- O4 (hbar IDENTIFICATION DEFERRED): Phase 1 defines the occupancy input
  ABSTRACTLY (its role, its units, its minimality). Whether it IS
  Planck's constant -- whether its value, once fixed by one bottleneck,
  matches hbar -- is a Phase 2 confrontation, not assumed in Phase 1.

## PHASE 2 (SEALED): DOES ONE INPUT SERVE FIVE? (the unification test)
The five bottlenecks loaded only after Phase 1 commits the input's form.

Fix the ONE constant's value by ONE bottleneck (the cleanest: the
occupancy step itself, or the electron mass), then PREDICT the other four
with NO further freedom:
  - O-A: does it give the electron mass? (the M-point calibration becomes
    a PREDICTION if occupancy fixes it independently -- the consistency
    test F1 said was the only non-circular prize.)
  - O-B: does it give the 13.6 eV atomic coupling / Bohr scale?
    (FND-MATTER-001, currently Open, would close.)
  - O-C: does it give the spin/degeneracy factor? (the 2 in 2(2l+1),
    NUC-007's capacity, M's closure rule.)
  - O-D: does it give the induced-gravity normalization? (the ledger's
    fourth input, tying the matter and gravity quantum boundaries.)
  - O-E: does its value MATCH hbar? (O4's deferred identification: if the
    constant fixed by matter-sector occupancy equals Planck's constant to
    within the grade, the framework has ACQUIRED hbar as a single measured
    input, the vacuum-tension/M-point pattern completed for the quantum
    sector.)

### Phase 2 bars
- O5 (ONE FIXING, FOUR PREDICTIONS): the constant is fixed by exactly one
  bottleneck; the other four are PREDICTIONS with no retuning. Predictions
  within F4-style bars (order-of-magnitude is the floor for "serves";
  factor-3 whisper; 25 percent promotes). Any bottleneck needing its own
  separate constant BREAKS unification and is registered as such.
- O6 (LOOK-ELSEWHERE ON UNIFICATION -- a HARD GATE, not a footnote): "one
  constant serves five" is a strong claim; the session must report how many
  of the five are independent predictions vs definitionally linked. Two
  genuinely independent hits is the threshold for a unification claim; five
  is the full result. Definitional links (inputs that are the same quantity
  wearing two names) are disclosed, not counted as independent. NAMED RISK
  from Phase 1's freeze (Fable 5): the electron mass (O-A) and the 13.6 eV
  coupling (O-B) both live in the atomic-scale problem FND-MATTER-001; if
  both are computed from the SAME star-mode action condition they are ONE
  prediction, not two, and a "five hits" report that is secretly two
  action conditions wearing five names is a FIT reported five ways, NOT
  unification. The session must exhibit the independence of O-A from O-B
  and of O-C/O-D from both, by showing the DISTINCT physics fixing each,
  before any of them is counted. This bar outranks the hbar match: a
  matched hbar value on a secretly-single condition is not the result.
- O6b (THE ZERO-POINT OFFSET, committed BEFORE the electron confrontation
  -- new seal point from Phase 1): Phase 1 froze I_mode = E/omega = m J0
  and DEFERRED the offset. The offset -- m J0 (pure Bohr-Sommerfeld ladder)
  vs (m + 1/2) J0 (ground-state offset) -- is exactly where the electron's
  absolute mass would come from or fail to, so it is the free knob that
  could manufacture O-A. It must be COMMITTED FROM MECHANICS (the mode
  boundary condition / node structure that fixes the half-integer, the
  same physics that gives it in a real cavity) BEFORE the electron mass is
  loaded, and the commitment justified, not chosen to make O-A land. A
  session that fixes the offset after seeing the electron target has
  fitted, not predicted, and the O-A hit is void.
- O7 (THE HONEST NEGATIVE): if no single input unifies -- if the sector
  needs two or more independent quantum constants -- that is registered at
  full volume as the ledger's conjecture FALSIFIED, which is itself a
  major result: it would mean the rope framework's quantum sector is
  irreducibly multi-input, and absolute masses require MORE than one
  imported constant. A clean, important negative.

## Registrable outcomes (all acceptable)
1. FULL UNIFICATION: one imported constant, fixed once, predicts the
   electron mass, the atomic coupling, the spin factor, and the induced-G
   scale, and its value MATCHES hbar. The framework acquires hbar as a
   single measured constant; absolute masses become derived consequences;
   F1's "circular" becomes "consistency test PASSED"; FND-MATTER-001
   closes. The campaign's summit -- mass from ONE honest quantum input,
   which is how real physics works.
2. PARTIAL UNIFICATION: one constant serves two-to-four bottlenecks but
   not all five. A real result narrowing which inputs are genuinely
   independent; the remaining inputs named as separate constants.
3. WINDING LOOPHOLE (O1 lands): occupancy is integer-winding
   compatibility after all, classical, no import needed -- N's verdict
   gets a registered loophole and the pursuit returns to classical
   ground. (Low prior; mandatory check.)
4. NO UNIFICATION (O7): the quantum sector is irreducibly multi-input;
   the ledger conjecture falsified; absolute mass requires more than one
   imported constant. Major negative, registered permanently.
5. CONSTANT MATCHES NOTHING KNOWN: one constant unifies the five internal
   bottlenecks but its value is NOT hbar -- the framework has its own
   quantum of occupancy, a registrable prediction (a new constant to
   confront against experiment), neither clean win nor clean loss.

## Seal and sequencing
Phase 1 (define the input, winding-check first, hbar-blind on VALUE) is
one session, self-sealed by O1-O4. Phase 2 (load the five bottlenecks,
fix once, predict four, match hbar) is a fresh session. No session runs
both phases. Gated on N's REGISTERED closure (run N Phase 2 first).

## Stopping rule
One input form, one fixing bottleneck, four predictions, one hbar match,
one registration. No per-sector constant tuning (O2/O5). A negative
(outcomes 3/4/5) prunes and is kept; O7's falsification is a full-volume
result.

## What this commission is and is not
IS: the hbar pursuit in earnest -- testing whether the framework can
acquire the quantum layer as ONE honest imported constant that unifies
the five bottlenecks and matches Planck's constant, turning F1's
circularity into a passed consistency test and closing FND-MATTER-001.
IS NOT: a derivation of hbar from nothing (N proved the classical medium
cannot supply occupancy; importing is correct, not defeat), nor a fit
(O2/O3/O5/O6 bar per-sector tuning). This is how the corpus acquired its
other irreducible constants; O tests whether the quantum sector admits
the same treatment.

## Depends on
Commission N (registered closure -- the PROVEN necessity of an occupancy
input), Commission M (the star-mode spectrum and the occupancy step),
the QUANTUM_LEDGER (the four bottleneck inputs and the unification
conjecture), F1 (the circularity theorem this could convert to a passed
test), F4 (the ratio bars), FND-MATTER-001 (the atomic-scale Open problem
O-B closes), FND-MATTER-044 (the M-point, which O-A would turn from
calibration to prediction), GG-006 (the winding O1 checks), NUC-007 (the
spin/degeneracy O-C), the induced-gravity normalization (O-D), and
SYNTHESIS_matter_boundary.md (the located wall O is chartered to cross).
