# COMMISSION Z ADDENDUM (BRICK 4): THE q^2 CONFRONTATION -- CLEAN
# NEGATIVE, LADDER EXHAUSTED, AND AN Omega-SCALE LEAD DISPLAYED AND
# REFUSED (2026-08-06)
# Script: z4_q2_confrontation.py (T1 structural, T2 symbolic, both
# complete; small-parameter table displayed after the verdict).

## VERDICT: THE q^2 SLOT CANNOT ABSORB THE RESIDUAL.
The slot would need a CONTINUOUS q^2 = 1.0001788. The registered value
set is DISCRETE with O(1) spacing: q^2 = 1 exactly (GG-006, Derived,
integer linking number, 288 downstream; the winding of the committed
ansatz verified symbolically as profile-independent integer 1), with
the nearest alternatives (q = 2: factor 4; NUC-003's unbuilt confined
sub-knot fractions: 4/9, 1/9) between 56 and 300 percent away. Topology
does not dress at the fourth decimal. The slot is exact; the residual
survives.

## THE RESIDUAL LADDER: EXHAUSTED
Brick 2 named three candidates; all three are now closed by
computation: the k/T0 branch (Brick 3, other branch off by 17 percent),
the r_min continuum limit (Brick 3 side effect, 0.4 ppm), and the q^2
slot (this brick, structurally impossible). The chain's value
    1/alpha = 4 pi^3 D_E = 137.060504,  +178.8 +/- 0.4 ppm
is the IRREDUCIBLE residual of the committed construction. Nothing in
the registered machinery can move it. Whatever explains it is physics
the chain does not yet contain -- or, if the anchor gate fails, the
chain's falsifier.

## THE OBSERVATION (displayed and refused, per the second-prediction rule)
Confronting the residual against the chain's OWN small parameters:
    delta = 1.788e-4;  Omega x D_E = 1.796e-4;  ratio 0.9957.
The residual equals Omega x D_E to 0.4 percent -- and Omega = pi/x* is
the construction's own rotation rate, its native small parameter. A
first-order-in-Omega correction to the chain (the finite-rotation next
order of the construction: the committed formulas are the leading order
of an expansion whose next term is O(Omega)) is a mechanically natural
home, and unlike every closed form refused so far, this one names its
own derivation route. REFUSED for adoption: it is registered as LEAD-Omega
with its graduation condition stated -- the coefficient (is it D_E? is
it D_req? the 0.4 percent gap between those two readings is itself the
discriminator) must be DERIVED from the construction's O(Omega)
expansion with alpha out of the room. A derived coefficient landing at
1.10 graduates the lead and closes alpha to sub-ppm; anything else
kills it cheaply.

## THE ARC, END STATE
    1/alpha = 4 pi^3 D_E (1 + O(Omega)?)
  D_E blind and twice reproduced; kappa = pi/4 uniquely selected with
  its metrology reading identified; the branch adjudicated (FND-021 ->
  k/T0 = 2, conditional); Delta honestly retired; the residual ladder
  closed by three computations; and the residual now carrying one
  refused-but-named lead at the chain's own expansion scale.
Open gates, unchanged: the anchor verification (at merge, decides
kappa's derivation), the 13.6 eV second prediction, and now LEAD-Omega's
coefficient derivation -- the first candidate explanation of the 179 ppm
that the construction itself can adjudicate.
