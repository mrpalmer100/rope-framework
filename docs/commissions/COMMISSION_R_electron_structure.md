# COMMISSION R: THE ELECTRON STRUCTURE
# (chartered 2026-08-05. The campaign's named successor programme, and its
# PROVEN common root. Every road -- F1/F4 on mass, L/M/N on selection, O on
# occupancy, P on degeneracy, Q on scale -- converged on ONE unbuilt
# object: the electron's internal structure (PM-005: mass an input,
# structure unbuilt). This is not one more commission; it is the keystone
# the whole arc identified, and G, hbar, and the absolute masses are proven
# to reduce to it.)

## The proof that this is the root (assembled from the campaign)
- F1: the electron ABSOLUTE identity is circular against the calibration;
  an independent structure is what would break the circularity.
- F4: the m_p/m_e ratio is blocked by the proton, but the electron is the
  ratio's OTHER pole and must be a definite structure to form it.
- P: the spin-degeneracy 2 lives IN the electron's spin structure, not in
  the photon's polarization doublet -- getting it out requires the model.
- O: the occupancy quantum lands ON the electron mode (J0 = hbar iff the
  electron confines at pi lambda_C).
- Q: hbar is not determinable because every scale re-spends m_e or hbar;
  the reduction traces to the particle-mass frontier.
- GRV-006: G is not derivable because it reduces to (m_p/M_Pl)^2 -- the
  SAME particle-mass frontier.
- QGATE-010 / the spin-meter: the electron's spinor-equivalence is OPEN
  and FALSIFIABLE (non-spinor -> (1,3)-negative photon quartic vs spinor
  (4,7)-positive, at the measurable 1e-23 scale).

CONCLUSION: G, hbar, and the absolute masses are ONE irreducible knot
whose core is the electron structure. Building it is the only move that
touches all three. This commission is the campaign's true north.

## The specification the corpus already wrote (the acceptance test EXISTS)
The corpus has, without building the electron, already specified what a
correct electron model must satisfy -- QGATE-010/011 turned the IOU into a
CONTRACT. The electron model must be:
- ONE-LOOP-EQUIVALENT to a minimally-coupled spin-1/2 fermion in its
  O(F^4) photon response (the (4,7)-positive quartic; else the framework
  PREDICTS a polarimetry anomaly QED does not, at VMB@CERN sensitivity);
- charge = integer winding (GG-006, Derived, must be preserved);
- consistent with the massless-photon carrier channel-map (the electron
  is the lightest charged matter the photon quartic fingerprints);
- its mode, under the occupancy quantum J0 (Commission O), must confine at
  a scale that -- IF the model is right -- makes J0 = hbar (the pi lambda_C
  condition), which is the CONSISTENCY the whole hbar sector waits on.

This is a sharper acceptance test than the sector has ever had. R does not
start from nothing; it starts from a contract.

## THE DISCIPLINE (this is a CONSTRUCTION, the hardest kind, PM-005-gated)
Unlike the audits and confrontations of F-through-Q, R is a genuine
build: it proposes an electron structure and tests it against the
contract. The failure mode is the one the whole corpus guards against --
building a structure tuned to pass the tests rather than forced by
mechanics. The bars enforce that the structure's FEATURES come from
registered strand mechanics, and that its successes are PREDICTIONS
against the contract, not fits to it.

## PHASE 1: WHAT IS THE ELECTRON, STRUCTURALLY? (propose from mechanics)

### The question
Propose the electron's internal structure as a configuration of the
registered strand medium (NOT a knot -- PM-004 killed the knot-excitation
route; NOT a static soliton -- N proved those forbidden; the live
candidates are a WINDING TERMINUS, F2's unrun hypothesis, or a
topological defect of the two-strand rope carrying the screw generator).
The structure must:
  (i)   carry charge = integer winding (GG-006);
  (ii)  possess a TWO-STATE internal degree of freedom that is the spin
        (P's requirement: the 2 must come from here, and it must connect
        to spin-statistics correctly, which is where the model earns or
        fails its spinor-equivalence);
  (iii) have a definite size/confinement scale from mechanics (the scale
        O's occupancy quantum acts on).

### Bars
- R1 (FEATURES FROM MECHANICS): charge, spin, and scale each trace to a
  registered mechanical fact (winding for charge; a named internal mode
  for spin; an equilibrium condition for scale), not to "what reproduces
  the electron." Each feature's provenance is exhibited.
- R2 (NOT A KNOT, NOT A SOLITON): the proposal must respect PM-004 (no
  knot-excitation spectrum spans the leptons) and N (no classical
  localized soliton) -- the structure is a topological/winding object or a
  driven configuration, not an excluded class. If the proposal reduces to
  an excluded class, it is void.
- R3 (SPIN IS THE HARD BAR): the two-state internal DOF must be shown to
  behave as spin-1/2 -- the correct rotation and exchange properties --
  which is where P's spin-statistics gap must be crossed. A two-state DOF
  that is not spinorial does not supply the electron's spin, and the model
  must confront the spin-meter: is it (4,7)-positive (spinor-equivalent)
  or does it PREDICT the polarimetry anomaly? Either is registrable; a
  model that cannot say which is incomplete.
- R4 (SEALED FROM THE MASS VALUE): Phase 1 proposes the structure and its
  qualitative features; it does NOT fit the electron mass or 1836. Whether
  the structure's scale and mode energy reproduce m_e (via O's occupancy
  quantum) is a Phase 2 confrontation.

## PHASE 2 (SEALED): DOES THE STRUCTURE PAY THE CONTRACT?
Targets (m_e, the (4,7) quartic, the pi lambda_C confinement) loaded only
after Phase 1 commits the structure.

- R-A: does the structure's mode energy, under O's occupancy quantum,
  reproduce m_e -- AND does its confinement scale land at pi lambda_C,
  closing J0 = hbar (the consistency the hbar sector waits on)? This is
  the payoff: if the electron structure confines at pi lambda_C from
  MECHANICS, Q's circularity is broken (the scale is now derived, not
  m_e-calibrated), and hbar becomes determined.
- R-B: does the O(F^4) photon quartic come out (4,7)-positive (spinor-
  equivalent, matches QED) or otherwise (a predicted polarimetry anomaly)?
- R-C: does the structure, as the ratio's electron pole, combine with any
  proton candidate (F4's gate) toward 1836?

### Phase 2 bars
- R5 (PREDICTIONS AGAINST THE CONTRACT): each of R-A/B/C is a prediction
  of the committed structure, not a fit. The mass, the quartic sign, and
  the confinement scale are computed from the structure's mechanics and
  compared; F4-style bars.
- R6 (THE hbar CLOSURE IS THE PRIZE, and it is CONDITIONAL): if R-A lands
  pi lambda_C from mechanics, state explicitly that this BREAKS Q's
  circularity -- the confinement scale is now derived, hbar is determined,
  and the reduction chain (hbar and G to the particle-mass frontier)
  bottoms out in a built object. This is the campaign's summit and it must
  be claimed only if the scale is genuinely mechanical, not reverse-fit
  from hbar.

## Registrable outcomes (all acceptable)
1. THE ELECTRON BUILT: a mechanically-forced structure carries charge,
   spin (spinor-equivalent), and a confinement scale that reproduces m_e
   and lands pi lambda_C -- closing hbar, breaking Q's circularity, paying
   the contract. The campaign's summit; G, hbar, and mass bottom out in a
   built electron. (Low prior, maximum value.)
2. PARTIAL STRUCTURE: the structure carries charge and a scale but the
   spin (R3) requires the quantum spin-statistics input -- the electron is
   built up to its spinorial nature, which stays a quantum input. Narrows
   the keystone: what remains irreducibly quantum is spin-statistics
   itself, the sharpest possible location.
3. SPINOR ANOMALY PREDICTED: the structure is non-spinor, giving the
   (1,3)-negative quartic -- a FALSIFIABLE PREDICTION against VMB@CERN
   that would KILL the framework if the anomaly is absent (the spin-meter's
   standing bet). A real, dangerous, registrable prediction.
4. NO FORCED STRUCTURE: no mechanically-forced structure satisfies the
   contract without tuning; the electron stays an input (PM-005 stands),
   and the keystone is confirmed unbuilt-and-hard. The honest floor:
   G, hbar, and mass remain one irreducible knot, now with the electron
   commission's failure characterizing why.

## Seal and sequencing
Phase 1 (propose structure, features from mechanics, mass-blind) is one
session. Phase 2 (load the contract, confront) is a fresh session. This
is the campaign's deepest commission and may itself spawn a sub-programme;
it is chartered as the named successor, not expected to close in one arc.

## Depends on
PM-004 (knot route killed), PM-005 (the fence this attacks), F1/F2/F4
(the identity findings; F2's winding-terminus is the live candidate),
Commissions L/M/N (the selection no-gos that constrain the structure),
Commission O (the occupancy quantum the structure's scale meets),
Commission P (the spin-degeneracy requirement), Commission Q (the
circularity this could break), GG-006 (charge = winding), GRV-006 (the G
reduction to the same frontier), QGATE-010/011 and ELEC-053 (the
spin-meter contract and the (4,7) acceptance test), EM-RECON-012/GRV-020
(the screw generator the structure may carry), the carrier channel-map,
and SYNTHESIS_matter_boundary.md (the located wall and the keystone
finding).
