# COMMISSION F: WHAT IS AN ELECTRON? (four sub-commissions)

## Standing evidence that the electron is not a knot
- PM-004 (Failed, kept): no untuned knot/soliton excitation spectrum spans
  the lepton ratios; natural soliton spectra are O(1)-spaced.
- FND-MATTER-006: the solver's own ratios (trefoil/ring = 5.36) are
  O(1)-spaced; m_p/m_e = 1836 requires three orders. If the proton is
  knot-like, the electron is structurally something else.
- QB-004 (Failed): the one-loop fluctuation mass mechanism missed by ~80x.

The corpus has never registered a positive identity claim for the
electron. These four sub-commissions each pose one, with a test. They may
run in parallel; their verdicts are independent; none may cite another's
result as support.

Global rule for all four: the M-point calibration (FND-MATTER-044) USED
m_e to fix a. Any candidate identity that merely re-derives the M-point
identity is CIRCULAR and must be registered as such. The prize outcome in
every sub-commission is an expression for m_e that is INDEPENDENT of the
calibration identity, turning the corpus's one spent calibration into a
consistency test. Each session must exhibit the dependency path proving
independence or declare circularity; "close to m_e" with an unexamined
path is not registrable.

---

## F1: THE THRESHOLD STAR
Hypothesis: the electron IS the minimal star-convergence at the onset of
tangibility (FND-MATTER-004's coverage threshold), and m_e c^2 is the
energy of that threshold configuration.

Task: write the threshold-star energy in registered quantities
(T0, a, N = f_c (R/a)^2, plus whatever the convergence geometry forces)
and evaluate it at the M-point values.

Bars: (B1) circularity check FIRST: exhibit whether the resulting
expression is or is not the calibration identity rearranged. (B2) no
free O(1) coefficients selected by the outcome; every coefficient must be
forced or displayed-and-refused per the MATTER058 protocol. (B3) if the
energy comes out at mesh scale (~GeV or above), the miss is registered
plainly and the hypothesis fails on magnitude, joining the MATTER050
hierarchy statement.

## F2: THE WINDING TERMINUS
Hypothesis: the electron is the endpoint defect where net winding
(= charge, GG-006, Derived) terminates; its mass is the twist self-energy
of the termination, of order lambda tau0^2 times a localization length,
with the healing length xi the natural candidate.

Task: pose the termination as a variational problem in the registered
energetics (T0, mu, lambda, k), solve for the localization length rather
than assuming xi, and evaluate the rest energy.

Bars: (B1) circularity check first, and SHARPENED by F1's result
(2026-08-05): F1 proved any electron energy reducing to
(dimensionless) x T0 x a is circular against the M-point calibration by
identity. F2's named escape quantities are NOT all off the calibration
surface -- xi = sqrt(lambda/T) is itself calibrated to the atomic scale
R (FND-MATTER-005, verbatim: "xi is calibrated to the atomic scale R,
circular"). So xi lives on a DIFFERENT calibration surface, not off all
of them. B1 must therefore trace xi's provenance SPECIFICALLY: an F2
result routed through xi inherits a second calibration dependency and is
circularity wearing a different letter, NOT escape. Escape requires the
result to route through quantities genuinely unpinned by any spent
calibration (candidates: tau0, mu, k, and lambda ONLY where it does not
re-enter through xi). The B2 variational solve for the localization
length is the intended escape mechanism precisely because a DERIVED
length is off both surfaces; if the solve returns xi, the result is
circular and B1 catches it at the provenance trace, not after.
(B2) the localization length must be an OUTPUT of the variational
problem; if the problem has no stable minimum, that kills the hypothesis
and is the registered finding. (B3) the charge quantization of the
terminus must survive: if the construction breaks winding integrality,
it is void.

## F3: THE COLLECTIVE GAP
Hypothesis: the electron mass is not the rest energy of a localized
structure but the gap of a collective mode of the weave, so that "the
electron-mass problem" and "the lambda suppression problem" are the same
hierarchy problem: mesh-scale energy times a small geometric fraction.

Task: (i) register the structural observation as its own claim: the
recurring grammar across sectors (lambda ~ 1e-5 here; the ~40-order
coupling suppression in gravity, GRV-005/006; the Quantum Ledger's single
normalization serving four inputs) with the corpus's evidence for and
against reading them as ONE suppression. (ii) Pose the sharpest test the
corpus affords: if one mechanism sets both, the RATIO of the two
suppressions is a derived pure number; state what each sector's registered
values demand of that ratio and whether any registered geometry supplies
it.

Bars: (B1) this sub-commission is explicitly a synthesis-and-reframing
claim (the QUANTUM_LEDGER style), registered Modeled at best; it may not
promote anything. (B2) any candidate ratio is subject to the MATTER055
look-elsewhere discipline: the candidate space is enumerated BEFORE any
value is computed, and one hit among several candidates is registered at
whisper grade with the caveat at full volume.

## F4: THE RATIO-FIRST TARGET
## (ELEVATED 2026-08-05 after F1: from "highest-value" to LOAD-BEARING
## for the entire identity programme.)
Hypothesis-free program: whatever the electron is, the corpus's sharpest
available SCALE-FREE number is m_p/m_e = 1836.15267. All absolute scales
cancel in a ratio; this is the cancellation FND-MATTER-006 was built for,
pointed at the most consequential target it has never been aimed at.

WHY F4 IS NOW LOAD-BEARING, not merely high-value (F1's gift): F1 proved
that EVERY absolute-energy electron identity reducing to
(dimensionless) x T0 x a is circular against the M-point calibration by
identity -- zero evidential weight by construction. In a RATIO, the T0 x a
combination appears in both numerator and denominator and cancels
identically, so the circularity theorem's premise (an absolute energy on
the calibration surface) is INAPPLICABLE. F4 is therefore not just
scale-free; it is the ONLY F-sub-commission whose success can carry
evidential weight while the calibration stands. F1, F2, F3 can each at
best convert the one spent calibration into a consistency test (and F1
showed even that requires a forced constant none of them supplies); F4
can produce a genuinely NEW derived number. This is the programme's one
route to a mass result that proves something, and it inherits nothing
from the a-dispute.

Task: (i) fix, from registered claims only, what structure the corpus
assigns the proton (composite knot per NUC series) and compute its mode
energy / ropelength on the solver at the best available grade;
(ii) for EACH electron identity F1-F3 that survives its own bars, compute
the corresponding electron energy in the same units; (iii) form the ratio.
NOTE on (ii) post-F1: F1 FAILED (no forced construction), so the
threshold-star electron is not a surviving identity to pair -- BUT its
geometric configurations are computed and registered, and the N=1 chord
configuration landed at 1.145x the electron target. This is not a
surviving identity, but it IS a registered geometry, and it feeds the
cross-check bar below.

Bars: (B1) the pre-committed comparison bar is declared here, before any
computation: a hit within a factor of 3 is registrable at whisper grade
given solver resolution (FND-MATTER-006's honest 3-percent-grade caveat
compounds through the ratio); within 25 percent is the promotion bar,
requiring the refined solver. Order-of-magnitude misses close the pairing.
(B1-CROSS, added 2026-08-05) THE MUTUAL-GEOMETRY CROSS-CHECK: F1's
closest refused landing was the N=1 chord threshold-star at 1.145x. If
F4's proton structure, computed independently, forms a ratio against that
SAME N=1 configuration that lands near 1836, the two results reinforce:
a mutually consistent electron-and-proton geometry is a DIFFERENT
evidential object than either alone, because the shared N=1 choice is
then forced from two directions rather than selected once. This
cross-check runs ONLY IF F4 lands on some electron geometry; it is a
bonus consilience test, explicitly NOT a licence to search over electron
configurations until one pairs with the proton (that would re-open the
F1 look-elsewhere landscape from the other end). The N=1 configuration
is named HERE, before F4 computes, so it cannot be chosen post hoc.
(B2) one ratio per surviving identity, no mixing, no post-hoc identity
selection: if two identities both land, BOTH are registered and the
look-elsewhere caveat applies at full volume. (B3) no absolute quantity
may be consulted anywhere in the chain; the computation is dimensionless
end to end, and the session must exhibit that.

---

## Sequencing and seals
F1, F2, F3(i) are single-session, self-sealed by their circularity-first
bars. F3(ii) and F4 receive their targets only after the constructions
commit (two-session seal each, the standard split). No sub-commission may
be run by a session that has performed another sub-commission in the same
arc; the identities must not negotiate with each other.

## Stopping rule
One pass per sub-commission. Failures are keepers (they prune the identity
space, which is the point). If all four fail, the registered synthesis is
itself valuable: the electron joins a with a characterized identity-space
exclusion map, the matter sector's analogue of the GRV-013 channel map.

## Depends on
PM-003, PM-004, PM-005, QB-004, GG-006, FND-MATTER-001 through 006,
FND-MATTER-044/049/050/055, NUC-001 through 009, GRV-005/006,
QUANTUM_LEDGER (docs/QUANTUM_LEDGER.md).
