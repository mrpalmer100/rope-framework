# COMMISSION X: THE J = J0 BOOKKEEPING RESOLUTION
# (chartered 2026-08-06. The honest next question after W falsified LEAD-2.
# NOT an alpha attempt -- the necessary FOUNDATION any future alpha attempt
# needs. W's validated solver produced a real cross-check, the pi^3/2 unit-
# fix, and a real tension. Resolving the tension either CONFIRMS J = J0
# (making O's J0 = hbar/(pi alpha) a hard constraint on the exact quantity
# where alpha now provably lives) or BREAKS it (separating the two
# campaigns). Either is decisive for what comes next.)

## Where the campaign actually stands (stated honestly)
- LEAD-2 is DEAD: W's validated EL-BVP solver computed the mechanical
  dressing D_int = 1.1051 (5 configs, 7-digit agreement, gate PASS), missing
  1.40681 by 21%. "D = 1.40681 = mass dressing mu/m_e" is falsified by direct
  computation. The mesh hit was coincidental to the wrong reading.
- alpha is NOT derived. It is LOCATED: 1.40681 = R*/(pi lambda_bar_C) is a
  LENGTH ratio set by the absolute magnitude of J at the occupancy rung,
  confirmed from three sides (O's located-not-closed, U/W's construction, the
  pi^3/2 cross-check).
- WHAT SURVIVES and is worth pressing: the pi^3/2 unit-fix. Under J = J0, the
  solver energy unit is fixed to m_e c^2 (a nontrivial dimensional closure,
  D-cancels, verified), which is real evidence toward J = J0. But it comes
  with a tension that must be resolved before J = J0 can be trusted.

## THE TENSION (the precise question X resolves)
Under the pi^3/2 unit-fix (J = J0 => solver energy unit = m_e c^2):
  E_rot = pi^3/2 (solver units) = pi^3/2 x m_e c^2  ... but that is 15.5 m_e c^2,
  and the dressed TOTAL = D_int x pi^3/2 = 1.105 x 15.5 = 17.13 m_e c^2.
A physical electron has rest energy 1 x m_e c^2, not 17. So EITHER:
  (a) the physical rest mass is NOT the total field energy but a SPECIFIC
      sector or combination (e.g. the rotational sector alone, or E - omega J
      the Routhian, or a virial-reduced piece), and the factor 17.13 is a
      bookkeeping artifact of counting the wrong energy as rest mass; OR
  (b) J is NOT J0 -- the unit-fix that made pi^3/2 land was a coincidence,
      and the two campaigns do not close at the energy level.
X decides which.

## PHASE 1: THE ENERGY BOOKKEEPING (what is the rest mass, mechanically?)

### The question
For the self-consistent rotating terminus at fixed circulation J, WHICH
energy is the physical rest mass m_e c^2? Candidates, adjudicated from
mechanics (not chosen to give 1):
  - the total energy E (gives 17.13 under the unit-fix -- the naive reading);
  - the Routhian R = E - omega J (the energy at fixed circulation, the
    correct thermodynamic potential for a rotating constrained system --
    this is the standard object and the first thing to compute);
  - a virial-reduced piece (if the elastic and rotational sectors satisfy a
    virial relation, the rest mass may be a specific combination);
  - the rotational sector alone (E_rot = pi^3/2 unit; needs a reason the
    elastic self-energy does not gravitate as rest mass -- unlikely but
    checked).

### Bars
- X1 (ROUTHIAN FIRST): compute R = E - omega J on the converged solver
  profile. This is the mechanically-correct energy for a system held at
  fixed circulation, and it is the leading candidate for the rest mass. If
  R (in the pi^3/2 unit) = 1 m_e c^2 within grade, the tension resolves and
  J = J0 is CONFIRMED at the energy level. Compute it.
- X2 (NO TARGET-TUNING): the rest-mass identification is chosen by MECHANICS
  (which potential is conserved/stationary for this constrained system), not
  by which one gives 1. State the mechanical reason before evaluating.
- X3 (BOTH BRANCHES): the k/T0 = 2 branch is where the solver converges;
  carry the high branch if it converges too.
- X4 (THE VERDICT IS BINARY AND EITHER WAY IS VALUABLE): X either CONFIRMS
  J = J0 (a bookkeeping choice grounded in mechanics makes the rest mass come
  out right) or FAILS to (no mechanically-justified energy gives 1, so J != J0
  or the unit-fix was coincidence). Do not force confirmation; a clean break
  is as valuable -- it separates the campaigns and redirects.

## PHASE 2 (only if J = J0 confirmed): THE HARD CONSTRAINT
If X confirms J = J0, then O's J0 = hbar/(pi alpha) becomes a HARD CONSTRAINT
on the circulation J whose absolute magnitude sets R*/(pi lambda_bar_C) =
1.40681. State precisely what that constraint is and whether it, combined
with the solver's now-fixed energy unit, over-determines anything about the
occupancy rung -- i.e. whether confirming J = J0 hands the NEXT (occupancy)
commission a new equation it did not have. This is the bridge from X to any
future alpha work; X does not attempt alpha itself.

## Registrable outcomes
1. J = J0 CONFIRMED: a mechanically-justified energy (most likely the
   Routhian E - omega J) gives the rest mass = 1 m_e c^2 within grade under
   the pi^3/2 unit-fix. The two campaigns close at the energy level; O's
   J0 = hbar/(pi alpha) becomes a hard constraint on the occupancy-rung
   quantity where alpha lives. The foundation for any alpha attempt is laid.
2. PARTIAL: a candidate energy is close but not within grade; the
   identification is likely but carries a stated residual (solver grade or a
   missing virial term). Registered; the residual named.
3. J = J0 BROKEN: no mechanically-justified energy gives 1; the unit-fix was
   coincidence; the circulation and occupancy quanta are NOT the same object
   at the energy level. The campaigns separate; the electron scale (U's J)
   and the occupancy (O's J0) are independent unknowns, and the pi^3/2
   agreement is downgraded to numerology. A clean, important negative.

## Seal and sequencing
Phase 1 (compute the Routhian and the candidate energies on the converged
solver profile) needs the working-zip solver (W's phase1c). One session.
Phase 2 (the hard constraint) only on confirmation. Blind discipline is
light here (this is bookkeeping on an existing converged solution, not a new
prediction), but X2 requires the rest-mass identification be justified from
mechanics before it is evaluated, so "which energy gives 1" cannot be reverse
-picked.

## What this is
The resolution of whether the electron's sizing circulation J and the
occupancy quantum J0 are one object at the level of the ENERGY bookkeeping,
not just the units. It is the honest next step: W killed the alpha lead but
left a real cross-check (pi^3/2) with a real tension, and X resolves the
tension either way. Confirming J = J0 is the foundation any future alpha
attempt requires; breaking it redirects the search. X does NOT attempt alpha.

## Depends on
Commission W (the validated phase1c solver, D_int = 1.105, the pi^3/2 unit-
fix and its tension), Commission U (the two-constraint closure, J), Commission
O (J0 = hbar/(pi alpha), the occupancy rung), Commission V (the J = J0
provenance match, earned structurally -- X tests it at the energy level),
Commission T (the pi^2 anchor), SYNTHESIS_matter_boundary. Needs the working
-zip solver.
