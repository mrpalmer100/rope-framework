# COMMISSION Y ADDENDUM (BRICK 4): THE ANCHOR-CONVERSION DERIVATION
# (2026-08-06)
# Go-decision follow-on to Brick 3 item (i). Script:
# y4_anchor_conversion.py (P1 symbolic, P2 numeric-scaling, P3 exact,
# all pass). Registered structures used: HBAR-001's surviving action
# form S = pi T A^2/(2c) (HBAR_SECTOR_CLOSURE, explicitly kept), V Phase
# 1's identification of J as the S^1 Noether charge, the committed
# solver's J = Omega int f^2 dA constraint structure. Alpha, 137, and
# 4/pi were used only in the post-derivation bookkeeping check.

## VERDICT: THE J0 STEP IS PROVEN SMOOTH-QUADRATIC ON BOTH SIDES.
## THE 4/pi CANNOT LIVE THERE. ITS ONLY ADMISSIBLE HOME IN THE CHAIN IS
## THE CHARGE FUNCTIONAL INSIDE THE ANCHOR a0 -- THE ONE LINEAR-RESPONSE
## OBSERVABLE THE CORPUS HAS NOT YET CONSTRUCTED.

## P1 -- THE LAB SIDE (derived, symbolic)
HBAR-001's registered action form S = pi T A^2/(2c) is a smooth quadratic
cycle integral: for a harmonic degree of freedom, oint p dq = pi m w A^2
with the pi arising exactly as int_0^2pi sin^2 = pi (the rectified
alternative int |sin| = 4 gives a different, wrong form), and
oint p dq = 2 pi E/omega exactly -- the same smooth E/omega relation O's
I_mode = E/omega froze. The laboratory action convention in the corpus's
own registered chain is quadratic. No rectified reading exists on the
lab side.

## P2 -- THE ROPE SIDE (derived, registered construction)
V's J is the S^1 Noether charge, realized in the committed solver as
J = Omega int f^2 dA and CONSTRAINED to J_T through int f^2 -- quadratic
in the field by registered construction (verified: J scales as amplitude
squared; a rectified linear circulation scales linearly and admits no
amplitude-independent normalization onto J_T). No rectified reading
exists on the rope side.

## THE THEOREM
The J0 = hbar/(pi alpha) conversion equates two smooth quadratic
invariants. Therefore the 4/pi cannot arise in the J0 step, under any
reading, from any registered structure. Brick 3 located the freedom at
the anchor conversion; this brick CLOSES the action half of that
conversion and forces the relocation: within the anchor
a0 = lambda_bar_C/alpha, alpha carries e^2, and the CHARGE coupling is
linear in field amplitude -- the unique linear-response observable
anywhere in the chain. A linear coupling sampling a rotating source
rectifies; that is the one mechanical site where an angular integral 4
can replace a pi.

## P3 -- LOCATION BOOKKEEPING (consistency, not construction)
One rectified two-component sampling applied once to the charge-coupling
observable -- e^2_lab = (pi/4) e^2_rope -- produces exactly one factor of
4/pi in 1/alpha, first power, correct direction: pi^4 -> 4 pi^3.
The bookkeeping is consistent. The charge functional itself (the
electric-response integral over the same committed profile, the
reviewer's route 3) remains UNCONSTRUCTED, and its construction is now
the single load-bearing open item of the entire alpha chain.

## WHAT CHANGED IN THE CHAIN'S STATUS
Before this brick, the 4 pi^3 prefactor was an import with an unknown
home. Now:
  - the action steps of the chain are DERIVED quadratic end to end
    (P1, P2, plus Brick 3's pairing and Brick 1's gate);
  - the import is localized to ONE object (the charge functional) with
    ONE property to derive (rectified linear sampling of the rotating
    source);
  - and the property is mechanically natural for that object in a way
    it provably is not for any other object in the chain.
The chain: 1/alpha = 4 pi^3 D_E (1 - Delta), residual -15.08 +/- 0.6
ppm, with every factor derived except the rectified character of the
charge conversion, now pinned to a named, constructible functional.

## NAMED FOR GO-DECISION (NOT opened)
THE CHARGE FUNCTIONAL COMMISSION: construct the electric-response
integral I_Q[f] from the committed profile (the winding's directed
tether load, per the corpus's carrier/polarization dictionary), exhibit
whether its coupling to the rotating phase is linear (rectifies, 4/pi
derived, prefactor falls) or quadratic (no rectification, 4 pi^3 dies
and the -21% wall returns). Second-prediction family attached: the same
functional must reappear in the magnetic-moment observable. This is a
falsifiable, chartered-shape question -- the last one.
