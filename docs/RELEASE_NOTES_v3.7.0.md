# Release v3.7.0 — The ring's quantum, and the detector that remembers (3 August 2026)

**477 registered claims (104 Derived, 335 Modeled, 4 EFT-constrained,
4 Conjecture, 5 Open, 25 Failed-and-kept); 459 code-backed, all passing.**

Fifteen claims across two arcs. The gravity arc (GRV-092..095) closes the
ring-quantum question and forces the substrate fork. The strand arc
(FND-STRAND-009..019, eleven sessions in one campaign) takes the
measurement chain's last generic ingredient — the derived gapped bath —
and interrogates its kinetics until the interrogation resolves into a
single sentence about detector click statistics. One benchmark repair
(GRV-086) retires a platform-fragile bar with the retirement argued on the
claim's face.

## Arc I — the ring's quantum and the fork (GRV-092..095)

- **GRV-092 — the fourth cancellation.** Snap energy and local ring
  frequency both scale as 1/sigma, so the snap ACTION is depth-blind and
  mass-blind — a universal medium constant the horizon chain produced
  unasked. Its ratio to hbar: each snap is sub-quantum; the whisper is
  CLASSICAL gravitational radiation at leading order — waves, not
  gravitons.
- **GRV-093 — one form, two areas.** The cross-registry action audit finds
  tonight's combination already living in the registered hbar relations:
  the gap becomes an equation, numerically closed with no tuning against
  GRV-092's independent value.
- **GRV-094 — the invariance sweep.** Four-for-four across the medium's
  registered deformations, and the one-power theorem: the action constant
  survives as the unique invariant combination.
- **GRV-095 — the fork forced.** The dictionary factor bounded before it
  could matter; F-Lor's induced tension misses c^4/16 pi G by 33.8 orders
  at the bracket's most generous edge, unrescuable; F-Sak solves at
  0.8..78 Planck lengths. THE FORK RESOLVES TO F-SAK by derivation and
  economy — a adopted at eight Planck lengths, F-Lor demoted not deleted,
  the PVLAS-level nonlinearity standing as the resolution's own falsifier.

## Arc II — the detector that remembers (FND-STRAND-009..019)

The campaign in one paragraph: the bath the corpus derived (STRAND-007/008)
gets its clock measured, its size-dependence mapped, its anomalies
interrogated by purpose-built instruments, its two natural mechanisms
killed cleanly, one beautiful theory buried at its own checkpoint, and its
mystery resolved — the non-exponential escape transient is real, intensive
per channel, and hidden at scale by arithmetic rather than removed by
physics.

- **STRAND-009 — the attempt rate's provenance.** Bars-first Arrhenius
  campaign; a marginal statistical breach logged literally and turned into
  the seed-pricing rule; the nu-identification supported at every handling,
  promoted at none.
- **STRAND-010 — the promotion rerun.** Both open items adjudicated on the
  bars' face (h re-locked with rationale; budget priced to the estimator);
  the identification PROMOTED: the attempt rate IS the weave band gap to
  O(1) — every symbol in the Kramers form corpus-internal.
- **STRAND-011 — the N-sweep.** Locked protocol fires (one censored run:
  NO-VERDICT), audit completes the run by deterministic replay, and the
  scaling comes back super-extensive — outside every committed grammar,
  registered as measured with the informed-addendum ceiling respected.
- **STRAND-012 — the blind four-point sweep.** No single power law exists:
  a CROSSOVER — finite-size barrier relief exhausting into rate-additivity
  — with both prior suspects real in different regimes, and the N = 24
  mean exposed as heavy-tail fragile (the arc's third pricing lesson).
- **STRAND-013 — the tail-robust refit.** Zero new compute; the
  identification survives BOTH estimators (opposite failure modes, one
  O(1) window); the barrier value flagged estimator-sensitive; half the
  shape ledger flagged non-constant-hazard.
- **STRAND-014 — the hazard census.** n = 256: escape is non-memoryless
  (hazard falls 3x), the fall is not traceable to measured weave
  functionals, and the batch-swing question is CLOSED as pure
  distributional dispersion. A clause conflict in our own bars resolved
  against ourselves; the frailty degeneracy named as our own flaw.
- **STRAND-015 — survivor thermometry.** The cooling-bath mechanism KILLED
  by its own designed test: bath temperature flat to 5e-4 while the hazard
  fall replicates. Instrument audited before interpretation.
- **STRAND-016 — force-noise spectroscopy.** The dressed-drive mechanism
  KILLED: drive intensity flat to 6e-4 against the closed-form FDT check.
  The exclusion triangle closes; the pre-committed consequence fires:
  theory session commissioned.
- **STRAND-017 — the theory session (Failed, kept).** The phase-winding
  picture dies at its own first checkpoint (mode energies reorganize ~90%
  against a 10% freezing bar); the stop-clause keeps the pre-registered
  falsifier in the drawer; and the failure DISSOLVES the paradox — the
  weave is an internally mixing thermal network, so flat aggregates over a
  mixing microstate were never a mystery.
- **STRAND-018 — the slow-mixing session.** tau_mix measured (~175) and
  INTENSIVE — identical at N = 24 and 96, because mixing is local. Two
  limbs pass (timescale coincidence; tail deceleration), the size limb
  fails informatively, and the "small-box anomaly" framing is retired.
- **STRAND-019 — the fork.** R(N) = 0.371 / 0.281 / 0.479 / 0.972 at
  N = 24/48/96/192 (slope +0.495): SIZE-EFFECT. The transient is strong in
  small boxes and gone by 192 — Poissonization by channel count, the
  classical Khinchin limit, is the theorem-shaped explanation candidate,
  cohering with 012's crossover and 018's intensive clock.

**The arc's sentence:** a finite detector's click statistics deviate from
Poisson by an amount set by how few independent nucleation channels it
contains, over an epoch set by its bath's own intensive mixing time.

**Prediction 10** gains a kinetic corollary (dark-count prefactors at the
strand mass scale, the same spectral location as the decoherence floor).
**Prediction 11** reaches its final empirical form pending derivation: the
non-Poisson dark-count transient carries a SIZE CONDITION — a counting
statement, not a new constant — and along the way sheds a
mechanism-conditional drift claim it could not support.

## Methodology ledger (this release's honest freight)

Three pricing lessons (seeds, window, tail shape) and two bar-drafting
lessons (pairwise clause consistency; never write an aging grammar without
naming frailty), all caught by the protocols they now improve. One
platform-fragile benchmark bar retired with the redundancy argued
(GRV-086). One theory registered Failed-and-kept within the hour of its
conception. Every session bars-first; every kill audited before
interpretation; every exploratory number quarantined from its own
session's verdicts.

## Commissioned and open

- **The Poissonization session** (headline debt): derive the Khinchin
  superposition limit for this engine; parameter-free R(N) from the
  measured kink correlation length against a table that already exists —
  a Derived-status candidate that would promote the eleven-session arc
  into a cornerstone.
- Exact-D on the 3D instrument (GRV-095 residual; precision, not decision).
- The hazard-shape session revives only if the derivation fails.

Full details in `claims.yaml` (the authority), the per-session analysis
files, and `CHANGELOG.md`.
