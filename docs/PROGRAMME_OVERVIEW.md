# The Rope Programme — Overview

*The front door to the corpus. This document is **generated** from `claims.yaml` and the computed roadmap by `tools/build_overview.py`, so its statistics, maturity table, and open-problems list stay in sync with the corpus by construction.*

Programme credit: the core Rope Hypothesis is due to Bill Gaede; this corpus develops and formalises it.

---

## What this programme is

The Rope Hypothesis is a speculative classical model in which interactions are carried by physical strands under tension. This corpus develops its **classical continuum sector** into a reproducible research programme: a coherent chain from microscopic mechanics up through electromagnetism, optics, and thermodynamics, with every principal claim assigned a status and, wherever possible, backed by an executable benchmark.

The programme's discipline is its defining feature: **derived results, empirical inputs, and open problems are kept strictly separate**, corrections and negative results are preserved as visibly as successes, and the mathematics is kept distinct from the ontology it proposes.

## Corpus at a glance (generated)

- **Bundled papers:** 60
- **Reproducible benchmarks:** 342
- **Registered claims:** 381 (366 code-backed and machine-verified)
- **Claim status distribution:** Derived 104, EFT-constrained 4, Modeled 242, Conjecture 3, Failed 23, Open 5

Verify it yourself in one command: `make verify` (runs every benchmark the registry references) or `python tools/verify_corpus.py`.

## The classical continuum chain (the strongest core)

The programme's spine is a single geometric structure developed level by level. Each link is a Mature, benchmark-backed sector:

```
Microscopic Mechanics
      -> Homogenization (Gamma-convergence)
            -> Effective Field Theory
                  -> Electromagnetism
                        -> Classical Optics -> Interface Optics
      -> Defect Theory (2D + 3D)  [shares the same functional]
      -> Statistical Mechanics    [BKT = defect-gas unbinding]
  All unified by: Gauge Geometry (bundle -> connection -> curvature -> topology)
```
The gauge-geometry paper is the mathematical reference showing these are one structure; the machine-readable dependency graph (`docs/dependency_graph.txt`) encodes the links claim by claim.

## Sector maturity (computed)

Maturity is **computed** from each sector's claim statuses and benchmark coverage, not hand-assigned. Readiness is cross-checked against it; there are currently no readiness-vs-evidence flags.

| Sector | Computed maturity | Claims | Derived | Benchmarked |
|---|---|---:|---:|---:|
| Microscopic Mechanics & Foundations | ? | 71 | 28 | 68 |
| Renormalization / EFT | ? | 1 | 1 | 1 |
| Electromagnetism | ? | 33 | 15 | 33 |
| Classical Optics | ? | 10 | 10 | 10 |
| Thermodynamics | ? | 6 | 4 | 5 |
| Condensed-Matter Analogues | ? | 1 | 0 | 0 |
| Gravity (weak field) | ? | 52 | 22 | 52 |
| Solitons & Knot Spectrum | ? | 1 | 1 | 1 |
| Electroweak (Weinberg angle) | ? | 1 | 0 | 0 |
| Particle Masses | ? | 5 | 2 | 3 |
| Gauge Geometry (unification) | ? | 6 | 5 | 4 |
| Quantum Boundary (Bell) | ? | 27 | 15 | 25 |
| Chemistry (bonding, geometry, dynamics) | ? | 15 | 2 | 15 |
| Nuclear Structure | ? | 25 | 3 | 23 |

## Suggested reading order

For a first read of the corpus:

1. **This overview** — assumptions, scope, maturity, open problems.
2. **Topology and Gauge Geometry Underlying the Rope Programme** — the mathematical backbone; why bundles, connections, curvature, and topology recur.
3. **Microscopic Mechanics** — the endpoint mechanics the chain starts from.
4. **A Gamma-Convergence Derivation for the Rope Medium** (homogenization) — how the discrete model becomes the continuum functional.
5. **Electromagnetism** and **Classical Optics** — the strongest, most self-contained physical sectors.
6. **Scope and Limits** — what the programme does not claim, especially the quantum boundary.

## Open problems and boundaries (generated)

The programme marks where it does not (yet) claim to explain nature. These are surfaced directly from the registry:

**Genuinely open** — not established either way; the honest frontier.
- `EM-RECON-008` The repulsive-core residual is located precisely and confirmed irreducible at quadratic order: it is the leading super-quadratic coefficient of the network density->energy relation, on which bond length, nuclear spacing, and ferro magnitude jointly depend — Went after the repulsive-core residual (the equilibrium-spacing gap in the mode-overlap functional, EM-RECON-005/006). ATTEMPTED ROUTE: the interpenetrability threshold (FND-MATTER-004) LOOKED like it supplies a parameter-free hard core (ropes cannot overlap past coverage f_c -> hard wall). RETRACTED after checking the actual postulate (magnetism paper 2.1): 'bundle density determines the local field strength' is a SMOOTH, continuous relation, NOT a hard pass/no-pass threshold. So interpenetrability does NOT supply the core for free -- it relocates the missing input to the curvature of the density->energy relation at high density. Nearly renamed the missing primitive as f_c; caught it by reading the postulate. SHARPENED RESULT (genuine): the missing input is precisely the LEADING SUPER-QUADRATIC COEFFICIENT of the network's density->energy relation (the first nonlinear elastic correction beyond the weak-field/quadratic limit). PROVEN (benchmarks/em/repulsive_core_residual.py): a purely quadratic overlap energy -|A(r)| is monotonic (no interior minimum -> no core), so a core is impossible at quadratic order; a super-quadratic term creates a core whose location MOVES with its coefficient, which the corpus does not fix. UNIFICATION (the value of this result): chemical bond length, nuclear spacing, AND the ferromagnetic magnitude all depend on this SAME single coefficient -- so they resolve together if it is supplied (by measurement or a deeper rope micro-model), but none can be derived without it. This is the honest boundary, located rather than fitted; positing a coefficient and tuning it to a spacing would be the fitting the derivation doc explicitly warned against. Kept Open: a sharpened, precisely-located open problem, not a solution. [UPDATE: EM-RECON-009 identifies the missing coefficient concretely -- finite strand EXTENSIBILITY (stretch modulus k) gives c4=(k-T0)/8 exactly, with the sign required by stability (pure tension is nonlinearly unstable). The residual is no longer anonymous; its value (k) is one new input, after which the four dependent scales become predictions.]
- `GRV-012` STANDING ADVERSE VERDICT (stated plainly): on current commitments, rope weak-field gravity is a SCALAR MIMIC with gamma = -1/2, predicting 0.44 arcsec light deflection vs the measured 1.75 arcsec -- in contradiction with data since 1919 -- unless a mechanism exists for a traceless 1/r conditioning locked at 1/4 the isotropic amplitude (none found in the corpus) — The tensor-structure campaign's conclusion, registered at full strength because burying it would poison everything else. THE CHAIN: (1) the isotropic sector is exhausted -- every constructed mechanism gives gamma in [-1, 0] (GRV-009: -4/7; GRV-010: -1/2; pure channels: -1/2, 0, -1); (2) the anisotropic route (GRV-011) produced the exact tidal theorem but also the RANGE OBSTRUCTION: elastic conditioning is capped at 1/r^3, and the only 1/r channel (harmonic bath deficit) is isotropic, giving gamma = -1/2; (3) gamma = -1/2 means light deflection (1+gamma)/2 x GR = 0.44 arcsec vs 1.75 measured -- WRONG BY 4x, falsified at the Eddington-1919 level, catastrophically so by Cassini (|gamma - 1| < 2.3e-5). THE ONE ESCAPE, exactly specified: a traceless 1/r conditioning with amplitude LOCKED at 1/4 of the isotropic channel's -- the mechanism audit within current commitments comes up empty (diffusive quadrupole suppressed by mean-free-path/r; ballistic shadow wrong range 1/r^2; elastic strain wrong range 1/r^3). HONEST FRAMING OF SCOPE: this verdict attaches to the WEAK-FIELD GRAVITY SECTOR under current commitments; it does not retroactively invalidate the derived EM/optics/chemistry/nuclear core (which never depended on the gravity sector), and the gravity sector was always flagged matched-not-derived -- the campaign converted that flag into a precise adverse finding. THE POSITIVE RESIDUE: the harmonic -2:1 theorem shows the network contains GR's tidal geometry EXACTLY at the order its mechanics reaches -- whatever the eventual resolution (a new commitment supplying the locked quadrupole, a reconception of the conditioning channel, or acceptance of the falsification), it must preserve that agreement. STATUS Open, not Failed: what is open is whether the escape mechanism exists; the computation itself is done and Derived. This is what the campaign was FOR: 'decides whether this is a scalar mimic or a metric theory' -- and on current commitments the answer is scalar mimic, in conflict with experiment. Registered the day it was found. [HARDENED BY GRV-013: the hunt was run systematically -- two new channels found (1D rope shadows do not decay along anchored ropes; anchored-tension constancy), full channel map computed, and every constructible channel misses the target on range or ratio, by the geometric dilution theorem (direction-carriers dilute as 1/r^2 by solid angle). The escape now requires a scale-free-transport commitment REFUSED under the programme's postulate-audit standard (necessity only; no independent motivation). The verdict stands at full strength: 'no mechanism found' is now 'the channel space excludes the target.'] [SCOPE REFINED (GRV-014, Mark's hypothesis, audited and admitted): the verdict's precise statement is that CLASSICAL rope weak-field gravity is falsified -- the channel-space no-go and the 0.44-vs-1.75 contradiction stand at full strength as the classical result. The framework's surviving gravitational hypothesis is the quantum-induced (Sakharov) completion: independently motivated by pre-verdict results (G's hbar-entanglement, GRV-006; the registered Sakharov route, GRV-007; exact LI of the coupled sector, FND-REL-002; the NUC-005 declared-omission precedent), adding no new commitment, and therefore admitted as Conjecture where scale-free transport was refused as rescue. Untestable within current reach; the sector's honest status: classically falsified, quantum completion open.]
- `FND-MATTER-001` REFRAMED via Gaede's atom: atomic size is a standing-wave WAVELENGTH (decoupled from mesh spacing), so the mesh->atom hierarchy worry DISSOLVES; the residual is the general (not special) problem of deriving m_e, hbar -> Bohr radius — Consequence of FND-REL-003 (mesh spacing a << 1e-16 m, far finer than atomic ~1e-10 m). READING A ('atom = single rope-endpoint node', which would make rope spacing = interatomic spacing) is REFUTED -- that scale is Lorentz-excluded. The corpus's endpoint-locking result J=T^2/kappa is a COARSE-GRAINED homogenization, so 'endpoint' is an effective node, consistent with READING B ('atom = extended composite/topological structure spanning ~1e6 mesh cells, size ~1e-10 m; atoms are coarse defects in a finer mesh, like a vortex spanning many underlying particles'). Reading B is the only SURVIVABLE option and is consistent with the corpus, but is a RECONCILING HYPOTHESIS, NOT a derived result. Three genuine gaps: (1) the scale hierarchy (~1e6+ from mesh to atomic) is not derived -- the one dimensionless coupling Pi=kappa a/T is a free parameter 'fixable only by a deeper theory'; (2) no model of an atom as a bound rope structure of the right size; (3) no reproduction of actual atomic physics (spectra, chemistry) from such structures. PARTIAL NARROWING (constructive probe, not a solution): the corpus's defect energy is LOGARITHMIC (E~piK ln(R/a)), the structure behind dimensional transmutation -- inverting a log gives R~a exp(binding/piK), an exponentially large size from an O(1) coupling (as QCD does). So the framework is NOT structurally incapable of a mesh->atom hierarchy. HONEST CAVEAT: exp(binding/piK) for reasonable inputs gives ratios ~10-1000, NOT 1e6 automatically; reaching the atomic scale still needs the coupling/binding in a specific range. Gaps remain: hierarchy not computed, no bound-atom model of the right size, no atomic physics (spectra, chemistry) reproduced. Arguably the framework's biggest conceptual debt, EXPOSED (not created) by the Lorentz result. Logged honestly; not resolved. GAEDE REFRAMING (Mark's steer; Gaede's atomic model, recorded as his ontology not corpus-derived): in Gaede's picture an atom is a hub-knot with ropes radiating off to other atoms, and the electron shells are STANDING TORSION patterns on the near-hub ropes. The atomic size ~1e-10 m is the WAVELENGTH of that standing pattern, NOT a multiple of the mesh spacing -- exactly as a guitar-string wave's wavelength (cm) is unrelated to the string's molecular spacing (nm). So mesh-scale and atomic-scale are INDEPENDENT and the ~1e6 'hierarchy' was a mis-framing: there is nothing to derive between them. The corpus's chemistry paper already uses this standing-wave-around-a-knot picture, so it is internally consistent. NET: the special hierarchy debt DISSOLVES; what remains is the GENERAL open problem (shared by all sectors) of deriving m_e and hbar, which then fix the Bohr radius a0=hbar^2/(m_e e^2) as the standing-wave scale. Downgraded from 'biggest special conceptual debt' to 'the general constant-derivation problem'. Still open, but not a unique matter-sector crisis. GAEDE IMAGE (viewed directly, 'Introducing The Atom' + '2D/3D EM rope torsions', https://www.youtube.com/watch?v=Y5MVggLmzV4): the picture shows an atom/proton as a CONVERGENCE of very many ropes to a focal core inside a woven circumferential shell, with ropes continuing outward -- a many-rope 'star', NOT a few-rope knot (correcting an earlier vague 'hub-knot' description). Light/EM is drawn as a two-strand TORSION travelling along a rope connecting two atoms point-to-point ('EM rope torsions'), which matches the corpus EM sector (transverse two-strand wave; screw/torsion current EM-014). The many converging ropes are naturally the flux lines of the charge source (consistent with the Gauss-law/charge-as-rope-count structure), and rope=atom-pair connection matches endpoint-locking. These are CONCEPTUAL renders, not quantitative: they confirm and sharpen the ontology (atom = coarse convergence pattern on a fine sub-nuclear rope mesh, consistent with atomic-scale != mesh-scale) but derive nothing new -- the rope number N and the atomic scale (Bohr radius) still require m_e, hbar. Net: ontology strengthened, open residual unchanged.
- `FND-MATTER-003` Derivation of the atomic scale is BLOCKED: two independent inputs missing (absolute scale a; rope-count N), and N cannot be fixed from charge (charge=winding=1, not the packing count ~1e12) — Attempted to push the R~a*sqrt(N) consistency window (FND-MATTER-002) toward an actual derivation of the Bohr radius. HONEST RESULT: blocked, and the attempt located exactly why. A derivation needs, independently of a0: (I) the absolute microstructure scale a -- but the corpus leaves a a FREE parameter (only a Lorentz bound a<~1e-16 m exists, no value); and (II) the rope count N -- for which the natural candidate 'N from charge' FAILS: charge is the winding/linking number (=1 for hydrogen), NOT the packing count (~1e12 ropes) the radius law needs. These are two DIFFERENT quantities sharing the letter N; conflating them would be the error to avoid. So the model currently has no independent handle on the number of ropes in an atom, and no absolute scale. This is a specific, named obstruction (not a vague 'masses undived'): the missing physics is 'what sets the rope COUNT of a bound structure'. A failed-but-diagnostic result; kept to prevent a future accidental charge=count conflation. [UPDATE: partially resolved by FND-MATTER-004 -- the rope count N is now fixed by the interpenetrability coverage threshold (Mark's density-onset principle), reducing this blockage from TWO missing inputs to ONE: only the absolute mesh scale a remains, which is likely an irreducible network constant rather than a derivable quantity.] [FURTHER UPDATE: FND-MATTER-005 completes the characterization -- the remaining input a is shown IRREDUCIBLE (a fundamental constant fixed by measurement, not derivation). Net: the atomic-scale problem is fully mapped -- N derived (004), a a normal fundamental constant (005). No longer a 'block', but a theory bottoming out in one measured constant.]
- `NUC-003` Fractional quark sub-knots are CONSISTENT with integer charge quantization (confined, sum to integers) but require an UNBUILT sub-integer topological layer — CONSISTENCY CHECK run before adopting the nuclear paper. The paper models quarks as sub-knots with fractional winding (+2/3,-1/3). SURFACE conflict with the corpus's integer charge quantization (FND-008, EM-001, GG-006, all Derived). RESOLUTION (passes): the fractional pieces are CONFINED (unstable alone) and only exist bound into configurations whose TOTAL winding is integer -- verified arithmetically (proton uud=+1, neutron udd=0, pi+=+1, Delta++=+2). So all OBSERVABLE charges remain integer; the corpus's observable-level claims are UNVIOLATED (same confinement logic real physics uses). HONEST GAP (why Open, not Derived): GG-006 defines charge as an integer IDENTICAL to the linking number, and the linking/Chern math (GG-002/003) is intrinsically integer-valued; a 2/3 winding is NOT a standard linking number -- the paper itself says these fractions 'require a more general topological classification than integer knots alone'. So the sub-knot layer is a NEW topological framework the corpus has NOT built, not a consequence of existing integer-linking machinery. Consistent at the observable level; the fractional-winding ontology is a flagged unbuilt dependency.

**Documented negative results** — kept as findings, not hidden.
- `EM-011` Cosmological alpha-variation test FALSIFIES the strong (local density-tracking) form of the rope-density hypothesis for the EM coupling — Picture A (EM coupling set by rope density) with the kinetic-term relation gives alpha ~ 1/L_v; c is protected (mu0,eps0 scale oppositely). If L_v tracks LOCAL matter density (~(1+z)^3), predicted |Delta alpha/alpha| ~ 0.88 at z=1 vs observed bound ~2.4e-6 (Keck MM) -- falsified by ~3.6e5. The only surviving version makes L_v a near-constant cosmic background, rendering Picture A observationally indistinguishable from Picture B (space-intrinsic field). The strong 'density literally sets alpha and should vary' reading does NOT survive. Constrains an INTERPRETIVE claim only; structural EM results (EM-001..010) are independent and unaffected.
- `THM-006` THE LAYER SEPARATION THEOREM (Modeled) -- the programme's failures are ENRICHED at the dynamical frontier, not scattered: classifying all registered claims into Layer I (Topological: discrete winding, knots, linking, conservation), Layer II (Geometric: bundles, connections, curvature, continuum fields), and Layer III (Dynamical: full quantum kinetics, amplitudes, measurement, absolute-mass origin, gravity closure) by REQUIREMENT rather than vocabulary, the Failed+Open claims concentrate at Layer III -- which is only ~10 percent of the corpus yet holds 5 of 12 failures, a ~4x enrichment over the chance expectation of ~1; the enriched failures are the DEEP ones (lepton masses PM-002/004, quantum entanglement QB-003, the electron-mass mechanism QB-004) while the Layer II failures are ordinary classical falsifications (cosmological alpha-variation EM-011, gravity-conditioning candidates GRV-009/010), kept but NOT frontier-clustered -- so the honest statement is not 'all failures are dynamical' but 'genuine failures are enriched ~4x toward the dynamical layer, with a real minority of classical falsifications'; the theorem is Modeled (it depends on a curated, auditable marker set; a different reasonable marker set could shift the magnitude), what is robust being the SIGN and rough size of the effect -- the programme's successes cluster in the topological and geometric layers and its failures share a coherent boundary, the jump from geometry to full dynamics; this is the measured content behind the Ladder and the Frontier figure (figures/rope_layers.png), which is the theorem's visual anchor — The reviewer proposed this as an outside observation; the corpus did what it does with every proposal -- built the courtroom and let the numbers rule -- and the first verdict was ADVERSE. A vocabulary-based classifier returned 0.5x (failures ANTI-clustered), because it mis-tagged the entire matter and QB sectors on incidental mentions of hbar and Bell -- the very sectors whose many DERIVED claims characterize the boundary rather than fail at it. That adverse result is kept in the theorem's own docstring, because the fix was the scientific act: reclassify by what a claim REQUIRES, not what it mentions, and the 4x signal emerged and survived. THE RESULT IS HONEST IN BOTH DIRECTIONS: it confirms the ladder figure's thesis (failures cluster at the dynamical frontier) AND it refuses the figure's implied absolutism (four failures are ordinary Layer II falsifications, kept and counted). That two-sidedness is why it earns Modeled and not a marketing-grade Derived: the classifier has genuine freedom in its marker set, and the claim says so. WHAT IT BUYS THE CORPUS: a quantitative back-stop for its most-praised qualitative feature. Reviewers kept singling out 'the failures are not scattered' as the programme's signature; THM-006 turns that impression into a number (4x) with an auditable method and a kept adverse first draft -- which is the programme's whole epistemology applied to a claim about itself. And it gives the hero figure a spine: the Ladder and the Frontier is no longer just a pedagogical arrangement but the picture of a measured enrichment. NAMED NEXT-ORDERS: a marker-set robustness sweep (report the enrichment across several reasonable classifiers -- does the sign ever flip?); per-sector layer profiles (which sectors are purely Layer I-II, which straddle); and a periodic re-run so the theorem tracks the corpus as it grows, flagging the day a Derived Layer III claim appears (which would be the single most important event the framework could record). [TRIPWIRE RE-RUN 2026-07-29, post-ELEC/ROPE-MODE integration: Failed+Open now 23; Layer III holds 5, enrichment 2.1x -- SIGN INTACT, MAGNITUDE HALVED from the original 4.2x. The 11 new electron-campaign failures classify predominantly Layer II under the requirement rules, correctly: they are constrained-optimization and geometric-spectrum defeats, not quantum-boundary-forced ones. The theorem survives with its magnitude's sensitivity to campaign composition now itself a documented property: when the corpus attacks hard NUMERICAL problems, ordinary failures accumulate and dilute the enrichment; the frontier signal is the stable 5-claim core, not the ratio.] [FALSIFIED BY THE CORPUS'S OWN GROWTH, and kept (2026-08-01): the pre-committed bar was enrichment > 2.0x, measured at ~4x when this claim was written. At 370 claims it is 1.85x -- Layer III is 10% of claims and holds 5 of 27 Failed+Open. The decline is real and mechanical: this session added a large block of Layer II claims, many of them registered failures (the hbar retirement arc), diluting the frontier concentration. THE BAR WAS NOT RELAXED to accommodate it; the benchmark now documents the falsification instead of asserting the dead threshold. WHAT SURVIVES: the SIGN. Failures still concentrate at the dynamical frontier at 1.85x over chance. What does not survive is the '4x' magnitude the ladder figure was built on, and any presentation quoting it should be corrected. Found by the full verify_corpus run during the 2026-08-01 tooling repair -- which is itself the argument for running the full verifier rather than the fast test subset.] [SELF-REFERENCE TRAP, caught and excluded (2026-08-01): reclassifying this claim to Failed made it evidence FOR ITSELF -- THM-006 is classified Layer III, so its own failure added a Layer III failure and pushed the enrichment from 1.85x back above the bar to 2.14x. A claim may not count itself as evidence for itself, so the benchmark now excludes THM-006 from its own statistic; on the remaining corpus the enrichment is 1.90x and THE FAILURE STANDS. Recorded because the trap is generic: any claim whose subject is the registry's own status distribution can bootstrap itself by changing status, and future registry-level claims should exclude themselves by construction.]
- `GRV-009` FAILED CANDIDATE: per-strand STRAIN conditioning -- fully specified by session primitives (k, P-VOL, torsion~r^4) with zero freedom -- gives gamma = -(k/T0+2)/(2k/T0+3) = -4/7 at k/T0 = 2: wrong SIGN of spatial curvature, Cassini-excluded; gamma = 1 would need k/T0 = -5/3 (impossible) — First candidate through the GRV-008 constraint, chosen because it is FULLY SPECIFIED with no adjustable coefficients: a strain field eps conditions tau = (k/T0)eps (stretch raises tension, EM-RECON-009), m = -eps (volume conservation thins the strand, P-VOL), l = -2eps (torsional rigidity ~ r^4 under volume-conserving thinning). RESULT: gamma_eff = -(k/T0+2)/(2k/T0+3), i.e. -4/7 at k/T0=2, approaching -1/2 at large k -- the WRONG SIGN of spatial curvature, excluded by Cassini (|gamma-1| < 2.3e-5) by a factor of ~70000 in deviation; gamma = 1 requires k/T0 = -5/3, impossible since stability demands k > T0 > 0. VERDICT: the gravitational conditioning channel is NOT per-strand stretch. CONVERGENT EVIDENCE: GRV-005's hierarchy already showed mass couples absurdly weakly to strain (~40 orders); now the tensor structure independently rejects strain -- two arrows, same direction. ALSO ESTABLISHED: pure COVERAGE conditioning (more ropes per volume, per-strand properties unchanged) is metric-INVISIBLE (tau = m = l = 0), so of EM-RECON-013's two channels one is metric-blind and the other tensor-wrong: whatever conditioning IS gravity, it is a per-strand renormalization of a different kind. Kept as a finding: the candidate space just shrank decisively, which is what a good failure does. The GRV-008 constraint m = 3tau - 2l now functions as the DESIGN SPECIFICATION every future candidate must hit Cassini-exactly.
- `GRV-010` FAILED: mode-bath conditioning -- after correcting my own posed estimates (mean strain relaxes; no inertia renormalization in the elastic Lagrangian; twist at higher order), the bath is TENSION-ONLY and gives gamma = -1/2 UNIVERSALLY (any kappa, any bath profile, either sign): Cassini-dead — Executed the kill-or-confirm, and it killed -- after THREE CORRECTIONS of the estimates I posed the candidate with (logged, mine): (1) tau: the mean-strain tension shift RELAXES (tension is uniform along a strand in statics), so tau = (k/2T0)<g^2> was wrong; what survives locally is the quartic FLUCTUATION coupling, homogenized isotropically from the EM-RECON-016 vertex ((12+4)/2 = 8): delta_T = 8*c4*<g^2>, tau = (kappa_tw - 1)<g^2>, with k_tw = k + lambda*gamma_lock^2*tau0^2 absorbing the twist-lock stiffening. (2) m = 0: 'wave energy adds inertia' was a RELATIVISTIC import the classical elastic Lagrangian does not contain -- the kinetic term is exactly quadratic, no quartic mixing. (3) l = 0 at O(<g^2>): bath-driven twist enters at O(<g^4>). CONSEQUENCE: the mode-bath is TENSION-ONLY conditioning, and pure-tension gives gamma_eff = -1/2 UNIVERSALLY -- independent of kappa, of the bath profile, of its sign. Cassini-dead. The redshift SIGN additionally requires a bath DEFICIT near mass (a Le Sage-flavored shadow -- the only sign-viable reading, noted), and it is still gamma = -1/2. The degeneracy/window structure posed last turn dissolves with the corrected coefficients -- it was an artifact of the wrong tau and m. FAILED, kept as a finding. WHAT THE FAILURE ESTABLISHED (the pattern, theorem-grade within GRV-008's model): pure tension -> gamma = -1/2; pure density -> 0; pure torsion -> -1; every pure channel lies in [-1, 0] and both mixed mechanical candidates landed at -4/7 and -1/2. gamma = +1 requires the ANTI-CORRELATED response m = 3tau - 2l (e.g. density up while torsion down), which NO isotropic per-strand mechanism in the corpus produces -- thickness ties m and l with the same sign. The campaign's conclusion so far: EITHER an unknown anti-correlated channel exists, OR the isotropic response model itself is the wrong frame -- the diagnosis pursued in GRV-011.
- `PM-002` Lepton ratios with the model's own Weinberg angle — Pinned failure mode; kept as a finding.
- `PM-004` The lepton mass spectrum does NOT fall out of the corpus knot/soliton excitation physics without tuning; lepton masses are irreducible inputs (negative result, no spectrum fitted) — Pursued the well-posed problem PM-003 set: compute the actual knot excitation spectrum. HONEST NEGATIVE RESULT with rules fixed in advance (model must be a corpus soliton/knot; NO parameter tuned to the lepton masses; ratios must be a pure output; non-match reported plainly). Every non-tuned mechanism fails: (1) natural topological-soliton spectra are O(1)-spaced -- real baryons have nucleon->Delta = 1.31x, not the 207x and 3477x the lepton ratios require; solitons simply lack towers spanning 3 orders of magnitude. (2) Energy~winding^2 cannot distinguish generations (all leptons winding 1, PM-003). (3) Tunneling splitting gives near-degeneracy (wrong direction). (4) Simple ladders all fail: sqrt(m/m_e)=1,14.4,59 (non-integer), log=0,5.3,8.2 (unequal spacing), successive ratios 207,16.8 (non-geometric). (5) The Koide relation K=0.66666 vs 2/3 matches to ~0.001% -- genuinely remarkable -- but only CONSTRAINS (one equation, two masses free); it does not generate the spectrum, and is already registered honestly as PM-001 (Conjecture); this work does not strengthen it. CONCLUSION: lepton masses are IRREDUCIBLE INPUTS, on the same footing as the mesh scale a (FND-MATTER-005) and the super-quadratic coefficient (EM-RECON-008) -- quantities the framework does not currently determine. Registered Failed (the excitation-spectrum COMPUTATION was attempted and does not work), kept as a finding to prevent re-attempts and to record that no spectrum was fitted. The excitation FRAMING (PM-003) remains correct as a classification; it is the computation that fails.
- `QB-003` Present counting form does not reproduce quantum entanglement — Documented boundary, retired as ambition, kept as finding. [RE-EXPLAINED (QB-006): this failure was THEOREM-FORCED -- Bell caps every local mechanism at CHSH = 2; the counting form could never have succeeded locally. The open door is nonlocal (pilot-wave-type), for which the substrate conditions are catalogued in QB-006.]
- `QB-004` One-loop fluctuation mass mechanism (log-det ≈ -1.29 vs electron ≈108) — Falsified by ~80x; kept as a finding.
- `NUC-010` THE KINETIC/ZERO-POINT DIAGNOSIS IS REFUTED: THE OMISSION NAMED AS THE SOLE REMAINING CAUSE MAKES THE MODEL 2.25x WORSE WHEN COMPUTED (Failed, kept) -- THE DIAGNOSIS UNDER TEST: NUC-009 closed with 'the kinetic/zero-point omission now stands ALONE as the diagnosis for the remaining baseline', inherited from NUC-005 and NUC-006, and it had never been computed; THE DERIVED IMPLEMENTATION with no freedom: a degenerate Fermi gas at nuclear saturation (rho = 0.17 fm^-3) gives k_F = 1.360 fm^-1, E_F = 38.3 MeV and <E_kin>/A = (3/5)E_F = 23.0 MeV, and since E_kin scales as rho^(2/3) while local density scales as coordination z the per-nucleon cost is kappa z^(2/3) with kappa = 23.0/12^(2/3) = 4.389 MeV -- every number derived, nothing fitted, with the bond constant recalibrated on He-4 exactly as before to preserve the one-constant discipline; THE RESULT: no kinetic term gives RMS = 0.888 MeV (baseline) while the DERIVED Fermi-gas term gives RMS = 2.000 MeV, 2.25x WORSE, the derived term overcorrecting so that A = 8-16 swing from +0.5 MeV overbinding to -1.7 and -2.9 MeV underbinding -- THE DIAGNOSIS FAILS; A SECOND CORRECTION to an intuition tested inside this session: a CONSTANT kinetic term is not absorbed by recalibration either, since B/A = eps bonds/A - kappa means raising kappa inflates eps and AMPLIFIES the spread in bonds/A, with RMS going 0.888 -> 2.026 -> 4.789 at E_kin/A = 0, 10, 23 MeV; WHAT DOES HELP AND WHY IT IS NOT AN ANSWER: scanning kappa z^p freely the optimum is p = 1/3 with kappa = 3.700 giving RMS = 0.415, less than half the baseline, but z^(1/3) means E ~ rho^(1/3) ~ 1/r which is a LENGTH or surface-tension scaling and NOT a kinetic one (kinetic energy scales as rho^(2/3), the exponent that fails), being two fitted parameters over fifteen points with no derivation -- a phenomenological hint that a coordination-dependent correction of the right MAGNITUDE exists, and evidence that it is not kinetic; VERDICT: the sector's inherited explanation for its residuals is wrong, the residual is real, a coordination-dependent term of about the right size can absorb it, and that term does not scale like kinetic energy — Four claims carried the same explanation for the same residual without ever computing it, and computing it takes one session and kills it. That is the cheapest possible lesson about inherited diagnoses, and the sector should take it in the same spirit the electron campaign took ELEC-023's void metric: a phrase repeated across claims acquires an authority its evidence never earned. THE FAILURE IS CLEAN because the implementation had no freedom. Nuclear saturation density is measured, the Fermi energy follows, the (3/5) factor is exact for a degenerate gas, and the rho^(2/3) scaling is the definition of the kinetic term. There was no knob to turn, and the answer came out 2.25 times worse than doing nothing. A diagnosis that survives only when left uncomputed is not a diagnosis. THE MOST USEFUL PART IS THE EXPONENT. Free scanning finds a genuine optimum that halves the baseline error, so a coordination-dependent correction of roughly the right magnitude really is missing -- but it wants z^(1/3), and kinetic energy wants z^(2/3). Those are different physics: rho^(1/3) is an inverse length, the scaling of a surface tension or a Coulomb-like term, not of confinement. The sector has been looking for the residual in the wrong place, and now knows which place is wrong and roughly what shape the right answer has. A CAUTION ON THE FITTED RESULT, recorded so it is not later quoted as a success: two parameters over fifteen points, no derivation, and an exponent that was scanned rather than predicted. It is a hint, not a result, and it should be promoted only by a mechanism that predicts p = 1/3 in advance. NAMED NEXT-ORDERS: identify what physically scales as rho^(1/3) in a contact-bonded droplet (surface tension of the bond network is the obvious candidate and is computable from the same geometry); re-examine NUC-006's surface/volume miss (2.05 vs 1.16 empirical) in that light, since a mis-sized surface term and a missing rho^(1/3) correction may be the same defect seen twice; and strike the kinetic explanation from NUC-005, NUC-006 and NUC-009's standing text.
- `ELEC-001` STABLE LOCALISED ELECTRON-CANDIDATE SEARCH, FIRST GATE (Failed, kept): perturbed unit-linked Hopf configurations preserve charge topology, dissipate 41–43% of their energy, and converge to a common attractor, but the attractor expands to R_rms = 2.58 and fails the pre-locked localization window 0.4–2.0; the present flexible-curve dynamics therefore does not reproduce the sourced-field Hopf scale near 0.84, so no electron identification is claimed — The electron campaign's first gate was deliberately minimal: before mass, spin, magnetic moment, or relativistic propagation, ask whether the current mechanics contains a robust finite charged object. Five bars were locked before data. B1 PASS: five smoothly perturbed Hopf links retained |Lk| = 0.984–0.992. B2 PASS: energy fell by 40.9–43.3 percent. B4 PASS: the final-radius coefficient of variation was only 0.15 percent, showing a real common attractor rather than numerical scatter. B5 PASS: an unlinked control remained exactly unlinked. B3 FAILS, decisively and reproducibly: the final combined RMS radius is 2.579–2.589, above the locked 2.0 ceiling and far from the sourced-field Hopf minimum R*=0.838 reported by the full-field soliton branch. The failure locates a consistency debt between two existing dynamical descriptions: the sourced-field radius scan and the flexible curve relaxation are not yet minima of one demonstrated common action. Status Failed, not softened to Modeled: topology and attraction are real, but localization at the claimed scale is not established. NEXT ORDER: construct one coupled curve-field evolution using the same energy functional for both the static scan and the dynamical relaxation; rerun the identical perturbation bars blind. Only after that gate passes should electron-specific properties be attempted.
- `ELEC-003` RESOLUTION AND STABILITY CAMPAIGN (Failed, kept): the ELEC-002 localized linked attractor survives practical grid refinement, tube-width variation, and repeated out-of-basis perturbation, but misses the locked Fourier-basis convergence bar because K=4 to K=5 shifts R_rms by 14.77% versus the 10% ceiling — Fifteen common-action relaxation runs tested the numerical debts named by ELEC-002. B1 PASS: every run remained in the linked sector, descended monotonically by more than 2.5%, and stayed localized with 0.4<R_rms<2.0. B2 PASS: practical grid convergence is good, with the N=14 and N=16 radii differing by 2.00%. B4 PASS: varying Gaussian tube width from 0.18 to 0.36 changes adjacent radii smoothly, with a maximum jump of 19.71% below the locked 25% bar. B5 PASS: lifting a K=3 solution into K=5 with new high-frequency perturbations and relaxing twice gives a radius CV of 3.67%, while topology persists. B3 FAIL, kept: independent K=4 and K=5 optimizations give R_rms=0.681 and 0.790, a 14.77% difference above the locked 10% convergence ceiling. Therefore localization is broadly robust but a basis-independent attractor is not yet established. NEXT ORDER: matched-start, longer-run basis convergence at K=4,5,6,8 to determine whether the miss is optimization depth or a real high-mode instability. No electron identification is made.
- `ELEC-004A` LINEAR-STABILITY GATE (Failed, kept): the matched K=8 state remains linked and localized but has a residual relative gradient of 0.375, so its Hessian cannot yet be physically classified — ELEC-004A loaded the exact saved K=8 state from the matched-start basis campaign and evaluated the common Poisson curve-field energy in the full 97-coordinate chart. B1 PASS: E=16.284581491, R_rms=0.593648, and |Lk|=0.831834 preserve the localized linked object. B2 FAIL, kept: the central finite-difference gradient norm is 6.10289, or ||grad E||/E=0.374765, far above the locked 0.10 stationarity ceiling. A deterministic 20-dimensional projected Hessian showed one nominal negative eigenvalue, but direct three-step directional-curvature checks along that eigenvector were all positive; because the expansion point is nonstationary, this disagreement is not evidence of a physical unstable mode. B3 and B4 are therefore inconclusive/fail, while B5 resolves a positive projected gap of 4.53269. INTERPRETATION: ELEC-003A demonstrated practical energy/radius basis convergence but not first-order stationarity. NEXT ORDER: deterministic L-BFGS-B or equivalent gradient-based repair from the saved K=8 state, topology guarded, with ||grad E||/E<0.02 required before Hessian classification. No electron identification is made.
- `ELEC-004A-R` DETERMINISTIC STATIONARITY REPAIR (Failed, kept): physical energy descends, but the first L-BFGS-B step leaves the linked basin and the residual gradient remains large — ELEC-004A-R started from the exact saved K=8 state and used bounded L-BFGS-B with an explicit central finite-difference gradient of the same sourced Poisson curve-field energy. The physical energy falls from 16.284581491 to 16.245606456 and R_rms remains localized (0.593648 to 0.589354), so B2 passes. However, the first accepted deterministic step collapses the numerical linking invariant from |Lk|=0.831834 to 0.141802, failing B1. The relative physical gradient also increases from 0.377383 to 0.427763 and is stable at 0.427763 across h=5e-5, 1e-4, and 2e-4, failing B3 and B4. A short-range inter-strand separation barrier was insufficient to retain the linked sector. INTERPRETATION: the candidate is not shown to be an unconstrained local minimum; its descending direction exits the linked sector. This is a kept negative result. NEXT ORDER: replace ordinary descent with a genuinely topology-preserving constrained variational method, increase curve quadrature/resolution, certify non-crossing and linking throughout the line search, and only then retry stationarity and Hessian classification. No electron identification is made.
- `ELEC-006` EXTENDED TOPOLOGY-PRESERVING VARIATION (Failed): certified descent persists without constrained stationarity — ELEC-006 extended the certified linked-sector optimization using active separation-constraint tangent projection, limited-memory BFGS directions, deterministic projected-gradient fallback, adaptive 128/256-point polygonal refinement, and topology-certified Armijo paths. Three deterministic four-step restart stages produced 12 accepted steps. Physical energy fell from 16.246060040 to 16.172284192 while the final state remained certified with d_min=0.06591359, Lk128=-1.00676822, and Lk256=-1.00344345. Four locked bars pass: adaptive certificate, path certification, physical descent, and extended run depth. The stationarity bar fails: the feasible projected-gradient ratio ends at 0.4362766 and does not trend toward zero. INTERPRETATION: simple optimizer under-running is no longer a sufficient explanation; the present K=8 Poisson curve-field functional has not yielded a constrained stationary linked soliton. This is a kept negative result, not a proof of nonexistence. NEXT ORDER: change the variational representation or preregister an additional stabilizing term before resuming Hessian or electron-property claims.
- `ELEC-007` AUGMENTED-LAGRANGIAN LINKED-SECTOR SEARCH (Failed): certified descent persists without constrained stationarity — ELEC-007 replaced the projected L-BFGS step rule with a buffered Powell-Hestenes-Rockafellar augmented Lagrangian and SQP-style linearized separation correction, while retaining the unchanged K=8 Poisson curve-field physical energy. Every accepted path was certified at 128 polygonal samples and every accepted endpoint at 128/256/512 samples. Five accepted steps lowered energy from 16.152913943 to 16.137416994. The final state remained certified with d_min=0.06578210, Lk128=-1.00763139, Lk256=-1.00386185, and Lk512=-1.00194220. Four locked bars pass, but constrained stationarity fails: ||P grad L||/E=0.2597405 versus the 0.05 ceiling. INTERPRETATION: augmented-Lagrangian/SQP treatment and higher endpoint resolution continue certified physical descent but do not establish a constrained stationary linked soliton in the current K=8 global Fourier representation. This is a kept negative result, not a nonexistence proof. NEXT ORDER: change to direct spline/polygonal degrees of freedom with adaptive remeshing before adding new stabilizing physics.
- `ELEC-008` Adaptive direct-spline representation does not establish constrained stationarity — ELEC-008 replaced the global K=8 Fourier chart with direct periodic cubic-spline controls and a curvature/proximity-driven 16-to-20 control-point remesh, while keeping the Poisson curve-field energy and hard separation/linking certificates unchanged. Eight accepted topology-certified steps were obtained. The remesh remained certified and changed energy by 0.588%, within the locked 1.5% fidelity bar, but final physical energy 16.228949267 exceeded the campaign start 16.162510362 and the final projected physical-gradient ratio 0.1452795 exceeded the 0.05 stationarity ceiling. INTERPRETATION: direct adaptive representation has not produced a representation-independent constrained stationary linked soliton, and remesh energy neutrality is now an explicit numerical debt.
- `ELEC-009` Variational remeshing fails the strict numerical-consistency gate — ELEC-009 retained the unchanged Poisson curve-field energy and hard separation/linking certificates while adding separate nonuniform periodic knot vectors and a topology-certified variational remesh candidate search. Five accepted constrained steps remained certified, and identical-geometry 48-to-64 source-quadrature energies agreed to 0.0248%, passing the quadrature bar. However, the 16-to-20 control remesh changed energy by 0.535%, RMS geometry by 0.0521, and Hausdorff distance by 0.0531, all above the strict locked remesh tolerances. Final energy 16.231969041 remained above the campaign start 16.162510362, and projected gradient / energy was 0.1508681 versus the 0.05 ceiling. INTERPRETATION: quadrature is not the dominant debt; the adaptive transfer operator is still too disruptive for a clean representation-independence test. This is a kept numerical failure, not a nonexistence result.
- `ROPE-MODE-001` Standing-wave shell gate: certified rope modes are ordinary closed-string harmonics — ROPE-MODE-001 tested the simplest version of the atomic-shell standing-wave hypothesis on the certified ELEC-009 linked geometry. A periodic finite-element constant-tension wave operator was assembled on each closed component and evaluated at 128/256/512 samples. The paired frequencies are mesh converged and follow omega_n/omega_1=n to R^2=0.99999997. Hydrogenic 1/n^2 or 1-1/n^2 templates fit far worse, and the observed fourfold scalar multiplicity does not reproduce shell capacities 2n^2. INTERPRETATION: the rope does support discrete standing waves, but geometry plus constant tension yields ordinary string harmonics, not atomic electron shells. This is a kept negative gate on the simplest shell interpretation; additional preregistered central, angular, nonlinear, or fermionic dynamics would be required.
- `ROPE-MODE-002` Central-field standing waves do not form robust atomic orbital multiplets — ROPE-MODE-002 kept the certified ELEC-009 linked geometry fixed and added the smallest central-field extension to the scalar standing-wave problem: a periodic finite-element rope Laplacian plus V(r)=-alpha/sqrt(r^2+eps^2), swept over alpha=0.1 to 10 with fixed softening and evaluated at 128/256/512 samples. Eigenvalues were mesh converged to a worst 256-to-512 change of 0.5135%. The central field split ordinary sine/cosine pairs into mostly singlets and residual doublets, but no coupling produced the preregistered 1,3,5 spatial multiplicities, s/p/d spherical-harmonic classification, or robustness across adjacent couplings. INTERPRETATION: a central interaction alone does not convert a scalar field confined to a one-dimensional closed rope into the rotational multiplets of atomic orbitals. This is a kept negative gate, not a test of a full higher-dimensional rope-plus-field atom.
- `ROPE-MODE-003` A surrounding 3-D field yields angular families but fails the localization gate — ROPE-MODE-003 moved the scalar excitation from the one-dimensional rope into a surrounding three-dimensional Cartesian field while keeping the certified ELEC-009 linked geometry fixed as an embedded tubular potential. A softened central attraction was used with alpha=2, and rope coupling beta was swept over 0, 0.25, 0.5, and 1 on 23^3 and 29^3 interior grids. The first nine excitation gaps converged to within 1.12%. The low modes classified robustly as one l=0 state, three l=1 states, a second l=0 state, and five l=2 states, with mean angular projection purity near 0.91; the cubic box split the d-like family approximately 3+2. However, only about 51% of the first twelve modes lay inside radius 2.2, failing the locked 85% localization bar. INTERPRETATION: three-dimensional field degrees of freedom recover recognizable s/p/d angular families, unlike a field confined to the rope, but the current finite-volume calculation does not establish well-isolated bound states and does not show that the rope causes the multiplets, since they already occur at beta=0. This is a kept numerical gate failure and a partial structural result, not an atomic-spectrum derivation.
- `ROPE-MODE-005` THE RESOLVED-TUBE SPECTRUM IS A SINGLE LEVEL: THE SHELL QUESTION IS PREMATURE AT THE CERTIFIED GEOMETRY, AND THE FAILURE EXPLAINS ITSELF (Failed, kept; floors measured before bars, the rule honored at last) -- the tube finally resolved (h/sigma = 0.65 and 0.52, repairing ROPE-MODE-004's central defect; free-box continuum edge 2.8905 and cross-grid jitter 0.012 measured FIRST), with bars locked (B1 >= 4 bound modes at beta = 8, fallback beta = 16 declared in advance; B2 gap convergence; B3 ring-vs-harmonic classification): B1 FAILED AT BOTH DECLARED SETTINGS -- beta = 8 binds exactly ONE mode (2.339) and beta = 16 exactly ONE (1.636), no spectrum exists to classify and B3 cannot be posed; THE FAILURE EXPLAINS ITSELF QUANTITATIVELY -- the loops' measured arc lengths put the first longitudinal ring excitation at (2 pi/L)^2 ~ 3, LARGER than the entire binding depth (1.25 at beta = 16): the resolved rope binds one transverse channel whose longitudinal excitations land above the continuum, a single-level binder BY GEOMETRY; WHAT SURVIVES -- (a) THE COOPERATION SIGNATURE: the linked pair's bound level (2.339 at beta = 8) sits 0.23-0.28 below BOTH single-loop levels (2.615, 2.565), a 22x-floor deviation: the one bound mode is a HYBRID spanning both tubes, the pair binding what neither loop binds alone; (b) THE INSTRUMENT IS FIXED: E0 grid-stable to 0.01 where ROPE-MODE-004 saw 106-percent chaos -- the resolution program worked, and what it revealed is a one-level system; NAMED NEXT-ORDER: the regime map on synthetic circular tubes with controllable (beta, sigma, L) to chart where multi-level binding begins, then return to the certified geometry in that regime — The best kind of Failed: bars honored, fallback exhausted, and the null result arriving WITH its own mechanism attached. Three sessions of shell-hunting now compose into one clean sentence -- the naive reading died on string harmonics (001), the crutch was exposed (002/003), the rope was proven to bind (004), and tonight the resolved instrument showed that at the certified geometry the binding has room for exactly one level, because the loops are too short and the wells too shallow for longitudinal structure to fit beneath the continuum. That is not a defeat of the shell hypothesis; it is its FIRST QUANTITATIVE DESIGN CONSTRAINT: multiplets require binding depth well above (2 pi/L)^2, a regime the corpus can now target deliberately instead of stumbling toward. THE COOPERATION SIGNATURE is the session's unexpected gift and deserves its own future: the linked pair binds a hybrid mode that neither loop supports alone -- the first spectral evidence in the corpus that two tubes SHARE field structure -- and the obvious next discrimination (linked versus unlinked-proximate pairs at matched separation: does TOPOLOGY, not mere proximity, leave a spectral fingerprint?) is precisely the kind of sharp, cheap, falsifiable question this programme was built to ask. THE RULE FINALLY HONORED: after three violations, this session measured its floors before locking its bars, and the difference showed -- every number tonight has a known noise pedigree, which is why a null result this clean can be trusted to mean what it says. NAMED NEXT-ORDERS: the regime map (synthetic tubes, the design constraint charted); the linked-vs-unlinked spectral fingerprint test (the topology question, now concretely posable); the preconditioned stationarity push (ELEC-010's lane, still queued); and cut v2.3.9 -- the cycle is full.
- `NUCQ-003` THE STRUCTURAL STRAND COUNT IS DERIVED, THE ESCAPE IS CLOSED, AND THE MESOSCOPIC-HBAR PICTURE IS REFUTED (Failed, kept) -- THE DERIVATION: with rho = 3 T_tube/(n c^2 a^2) and the tube's linear mass density mu = T_tube/c^2 = rho pi R^2, the strand count and tube radius are related by R_tube = a sqrt(n/(3 pi)), equivalently n = 3 pi (R_tube/a)^2, which uses ONLY the Lorentz bound a and the tube radius and does NOT involve rho_vac -- so unlike the corpus's earlier route it is not circular; THE NUMBER: lattice QCD measures the intrinsic flux-tube width at 0.35-0.5 fm, so with a = 1e-16 m one gets n = 115 (R = 0.35 fm), 151 (0.40) and 236 (0.50), placing the corpus's n_t = 111 just below the range -- CONFIRMED by measurement rather than chosen -- and because a is an UPPER bound any smaller a gives a LARGER n, so n >= ~115 firmly; A COMPANION RESULT in the other direction: run forward at n = 111 and the predicted tube radius is 0.343 fm against a lattice value of 0.35-0.5 fm, a parameter-free agreement at the low edge worth registering independently; THE ESCAPE IS CLOSED: NUCQ-002 demoted NUCQ-001's falsification to conditional on the grounds that a SMALL structural n_t would rescue the mesoscopic-hbar picture (n = 1 giving 99 patches in He-4), but n cannot be small -- n = 115 gives T0 = 1.63e3 N, L_hbar = 4.40 fm and 0.081 patches in He-4; n = 150 gives 1.25e3 N, 5.03 fm, 0.054; n = 236 gives 7.96e2 N, 6.30 fm, 0.027 -- so n can only be larger, T0 only smaller, L_hbar only larger and the patch counts only smaller: THE BORN FAILURE GETS WORSE, NEVER BETTER; VERDICT: NUCQ-001 is RESTORED TO UNCONDITIONAL, the structural strand count being pinned above 115 by the measured flux-tube width and the Lorentz bound, which forces L_hbar >= 4.4 fm, which places every nucleus below one sub-quantum patch, which contradicts the demonstrated success of standard quantum mechanics in nuclei -- THE MESOSCOPIC-HBAR PICTURE, HBAR-005 AND EVERYTHING RESTING ON IT, IS REFUTED — The question was how many strands a flux tube contains and what fixes it. Both halves now have answers, and the answers close the door the previous session had opened. WHAT FIXES IT is the tube's own measured width. The relation R_tube = a sqrt(n/(3 pi)) follows from two definitions the corpus already had, involves only the Lorentz-bounded lattice spacing and the radius, and touches neither the vacuum density nor any fitted quantity -- so it escapes the circularity that made NUCQ-002's version unusable. Inverted against lattice QCD's 0.35-0.5 fm it gives n = 115-236, which brackets the 111 the corpus has been carrying and turns a choice into a measurement. THE FORWARD DIRECTION DESERVES ITS OWN LINE and is the session's one positive result: at n = 111 the framework PREDICTS a flux-tube radius of 0.343 fm with no free parameters, against a lattice value of 0.35-0.5 fm. That is a genuine parameter-free agreement with strong-interaction phenomenology, and it survives everything that follows. THE REFUTATION IS THE MAIN RESULT and it is now airtight in the direction that matters: n has a floor, not a ceiling, so every allowed value drives L_hbar UP and the nuclear patch count DOWN. There is no value of the structural strand count consistent with the flux-tube width that also lets Born statistics be exact in helium. The mesoscopic-hbar picture required hbar to be the action of a 4 fm patch; nuclei are 2-7 fm across and quantum mechanics works in them to percent accuracy; and the strand count that would have rescued this is excluded by the very strong-interaction data the framework otherwise fits. THIS IS THE RIGHT KIND OF DEATH. HBAR-005 was not vague or unfalsifiable -- it made a specific claim about a specific scale, the claim generated a specific consequence in a specific place, and the consequence is not observed. The corpus registers it Failed and keeps it, along with HBAR-006's nuclear-scale prediction which dies with it. NAMED NEXT-ORDERS: mark HBAR-005 and HBAR-006 as superseded-by-refutation and audit which of HBAR-001 through HBAR-010 survive independently (the standing-wave action formula and the L = 2S relation do not depend on the patch scale and should survive); register the flux-tube radius prediction as a standalone result in the nuclear sector, where it belongs; and accept that the quantum sector's route to hbar is now closed on both branches -- reconnection by ELEC-043, standing waves by this claim. [BAND CAVEAT by ELEC-050 (2026-07-31): the companion low-edge agreement (0.343 fm predicted vs 0.35-0.5 measured) IS the same residual as the T0/Sigma 4.5%: R_pred = sqrt(T_tube/(pi Sigma)) = 0.342 fm. One prediction, one external test: a lattice width determination excluding ~0.35 fm shifts the entire scale chain.] [MODERN-LITERATURE ADJUDICATION by ELEC-051 (2026-07-31): the 0.35-0.5 fm band conflated width definitions. Current state of the art (Baker-Cea-Chelnokov-Cosmai-Papa, EPJ C 85, 29 (2025); full QCD, 2+1 HISQ at physical masses): E-weighted RMS w ~ 0.5 fm, sech^2 profile at sqrt(gH0) ~ 1.0 GeV. Converted to the mass-density (E^2-weighted uniform-cylinder) radius this formula requires: R_eq = 0.404 fm, giving n = 154 and T0 = 1220 J/m -- a 28% tension with the Sigma-route 1700, sharper than the old factor-2 band but inside the stated conversion model-dependence. Definition spread logged: intrinsic tail lambda = 0.109 fm ((2+1)d SU(2), Verzichelli et al. 2025-26); Clem lambda 0.17-0.19 fm (SU(3) pure gauge).] [DATA-BACKED by ELEC-052 (2026-07-31): the definitive integral on arXiv 2409.20168v1's own points gives the mass-density radius 0.407(14) fm, n = 156, T0 = 1201 J/m. The 28% tension with the Sigma-route is now measurement-backed; the strand-count-follows-energy-density identification is the registered load-bearing assumption at the scale-chain fork.]
- `ROPE-SOURCE-AUDIT-002` UNDRIVEN CLOSED-ROPE CIRCULATION DOES NOT SOURCE A NONTRIVIAL OBSERVABLE HOLONOMY; INTEGER WINDING IS AB-SPECTRALLY TRIVIAL — Locked corpus audit. Static helical handedness fixes only response orientation; EM-014 requires prescribed angular rate (I=omega/2pi); EM-008 requires a pump/return; EM-010 takes J as input; no existing action selects or protects a nonzero circulation rate. The only derived phase is integral dtheta=2pi N, while the validated AB instrument is 2pi-periodic, so E_n(2pi N)=E_n(0). Finding NO_UNDRIVEN_NONTRIVIAL_PERSISTENT_HOLONOMY with structural obstruction INTEGER_WINDING_HOLONOMY_SPECTRALLY_TRIVIAL. Physical AB branch closed as unsupported under the current framework. ROPE-VALIDATION-001..004 retained as a validated gauge instrument with no nontrivial internally sourced target. Reopening requires a new, explicitly labeled postulate/mechanism deriving a stable non-integer holonomy and magnitude. [REVIEW REPAIRS (2026-08-01): the closure is now REPRODUCIBLE -- benchmarks/foundations/rope_source_audit_circulation.py re-executes each mechanism at zero drive (screw I=0 at omega=0, loop I=0 with no pump, field B=0 at J=0), carries positive controls proving the zeros are an absent source rather than a broken benchmark, and confirms numerically that every integer winding 2 pi N returns exactly the zero-flux spectrum while a half-integer phase would give a 0.5 response. OBSTRUCTION PROVENANCE recorded: the integer-winding triviality and the gapless-twist result are DERIVED obstructions; the absence of a rate-protecting invariant and of a metastable circulating minimum are ABSENCES, and the rate-protection absence is the soft joint any reopening will push on. Locked bar 7 (parameter determination) restored to the machine-readable summary.]

**Conjectural** — numerically suggestive or conditional; not established.
- `PM-001` Lepton mass ratios via Koide phase (3+Phi)=4.618 to ~1% WITH measured sin2thetaW — CONDITIONAL: with the model's own sin2thetaW=1/(3sqrt2) it FAILS (mu/e~1605). [STRUCTURAL CONTEXT (PM-003): the lepton mass problem is a 3-level knot EXCITATION spectrum, not composite counting -- which is why a Koide-type RELATION AMONG LEVELS (this claim) is the relevant kind of tool. The knot-count mass mechanism that works for nuclei (NUC-001) provably cannot apply here, since all leptons share winding number 1.]
- `EW-001` Weinberg angle sin2thetaW = 1/(3sqrt2) = 0.2357 from winding/Hopf geometry — ~1.94% from measured 0.23122; the 'soft external input' that breaks PM-001.
- `NUC-002` Strong nuclear force identified as rope-bundle contact force at nuclear scale (atomic contact physics scaled by ~1e5) — The paper's central proposal: the strong force IS the rope-bundle contact force that creates chemical contact at atomic scales (1e-10 m, eV), operating at nuclear scale (1e-15 m, MeV) -- 1e5x smaller, correspondingly greater energy. Quark confinement follows from fractional sub-knots being unstable alone (see NUC-003). QUALITATIVE identification with a scaling story; the paper lists the quantitative Yukawa potential from rope bundles as an OPEN problem. Not derived; a mechanism hypothesis. Registered as Conjecture. [UPDATE: NUC-004 upgrades this -- the Yukawa FORM is now derived exactly from the mode-overlap mechanism (Green's function of the screened mode equation); only the absolute range remains input, tracing to the EM-RECON-008 super-quadratic coefficient. The 'quantitative Yukawa from rope bundles' open problem is resolved at the level of FORM.]

## How to evaluate this corpus

- **Dependency graph:** `docs/dependency_graph.txt` — what rests on what.
- **Claim registry:** `claims.yaml` — every claim's status, paper, and benchmark.
- **Computed roadmap:** `docs/roadmap.md` — sector maturity, auto-flagged if any readiness outruns its evidence.
- **One-command verification:** `make verify` — runs every code-backed claim's benchmark.
- **Heartbeat:** `make heartbeat` — the core validation runs (currently 75/75 + 10/10 + 6/6).

The programme's strongest posture is not any single prediction; it is that the classical core can be independently audited, sector by sector and claim by claim, and that its boundaries are stated rather than blurred.

