# v2.3.7 — Two branches, asymmetric fates

Corpus state: **242 registered claims (102 Derived, 121 Modeled, 4 EFT-constrained, 3 Conjecture,
5 Open, 7 Failed-and-kept); 232 code-backed benchmarks, all passing; 60 papers.**

Nine claims and a full comprehension layer. The QGATE campaign — three days that converted
"derive quantum mechanics" from an aspiration into an itemized account — plus the reviewer-
driven outreach infrastructure: the Roadmap of Knowledge, the Eleven Results narrative, eight
status-labeled figures, and the corpus's first outside-proposed theorem.

## The Layer Separation Theorem (THM-006)
The corpus's failures are not scattered: classified by REQUIREMENT into Topological/Geometric/
Dynamical layers, the Failed+Open claims concentrate at Layer III with 4.2x enrichment (the
layer is ~10% of claims and holds 5 of 12 failures). An adverse first draft (a vocabulary
classifier returning 0.5x, ANTI-clustering) is kept in the benchmark docstring; the corrected
requirement-based classification produced the signal and its honest nuance (four failures are
ordinary classical falsifications, counted). The hero figure (rope_ladder.png) is this
theorem's picture.

## The QGATE campaign (QGATE-001..008)
- **001 — the amplitude-scaling test** (bars locked before data): NO action floor exists in
  smooth rope dynamics (nu = 1-2, every candidate); the one non-smooth native process —
  reconnection — carries an amplitude-independent separatrix action W = 1.80 T D²/c. Honest
  magnitude: W ~ 0.009 hbar, stated in the claim title.
- **002 — the quantum-input ledger**: 92 claims say "quantum," 11 mean it, FOUR inputs carry it
  (electron mass, spin degeneracy, the 13.6 eV calibration, the induced-gravity normalization);
  14 claims are PREVIOUSLY-QUANTUM-NOW-CLASSICALIZED — the lambda saga is a repeatable move;
  the nonlocal residues carry ZERO scale inputs.
- **003 — the cross-sector transfer test**: three sectors demand the SAME normalization (spread
  4.6%, with the nuclear inversion genuinely independent); the gap becomes the forced
  prediction n_t ~ 111 collective strands, D/w ~ 19.
- **004 — the tube census**: underdetermined by exactly one ratio (the constituent width); and
  a kill line nobody arranged — the strand-sector solidity criterion bisects the demanded
  coverage window at f_c = 0.160, and the percolation measurement (0.309) had already landed on
  the living side.
- **005 — the trilemma**: {tension additivity, Sigma_ATLAS, the Lorentz bound, n_t = 111} are
  mutually inconsistent by 5e10; any three kill the fourth; the verdict handed to experiment.
- **006 — the PVLAS confrontation**: the final PVLAS band brackets BOTH signs inside one sigma
  — the discriminator is live, unexcluded, and ~7x of sensitivity from decision (VMB@CERN's
  design target).
- **007 — the ATLAS computation**: unpolarized light-by-light is STRUCTURE-BLIND (proven by
  direct helicity amplitudes — polarimetry's monopoly is a theorem); the ATLAS Sigma pin snaps
  on both sides (contact-vs-box energy shape; rope trans-cutoff); the trilemma relaxes into the
  PREDICTION Sigma >= 5.1e35 J/m³ (invoice: >= 5.6e18 kg/m³ of non-gravitating vacuum tension,
  recorded); and the wager's payoff matrix INVERTS. Two week-old claims corrected by the
  campaign's own arithmetic; correction pointers filed.
- **008 — the synthesis**: a capstone that polices itself — its benchmark re-verifies the whole
  chain (claims, pointers, ledger, funnel constants) on every run, and it caught its own first
  drift on its maiden run.

## The frontier's new shape
THE SCALE BRANCH is one conditional chain: if collective reconnection (n_t ~ 111, D/w ~ 19) and
Sigma >= 5.1e35 J/m³, then hbar = W_collective serves all four quantum inputs with zero
retuning — alive, expensive, precisely invoiced, and decided by VMB@CERN-class polarimetry
under the inverted payoff (a QED-like result SUPPORTS the candidate; a rope-signature negative
kills it). THE NONLOCAL BRANCH (Born equivariance, measurement, Bell) carries zero scale
inputs, is immune to every campaign result, and is missing a kind of dynamics, not a number.

## The comprehension layer (reviewer-driven)
The Roadmap of Knowledge (wall chart + self-contained interactive dependency explorer + the
textual ladder, all generated from claims.yaml); "Eleven Results We Did Not Expect" (the
non-technical narrative, README front door, category-tagged per review); eight status-labeled
figures (atom, electricity, magnetism, chemistry, gravity, optics, nuclear, and the hero
ladder), four of which passed through a correction gate before inclusion; the figures index as
the honesty layer; the quantum ledger and QGATE synthesis documents; requirements.txt and the
CI workflow fix (shipped separately as github-workflow.zip — drop into .github/ once).

## What stands, said plainly
No knot is identified with any particle. The framework's hbar candidate survives not by winning
but by invoice: a stated vacuum-tension prediction, a named matter-sector debt, and a scheduled
experiment whose ordinary outcome now favors it and whose anomalous outcome executes it. The
smooth mesh will never yield an action quantum — proved three ways. The nonlocal branch is
untouched and said so. Every hypothesis is marked; every correction is filed; and the corpus's
own capstone fails loudly if any of this drifts.
