# v3.27.2 (18 Aug 2026) — STAGE 2: the state is real, the pins are far, and the resonance has a tenant

**733 claims.** One new claim (FND-142), one benchmark (heavy class),
one locked bars doc, one results doc. Sigma_wave untouched.

## The one-paragraph version

The two-frequency composite state FND-140 adjudged **exists** — the
solver holds machine-precision members of the invariant-torus branch
from infinitesimal amplitude to A2 = 0.0108, satisfying pointwise
inextensibility exactly (the property the ansatz violated), with the
linear internal spectrum measured at **Om2 = 3.201 / 2.221** (0.48x /
0.34x the composite build; the FND-139 display's direction confirmed,
its magnitudes superseded). But the registered amplitude is
**pin-unreached-in-search**: the aligned branch runs into a
near-vertical gamma(A2) tangent at A2 ~ 0.0102 and steepens toward
the z' -> 0 parametrization boundary an order below R2 — with the
harmonic tail concentrating slope far beyond superposed geometry —
while the anti-aligned root sits **exactly on the Om1/2 subharmonic
resonance** (the n = 1 cell mode): FND-140's resonant-k2 exception
window, named as possible-in-principle and unexplored, is inhabited.
Sigma_wave is not re-priced (the pricing was chartered from the
solution at the registered pins, and no such solution was reached);
the FND-139 rider stands; the author's interim deferral continues.
Two sharp next-orders are named: the steepened-regime traversal in a
general-position representation, and the resonant root by degenerate
perturbation or a de-rationalized cell.

## House

Bars locked before computing (analysis/TRUESTATE_stage2_bars_LOCKED.md);
results at analysis/TRUESTATE_stage2_results.md; benchmark
benchmarks/foundations/truestate_stage2.py (heavy; ~20 min). A 48x20
build HALTED at its own level-1 recovery bar and was sharpened — the
halt is the control working. Three instrument catches on the record,
each under a printed control; the benchmark gains a Jacobian
sparsity-pattern control. Full suite verified green by the author on
CI before this session; this cut adds one release of debt.
