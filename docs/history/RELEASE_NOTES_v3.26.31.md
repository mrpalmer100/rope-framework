# Release Notes -- v3.26.31 (15 Aug 2026): The Fourteen-Release Day

*Consolidates v3.26.17 -> v3.26.31, all cut on 2026-08-15. 679 -> 691
registered claims. Three arcs closed or parked at named termini, one
sector question resolved the honest way, two author-caught corrections
registered by name, three discipline rules ratified.*

## The gravitomagnetic book closes (GRV-115..118)

The SHIFT-C3 grant's last owed condition -- the GRV-055/071/106
supersession commission -- discharged with a MIXED verdict the bars
admitted in advance: two claims superseded in part with their
surviving content named, and the impossibility certification NOT
superseded, because it named the terms of its own defeat and was
defeated on exactly those terms, which is a certification working. The
sector's binding one-line status entered KNOWN_LIMITATIONS at equal
volume, not softened: granted, sourced, corrected, bounded, kept -- a
term in the action, barred from advertising LARES-class kill tests,
at most 2.49e-19 of natural strength if the vacuum weave is its
source.

The routing audit (GRV-116) then asked GRV-106's stored question --
do the sector's remaining conditional predictions route through the
capped slot? -- and was CORRECTED THE SAME DAY (GRV-117,
author-caught): its propagation leg had cited EM-RECON-023 by title
("THE TORSION CARRIER") while missing that the claim registers that
candidate's kill. The corrected chain was then completed by the
existence-first commission (GRV-118): twist radiation EXISTS-VIA-LOCK.
The screw-stretch branch is live dynamics (its kill was scoped to the
photon identification); the source couples by identity at the pinned
beta_J = 1; and transport exists only through the twist-stretch lock
chain, because the same azimuth-blindness exactness that protects
m_gamma = 0 kills direct crossing transfer. One identity protects the
photon's masslessness and throttles twist radiation; whether that is
deep consistency or coincidence belongs to the vertex session, whose
three obligations are enumerated with every input registered and
derived.

## The scale ladder is drawn (FND-110)

SINGLE-ANCHOR: the corpus fixes an absolute length in exactly one
place, the vacuum-strand M-point. The rope level is suspended on
n_rs, the fine level on (m, n_sub); three underived integers are the
complete list separating the other rungs from calibration. The sweep
also surfaced, and deliberately did not resolve, the registered
ELEC-052 fork appearing in the numeric value of w.

## The k-string question resolved the honest way (FND-111..113)

The acquisition ran first: the SU(3) flux-tube transverse profile
adopted EXTERNAL-ADOPTED in the Clem-ansatz form
(Cea-Cosmai-Cuteri-Papa, PRD 89, 094505), parameters quoted and never
re-fit, with the newer large-lattice series recorded supplementary
because adopting it would have required a re-fit the bars refused.
The author then granted GRANT-EXDYN-OVERLAP -- exchange amplitude is
profile overlap along the transport path, ZERO new constants, grant
text fixed blind before any integral -- and the pre-built three-shadow
adjudication killed it the same day: the zero-parameter
self-consistency equation has NO solution for any finite N across the
whole sensitivity band. The Clem profile's K0 tail decays too slowly
for the granted path reading; the demand exceeds unity at every
separation. Structural, not tuning. A grant that pays and a grant
that dies at its own bar are the same discipline working, and the day
had one of each. The sector parks with its successor's constraints
named: decoherence must arise at near-coaxial geometry, and no rescue
of the overlap form is admissible.

## The core-closure arc runs to a named terminus (EM-RECON-034..038)

Five sessions, each narrowing the last:
- The 2D profile computation largely convicted the 1D idealization
  behind the old fourfold miss (the cross-terms are
  geometry-suppressed in the physical dimension) and returned a
  razor-thin INDICATED it disclosed against its own interest.
- The window-narrowing commission then found the swept parameter DOES
  NOT EXIST: the registry had already constructed the mode profile
  (w(u) e^(-u), all lengths in xi units, no free core ratio), and
  both prior closures had run invented families instead of reading
  it.
- The registered-class closure ran on the corpus's own profile and
  FAILED at the policy amplitude with nothing left to blame --
  isolating the operating-amplitude identification as the last
  suspect.
- The policy audit found that identification was never registered at
  all: an onset threshold adopted as a disclosed modeling step. One
  registered amplitude-fixing mechanism exists in 691 claims:
  collision saturation.
- The caged derivation ran that mechanism to NO-BINDING-CONTRADICTION
  (strains six orders below threshold in chemistry, a factor 13-24 in
  the nuclear sector) at the registered wall gap, with gap
  substitution barred in advance.

Both registered amplitude candidates are dead; the corpus contains no
third. The arc parks on the PVLAS/VMB@CERN polarimetry clocks with a
registered trilemma naming where any future internal move must break
ground. What survives: the coherence-blind extension of collision
saturation, a true constraint on all future amplitude stories.

## The day's ledger

Two author catches, both in the prose layer (a handoff desk listing;
a title read as a verdict), zero registry-layer errors found; both
corrected by named claims or named corrections, never silent edits.
Three discipline rules ratified into STRATEGIC_TARGETS: a title is
not a verdict; before idealizing any object, check whether the
registry already constructed it; the prose layer fails more often
than the registry layer and is corrected in the open.

Failed-and-kept ledger brought to parity (45/45) in
KNOWN_LIMITATIONS, twenty entries behind before this release. All
generated docs regenerated; counts synced; version metadata aligned.
