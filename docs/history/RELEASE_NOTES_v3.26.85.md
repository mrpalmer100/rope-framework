# v3.26.85 (18 Aug 2026) — STAGE 1: the equations eject the second winding, and the true state moves up a frequency

**731 claims.** One new claim (FND-140), one benchmark, one locked bars
doc, one results doc; Sigma_wave rider updated.

## The one-paragraph version

The commissioned true-state solve opened in the relative-equilibrium
class — rigid rotation, inextensible, adjudicated force law, the class
containing the verified level-1 state as its closed-form member — and
returned a nonexistence result of unusual cleanliness: continuation
from the exact level-1 member at a level-2 pin of 1e-3 converges with
the residual EQUAL to the pin violation — the solver satisfies the
equations exactly by driving the level-2 amplitude to zero. Rigid
rotation demands Omega² = Tk² per mode; two wavenumbers cannot share
one Omega; the sidebands don't rescue it. Three sessions close into
one picture: the FND-135 ansatz's two-frequency FORM was right, its
numbers were exact averages of a non-solution, and no one-frequency
solution exists for it to have relaxed into. The true composite state
is a two-frequency object — an invariant-torus state — and stage 2 is
chartered to solve it, ansatz as seed, refit display bracketing the
search. Sigma_wave is not re-priced (nothing to price from); the rider
stands.

## Files

- FND-140 (Modeled); benchmark `benchmarks/foundations/truestate_stage1.py`
- `analysis/TRUESTATE_stage1_bars_LOCKED.md`, `analysis/TRUESTATE_stage1_results.md`
- Registry: Sigma_wave rider updated
