# v3.27.1 (18 Aug 2026) — TRUE-SOLVE: the ceiling of record moves, and the kb = 0 sector proves itself untouched

**732 claims.** One new claim (FND-141), one benchmark, one locked bars
doc, one results doc; the FND-126 instrument imported, not rebuilt.

## The one-paragraph version

The only mover of record moved. The Bloch bending re-solve under the
adjudicated true curvature lands the feasibility ceiling at
**kb <= 0.09332 T0_f a_f^2** — looser than the 0.079 of record, as
the zeroth-order display's direction predicted, and below the
display's 0.102 value: at the feasibility edge the anchor system
re-solves jointly (k_f moves to 8.557), so the energy-weighting ratio
is a **direction estimator, not a value estimator**, and that finding
is registered alongside the number. The instrument-unchanged proof is
the release's quiet backbone: at kb = 0 the bending channel is the
only consumer of the curvature field, and the anchors reproduce
FND-126 identically (k_f = 9.008, T = 1.501, window drift 0.0343%) —
every kb = 0 object stands untouched by construction and by
measurement. SHIN7's owed worst case is paid (max |kappa_true| =
4.4689, the 5.713 bound holding at 1.28x); KBSAT's candidate values
0.126 and 0.282 both remain outside the new ceiling (desk display
only); the 17.926 re-derivation is gated on TRUE-STATE stage 2 by
name. FND-131's dynamical-background gap carries unchanged on the
new bound. Sigma_wave untouched per the author's deferral — the
FND-139 rider stands through stage 2.

## House

Bars locked before computing (analysis/TRUESOLVE_bars_LOCKED.md);
results at analysis/TRUESOLVE_results.md; benchmark
benchmarks/foundations/truesolve_bloch.py. The imported ANSATZ module
replays its halt banner before this commission's output — cosmetic,
per the standing annotation. Full benchmark suite debt now NINE
releases; run overnight before stage 2.
