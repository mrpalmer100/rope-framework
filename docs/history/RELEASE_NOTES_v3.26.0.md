# RELEASE NOTES -- v3.26.0 (2026-08-13): THE ADJUDICATION RELEASE

*Consolidates v3.25.1 through v3.25.12 (twelve patch releases, one
day). 657 -> 667 claims; 41 -> 42 registered Failed-and-kept. The
registry (claims.yaml) is the authority; this page is the day in one
read.*

## The headline: external data adjudicated the k-string sector on the
## corpus's own pre-registered terms, and both verdicts were taken

The demanded continuum SU(6) k-string determination turned out to
exist already (Athenodorou-Teper 2021, found by literature hunt,
predating the corpus's derivation by five years -- the confrontation
was blind in the direction that matters).

- **FND-101 (Commission NUN, split verdict):** the sine law's
  FUNCTIONAL CLASS is REJECTED on continuum data (nonzero leading
  1/N preferred at chi2/ndf 0.5 vs 2.2; sine has identically zero
  1/N term). The binding structure only GRANT-N2's label carrier
  could express (FND-053) is the structure the vacuum exhibits. The
  old sine-favouring record is superseded. The exact derived
  coefficient misses at 3.8 sigma at fit level.
- **FND-102 (Commission NUN2, Failed and kept -- THE KILL):** at
  point level (Table 14: SU(6) 1.654(13), safe-volume group) the
  pre-registered FND-092 kill clause FIRED: 4.3 sigma above the
  softened-Casimir band top against the pre-committed 3-sigma
  threshold, honored without adjustment within 48 hours of the
  band's registration. Exact Casimir dies at 4.15 sigma from above;
  sine at 6.0 sigma from below. The vacuum sits strictly between
  the laws. Kept lesson: the fit-level neither-fires reading was an
  artifact of a correlation-blind error three times the true one;
  the bars had pre-permitted exactly one point-level re-read.
- **FND-103/104 (NUN3/NUN4):** eleven candidate shortfall mechanisms
  eliminated across two locked sweeps. Fixed suppressions die on
  N-dependence; all eight countable decoherence weights fail a
  three-part bar whose safe-volume profile clause executed the
  two-committed-number seduction w = 4/(N-1) at 11.4 sigma. The
  surviving family requires PARTIAL AND PERMANENT orientation
  decoherence (asymptote strictly above 1/2).
- **FND-107 (NUN5, certificate):** the one counting inside the
  surviving window is UNWRITABLE in the registered exchange language
  (the grammar counts channels; it has no weights). SAMEKH-class.
- **FND-108 (NUN6, scoping):** the missing structure is ONE object
  seen from two directions -- the exchange interaction's dynamical
  form (orientation weights AND bound-pair separation). Tube width
  already registered (ELEC-050, 0.342 fm); profile shape acquirable
  from lattice data. THE EXCHANGE-DYNAMICS GRANT QUESTION is on the
  author's desk, deliberately pending, with a three-shadow
  acceptance test pre-built.

## The pin's clock goes kill-ready

- **FND-105 (TET2, verified null):** blind-bar literature sweep --
  NO admissible SU(3) 4d determination tighter than Bali 2000's 5
  percent exists anywhere in 2000-2026. The kappa_pack floors are
  Conjecture-held because the deciding measurement has been
  technically routine and unperformed for a quarter-century.
- **FND-106 (TET3, the adjoint bands):** CHET's forward predictions
  (-1.25 / -0.25 percent) pre-registered as decision bands with a
  tiered grammar (pre-accepted TOWER KILL at 0.25 percent precision;
  sign clause killing the BET softening on any resolved positive
  deviation; frozen-pipeline self-measurement rule). Blindness
  provable from the null. Standalone measurement spec for external
  lattice groups: docs/ADJOINT_CS_MEASUREMENT_SPEC.md.

## The substructure arc's protection is complete

- **EM-RECON-033 (SHIN8):** the refinement-invariance audit runs SIX
  OF SIX, NO BREAK -- c (exact algebra), Sigma and the hbar route
  (by-inputs), the T0 fork (untouched both directions), zero-point
  (closed same day, below), and k_f/T0_f = 8.09 REFINEMENT-PROOF at
  every n_sub (the falsifier confronts any future fine-level
  determination). Ride-along: hierarchy counts ruled DISTINCT
  (n_rs vs n_sub; NAME_REGISTRY addendum).
- **FND-109 (TSADE3, codification):** ZP-CONV registered -- the
  zero-point ledger integrates carried (strand-level) modes only;
  the lattice is the regulator (GRV-021's own registered words,
  promoted to the registry-wide rule it had always been in
  practice). Tripwire armed against silent fine-mode integration.

## Documentation

KNOWN_LIMITATIONS de-staled twice and extended (the FND-050 ledger
through the kill; the verified-null tower entry; the completed
substructure protection). The predictions paper synced three times
in one day (finally at v3.25.11), including a same-day correction of
its own hours-old fit-level sentence. HANDOFF rewritten; STRATEGIC
TARGETS T2/T4 current; README and STATE_OF_THE_PROGRAMME carry the
adjudication day; full doc sync at v3.25.12.

## The state after the cut

667 claims (601+ code-backed), 42 kept failures, two external clocks
armed on pre-registered terms (the adjoint bands; PSR J1713), one
grant question deliberately open (exchange dynamics, three-shadow
test pre-built), one queued interior item (the scale-class
determination). The day's lesson, once: a pre-registered falsifier
honored on schedule is worth more to the programme than the result
it killed.
