# v3.26.73 (17 Aug 2026) — THE CONTACT RULE SESSION: the bottleneck was sitting on its own answer

**719 claims.** One new claim (FND-128), three amended faces, one benchmark.
A chartering session in the FND-117 tradition: prices computed, nothing granted.

## The one-paragraph version

The corpus spent a year building an interpenetrability structure — contact
primitive at the bottom, tangibility emergent at a derived, zero-parameter
threshold (f_c = 0.309), contact-as-constraint where it operates — and the
fine weave's own sparse certification, registered two days ago for coverage-
audit reasons, is the number that plugs into it: **the fine weave sits 5–14×
below the corpus's own onset of tangibility.** Class C (contact-free) is
therefore not an invention but a reading, and it arrives derivation-shaped
where BASEFIBER and KBSAT arrived as commitments. Its price sheet is the
heaviest in the arc: adopting it **executes FND-121's own tripwire** (the
first grant candidate to arrive carrying another grant's execution warrant —
kb ≤ 0.079 converts to a determination, KBSAT auto-supersedes, the reverting
set is enumerated on the face) and it forces the winding's ontology from
force-held static geometry to **topologically maintained state** (Lk = Tw +
Wr), a re-reading bundled into the grant because without it the pre-stress
conversion does not run. Class A (clearance constraint) is the only door
left through which k/T0 = 2 can still derive at this instrument — 10.2%
non-affine softening carries 3.00 c to 2.844 c — at the cost of one
parameter the sparse certification says should not matter. Class B defers.
No class is null. The decision — class, and if C, the maintenance reading —
is at the author's desk.

## Files

- FND-128 (Modeled), benchmark `benchmarks/foundations/contact_rule_candidate.py`
- Amended: FND-123, FND-121, FND-124
