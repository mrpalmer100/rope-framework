# Release v2.5.1 — The hbar reckoning: two routes dead, one scale adjudicated, one prediction confronted (2026-07-31)

Eight claims in one arc, ELEC-044 through ELEC-051, all bars locked before
computation. The arc set out to derive hbar from strand reconnection and
ended by killing that route at both joints, adjudicating the corpus's most
load-bearing scale, and putting one number against the current lattice
state of the art.

## The kill (ELEC-044..048)
- ELEC-044 re-derived the WKB separatrix action honestly (catching a unit
  bug invisible in natural units) and found the coherence-cell fit.
- ELEC-045 derived the 3D two-strand action in closed form,
  W = (pi/4 sqrt3) T w^2 / c = 0.4534 T w^2 / c, with D = w derived, not
  assumed -- killing the fitted cell (locked band violated).
- ELEC-046 re-ran the QGATE-008 funnel at the derived action: every demand
  moves coherently (111 -> 498, nuclear leg independently 475), retiring the
  two-normalization inconsistency.
- ELEC-047 ran the bundle census: geometry says 498 strands fit; dynamics
  says no mechanism recruits them (no separatrix slowing exists -- the locked
  premise itself was wrong, caught by the arc's own instrument). THE
  RECONNECTION-HBAR CHAIN IS DEAD AS A DERIVATION.
- ELEC-048 swept four ordering candidates: four noes -- and discovered the
  pre-existing HBAR campaign (v2.5.0) had already superseded the route via
  standing waves, uncited by ELEC-044..047. Corrections filed. The frontier
  consolidates to ONE open problem: scale selection on the standing-wave
  branch.

## The adjudication (ELEC-049..050)
- ELEC-049 adjudicated the two-ledger w discrepancy: w = 5.78e-17 m
  (Ledger B -- one number wearing four hats, pedigree clean). Ledger A was
  never registered; it was a promotion error inside this very arc, caught
  and corrected. All arc verdicts survive re-basing (the deaths strengthen).
- ELEC-050 re-posed the "4% T0 tension" honestly: the two agreeing routes
  were one route plus an identity, and the true uncertainty was the
  conflated lattice-width band -- factor 2, not 4%. It priced the fix at
  zero cost and named it: read the current lattice literature.

## The confrontation (ELEC-051)
- The reading found the old 0.35-0.5 fm band was THREE width definitions
  wearing one label (intrinsic tail 0.109 fm; Clem lambda 0.17-0.19 fm;
  E-weighted RMS 0.5 fm), spanning 5x in the literature.
- Against the state of the art (Baker-Cea-Chelnokov-Cosmai-Papa, EPJ C 85,
  29 (2025); full QCD, 2+1 HISQ at physical masses), with a conversion
  instrument validated to 0.4% against the paper's own headline, the
  mass-density radius the framework requires is R_eq = 0.404 fm.
- Verdict: R_pred = 0.342 fm vs 0.404 fm (+18%) -- CONSISTENT WITH TENSION.
  Propagated with one consistent sign: n = 154, T0 = 1220 J/m,
  Sigma_eq = 3.66e35 J/m^3, each 28% below the Sigma-route registrations.
  The factor-2 band contracts to a single 28% question.

## Self-catches (six, all by the arc's own instruments)
Unit bug (ELEC-044); locked-formula mis-derivation, twice (ELEC-045);
wrong locked premise (ELEC-047); uncited supersession (ELEC-048);
unregistered-scale promotion (ELEC-049); definition conflation (ELEC-051,
inherited from NUCQ-003 and resolved).

## Open at cut
- THE 28% DECIDER, blocked-with-path: the definitive E^2-weighted integral
  on the paper's actual lattice points. The data is public in the arXiv
  ancillary files of 2409.20168 (key file: ENP_1064_0532.agr, the
  nonperturbative profile at d = 1.064 fm), but arXiv /src is unreachable
  from this environment (robots-disallowed for fetch; domain not in the
  bash allowlist). PATH: download https://arxiv.org/src/2409.20168v1/anc
  manually and upload the anc files to a session; the computation is one
  script against the validated instrument in
  benchmarks/foundations/elec_lattice_width.py.
- Scale selection on the standing-wave hbar branch (HBAR campaign frontier).

Corpus at cut: 343 registered claims, 329 code-backed, 3/3 tests green.
