# v3.26.70 (17 Aug 2026) — COMMISSION BLOCH-L: the constant does not derive, and the mapping is superseded in form

**717 claims.** One new claim (FND-126), five amended faces, one benchmark,
one superseded structure, two named corrections, one grant left standing on
notice.

## The one-paragraph version

The computation chartered to decide whether `k/T0 = 2` is a theorem ran, cleared
its locked reading window with 0.034% drift against a 0.5% bar, and read
`c_L,f = 3.00 c` — which converts to `r_s = 0.2365 a_f`, off the derive-point and
inside FND-122's CONFLICT region. **`k/T0 = 2` does not derive.** FND-114 stands
where it stood, ADOPTED-ADJUDICATED, and the alpha chain's inheritance rider is
unchanged. The sheet's "angles or mapping" fork resolved cleanly and not in the
direction anyone expected: **the mapping is the defendant**, and it is convicted of
a form error rather than an arithmetic one.

## What was found

**The dynamical mapping law, exact closed form.** Imposing the two registered
anchors — `c_T,hom = c` (the SHIN transverse-c invariant) and
`c_L,hom = sqrt(k/T0) c` (FND-114's channel floor) — on the affine filament-network
response gives

    k_f/T0_f     = 6 (k/T0) − 3            →  9 exactly at k/T0 = 2
    T_fibre/T0_f = 9/2 − (3/2)(k/T0)       →  3/2
    positivity of the fibre tension  ⇒  k/T0 < 3   (a new structural bound)

This is exact at the derived angles, not approximate, because FND-088 derived those
angles to make the fourth-order orientation tensor isotropic — and the affine
response of a filament network depends on the orientation distribution *only*
through its second and fourth moments. Control: isotropy to 8.9e-15, reproducing
FND-088's registered 2.9e-13.

**Why the angles walk free.** The law contains no winding angle. Any isotropic
winding returns it. A theory whose answer does not move when you move its
parameters has told you the parameters are not on trial. FND-117's factorization —
multiplicative, angle-dependent — is **superseded in form**, superseded-not-erased.
Its refusal of the gamma/stretch-ratio name-adjacency, its exclusion of an
inextensible bottom fibre, and its non-circularity argument all survive untouched.

## Two corrections, both found by controls

**The thirteenth catch: the charter's own radius input was convention-inverted.**
`kappa` is convention-blind (`sin 2psi` is invariant under the swap) and therefore
cannot adjudicate — which is exactly why the inversion survived the form the corpus
had checked hardest. `tau` and `R` do adjudicate, and FND-088's isotropy theorem
decides against the registered reading. Corrected: `R_1 = 0.22508 a_f`,
`R_2 = 0.09396 a_f`; `tau_1 = 2.0944/a_f`, `tau_2 = 4.6593/a_f`. FND-125's *method*
is vindicated and its *reading* inverted; GRV-112 carried the same inversion in from
an earlier session. FND-091/SHIN7's pricing and FND-088's angles are untouched.

**The instrument catch, disclosed before the run it governed.** One fibre
orientation per cell leaves the local energy rank-deficient — a filament penalises
only gradients along itself — and the first wound build collapsed to `c_L = 0.265`
with 5.7% directional spread. The registered medium is a *bundle*, carrying fibres
at all phase pairs, so each cell must sample the full phase torus. Repaired, the
engine converges by 16 fibres/cell and reproduces the affine limit to 0.05%,
isotropically, with the straight control exact to the same 0.05%.

**The sensitivity leg** ran the commission end-to-end on the charter's *uncorrected*
geometry. It lands in the same CONFLICT region (`c_L,f = 3.50 c`) while failing
FND-089's inherited isotropy bar at 17.06% spread against 0.05. The verdict does not
rest on the correction.

## What was deliberately not done

**The KBSAT tripwire was approached and not pulled.** With the bending channel at
the granted `kb = 0.126 T0_f a_f²` the input set is infeasible — it demands negative
fibre tension — and feasibility requires `kb ≤ 0.079 T0_f a_f²`, below the ceiling
FND-121 saturated. That is dynamical, measurement-shaped, and exactly the class
condition 1 names for auto-supersession. It is registered **conditional** instead,
because the omitted second-order pre-stress terms are the same order as the effect,
their sign is undetermined, and they depend on the unregistered contact rule. A
grant executed on a computation with a hole that size is a grant executed badly.

**The rigidity demand did not fire, and was not spent.** Transmission lands *at* the
static stretch-projection to 0.09% — because nothing registered makes the response
non-affine. FND-124's demand passes without doing the discriminating work it was
derived to do.

## The bottleneck, named

Three live questions — the non-affine correction, the bending pre-stress terms, and
whether KBSAT survives — all terminate at the same missing registration: **the fine
weave's contact/clearance rule**, which FND-123 named grant-class five sessions ago.
It is no longer one gap among several. It goes to the desk with three customers on
its face.

## Owed, and not paid in this release

Every quantity priced through the 4.046 double compensation — SHIN7's `kb` ceiling
and FND-091's `eps` headroom among them — inherits a re-pricing question and should
not be quoted until re-priced.

## Files

- `FND-126` (Modeled), benchmark `benchmarks/foundations/blochl_longitudinal.py`
- Results `analysis/BLOCHL_longitudinal_results.md`; charter (locked 16 Aug)
  `analysis/COMMISSION_BLOCHL_charter_LOCKED.md`
- Amended faces: FND-125, GRV-112, FND-117, FND-124, FND-121
- `KNOWN_LIMITATIONS.md` gains the fine-longitudinal-channel entry
