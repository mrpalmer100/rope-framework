# RELEASE NOTES -- v3.21.0 (2026-08-12)

One day, one arc: nine commissions and two author's acts, FND-074
through FND-081, atop v3.20.8. The registry closes at 636 claims,
verified.

## Headlines

1. **ONE-MEDIUM IS CONVICTED WHOLE (SHIN, FND-076).** Both components
   of the identity -- density (kappa_pack = 1) and strand (vacuum
   strand = tube constituent) -- are excluded x50-x137 by the
   registered Casimir-scaling bound through the derived softening
   coefficient. The kappa_pack floors are vindicated by confrontation;
   FND-040's (a, T0) pairs become the registry's only live readings;
   the width ledger is clean fact (the tube constituent is not a
   single vacuum strand, factor 4-9).

2. **THE TWO-LEVEL STRAND HIERARCHY OPENS AND HOLDS (TAV, KAF2, MEM;
   FND-077..079).** The tube constituent can be a solid bundle of
   axial vacuum strands: geometry, solidity, and the strand budget all
   pass, with the count confrontation reducing to the bundle's void
   fraction by an exact f_c cancellation (demoted, per precedent, to
   consistency). The fusion calibration point is adjudicated empty as
   an n_b decider (it is one Casimir measurement, already consumed by
   recruitment -- which uniquely survives a new two-sided
   confrontation). The m_b census finds the hierarchy safe everywhere
   plausible and pins n = 63-73 under the at-threshold demand.

3. **GRANT-THRESHOLD ADOPTED (FND-080, author's act).** Matter sits at
   its own survival threshold -- the marginality three commissions
   generated independently and correctly refused under the resemblance
   rule, adopted at the desk with its price visible. Purchases: the
   contrast g fixed [0.395, 0.460]; m_b pinned [63.0, 73.4];
   conditionally n_b = [63, 73] at (kappa250, hex).

4. **THE RECONNECTION CHAIN IS CONDITIONALLY CROWNED AND THE
   MESOSCOPIC LANDING CLEANLY MISSES (QOPH, RESH, TET; FND-074, 075,
   081).** The granted contrast fed through the data-selected pinned
   spectra returns p_eff inside the sealed v0 band: the chain
   v0 -> p -> window -> grant -> spectrum -> v0 closes end-to-end,
   with the pinned-vs-unpinned fork settled by external data. The same
   physics forces the encounter scale high, converting C4 -- the only
   class ever to contain the mesoscopic target -- to a clean miss
   (fourth conviction, first from above, best approach 1.16x on
   record, not leaned on). The wall stands, shaped.

## Discipline record

Bars locked before computing in all nine commissions; three pre-lock
noticings disclosed as guards and none allowed to select an outcome;
two consistency landings demoted under the shared-provenance
precedent; one day-old grant exposed to its own falsifier the same
day and survived; one arc casualty schedule (SHIN's) pre-enumerated
and executed against the arc's own prior commissions. Failures kept:
C4's miss registered without rescue at a 1.16x approach.

## Stale after this release

FND-073's w/a bands; the n_t = 111 liveness; ka = 1 as an evaluation
scale; "one-medium" as a premise; the reconnection window quoted
without its spectrum. See HANDOFF section 6.
