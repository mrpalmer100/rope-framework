# v3.26.83 (18 Aug 2026) — THE HALT THAT PAID: the composite curvature was two objects wearing one symbol, and the bending channel goes on notice

**729 claims.** One new claim (FND-138), one benchmark (exits nonzero by
design — the halt condition fired), one locked bars doc, one results doc.

## The one-paragraph version

The ansatz self-consistency session halted at its own surface control,
and the halt is the finding. The registered composite curvature
K = k1 + kappa_2(frame) — carried by the bending machinery since the
substructure sessions — is a **per-level curvature sum**; the Frenet
curvature of the actual nested curve is a **different object**, and
they disagree at order one in both pieces (the sum weights level-1 at
unit backbone rate where the backbone advances at less, and gives
level-2 a straight-axis rate the curved backbone does not have). The
bending-energy weighting lands at 0.73x. The chartered test cannot run
— the force side is ambiguous until the corpus adjudicates which
curvature the granted rod bends against — so the question goes to
COMMISSION CURVE-OBJ with the grant's own no-wound-reference clause as
first exhibit, and every bending-channel number priced through the
sum-form goes ON NOTICE (kb ceiling direction if the true curvature
wins: looser). Level-1 objects are untouched — a single helix's Frenet
curvature is its per-level curvature — so the neutrality theorem, the
material-speed identity, and the DBC null all stand. One correction on
the record: the first diagnostic's transport hypothesis was refuted by
its own printed control and replaced with the empirical decomposition
— the same-night second instance of the verdict-prose lesson, caught
because the control printed.

## Files

- FND-138 (Modeled); benchmark `benchmarks/foundations/ansatz_selfconsistency.py`
- `analysis/ANSATZ_selfconsistency_bars_LOCKED.md` (locked pre-compute)
- `analysis/ANSATZ_selfconsistency_results.md`
