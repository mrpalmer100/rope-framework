# v3.26.81 (18 Aug 2026) — THE TIGHTENING: the coverage pays the path debt, and the audit names the ansatz

**727 claims.** One new claim (FND-136), one benchmark, one locked bars
doc, one results doc; FND-135 amended (audit).

## The one-paragraph version

An independent audit of the composite build (fresh derivation,
Monte-Carlo control against the closed form, both path factors, all
four corners) confirms every number — and adds the caveat the build
should have carried: the closed form is exact FOR THE ANSATZ
(superposed rigid rotations), whose dynamical self-consistency at
order (R_2/R_1)^2 = 0.174 is not established; precedented by MAINT's
exhibited state, now named on FND-135's face. The tightening session
then ran: the fine coverage bracket moves to **[1.80%, 5.07%]** under
the load-bearing reading (down by exactly 0.8349), packing headroom
unchanged, both fine-weave assertions passing with more margin. The
sweep-blindness mechanism is registered where future sweeps will read
it: a sweep's clearance extends exactly as far as the invariance of
the quantity it checked, no further. Every live quote of the
reading-B factor is now dispositioned; the inherited debt is paid.

## Files

- FND-136 (Modeled); benchmark `benchmarks/foundations/sweeppath_coverage.py`
- `analysis/SWEEPPATH_bars_LOCKED.md` (locked pre-compute), `analysis/SWEEPPATH_results.md`
- Amended: FND-135 (ansatz caveat, audit)
