# v3.27.3 (20 Aug 2026) -- TRAVERSE: the steepened regime of record was an aliasing artifact, and the tell was on the face

**734 claims.** One new claim (FND-143), two commissions
consolidated, one method lesson promoted into the successor's bars.

## The one-paragraph version

The 64 x 24 steepened-regime members of the aligned branch are
RESOLUTION ARTIFACTS at the 2-7 percent level: mild members
re-solve on the fine grid to 7.1e-12 with 1e-4-grade drift, while
steepened members (gamma <= 0.558) fail their fine-grid re-solve
with drift tracking a continuous-level closure violation reaching
8e-3, terminating in a genuine least-squares stationary point.
The quantitative steepening record past gamma = 0.558 is
SUPERSEDED-NOT-ERASED; FND-142's verdict (a search failure:
PIN-UNREACHED, Sigma_wave not re-priced) stands untouched where
it matters, its resolution-exhaustion diagnosis strengthened.

## The method lesson (the reusable content)

Closure residuals are equations in the tangent-sphere chart, and
the inherited bars printed them without gating (1.9e-9 mild ->
8.3e-3 steepened under a clean 1e-8 field-RMS acceptance).
Successor NATIVE-96 promotes closure to a HALT-grade bar at 1e-6
and runs natively at 96 x 36 from level 1.

## Also in this release

Instrument fixes carried in-place from the suite run
(verify_corpus budget map and cache; ansatz_selfconsistency exit
semantics; electron_kkt_push FD ladder; numpy>=2 portability;
truestate_stage2 --task driver). Full suite: ALL CHECKS PASS
(637/637 code-backed).
