# Release notes: v3.12.0 (2026-08-09)

## The exact-D arc: the number that wasn't there

Six chartered commissions (GRV-096..101), each with bars locked before
computation, executed in a single day. The arc set out to extract the
induced-gravity coefficient D named by GRV-095 and ended by proving the
target malformed, deriving what replaces it, and correcting a flagship
number.

- GRV-096 (Failed, kept): the extraction is protocol-dependent
  (12-57%); volume limit clean; diagnosis: m/m^3 collinearity.
- GRV-097 (Failed, kept): the ordered-window protocol reveals a
  two-term m-odd structure with the target subdominant (B/A = 5.6);
  the FD3 annihilator identity verified at machine precision.
- GRV-098 (Failed, kept): the q -> 0 and m -> 0 limits do not commute;
  the continuum machinery reproduces the lattice at the lattice's q
  (the reconciliation) and gives 2.7x different coefficients q-first;
  the ordered-limit extraction hits a numerical cliff.
- GRV-099 (Modeled): the form of D adjudicated -- the ordered-limit
  sqrt-channel amplitude is ZERO (A ~ q); the IR-universal content is
  the even-log u ln u with derived coefficient L1 = -1.61e-4 (tadpole
  exactly solvable as internal control at |A/L1| = 2e-4); one derived
  number absorbs 82-88% of the lattice profile at R^2 = 0.9996.
- GRV-100 (Modeled): the even-log channel is Einstein-Hilbert-patterned
  at 0.0003-0.017 against the locked 0.2 bar -- two orders cleaner than
  the original verdict; the normalization is scheme-borne and
  sign-indefinite, so the standing scale problem is STRUCTURAL: the
  loop predicts pattern and running, not absolute strength; the F-Lor
  exclusion survives trivially.
- GRV-101 (Modeled; erratum on GRV-075): chi_2 is EXTENSIVE (ratio
  2.035 at doubled ring) -- the registered zeta carried sqrt(96), and
  the famous a = 8 Planck lengths corrects to 0.80 l_P (1D chain) and
  (0, 0.18] l_P (covariant chain). The PLANCK-CLASS selection stands,
  stronger; the digit does not. Positivity of the induced tension
  selects the renormalization half-line above the band gap -- the
  first physically motivated candidate for GRV-100's open principle.

## Also in this release

- Registry integrity: the quantum-audit ledger fix (curated OVERRIDE
  entries always ledger members; QB-003 and PM-002 restored; 248
  touching / 21 fences), unblocking QGATE-002 CI.
- The stopping-point convention exercised at scale: forward-pointer
  amendments on NUC-005 (light-table extension), GRV-012 (metric
  later derived), NUC-006, NUC-009 (refuted diagnosis struck at title
  level), PRED-002/PRED-003 (confrontation lineages), plus the arc's
  own supersession pointers (GRV-095/097/098/099/100, GRV-024/025
  doctrine brackets, the GRV-075 erratum bracket).
- Nuclear two-track statement propagated: discrete bond-counting
  A = 2..16 (one He-4-calibrated constant, r = 0.978) meeting the
  derived SEMF A~8..U-238 (~1%), across README, STATE, CONSTRAINTS,
  and the comparative audit.
- New plain-language paper: "The Number That Wasn't There"
  (docs/EXACT_D_ARC_PLAIN_LANGUAGE.md; papers/
  the_number_that_wasnt_there.pdf), joining the alpha and detector
  accounts.
- Scale-correction propagation: STATE, falsifiable_predictions, and
  rope_gravity papers carry bracketed GRV-101 updates; residual sweep
  clean.
- Detector paper: the corrected double-slit infographic embedded at
  scoreboard item 0 (figures/double_slit_corrected.png).
- Benchmarks added: exact_d_extraction, exact_d_ordered,
  exact_d_derived, exact_d_asymptotics, exact_d_interpretation,
  exact_d_scale (benchmarks/gravity/). Charters COMMISSION_EXACT_D..D6
  in docs/technical/, each with its outcome addendum.

## Census

524 registered claims (111 Derived, 372 Modeled, 4 EFT-constrained,
4 Conjecture, 5 Open, 28 Failed-and-kept); 503 code-backed.
