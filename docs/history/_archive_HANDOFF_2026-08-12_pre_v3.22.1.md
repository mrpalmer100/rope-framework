# HANDOFF -- Rope Framework, next session

*Written 2026-08-12 at the close of the v3.21.0 arc (QOPH through TET:
nine commissions and two author's acts in one day). Everything a fresh
session needs, in reading order. The corpus zip is
`rope-framework-github.zip`; unpack it and work in `rope/`. The prior
handoff is archived at
docs/history/_archive_HANDOFF_2026-08-12_pre_v3.21.0.md.*

---

## 0. Ninety-second orientation

The Rope Framework is Mark Palmer's object-based physics programme
(GitHub: mrpalmer100/rope-framework), building physics from mechanical
strands rather than fields. It runs as a registry of claims
(`claims.yaml`, 636 registered) each carrying a status grade, a
benchmark, and its provenance. **The registry is the authority.**

**House discipline, non-negotiable:**
- Bars locked in `analysis/*_LOCKED.md` BEFORE computing. Always.
- Failures register as `Failed-and-kept`, never rescued, never quietly
  re-scoped. Bar-shopping is refused by rule.
- No post-hoc refitting; no O(1) rescues argued after a miss.
- Every session ends with: claim registered via `tools/add_claim.py`,
  annotations appended to affected claims, CHANGELOG entry,
  `tools/verify_corpus.py --quick`, re-zip, `present_files`.
- Em dashes are forbidden in file content (use `--`).
- Premise adoptions ("grants") are MARK'S CALL, not Claude's. Name the
  grant, price it, and leave it for him.
- GEOMETRIC/NUMERICAL RESEMBLANCE IS NOT PHYSICAL IDENTIFICATION.
  Require a DEPENDENCY PATH before identifying a derived structure with
  an observable. The rule's vindication this arc: three separate
  commissions displayed-and-refused the matter-at-threshold adjacency
  under this rule, the pattern accumulated anyway, and adoption then
  happened AT THE DESK, by decision, with the price visible (FND-080).
  That is the intended division of labor between the rules and the
  author.
- Before writing a law, bar, or next-order leaning on a claim, OPEN
  THAT CLAIM and read its title, note, and every bracketed annotation
  VERBATIM. A neighbour's paraphrase is not the claim.
- "No derivation mentions X, so none is disturbed" is an INVALID audit
  argument (FND-056): counting is the channel and this corpus kills on
  counts.
- NAME HYGIENE: two live g's -- the crossing CONTRAST g (fixed
  [0.395, 0.460] by grant) and the MESOSCOPIC ratio g_C4 (~83-108,
  unexplained). Write them distinctly. The kappa rename sweep (three
  objects wearing kappa) is still queued and still owed.

---

## 1. State of the corpus

**Release:** v3.21.0 (the 2026-08-12 arc: FND-074..081 atop v3.20.8).
**Registry:** 636 claims. Counts regenerate from claims.yaml -- trust
`tools/sync_doc_facts.py`.

**Read these first, in order:**
1. Section 2 below -- the arc, because it convicts an identity the old
   handoff treated as live, retires numbers you will otherwise reuse.
2. `KNOWN_LIMITATIONS.md` (the mesoscopic entry was rewritten).
3. CHANGELOG entries 3.20.9 through 3.21.0.
4. `docs/STATE_OF_THE_PROGRAMME.md`.

---

## 2. THE 2026-08-12 ARC (FND-074..081) -- READ BEFORE REUSING ANY w, a, ka, OR "ONE-MEDIUM" STATEMENT

**QOPH (FND-074).** The C3 reconnection-vs-survival tension was an
evaluation-scale artifact: g_demanded is exactly linear in ka, and the
registered mode scales overlap the survival floor. The window became a
family; later collapsed by TET (below).

**RESH (FND-075).** SCALE-001's C4 (mean-free path), posable for the
first time via the derived p, CONTAINED the mesoscopic target --
first class ever to do so. Later adjudicated by TET: clean miss.

**SHIN (FND-076). ONE-MEDIUM IS CONVICTED WHOLE.** Both components --
OM-D (Sigma_vac = Sigma_eff, kappa_pack = 1) and OM-S (vacuum strand =
tube constituent) -- are excluded x50-x137 by the registered
Casimir-scaling bound through FND-040's derived coefficient.
Consequences that BIND every future session:
- The kappa_pack floors are VINDICATED; FND-040's re-solved (a, T0)
  pairs -- (1.63e-17 m, 1599 J/m) at kappa 50 and (0.953e-17 m,
  2734 J/m) at kappa 250 -- are the ONLY live readings.
- FND-073's w determination REVERTED (w undetermined; the n_t = 111
  liveness unwound). Do not quote its band.
- ELEC-050 B1's two T0 routes are genuinely distinct; NUCQ-002's
  same-number-twice agreement is flagged an UNEXPLAINED COINCIDENCE
  (unresolved; any resolution now routes through n_b).
- The vacuum-mesh strand and the tube constituent are DIFFERENT
  objects (widths differ x4-9).

**TAV (FND-077).** The bundle census: the tube constituent CAN be a
solid bundle of axial vacuum strands -- geometry, solidity, and the
strand budget all pass, the natural cell balancing to the bundle's
void fraction (f_c cancels identically; demoted per the FND-067
precedent as consistency-not-evidence). n_b undetermined; integer
windows 5-9 (kappa50/f_c) up to 40-81 (kappa250/hex).

**KAF2 (FND-078).** PHI's fusion point is ONE measurement (the Casimir
9/4) in two coordinates, consumed exactly by FND-037's n_b-free
recruitment; delta_fusion can never decide n_b. Real yield: the
adjoint tube's excess is RECRUITMENT, not strain coherence --
FND-037's law uniquely survives a two-sided confrontation (winding
null and forced coherence both excluded).

**MEM (FND-079).** m_b(n) run at both identities: survival passes
everywhere plausible (the hierarchy cannot be killed by the
matter-stability census). The at-threshold demand pins m_b = 63-73
through full-section contact alone, intersecting the (kappa250, hex)
constituent window and the tube census. GRANT-CANDIDATE-THRESHOLD
priced and placed on the desk.

**FND-080 (author's act). GRANT-THRESHOLD ADOPTED:** matter sits at
its own survival threshold. Purchases: the contrast g FIXED at
[0.395, 0.460]; m_b pinned [63.0, 73.4]; under the named full-section
conditional (Chain A), n_b = [63, 73] at (kappa250, hex). Falsifiers
armed: any future independent g or n_b determination landing elsewhere
returns the grant to adjudication.

**TET (FND-081). CHAIN-CLOSES + C4-MISSES.**
- CLOSES: granted g through the PINNED spectrum class (segment
  fundamental ka = pi; step-kink harmonics ka_eff = 3.88) returns
  p_eff = [1.30, 2.67]e-3 INSIDE the sealed v0 band. The chain
  v0 -> p-form -> window -> grant -> spectrum -> v0 closes end-to-end.
  NUC-030 is conditionally CROWNED (conditions: the grant; the pinned
  class; QGATE's hbar conversion). The pinned-vs-unpinned fork was
  settled BY DATA: transit (ka = 1) and the unpinned continuum are
  retired as spectra. Do not evaluate p at ka = 1 again.
- MISSES: no surviving spectrum lands g_C4. C4 is a CLEAN MISS (the
  fourth; the first from above; closest approach 1.16x displayed and
  not leaned on -- closing it needs three simultaneous selections,
  refused as bar-shopping). FND-044's residual STANDS.

---

## 3. THE MESOSCOPIC WALL (FND-044) -- the standing open problem, now shaped

g_C4 = l_q/a in [82.6, 108.0]. Four classes evaluated, four misses:
three from below (defect-log; SCALE-001's C2/C8/C3), one from above
(C4 under the data-selected spectra). The registered instrument set is
EXHAUSTED: any new attempt requires either a new registered carrier
(disorder, vacuum temperature, a non-circular amplitude, located
coverage -- SCALE-001's surviving underspecified list) or new physics
to the desk. The dependency-path protocol and the sealed-target
machinery (tools/scale001_seal.py) remain binding on any g_C4 session.

---

## 4. ON THE AUTHOR'S DESK

1. **GRANT-CANDIDATE-ROT** (GRV-103): unchanged from the prior
   handoff; tau = beta_J J, frame-dragging's source. Untouched this arc.
2. **GRANT-N2-GAP** (FND-056): unchanged, live.
3. **The kappa rename** (three objects) and now the **w/g name
   hygiene** (vacuum-mesh w vs tube-constituent w; contrast g vs
   mesoscopic g_C4): registry-wide sweep owed, cheap, prevents the
   next FND-066-class collision.
4. **The NUCQ-002 coincidence**: the two T0 routes agreed at the
   Lorentz-bound a because one-medium was implicitly kappa = 1 there;
   post-conviction the agreement is unexplained and any resolution
   routes through n_b (FND-077's filed observation).
5. **Chain A's full-section conditional** (FND-079/080): the
   implausibility commentary was priced for touching parallel
   cylinders, not a bound web; a re-derivation at constituent scale
   would settle whether n_b = 63-73 is bought cleanly.
6. **ZENODO_RELEASE_NOTES.md** still frozen at the v2.2.1 era.

---

## 5. The ranked queue

**SUPERSEDED BY AUTHOR DIRECTION (2026-08-12, post-arc): see
docs/STRATEGIC_TARGETS.md. The next session is the v3.21 predictions
reconciliation (T1); the queue below is retained as the interior
(T4) backlog and does not govern session selection.**


1. **The n_b determination.** The hierarchy is coherent (TAV), safe
   (MEM), and pinned-conditional (n_b = 63-73 under the grant plus
   Chain A). What remains: an INDEPENDENT n_b observable. The fusion
   point is adjudicated empty (KAF2). Named routes: the full-section
   re-derivation (desk item 5 -- if it holds, the grant's Chain A
   buys n_b outright); the strand-engine penetration-barrier spectrum
   vs bundle size (FND-STRAND-004's instrument, computable now);
   lattice constituent substructure (literature reading).
2. **The NUCQ-002 coincidence resolution** (desk item 4): the T0
   chain's post-conviction bookkeeping, now well-posed through the
   hierarchy. Fork-free entry: recompute the additivity route with
   constituents-as-bundles and confront the kappa-floor route.
3. **The Born joint-outcome gate**: untouched for two arcs. Can the
   derived conservation/topological structure produce joint detection
   probabilities without a configuration-space guidance object?
4. **The nuclear residual's other channels** (NUC-026): isospin
   quartic (~-47), curvature (~+6 MeV).
5. **g_C4**: blocked on new carriers (section 3). Do not spend a
   session here without one.

**Running externally:** the lattice-precision push -- FND-047's
decision table (0.5-1 percent long-distance Casimir-scaling ratio now
also tests the FND-040 floors the conviction vindicated) and FND-055's
SU(6) k-string determination (GRANT-N2's exclusion statistic).

---

## 6. Live cautions

**Stale-number tripwires from this arc:** FND-073's w/a bands (dead);
n_t = 111 liveness (unwound); ka = 1 as an evaluation scale (retired
by data); "one-medium" as a premise (convicted); the [0.082, 0.265]
window quoted without its ka (superseded -- at the surviving spectra
the window contains the granted g).

**The conditionality stack on the crowned chain, never to be dropped:**
NUC-030's crowning is conditional on (i) GRANT-THRESHOLD (FND-080),
(ii) the pinned spectrum class (FND-081), (iii) QGATE mode
quantization (unchanged). Each carries an armed falsifier.

**The prosecute-forks-first lesson** (carried): a registered
contradiction whose escape is unrun is a blocking item. This arc's
compliance record: SHIN ran before anything was built on FND-073's
conflict. Keep it that way.

**The light carrier phrasing** (carried): light is the COLLECTIVE
TRANSVERSE Goldstone pair; the screw/torsion mode carries charge.
The photon-sector limitation remains DISCLOSED (FND-062, route (c)).

---

## 7. First message to paste into the new session

> Continuing the Rope Framework. Corpus attached
> (`rope-framework-github.zip`, v3.21.0, 636 claims). Read `HANDOFF.md`
> first (section 2 covers the 2026-08-12 arc: one-medium convicted,
> the bundle hierarchy opened, GRANT-THRESHOLD adopted, the
> reconnection chain conditionally crowned, C4's mesoscopic landing
> cleanly missed), then `KNOWN_LIMITATIONS.md`.
>
> Live grants: GRANT-THRESHOLD (FND-080, falsifiers armed),
> N-universality (FND-050), GRANT-CANDIDATE-ROT and GRANT-N2-GAP on
> the desk. Rank-1 is the n_b determination (routes in HANDOFF
> section 5); the g_C4 wall is blocked on new carriers and stays
> closed without one.
>
> Standard house discipline throughout.
