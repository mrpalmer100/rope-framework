# v3.26.79 (18 Aug 2026) — THE FOURTH SIGMA: named before the collision, and the collision was already half-happening

**725 claims.** One new claim (FND-134), one registry addendum. No
benchmark (a naming session); no registered number moves.

## The one-paragraph version

The registration FND-132 flagged ran ahead of the composite build, per
the EM-020/DALET-2 precedent — and recomputing the registered bill
instead of quoting it found the slippage already underway: FND-132's
prose labels the wave total "2.6–4.5 T0/a²-class", per-strand NUMBERS
wearing an AREAL unit label, the exact hazard class the registration
exists to retire, present in the very claim that flagged it. The
registry now carries THE SIGMA FAMILY — Sigma_eff (pinned), Sigma_EM
(different object, DALET-2), Sigma_vac (static bookkeeping, 3 T0/a²
areal = 1 T0 per strand per axial length), and **Sigma_wave** (the
rotating winding's total, canonically per-strand per axial length,
bracketed [2.598, 4.522] T0, exact value owned by the composite
build). The correction to FND-132 is attached by name, not edited in;
the mixed-unit adjacency (per-strand 2.6 beside areal 3) is on the
face; the dynamical share keeps its name and gets no letter. The
ledger is clear for the build.

## Files

- FND-134 (Modeled); registry: docs/NAME_REGISTRY.md, "THE SIGMA FAMILY" addendum
