# Release v3.10.0 — The scale campaign: coherent and unconfronted (4 August 2026)

**496 registered claims (109 Derived, 349 Modeled, 4 EFT-constrained,
4 Conjecture, 5 Open, 25 Failed-and-kept); 476 code-backed, all passing.**

The release in one sentence: the absolute-scale problem was converted from
philosophy into an over-determined system, solved, and the redundant equation
said NO — resolving the corpus's substrate fork to two genuine micro-scales,
producing an m_e-consistent parameter point on one spent calibration, and
then two internal audits voided the campaign's own phantom anchor and stale
target, leaving a sector that is internally coherent, externally
unconfronted, and honest about the difference.

## The pipeline (FND-MATTER-039..041)

**The ledger (039)** cost nothing but reading: a year of relations built for
other reasons turned out to surround the same three unknowns from
independent sides. The conversion — FND-MATTER-005's irreducibility theorem
stands (a is not derivable from the postulates), but the measurement fixings
are now REDUNDANT, and redundancy is testability. Three equations, three
unknowns, one already solved.

**The closure (040)** spent the campaign's one permitted calibration on the
electron mass and ran the pipeline: a = 8 l_P → T0 = 2.02e20 J/m →
l_q = 3.79e-24 m → **l_q/a = 2.9e10**, clearing the pre-drafted 1–100
reconciliation window by eight orders under full bands. The first time the
scale question had a way to say no, and it said no. The mismatch fingerprinted
its own culprit: independent T0 anchors cluster tightly while the closure's
T0 sits seventeen orders above them, and the whole gap traces to one
commitment — the identification of the induced-gravity cutoff with the mesh.

**The fork reweighed (041):** F-Sak-as-mesh excluded (8 orders), F-Lor-as-
sole-gravity reconfirmed excluded (33.9 orders, ζD = 7.6e35 needed against a
locked [1e-2, 1e2] bracket). Survivor: **F-2SCALE** — the weave mesh carrying
EM and matter, a distinct gravitational stiffness scale carrying the induced
coefficient, the ~18-order relation between them now a named open problem
rather than a silent assumption. The arm was pre-reserved by the
G-investigation's own conjecture note a month earlier. Inverting the pipeline
with the same single calibration re-applied puts **l_q/a = 33.4 — inside the
window**, giving GRV-093's open geometry question its first number: the
quantum length is a few tens of mesh cells.

## The suppression thread, bottomed out honestly (042..043)

**The sweep (042)** killed its own leading lead first: GRV-093's 17–18-order
"fork lever" equals the a-ratio by construction — one geometric ratio under
two bookkeepings, registered as a guardrail. Seven candidates, five failures
displayed at full size (including the mechanically motivated one at 714x), one
category refusal, and a single caveated survivor logged at Conjecture under a
new methodological rule: **any power ≥ 6 is automatically down-weighted**,
because a twelfth power turns a radius-vs-diameter convention into 4e3, so
such candidates can only ever be held by tracking predictions, never numerics.

**The kill (043):** the survivor's named mechanism was handed an instrument
and every benefit of the doubt, and returned **p = 2.000** across five
decades on both weight definitions against a required 12 ± 1. Conjecture
killed-and-kept. What replaced it is better: a derived-shape (2/3)(a/r)²
law pricing one physical channel at six orders, leaving the residual pinned
at **1e30** — a number future mechanisms must hit exactly rather than gesture
at. Kept in the record: the pre-written verdict text guessed p = 3, the
instrument said 2.000, and the text was corrected to the instrument.

## The parameter point (044)

The 4.6 "whisper" between the matter route and the lattice anchor was
**bound-saturation accounting, not physics**. Solving the m_e-pinned
combination jointly with FND-017's Derived invariance at the registered Σ —
zero adjustable choices, the spent calibration re-used and not re-fit — gives
the **M-point: a = 6.0e-17 m, T0 = 434 J/m**, with the Lorentz bound
satisfied at 40% margin instead of saturated. The whisper decomposes exactly:
4.62 = 1.67 × 2.77. Every deduplicated pairwise comparison lands inside the
zero-point band.

## Two audits that corrected the campaign itself (045..046)

**The phantom anchor (045):** GRV-074's 1.4–2.0e39 quantification matches
EH/T0 at the card's own tension to under 2% — it was the gap in tension
units, never an independent determination. The campaign had tabulated it as a
fourth anchor through three claims, committing the exact double-count whose
guardrail it wrote that morning. Corrected at full volume, and the correction
*strengthens*: the cluster tightens to three independent anchors within
14.2x. GRV-074's fifteen-session-old named decider closed in passing — the
thin-strand mechanism supplies one power, not five or six.

**The stale target (046):** commissioned to invert the snap band into a
core-thickness demand (computed and displayed as ordered), the
target-provenance audit voided it first — the band and the value are one
expression with shared inputs, and the registered bracket was that expression
frozen at a superseded mesh point. R7 reclassified from landing zone to
**tracking identity**, the fourth guardrail, with a new ledger rule: *a
relation is a landing zone only if at least one input is external to the
system being solved.*

## The honesty clause, and where this leaves the sector

Dissolving the last internal residual means the EM scale sector **can no
longer fail from inside**. Zero residuals is internal coherence, not external
confirmation, and that is a weaker epistemic position than having one live
discrepancy — registered on the claim's face rather than celebrated. The
falsification burden rests entirely on the external judges: the PVLAS-class
vacuum nonlinearity, which now separates the surviving branches by 36 orders,
and the J1713 pulsar structure, untouched throughout.

**The campaign ledger at eight bricks:** four identities guarded, two
conjectures killed-and-kept, two phantoms/stale-targets voided, one
calibration spent, one parameter point produced, one 30-order open item
quantified with its native channel priced. FND-MATTER-003 remains OPEN,
sharpened twice.

## Release checklist

- README release line bumped to v3.10.0; corpus-state line synced.
- CITATION.cff at 3.10.0, date current.
- Release notes at `docs/history/RELEASE_NOTES_v3.10.0.md`.

## Carried forward

The a_grav ↔ a_mesh relation (the 1e30, one channel priced); the 3D
dynamical-matrix refinement of the power-2 law; the ledger v2 regenerated at
the M-point with R7 reclassified; the ZPE lever still blocked at
FND-MATTER-003; and the standing external confrontations.
