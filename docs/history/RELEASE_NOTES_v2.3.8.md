# v2.3.8 — The first dynamical Bell violation, and the mechanism of the Born rule

Corpus state: **251 registered claims (102 Derived, 130 Modeled, 4 EFT-constrained, 3 Conjecture,
5 Open, 7 Failed-and-kept); 237 benchmark-backed claims, all passing; 60 papers.**

Nine claims completing the QGATE campaign's second arc, plus two registry repairs that are
themselves demonstrations of the discipline. The quantum frontier's nonlocal branch went from
"missing a kind of dynamics" to: the dynamics named and priced, a working Bell violation, and
the mechanism of Born relaxation identified.

## The scale branch closes its audit trail (QGATE-009, -010)
- **009 — the vacuum-tension audit**: NO FOURTH KILL. Six confrontations of Sigma >= 5.1e35
  J/m³ against every registered normalization; zero inequalities violated. THE SCHWINGER
  DIAGNOSIS: the dead pin (Sigma ~ 1e25) had placed mesh nonlinearity exactly AT the Schwinger
  field — the old identification flattered the framework twice, and both flatteries were one
  coincidence, now named. The fence's burden stated in physical units (the vacuum outweighs
  neutron-star matter 25:1) without converting an open problem into a retroactive kill. One new
  tension filed: matter as a ~12% perturbation on the background, handed to gravity.
- **010 — the matter-sector birefringence**: an IOU converted to a CONTRACT. The corpus's own
  imported inputs reproduce the Euler-Heisenberg constant (A_e = 1.325e-24 vs literature
  1.32e-24) — bookkeeping consistency, labeled as such. THE SPIN-METER: the photon quartic
  fingerprints the lightest charged matter's structure class (spinor 7:4 positive vs mesh 3:1
  negative — 71% + a sign flip); one VMB@CERN-class apparatus now reads BOTH branch-deciding
  quantities. Any future rope electron inherits its acceptance test: arrive one-loop
  spinor-equivalent, or the framework predicts a polarimetry anomaly QED does not.

## The nonlocal branch opens — and delivers (QGATE-011, -012, -013)
- **011 — the guidance flow**: the missing dynamics NAMED (a flow on joint configuration
  space), DEFINED (joint dependence, Bell-forced; equivariance as a PDE; statistical
  no-signaling), UNIQUE in 1D (the corpus's own current forces v = j/rho), DEMONSTRATED
  (trajectories thread interference fringes; a plausible local rule scatters ~15x wide), and
  PRICED (the cheapest honest exit costs the ontology its 3-space purity: a real Psi on
  configuration space, guiding — dualism, priced not smuggled).
- **012 — THE FIRST DYNAMICAL BELL VIOLATION**: the corpus-native singlet under the guidance
  flow, run as a full Bell experiment — outcomes from positions only, no collapse. CHSH
  S = 2.72–2.78 against the local cap of 2.000 (Tsirelson 2.828); marginals flat
  (no-signaling verified empirically); the nonlocal coupling probe 38x on/off exactly with
  packet overlap. The arc failure (QB-003) -> impossibility theorem (QB-006) -> priced
  extension (011) -> working demonstration: a scientific boundary's complete life cycle, every
  stage preserved. First run FAILED its pre-locked bars (light pointer spread faster than it
  separated) — kept, diagnosed, fixed. Provenance honest: Psi is textbook; deriving it from
  ropes remains the open mountain.
- **013 — Born relaxation: substantial, controlled, incomplete**: the Valentini test with a
  kept failure history (the PDE scheme could not survive the node singularities that ARE the
  mechanism; the particle method showed genuine relaxation) and two bars missed and kept,
  including the Catch-28 standing rule: measure the noise floor before setting the bar.

## The mechanism arc (QGATE-014, -015, -016, -017)
- **014 — the locked-bar relaxation**: the corrected protocol; ALL bars pass (H down 91.6%,
  L1 down 70.5%, control 0.000%). An existence demonstration, not a universal-attractor
  theorem, and says so.
- **015 — the M-scaling test**: the commissioned fork executed over M = 1..64, three seeds,
  with a kept failed pilot and its timestep-convergence correction. Relaxation is ROBUST but
  NOT MONOTONIC in mode count — the fork's first tine, with a wrinkle.
- **016 — local chaos on trial**: four bars pass, and THE DECISIVE EXPLANATORY BAR FAILS AND
  IS KEPT — FTLE correlates with relaxation at +0.573, BELOW raw mode count's +0.606. Local
  instability accompanies relaxation but does not control it. The benchmark re-verifies its
  own failure on every run.
- **017 — GLOBAL TRANSPORT WINS**: over the same 21-state family, cross-cell transport
  (rho_H = +0.782) and the transfer-operator spectral gap (+0.767) decisively out-predict
  local chaos. The Born rule relaxes because probability MOVES BETWEEN REGIONS, not because
  trajectories diverge locally — the mechanism identified, with locked bars in analysis/.

## Registry repairs (integration audit)
- A duplicate QGATE-013 from a double registration pass: excised, findings preserved.
- The QB-015..019 COLLISION: two parallel campaigns (the quantum-engine track and the
  bell-rung track) had claimed the same five IDs. Both are real work with green benchmarks;
  the repair is renumbering, not deletion. The rung track keeps the canonical numbers (the
  corpus narrative chains onto it); the engine track becomes QB-023..027 with renumbering
  annotations. Historical release notes deliberately untouched. All IDs verified unique.

## What stands, said plainly
No knot is identified with any particle. The nonlocal branch now holds a working demonstration
that the priced minimal extension violates Bell dynamically and relaxes toward Born — with the
mechanism (global transport) identified and the incompleteness (residues, non-monotonicity,
textbook-Psi provenance) stated on every claim it touches. The scale branch awaits VMB@CERN
under the inverted payoff, with the spin-meter reading both verdicts on one axis. Two unbuilt
things remain between the framework and its exams: a rope electron, and a rope-derived Psi.
Every hypothesis is marked; every kept failure is load-bearing; and the registry repaired its
own collisions in public.
