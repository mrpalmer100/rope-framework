# RELEASE NOTES -- v3.24.0 (2026-08-12)

The two-discriminator surface: RESH (T2), one author's ruling, and
the doc pass that makes every surface state the same counts. The
registry closes at 648 claims; structural checks clean; the four
benchmarks new since 3.22.1 re-executed and reproducing; full-corpus
benchmark re-execution deferred to CI, disclosed in the CHANGELOG.

## Headlines

1. **THE DISCRIMINATION COUNT GOES TO TWO FIRM (RESH + FND-093).**
   The Casimir pin's only blocker -- the FND-063 grade
   conditionality -- is discharged by author's ruling: K_c is
   EM-RECON-017's contact-amplitude channel, a property of defect
   cores, and is absent from the core-free confining-mesh strands
   the prediction lives on. Endpoint contact is disclosed as
   boundary-subleading ON THE RULING'S FACE, and the ruling carries
   an armed return clause (any core on the flux path, or endpoint
   contact at bulk order, returns it to adjudication). Promotion
   with a tripwire, not tenure. The count: TWO FIRM (Prediction 6,
   clock PSR J1713 ~2027-2030; the pin, -1.25/-0.25 percent,
   lattice-reachable) plus ONE CONDITIONAL.

2. **THE k-STRING KILL BANDS ARE PRE-REGISTERED (FND-092).** Before
   any continuum SU(6) determination exists: k=2 softened-Casimir
   band [1.590, 1.598] vs sine 1.732; k=3 [1.786, 1.797] vs 2.000;
   kill at three quoted errors above the band, select inside it and
   three below sine, no post-hoc adjustment on arrival. At the
   registered ~2 percent error scale a confirmed sine value kills
   at either k. The corpus's sharpest way to die, now automatic.

3. **THE PHOTON LIMITATION LEAVES KNOWN_LIMITATIONS (3.23.1,
   carried into this release).** Removed as a limitation, relocated
   whole -- never deleted -- to docs/RESOLVED_LIMITATIONS.md with
   the resolving claims named; residual general-class items routed
   to their standing homes; README and STATE rewritten to the
   resolution.

## Discipline record

The RESH commission computed everything computable and refused to
rule; the ruling is recorded as a separate author's act with its
caution on its face; the adjudication-prep grammar and the decision
bands were locked before computing; the division of labor between
commission and desk worked as the house intends.

## Stale after this release

"One prediction" / "one firm discriminator" anywhere; Prediction
20's status line with the FND-063 clause; the k-string exposure
quoted without its pre-registered bands; the photon entry cited at
its old KNOWN_LIMITATIONS location.
