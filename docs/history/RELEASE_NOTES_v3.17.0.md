# Release Notes — v3.17.0 (2026-08-11): The Vacuum Tower Prosecuted

Nine commissions in one day (GIMEL through YOD plus the paper sync), bars
locked before every computation, one kept failure, two self-corrections,
and the arc ends with its central Conjecture standing in front of real
data holding a decision table.

## The result in one paragraph
The vacuum-stiffness tower (FND-037's Conjecture-grade kappa_pack floors)
was prosecuted end to end. The tower's sole registered refusal — l_q/a
exiting its 1–100 window — dissolved as a stale-value comparison (l_q
rescales with the mesh by R1's own registered form; the true ratios are
82.6 / 108.0, obeying the sixth-root law l_q/a = 43.0·kappa_pack^(1/6),
FND-041). The window itself was then adjudicated HALF-WINDOW — lower edge
physics, upper edge grammar — so the tower has floors and no ceiling
(FND-042). The old L1 question ("what selects the quantum area?")
excavated its own answer-shape from the registry: with eta = 1 already
derived, five former open questions (area selection, vacuum packing,
amplitude selection, lambda, locking softness) collapse to ONE unknown —
the mesoscopic source length g (FND-043/044) — whose best-shaped candidate
mechanism was then excluded structurally (the electron's own rest energy
exhausts its defect logarithm at 7 cells against a target of 83–108;
FND-045, Failed-and-kept, demand registered: 2.2–3.2 m_ec² blind). The
correlated-coefficients computation returned PURE-LINEAR (the quartic-
Casimir component vanishes at leading order; ALEPH's W2 corrected — the
arc's fifth self-correction) and registered the pin: kappa_pack =
(C_D/C_f − 1)/(2|delta_D − delta_f|), with forward numbers −1.25% / −0.25%
for the adjoint/fundamental pair (FND-046). The pin then met the lattice:
CONSISTENT-AND-ARMED against Bali's 5% continuum bound (kappa_pack ≥ 12.5;
decision table registered — 1.25% decides the CS floor, 0.25% tests the
whole tower; achievable modern lattice work; FND-047). The confrontation's
one flag — SU(N≥4) k-string sine-law positivity against BET's derived
softening — was adjudicated a category error: k-strings are BUNDLES of
unit windings (by screening/N-ality and by the registry's own additive
winding charge independently), the data measures tube binding, and the
softening's qualitative bundle prediction (attraction, sub-additivity)
matches every dataset under both laws (FND-048).

## The kept failure
FND-045 is the release's honest centerpiece: a pre-committed 3×3 candidate
table whose entire range (x = 0.10–2.00) lies below the bar (4.41–6.40) —
chance hit rate exactly zero, the exclusion structural. Whatever holds the
mesoscopic source length open is bigger than the particle it serves; the
answer points into the ambient weave, converging with MATTER053's
direction for the zero-point problem.

## The self-corrections
ALEPH's W2 (quartic structure "folds in") corrected by explicit matrices
(FND-046); the OMEGA/BET l_q/a strain corrected as a stale-value
comparison of the MATTER045 class (FND-041); ETA's MeV-adjacency
dissolved as the identity J = m_ec²/(2L) (FND-044); FND-041's own
kappa_pack ≤ 157 cap withdrawn by the very commission it commissioned
(FND-042) — the labeling discipline making each correction one session
cheap.

## Three routes to one Conjecture
kappa_pack now has three registered external routes: the g-mechanism
(blocked, demand on file), the PRED-003 drift ratio (2027–2030 window;
second job registered), and the adjoint Casimir ratio (technique exists
today; decision table in Prediction 20). FND-037 is the most falsifiable
Conjecture the corpus has carried.

## Open at the author's desk
The N-universality scope of the softening mechanism (FND-047/048):
SU(3)-scoped (safe, narrower) or N-universal (owes the bundle-binding
relation, with the qualitative gate already passing). Neither branch is
contradicted by data; the premise is unregistered by design pending the
author's decision.

## Papers and documents
falsifiable_predictions gains PREDICTION 20 (Casimir-scaling violation:
relation, numbers, pin, decision table, falsifiers, scope flag; title
count 19 → 20) and loses three stale Sigma ≥ 5.1e35 lines;
ROPE_PARAMETERS.md's M-point table corrected (stale 158/271 → 82.6/108.0)
with the kappa_pack-routes sync note; KNOWN_LIMITATIONS gains the
vacuum-tower section. New benchmarks: gimel_whisper_reaudit,
dalet_window_adjudication, he_area_selection, vav_n_computation,
zayin_defect_exponent, chet_correlated_coefficients,
tet_lattice_confrontation, yod_kstring_category — all passing.

## Corpus state
577 registered claims (116 Derived, 413 Modeled, 4 EFT-constrained,
4 Conjecture, 6 Open, 34 Failed-and-kept); 539 code-backed; counts
regenerate from claims.yaml.
