# v3.26.78 (18 Aug 2026) — THE NULL CORRECTION: the wave changes nothing, exactly, and the bound sheds a rider

**724 claims.** One new claim (FND-133), one benchmark, one locked bars
doc, one results doc. No registered number moves.

## The one-paragraph version

The handoff's one job — the dynamical-background correction to the
BLOCH-L anchors — ran and returned **zero, exactly, with the mechanism
on the face**. The material frame carries no convective or Coriolis
term (the cross term cancels by the background's own centripetal
equilibrium); the rotating state at any instant is the static
configuration under a common phase shift (rigid rotation is an
isometry); and the instrument's phase-ensemble averaging is exactly
shift-invariant at the production multiplicity — spreads at the 1e-13
level against a 1e-6 bar, both read directions, bending channel on,
arbitrary independent rotation rates at both winding levels. The
Floquet problem degenerates to the static one, so the anchors carry
zero background-rotation correction **at every order**. The standing
kb bound sheds its static-background rider and keeps its
Kirchhoff-only one (pre-stress, contact-gated — untouched). One
deviation from the pre-registration, recorded not smoothed: nullity
arrived already at m = 4, so the fourth phase-harmonic of the energy
form vanishes in the read projections for a reason not yet found —
open, small, named.

## Files

- FND-133 (Modeled); benchmark `benchmarks/foundations/dbc_background_correction.py`
- `analysis/DBC_background_correction_bars_LOCKED.md` (locked pre-compute)
- `analysis/DBC_background_correction_results.md`
