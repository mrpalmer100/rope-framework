# v2.2.6 — The quantum half of gravity, and the one question left

Corpus state: **176 registered claims (85 Derived, 69 Modeled, 4 EFT-constrained, 4 Conjecture,
7 Open, 7 Failed-and-kept); 162 code-backed benchmarks, all passing; 58 papers.**

This cycle is a single continuous line through the gravity sector — seven claims in one arc, run at
full risk, ending where this programme's best work always ends: on a sharply posed, falsifiable
question. The sector began the cycle carrying its worst standing result: the classical channels give a
scalar mimic (gamma = -1/2; light deflection 0.44 arcseconds against the observed 1.751), with a no-go
closing every classical escape and one registered conjecture (GRV-014) claiming the missing structure
arrives at the quantum level. The cycle tested that conjecture to completion.

## The arc (GRV-021 → GRV-027)

- **GRV-021 — the conjecture funded.** The measurement campaign had built the conjecture's missing
  supplier without knowing it: the measured, gapped weave band is exactly the mode-vacuum a
  Sakharov-type induction integrates over. Computed: the zero-point density is finite with the lattice
  as its own regulator (no imported cutoff), and the mode vacuum carries a finite induced gradient
  stiffness (chi(q) = -6.922 + 0.967 q^2 at r^2 = 0.99997) — induced elasticity, computed rather than
  invoked.
- **GRV-022 — the naive route refuted.** The first tensor test: linearized Einstein-Hilbert
  (sympy-derived, hand-verified against Fierz-Pauli) forces the parameter-free degeneracy
  chi_xy = chi_xz; the measurement violated it 27-fold. The dominant induced invariant is
  longitudinal-elastic. Einstein-Hilbert does not land automatically. A registered negative of
  theorem quality.
- **GRV-023 — the gate mapped.** One-metric coupling is necessary but not sufficient; the obstruction
  is the lattice regulator's non-covariant UV content; the clean EH window cannot exist for a band
  whose gap is the lattice scale; and since a covariant UV completion would contradict the preferred
  frame (QB-008), the ABSORPTION HYPOTHESIS is the only door.
- **GRV-024 — the exam armed, and its finest result a refusal.** The principled decomposition locked
  before computation: pattern absorption disqualified (stiffness invariants span the whole channel
  space); the non-gameable split is analyticity in m^2, sharpened by the 3D fact that the continuum
  covariant expansion is entirely m-odd — the exam's target is parameter-free. The first run's ratios
  would have EXECUTED the conjecture; the audit found the omitted second-order tadpole first, and the
  benchmark refused its own verdict until the instrument was honest.
- **GRV-025 — THE EXAM PASSED.** The tadpole completed (factors determined empirically by matrix
  perturbation theory; a spurious phase carried harmlessly for three sessions caught by plane-wave
  fingerprinting; final validation 0.025 percent against exact diagonalization). The m-odd tensor went
  from (0.72, 0.14, 1, 0.33) — a false kill — to (0.064, 0.050, 1, 0.005): **the IR-universal
  remainder is Einstein-Hilbert**, and the non-covariant excess is exactly m^2-analytic — medium
  renormalization, as absorption demanded. The quantum half of the two-level story exists.
- **GRV-026 — 1.751 arcseconds as a two-condition theorem.** EH dynamics plus covariant sourcing
  (GRV-005's force balance) give gamma = 1 and all the classical-test numbers automatically — unless
  the one candidate scalar (the gap-mismatch field) is sourced. The induced scalar sector was
  measured; its covariant R-phi fingerprint passed at 0.0035 (the arc's fourth parameter-free
  covariance check); the bridge to observation: |gamma - 1| = 4.2 eps^2, so Cassini requires the gap
  to track the covariant combination to 0.24 percent. The deflection is scale-free: the absolute-scale
  problem does not block this number.
- **GRV-027 — the lock selects.** The gap-lock computed from first principles on the strand engine
  with the exact contact law: the crossing stiffness scales as T^1.11 across two decades — the lock's
  3/2 is structurally outside the quartic contact's range — so pure-tension conditioning is EXCLUDED,
  and gamma = 1 survives only on the conditioning locus d ln l = +0.39 d ln T, held to 0.6 percent.
  Bonus engine physics: the crossing punches through above T ~ 1-2 — the finite barrier's surrender,
  mapped rather than assumed.

## Where gravity now stands

The chain reads: Einstein-Hilbert dynamics derived from the corpus's own measured spectrum; gamma = 1
and the 1.751-arcsecond deflection conditional on a single remaining question — does the defect's
equilibrium far field lie on the measured locus? On-locus, the deflection is derived and
parameter-free. Off-locus by delta, gamma - 1 = -4.2 delta^2 and the framework takes the hit at a
named number, as it did at 0.44. Three months ago this sector's registered prediction was wrong and
kept; today its prediction is right and conditional, with the condition computable.

## The discipline's cycle

Four parameter-free covariance fingerprints passed. Two instrument flaws caught before they could
lie — one of them minutes before it would have executed a true conjecture on a broken instrument.
Five instructive failures logged by name. One benchmark that refused to emit its own verdict. The
release also carries the corpus's outward-facing triad completed earlier in the cycle: the rebuilt
fifteen-prediction paper, the instruments map, and the guide's applied chapters (quantum computers;
machines you already own).

A programme that can lose small is allowed to win big. This cycle it won the quantum half of gravity
— and had the discipline to stop one question short of claiming the whole.
