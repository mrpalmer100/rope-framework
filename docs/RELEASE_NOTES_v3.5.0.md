# Release v3.5.0 — The whisper's mechanism, and the number it contests (2 August 2026)

**459 registered claims (104 Derived, 318 Modeled, 4 EFT-constrained, 4 Conjecture,
5 Open, 24 Failed-and-kept); 441 code-backed, all passing.**

Four claims (GRV-085..088) close the strong-field campaign's temperature chain
and climb its last summit -- which returns the best kind of ending: the LAW
derived, the NUMBER contested, and the disagreement given an address.

## The chain completed

- **GRV-085 -- P-EQ split.** The driven shell's kinetics DERIVED (unique stable
  fixed point; the equilibrium formula is its weak-drive limit, correction
  exact) -- and the bare two-state temperature closure REFUTED at the measured
  bit-cost by its own pre-committed bar: fifteen barriers per bit cannot live in
  a two-level system. The reservoir's heat moves to auxiliary local modes; the
  shape T ~ a_proper survives (every conversion is linear). And THE CANCELLATION
  THEOREM: W/T is sigma-independent -- the shell is thermal because the Rindler
  profile divides out of its own ratio.
- **GRV-086 -- m* counted.** Three sectors, one line each: the wave sector's
  polarizations IN (n_q = 8), the symmetry sector's twist FLAGGED OUT, the
  quantum sector's fiber theorem (QB-029, FDT) excluding the fiber from heat
  entirely. m* = 4-6, participation-verified at 17/18 modes on an anharmonic
  crossing cell (the exact-flatness bar failed to FPU near-integrability and is
  kept on the record). Consequence: the shell is a STRONG UNIFORM THERMAL
  EMITTER -- Boltzmann factor 0.68-0.77 at every depth.
- **GRV-087 -- the confrontation from below.** THE THIRD CANCELLATION:
  T_loc ~ 1/sigma times the Tolman factor ~ sigma leaves
  T_inf = (beta K h/m*) kappa -- the HAWKING LAW-FORM, with Hawking nowhere in
  the inputs. The lineage's committed omega_inf ~ kappa is met IN FORM; its
  switch-class luminosity is met IN KINETICS (the bottom-up mechanism lands on
  GRV-047/053's corrected side without having been shown either claim).
- **GRV-088 -- the summit.** K DERIVED (the pressing per crossing is the cell's
  WEIGHT: no free strength remains in the profile) -- and the coefficient
  evaluation returns a LOCATED DISAGREEMENT: C = 2e-6..3e-5 on the favourable
  fork against the committed 0.23, four orders low, registered at full strength
  with four pre-listed suspects on both chains (beta's engine-parameter
  provenance prime among them; the pile-up correction the one suspect pushing
  up; GRV-040's mode identification on the table for the first time). All three
  cancellations are coefficient-independent: THE FORM STANDS whichever chain
  carries the unearned link.

**The strong-field programme, two releases end to end (GRV-074..088): from the
weak field's oldest assumption to the Hawking law-form -- a first derived G
exponent pair with a Planck-class scale selection; the a-fork named; load-share,
staticity, and settling derived; an instrument retired by its own energy audit
and rebuilt honest; accretion emerging unasked; collapse as recorded structure;
a temperature chain of two theorems, two three-digit measurements, and a counted
bracket; three exact cancellations; and a final prediction-meets-prediction test
whose four-order tension is the next campaign's commission.**

## Verification

`tools/verify_corpus.py --quick` passes; per-claim benchmarks pass; bars for
every session locked before computation in `analysis/`.
