# v2.3.0 — Two knots, one ribbon

Corpus state: **200 registered claims (94 Derived, 87 Modeled, 4 EFT-constrained, 3 Conjecture,
5 Open, 7 Failed-and-kept); 190 code-backed benchmarks, all passing; 59 papers.**

The two-hundredth claim, and a minor-version release to mark what it closes: a complete computed
chain from rope wave energetics to a running Bell experiment — five claims in one arc, opened by a
founder's question ("can't it possibly be two or more ropes together?") and closed by CHSH violated
at sixty sigma with every response function measured from lattice dynamics. Claim 1 registered a
lattice; claim 100 belonged to the electromagnetic reconstruction; claim 200 runs Bell on rope.

## The arc

- **QB-015 — The shared-ribbon singlet** (Mark's conjecture): entanglement as two knots on ONE
  ribbon, and the idea explains the wall before beating it. In severed bookkeeping (both wings as
  independent responders — the structure of every model in QB-013's measured class) the ribbon
  lands on the registered ceiling TO FOUR DIGITS (0.9428 = 2√2/3): **B = −1/3 is what treating one
  object as two looks like.** Honor the oneness — the reel: measurement projects the shared frame,
  and both ends reorient because there is only one frame — and E = −a·b exactly, CHSH = Tsirelson,
  no-signaling identical. Bell honored in capitals: the reel IS the nonlocal element; the ribbon
  supplies its ontology — the same conserved 2π frame currency GRV-045 measured at the horizon.
  One framework, one currency, from the black hole to the Bell test.
- **QB-016 — The half-angle from rope energetics** (now Derived): cos²(θ/2) derived at 1e-16 from
  the three-channel energy split of a circular frame-wave — co-rotating cos⁴, counter-rotating
  sin⁴, and a handedness-NEUTRAL axial channel whose angular momentum about the analyzer axis is a
  COMPUTED zero, forcing a 50/50 split — closing to the half-angle exactly, no parameters. The
  measure diagnosis retires QB-002's old wound gently: counting by angle and detection by energy
  answer different questions; both claims stand.
- **QB-017 — The junction realized** (Derived): asked for one analyzer, the engine produced two.
  The anisotropic junction realizes Malus's law engine-measured (θ_P = 2φ emerging from the lattice
  unprompted); the gyroscopic junction is a near-perfect circular MIRROR (T_R = 2e-4 vs
  T_L = 0.93) — the corpus's first passive handedness-selective device, with the charge-sector
  implications filed, not claimed. One law, two hardware realizations, zero fits; QB-016's
  stylization discharged and the claim promoted.
- **QB-018 — The knot-proxy Stern-Gerlach**: a genuinely massive packet (group velocity tracking
  the gapped band at half a percent) split into two fully resolved beams by a gyroscopic-gradient
  strip — Stern-Gerlach from springs, a Coriolis term, and a slope — control dead center, weights
  at the half-angle law to half a percent under self-calibrated channels. The mystery relocates to
  where this programme says it lives: not the splitting (mechanical) but detection discreteness
  and the reel.
- **QB-019 — THE 200TH CLAIM: the Bell experiment, whole**: no analytic response law anywhere in
  the loop — the wings respond by a table MEASURED from the engine analyzers, leakage and all.
  CHSH = 2.72, matching the table's exact closed form E = W(−a·b) − W(a·b) inside half a percent:
  the run hits what a laboratory with these analyzers would hit, and proves the pipeline. The
  one-switch demonstration: hardware fixed, bookkeeping flipped — reel on, a sixty-sigma violation;
  severed, 0.79, below even the ideal wall. **The distance between classical and quantum, in this
  corpus, is whether the accounting respects that two knots on one ribbon are one object.**

## The ledger at two hundred

94 Derived. The Bell chain carries exactly two Modeled premises, in plain sight and named at every
step: detection discreteness (QB-005's threshold-detector premise) and the reel (QB-015's nonlocal
element — Bell's theorem honored, given an ontology rather than an evasion). The standing walls
named where they stand: QB-002's counting measure; the literal 3D knot (the named gateway to the
matter sector — knot spectroscopy, mass ratios, and someday the question asked tonight: the atomic
weights, currently Blocked-with-a-roadmap and refused as a shortcut).

## The discipline's cycle

Four instrument catches (the CHSH sign harness convicted by exact curves; the phasor decomposition
where the hand-derivation caught the code; the Stern-Gerlach channel self-calibration; the
closed-form consistency assert), bringing the lifetime count to seventeen. Two devices invented.
One conjecture of the founder's built into the chain the day it was proposed. Every bar locked
before its measurement.
