# Release v3.2.0 — The provenance arc (2 August 2026)

**438 registered claims (104 Derived, 297 Modeled, 4 EFT-constrained, 4 Conjecture,
5 Open, 24 Failed-and-kept); 420 code-backed, all passing.**

A minor version with a single spine: the corpus took its sole surviving T1
prediction, PRED-003's coupling-drift ratio, and audited its provenance all the way
to the floor. Nine claims in one sitting (HBAR-011, PRED-003-XCHAIN, ELEC-082,
PRED-003-CONST, -CHAIN, -DICT, -LOCK, -ETA, ELEC-083), three owned corrections, two
killed candidates, one vacuous test caught in its own commission, one enslaved
vacuum modulus, and a prediction that emerges with every premise named and nothing
it had not earned.

---

## The headline: the PRED-003 provenance arc

- **XCHAIN**: the corpus's two alpha chains are consistent by a locking relation;
  the static agreement is proven a calibration-closed tautology and said so.
- **CONST**: the sole T1's alpha expression is found registry-underived and
  dimensionally open (the NUC-013 units class, in the one place it mattered most);
  its unique closure IS a constitutive relation, e_eff^2/(4 pi eps0) = 2 lambda J a.
- **CHAIN**: the naive derivation route is killed by theorem — winding charge is a
  circulation, and scalar phase electrostatics is logarithmic (lattice slope 2 pi K
  to 2.4%), not Coulomb. Why the chain was never registered, now known.
- **DICT**: the Maxwell-sector evaluation collapses by impedance reduction:
  eps_med = 1/T (the medium's permittivity is the inverse tension), and
  alpha = l_q^2 T/(4 pi hbar c) — everything unknown packed into one source length.
- **LOCK**: the provenance mystery resolves (the P6 expression is the derived
  FND-002 director stiffness), and two Derived claims never before combined
  (OPT-006's impedance; the director stiffness) ENSLAVE the locking modulus:
  kappa = 2T/(eta a). The EM vacuum runs on two primitives. The drift ratio is
  reassigned: tension channel -1, spacing channel -2.
- **ETA**: eta = 1 by one-metric uniqueness; kappa = 2T/a, l_lock = a/2, J = Ta/2
  exact; measured alpha bounds the winding's source length below at 13-16 lattice
  spacings; the bare 2 pi normalization is killed by factor 4.3.
- **ELEC-082/083**: the amplitude-selection candidates re-graded (three re-killed
  on scaling; the collective count killed without the precision it was waiting
  for), and the ratio test's own commissioned vote proven vacuous before it could
  mislead — with the residue exact: under the shared-origin hypothesis,
  1/alpha = 2 pi^2 rho^2, rho = 2.6348 required, numerology guard posted.

Where the sole T1 stands: the -2 survives on the spacing channel, -1 on tension; a
measured nonzero drift ratio now identifies the drift channel; the J1713+0747
decision window (2027-2030) is unchanged; thresholds are stated for both channels.

## Also in this release (since v3.1.0)

- **The confrontation campaign**: PRED-003 confronted (0.93 sigma, null-vs-null),
  widened (1.74 sigma combined; J1713 the named weak point at 2.06 sigma), and
  decision-dated; PRED-002 confirmed-and-demoted with its frequency scaling
  derived; ELEC-064's T1 audit; the whisper arc (GRV-049..053) committed and
  channel-saturated. The T1 count stands at ONE.
- **The dynamical-electron line** (ELEC-066..081): conjecture to clean negative,
  with the reservoir premise resolved as required-by-optics.
- **The frame-dragging arc** completed through K_0 DERIVED (GRV-073).
- **The parameter card** (FND-020) with verifier, and the two-particle carrier
  line (FND-022..026): the channel is required, topological, and the SHARED OBJECT
  works where the scalar fails — the line this release's successor campaign
  (ribbon production) now tests at its source.
- **Repairs owned**: HBAR-010's dependency defect repaired at its honest size
  (HBAR-011, T3 ceiling); the kappa name collision registered with rename queued;
  the predictions paper re-sync queue carries four P6 corrections, batched.

## Verification

`tools/verify_corpus.py --quick` passes; per-claim benchmarks pass individually;
the full sweep runs in CI. Bars for every session in this release were locked
before computation; the locked files live in `analysis/`.
