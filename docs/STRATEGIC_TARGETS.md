# CHARTER -- THE v3.21 PREDICTIONS RECONCILIATION (next session)
# and the standing strategic target ranking

Written 2026-08-12 at the author's direction, after the strategic
step-back. Purpose: stop spending tokens on interior conditionals and
point the program at its external surface. This file is the next
session's charter and the ranking that governs session selection
until the author revises it.

## A. The reconciliation session (one session, mostly reading)

Object: papers/falsifiable_predictions.pdf, frozen at v2.2.5
(July 2026) with the 1 Aug 2026 discrimination audit. Registry now at
v3.21.1, 637 claims. Roughly 300 claims postdate the paper.

Tasks, in order:
1. AUDIT the existing entries against the current registry. For each
   of the ~19 entries: does the corpus still stand behind it verbatim?
   Known suspects: any entry touching the retired mesoscopic-hbar
   relations, the pre-BET stiffness numbers, or one-medium language.
   Each entry gets a one-line verdict: STANDS / SUPERSEDED (by claim
   ID) / RETIRE (with reason).
2. ADD the post-v2.2.5 confrontable content, drafted to the paper's
   own input-classification standard:
   - FND-046's Casimir-scaling pin: kappa_pack read directly from a
     resolved long-distance Casimir-scaling ratio; forward numbers
     -1.25 / -0.25 percent at the two floors. Lattice-reachable.
   - FND-047/055's k-string discriminator: the SU(6) sine-vs-Casimir
     statistic under GRANT-N2; a named exclusion channel.
   - The reconnection chain's conditionality stack (NUC-030 crowned
     conditional on three named premises, each with an armed
     falsifier) -- stated as a falsification ROUTE, not a number.
3. RERUN the discrimination audit's question on the updated list:
   which entries have outcomes that DIFFER from standard physics,
   are checkable, and are live? The count was one (Prediction 6).
   If the Casimir pin or k-string entry raises it to two or three,
   that is the session's headline.
4. DISCLOSE the photon-sector limitation inside the paper on the
   paper's own terms (KNOWN_LIMITATIONS already carries it; the
   external document should not be caught with it).
5. Version the paper (v3.21-aligned), regenerate the PDF, and
   unfreeze ZENODO_RELEASE_NOTES.md in the same pass.

Explicitly OUT of scope: any new derivation, any commission, any
touch of n_b, g_C4, or the multiplicity-currency question.

## B. The standing target ranking (tokens go top-down)

T1. THE EXTERNAL SURFACE (the session above, then the alpha-chain
    standalone writeup). Rationale: the program's interface has
    drifted ~300 claims behind its interior; reconciliation converts
    sunk work into reviewable surface at reading cost, no compute.

T2. THE DISCRIMINATION COUNT. The paper's own audit says the
    framework currently offers ONE way to be distinguished from
    standard physics. Raising that count is worth more than any
    interior result. The two candidates are already in the registry
    (the Casimir pin; the k-string statistic); the work is
    presentation and confrontation against published lattice data,
    not derivation. PSR J1713+0747 remains the named external clock
    on Prediction 6 (~2027-2030).

T3. THE PHOTON SECTOR (the PeV failure). The program's most serious
    open problem and the first thing a hostile reader finds. Both
    named repair candidates cost a new primitive (strand
    substructure below d_c, or a second carrier owing two
    polarizations and a coupling). This is a GRANT-CLASS decision:
    a session here should PRICE the two candidates side by side for
    the author's desk, not attempt a repair unpriced. One session,
    bounded, decision-oriented.

T4. INTERIOR DEBTS, RATIONED. The multiplicity-currency audit (one
    verbatim reading, minutes), the widened G-AX sweep, the kappa/w/g
    rename sweeps, NUCQ-002, the Born gate. Real, cheap-ish, and
    strictly below T1-T3. None of these changes what an outside
    reader sees.

FROZEN UNTIL A NEW CARRIER OR EXTERNAL DATA ARRIVES: g_C4 (four
misses, instrument set exhausted); the black-hole channel (closed
twice over by the corpus's own work); further n_b sessions beyond
the two rationed items in T4.

## C. The session-selection rule (to control token burn)

Before chartering any session, it must name which target tier it
serves. Tier-4 work rides along at the END of a session that served
T1-T3, or in a deliberately short session; it does not get arcs.
