# Release v3.8.0 — The detector, understood — and its edge surveyed (4 August 2026)

**482 registered claims (105 Derived, 339 Modeled, 4 EFT-constrained,
4 Conjecture, 5 Open, 25 Failed-and-kept); 464 code-backed, all passing.**

Five claims, one Derived cornerstone, a two-regime prediction suite, a
plain-language account, and a measured survey of the theory's own
boundary. This release completes the strand-kinetics program begun in
v3.7.0 and closes the three-session exclusivity arc at the classical
engine's jurisdiction line.

## The cornerstone (FND-STRAND-020, Derived)

**The Poissonization law: S_N = S_24^(N/24), parameter-free.** Under
channel independence, an intensive per-channel law, and count
proportional to N, the aggregate survival at any size is the reference
curve raised to the size ratio — the channel count cancels; nothing is
fitted. Confronted against three independently measured sizes: all three
hazard ratios inside registered CIs, medians to factors 1.10-1.30.
Mechanism: window compression — nothing Poissonizes, nothing forgets;
first-passage among many channels reads only the hazard's flat opening
stretch. The eleven-session detective story of v3.7.0 gets its law.

## The resolution (FND-STRAND-021)

**The falling per-channel hazard was product-state slip.** One
experiment, one changed variable: prepare true metastable-well
equilibrium instead of the product state every prior script used, and
the transient vanishes (R_eq = 0.93 vs the thrice-replicated 0.33-0.43),
at exactly the plateau rate (factor 1.34), escaping exponentially. The
mystery had been hiding in the first line of every script (phi = zeros);
seven sessions of flat instruments were correctly measuring stationary
aggregates while the slip lived in the correlations they integrate out.
Every earlier kill was a load-bearing exclusion.

**Prediction 11, final two-regime form:** after a quench, a small
detector shows the switch-on transient for one bath-correlation epoch,
compressed away at scale by the exact power law; in steady state, every
size clicks Poisson at the plateau rate with the band-gap prefactor.

## The predictions update

`papers/falsifiable_predictions` updated: Prediction 11 rewritten in
final form (with the retired interim drift claim recorded on its face);
Prediction 10 gains its kinetic corollary; and THREE NEW PREDICTIONS
join the list — **P23** the switch-on fingerprint (dark-count drift at
CONSTANT temperature, a signature no thermal-drift model supplies),
**P24** the detector size law (zero adjustable constants, Derived), and
**P25** one scale, two instruments (Predictions 10 and 11 pinned to the
same spectral location; either falsifies both). New plain-language
companion: `docs/DETECTOR_KINETICS_PLAIN_LANGUAGE.md`.

## The exclusivity arc and the surveyed edge (FND-STRAND-022..024)

Three sessions on "one quantum, one click," two of them stopped by their
own protocols, the third decisive:

- **022** — the pre-committed alibi clause caught the FIRE observable
  counting transient overshoots (NO-VERDICT-BY-INSTRUMENT), and the
  surviving measurement taught the first half of the lesson:
  CONCENTRATION, not energy, feeds the click.
- **023** — the priced grid was null, the no-extension stop-clause held,
  and the arithmetic diagnosed both nulls at once: the
  delocalized-mode "photon" dilutes as 1/N and was sub-thermal per site
  everywhere. Price in the INTENSIVE variable (the fifth lesson).
- **024** — the corrected split-packet design, intensively priced,
  bracketed on first outing and revealed a CLIFF: half a quantum clicks
  nowhere (0/48) and the one-quantum split clicks NEITHER lobe (0/64).
  Exclusivity is closed trivially at the classical level — and the
  session's real product is **the measured classicality limit**: real
  photons behind a beamsplitter click one arm at ~50%; this engine's
  split packet clicks neither. THE FUNNELING STEP (full-quantum
  reassembly at one absorption site) is extra-classical here, bounded
  by data rather than philosophy. Which-path, delayed choice, and the
  eraser now have a precise address: the far side of the funneling step.

**Flagged for the author (a decision, not a computation):** whether the
funneling step becomes the framework's third grant (the candidate is the
minimal INDIVISIBLE-DELIVERY postulate — unit energy transfer at
absorption, which with Born-from-channel-energy yields the beamsplitter
statistics), a new dynamical ingredient, or the permanent honest edge.
The decision is queued, with the recommendation recorded in session.

## Methodology ledger

The pricing ledger closes this release at six lessons (seeds, window,
tail shape, calibration bracket, intensive variables, sigmoid-foot
interpolation), each purchased by the session that would have preferred
not to pay. Two new standing rules: persistence-defined thresholds with
paired instruments, and the two-stage marginal gate. Two sessions
stopped by their own clauses and registered at full volume — the
difference between a discipline that compounds and one that apologizes.

## Commissioned and open

- The third-grant session (indivisible delivery), pending the author's
  decision.
- The predictions-paper LIMIT statement for the funneling step (a
  boundary, not a prediction) — the deferred docs item.
- Residuals: the per-N saddle (STRAND-012), exact-D on the 3D instrument
  (GRV-095), the analytic slip curve (optional polish).

Full details in `claims.yaml` (the authority), the per-session analysis
files, and `CHANGELOG.md`.
