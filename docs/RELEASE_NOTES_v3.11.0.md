# Release v3.11.0 — The alpha chain at summit, and the nuclear gap closed (6 August 2026)

**518 registered claims (111 Derived, 369 Modeled, 4 EFT-constrained,
4 Conjecture, 5 Open, 25 Failed-and-kept); 497 code-backed, all passing.**

The release in one sentence: the electron-dressing route to the fine-structure
constant was driven to its honest summit — every factor of 1/alpha = 4 pi^3 D_E
now derived, three independent observables consistent with one convention
structure, g = 2 falling out as a free byproduct, and the +178.8 ppm (0.018%) residual
cornered as radiative physics the static construction cannot contain — while in
parallel the nuclear heavy-table binding gap was closed from ~13% to ~1% with
derived classical physics, complete by theorem.

Not a derivation of alpha's value. The headline is exact and holds throughout:
every factor derived, one residual unexplained, pinned against a construction
with no continuous dials. The discipline that produced it retired three
flattering results along the way, and every one of those retractions stands in
the record.

## The alpha chain (ALPHA-DE-CHAIN)

The chain closes as **1/alpha = 4 pi^3 D_E = 137.060504** against the measured
137.035999, a residual of **+178.8 ppm (0.018%)**. Every factor is derived:

- **D_E = 1.1051029** — the electron dressing, computed BLIND by the Commission
  W Euler-Lagrange boundary-value solver on the rotating winding terminus (5
  configs, 7-digit agreement, gate PASS), before any comparison to alpha.
- **the prefactor 4 pi^3** — the naive geometric form pi^4 decomposes as pi^2
  (U-closure) x pi (target scale) x pi (J0 anchor); the rectified 4/pi enters at
  the J0-anchor conversion, turning pi^4 into 4 pi^3. This corrected a premature
  pi^4 lock caught on external review.

### The three gates, all run

- **Gate 1 (anchor verification): CLOSED.** kappa = pi/4 was FORCED from the
  registered occupancy freeze I = E/omega = m J0, not chosen — a first-power
  linear recording of a pure harmonic has identically zero smooth mean, so the
  rectified recording is the only one that produces a number at all. The
  deleted O Phase 2 calibration artifact was not needed: the convention was
  recovered from surviving structure (Option A). It over-delivered out of
  sample — one formula, J0 = m_e c A / pi, reproduces both hbar/(pi alpha) at
  the anchor scale and hbar at the confinement scale; the quadratic competitor
  misses both by pi/4.
- **Gate 2 (charge functional): LINEAR, 4/pi derived.** The electric-response
  integral is the winding's directed tether load (degree 1, computed on the
  committed solution; the energy competitor is degree 2 and already spent as
  D_E). A self-audit demoted the sign-structure argument (EM-015 shows sign can
  ride topology through a quadratic field), leaving the verdict on EM-013's
  force-type coupling plus Gate 1's rectification theorem.
- **Gate 2b (magnetic moment): g = 2 out of sample.** With every input frozen
  before the moment was mentioned, the mechanical moment pi mu_B converts under
  the Gate-1 recording to exactly 1 mu_B — the Dirac value, zero freedom. It
  misses the measured moment by -1160 ppm, which is the radiative Schwinger
  anomaly alpha/2pi (+1161 ppm) to 0.15%. This discharged Gate 2's identification
  caveat on an observable the chain never used.
- **Gate 3 (13.6 eV): PASSED.** The derived-alpha chain reproduces the Rydberg
  at exactly the residual squared (-357.6 ppm = 2 x the chain residual). The
  consistency core is algebra (correctly not oversold); the independent content
  is the 1/2 derived from the EM-015 Coulomb virial and the convention-count
  confirming the single 4/pi lives in e^2. A pre-statement sign slip (+357.6
  guessed, -357.6 actual) is logged.

### The residual, cornered

Z Brick 5's scale-invariance theorem (d ln D_E/d ln x* = 0 exactly) established
the chain has no continuous dials; the residual ladder (branch, continuum, q^2
slot) was exhausted. Two further candidates were then run and closed:

- **V-A next-order boundary term: MISS.** The finite-strain landing correction
  gave a coefficient of effectively zero and the wrong sign against a required
  -1.10 — run once against a pre-committed bar, no scan, stopped.
- **D-E-COMPLETE (functional completeness): AUDIT CLEAN.** A structural audit of
  the tethered terminus against the W functional found no omitted energy channel
  (four items ruled with citations; A2 exhibited the LOG subtraction's scope as
  the linearized winding self-energy, verified in-package: inner-decade kept
  energy vanishes with r_min, 1.35e-12 to 1.35e-14).

Every closed door points at one class: **LEAD-RAD**, the rotating terminus
radiating into and dressed by the medium's transverse field — back-reaction the
static functional cannot contain by construction. Its second prediction is
pre-installed: one radiative mechanism must produce both the alpha residual and
the g = 2 Schwinger gap with no separate freedom. A hard, dynamical campaign,
chartered but not opened.

### Provenance documented

The full factor-by-factor derivation is recorded three ways: a diagram
(docs/alpha_derivation_map.png, each factor with an arrow to its theorem), a
companion note (docs/HOW_ALPHA_WAS_DERIVED.md), and the mass paper's honest-
boundary section.

## The nuclear 13% campaign (NUC-A..D2), complete by theorem

The heavy-table binding gap — a declared quantum omission at ~13% — was closed
to ~1% with derived classical physics:

- **Symmetry energy derived in both parts:** the kinetic asymmetry coefficient
  from proton/neutron level-filling (NUC-A, a_A = 16.6 MeV) plus the mean-field
  overlap potential (NUC-B, ~2-3 MeV), with zero new constants.
- **Coulomb derived:** diffuseness and exchange (NUC-C), the exchange
  reproducing the SEMF Z^(4/3)/A^(1/3) form; the valley of stability derived
  end-to-end.
- **Surface superseded** to the finite-range bond-kernel value a_S/a_V ~ 1.26
  (NUC-D, correcting NUC-006's monolayer 1.108).
- **Curvature proven zero** (NUC-D2): the ball covariogram has no s^2 term, so
  the liquid-drop expansion of this model class terminates at surface order —
  and a spurious grid-bias term was caught and corrected, dropping the residual
  quantum shell/pairing target to ~2.9 MeV rms.

The reconciliation with NUC-010 is registered: NUC-010 refuted a naive Fermi-gas
kinetic term for the ABSOLUTE binding baseline; NUC-A's is the ASYMMETRY
coefficient, where the absolute kinetic energy cancels — no contradiction. The
classical program is at its floor; the genuinely quantum shell/pairing tier is
the named remainder.

## The discipline, on the record

Three flattering results were retired during the alpha arc and the retractions
stand: the premature pi^4 prefactor lock (corrected to 4 pi^3 on external
review); the -15 ppm Delta correction (retired by Z's consistency audit as a
reading-mixing artifact); and the Omega x D_E numerical coincidence (its rate
channel nulled when the derived coefficient came out -0.19). Every load-bearing
number is now backed by a script that reproduces it in-package. The surviving
result is trustworthy because of what did not survive.

## Scripts and charters added

explorations/: gate1_close_full, gate1_close_reconstruction,
gate2_charge_functional, gate2b_magnetic_moment, gate3_atomic_coupling,
va_next_order_boundary, de_complete_phase1_audit; the Y/Z arc
(y_action_vs_energy, y_eps4_robustness, y2_spatial_f4, y3_pairing_derivation,
y4_anchor_conversion, z2_consistency_audit, z3_branch_check, z4_q2_confrontation,
z5_omega_expansion, z5b_xstar_family); the nuclear campaign (nuc_a_asymmetry,
nuc_b_potential, nuc_c_coulomb, nuc_d_surface, nuc_d2_curvature).

docs/commissions/: the T–Z commission record, all three Gate closures, and the
three forward charters (V-A-next-order MISS, D-E-COMPLETE clean, LEAD-RAD
stamped).

## Honest bottom line

The classical construction has been pushed to exactly where it ends. The
fine-structure constant is reduced — not derived — to one blind number times a
fully-derived prefactor, with a single radiative-class residual and its arbiter
attached. The nuclear program is complete by theorem at its classical floor.
Neither constant is claimed derived; both are documented with every correction
visible.
