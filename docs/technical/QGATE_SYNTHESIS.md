# The QGATE Campaign: Synthesis

*Eighteen claims (QGATE-001..018, with THM-006 as prologue), reorganizing the
quantum frontier across two arcs: the scale branch (001--010) and the nonlocal
branch (011--018). This page is the map; every statement below is benchmarked in
the registry, and QGATE-008's benchmark re-verifies the chain on every corpus run.*

## The chain, one line each

1. **THM-006** — failures cluster at the dynamical frontier (4.2x enrichment; adverse first
   draft kept).
2. **QGATE-001** — the smooth no-go: no action floor in smooth rope dynamics (nu = 1-2, all
   candidates); the one non-smooth process (reconnection) carries an amplitude-independent
   separatrix action W = 1.80 T D²/c. Honest magnitude: W ~ 0.009 hbar.
3. **QGATE-002** — the ledger: 92 claims say "quantum," 11 mean it, FOUR inputs carry it;
   14 claims classicalized; the nonlocal residues carry zero scale inputs.
4. **QGATE-003** — the transfer test: three sectors demand the SAME normalization (spread
   4.6%); the gap becomes the prediction n_t ~ 111 collective strands, D/w ~ 19.
5. **QGATE-004** — the census: underdetermined by one ratio (w); the solidity kill line
   (f_c = 0.160) bisected the allowed window and the measurement had landed on the living side.
6. **QGATE-005** — the trilemma: {additivity, Sigma_ATLAS, Lorentz, n_t=111} inconsistent by
   5e10; the verdict handed to vacuum-birefringence experiment.
7. **QGATE-006** — the confrontation: PVLAS brackets both signs inside 1 sigma; the
   discriminator live; sensitivity gap ~7x (VMB@CERN's target).
8. **QGATE-007** — the inversion: unpolarized light-by-light is structure-blind (proven);
   the ATLAS Sigma pin snaps (both sides); the trilemma relaxes into the PREDICTION
   Sigma >= 5.1e35 J/m³; the wager's payoff matrix flips.
9. **QGATE-008** — the synthesis capstone: the scale/nonlocal split registered mechanically,
   its constituents re-verified on every corpus run so the map cannot silently drift.
10. **QGATE-009** — the vacuum-tension audit: the candidate survives a fourth kill attempt,
    and the corpus gains a Schwinger diagnosis of *why* its birefringence debt exists.
11. **QGATE-010** — the birefringence debt converted from an IOU into a specification: what
    the matter sector must deliver, written as a contract the corpus cannot yet pay.
12. **QGATE-011** — the nonlocal branch OPENED (not untouched): the missing piece is named,
    defined, and priced — the corpus is not missing quantum mechanics, it is missing one
    structural object (configuration-space guidance).
13. **QGATE-012** — the corpus's first dynamical Bell violation: the two-particle singlet
    under guidance, the claim QB-003 was always waiting for — a violation produced, with the
    guidance object imported rather than derived.
14. **QGATE-013..017** — the Born-relaxation campaign: substantial, controlled, honest.
    Relaxation toward Born weights is robust across mode count after timestep convergence
    (015); local trajectory instability accompanies but does not control it (016); global
    configuration-space transport outperforms local chaos as the predictor (017). Bars locked
    before data throughout; one bar failed and was kept (016 B5).
15. **QGATE-018** — the Sigma experiment specified for an experimentalist to read — and the
    honest surprise that writing it produced: the arbiter is subtler than the campaign assumed.

## The frontier's new shape: two branches, asymmetric fates

**THE SCALE BRANCH** is now one conditional chain:

> IF collective reconnection (n_t ~ 111 strands, D/w ~ 19) AND Sigma >= 5.1e35 J/m³,
> THEN hbar = W_collective serves all four quantum inputs with zero per-sector retuning.

Alive, expensive, and precisely invoiced: the vacuum must carry >= 5.6e18 kg/m³ of
non-gravitating tension (worsening EM-RECON-015 ten-billion-fold — recorded); QED's own
birefringence must eventually emerge from the framework's matter sector (the named debt); and
the deciding experiment is VMB@CERN-class polarimetry with the INVERTED payoff — a QED-like
result supports the candidate; a rope-signature negative result kills it.

**THE NONLOCAL BRANCH** (Born equivariance, measurement, Bell) carries zero scale inputs and
is immune to every result on the scale branch — it is missing a KIND OF DYNAMICS, not a number.
The first arc called it "the deeper half" and left it untouched; the second arc (011--018)
opened it. What that opening established, honestly graded: the missing piece is not quantum
mechanics wholesale but one structural object, configuration-space guidance (QGATE-011); with
that object supplied, the corpus produced its first dynamical Bell violation from a nucleated
singlet (QGATE-012); and the Born-relaxation campaign (013--017) showed relaxation toward Born
weights is robust and driven by global configuration-space transport rather than local chaos.
The branch is therefore no longer untouched — it is opened, measured, and priced — but the
derivation proper is still open: the guidance object is imported, not yet derived from a rope
model of the wavefunction's dynamics. That remains the deeper half; it is now mapped rather than
merely named.

## What the campaign established, at each grade

- **Proved (within stated scope):** the smooth no-go; unpolarized-LbL structure blindness;
  rate sign-blindness — polarimetry's monopoly.
- **Measured:** the four-input dependency surface; the classicalized census; the
  uniform cross-sector demand (4.6%); Born-relaxation robustness across mode count (015--017).
- **Demonstrated (nonlocal branch):** the corpus's first dynamical Bell violation from a
  nucleated singlet (QGATE-012), with the guidance object imported not derived — the honest
  residual of the deeper half.
- **Predicted (forced, not tuned):** n_t ~ 111; D/w ~ 19; Sigma >= 5.1e35.
- **Corrected (its own claims, same week):** EM-RECON-014's ATLAS pin; QGATE-006's payoff
  matrix; the "nonlocal branch untouched" framing itself, once 011--018 opened it. The
  metabolic rate is itself a finding.
- **The pattern, three occurrences:** native-hbar demands keep forcing over-coarse meshes;
  the surviving resolutions (Sigma-large; hierarchy) both point away from "hbar lives in the
  smooth mesh."

## The question, in one sentence

*Does the vacuum hold five times ten-to-the-thirty-five joules per cubic meter of
non-gravitating tension, and will VMB@CERN see ordinary QED?* — plus one branch that no
number can touch, now opened: the nonlocal dynamics are named, a Bell violation is
demonstrated, and the one missing object (configuration-space guidance) is priced but not
yet derived.
