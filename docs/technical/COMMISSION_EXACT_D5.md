# COMMISSION: THE RUNNING COEFFICIENT VERSUS THE CONSTANT-D FORM (EXACT-D V)

Successor to GRV-099. Charter written and bars locked before
computation. Operator GO 2026-08-09. Operator: Mark Palmer.
Computational collaborator: Claude (Anthropic).

## The interpretive question, made computable

GRV-095's induced tension zeta D hbar c / a^2 assumed a constant
covariant coefficient. GRV-099 found the sqrt-channel vanishes and the
non-analytic content is the even-log u ln u. The physical weave band is
gapped at the strand mass (FND-STRAND-008: u in [1, 3.56], same
kt = 0.64 units), where every u^n ln u vanishes AT u = 1 -- a unit
artifact: the log's argument carries the scheme, so only the log's
SLOPE (L1 per channel) is universal; the absolute normalization is
renormalization-point dependent. Three registered facts frame the
adjudication: GRV-024's doctrine had two categories (m^2-analytic =
medium renormalization; m-odd = gravitating) and the even-log is a
third, in neither; GRV-022 showed the FULL response is non-covariant;
GRV-021's 1D route used the full finite response (1D needs no split).

## Plan and pre-committed bars

- P1/B1 (integrity): all six prior benchmarks PASS unmodified.
- P2/B2 (the pattern of the log -- the decisive test): extract L1 per
  tensor channel (xx, zz, xy, xz) with the GRV-099-validated four-term
  model at q = 2e-3. Bar, identical to GRV-024's locked pattern bar:
  |L1_xx / L1_xy|, |L1_zz / L1_xy|, |L1_xz / L1_xy| each < 0.2 means
  the even-log family IS Einstein-Hilbert-patterned (the covariant
  induced action lives in the log channel, coefficient = the universal
  slope L1_xy). Ratios >= 0.2 mean the log is non-covariant medium
  physics and the covariant residue at the physical point is zero.
  EITHER outcome is registrable; the bar adjudicates which.
- P3/B3 (the 1D/3D disanalogy -- the dictionary's seat): the 1D band
  response (GRV-021's object, gapped band, same kt) recomputed at two
  internal resolutions must be grid-stable < 1% with no subtraction --
  finite by construction -- while the 3D log requires one. Registered
  as the precise location of GRV-095's dictionary-factor ambiguity.
- P4/B4 (cascade robustness): the scheme swing of the 3D log-channel
  normalization across renormalization points u0 in [1/4, 4] --
  |L1 ln u0| span -- expressed in orders of magnitude. Bar: if the
  swing is < 5 orders, GRV-095's F-Lor exclusion (33.8 orders at the
  generous edge) SURVIVES the finding and is registered as such; the
  precision scale selection (a = 8 Planck lengths within a factor of
  ten) is registered as inheriting the swing. If the swing were >= 5
  orders, the fork reopens and is registered as reopened.
- P5 (doctrine amendment): the two-category split of GRV-024 gains its
  third category in the record, with this commission's numbers.

## Kill and honesty conditions

No bar widens. B2's two outcomes are both adjudications, not successes
and failures; only a ratio landing ambiguously astride 0.2 within its
own numerical scatter registers as unresolved. The re-derivation of the
a = 8 l_P selection under the amended doctrine is NAMED, not attempted:
it requires GRV-075's machinery traced end to end and is its own
session. Every number enters the record.

## Deliverables

benchmarks/gravity/exact_d_interpretation.py; a registered claim
(proposed GRV-100); charter outcome addendum; document propagation;
release at the operator's preference.

## OUTCOME (registered 2026-08-09, GRV-100, Modeled)

B1 PASSED (six). B2 PASSED, the decisive verdict: per-channel log
slopes (xx, zz, xy, xz) = (-5.3e-8, +2.8e-6, -1.61e-4, -8.2e-7),
pattern ratios 0.0003 / 0.017 / 0.005 against the locked 0.2 bar --
the covariant induced action lives in the even-log channel, two orders
cleaner than the original m-odd verdict, with universal slope
L1 = -1.61e-4. B3 PASSED: the 1D gapped-band response is finite with
no subtraction (0.05% grid shift) -- the exact seat of the dictionary
ambiguity. B4 PASSED both ways: C(u0) = L1 ln(1/u0) is sign-indefinite
over u0 in [1/4, 4]; the normalization is not loop-predicted; the
F-Lor exclusion survives trivially (bridging 33.8 orders needs
ln u0 ~ 1e34) and the a = 8 l_P precision selection inherits the
O(1)-and-sign scheme dependence. Doctrine amended: the even-log is the
third category and the covariant one. Named next-order: the a = 8 l_P
re-derivation under the amended doctrine, and the physical principle
fixing the renormalization point. Benchmark:
benchmarks/gravity/exact_d_interpretation.py.
