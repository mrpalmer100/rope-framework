# T3 PRICING -- THE PHOTON SECTOR'S REPAIR CANDIDATES, ALL FIVE, PRICED FOR THE DESK
# (2026-08-12; docs/STRATEGIC_TARGETS.md T3. A pricing document, not a
# commission: no claims registered, no grants taken. Adoption is the
# author's call.)

The failure, restated in one line: the observed 1.4 PeV photons need
transverse coherence sampled at <= 1.409e-22 m AND isotropic
propagation; the registered medium supplies neither (spacing 5 orders
too coarse; three straight strand axes give a 1e-9 sky fraction).
Every candidate must clear BOTH axes: the LENGTH and the DIRECTION.

## C1. Straight substructure (thinner fundamental strands)

The author's candidate, and it has fresh registry support: SHIN/TAV
established a two-level width hierarchy (vacuum strand vs tube
constituent, x4-9) as live registered structure. The escape from the
registered length-tuning block: the m_e calibration T0 a = 2.6065e-14 J
prices the BUNDLE, not the fundamental strand.

THE GATING ARITHMETIC (computed this session):
- Required shrink factor in sampling length: 6.8e4 (kappa250 reading)
  to 1.2e5 (kappa50). Areal substructure count n_sub ~ 4.6e9 to
  1.3e10 fine strands per coarse cell.
- The NAIVE tuning route (T0 fixed by the anchor, a shrunk) raises
  Sigma_vac by 3e14-1.5e15 -- the registered destruction, confirmed.
- The REDISTRIBUTION route (same total tension carried by n_sub finer
  strands: T0_f = T0/n_sub ~ 1e-7 J/m, fine spacing a/sqrt(n_sub))
  leaves the tension per area, hence Sigma and the Lorentz margins,
  UNCHANGED by construction -- the destruction argument does not
  fire. This is the pricing session's genuine find: the length axis
  is purchasable without touching the Lorentz bound.
- PRECEDENT for the count: N ~ 3e11 (FND-MATTER-004) shows the corpus
  already lives with counts of this size at the atomic threshold.
PRICE: one new primitive (the fine strand, with n_sub ~ 5e9-1e10);
one owed explanation (why d_c = 1.87e-19 m reads as the coarse width
-- plausible as the bundle envelope, underived); one owed
verification (the wave-sector speed and every accessible-energy
result must reproduce on the fine mesh -- expected, since c^2 = T/mu
is scale-free under redistribution, but owed as a benchmark).
WHAT IT DOES NOT BUY: the direction axis. A finer straight lattice is
still a slab. C1 ALONE DOES NOT REPAIR THE SECTOR.

## C2. Helical substructure (the winding removes the axes)

The fine strands of C1 wind helically within the coarse strand. Below
the pitch scale there is no straight preferred axis: the direction
field precesses, and the slab argument (FND-059/061) loses its
premise. Ontologically CONSERVATIVE: the founding object of the
program is a twisted two-strand rope; the straight three-family weave
was the modeling simplification, not the ontology.
PRICE: the C1 primitive plus one parameter (the pitch, which must sit
at or below the PeV sampling scale); the owed re-derivation of the
collective transverse branch on wound carriers, with FND-REL-002's
Lorentz-forced isotropy as the acceptance test rather than the
casualty; and the consistency burden that ELEC-096/099's MEASURED
coarse-scale axis pinning (anisotropy 0.21) must emerge unchanged at
accessible energies.
POSSIBLE DOUBLE DUTY, flagged not claimed: the corpus's cosmic
birefringence mechanism (Prediction 3) is sourced in rope chirality,
and PRED-002's audit records a NINETEEN-ORDER suppression owed on the
structural scale in optical observables. A derived pitch hierarchy is
exactly the kind of object that could owe both stories to one
structure. If C2 is chartered, that cross-check rides along free.

## C3. Second carrier

Already named and priced in KNOWN_LIMITATIONS: a new quantum owing
two polarizations and a coupling, duplicating the photon at high
energy with an invisible crossover (no spectral feature is observed
across TeV-PeV). Highest primitive cost, no double duty, no fresh
support. PRICED AS THE FALLBACK.

## C4. Network-waveguide transport (rerouting at crossings)

Proposed this session; killed by its own gating number, registered
here so it stays dead honestly: point-source pointing requires the
per-crossing deflection to satisfy theta_c <= 0.005/sqrt(N) with
N ~ 1e37 crossings over 5 kpc -- theta_c <= 1.3e-21 rad. Random
rerouting diffuses point sources into the sky; coherent forward
transmission is straight-line propagation, which restores the slab.
CLOSED AT PRICING. (The one survivor of the idea: guided transport
needs no transverse coherence, a reminder that the 1.41e-22 m demand
is inherited from the plane-wave form -- but the pointing number
closes the loophole regardless.)

## C5. Regime change (PeV quanta as topological excitations)

The corpus owns dissipationless kink transport (FND-KIN-002) and an
arriving-kink spectrum (TET). Priced honestly against three walls:
(i) kinks ride strands, so DIRECTION fails exactly as C4 (same
pointing number applies); (ii) the carried light-carrier caution --
the screw/torsion mode carries CHARGE, so identifying photons with
topological strand excitations crosses a registered identification
the corpus has explicitly fenced; (iii) energy bookkeeping: one PeV
quantum = 8.6e9 kink units (E/T0a), an object with no registered
formation or detection channel. THREE WALLS, EACH INDEPENDENT.
CLOSED AT PRICING unless combined with C2 -- and under C2 the
collective-mode repair is available anyway, making C5 redundant.

## The desk summary

One purchasable path: C1+C2 TOGETHER (helical substructure). C1 buys
the length axis at zero Lorentz cost (the redistribution arithmetic,
this session's find); C2 buys the direction axis and is the only
candidate that attacks it at all; the pair costs one primitive
family (fine strands, count ~5e9-1e10, pitch parameter) plus three
owed verifications, and carries a possible double-duty payoff on the
birefringence suppression debt. C3 is the fallback. C4 and C5 are
closed at pricing with their kill numbers on the face.

DECISION FOR THE AUTHOR: charter the C1+C2 acceptance computation
(the fine-mesh wave sector: speed, isotropy above the pitch,
accessible-energy invariance, Lorentz margins) as a bar-locked
commission, or refuse and leave route (c) disclosure standing.
Nothing is adopted by this document.
