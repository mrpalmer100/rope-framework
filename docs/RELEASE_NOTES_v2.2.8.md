# v2.2.8 — The whisper: Hawking's radiation, rebuilt from rope — with a temperature that runs

Corpus state: **191 registered claims (88 Derived, 81 Modeled, 4 EFT-constrained, 4 Conjecture,
7 Open, 7 Failed-and-kept); 179 code-backed benchmarks, all passing; 59 papers.**

One arc, one day, five claims — opening on the summit v2.2.7 named and closing with the corpus
having rebuilt every public feature of Hawking radiation from crossing mechanics, by a different
power source, with three independent observational discriminators standing where the pictures part.

## The whisper arc (GRV-040 … GRV-044)

- **The scale, with a cancellation** (GRV-040): a ratchet event at the reconnection shell emits at
  strand-scale frequency from strand-scale depth, and the Rindler redshift makes the strand scale
  CANCEL — any microscopic emission from its own natural depth arrives at infinity at the horizon's
  own frequency. The O(1) coefficient, built from engine quantities measured for other purposes
  (post-collapse ringdown 0.35; survival-weighted shell depth 0.65), came out at **0.23 kappa —
  within a factor of two of Hawking's spectral peak (0.45 kappa)**. Escape verified by exact
  transfer matrix (4–11 percent); luminosity law closed under energetics (L = f Mdot c^2 T_grey,
  f ≤ 1): accretion-powered, so the isolated silence of GRV-039 is preserved exactly. Observational
  face: ~182 Hz for ten solar masses; the LISA band for Sgr A*.
- **The shape verdict, disciplined** (GRV-041): engine-faithful quenches inside the graded profile
  (the chain doing its own redshifting; deeper events observed lower — the ordering check). The
  ensemble preferred Planck over a power law with two independent temperature estimators agreeing
  at ten percent — and the locked exponential-tail bar FAILED (r² = 0.58 vs 0.98), so thermality
  was NOT claimed: Planck-preferred, tail-unproven.
- **The tail convergence** (GRV-042): three instrument generations, r² = 0.58 → 0.33 → 0.91 — and
  the dip proved the diagnosis: discrete depth grids truncate the deep-redshift pile-up whose
  completion IS the exponential. The mechanism named — universality from geometry, the redshift
  pile-up laundering microphysics out of the spectrum — and the bar still held: a trend, a
  mechanism, an apparent limit; nothing more. (The compact benchmark initially failed its own bar
  because fewer depths reproduce the contamination — the encoder corrected by the phenomenon it
  certifies — and now asserts the convergence itself.)
- **The pile-up integral** (GRV-043): the analytic continuum, every ingredient independently
  measured (Gaussian-tailed burst kernel; the GRV-035 survival curve; the GRV-040 greybody). The
  locked bar PASSES (r² = 0.983) — confirming the apparent limit and vindicating the refusal: had
  0.91 been declared thermal, this would be confirmation theater; because it wasn't, it is a
  derivation. **GEOMETRY THERMALIZES**: the depth integral converts the Gaussian kernel into a
  thermal-class envelope. **THE TEMPERATURE: T = 0.21 kappa against Hawking's kappa/2pi = 0.16 —
  ratio 1.3, from inputs that never met a 2pi.** Recorded as a striking O(1) agreement, offset
  unexplained, no exact claim.
- **The saddle-point derivation** (GRV-044, **Derived**): boundary-Laplace closed form verified to
  five percent (the flat first-pass ratio catching a dropped constant — the arc's ninth
  assert-catch, of the analyst's own algebra). THE AMENDMENT: the true asymptotic tail is GAUSSIAN
  with a **running effective temperature T_eff ∝ 1/omega** (verified to 0.8 percent); the
  'exponential' was the finite-window effective slope, the window temperature (0.209 kappa)
  reproducing the fitted 0.21 exactly. The pattern named: the numerics propose, the bar restrains,
  the derivation disposes.

## The observational ledger

Three independent discriminators between strand black holes and textbook ones: **(1) isolated
holes are silent** — a detected primordial-black-hole evaporation burst falsifies the branch
(Prediction 16); **(2) emission tracks feeding** — L proportional to the accretion rate;
**(3) the tail runs cold** — Hawking's temperature is constant, the whisper's falls as 1/omega,
leaving the spectrum tail-deficient against a matched Planck curve (Prediction 17). Measure the
tail, and the power source identifies itself.

## Documents

The black-hole paper gains Section 7 — The Whisper — with the three-discriminator ledger, its open
program refreshed, and the registered-claims roll extended through GRV-044; GRV-034…039 repointed
to it. The predictions paper is retitled **Seventeen Predictions and a Reproducibility Standard**,
adding Part V (Predictions 16 and 17) and two glance rows. Thermodynamics and cosmology carry the
honest Bekenstein–Hawking status (form derived; temperature 1.3 kappa/2pi; 1/4 open; divergence
registered). The plain-language guide gains "A Comb, Not a Point — What a Black Hole Actually Is"
(a feeding hole of ten suns hums an F-sharp below middle C; "we would rather be falsifiable than
fashionable"). INSTRUMENTS.md gains the whisper as an in-principle observable, with f
bounded-not-derived in bold and the PBH fork noted as already in-principle under test.

## The discipline's cycle

Two refusal-then-derivation sequences beat declare-then-defend; two instrument-catches (the
benchmark bitten by its own phenomenon; the dropped e^lnA found by a flat ratio); every bar locked
before its measurement; and the one bar that mattered held shut through three lattice generations
until mathematics opened it. A framework that declined Hawking's mechanism at v2.2.7 has now
rebuilt his radiation's every public feature — and knows exactly where the pictures part.
