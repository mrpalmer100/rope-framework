# Release v3.4.0 — Strong field, from statics to the Unruh shape (2 August 2026)

**455 registered claims (104 Derived, 314 Modeled, 4 EFT-constrained, 4 Conjecture,
5 Open, 24 Failed-and-kept); 437 code-backed, all passing.**

One campaign, eleven claims (GRV-074..084), opened on the weak field's oldest
assumption and closed at a horizon temperature with a measured coefficient and a
single residual premise.

## The G question (GRV-074, GRV-075, GRV-076)

- **GRV-074**: the assumed G ~ 1/(Ta) is DIMENSIONALLY OPEN -- the CONST defect
  class, found in the sole T1's other leg -- and its closure power was never
  derived. The keeper: J1713 refutability is G-FORM-ROBUST across every
  candidate (2.9e6 clock sigmas per row). Values conditional; decidability not.
- **GRV-075**: the corpus's FIRST DERIVED G EXPONENT PAIR -- the Sakharov route,
  with the band coefficient recomputed on GRV-021's own machinery and LOCK's
  enslavement making the band shape parameter-free: G = c^3 a^2/(16 pi zeta
  hbar), zeta = 1.208, exponents (0, 2), tension-inert. Solving for a SELECTS
  1.26e-34 m -- eight Planck lengths -- from a 96-site lattice zero-point
  coefficient. GRV-074's magnitude framing corrected on the record.
- **GRV-076**: the a-determination survey -- ZERO live pins exist. The scale
  sets are "Sigma and the bound wearing a different hat" (FND-017's words); the
  two-scale tension re-characterizes as a one-bit FORK, equivalently "is the
  Sakharov identification true?", with two named discriminators. Every physical
  invariant rides the degeneracy untouched.

## The horizon chain, derived downward (GRV-077, GRV-078, GRV-079, GRV-080)

- **GRV-077**: the load-share premise DERIVED -- exact static conservation of
  the directional-tension medium (mu = T/c^2) gives zero longitudinal demand
  and transverse demand e a_proper/c^2, exactly the component pressing can
  supply. One covariant divergence, both halves of GRV-038's premise.
- **GRV-078**: staticity DERIVED at linear order -- positive energy forbids
  growth outside exhaustion, two-sided radiation empties every compact shell by
  5e14 (reflecting control at 0.00%). The frozen star froze itself.
- **GRV-079**: nonlinear settling SPLIT by the exhaustion scale -- every
  sub-exhaustion disturbance settles (decays 2e7-1e20); the crossing regime
  registered as an honest negative under both closures tried.
- **GRV-080**: three findings, none expected -- crossings cannot self-generate
  from the exterior (focusing buys ~2x before removal wins); the trap is
  physics, not 1D; and the softening stand-in is DISQUALIFIED by an
  energy-consistency audit (a measured pump), its successor specified.

## The interior built, and the temperature (GRV-081..084)

- **GRV-081**: the ratchet-wave coupling -- passes the entrance exam the old
  instrument failed (0.37% drift, walls closed), and ACCRETION EMERGES UNASKED:
  infalling waves blueshift past the local threshold and are captured in a
  near-horizon shell (a failed bar, kept as the finding). Collapse becomes
  RECORDED STRUCTURE: a permanent footprint, exterior at machine quiet, energy
  accounted, 0.31 per broken crossing.
- **GRV-082**: the bit-cost sweep -- e_bit = 15.67 x e_th EXACT to three digits
  across a factor-4 sweep at matched drive. The chain threshold <- pressing <-
  proper acceleration gives the whisper an UNRUH-CLASS temperature profile.
- **GRV-083**: the lift-over theorem -- passing a pressed hard-core strand
  costs W = N h for ANY lift profile, no friction law imported: P-TH becomes
  theorem-plus-geometry, the chain drops to one premise.
- **GRV-084**: P-ENT quantitative -- exact two-state thermodynamics by machine,
  the log-odds coefficient MEASURED from the engine's own fired shells
  (L* = 2.0-3.3). T_res(sigma) = N(sigma) h/L*: the Unruh shape with a
  quantified coefficient and exactly ONE residual premise (P-EQ, the driven
  shell's steady state -- the named successor).

**The campaign's headline: from the frozen star's statics to an Unruh-shaped
horizon temperature through two theorems, two three-digit measurements,
registered geometry, and one named premise** -- with a first derived G exponent
pair, a Planck-class scale selection, and the corpus's honesty ledger carrying
three owned corrections, two failed bars kept as findings, and one instrument
audited into retirement along the way.

## Verification

`tools/verify_corpus.py --quick` passes; per-claim benchmarks pass; bars for
every session locked before computation in `analysis/`.
