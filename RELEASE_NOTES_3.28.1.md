# v3.28.1 (28 Aug 2026) -- THE MECHANISM CLOSES: E-LEDGER AND ITS CONTROL

**744 claims, 640/641 code-backed passing (1 documented waiver).**
Two new claims completing the winding-field evidence set, and the
verification defect behind the CI failures fixed at its root.

## The one-paragraph version

The stage-2c profile experiment re-marched the collapsing q = 4/3
branch with per-step state retention and measured, twelve gated
triples deep, that the amplitude collapse IS a rotation of the
branch's motion: the per-step arclength allocation moved from 89
percent amplitude / 11 percent winding to 51/49, the winding
velocity doubled as the rate fell sevenfold, and the winding's
acceleration PRECEDED the rate's break by two points (r = 0.870
against a pre-registered 0.8 line; f_dir +0.378 against +0.15).
The chartered control then ran the identical instrument on the
flat q = 5/3 branch and returned the required E-NULL (r = 0.227,
f_dir +0.002, V_pt -0.2 percent), excluding every instrumental
reading of the result: arc steps do not accumulate winding on
their own, and nothing in the instrument manufactures sector
rotation. With FND-150 (the winding field cannot be frozen) and
FND-151 (it runs hot where collapse lives), the S1-SPLIT
selectivity now reads: the collapsing cell demands winding; the
flat cell does not.

## Also in this release

Verification: claim backing must never depend on session
scratch. Campaign instruments (traverse96_scout,
native96_continuation) now carry bounded `--verify` paths that
check SHIPPED analysis/ evidence; FND-144 passes in a second and
its waiver is removed, FND-146's seeding key is corrected, and
FND-143 reports its documented archival gap cleanly as the single
waived item.
