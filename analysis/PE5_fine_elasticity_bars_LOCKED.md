# THE FINE-ELASTICITY SESSION (PE5) -- BARS LOCKED (2026-08-16)

Locked before adjudication. FND-116's reversal target: an
INDEPENDENT k_f/T0_f from sub-strand physics, zero coarse-ratio
input. Two stages; stage 2 runs only if stage 1 finds material.

## B0 -- STAGE-1 FINDINGS DECLARED (from the search bar)
(i) EM-RECON-012 verbatim: "gamma = 1/sin^2(theta) DERIVED EXACTLY
FROM HELIX GEOMETRY" -- gamma_lock is a twining-geometry parameter,
NOT a stretch ratio. Any route using gamma_lock = k_f/T0_f is a
name-adjacency and is REFUSED at lock (the day's fifth).
(ii) No registered base-fiber axial constitutive model exists
(search returned no live candidate).
(iii) Structural observation to adjudicate: the mapping's
compensation factors 1/sin^2(psi_i) have the SAME functional form
as EM-RECON-012's lock gamma -- two independent derivations
(SHIN3's speed-restoration pricing; EM-RECON-012's helix
kinematics), neither taking the other as input. If it holds, the
coarse->fine stiffness mapping FACTORIZES as
k_f/T0_f = (k/T0) x gamma(psi_1) x gamma(psi_2).

## OUTCOMES (exhaustive)
- DERIVED / CONFLICT / FALSIFIER-FIRED: only via a registered
  independent route (expected unreachable per B0(ii)).
- INVENTORY-INSUFFICIENT + NAMED OBJECT: the missing constitutive
  model goes to the author's desk as GRANT-CANDIDATE-BASEFIBER
  with FND-116's double-armed price sheet; any registered
  EXCLUSIONS on its class (derivable from the finite coarse ratio
  plus the multiplicative mapping) are registered alongside.

## REFUSALS
No constitutive model invented; the factorization registered as
structure only (no new number); gamma_lock never identified with
any stretch ratio; condition 4 and all riders unchanged.
