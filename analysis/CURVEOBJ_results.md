# COMMISSION CURVE-OBJ -- RESULTS (2026-08-18)

Executed under analysis/CURVEOBJ_bars_LOCKED.md.
Benchmark: benchmarks/foundations/curveobj_adjudication.py (imports the
ANSATZ module for its validated operator; the import replays that
benchmark's halt banner before this commission's output -- cosmetic).

## VERDICT, THREE PARTS

1. **TRUE-CURVATURE-ADJUDICATED.** The granted rod bends against the
   Frenet curvature of its actual centerline, within the granted class,
   by the grant's own clause: FND-118 carries no stress-free wound
   reference (the MAINT channel-i inadmissibility ruling stands on it),
   and the per-level sum is the bending strain of a rod referenced to
   the level-1-wound backbone -- an object the grant forbids. The
   numeric indictment seals it: |t . K_registered| = 1.506 at order
   one, while a curvature is orthogonal to its tangent -- the sum-form
   is not the curvature of ANY curve with these tangents
   (|t . kappa_true| = 4e-11 on the same operator). No new grant
   issued.

2. **ANSATZ-GAPPED, at order one.** The recovered self-consistency
   test (force side now unambiguous: tension times the true curvature;
   kb = 0) fails the 5% bar by a factor of ~25: residual RMS 1.38
   (aligned) and 1.09 (anti-aligned) in units of the level-2
   centripetal scale, dominated by the first b-harmonic -- the
   cross-coupling of the two rotations that the co-moving balance
   ignored. The metric leg fails independently: the stated motion does
   not preserve arclength (stretch rate RMS 0.15 Omega_2), implicating
   the k_f channel in any true solution.

3. **CONFLICT-REGISTERED.** The refit display (not adopted): the
   residual-minimizing Omega_2 is 0.43x the build's (aligned) and
   rails at the search boundary 0.20x (anti-aligned -- the minimizer
   wants even less; the displayed number is a bound, not a minimum).
   The kb = 0 corner at the aligned refit is 3.335 T0 (inside the
   registered box); at the anti-aligned refit it is 3.089 T0 --
   **BELOW the registered box's lower edge 3.222**, and a fortiori
   lower still past the rail. Per the outcome sheet this is a CONFLICT
   for the author's desk, offending corner named: the anti-aligned
   kb = 0 corner.

## WHAT THIS MEANS FOR Sigma_wave, STATED PLAINLY

FND-135's numbers are exact phase averages of a state that is now
MEASURED to be order-one off-shell at kb = 0. The box [3.222, 4.313]
gains the named rider (registry updated): values conditional on the
stated ansatz; the true composite state's energy is an open number,
with the refit display suggesting the lower edge is not safe. Nothing
is re-priced tonight; the author decides between (a) holding the box
with the rider, (b) commissioning the true-state solve (the
variational composite state on the true-curvature instrument), or
(c) retreating Sigma_wave to its level-1-exact component (2.598,
which is a verified solution's number) plus an open two-level
increment.

## THE ON-NOTICE LIST, DISPOSITIONED

- kb feasibility ceiling: TRUE-CURVATURE now adjudicated; the
  zeroth-order energy-weighting display is 0.079/0.776 = 0.102
  (LOOSER); the Bloch-instrument re-solve is chartered out and is the
  only thing that can move the registered value.
- SHIN7 worst-case bound: HOLDS (actual max 4.469 < 5.713); safe
  direction confirmed; on-notice discharged for the bound, value owed
  to the re-solve.
- Level-2 coefficient 17.926: its role in Omega_2's fixing is part of
  the ansatz just measured off-shell; re-derivation belongs to the
  chartered re-solve.

## CONTROLS AND CORRECTIONS

Operator controls: D_s R vs tangent 3.9e-11; Frenet orthogonality
3.8e-11. <|kappa_true|^2> = 12.29 /a_f^2 (MC drift 4.6e-2, PASS at the
MC bar), ratio to registered 0.776 (the 48^2-grid value 0.732 from
FND-138 is the tighter estimate; both displayed, grid value preferred).
The anti-aligned refit railing at its bound is reported, not smoothed.
Verdict prose written after the run.
