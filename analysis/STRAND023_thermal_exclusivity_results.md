# FND-STRAND-023 — exclusivity at the thermal point: results

Bars: analysis/STRAND023_thermal_exclusivity_bars_LOCKED.md. Data
archived: analysis/STRAND023_thermal_data.json (48 baseline + 120 grid
runs, persistence observables throughout).

## The locked-protocol outcome, and the stop honored

B0 PASSED: 0/48 baseline fires at h = 0.30, T = 0.40 over the window —
the operating point is silent as registered. The PRICED calibration grid
(E_sig = 1, 2, 4, 8, 16; the pre-estimate and its bracket stated on the
bars' face) fired ZERO of 120. Unbracketed despite the pricing — and per
the bars' own clause, written after 022's lesson, THE SESSION STOPS
HERE. No ad-hoc extension; B1/B2/B3 unevaluated; no exclusivity verdict.

## The diagnosis (arithmetic, not speculation)

The null is not physics refusing to click; it is the IDEALIZATION
diluting itself. A delocalized fundamental mode spreads E over N sites:
per-site injected kinetic energy is E/N x (1/... ) = A^2/4, and even at
the grid's top (E = 16) that is 0.167 — LESS THAN ONE THERMAL UNIT
(T/2 = 0.20) per site. The committed grid, in per-site terms, spanned
5% to 83% of thermal. Of course nothing fired: the "quantum" as modeled
was sub-thermal everywhere, at every committed energy, and dilutes as
1/N by construction. The 022 cold null now reads the same way, more so.

This retro-diagnoses BOTH stopped sessions with one sentence: the
delocalized-global-mode idealization of "one photon" guarantees a null
at any fixed energy as N grows, because real absorption is LOCAL. An
optical quantum on a real bench is ~40x kT delivered into a packet-width
region, not a whisper spread over the whole screen.

## The pricing lesson, refined (fifth entry, superseding the fourth's form)

Price calibration grids in the INTENSIVE variable the mechanism cares
about — per-site energy density against the thermal scale — not in
total energy. The fourth lesson (bracket your calibration) was executed
tonight and still failed, because the bracket was priced in the wrong
variable. The rule now reads: identify the intensive driver, pre-estimate
its onset, bracket THAT.

## What the two stopped sessions have established (measured, cumulative)

Across 022 + 023: 360 injected runs, cold and thermal, delocalized
coherent energy from 0.24 to 24 units, ONE genuine nucleation total —
while localized thermal fluctuations at comparable per-site densities
nucleate the entire 009..021 record. The click channel is fed by LOCAL
energy density, full stop. The wave delivers the odds; the bath delivers
the dot; and the odds themselves must arrive as local density, not as a
global whisper.

## The corrected redesign (third design, now correctly idealized)

FND-STRAND-024 candidate, fully specified: model one quantum as a
LOCALIZED packet — width xi ~ 8 sites, per-site density several x T
(the ~40x-kT-over-a-packet scaling of a real optical quantum) — SPLIT
into two coherent half-packets (E/2 each) delivered to two separated
regions of one thermal chain: the two-slit output in mechanical dress.
Observables: persistence-defined clicks per region; the exclusivity
statistic is p_double vs the independence baseline p_A x p_B measured
from single-lobe controls. Grid priced in per-site density (intensive),
bracketing the half-packet threshold by construction. B3 alibi: the
full-packet-in-one-lobe control must double reliably... must click
reliably, and the two-FULL-packet control must produce doubles.

## Ledger

- B0 pass; priced grid null 0/120; STOP honored per the bars' own
  clause; no exclusivity verdict issued or implied.
- The dilution diagnosis registered with its arithmetic; the pricing
  rule refined to intensive variables (fifth lesson).
- The cumulative two-session finding stands: local density, not total
  energy, feeds the click.
- Status: Modeled (the record, the diagnosis, the finding). Absolute
  scale untouched (FND-MATTER-003).
