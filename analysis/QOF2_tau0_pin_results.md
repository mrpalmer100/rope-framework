# THE tau0 PIN SESSION (QOF2) -- RESULTS (2026-08-16)

Bars: analysis/QOF2_tau0_pin_bars_LOCKED.md (locked first; the
corpus search ran as B1 and paid a third time). Benchmark:
benchmarks/em/tau0_pin_qof2.py (sympy-exact; one instrument catch
self-caught and logged: a draft print block hardcoded stale bracket
numbers, corrected to computed-and-assertion-locked before any
registration -- the eleventh instrument-catch class event).

VERDICT: DIMENSIONLESSLY PINNED at one named gate (beta), the
pre-committed second outcome. Absolute tau0 remains gated on r
exactly as pre-committed (scale ladder untouched).

## B1's THIRD PAYOUT -- the name collision

The corpus's only numeric "tau0" (1.95 w/c, ELEC-047 lineage) is a
causal traversal TIME in the retired hbar sector. Importing it would
have poisoned every downstream number with a duration wearing a
twist rate's clothes -- exactly the misassigned-ontology failure
mode FND-MATTER-047 named as the campaign's recurring pattern.
NAME_REGISTRY addendum: tau0_lock (twist rate, rad/length,
EM-RECON-012 lineage) is DISTINCT from tau0_trav (duration,
ELEC-047 lineage, retired sector).

## THE PIN, in three exact steps

1. THE INVERSION (Derived-grade rearrangement of EM-RECON-012):
       r tau0 = 1/sqrt(gamma - 1)      EXACTLY
   On the registered gamma bracket [2, 4]:
       r tau0 in [1/sqrt(3), 1] = [0.5774, 1.0000]
   The dimensionless twist-times-radius is BRACKETED BY GEOMETRY.

2. THE REDUCTION (the finding that makes the gate fall): the
   dynamical mixing angle needs only DIMENSIONLESS products --
       tan(2 chi_d) = 2 (v_t/c)^2 beta gamma (r tau0) / (k/T0 - (v_t/c)^2)
   and at the registered ratios (v_t/c)^2 = 1/5 (FND-MATTER-047),
   k/T0 = 2 (registered use, grade carried on the face):
       tan(2 chi_d) = (2/9) beta * gamma/sqrt(gamma - 1)
   Absolute tau0 CANCELS OUT of every downstream quantity GRV-119
   listed. The "tau0 gate" was, for the closed forms, a
   dimensionless gate all along -- and the dimensionless part is
   registered geometry.

3. THE BRACKETS. f(gamma) = gamma/sqrt(gamma-1) has its interior
   MINIMUM exactly at gamma = 2 (machine-verified critical point),
   so the bracket is honest: f in [2, 4/sqrt(3)] = [2, 2.3094].
       tan(2 chi_d) = beta * [0.4444, 0.5132]
   At the beta = 1 REFERENCE (labeled reference, not derivation):
       chi_d        in [0.2091, 0.2371] rad
       sin^2(chi_d) in [0.0431, 0.0552]
       eta_conv     in [0.0825, 0.1042]
       L_az/L_tr    <= ~4.3-5.5 percent
   Monotonicity audited: everything increases in beta and f, so the
   gamma edges map to interval edges.

## WHAT THE NUMBERS SAY, physically

At reference geometry, spin's per-crossing leak is four to five
percent of vibration's -- a twenty-fold protection ratio from pure
registered geometry, no tuning anywhere. The lock converts eight to
ten percent of a twist source's emitted energy into
crossing-available stretch. And the sector's rate formula
Omega = lambda gamma tau0 E0 / (n_x g sin^2(chi_d) C26) now has its
angular factor bracketed; the remaining gates are beta (O(1)
strand-geometry ratio, named, unregistered -- the honest residual),
n_x, C26-via-SIGMA, and g, plus r alone for any ABSOLUTE tau0.

## GATE LEDGER, before and after
Before (GRV-119): tau0, n_x, C26, g          [4 gates, tau0 blocking chi_d]
After  (this):    beta, n_x, C26, g          [4 gates, chi_d BRACKETED]
plus r for absolute tau0 only. The count is unchanged; the QUALITY
is not: the blocking gate on every angular quantity was dissolved
into registered geometry plus one named O(1) ratio, and the sector's
protection story now carries numbers.

## REFUSALS HONORED
No experiment consulted; beta not invented; scale ladder untouched;
k/T0 = 2's grade carried on the face of every number; the hbar
sector's retired numeric identified and quarantined, not imported.
