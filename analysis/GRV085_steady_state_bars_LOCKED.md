# GRV-085 bars — LOCKED before computation (2026-08-02)

Commission (the chain's residual premise): P-EQ -- the driven shell's bit
population was read with the equilibrium two-state formula; derive the steady
state instead, and say exactly what the equilibrium reading is the limit of.

The model, fixed in advance:
- The shell's bits obey a driven two-state master equation: intact -> broken at
  rate k+ = D + nu exp(-W/T) (accretion drive D plus thermal activation over the
  lift-over barrier W); broken -> intact at rate k- = nu exp(-(W + E)/T)-class
  with E the state-energy gap -- Arrhenius kinetics with a single attempt rate
  (premise P-ARR, named: GRV-037's metastability grammar; the ratchet's
  low-temperature one-way behaviour is the D >> thermal, T -> 0 limit of this
  law, so P-ARR EXTENDS the engine rather than contradicting it).
- Re-formation events RETURN energy to the wave field -- the reverse of
  GRV-081's two ledgers. Re-formation IS the emission channel: the whisper's
  microscopic identity, stated here as identification, quantified only in
  structure (rule R3).
- Self-consistency: the shell's temperature is set by its own reservoir,
  T = e_bit/L(f) with L(f) = ln((1-f)/f) and e_bit ~ W (GRV-082's measured
  tracking); the steady state is the joint fixed point (f*, T*).

Rules fixed in advance:
- R1: the master-equation fixed point and its stability are proven by machine;
  the log-odds formula WITH the drive correction is derived exactly:
  ln((1-f*)/f*) = (gap term) - ln(1 + D/(thermal rate)) -- the equilibrium
  formula must appear as the D -> 0 limit, or P-EQ is not discharged.
- R2: the self-consistent fixed point is computed across >= 4 decades of drive;
  the pre-committed bar: a stable fixed point EXISTS at every drive, and its
  log-odds L* lies in [1, 5] across the sweep (the O(1) claim, made falsifiable
  before running). The engine's frozen L* = 2.0-3.3 (GRV-084) is the comparison
  band and its reinterpretation (a frozen drive record, not a steady state) is
  recorded.
- R3: the flux is assembled as STRUCTURE only (formula, no constants, no
  confrontation with GRV-049..053 tonight).

Bars:
- B1: fixed-point theorem + exact drive-corrected log-odds per R1.
- B2: the self-consistent sweep per R2.
- B3 (the structural finding, pre-registered as the expectation): W and T both
  inherit the SAME pressing profile N(sigma), so their ratio W/T = L* is
  sigma-INDEPENDENT -- machine-checked symbolically. The Boltzmann factor of
  emission is constant across the shell: THERMALITY of the horizon shell
  derived from the profile cancellation, not assumed.
- B4: the emission-flux structure per R3; P-EQ -> DISCHARGED (equilibrium
  reading = the weak-drive limit of the derived steady state, drive correction
  exact); premise ledger after tonight: P-ARR alone.
