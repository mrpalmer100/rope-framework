# THE EA DERIVATION SESSION (PE6) -- BARS LOCKED (2026-08-16)

Locked before computing. Runs under FND-118's grant (elastic-rod
class) with the double-armed price sheet as binding structure.

## B0 -- CLEAN-ROOM (doubled per the grant's face)
The target value 8.091 and the floor 4.046 may NOT appear in any
derivation leg. They enter only in the final DEMAND-CONVERSION leg,
after the derived relations are fixed.

## B1 -- REGISTERED INPUTS (fine level, zero coarse-ratio input)
- The grant: one modulus E_f; k_f = E_f pi r_s^2; kb = E_f pi r_s^4/4.
- kb <= 0.126 T0_f a_f^2 (FND-091, registered bound; its
  string-like reading kb ~ T0_f r_s^2 gave the 0.355 slenderness --
  note the READING's coefficient is 1, i.e. it presumed a ratio;
  the rod identity replaces the presumption, see B2 disclosure).
- r_s <= 0.355 a_f (FND-091 ceiling; SEMI-DERIVED from the same kb
  bound -- circularity between these two inputs must be tracked and
  any near-coincidence they force must be DISPLAYED AND NOT SPENT).

## B2 -- WHAT THE SESSION CAN AND CANNOT PRODUCE (fixed at lock)
The grant makes E_f a bottom-level constant; with kb and r_s only
BOUNDED, the ratio k_f/T0_f cannot land as a number tonight. The
honest deliverables, pre-committed:
- THE ROD IDENTITY: k_f = 4 kb / r_s^2 exactly (shared modulus) --
  converting the ratio question into the PAIR (kb, r_s).
- RELATION + BOUNDS: k_f/T0_f expressed in the pair, with the
  registered bounds mapped through.
- DEMAND CONVERSION (final leg only): the price sheet's three
  outcomes re-expressed as regions in (kb, r_s) space.
- ANY corner/exclusion results derivable from bounds alone.
Outcome grammar: RELATION-REGISTERED (+ named exclusions) /
NUMBER-DERIVED (only if an unnoticed registered pin exists) /
CONFLICT / FALSIFIER-FIRED.

## REFUSALS
No kb value invented; no r_s value invented; semi-circular
near-matches displayed, never spent; k/T0 = 2 stays at FND-114's
grade unless NUMBER-DERIVED lands on target; condition 4 unchanged.
