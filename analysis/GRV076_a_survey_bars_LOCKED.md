# GRV-076 bars — LOCKED before computation (2026-08-02)

Commission (GRV-075's named audit): which registered results actually PIN the
lattice scale a, rather than bound it or assume it? The two-scale tension
(Lorentz-bound-class vs Planck-class) stands or dissolves on the answer.

Excavation findings, recorded before the arithmetic:
- FND-MATTER-005: the Lorentz bound a <= 1e-16 m is an UPPER BOUND.
- FND-017 (Modeled): T0 = Sigma a^2/3 exactly (ELEC-053's invariance theorem), and
  ITS OWN TEXT says the registered scale sets are "Sigma and the bound wearing a
  different hat" -- i.e. T0 = 1203/1700 N was CONSTRUCTED with a set to the
  Lorentz bound by convention, not determined.
- The 5.78e-17 m ambient spacing lives exclusively in the ELEC-041..053 lineage,
  descending from the RETIRED mesoscopic-hbar relation (ELEC-061); it is not a
  live pin.
- Sigma is empirically bounded BELOW (EM-RECON-016: Sigma > ~8.6e27 J/m^3), never
  pinned.

The hypothesis to adjudicate: NO LIVE PIN OF a EXISTS -- a is bounded above
(Lorentz), Sigma is bounded below (PVLAS-class), T0 is their product, and the pair
(a, Sigma) is DEGENERATE along the line Sigma = 3 T0/a^2 at fixed T0.

Rules fixed in advance:
- R1: classification is mechanical -- every registered a-relevant quantity is
  tagged PIN / BOUND / CONVENTION / RETIRED-LINEAGE / DERIVED-FROM-OTHERS, and
  the tag must be defensible from the claim's own text.
- R2: the consistency arithmetic runs BOTH forks at fixed T0 = 1203-1700 N:
  (F-Lor) a = 1e-16 -> Sigma = 3T0/a^2; (F-Sak) a = a_Sak = 1.26e-34 ->
  Sigma likewise. Each fork checked against the PVLAS lower bound.
- R3: the fork's physical discriminators are derived, not asserted: (i) the
  Sakharov channel's induced G at each fork (P1 viability); (ii) the vacuum
  nonlinearity scale (|Delta n| ~ 1/Sigma), 36 orders apart between forks.
- R4: if the no-live-pin hypothesis holds, GRV-075's "two-scale tension" is
  RE-CHARACTERIZED: not a contradiction but a one-bit FORK, and the annotation
  says so. No tier motion.

Bars:
- B1: the classification table produced mechanically (R1); the hypothesis
  adjudicated -- expected: zero PINs.
- B2: the degeneracy line verified (T0 = Sigma a^2/3 reproduces both scale sets
  at the bound, per FND-017's tenth-of-a-percent statement, recomputed).
- B3: both forks' Sigma computed and checked against the PVLAS lower bound (R2).
  Expected: BOTH allowed -- the empirical data do not discriminate today.
- B4: the discriminators (R3): the fork is decided by (i) whether the Sakharov
  channel IS gravity (F-Sak predicts G by construction; F-Lor makes the same
  channel 36 orders too strong, so under F-Lor premise P1 must be FALSE), and
  (ii) any Sigma-sensitive observable (vacuum nonlinearity onset).
- B5: propagation: GRV-075's tension re-characterized as the fork; the
  T-conditionality of GRV-075's "invariants" stated exactly (l_q and A are
  (alpha, hbar c, T)-built and stand at fixed T0, which the degeneracy line
  preserves); the retired-lineage flag on 5.78e-17 recorded.
