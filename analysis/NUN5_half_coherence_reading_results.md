# COMMISSION NUN5 -- RESULTS (FND-107, 2026-08-13): NOT-FORCED, WITH THE MISSING RULE NAMED EXACTLY

*Bars: analysis/NUN5_half_coherence_reading_bars_LOCKED.md. This is
a reading of registered text; the operative passages are quoted
verbatim below. No numeric grading of any new statistic occurred.*

## The registered grammar, verbatim

From the GRANT-N2 adoption (AYIN bars, locked 2026-08-11): "Strands
carry one of N labels. A unit winding is label-carrying; a k-string
is k unit windings. Inter-tube attraction arises from LABEL EXCHANGE
between tube pairs. One primitive, one coupling; nothing else is
granted." And: "v(N) must come from counting exchange channels
against available label partners."

From the AYIN derivation (results, statistic B): "for a tube of
label i the partner's label is one of N-1 others, and the exchange
carries 2 orientations: v_B = 2/(N-1)."

## The verdict: NOT-FORCED

The registered grammar is CHANNEL COUNTING and nothing else. It
contains no amplitudes, no phases, no interference, and no rule by
which two counted channels could carry unequal weight. All three
factors of the candidate reading fail the forced-reading criterion
simultaneously:

- the 1/2 on the reverse orientation cites nothing registered;
- the +1/(N-1) echo cites nothing registered;
- the very ASYMMETRY between orientations is inexpressible: the
  grammar's only operation is to count a channel at weight 1 or not
  count it at all.

w = 1/2 + 1/(N-1) is therefore not merely unforced; it is not
WRITABLE in the registered language. Per the locked grammar:
SAMEKH-class certificate, registered at full volume.

## The missing structure, named exactly (the certificate's content)

An ORIENTATION-COHERENCE RULE: an assignment of weights (equivalently
amplitudes) to exchange orientations, i.e. an upgrade of the exchange
primitive from a counted channel to a weighted channel. This is
primitive-level structure: it cannot be derived from label counting
(counting has no weights to derive) and therefore sits in grant
territory, reserved to the author, exactly as the N-valued label
itself did before GRANT-N2.

## Display (restatements of registered arithmetic only; nothing
## graded, nothing promoted)

Two grant SHAPES would make the window point writable, listed for
the author's decision with face values already registered at NUN4:

- G-A (HALF-WEIGHT REVERSE + ECHO): grant the coherence rule
  directly with w = 1/2 + 1/(N-1). Face values f(4) = 0.917,
  f(5) = 0.875, f(6) = 0.850, f(inf) = 0.750: inside NUN4's full
  three-part bar with margin (registered arithmetic, FND-104).
- G-B (STATISTIC MIXTURE): note that v_eff = (1 + w)/(N-1) with the
  G-A weight equals 1.5/(N-1) + 1/(N-1)^2, whose leading term is the
  ARITHMETIC MEAN of the two statistics AYIN already computed
  (A free-label: one channel; B exclusion: two channels). A granted
  rule making bundle exchange an equal mixture of free and exclusion
  currency, plus an exclusion echo at second order, writes the same
  point. Same face values; different ontology; the echo term is the
  part G-B must still supply.

Both shapes carry the identical acceptance test, pre-built and
unchanged: NUN4's three-part bar (both FND-102 numbers plus the
safe-volume profile) with derivation-or-demotion armed. Whichever is
granted, if either is, the confrontation is one short session and
its bands are already public.

## Standing

The window point remains uninhabited. The decoherence family remains
alive (FND-104's lemma). The grant decision is the author's; the
corpus's own next honest move in this sector, absent a grant, is
the chromo-profile acquisition (FND-103 C2) or rest.
