# THE kappa_conv NORMALIZATION SESSION (NUN-GRV17) -- BARS LOCKED (2026-08-16)

Locked BEFORE any computation. GRV-123's named next-order (1): price
the operator normalization mapping the granted L_C3 coefficient
lambda into GRV-120's dipole slot C_d. kappa_conv = C_d/lambda.

## REGISTERED FIXED POINTS (B1)
- lambda's UNITS are registered: lambda = chi (G_shear I_p)|tau|
  (GRV-112) = Pa.m^4 x 1/m = N.m = JOULES. (GRV-113's own refusal
  "no absolute lambda in joules quoted" confirms the unit.) This
  ADJUDICATES the slot question GRV-123 carried: slot A
  (per-strand, joules) is THE slot; slot B (modulus, J/m^2) dies.
- C_d's units from GRV-120's registered forms: ratio =
  Lambda J sin^2(theta)/(Mc) with Lambda dimensionless forces
  C_d = 2 Lambda C_m/c^3, units s/kg.
- Therefore kappa_conv = C_d/lambda has units s^3/(kg^2 m^2), FIXED.
- GRV-073's registered pair: per-strand C = 4.21e-36 J.m and
  continuum gamma_grav = 4.21e-4 J/m. Their ratio C/gamma_grav has
  units m^2 and DEFINES the conversion area: a^2 = C/gamma_grav.
  Consistency against the registered mesh spacing to be CHECKED,
  not assumed.
- T0 recoverable from GRV-073's own chain: k = E pi r^2, k = 2 T0.

## PRE-COMMITTED OUTCOMES (B2, exhaustive)
- FULLY-PRICED: kappa_conv lands as a unique registered closure;
  Lambda_nat becomes a number. (Reachable only if the shift
  sector's kinetic normalization is registered with a coefficient.)
- SLOT-ADJUDICATED + BLOCKED-AT-SHIFT-NORMALIZATION: the units
  adjudicate the slot; the dimensional decomposition reduces
  kappa_conv to registered factors times the shift kinetic
  coefficient K_shift; admissible closures are ENUMERATED with
  their Lambda_nat consequences EXHIBITED (display volume), and the
  spread is reported honestly. No number quoted as final.
- INCONSISTENCY-FOUND: the registered fixed points contradict;
  register the contradiction.

## CONSEQUENCE LADDER, pre-committed so no closure can be spun (B3)
For each admissible closure, exhibit what it implies for the
GRV-122 attribution (chi from beta ~ 1.6e-19):
- If Lambda_nat(closure) x 1.6e-19 >> 1: attribution OVERSHOOTS
  Lense-Thirring -- birefringence-chi CANNOT be the frame-dragging
  chi under that closure.
- If ~ 1: the attribution is CONSISTENT -- birefringence IS frame
  dragging under that closure (the GRV-123 flagged-landing class).
- If << 1: dragging undershoots -- tension with LARES-class data.
The ladder is exhibited per closure; NO closure is chosen; NO
observational verdict (condition 4 unchanged).

## REFUSALS (B4)
- No closure adopted without the registered shift kinetic term; the
  deciding object is NAMED, not guessed.
- The a^2 = C/gamma_grav consistency check may CONFIRM the
  registered spacing; any resemblance beyond confirmation is
  flagged per the standing rule.
- Level-assignment residue (GRV-123) carried, unresolved.
- No LARES numbers; no kill language; riders (k/T0 = 2 etc.) on
  every derived figure.

## MACHINE CONTENT (B5)
benchmarks/gravity/kappaconv_nungrv17.py: unit algebra (sympy
symbols kg/m/s), the slot adjudication, the a^2 extraction and
spacing check, T0/mu recovery, two admissible closures priced with
the consequence ladder, spread reported.
