# COMMISSION RESH -- THE T2 DISCRIMINATION PUSH (bars LOCKED)

Locked 2026-08-12, before computing. Tier T2 per
docs/STRATEGIC_TARGETS.md: presentation and confrontation, no new
derivation. Two halves, each with pre-committed grammar.

## HALF 1 -- adjudication prep on the FND-063 conditionality
## (the pin's only blocker from FIRM status)

Three questions, each answered ONLY from the registered record plus
symbolic verification of already-registered formulas (no new physics):
Q1 Does FND-040's premise chain (FND-017 tension as global Lagrange
   multiplier; FND-KIN-001 inextensibility; the B4 competition search
   returning no Hookean term) contain K_c at any point? (Answer by
   verbatim reading of the FND-040 chain; machine check that the
   softening coefficient -eps/2 follows from the T0 sqrt(1+g'^2)
   expansion with no K_c symbol anywhere.)
Q2 Does BET4's stability indictment reach the mesh regime? (Answer by
   verbatim quotation of BET4 B3's own scope sentence; no extension
   in either direction.)
Q3 In BET3's registered functional c4_eff = [k(K_c + f k)/(K_c + k)
   - T0]/8, verify symbolically: (a) the f->0, K_c->0 corner is
   -T0/8 (registered); (b) dc4_uni/dK_c = 0 at f = 1 (BET4's check,
   re-verified); (c) at f->0 with K_c finite the coefficient RETAINS
   K_c -- so the corner statement is true and the question is
   GEOMETRIC (is the contact channel present on core-free mesh
   strands?), which is an author's reading, not a computation.
VERDICT GRAMMAR (pre-committed): if Q1 = no-K_c-in-chain, Q2 =
confined-to-localised, Q3 = as stated, the memo goes to the desk as
DISCHARGEABLE-BY-READING: the author may rule that the contact
channel is absent from core-free mesh strands (a reading of
registered geometry, reserved to the author), which discharges the
FND-063 conditionality clause and converts Prediction 20's pin to
FIRM. If any answer differs, the memo reports NOT-DISCHARGEABLE and
the conditional status stands. THIS COMMISSION DOES NOT RULE; it
prepares.

## HALF 2 -- the k-string decision bands, pre-registered numerically

Inputs, all registered (AYIN magnitude table; FND-040 floors; the
softening form delta_k - delta_1 = -(eps/2)(C_k/C_1 - 1),
eps = 1/kappa_pack at floors 50 and 250; FND-047's ~2 percent error
characterization of the standing record):
- Committed ratio: sigma_k/sigma_1 = (C_k/C_1)(1 - (eps/2)(C_k/C_1 - 1)),
  C_k/C_1 = k(N-k)/(N-1).
- Compute at SU(6) k=2 and k=3, both floors; sine comparators
  sin(pi k/N)/sin(pi/N).
BANDS (locked): for a future continuum SU(6) determination with
central value x and quoted error e --
- SELECTS-ROPE: x inside the softened Casimir band (floor-250 value
  down to floor-50 value, edges +-1e) AND at least 3e below sine.
- KILLS: x at least 3e above the band's upper edge (the corpus's
  pre-refused retreat makes this a corpus-level kill channel per
  FND-050/055's own words).
- UNDECIDED: otherwise.
The bands are registered NOW, before any such determination exists;
no post-hoc adjustment when data arrives.

## Out of scope (locked)
No re-grade of FND-040 (author's act only); no touch of n_b, g_C4,
or multiplicity currency; no paper edit this session (the paper sync
inherits any desk ruling at the next cut).
