# GRV-075 bars — LOCKED before computation (2026-08-02)

Commission (GRV-074's decider): the band coefficient in physical units -- evaluate
GRV-021's induced stiffness on FND-STRAND-008's registered band with dimensional
bookkeeping, deriving the Sakharov row's exponent pair and its scale selection.

The bookkeeping, fixed in advance:
- The band (FND-STRAND-008, measured): omega^2 = omega_0^2 (1 + 4 kt sin^2(pi k/M)),
  gap = the on-site scale, kt = J_grad/J_on the dimensionless stiffness ratio.
- PHYSICAL UNITS: torsion waves are light, so the long-wave speed is c:
  c = a omega_0 sqrt(kt) => omega_0 = c/(a sqrt(kt)). Under the LOCK enslavement
  every modulus is slaved to (T, a) (J_grad = Ta/2 exactly; J_on the per-site
  locking, also ~ Ta), so kt IS A PURE NUMBER and the band's SHAPE carries no
  residual modulus: the coefficient zeta below is pure.
- The induced 1D coefficient from GRV-021's own machinery: K_1D = (chi_2/sqrt(kt))
  hbar c; the 3D per-volume version K_3D = zeta hbar c/a^2, zeta = chi_2/sqrt(kt)
  times a metric-dictionary factor of order one (premise P3: the kt-modulation to
  metric-h conversion is O(1) and pure; GRV-025's tensor instrument would supply
  it exactly and is not rerun here).

Premises, stated as premises:
- P1: the Sakharov identification c^4/(16 pi G) = K_3D (GRV-021's funded
  conjecture; Modeled).
- P2: omega_0 = c/(a sqrt(kt)) (torsion-wave speed anchors the band; registered).
- P3: the dictionary factor is O(1) and pure (untraced tensor normalization).

Rules fixed in advance:
- R1: GRV-021's chi_2 is RECOMPUTED from its registered benchmark machinery, not
  quoted; the per-site zero-point values must reproduce 0.662/0.739/0.839.
- R2: the scale selection a_Sak = sqrt(16 pi zeta hbar G/c^3) is computed and
  confronted with the corpus's a-constraints HONESTLY: the Lorentz bound is an
  UPPER bound (a <= 1e-16 m), so a Planck-class a_Sak is CONSISTENT with it --
  and GRV-074's B3 framing, which computed the 7.6e35 gap AT a = 1e-16 as if a
  were pinned there, must be corrected on the record.
- R3: every cross-sector consequence of a_Sak is swept and reported, favourable
  or not -- explicitly including PRED-003-ETA's flagged J ~ 0.4-0.5 MeV
  observation (filed at a = the Lorentz bound; it dissolves at a_Sak) and the
  a-independent invariants (l_q physical = sqrt(4 pi alpha hbar c/T), which does
  not move).
- R4: no tier motion; the exponent pair is DERIVED-CONDITIONAL (P1-P3), the
  first derived G exponent pair in the corpus, and said exactly that way.

Bars:
- B1: band bookkeeping verified (omega_0, kt purity under enslavement).
- B2: chi_2 recomputed per R1; zeta = chi_2/sqrt(kt) stated with P3's O(1) flag.
- B3: exponents: G = c^3 a^2/(16 pi zeta hbar) -> (p_T, p_a) = (0, 2), derived
  conditional on P1-P3; the hbar-medium branch re-checked (LOCK co-drift cancels
  T as in GRV-074).
- B4: the scale selection a_Sak computed; GRV-074's B3 corrected; consistency
  with the Lorentz UPPER bound adjudicated.
- B5: the cross-sector sweep per R3: what moves under a_Sak (lattice-unit
  quantities: ETA's g bound, the J observation) and what does not (physical
  invariants: l_q, A_hbar in metres, PRED-003's channel ratios and J1713
  robustness).
- B6: verdict and the named decider for P1 itself (an independent a
  determination, or the tensor coefficient in physical units).
