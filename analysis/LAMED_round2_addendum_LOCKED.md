# COMMISSION LAMED -- ROUND 2 ADDENDUM (LOCKED BEFORE ROUND 2 EVALUATION)

*Locked 2026-08-11, after round 1 ran and before any round-2 residual is
inspected. Round 1 is registered AS RUN; nothing in it is rescinded.*

## Why a round 2 exists (the auditable reason)

Round 1's locked baseline was the NUC-018 corrected-BOTH variant. Its
residual (rms 110 MeV, 8.1 percent of binding, dominated by a smooth
A^(1/3) term) is a diagnosis of that baseline, not the few-MeV structure
the queue chartered -- and NUC-018's OWN registered table already ranked
the variants: original +2.9 percent, corrected-spacing-only +1.5 percent
(the best), corrected-both degraded. The round-1 bar committed to a chain
the registry itself grades as worse; the correction is made by the
registry's ranking, not by peeking at outcomes. The stale-value tripwire
(HANDOFF section 7) caught the baseline one commission late; logged.

## Round 2 baseline (fixed): the NUC-018 registered-best variant

d0 = 2.026 fm (NUC-017), a_S/a_V = 1.108 (NUC-005 facet value, per
NUC-018's best row), a_C = 0.772 (derived at the corrected spacing),
a_V calibrated once on Ca-40, a_A = 19.85 (NUC-A/B derived). Pairing
still excluded from the baseline (NUC-024 scaling mismatch), so D2
remains a descriptor.

## Everything else UNCHANGED from the round-1 lock

Same closed descriptor list D1-D6 (no transformation, no addition), same
D7 UNDERSPECIFIED declaration, same seed-3141 50/50 split protocol, same
univariate (>= 0.5) and joint (>= 0.6, swap, p < 0.01) bars, same verdict
grammar. The descriptor list predates both rounds' residuals; the only
change is the baseline, and its selection cites NUC-018's table alone.
