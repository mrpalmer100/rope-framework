# GRV-083 bars — LOCKED before computation (2026-08-02)

Commission (GRV-082's L3, in chain order): derive P-TH -- that the breaking
threshold is set by the pressing -- from crossing statics on GRV-077's machinery.

The route, recorded before computing:
- A crossing is two HARD-CORE tensioned rods (the strands ARE rods: GRV-073 via
  GRV-009's r^4 primitives; thickness registered at HBAR-005) pressed together
  by the normal load N -- which near a horizon is the derived static load share
  (GRV-077's transverse-only theorem delivered per crossing by GRV-038's
  Rindler-class profile): N(sigma) ~ K c^2/sigma in proper distance.
- THE LIFT-OVER OBSERVATION, the session's spine: to reconnect, the over-strand
  must pass the under-strand, and for hard-core rods that requires LIFTING by
  the core height h ~ the strand thickness, against the pressing N. The barrier
  is therefore W = N h -- pressing times thickness, LINEAR in the pressing,
  with NO friction coefficient imported: the corrugation is the strand's own
  cross-section. (A Coulomb-friction reading would have added an O(1); the
  hard-core geometry does not even need it.)
- COMPOSITION: W(sigma) = N(sigma) h ~ a_proper. With GRV-082's measured
  e_bit ~ barrier (linear, three digits), the reservoir temperature chain loses
  premise P-TH and keeps only P-ENT.

Premises, stated:
- P-GEO: hard-core crossed rods with a definite core height h (corpus-native:
  rods per GRV-073, thickness per HBAR-005); h is the geometric O(1) times the
  thickness and is NOT evaluated numerically tonight (the profile needs only
  linearity in N, which h's constancy delivers).
- Carried: P-ENT (the two-state temperature reading, GRV-082).

Rules fixed in advance:
- R1: the work theorem is proven by machine for a GENERAL monotone lift profile
  z(x) from 0 to h under constant normal load: W = N h independent of the
  path's shape -- so the barrier's linearity in N is profile-independent, and
  no corrugation model can change it.
- R2: the numeric leg verifies the theorem by quadrature on two dissimilar lift
  profiles (agreement to 1e-10) and reports the threshold FORCE (max slope
  times N) as profile-dependent -- the honest split: the BARRIER is universal,
  the threshold force is not, and the temperature chain uses the barrier.
- R3: no flux/spectrum claims (carried from GRV-082); no evaluation of h or K.

Bars:
- B1: the lift-over theorem per R1 (sympy, general profile).
- B2: the quadrature check per R2, two profiles.
- B3: the composition W(sigma) = N(sigma) h ~ a_proper with each link's
  provenance printed; P-TH DISCHARGED-GIVEN-P-GEO; the whisper chain's premise
  ledger reduced from two named premises to one (P-ENT) plus corpus-native
  geometry.
- B4: propagation and next-orders (P-ENT quantitative; then the flux).
