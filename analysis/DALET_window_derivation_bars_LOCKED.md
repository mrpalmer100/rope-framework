# Commission DALET -- derive or retire the 1-100 window: locked bars

*Locked 2026-08-11, before any sweep or computation. Rank-1 next brick
from FND-041. The window is currently GRV-093-arc pre-drafted grammar
("order 1-100 reconciles the two action areas as cell-scale geometry"),
never derived; FND-041's kappa_pack <= 157 cap inherits its grade and
stands or falls with it.*

## The question

Which edges of the l_q/a window, if any, are FORCED by registered
claims? Each edge is adjudicated separately.

## Pre-named candidate classes (the sweep may not invent others)

- C1 (lower edge): the registered mesh cutoff k <= 1/a (MATTER051/053's
  committed instrument; the homogenization validity domain). If the
  quantum length must be an admissible collective length of the medium,
  the cutoff forces l_q >= O(1) x a. The O(1) must be stated with its
  convention (wavelength vs length) and not tuned.
- C2 (upper edge): the action-area architecture (GRV-093): any
  registered requirement that the quantum area engage cell-scale
  structure. NOTE HONESTLY: MATTER053 killed the cell-mode picture
  (empty band), so any C2 argument that routes through internal knot
  modes is dead on arrival and must be declared so.
- C3 (upper edge): n_q as a physical fraction. If any registered claim
  requires the snap fraction n_q <= 1 (or >= some floor), evaluate what
  it bounds. Direction must be computed, not assumed.
- C4 (either edge): the coherence-cell / knot-scale structures
  (MATTER051's "knot is one cell", MATTER052's candidate areas) --
  only as REGISTERED constraints, not as the withdrawn grant.
- C5: anything found by a literal registry sweep for "l_q" bounds not
  in C1-C4 is reported but graded as FOUND-IN-SWEEP and adjudicated
  under the same no-invention standard.

## Verdict grammar (pre-committed)

- WINDOW DERIVED: both edges forced by registered claims; the window
  re-registers at its DERIVED edges (which need not be 1 and 100), the
  FND-041 cap recomputes at those edges and PROMOTES to the weakest
  grade in its derivation chain.
- HALF-WINDOW: only the lower edge is forced. The upper edge retires as
  grammar; the kappa_pack <= 157 cap is WITHDRAWN the same session at
  full volume (FND-041 corrected with a bracketed pointer); the
  continuum reading is un-excluded; l_q/a = 108 ceases to be a strain.
- RETIRED: neither edge is forced; the window retires entirely; same
  cap withdrawal.

## Honesty clauses

1. No rescue by re-choosing conventions: cutoff, boundary conditions,
   and the R1 form stay exactly as registered.
2. The 3-percent l_q coincidence flagged by FND-041 (the 5% floor
   reproducing the registered femtometre) may be REPORTED where the
   sweep touches it but may not be USED as a bound -- it is a
   coincidence until derived.
3. Killing FND-041's cap is a success condition of this commission,
   not a failure: the cap was registered at window-grade precisely so
   this session could adjudicate it.
4. One sweep, one evaluation per candidate; numbers as computed.
