# COMMISSION RESH -- SCALE-001 C4 RE-OPENED: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number is computed. This is the
dependency-path protocol's intended use case (FND-044/FND-051 protocol
rule): the mechanism (one-crossing reconnection transfer) was derived for
the NUCLEAR PAIRING chain under seal (FND-071/072), with no reference to
the mesoscopic scale; SCALE-001's C4 law form (lambda_mfp = 1/(n_x
sigma_x)) was locked on 2026-08-11 BEFORE p existed. RESH services the
locked law with the now-registered carrier. No part of the law or the
carrier may be modified in this commission.

## Blind-integrity statement, at full volume (SCALE-001 precedent)

The sealed target g = l_q/a in [82.6, 108.0] is disclosed in the session
context (HANDOFF, FND-044). As in SCALE-001, the protocol's value is
that NOTHING CAN BE TUNED: every input below is a registered number or
an exact registered form, and the enumeration is closed at lock. Rough
order-of-magnitude scoping (tens to hundreds) occurred before lock and
is disclosed here as a guard; the verdict grammar below was written
before the benchmark.

## Inputs, enumerated and CLOSED AT LOCK

- p(g, ka) = g^2/(2(g^2 + 4(ka)^2)), exact (FND-072). Not modifiable.
- sigma_x = p a^2 (FND-071's locked conversion). Not modifiable.
- CROSSING DENSITY, two registered readings, BOTH evaluated:
  D1 LINEAR-ENCOUNTER: EM-RECON-017's registered crossing density 2/a
     along a strand; 1/lambda = (2/a) p, so g_C4 = lambda/a = 1/(2p).
  D2 VOLUME FORM: n_x = (2/a) x (3/a^2)/2 = 3/a^3 (three-family O_h
     weave, length density 3/a^2, pair double-count halved -- the
     one-line geometric consequence derived in the benchmark, shown not
     assumed); g_C4 = 1/(3p).
  No third reading may be introduced after lock. If D1 and D2 disagree
  on the verdict, the verdict is the weaker one (robustness rule).
- CONTRAST INPUT: g_contrast in [0.395, 0.460], the survival band under
  FND-073's C3 per-pair identification. CONDITIONALITY NAMED AND
  CARRIED: this identification is registered but not prosecuted; every
  verdict below wears the prefix CONDITIONAL. The demanded family
  [0.082, 0.265] x ka is REFUSED as an input: it descends from v0
  through the same p-form and would make the evaluation circular.
  The G3 floor (g >= O(1e-2)) is one-sided and admits no evaluation.
- ENCOUNTER SCALE: QOPH's locked family ka in [1, pi], evaluated at the
  three locked route points (1, 1.594, pi) and swept continuously for
  display. No route selected by outcome (QOPH's own rule inherited).

## Verdict grammar, pre-committed

- CONDITIONAL-LANDS: at some single locked route point, BOTH density
  readings land inside the sealed range. (Landing at one reading only
  is not a landing -- prefactor dependence is the exact failure mode
  the robustness rule exists for.)
- CONDITIONAL-CONTAINS: no robust landing, but the sealed range lies
  inside the swept family's span. MANDATORY companion: the
  look-elsewhere containment rate under SCALE-001's committed
  log-uniform 10^0..10^4 prior, printed next to the verdict, and the
  INVERSION -- the ka each density reading demands for an exact
  landing -- registered as a demand on the arriving-spectrum
  derivation (QOPH's named acquisition), sealed-target style.
- CONDITIONAL-MISS: every cell falls on one side of the sealed range
  beyond the L1 factor-3 band. SCALE-001's C4 registration then
  converts from UNDERSPECIFIED to a fourth evaluated miss, and the
  FND-044 residual hardens. Pre-authorized without rescue.

## What RESH may NOT do

Modify the law; average the density readings; select a ka; treat the
survival band as a determination of g_contrast; open, move, or restate
the sealed target's provenance; touch the one-medium question.

## Deliverables

benchmarks/foundations/resh_c4_reopening.py;
analysis/RESH_c4_reopening_results.md; claim via tools/add_claim.py;
annotations to FND-051 (C4 ledger), FND-044, FND-072, FND-074.
