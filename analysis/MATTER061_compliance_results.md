# FND-MATTER-061 — the compliance correction is not a factor, it is a fork: the exclusion count has no registered foundation, and the lever's grade becomes conditional

Date: 2026-08-09. Commission: 060's named next brick (the compliance
correction, computed rather than gestured at).
Benchmark: `benchmarks/foundations/matter061_compliance_adjudication.py`.

## Bars (locked before computing)

1. The adverse outcome permitted and pre-named: a foundation failure is
   registered against 056/059/060, whose grades become conditional. The
   1.44× landing buys no protection.
2. Exact method: finite-string Dirichlet mode sums under BOTH sharp and
   smooth regulators; only regulator-stable contributions carry weight.
3. Registered inputs only: inextensible continuous strands, finite smooth
   contact, no crossing axiom, no hard wall (FND-STRAND-001; FND-MATTER-004
   as corrected).
4. 060's falsifier binds; a parametric collapse (all knots down together)
   is a permitted outcome.
5. Nothing re-fitted; spend stays at ONE.

## The three computations

**(1) The point-constraint cost is a regulator artifact.** Pinning an
interior point of a string costs +0.785 ħcΛ under a sharp cutoff at every
length — and tends to zero (as −π/8L) under the smooth regulator. A
contribution that changes class between schemes is unphysical: constraining
a continuous strand at a point removes NO regulator-stable zero-point
content.

**(2) Only actual length removal removes content.** The extensive term is
scheme-stable in class: ΔE/Δℓ converges to the scheme's own ZP density
(−Λ²/2π, verified to 0.1%), and the density prefactor cancels in the ratio
λ = displaced/total. The exclusion count is well-defined if and only if
mode-carrying length is genuinely removed.

**(3) The registered strand model removes no length.** Strands are
inextensible, continuous, finitely and smoothly contact-coupled, with no
crossing axiom and no termination. A strand meeting a thin inclusion
reroutes; the exact detour length vanishes as (r−b)²/X with anchor distance
X. Under the corpus's own registered physics, a thin inclusion deletes no
length and pins no stable point cost. **The dilute-exclusion count has no
registered mechanism underneath it.**

## The fork, registered as the outcome

- **(A)** If ambient strands are genuinely ABSENT from the knot's volume as
  a routing fact (fixed routing: the weave configuration never ran strands
  through where the knot sits), the 056/059 count stands on that fact and
  λ = 6π(r/a)² survives with the 1.44× overshoot open. The corpus currently
  registers no such fact.
- **(B)** If routing is dynamic (the natural reading of FND-STRAND-001's
  relaxational dynamics), the exclusion count is unfounded, the correction
  is a parametric collapse rather than a modest factor, and the 1.44×
  landing is coincidence-class. The mechanism dies in detail — consistent
  with 060's falsifier (all knots move down together).

## Consequences for standing claims

FND-MATTER-059's promotion and FND-MATTER-060's propagation are flagged
**CONDITIONAL on fork A**. The Crofton theorem itself is untouched (it is
mathematics); what is conditional is whether the excluded-length count it
computes corresponds to anything the weave actually does. The restated ZPE
band (1.84×) reverts to the pre-derivation width if fork B resolves.

## The settler, named precisely

A strand-engine computation, decidable with existing machinery
(FND-STRAND-001's relaxation dynamics): relax an ambient weave around a
fixed knot to steady state and measure the strand length density inside the
knot's volume. Density ≈ ambient → fork B (collapse). Density deficit ≈ the
excluded volume → fork A (the count stands as a routing fact). Blind bars
would commit the density-deficit threshold before running. Not run here.

## Not claimed

Any change to λ's value; fork adjudication; any strand-engine result; any
new parameter. Spend remains ONE.
