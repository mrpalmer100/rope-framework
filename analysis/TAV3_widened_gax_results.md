# COMMISSION TAV3 -- THE WIDENED G-AX SWEEP: RESULTS

Executed 2026-08-13 under analysis/TAV3_widened_gax_bars_LOCKED.md.
Benchmark: benchmarks/foundations/tav3_widened_gax_sweep.py. Sweep,
settling criteria, and verdict corners exactly as locked; no
extension, no re-binning.

## Verdict (locked grammar): CORNER 2 AT BOTH PACKINGS --
## FULL-SECTION CONFIRMED, CURRENCY CONVICTED, THE PIN RE-POINTS

Both numbers SETTLED at both packings, so the decider decided:

- p_inf = 1.053 (hex) / 1.046 (f_c), local-exponent spreads 0.009 /
  0.008 against the 0.05 bar, top-half stability 0.017 / 0.014
  against 0.10. TAV2's downward drift relaxed exactly as its
  monotonicity suggested: axial contact IS full-section. Chain A's
  GEOMETRY premise is now MEASURED, not commentary.
- r_inf = m_eff/n = 4.87 (hex) / 3.58 (f_c), last-three relative
  spreads 1.6 / 1.3 percent against the 10 percent bar. TAV2's
  displayed ~3-4 was real and survives settling. Chain A's CURRENCY
  step (m_b = n, one pair per constituent) is CONVICTED at engine
  level: each facing constituent engages ~3.6-4.9 opposite ends.

## The measured inversion (the twist)

The pin m_b = [63.0, 73.4] inverted through the MEASURED currency:

- hex: n = [12.9, 15.1]
- f_c: n = [17.6, 20.5]

This MISSES the (kappa250, hex) window [40, 81] that Chain A
selected -- the grant's n_b = 63-73 purchase is convicted in its
inversion step and RETURNS TO ADJUDICATION under FND-080's armed
return clause -- but it does NOT miss every registered window: both
packings' inversions intersect the intermediate hierarchy window
[14, 27] ((kappa50, hex) / (kappa250, f_c)), the f_c inversion
sitting entirely inside it. The locked Corner 2 clause conditioned
tension-confirmation on missing EVERY window; that condition did not
obtain. What obtains instead is a RE-POINTING: at-threshold plus
full-section plus measured currency selects n_b ~ 13-21, a different
cell of the same registered hierarchy.

DISPLAYED AND NOT ADOPTED (the resemblance rule, applied hard): the
intersection of the measured inversion with [14, 27] is arithmetic.
Adopting it as the bundle count would swap Chain A's cell selection
under the same at-threshold premise, and that is a grant-scale
decision belonging to the author, not to this commission. It goes to
the desk as CHAIN A-PRIME: same premises as Chain A with the
inversion corrected by measurement, selecting n_b = [14, 21]
(envelope of both packings' overlap with the window) at the
(kappa50, hex) or (kappa250, f_c) cell -- note the selected cell is
no longer unique, so the "triple discrimination in one stroke" that
made Chain A attractive does NOT carry over.

## Honest edges (named, none moving the verdict)

- r(n) is still rising slowly at n = 547 (finite-size approach to
  the kernel's areal limit); it passes the locked settling criterion
  with margin, but r_inf is a floor-ish read and the true asymptote
  may sit a few percent higher. Direction: pushes the inversion
  LOWER, deeper into or below [14, 27]; cannot rescue [40, 81].
- r_inf differs between packings (4.87 vs 3.58) because the gap
  differs; the confrontation cell (hex) is the tighter one, per bars.
- The FND-STRAND-004 caveat carries: engine units, Modeled, contact
  MODEL scaling class and currency, not a physical n_b measurement.

## Ledger consequences

- FND-080 (GRANT-THRESHOLD): the g fix and the m_b = [63.0, 73.4]
  pin are UNTOUCHED (pair currency, pair machinery, unchanged by
  this result). The Chain A n_b = 63-73 purchase at (kappa250, hex)
  is RETURNED TO ADJUDICATION per the grant's own clause. The grant
  itself is NOT retracted; its return clause named independent n_b
  determinations and this is an engine-level model result, but the
  purchase can no longer be quoted as standing.
- FND-094's pre-commitment: honored exactly; the survival corner
  (p -> 1, r -> ~1) did not obtain, r landed at 3.6-4.9.
- TAV2 (FND-082): the axial UNSTABLE resolved (p -> 1.05 settled);
  its lateral exclusion untouched; its tension clause CONSUMED by
  this measurement (superseded by the settled inversion).
- STALE-STATEMENT TRIPWIRE (new): do not quote n_b = 63-73 or the
  (kappa250, hex) selection as a standing grant purchase; the
  standing statement is the pin m_b = [63.0, 73.4] plus the measured
  inversion n ~ 13-21 (displayed, on the desk as Chain A-prime).

## On the author's desk

ADJUDICATION OF CHAIN A (owed by FND-080's return clause): retract
the n_b = 63-73 selection outright, or replace it with Chain A-prime
(n_b = [14, 21], cell no longer unique), or hold the n_b question
open pending a non-engine determination. The author's call, per the
standing rule; nothing adopted today.
