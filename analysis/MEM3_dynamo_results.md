# COMMISSION DYNAMO (MEM3) -- RESULTS (2026-08-16)

Bars: analysis/MEM3_dynamo_bars_LOCKED.md. Benchmark:
benchmarks/em/dynamo_mem3.py (sympy-exact; one prose-layer catch
corrected to the machine mid-session, logged on the face).

VERDICT: LOOP-CLOSES-STRUCTURALLY (with the Earth/Sun split of the
plasma flag), PLUS a null STRONGER than the one the bars demanded.

## THE LOOP, link by link, all registered
L1 (circulation -> drive): E = rho kappa_0 (v x zhat) exerts the
   q-linear force on windings in a moving conductor (EM-RECON-026;
   TH1 on face). MACHINE: a horizontal (poloidal) flow component
   gives curl_z(E) = rho kappa_0 u_c / s != 0 -- a genuine
   closed-loop EMF.
L2 (drive -> transport/spin): the screw relation and this week's
   arc -- Gamma_inj = lambda gamma tau0 E0, persistence protected at
   sin^2(chi_d) in [0.043, 0.055] leak fraction (reference).
L3 (transport -> circulation): a winding sources azimuthal mesh
   flow v_theta = q kappa_0/(2 pi r) (EM-RECON-026 confrontation a);
   B is curl-of-pitch (Gaede reconciliation, registered).
L4 (feedback sign): the jump-rope gap rule (derived, force sign
   benchmark) makes co-oriented transport loops reinforcing;
   sign propagation around the full cycle is (+)(+)(+)(+) = +.

## THE STRONGER NULL (registered as a result, per B3)
NO PURELY TOROIDAL FLOW DRIVES THE LOOP: for v = Omega(s) s theta-hat
with ANY rotation profile -- rigid or sheared -- the induced E field
is exactly radial and curl-free; the closed-loop EMF is ZERO. The
bars anticipated only the rigid null; the machine returned the
general one (a draft print claiming shear suffices was corrected to
the machine, the day's second prose-layer catch). THE CONSEQUENCE:
in this framework a body magnetizes only if it CONVECTS -- rotation
organizes the field but cannot create it. Displayed (not registered
as confrontation): the resonance with the solar system's pattern --
convecting cores field, dead cores none.

## THE SELF-EXCITATION CRITERION (the rope magnetic-Reynolds analogue)
    R_rope = rho kappa_0^2 u_c / (2 pi L n_x g sin^2(chi_d) C26) > 1
Gain is convective transport of circulation; loss is the registered
spin leak (GRV-119's eta_chain machinery, EM-RECON-040's bracket).
GATES, all named prior gates plus flow inputs: kappa_0 (via SIGMA,
registered bound <= ~26-50 SI), u_c, L, n_x, g, C26, beta. No new
unknowns invented; the criterion is assembled entirely from
registered objects.

## THE ANSWER TO THE AUTHOR'S QUESTION
A planetary field, in this corpus, is the mesh's standing
circulation pattern maintained by convecting transported winding:
convection supplies the closed-loop EMF (rotation alone provably
cannot), the EMF spins wound strands (current-as-spin), the spun
windings sweep the mesh (Gaede), the sweep reinforces co-rotating
loops (jump-rope sign), and rotation plus GRV-020's no-monopole
lemma organizes the sum into a dipole-led global field. Remanent
fields (magnetized crust) are the registered permanent-magnet
account: aligned atomic spins sweeping the network, field without
transport current.

## SCOPE SPLIT
EARTH-CLASS (bound conduction in liquid metal): unconditional on
the plasma question. SUN-CLASS (plasma): conditional -- whether
unbound windings sweep the mesh identically is UNEXAMINED and
flagged, not resolved.

## REFUSALS HONORED
No field magnitudes; no geodynamo data confrontation (the solar-
system resonance displayed only); gravitomagnetic grants untouched;
frame dragging not pre-computed -- its charter is cut separately
on this verdict.
