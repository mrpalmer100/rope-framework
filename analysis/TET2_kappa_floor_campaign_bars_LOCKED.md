# COMMISSION TET2 -- BARS, LOCKED BEFORE READING

*2026-08-13. Candidate claim FND-105. The kappa_pack floor-from-data
campaign: hunt the literature for a primary admissible bound tighter
than Bali 2000's 5 percent and run TET's registered machinery on it.
Author-directed. Bars written BLIND to what the literature holds; a
null is a registerable outcome and will not be shaded.*

## Inherited machinery (reused verbatim, not re-derived)

- Inversion: kappa_pack >= (C_A/C_F - 1)/(2b) = 0.625/b for an
  admissible SU(3) Casimir-scaling deviation bound |delta| <= b
  (TET's floor_from_bound; b = 5% -> 12.5; 1.25% -> 50;
  0.25% -> 250).
- Admissibility A1-A4 (TET bars): SU(3); static potentials in the
  confining, unscreened window (adjoint: below string breaking,
  r <~ 1.2 fm; long-distance emphasis r >= ~0.5 fm); short-distance
  data T3-firewalled out; PRIMARY sources only (a stated bound or
  quoted deviations with errors in the primary paper -- secondary
  characterizations displayed, never adopted: the A3 rule that kept
  the ~1 percent Shevchenko-Simonov reading out of the pin).
- Decision table (TET, pre-committed): b <= 1.25% decides/excludes
  the kappa = 50 CS-floor reading; b <= 0.25% reaches the continuum
  reading.

## Session-specific rules

- R1: eligible observables: adjoint (or higher-rep) static potential
  vs fundamental on SU(3) lattices, continuum-extrapolated or fine
  lattices with the paper's own systematics statement; deviation
  defined as in TET (relative CS deviation of the potential/tension
  ratio).
- R2: if a paper quotes per-representation ratios with errors rather
  than a bound, the adopted b is the LARGEST of (central deviation +
  1 quoted error) across admissible representations and distances in
  the window: the conservative envelope, fixed here before any
  number is read.
- R3: k-string data is NOT admissible for this pin (bundles measure
  binding, FND-048); the 2+1d literature is inadmissible (wrong
  dimension); G2/SU(2) inadmissible (wrong group) but displayable.
- R4: at most one floor is adopted, from the single best admissible
  primary source; others displayed.

## Pre-committed verdict grammar

- Admissible primary b < 5%: PIN TIGHTENED to 0.625/b. Additionally:
  b <= 1.25% -> the 5%-CS floor (kappa >= 50) moves from Conjecture
  support to DATA-DECIDED per the TET table (in whichever direction
  the sign falls; a resolved POSITIVE deviation beyond the CHET
  forward predictions is a hit on the tower and is registered as
  such, not absorbed).
- No admissible primary source beats 5%: NULL; the pin stays 12.5;
  the decision table stands; the shopping-list entry is re-priced
  with whatever the hunt learned about feasibility.
- Forward check carried: CHET's predictions (-1.25% at kappa=50,
  -0.25% at 250) must remain INSIDE any adopted bound; an adopted
  bound excluding both would be a corpus hit, registered at full
  volume.

## House

Bars locked before reading. Em dashes forbidden. Stale tripwires
honored. No bar motion after numbers are seen.
