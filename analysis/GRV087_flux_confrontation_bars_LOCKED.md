# GRV-087 bars — LOCKED before computing (2026-08-02)

Commission (the campaign's endgame): confront the whisper lineage's committed
structure (GRV-040/047/049/052/053) with the mechanism built from below
(GRV-081..086). The absolute wattage is out of reach tonight (K, h, n_x, nu
unevaluated, per standing rules); the confrontation is on FORM and CLASS, and
the bars say which forms must match.

The two committed targets:
- T1: the frequency law -- the lineage committed omega_inf = 0.23 kappa (kappa
  the surface gravity; 186 Hz at 10 solar masses).
- T2: the luminosity class -- GRV-047 revised the law to a SWITCH (the horizon
  emits at its own kinetics-set rate, not supply-limited), and GRV-053's
  63-order correction adjudicated the sector onto that side.

The mechanism-side derivations to run:
- B1 (THE REDSHIFT CANCELLATION, the expected centerpiece): the shell's local
  temperature is T_loc(sigma) = beta K h c^2/(m* sigma) (the chain: pressing ->
  barrier -> bit-cost -> mode count), and the near-horizon Tolman factor is
  alpha(sigma) = kappa sigma/c^2. Machine-check that sigma CANCELS in
  T_inf = alpha T_loc, leaving T_inf = (beta K h/m*) kappa -- the observed
  temperature proportional to the surface gravity with a geometric coefficient.
  This is the THIRD cancellation of the arc (after W/T and the Boltzmann
  factor), and it is the Hawking law-form: bar = exact symbolic cancellation.
- B2 (T1 form-match): the mechanism's emission frequency scale omega ~ T_inf/
  hbar is proportional to kappa -- the SAME form the lineage committed. The
  coefficient ledger is stated, not resolved: mechanism beta K h/(m* hbar-scale)
  vs committed 0.23, comparable only after K and h are evaluated; the bar is
  the FORM (proportionality to kappa, sigma-independence at infinity), and the
  coefficient comparison is registered as the named remaining computation.
- B3 (T2 class-match): the mechanism's flux, n_x f* nu exp(-(W+E)/T) e_bit,
  has an ORDER-UNITY Boltzmann factor (GRV-086) and no supply-side factor: it
  is kinetics-limited -- SWITCH-class. Bar: the mechanism must land on
  GRV-047/053's corrected side of the sector, not the superseded supply law.
- B4: the full-numeric ledger (K, h, n_x, nu -- all geometric/kinetic, all
  named) and the verdict.

Rules: R1 no coefficient is fitted or evaluated; R2 a form MISMATCH on either
target registers at full strength as the campaign's negative.
