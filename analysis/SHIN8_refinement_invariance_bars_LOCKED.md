# COMMISSION SHIN8 -- BARS, LOCKED BEFORE COMPUTING

*2026-08-13. Candidate claim EM-RECON-033. The refinement-invariance
audit HANDOFF names as the active queue's protective item: exhibit
n_sub-invariance of the load-bearing coarse quantities under
GRANT-SUBSTRUCTURE-TIGHT's redistribution, or find the break.
Ride-along: the scale-class naming question (is the matter-rope
strand count the same object as FND-087's n_sub?).*

## The refinement map R(n_sub), fixed from the grant's own text

Per FND-087 (redistributed tension, zero change to coarse
aggregates): per-sub-strand tension T0_f = T0/n_sub; per-sub-strand
line mass mu_f = mu/n_sub; per-sub-strand stretch modulus
k_f = k/n_sub (moduli in parallel sum to the coarse value, same
redistribution logic as tension -- stated here as part of the map,
consistent with EM-RECON-032's usage); carrier count multiplied by
n_sub; coarse aggregates (T0, mu, k, a, w, kappa_pack) FIXED; the
derived winding angles psi_1, psi_2 (FND-088) are geometry, carrying
no n_sub dependence on their registered derivation.

## The audit list (fixed; the four HANDOFF names plus two hardening
## items)

Q1 c (wave speed); Q2 Sigma (vacuum stiffness, by its registered
inputs); Q3 the hbar route (GRV-093's hbar = T0 [l_q^2/(4 pi
alpha)]/c, by its registered inputs); Q4 the T0 fork (ELEC-052's
28 percent tension: does refinement resolve, worsen, or leave it);
Q5 zero-point (the named risk: mode multiplication under
refinement); Q6 the fine constant itself (k_f/T0_f = 8.09 and its
falsifier: n_sub-dependent or not).

## Verdict grammar per item (pre-committed)

- INVARIANT: exhibited by direct algebra under R.
- INVARIANT-BY-INPUTS: the registered formula's inputs are all
  coarse-fixed under R; exhibited by listing the inputs against the
  map. (Weaker grade, honestly labeled: it audits the formula as
  registered, not re-derivations that might reach inside.)
- CONDITIONAL: invariance holds under a named condition on
  registered structure; the condition is stated exactly and its
  settling claim or missing claim is named.
- BREAK: an n_sub dependence is located; registered at full volume
  wherever it lands.

No item may be dropped; the zero-point item may not be upgraded from
CONDITIONAL to INVARIANT without a registered ledger-cutoff claim
cited by id.

## Ride-along grammar

The naming question is adjudicated as SAME-OBJECT or DISTINCT with a
NAME_REGISTRY-class ruling; distinct objects get distinct canonical
names.

## House

Bars locked before computing. Em dashes forbidden. Stale tripwires
honored.
