# ELEC-083 bars — LOCKED before computation (2026-08-02)

Commission (ETA's and ELEC-054's joint next-order): the A-to-l_q ratio test — the
shared-origin hypothesis for the corpus's two mesoscopic lengths, with the two
registered scale sets voting.

A FLAW IN THE COMMISSION, found in reconnaissance and recorded before computing:
the proposed "scale-set vote" is expected to be VACUOUS. A_hbar = sqrt(2 hbar c/
(pi T)) and the ETA bound g_min a = sqrt(4 pi alpha hbar c/T) both scale as
sqrt(hbar c/T), so their ratio should eliminate T and a IDENTICALLY — scale-set
invariance of the ratio-at-bound would be algebra, not evidence. The session must
prove or refute this before any invariance is celebrated. (The calibration-closure
trap, third appearance in this arc; this time caught in the commission itself.)

Rules fixed in advance:
- R1: If the vacuity is confirmed, the session must extract whatever non-tautological
  content remains, or register that none does.
- R2: NUMEROLOGY GUARD, armed maximally: if a pure-number relation for alpha emerges,
  NO candidate constants may be hunted or tested against it. The required value is
  computed and stated; mechanisms must produce it blind. (The 111-vs-137 rule.)
- R3: Both branches of the hypothesis carried: shared origin (one mechanism sets both
  lengths) and independent origins (the ratio is meaningless). Neither is selected.
- R4: All inputs are registered corpus quantities (ELEC-054's A; ETA's bound and its
  inputs); nothing new retrieved.

Bars:
- B1 (the vacuity check): compute A/(g_min a) symbolically. Expected: T and a cancel
  exactly, leaving a function of alpha alone. If so, the scale-set vote is DEAD and
  said so; if not, run the vote as originally conceived.
- B2 (the reframing theorem, if B1 confirms): under the shared-origin hypothesis
  A/l_q = rho (a pure number), derive the exact relation between alpha and rho.
  Expected: alpha = 1/(2 pi^2 rho^2), equivalently 1/alpha = 2 pi^2 rho^2 — the
  fine-structure constant collapses to ONE pure geometric ratio. Verified
  symbolically; the measured-alpha-required rho computed to full precision.
- B3 (consistency): verify that under the hypothesis the two mesoscopic demands
  (ELEC-054's A ~ 60-71 w; ETA's g >= 13-16) collapse to ONE demand, mutually
  consistent for any a — i.e. the hypothesis ties the scales without fixing a.
- B4 (rule R2 applied): rho_required stated; no candidates tested; the claim text
  must state what a mechanism owes (produce rho blind) and what would break the
  degeneracy (any independent determination of a or kappa).
- B5 (the independent-origins branch, rule R3): state what the corpus holds if the
  hypothesis is false — two separate mesoscopic mechanisms, alpha not reducible to
  one ratio — without prejudice.
