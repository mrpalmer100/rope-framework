# COMMISSION SHIN8 -- RESULTS (EM-RECON-033, 2026-08-13): FIVE INVARIANT, ONE CONDITIONAL, NO BREAK -- AND THE RIDE-ALONG RULES DISTINCT

*Bars: analysis/SHIN8_refinement_invariance_bars_LOCKED.md.
Benchmark: benchmarks/foundations/shin8_refinement_invariance.py.*

## The audit

Under the refinement map R(n_sub) fixed from FND-087's own text
(per-carrier tension, line mass, and stretch modulus each 1/n_sub;
count times n_sub; coarse aggregates fixed; derived angles
n_sub-free geometry):

- **Q1 c: INVARIANT** (exact algebra; T0_f/mu_f = T0/mu identically).
- **Q2 Sigma: INVARIANT-BY-INPUTS** (registered inputs T0, a,
  kappa_pack all coarse-fixed; the weaker grade honestly carried --
  the audit certifies the formula as registered, not any future
  re-derivation reaching inside a strand).
- **Q3 hbar route: INVARIANT-BY-INPUTS** (GRV-093's tension x area /
  c consumes only coarse objects).
- **Q4 T0 fork: INVARIANT** in the protective double sense: the
  ELEC-052 tension (+19 percent, the 28 percent scale question)
  neither resolves nor worsens under refinement -- no substructure
  ESCAPE from the fork exists, and no substructure BLAME for it
  either. The fork still waits on non-engine data, exactly as filed.
- **Q5 zero-point: CONDITIONAL, and this is the audit's finding.**
  The carried band is invariant by construction: below the winding
  pitch scale sub-strands are locked, the bundle is one carrier, the
  mode count per volume is unchanged and the frequencies ride the
  invariant c. Relative sub-strand modes exist only at lambda < p <=
  lambda_min/4 -- strictly outside the carried band by the grant's
  own over-resolution condition. Whether those fine modes contribute
  zero-point depends on the ledger's cutoff convention, AND THE
  REGISTRY CARRIES NO SUCH CLAIM (searched; FND-038's zero-point
  readings are superseded coefficients, not a convention). The named
  condition: total invariance iff the zero-point ledger integrates
  carried modes only. The missing claim is the cutoff convention
  itself -- small, definitional, and now precisely located.
- **Q6 the fine constant: INVARIANT.** k_f/T0_f is a ratio of two
  quantities scaling identically, so 8.09 = 2/s and the bound 4.05 =
  1/s hold at every refinement depth: EM-RECON-032's armed falsifier
  confronts ANY future fine-level determination regardless of n_sub.
  This hardens the arc's first derived fine constant against the one
  parameter that could have dissolved it.

## Verdict

NO BREAK LOCATED. The substructure grant's protective claim (zero
change to c, Sigma, Lorentz margins) is now AUDITED rather than
asserted for the four HANDOFF-named quantities, at the grades stated.
The one conditional is not a wound but a to-do: a one-session
ledger-cutoff convention claim (definitional, TSADE-class) closes Q5
to full INVARIANT.

## Ride-along ruling (NAME_REGISTRY-class): DISTINCT

The matter-rope strand count (a rope is a bundle of VACUUM STRANDS;
FND-MATTER/NUCQ usage) and FND-087's n_sub (a vacuum strand is a
bundle of SUB-STRANDS) are objects at DIFFERENT hierarchy levels:
rope-of-strands vs strand-of-sub-strands. They are not the same
object and must not share a symbol. Canonical names ruled here and
appended to docs/NAME_REGISTRY.md: n_rs (rope strand count,
matter-sector) and n_sub (sub-strand count, FND-087's object,
unchanged). Old text is quoted with brackets per FND-097's regime.
The scale-class question HANDOFF queued (which level a given formula
addresses) is thereby answerable claim-by-claim by symbol alone
going forward.
