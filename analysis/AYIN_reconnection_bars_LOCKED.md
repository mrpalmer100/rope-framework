# COMMISSION AYIN -- THE RECONNECTION RATE ACQUISITION: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number in this commission was computed.

## Charter

The registry's most-wanted acquisition, demanded in five independent
contexts (FND-051 C4, FND-053 S2, NUC-026 D7, NUC-028, NUC-029/030's
configuration mobility). This commission does NOT invent a rate. It does
three lawful things: (Q1) classify what object each demanding context
actually asks for, verbatim from the claims; (Q2) derive what registered
structure ALREADY fixes about that object; (Q3) confront the result with
NUC-030's sealed target v0 = 16.97 MeV, display-only, under the QGATE
conditionality. Claims opened and read verbatim at charter: FND-051,
FND-053, NUC-026, NUC-028/029/030, QGATE-004/005, HBAR-001/002,
FND-KIN-004/005, QB-023, ELEC-043 (the causality kill, honoured below).

## Bars

B1 (classification locked): each demand is classified into exactly one of
    three dimensional objects before any derivation:
      [F] a frequency (events per time),
      [P] a probability or cross-section per encounter,
      [T] a hopping/hybridization amplitude (an energy).
    The classification is read off the demanding claim's own wording. Any
    registration must carry the conversion chain F = nu x P,
    T = hbar x F (conditional on QGATE), sigma = P x a^2, so one object
    serves all five contexts.

B2 (the athermality constraint, stated as a theorem candidate): FND-051's
    sweep certifies NO vacuum temperature is registered. Therefore no
    Arrhenius factor exp(-W/kT) is available to any rate law, and any
    exponential suppression must come from a registered NON-thermal
    structure or not at all. This is derived from the registry, not
    assumed.

B3 (the interpenetrability constraint): magnetism 2.1 and FND-KIN-005
    register free single-strand interpenetration and free annihilation
    with exact charge-class conservation. Consequence to be adjudicated:
    whether any hard-core obstruction gates reconnection at single-strand
    level, or whether the gate is geometric/topological only.

B4 (the attempt scale): the kinematic encounter frequency nu = c/a is to
    be computed from registered quantities only (wave speed from
    mu = T/c^2, HBAR-001's registered identity; spacing a carried at ALL
    registered readings per FND-066's a-carrying rule, never averaged).
    hbar-conversions are displayed under the QGATE conditionality flag.

B5 (the branching ratio is NOT chosen): if Q2 reduces the acquisition to
    a dimensionless branching ratio p per encounter, this commission
    BOUNDS p and derives its structural constraints; it does not select a
    value. Selecting p is a future derivation with its own bars. Any
    temptation to set p = 1/2 by symmetry is refused here as unlocked.

B6 (the sealed-target confrontation, display only): NUC-030's falsifier
    v0 = 16.97 MeV is converted into the demanded window on p (or on
    whatever object survives Q2), under the stated conditionalities. The
    window is REGISTERED AS A TARGET BAND for the future p-derivation --
    the sealed-target structure again: the band exists before the
    mechanism that must hit it.

B7 (causality honoured): ELEC-043's kill stands -- no recruitment across
    unreachable regions. Any rate structure proposed must be local to a
    crossing. Checked explicitly, not waived.

B8 (adverse outcomes pre-authorized): acceptable outcomes include:
    nothing beyond dimensional structure is derivable (registered as
    such); the athermality theorem fails (a registered thermal structure
    is found); the target window is empty or absurd (the adoption's
    falsifier fires early and NUC-030 returns to adjudication). No
    rescue, no bar-shopping.

B9 (house): no em dashes in file content; benchmark under
    benchmarks/foundations/; verify before re-zip.
