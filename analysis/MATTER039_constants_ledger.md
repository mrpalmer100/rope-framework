# FND-MATTER-039 — the constants ledger: the scale problem posed as a solvable system

Date: 2026-08-04. Commission: the first brick of the absolute-scale
campaign (the ELEC-084-ranked #1 unlock). Method: registry archaeology
only — every relation below is quoted from a registered claim with its
status; no new computation is performed here. The ledger's product is
the scale problem CONVERTED from "derive a from the postulates" (proved
impossible: FND-MATTER-005, Derived, stands untouched) into "solve a
specific over-determined system of REGISTERED relations against
measured constants, and confront the redundant equation" — which is a
computation with bars, not a philosophy problem.

## The unknowns

Internal: T0 (strand tension), a (mesh spacing), Pi = kappa a / T (the
single dimensionless internal ratio, FND-005, Derived). Emergent-but-
internal: l_q (the "quantum area" length of the registered hbar
relation, whose relation to a is GRV-093's named open geometry
question).

## The relation table (id, status, the equation, what it pins)

R1 — the hbar relation (GRV-093, Modeled; provenance PRED-003):
    hbar = T0 * [ l_q^2 / (4 pi alpha) ] / c
    Given measured (hbar, c, alpha): pins T0 * l_q^2.

R2 — the induced-gravity tension (GRV-095 with GRV-025/029, Modeled;
    the fork's resolution):
    c^4 / (16 pi G) = zeta D * hbar c / a^2 , dictionary factor bounded
    geometrically in [1e-2, 1e2] by pre-committed enumeration.
    Given measured (G, hbar, c): pins a = ~8 Planck lengths within
    sqrt(D). THE GRAVITY-ROUTE a — already computed and adopted.

R3 — the rigidity identity (GRV-006, Derived; GRV-074's units finding):
    c^4/(4 pi G) = 9.6e42 N is the network rigidity, and c^4/(16 pi G)
    is machine-checked to be a TENSION — the SAME quantity R2 equates
    to the induced EH tension. R3 is therefore R2's restatement, not an
    independent equation; logged to prevent double-counting.

R4 — the Lorentz bound (FND-REL-003, EFT-constrained):
    a < ~1e-16 m. A one-sided constraint; R2's Planck-scale a satisfies
    it by eighteen orders. Consistency, already banked.

R5 — the matter route (FND-MATTER-007/009, Modeled; the two-term mass
    model):
    m(K) c^2 = Sigma_T0 * L(K) * a + lambda * dE_zp(K)
    with L(K) the knot ropelength in units of a (ring 3.141, trefoil
    16.84, 5_1 25.12), the zero-point lever measured at ~25% and lambda
    honestly blocked at FND-MATTER-003. Given measured m_e and a knot
    identification: pins T0 * a to within the ZPE factor (order 2-3).
    THE MATTER-ROUTE T0*a — posed, not yet spent.

R6 — the extensibility cross-check (EM-RECON-010, Modeled):
    the stretch modulus k is independently measurable via vacuum
    optical nonlinearity — the SAME PVLAS-class instrument GRV-095
    armed as the fork's falsifier. One instrument, two sectors; logged
    as the system's external tie-breaker.

R7 — the snap-action bracket (GRV-092/093, Modeled):
    n_q = A*/hbar = 4 pi alpha * (3 beta/(0.23 chi)) * (a h / l_q^2),
    reproduced independently at 1e-4..5e-4 with no tuning. A derived
    consistency band the solved system must land inside.

R8 — the hierarchy identity (GRV-006, Derived):
    G m_p^2/(hbar c) = (m_p/M_Pl)^2 — exact by definition of M_Pl;
    logged as an identity, NOT a constraint, to prevent its accidental
    use as one.

## The over-determination, and the well-posed computation

Independent equations consuming measured constants: R2 (uses G, hbar),
R5 (uses m_e), R1 (uses hbar, alpha). Internal unknowns they touch:
a, T0, l_q. COUNT: three equations, three unknowns — BUT R2 has already
been solved by the corpus (a adopted at eight Planck lengths), so the
pipeline is:

  STEP 1: a from R2 (done; carry the sqrt(D) band honestly).
  STEP 2: T0 from R5 with that a (spending the campaign's ONE permitted
          calibration on the electron mass, in the NUC-005 tradition —
          the calibration decision, stated here in advance as the
          recommended spend, final say the author's).
  STEP 3: l_q from R1 with that T0 — no freedom remains.
  STEP 4 (the confrontation, where the campaign becomes falsifiable):
          the derived l_q/a ratio ANSWERS GRV-093's open geometry
          question numerically. Pre-commitable grammar for the closure
          session: l_q/a of order 1-10^2 reconciles the two registered
          action areas as cell-scale geometry (the "cell cross-section"
          answer); a ratio wildly outside that, with the ZPE factor and
          sqrt(D) bands honestly propagated, is a REGISTERED TENSION
          between the gravity and matter routes — the whisper-
          coefficient precedent says either outcome is corpus-grade,
          and the mismatch's SIZE will itself point at the responsible
          band (ZPE lever vs dictionary factor vs a real crack).
  STEP 5 (dividends, automatic): T0 in newtons confronts GRV-074's
          registered 1.4-2.0e39 x T0 quantification and R3's rigidity;
          n_q must land in R7's band; and a, T0 in SI units convert
          P23's epoch to seconds and P25's location to hertz — the
          ELEC-084 unlock, executed.

## What this ledger does NOT do (honesty clauses)

- No numbers are computed here beyond quoting registered ones. The
  closure computation is the NEXT session, under locked bars with the
  step order, the calibration spend, the band propagation, and the
  Step-4 grammar all pre-committed (drafted above so the bars session
  is a formality).
- FND-MATTER-005's irreducibility theorem STANDS: a is not derived from
  the postulates; it is over-determined by MEASUREMENTS through
  registered relations, which is exactly what "fundamental constant
  fixed by measurement" means. The new content is that the fixings are
  now REDUNDANT, and redundancy is testability.
- Statuses propagate: every step inherits Modeled from R2/R5/R1 (and
  Grant-adjacent caveats where their provenance carries them); nothing
  downstream may claim Derived.
- Absolute scale remains registered OPEN (FND-MATTER-003) until Step 4
  adjudicates; this ledger poses, it does not close.
