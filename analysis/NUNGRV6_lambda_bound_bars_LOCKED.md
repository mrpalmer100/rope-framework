# COMMISSION NUN-GRV6 -- THE lambda-BOUND AUDIT (C3 vs REGISTERED EM OPTICS) -- BARS LOCKED (2026-08-14)

Locked BEFORE any computation. Run PRE-GRANT as pricing information
for the GRANT-CANDIDATE-SHIFT-C3 decision; nothing here adopts,
licenses, or presupposes the grant. The candidate term is examined
AS IF present, to learn what the registered optics sector would do
to it.

## QUESTION
If L gains lambda (grad phi) . u_t, what does the REGISTERED optics
sector (light = transverse u waves; the corpus's registered mode
counting, dispersion and birefringence results) do to lambda? Three
pre-committed outcomes:
- BOUND-BINDING: registered optics constrains lambda at an order the
  audit computes, with the constraint's structure displayed. The
  bound's VALUE is registered; whether it kills the gravitational use
  is NOT computed here (clean-room: no frame-dragging observable, no
  LARES-2, no target rate -- the comparison happens, if ever, in a
  later commission after the grant).
- BOUND-VACUOUS: the transverse wave sector is unaffected at the
  computed order (the modification confined to sectors the registered
  counting constrains out), and the operative constraint moves to a
  named weaker channel whose suppression is displayed.
- NOT-CLOSED: the calculation cannot be completed from registered
  structure; the missing piece named.

## THE COMPUTATION, fixed in advance (B1)
(a) With NO registered microinertia, delta-phi enters the quadratic
    action only through gamma |grad delta-phi|^2 and the C3 mixing
    lambda grad(delta-phi) . delta-u_t: delta-phi is a CONSTRAINT
    field. Integrate it out exactly (Gaussian) and display the
    induced u-sector operator at O(lambda^2).
(b) Evaluate the induced operator on TRANSVERSE u (div u = 0,
    the registered light sector) -- symbolically, not by assertion.
(c) Evaluate on longitudinal content, and state which registered
    claims govern that sector's observability (the FND-REL-002 L1
    annotation and the EM mode counting).
(d) Background terms: lambda grad(phi_bar) . delta-u_t is LINEAR in
    perturbations; display why linear terms shift equilibrium and do
    not enter the propagation operator, or find otherwise and say so.
(e) The discrete channel: any residual transverse effect via the
    FND-REL-004 O((ka)^2) longitudinal admixture is priced with its
    suppression order displayed.

## REFUSALS (B2)
- No external literature values imported; the audit is corpus-internal
  (registered claims and constants only). An external-data optics
  confrontation, if ever wanted, is its own commission.
- No frame-dragging quantity of any kind (clean-room continues).
- No adoption: a favorable outcome here does NOT make the grant; it
  revises the candidate's price sheet, which returns to the author.
- If the outcome is BOUND-VACUOUS, the S2 price line on GRV-108's
  face is REVISED on the new claim's face, not edited in GRV-108
  (superseded-not-erased applies to price sheets too).

## DELIVERABLE (B3)
One registered claim (next GRV id) with the outcome, the induced
operator displayed, and the revised C3 price sheet. Either outcome
is kept.

## FAILURE MODES NAMED IN ADVANCE
- "Transverse is unaffected" claimed without the symbolic projection
  is assertion, not audit.
- Discovering the price got CHEAPER is exactly when scrutiny doubles:
  a pricing audit that flatters the candidate must show every step,
  because the house knows which direction temptation points.
