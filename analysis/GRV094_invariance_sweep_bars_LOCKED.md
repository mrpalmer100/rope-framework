# GRV-094 bars — LOCKED before computing (2026-08-03)

Commission (two queued items, one session): the h fm-audit (GRV-088's suspect
(ii), the coefficient campaign's last unadjudicated item; made load-bearing
again by the ring-quantum arc) and GRV-076's matter-scale fork sweep -- unified
as THE INVARIANCE SWEEP: classify every length in the horizon/hbar arc as
FORK-INVARIANT (physical) or FORK-DEPENDENT (convention-carrying), by
registered provenance, and machine-derive the fork exponent of every quantity
the arc built.

Classification criteria, fixed in advance:
- INVARIANT: anchored to a measured physical object or invariant combination
  (the electron calibration; the alpha relation's l_q; T0 on the degeneracy
  line, per GRV-076's exactness).
- CONVENTION: the lattice spacing a (GRV-076: zero live pins; scale sets are
  the bound wearing a different hat) and anything scaling with a power of it.
- RETIRED: the ELEC-061-era spacing w = 5.78e-17 m (GRV-076 retired the
  lineage); any chain still leaning on w is flagged CONTAMINATED.

Bars:
- B1 (the provenance table): every length cited to its registered source --
  d_c (ELEC-021, electron-knot calibration, stated calibration-independent),
  l_q (PRED-003 alpha relation), a (GRV-076 fork), w (retired), T0/Sigma
  (FND-017 with GRV-076's degeneracy). The d_c chain is checked for
  w-contamination at the level the registry states; a full ELEC re-derivation
  is out of scope and said so.
- B2 (the exponent theorem, by machine): sympy-derive the a-exponent of every
  arc quantity holding the invariants (T0, l_q, d_c, alpha, c) fixed:
  Sigma (-2 expected), the barrier W, e_bit, T_inf, the Hawking coefficient C,
  the snap action A*, n_q, and the quantum area. Pre-committed expectation:
  the ENTIRE fork-dependence of the horizon/hbar arc is ONE POWER OF a in
  each of {C, A*, n_q}, and ZERO in the quantum area. Deviations register.
- B3 (the adjudication): if d_c classifies INVARIANT, GRV-088's suspect (ii)
  CLOSES -- h is not convention-dressed, the mechanism-side numbers do not
  rescale, and the coefficient campaign's suspect ledger is complete (all
  four adjudicated). If contaminated, the rescaling is computed and
  propagated.
- B4 (guarded observations): any numeric adjacency noticed during the sweep
  (e.g. between a and combinations of invariants) is registered ONLY under
  the numerology guard (the MeV-adjacency precedent): stated with its
  percentage, flagged as unexplained, with no claim built on it -- and
  adjacency to a BOUND-VALUED convention is called out as especially weak.
