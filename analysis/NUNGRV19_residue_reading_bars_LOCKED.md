# THE RESIDUE READING (NUN-GRV19) -- BARS LOCKED (2026-08-16)

Locked before the adjudicating read. Target: GRV-070's overlap
residue R_0 ~ (a+b)(c+d), declared generic when the action had
neither a source coupling nor a metric feed. Both have since been
supplied (GRV-104/105; GRV-110). The session tests whether the
residue is now a READING rather than a derivation.

## B0 -- THE REVIEWER'S CORRECTIONS, quoted as binding constraints
GRV-070 carries two corrections owed to an external reviewer, and
this session stays inside them:
(R1) "'SOURCES NOTHING WHATEVER' IS WITHDRAWN: on the line a = -b a
rigidly co-rotating configuration does not source THIS ROTATIONAL
CHANNEL, while it could still source scalar gravity, compression,
ordinary displacement, parity-odd channels or other vector fields."
-- Consequence: any null-line statement here is scoped to THIS
channel only.
(R2) The metric residue vanishes on c = -d ("the metric seeing only
relative rotation"), "a different and weaker condition whose
parallel with the source residue is exact." -- Consequence: the two
factors are read INDEPENDENTLY; no shared-fate shortcut.
Also binding, GRV-069 C1: a zero mode can stay invisible via source
orthogonality, observable projection, derivative cancellation, or
rigid-global-mode status -- each of the four must be dispositioned,
not waved.

## B1 -- THE TWO READINGS TO PERFORM (fixed)
SOURCE MAP L = J.(a Omega + b phi): GRV-104's grant identifies
mechanical J with topological-geometric TWIST -- the frame/internal
azimuth channel phi -- at totality (beta_J = 1, GRV-105), with
GRV-118 T2 quoting "no lambda and no g_0i anywhere in the source
leg." Read whether this forces (a, b) = (0, 1) in the grant's
normalization, i.e. (a+b) = 1.
METRIC MAP g_0i = c Omega_i + d phi_i: GRV-110's L_C3 =
lambda (grad phi_grav) . u_t is the SOLE registered feed into g_0i
(GRV-116: "frames are dragged by g_0i, whose sole feed is L_C3").
u_t is the DISPLACEMENT velocity -- the backbone channel, whose
rotation content is Omega = (1/2) curl u. Read whether this forces
(c, d) = (1, 0) in the dictionary's normalization, i.e. (c+d) = 1.

## B2 -- OUTCOMES (exhaustive)
- RESIDUE-READ: both factors forced by registered text; R_0 = 1 in
  the registered normalizations (any residual convention factor
  identified with GRV-120's already-fixed kappa_conv bookkeeping,
  not double-counted); distance from both null lines stated;
  Lambda_nat becomes QUOTABLE with all riders on its face;
  chi_required quoted.
- RESIDUE-NEAR-NULL: a reading lands on or near a = -b or c = -d;
  the framework predicts suppressed dragging -- register at full
  volume as the kill-shaped outcome (scoped per R1).
- FACTOR-FREE (named): a coefficient survives unforced; the
  sector's residual shrinks to it by name.

## B3 -- THE GRV-069 C1 FOUR, dispositioned one by one (mandatory)
(i) source orthogonality; (ii) observable projection onto the
massive mode; (iii) derivative cancellation of the 1/k^2 pole;
(iv) rigid-global-mode status. Each answered from registered text
or the outcome degrades to FACTOR-FREE.

## B4 -- REFUSALS
Condition 4 unchanged (no LARES verdict; the chi_required quote is
internal structure). Level-assignment residue carried, still owed
before FINAL numbers -- any Lambda_nat quoted here carries that
rider plus k/T0 = 2 and the gamma_lock bracket. No promotion of any
Modeled claim. Normalization factors are identified, never invented.
