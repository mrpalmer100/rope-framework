# COMMISSION EM-RECON-038 -- THE CAGED STRAIN DERIVATION -- BARS LOCKED (2026-08-15)

Locked BEFORE any number is computed. This session executes the
charter and cage written on EM-RECON-037's face, transcribed binding:
route fixed in advance as collision-saturation geometry applied to
the registered bound-mode class; the mesh-to-bound scope extension
DERIVED as the first obligation; output registered whatever it is;
landing near the quarantined g ~ 0.75 display is NOT confirmation
and landing far is NOT refutation of it (the display was never a
claim). The re-run of the closure at any derived strain is a FURTHER
session per the charter, not this one.

## THE ROUTE'S REGISTERED INPUTS, quoted (B1)
- FND-MATTER-036 (collision saturation): transverse jitter saturates
  at the uniform value <A^2> = (a - D)^2 / 12, MC-verified,
  drive-independent above the crossover; x = D/a phenomenologically
  windowed to [0.27, 0.59]; CONDITIONAL on the registered assumption
  that the medium lives above the crossover; caveats carried on its
  face (mean-field hard walls, per-mode vs per-site bookkeeping at
  O(1)).
- Wall separation: FND-MATTER-036's text reads "in a mesh of spacing
  a each rope's transverse jitter" -- the registered wall gap is the
  VACUUM MESH spacing a (M-point, FND-MATTER-044: a = 6.0e-17 m).
  No other spacing is registered for this mechanism; substituting an
  unregistered one (intra-bundle, xi-scale, or otherwise) is BARRED
  as exactly the tuning the cage exists to prevent. If the registered
  gap produces an absurdity, the absurdity is the result.
- Bound-mode class: w(u_perp) e^(-u) e^(i s phi), u in xi units
  (MODE_OVERLAP_DERIVATION.md Sec 2), displacement field psi with
  peak displacement amplitude A.
- Sector decay lengths, from the registered targets (EM-RECON-031
  bars): chemical xi = 0.443 Angstrom (H2 bond 0.74 A / 1.67);
  nuclear L = d0/1.36 with the registered spacing, ~1.4e-15 m class.

## THE EXTENSION ARGUMENT TO BE DERIVED (B2, first obligation)
The cap is hard geometric contact of rope envelopes: FND-MATTER-036's
own registration says geometry is frequency-blind, and the derivation
must establish (or fail to establish) that it is likewise
COHERENCE-BLIND -- that a coherent winding displacement pattern is
capped by the same walls as incoherent jitter, inheriting the same
saturated value and the same above-crossover conditionality. If the
extension cannot be carried by the registered texts, the outcome is
EXTENSION-FAILS and the session ends there.

## THE CONVERSION, fixed at lock (B3)
Displacement to strain through the registered class only:
psi(r) = A * S(r)/S_max with S = w(u_perp) e^(-u), S_max = 0.284878.
Strain field |grad psi| includes the azimuthal phase gradient
(|psi|/rho_perp term). Operating strain:
    g_op = (A_sat / xi) * C_shape,
    C_shape = max over space of |grad_u (S e^(i phi))| / S_max,
computed numerically once, convergence-checked. A_sat = a (1 - x) /
sqrt(12) over the registered x window. g_op is computed PER SECTOR
(chemical xi; nuclear L). No other conversion may be substituted.

## OUTCOMES, pre-committed (B4)
- STRAIN-REGISTERED: g_op lands where the registered energy
  landscape binds (above the no-binding threshold ~0.6 recorded in
  EM-RECON-036's sweep). The strain is registered with its window
  and conditionality; the closure re-run is chartered for a further
  session.
- NO-BINDING-CONTRADICTION: g_op lands below the binding threshold
  in a sector where bound states manifestly exist (chemical bonds,
  nuclei). Registered as the route's output CONTRADICTING binding
  existence within the registered energy model: the
  collision-saturation candidate is KILLED for bound-mode operation
  at the registered wall gap, the failure kept, and the arc parks on
  its external clocks per the charter. The kill is of the ROUTE, not
  of FND-MATTER-036 (whose alpha-chain scope is untouched).
- EXTENSION-FAILS: B2 cannot be carried; registered; arc parks.
- Whatever lands, the two cage clauses are restated in the verdict.

## REFUSALS (B5)
- No wall-gap substitution (B1). No conversion substitution (B3).
- No consultation of the quarantined display for any decision.
- No re-run of the closure this session.
- No touching FND-MATTER-036's alpha-chain standing.

## DELIVERABLE (B6)
One registered claim (EM-RECON-038) with the extension argument, the
computed C_shape, the per-sector g_op windows, the verdict, and the
cage clauses on its face; benchmark script; annotations; CHANGELOG;
verify green; re-zip.

## FAILURE MODES NAMED IN ADVANCE
- Gap-shopping toward a bindable strain (barred at B1; the registered
  gap decides even if it embarrasses the route).
- Quiet dropping of the azimuthal strain term to move C_shape.
- Reading the verdict against the quarantined display in either
  direction, in any layer, including this file.
