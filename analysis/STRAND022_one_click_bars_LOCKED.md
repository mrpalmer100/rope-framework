# FND-STRAND-022 — one quantum, one click: bars locked first

Date: 2026-08-04. Commission: the honest boundary named in the detector
account — WHY does one photon's worth of delocalized channel energy fund
exactly ONE nucleation event, never two? The framework's informal answer
(the first nucleation drains the channel) is reasonable and underived.
This session makes it a measured mechanical statement or refutes it.

## The model, committed

The registered composite (FND-STRAND-008 engine verbatim: chain + per-site
gapped weaves, kt = 0.64, K = 16, c0 = 0.35, dt = 0.02), N = 96, static
tilt h = 0.30 — the registered SILENT operating point — at T = 0.15 (cold
enough that thermal nucleation is negligible over the window; B0 verifies
rather than assumes). Window: 4000 units.

THE QUANTUM: a single delocalized excitation of the chain — the
fundamental standing mode — injected at t = 0 as a velocity profile,
pphi_n += A cos(2 pi n / N), carrying energy E_inj = A^2 N / 4. This is
the model's photon-on-the-screen: one extended coherent excitation
overlapping every potential nucleation site at once.

## The observable, committed

FIRE: any site's phase crosses pi within the window. EVENT COUNT n_ev:
at t_first + 25 units, the number of DISJOINT spatial clusters of sites
with phi > pi (periodic adjacency). One cluster = one nucleation event
growing; two disjoint clusters = two independent events funded by the
same injection. (The cluster instrument reuses the STRAND-012 multi-seed
grammar.)

## Calibration, procedure committed (not tuned)

E_ref := the injection energy at which P(fire) = 0.5, obtained by a
committed coarse scan over A in {0.10, 0.14, 0.18, 0.22, 0.26, 0.30,
0.36} at S = 24 seeds per point (generators 171 + point index), linear
interpolation between bracketing grid points. All subsequent energies are
stated in units of E_ref. If the scan does not bracket 0.5, extend the
grid upward by the same spacing (pre-authorized) and say so.

## B0 — the thermal control

Zero-injection ensemble, S = 48 (generator 170): fire fraction <= 5%
within the window. FAIL -> T is re-locked colder in an addendum (with the
informed-status ceiling that implies) before anything else runs.

## B1 — exclusivity at one quantum and a half

At E_inj = 1.5 x E_ref (S = 48, generators 181-182): among FIRED runs,
P(n_ev >= 2).
- EXCLUSIVITY: P(multi) <= 0.15 — a single delocalized quantum funds one
  event; the remainder does not nucleate a second.
- REFUTED: P(multi) >= 0.40 — energy accounting does not enforce
  one-click; the informal drainage story dies and the exclusivity
  problem is registered OPEN with the measurement recorded.
- INTERMEDIATE: as measured.

## B2 — the threshold prerequisite

P(fire) at 0.5 x E_ref <= 0.20 AND at 1.5 x E_ref >= 0.80 (S = 48 at the
low point, generators 183-184; the high point shares B1's ensemble). The
device must BE a threshold detector for B1 to mean anything; a mushy
response voids B1 (pre-stated) and registers the response curve as found.

## B3 — the two-quanta control (the observable's own alibi)

At E_inj = 3.0 x E_ref (S = 48, generators 185-186): P(n_ev >= 2) >= 0.30.
Two quanta's worth of energy SHOULD fund two events at least sometimes;
if multis never appear even here, the cluster instrument (not the
physics) is the suspect, B1's exclusivity reading is voided, and an
instrument audit runs. This control is what makes a B1 pass evidence
rather than blindness.

## Promotion criterion

PROMOTE iff B0 passes, B2 holds, B1 lands EXCLUSIVITY, and B3 lands (the
alibi). Registered consequence at full volume: ONE QUANTUM, ONE CLICK is
MECHANICAL — the first nucleation's growth consumes the delocalized
channel energy faster than a second coherent concentration can form, so
exclusivity is energy dynamics, not postulate. Prediction 14 (Born from
channel energy) gains its exclusivity clause; the wave-plus-dot account
of the plain double slit COMPLETES (the dot is a threshold event, its
location Born-weighted, its uniqueness energetic). If refuted or voided:
the exclusivity problem is registered OPEN with the honest measurement,
and the corpus says so wherever the informal story was told.

## Honesty clauses

- Model, injection form, observable, calibration procedure, generators,
  and every threshold fixed above. The energy grid may extend only as
  pre-authorized.
- This session addresses spatial exclusivity on ONE connected medium at
  the plain-double-slit level; which-path, delayed choice, and the
  eraser are explicitly OUT OF SCOPE and remain the standing hard cases.
- Status ceiling: Modeled. Absolute scale untouched (FND-MATTER-003).
