# FND-MATTER-062 — the settler resolves fork B: exclusion is purchased with length, never financed by removing it. The lever falls.

Date: 2026-08-09. Commission: 061's named settler, run on operator GO.
Benchmark: `benchmarks/foundations/matter062_settler.py`.

## Bars (locked before computing)

1. The fork threshold committed BLIND before any relaxation step: Q =
   (probe-volume strand length at steady state)/(at t=0); Q < 0.5 →
   exclusion geometry confirmed; Q ≥ 0.5 → no exclusion.
2. Two observables pre-named, both reported regardless: Q, and the
   total-length audit ΔL/L — because 061 established length removal as the
   only regulator-stable ZP channel.
3. The engine is the registered one (FND-STRAND-001): inextensible curves
   (enforced as an exact constraint by projection), bending, finite smooth
   contact Ac/(1+(r/σ)⁴), no crossing axiom. Adapted only for open anchored
   strands and a rigid fixed inclusion. No new physics.
4. Adjudication rule pre-committed: a density deficit with ΔL_tot ≈ 0 (or
   ≥ 0) is rerouting, not removal — fork A's framing fails regardless of Q,
   and fork B resolves on the ZP question.
5. Nothing re-fitted; spend stays at ONE; no λ target anywhere in session.

## The run

Fifteen ambient strands (anchored at ±9a, grid of transverse offsets)
relaxed for 12,000 steps around a rigid inclusion; tight-constraint control
(projection ×8) run identically.

| | main run | tight control |
|---|---|---|
| Q (blind threshold 0.5) | **0.000** | 0.000 |
| ΔL/L | **+6.5e-3** | **+3.5e-3** |

**Both observables are unambiguous.** The exclusion geometry is real —
strands vacate the inclusion volume decisively. And in every run, total
strand length INCREASES, by less under the tighter constraint, never by a
negative amount. The invariant is ΔL ≥ 0: exclusion is purchased with
length drawn from the anchoring (the FND-017 reservoir — T₀ as Lagrange
multiplier, the far field supplying length when configuration changes),
never financed by deleting any.

## The adjudication (bar 4, mechanical)

Density deficit + length added = rerouting, not removal. 061 proved removal
is the only scheme-stable zero-point channel. The corpus's own registered
dynamics therefore supplies NO mechanism for the dilute-exclusion count —
and in fact runs the sign the other way (the knot's presence slightly
INCREASES ambient mode-carrying length). **Fork B resolves.**

## Consequences, registered

- FND-MATTER-059's promotion REVERTS (the conditional flag resolves
  adverse). The Crofton theorem stands as mathematics — it correctly counts
  a quantity the weave does not remove.
- FND-MATTER-060's propagation reverts with it; the restated ZPE band
  (1.84×) returns to its pre-derivation width.
- λ returns to OPEN, target 1.156e-5 unchanged, spend still ONE.
- The 1.44× landing of λ = 6π(r/a)² is registered as coincidence-class —
  the campaign's sixth flattering number retired by its own discipline.

## What survives, named precisely

The successor channel identified at 061: **contact-modified dispersion**.
The finite smooth contact between ambient strands and the knot's strand
shifts mode frequencies over the crossing regions without removing length —
extensive in contact length (scheme-stable in class by 061's criterion),
preserving the power-2 thinness form, with ONE unregistered coefficient η
(the fractional dispersion shift per contact). A future commission would
derive η from the registered contact form Ac/(1+(r/σ)⁴) and the strand wave
equation, blind bars first. That is the honest remaining path to λ.

## In-session catches (logged per house discipline)

1. A sign inversion in the constraint-projection sweep (amplified instead
   of corrected; caught by NaN blowup, traced, fixed).
2. The control's original framing predicted the taut limit would RETAIN the
   strands; it did not (the required detour is only ~0.3%, admitted even by
   tight projection). The prediction as phrased failed and the corrected
   framing (the control's content is the SIGN of ΔL, not retention) is on
   the script's face.
3. A stiff-spring integrator was tried first and rejected by the length
   audit itself (10% stretch — outside the inextensible regime).

## Not claimed

λ; the successor channel's coefficient η; any change to the target; any
new parameter. Spend remains ONE.
