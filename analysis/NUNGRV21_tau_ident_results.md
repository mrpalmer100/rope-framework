# COMMISSION TAU-IDENT (NUN-GRV21) -- RESULTS (2026-08-16)

Bars: analysis/NUNGRV21_tau_ident_bars_LOCKED.md (clean-room held:
derivation legs before the comparison leg). Benchmark:
benchmarks/gravity/tau_ident_nungrv21.py.

VERDICT: DISTINCT-CERTIFIED on the identity + FINE-BOUND-DERIVED on
the amplitude -- the author's challenge produced the corpus's FIRST
registered upper bound on the frame-dragging natural scale, and
dissolved a two-unknown gate into one.

## B1 -- the identity: the transplant stays demoted, now on physics
The SHIN windings are fine-pitch BY REGISTERED TEXT (p in
[a_f, lambda/4], worst case p = a_f; every curvature and torsion
quoted in 1/a_f). No winding at the strand radius exists anywhere
in the registry. GRV-127's demotion is upheld -- and is now a
physics statement, not merely a registry one.

## B2 -- the computation nobody had run, and the cancellation
The fine-level lambda assembles entirely from registered numbers:
(G I_p)_f mirrored from GRV-073's chain at the DERIVED fine ratio
k_f/T0_f = 8.091 (EM-RECON-032), times |tau_1| = 4.1888/a_f,
summed coherently over n_sub sub-strands under redistribution
T0_f = T0/n_sub. THE n_sub CANCELLATION IS EXACT: the coherent sum
against redistribution leaves

    lambda_strand <= 0.854 T0 a_f        [upper bound]

with r_s eliminated at the registered slenderness ceiling and
coherence taken as the bounding case (any intra-strand handedness
mixing only lowers it). At the registered a_f ceiling
(lambda_PeV/4 at the 1.400 PeV FND-083 ceiling):

    lambda_strand <= 2.28e-19 J
    Lambda_nat    <= 9.5e34              (T-branch, R_0 = 1)
    chi_required  >= 1.1e-35             for GR-strength dragging

GATE LEDGER IMPROVES: Lambda_nat is LINEAR in a_f -- the
cancellation removed (G I_p)_f as an independent unknown. The
fine-scale gate is now ONE number: {a_f} (equivalently m).

## B3 -- the coincidence, adjudicated (comparison leg, opened last)
The transplant band sits 114-198x ABOVE the registered ceiling. The
landing was CLASS-LEVEL, not object-level: both expressions are
(medium stiffness) x (O(1) winding geometry) x (a structural
length), which is why they agree to ~2 orders across a 37-order
quantity -- and why they do not agree in value. The 1e37 / 1e-37
point values do NOT resurrect. What registers instead is stronger
in kind: a registered CEILING on the natural scale, falling
linearly as a_f falls.

## Standing consequences
- The birefringence attribution remains GATED, with its consistency
  point now QUANTIFIED: Lambda_nat ~ 6e18 requires a_f ~ 1.4e-38 m
  (m ~ 2e16) -- sixteen orders below the PeV ceiling. Named demand
  on the m-suspension; neither excluded nor favored.
- The chirality-meter structure, R_0 = 1, and condition 4 are all
  unchanged. Every number above is a bound or a demand.
- Riders on the face: k/T0 = 2 (FND-114); nu = 0.25 (GRV-073
  chain); coherent-handedness bounding case; T-branch closure;
  a_f ceiling tied to the FND-083 1.400 PeV point (the soft
  falsifier moves it with m).

## REFUSALS HONORED
Clean-room held (transplant numbers untouched until B3); no grant
proposed; the m-suspension used only through its registered
ceiling; condition 4 unchanged.
