# COMMISSION NUN4 -- BARS, LOCKED BEFORE COMPUTING

*2026-08-13. Candidate claim FND-104. The orientation-decoherence
charter FND-103 named. Author-directed.*

## The mechanism class under test

AYIN's exclusion statistic weights both exchange orientations at 1:
v_B = 2/(N-1). The decoherence hypothesis: the second orientation
carries a coherence weight w(N) derivable from LABEL COUNTING ALONE,
so v_eff = (1 + w(N))/(N-1) and

    b/b_CS = f(N) = (1 + w(N))/2.

w must be a parameter-free counting expression over the label space
(GRANT-N2's Z_N carrier and AYIN's exchange grammar). No continuous
parameters, no fitting, zero data contact in any derivation.

## Closed inventory (FIXED; no additions or adjustments mid-session)

Each with its counting story; the story's strength is graded in
results, and a passer with a weak story lands at Conjecture.

D1  w = (N-2)/(N-1)  second orientation coherent iff the exchange's
                     intermediate label differs from both endpoints.
D2  w = 1/(N-1)      coherent iff the intermediate label returns the
                     initiating label exactly.
D3  w = 2/(N-1)      as D2 with both termini admissible.
D4  w = 4/(N-1)      as D3 with both orientations x both termini
                     (weakest story; flagged at lock).
D5  w = 2/N          free-label variant of D3 (statistic-A currency).
D6  w = (N-2)/N      free-label variant of D1.
D7  w = 1/2          constant half-coherence (N-blind CONTROL;
                     expected to fail, included to complete the
                     bracket).
D8  w = N/(2(N-1))   half of partner channels coherent per
                     orientation.

## The three-part bar (all pre-committed; pass = all three)

B1: T1 = f(6) within 2 sigma of 0.865(33)          [FND-102]
B2: T2 = f(infinity) within 2 sigma of 0.64(10)     [FND-102]
B3: PROFILE: f(4) within 3 sigma of 0.928(21) AND
    f(5) within 3 sigma of 0.898(22)                [safe-volume
    Table 14 points in b/b_CS units; errors propagated from the
    quoted tension errors. Registered here at lock, BEFORE any
    candidate is computed, to prevent a two-point pass that
    contradicts the same dataset's safe-volume profile.]

## The shape lemma (computed alongside, display + possible exclusion)

For the one-parameter family f(N) = A + c/(N-1) (which contains
D1-D4, D8 exactly and D5/D6 to O(1/N^2)): solve (A, c) from B1+B2
compatibility and test against B3's slope f(4) - f(6) = 0.063(39).
If NO (A, c) with A within 2 sigma of T2 satisfies the slope within
3 sigma, the ENTIRE 1/(N-1)-decay family is excluded by shape,
independent of counting story. Computed, reported either way.

## Pre-committed verdict grammar

- Any inventory member passes B1+B2+B3: SURVIVING CANDIDATE at
  Conjecture (weak story) or Modeled (strong story), promotion path
  chartered, derivation-or-demotion clause armed.
- None passes AND the shape lemma excludes: FAMILY EXCLUSION at full
  volume -- countable orientation decoherence with 1/(N-1)-class
  decay is dead as a class, and the mechanism search moves to
  structure acquisition (the chromo profile, FND-103's C2 unblock).
- None passes but the lemma leaves an (A, c) window: the window is
  displayed as the surviving target for a future counting that lands
  in it; no member is promoted.
- No member may be modified after computing; the control D7 cannot be
  promoted regardless of its numbers.

## House

Bars locked before computing. Em dashes forbidden. Zero data contact
in derivations; B3's numbers are transcribed from FND-102's kept
display, used only as a bar.
