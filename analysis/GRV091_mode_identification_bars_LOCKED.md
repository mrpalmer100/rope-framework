# GRV-091 bars — LOCKED before computing (2026-08-03)

Commission (the coefficient campaign's likely decisive session): GRV-040's
mode identification -- the source of the committed omega_inf = 0.23 kappa --
re-derived against the ratchet mechanism (GRV-081..090).

The question, stated precisely before computing:
GRV-040 identified the whisper's OBSERVED FREQUENCY with the throat-fitting
mode of the near-horizon geometry (a kappa-scale, geometric identification).
The mechanism has since derived the shell's THERMODYNAMIC TEMPERATURE at
T_inf = C hbar kappa with C ~ 1e-5 (GRV-087..090). GRV-088 confronted the two
coefficients as rivals. Tonight decides among THREE pre-named verdicts:
- V1 (lineage convicted): the identification implicitly assumed a source hot
  enough to populate the throat window; with the derived T it fails, and the
  observable peak moves to the T-scale. The 0.23 comes down; case closes.
- V2 (mechanism convicted): the temperature chain missed kappa-scale physics.
- V3 (THE CONFRONTATION convicted -- category error): the emission is not a
  thermally OCCUPIED mode population but a train of DISCRETE IMPULSIVE
  EVENTS (GRV-085's own identity: re-formation runs the two ledgers backward,
  one snap at a time). An impulsive event rings the throat at its resonance
  REGARDLESS of the rate statistics; the spectral peak is then geometric
  (0.23 kappa, GRV-040 right) while the reservoir temperature governs RATES
  and occupancies (T_inf = C hbar kappa, GRV-087 right) -- and GRV-088
  compared a temperature coefficient to a ringing-frequency coefficient:
  different questions, no contradiction.

The machine content (the physics that decides):
- B1 (the impulsive-source theorem): for a damped resonator (the throat mode,
  resonance omega_r, quality Q) driven by a Poisson train of impulses at rate
  R, Campbell's theorem gives PSD(omega) = R A^2 |chi(omega)|^2 -- the
  emitted spectrum PEAKS AT omega_r INDEPENDENT OF R. Verify analytically
  (sympy: the shot-noise PSD) and numerically (simulate the Poisson-driven
  resonator; measured PSD peak within 5 percent of omega_r across TWO rates
  differing by 30x -- rate moves the amplitude, never the peak).
- B2 (the thermal contrast, so the distinction is observable, not verbal):
  a thermally OCCUPIED emitter at temperature T << hbar omega_r radiates at
  omega_r suppressed by exp(-hbar omega_r/T) -- evaluate the suppression at
  the mechanism's C (it is astronomically fatal) -- whereas the impulsive
  train pays NO Boltzmann factor at omega_r because each event carries
  e_bit = 35.4 W >> the soft thermal scale. The mechanism's OWN structure
  (discrete ledger reversals, energy e_bit per event) selects impulsive.
- B3 (the adjudication): apply the selected picture to the confrontation.
  Under V3, hbar omega_peak / T_res = 0.23/C ~ 3e3..5e4 is a LEGITIMATE
  ratio (an emitter of rare, energetic snaps -- crackling-noise class), not a
  contradiction. The residue is named honestly: whether each snap's energy
  e_bit matches the ring quantum hbar_med omega_r is the QUANTUM of the ring,
  which is the L1 frontier's territory, flagged not resolved.
- B4 (propagation, grammar fixed in advance): whichever verdict, GRV-088's
  tension is re-classified accordingly; the whisper lineage's observational
  numbers (strain, spectrum shape) and the mechanism's rate law are
  re-attributed to their proper questions; the campaign's score is stated;
  the document sweep (predictions paper, figures' in-court annotations) is
  named as the follow-up, not smuggled into tonight.

Rules: R1 no new coefficients evaluated; R2 verdicts V1/V2/V3 exhaust the
space and one must be selected on the machine results; R3 failures register
at full strength.
