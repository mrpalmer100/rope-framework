# Q-SWEEP STAGE 2b -- RESULTS: E-DIRECTION FIRED (Q = 2.515)
# Executed 2026-08-26 under analysis/QSWEEP_stage2b_charter_LOCKED
# .md (bars locked before the measurement script ran). Data: the
# stage-1 checkpoint's surviving gated states ONLY; zero new
# solves. Statistic, thresholds, and node treatment fixed in
# advance; nothing revised after the numbers.

## THE MEASUREMENT

pt-velocity V = RMS_nodes(delta pt mod 2pi) / delta s(free
sector), on the two surviving intervals per branch:

              V_ramp (m1 -> m2,        V_anchor (final arc pair
              matched A2 interval)     of each rate march)
  q = 4/3     0.00498  (ds 3.873)      0.01346  (ds 0.0576)
  q = 5/3     0.00446  (ds 4.170)      0.00479  (ds 0.0756)

  C-A (matched-interval ratio):  0.00498 / 0.00446 = 1.12
      -> SIGNATURE-ABSENT while both branches are healthy: the
      collapsing and flat cells wind at the same velocity over
      the identical A2 interval. (The built-in control.)
  C-B (within-branch anchor/ramp): 2.704 (4/3) vs 1.075 (5/3).
  DOUBLE RATIO Q = 2.704 / 1.075 = 2.515  >=  the locked line 2.
  ** VERDICT: E-DIRECTION. **

## READING (with FND-150)

FND-150 established that the winding field is load-bearing: pt
evolution is required for branch continuation. This measurement
adds the collapse-region signature: precisely where q4/3's
amplitude rate had fallen 5x, its winding velocity is elevated
2.7x over its own healthy baseline, while the flat branch's
winding velocity is unchanged (1.075) in its own anchor region.
On this data, THE COLLAPSE COINCIDES WITH ARCLENGTH ROTATING
FROM THE AMPLITUDE SECTOR INTO THE DIRECTION SECTOR: dA2/ds
falls as d(pt)/ds rises. The rationalization-selectivity of
S1-SPLIT then reads naturally as cell-dependent winding demand.

## HONEST SCOPE (as chartered)

- The anchor intervals sit at different A2 by necessity of what
  survived (4/3 near 0.0050, 5/3 near 0.0063); C-B is the
  within-branch normalized comparison and the verdict is "on
  this data".
- Two intervals per branch is a signature, not a profile. The
  quantitative question -- does V's rise ACCOUNT for the rate's
  fall (arclength conserved between sectors)? -- needs the full
  V(A2) profile from a re-march with per-step state retention.
- Per the charter: E-DIRECTION does NOT lift the FND-147
  interpretive freeze; it justifies chartering the profile
  experiment.

## STANDING

Draft registration: analysis/QSWEEP_stage2b_draft_registration.md
(FND-151 suggested), AWAITING THE AUTHOR. Follow-up experiment
(re-march with retention, the V(A2)-vs-rate ledger) awaiting the
author's charter word.
