# COMMISSION PE -- THE BRANCHING RATIO: RESULTS

Executed 2026-08-12 under analysis/PE_branching_ratio_bars_LOCKED.md.
Benchmark: benchmarks/foundations/pe_branching_ratio.py.

## Verdict (locked grammar): CONSISTENT-UNDERDETERMINED

The chain neither closes to a number nor breaks. p is now DERIVED IN
EXACT CLOSED FORM from registered structure; the sealed band converts to
a demanded crossing contrast strictly inside the registered inventory;
the NUC-030 falsifier does not fire; and the acquisition descends one
rung, from p to the contrast g.

## The derivation

Realization: reconnection = inter-strand transfer at one crossing.
From EM-RECON-025's registered relative-displacement coupling, the
symmetric channel passes the crossing FREE (the s/a terms cancel exactly;
this is the acoustic branch's gaplessness, exhibited not assumed), and
the antisymmetric channel scatters off a point pinning of Commission G's
registered contrast g. Exact result, unitarity verified symbolically:

    p(g, ka) = g^2 / (2 (g^2 + 4 (ka)^2))  =  x^2 / (2 (1 + x^2)),
    x = g/(2 ka)

Properties, all structural: amplitude-free (the unregistered amplitude
cancels in the probability ratio, so SCALE-001's amplitude-circularity
does not block it); athermal; local to one crossing (ELEC-043 honoured
by construction); monotone in g; and saturating at the TOPOLOGICAL
CEILING p -> 1/2 at infinite contrast (a hard node splits the
disturbance evenly). FND-071 predicted "an O(1) topological branching
moderated by a geometric acceptance"; the derived form IS exactly that,
with the acceptance being the contrast-to-wavenumber ratio.

## The confrontation

At the locked encounter scale ka = 1 (FND-071's own attempt kinematics,
k = 1/a), the sealed band p in [8.3e-04, 8.6e-03] inverts to

    g DEMANDED in [0.082, 0.265].

Registered inventory: floor g >= O(1e-2) (Commission G, G3); material
band E_x/(T0 a) in [0.019, 87] (FND-029). The demanded window sits
strictly inside the inventory and above the floor -- and is NOT pinned
by it (the material band spans 3.7 orders). Hence the middle verdict.

## Sensitivity, carried honestly

p at fixed g swings ~100x over the reported ka sweep [0.3, 3], so the
ka = 1 lock is load-bearing. It was locked from FND-071's kinematics
before any number was computed, and the sweep is reported so a future
commission that derives the encounter spectrum can re-evaluate without
this claim's choice binding it. Under the sweep the demanded g-window
moves within roughly [0.03, 0.8] at the extremes -- still inside the
material band at every ka in the sweep.

## The structural observation (displayed, not adopted)

The acquisition chain now reads: v0 (sealed, NUC-030) -> p (derived in
form, this claim) -> g (a material contrast) -> and FND-029's import
already ties the material ratio's decision to the constituent width w.
This is the THIRD independent census to converge on w (the hbar branch
via QGATE-004, matter stability via EM-RECON-017/018, now the
pairing/reconnection chain via PE). Three sectors, one unknown. The
tripwire extends: any future claim fixing w feeds through all three.

## What fires when

- If a future g determination (via w, or directly) lands in
  [0.082, 0.265] at ka = 1: the chain closes end-to-end and the
  adoption is crowned.
- If it lands outside: NUC-030 returns to adjudication exactly as its
  falsifier demands, with this claim's closed form the conviction
  instrument.
