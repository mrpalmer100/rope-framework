# GRV-086 bars — LOCKED before computation (2026-08-02)

Commission (GRV-085's named coefficient): m* -- the quadratic-mode count per
broken crossing, the divisor in T = e_bit/m*, and the last coefficient between
the corpus and an absolute shell temperature.

The count, registry-anchored, recorded before computing (rule: every inclusion
and exclusion must cite its source claim):
- IN: each strand's TWO transverse displacement polarizations -- the wave
  channel, the registered operator's field content (GRV-029). Two strands meet
  at a crossing: 2 strands x 2 polarizations x (kinetic + potential) =
  n_q = 8 quadratic terms per crossing cell.
- OUT: the azimuth/twist -- the medium's ONE internal Goldstone is the EM
  channel (GRV-020), a propagating shared field, not a local heat pocket;
  excluded WITH FLAG (its inclusion would give n_q = 12 and is carried as the
  bracket's upper edge).
- OUT, by theorem: the fiber -- holonomy with no energy coupling (QB-029's FDT
  argument, reused across sectors): a variable with no Hamiltonian cannot store
  heat.
- P-CELL (premise, named): the deposited energy is shared per crossing cell
  before hand-off to neighbouring cells (the n_x ~ 1/a^3 grammar); m* counts
  one cell's content.
- THE CONVENTION: equipartition gives T/2 per quadratic term, so
  T = 2 e_bit/n_q = e_bit/m* with m* = n_q/2 = 4 (transverse-only) to 6
  (twist included) -- the pre-committed bracket.

The engine leg (the principle verified, not assumed):
- A minimal crossing cell: two chains (over/under strand segments, one
  polarization each -- polarizations multiply trivially and the simulation
  verifies the PRINCIPLE), fixed ends, weak corpus-native anharmonicity (the
  sine potential's quartic term) to mix modes; energy deposited at the junction
  as a break does it; conservative evolution; the time-averaged energy must
  EQUIPARTITION across the enclosed quadratic terms.
- Bars: energy conservation < 1% over the run; late-time per-site energies flat
  across both chains within 30% (thermalization on a small anharmonic cell is
  not precision equipartition; the bar says so in advance).

Consequences to assemble (structure, no confrontation):
- T = e_bit/m* with m* = 4-6 measured-bracketed; with beta = 15.67 (GRV-082),
  W/T = m*/beta = 0.26-0.38 -- the emission Boltzmann factor is UNSUPPRESSED
  (e^{-W/T} ~ 0.7-0.8), consistent with GRV-085's hot-shell finding, and
  sigma-independent by the cancellation theorem.

Rules: R1 no constant beyond the count is evaluated; R2 the twist bracket is
carried, not resolved; R3 failures register at full strength.
