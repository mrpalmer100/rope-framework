# COMMISSION SHIN7 -- THE BENDING-COST PRICING (bars LOCKED)

Locked 2026-08-12, before computing. Tier T3, GRANT-SUBSTRUCTURE-TIGHT
debt 4. kb is UNSCALED and stays unscaled: the deliverable is not a
value but a PRICED CONSTRAINT -- the registered bound kb must satisfy
for the grant to survive, evaluated at the worst case, plus a
plausibility reading against the corpus's standing slenderness
picture (reported, not barred).

## Locked inputs (verbatim sources)
- Margins: Sigma-spend budget 6.1x (kappa50) / 10.5x (kappa250)
  (SHIN3 W-b, FND-040 readings). Winding compensation spends
  1/sin^2(psi_eff) = 4.046x (FND-088 D3). REMAINING MULTIPLICATIVE
  HEADROOM: 6.1/4.046 = 1.5077 (tight floor governs; the bar is the
  tight floor). Allowed additional fractional spend eps = 0.5077.
- Derived angles (FND-088/090): sin(2 psi_1) = 2 sqrt2 / 3,
  sin(2 psi_2) = 2 sqrt(v(1-v)), v = (15 + 2 sqrt30)/35.
- Helix curvature closed form: kappa = (pi/p) sin(2 psi) for pitch p
  and pitch angle psi (from the transverse plane); levels add in the
  worst case, kappa_tot = kappa_1 + kappa_2 (upper bound, locked as
  the conservative convention).
- Worst-case pitch: the regime a_f < p <= lambda/4 pins p -> a_f at
  the shortest carried wavelength (lambda_min = 4 a_f makes
  lambda/4 = a_f); the pricing is evaluated at p = a_f for BOTH
  levels (most expensive point; longer wavelengths cost strictly
  less, kappa ~ 1/p).

## The two prices (both against eps, the conservative convention
## disclosed: the dispersive shift is charged to the same Sigma
## headroom)
P1 static: bending energy per length vs compensated tension,
  R_E = kb kappa_tot^2 / (2 T_f), T_f = 4.046 T0_f.
  BAR: R_E <= eps.
P2 dispersive: fractional speed shift at lambda_min,
  delta = kb k^2 / (2 T_f), k = 2 pi / lambda_min = pi/(2 a_f).
  BAR: delta <= eps.
THE REGISTERED BOUND is the tighter of the two, expressed as
kb <= C x T0_f a_f^2 with C in closed form and numerically.

## Plausibility reading (reported, not barred)
String-like rods carry kb ~ T0_f r_s^2 (r_s the strand radius); the
bound then reads (r_s/a_f)^2 <= C x 4.046, a slenderness condition.
The corpus's standing picture (spacing-separated fibers) is either
inside it or not; the answer is REPORTED either way.

## Verdict grammar, pre-authorized
- Bound derived, slenderness condition consistent with the standing
  picture -> debt 4 converts to a REGISTERED PRICED CONSTRAINT;
  Modeled claim; kb stays unscaled.
- Bound derived but the slenderness condition CONTRADICTS a
  registered corpus statement -> the tension is FILED against
  FND-087, disclosed, nothing rescued.
No bar-shopping; eps, the worst-case convention, and both price
forms are final.
