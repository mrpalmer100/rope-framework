# THE Lambda_nat PRICING SESSION (NUN-GRV16) -- RESULTS (2026-08-16)

Bars: analysis/NUNGRV16_lambdanat_price_bars_LOCKED.md. Benchmark:
benchmarks/gravity/lambdanat_price_nungrv16.py (arithmetic +
assertions, every factor sourced).

VERDICT: NUMERATOR-PRICED; DENOMINATOR-BLOCKED-AT (two named walls);
STRUCTURAL-CONSONANCE AFFIRMED; one flagged resemblance held at
display volume and one numerology refused outright.

## 1. THE NUMERATOR, PRICED (coarse level, all registered)
tau0 in [6.17e18, 1.07e19] /m (EM-RECON-040's exact inversion at the
registered r = 9.35e-20 m; enters DIRECTLY, no beta gate needed --
verified). Both slot forms carried pending kappa_conv:
  slot A (per-strand):  C tau0          in [2.60e-17, 4.50e-17] J
  slot B (modulus):     gamma_grav tau0 in [2.60e15, 4.50e15] J/m^2
Riders inherited on the face: k/T0 = 2 (FND-114's
alpha-chain-floor conditionality); gamma_lock ~2-4; LEVEL
ASSIGNMENT = COARSE, registered as a named residue (GRV-111 derived
on mesh strands, GRV-112 quoted fine-level |tau|; never adjudicated
against each other; this session does not resolve it).

## 2. THE DENOMINATOR, BLOCKED AT TWO NAMED WALLS
(i) kappa_conv: the operator normalization mapping the granted L_C3
coefficient into GRV-120's dipole slot is unregistered bookkeeping;
the dimensional exhibit shows exactly what it must supply (inverse
kg^2 m^2/s^3 for slot A; inverse kg^2/s^3 for slot B).
(ii) THE INDUCED-STRENGTH WALL: C_m routes through the registered
~40-order mass-to-strain suppression (GRV-005: "LOCATED not
solved"; FND-MATTER-047's trichotomy). THE TRACE, stated per the
bars: Lambda_nat's remaining question IS gravity's own strength
question. One wall, two sectors. Frame dragging's amplitude has
been traced to the programme's oldest open problem, and will move
exactly when it moves.

## 3. STRUCTURAL CONSONANCE, affirmed at its ceiling
The registered suppression ledger is ASYMMETRIC: the twist source
couples BY IDENTITY (beta_J = 1, "no lambda and no g_0i anywhere in
the leg") while the mass source is ~40-order suppressed. So
Lambda_nat = UNSUPPRESSED / SUPPRESSED is structurally LARGE. The
demand Lambda_nat >= ~4-6e18 is therefore CONSONANT with the
registered structure: a hierarchy-enhanced natural amplitude tamed
back to Lambda ~ 1 by a ~1e-19 chirality fraction is the shape the
ledger produces on its own. Ceiling of the claim: PLAUSIBLE, NOT
DERIVED.

## 4. THE FLAGGED RESEMBLANCE (display volume, nothing built on it)
The raw pre-normalization magnitude of the target formula,
num_A c^3/(2 G), evaluates to [5.3e18, 9.1e18] -- landing on the
demanded ~4-6.2e18 window -- BUT it carries units (kg^2 m^2/s^3
class) and becomes Lambda_nat only if kappa_conv is O(1) in exactly
the dimension-clearing sense. Per the standing rule this is
FLAGGED, held at display volume, and decided by the normalization
session alone: if kappa_conv prices O(1), the coarse registered
mechanics lands the frame-dragging amplitude INSIDE the demanded
window with no tuning; if it prices far from O(1), the resemblance
was clothing. Either way the deciding session is now named and
cheap.

REFUSED OUTRIGHT (numerology class, GRV-113 precedent): the
resemblance "19 orders ~ half the 40-order hierarchy."

## WHAT THE VISE LOOKS LIKE NOW
beta (polarimetry) -> chi. LARES-class -> chi Lambda_nat. Coarse
mechanics -> Lambda_nat's numerator priced, denominator = kappa_conv
x the induced strength. The single cheapest decisive move left in
the sector: THE kappa_conv NORMALIZATION SESSION -- pure
bookkeeping, one careful pass through the two registered operator
conventions, and it decides whether the flagged resemblance is
physics.

## REFUSALS HONORED
No fine-scale numerics; no kappa_conv value assumed; consonance
capped at plausible; numerology refused; condition 4 unchanged;
riders quoted.
