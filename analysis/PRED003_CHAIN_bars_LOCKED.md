# PRED-003-CHAIN bars — LOCKED before computation (2026-08-02)

Commission (PRED-003-CONST's outstanding obligation): DERIVE alpha =
2 lambda T^2 a/(kappa hbar c) from the EM energy coefficient — i.e. derive the Coulomb
coefficient e_eff^2/(4 pi eps0) = 2 lambda J a from registered mechanics — or fail and
register the failure with its exact location.

The candidate route available to a single session, stated before computing:
NAIVE PHASE ELECTROSTATICS. The locked field Phi has coarse-grained gradient energy
density (K/2)(grad Phi)^2 with K = J/a (FND-001, benchmark-verified). Charge is the
winding of Phi (GG-006). If a unit charge sourced Phi like a point flux, the standard
cross-term energy would give a 1/r interaction with coefficient ~ K, and the chain
would close in one line. The session must test whether that route is available AT ALL
before any coefficient is extracted.

Rules fixed in advance:
- R1: NO CONFABULATION. If the naive route fails, no substitute mechanism may be
  invented in-session; the failure is registered with a specification of what a
  completing computation must produce, on machinery the corpus actually has.
- R2: The failure mode suspected from dimensional reconnaissance must be tested
  honestly with the possibility of surprise: winding number is a LINE CIRCULATION
  (loop integral of grad Phi, dimensionless), not a SURFACE FLUX (which would carry
  units of length). If that suspicion is wrong — if the lattice yields 1/r — the
  session registers the success instead.
- R3: The lattice test is decided by pre-committed fit comparison: interaction energy
  E(d) of a unit winding-antiwinding pair fitted against ln(d) and against 1/d;
  whichever fit has R^2 higher by >= 0.05 wins; closer than that is UNRESOLVED and
  registered as such.
- R4: Whatever the verdict, PRED-003's provenance grade is updated to match: the
  alpha form is (a) derived, (b) blocked-with-specification, or (c) unresolved. No
  tier motion this session in either direction.

Bars:
- B1 (dimensional theorem): show that for the phase field, the unit charge is
  circulation (loop integral = 2 pi N, dimensionless) while a Gauss-law point source
  of grad Phi requires a flux with units of LENGTH — the naive point-Coulomb route is
  dimensionally unavailable to a winding charge. Machine-checked.
- B2 (analytic form): for two parallel unit winding lines, the gradient-energy
  cross-term is LOGARITHMIC in separation (per unit length ~ 2 pi K ln), not 1/r.
  Derived symbolically.
- B3 (lattice confrontation, rule R3): superposed vortex-antivortex phase fields on a
  2D grid; E(d) computed for a ladder of separations; log vs 1/d fits compared by the
  locked rule. Expected: log wins decisively. Registered whichever way it falls.
- B4 (the specification, rule R1): state exactly what the completing computation must
  produce — the corpus's REGISTERED Maxwell sector (Chern-Weil two-form; the one-metric
  photon dictionary) must map the unit linking number to a source strength q_s with
  q_s^2 = 4 pi eps0 x 2 lambda J a. This converts "derive alpha" into a bounded
  computation on existing machinery, with lambda falling out of the same step.
- B5 (propagation): the predictions paper's "whose chain is derived" language and
  PRED-003's provenance list updated per R4; PRED-003-CONST's premise annotated with
  the verdict; no rescue, no softening.
