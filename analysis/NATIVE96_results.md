# COMMISSION NATIVE-96 -- SESSION RESULTS (2026-08-20)

*Executed under analysis/NATIVE96_bars_LOCKED.md (locked before
computing, zero amendments from the draft). Benchmark:
benchmarks/foundations/native96_continuation.py. Probe:
benchmarks/foundations/native96_probe94.py. Registration state: FND-143
registered (v3.27.3) before this commission's first computation;
FND-144 GRANTED and registered at session-2 close (v3.27.4) with the
round-38 confirmation partial folded in.*

## VERDICT SHEET

1. THE INSTRUMENT IS PROVEN NATIVELY. Control (i): level-1 recovered
   at 96 x 36 (theta constant to 3.3e-10, value 5.4e-6 from 0.955317,
   Om1 rel 9.8e-6, T uniform to 6.4e-10). Control (0*): the native
   members at the first two shared pins agree with the FND-142
   members regenerated by stage 2's own code at 64 x 24 (both to
   machine grade there) at 7.9e-5 / 7.7e-5 relative in gamma and
   6.1e-5 / 1.4e-4 in Om2 -- thirtyfold-plus inside the 5e-3 bar and
   matching the scout's measured resolution drift grade. Partial
   2-of-3 discharge; the third pin's comparison is subsumed by
   PROBE-94 below.

2. TWO NATIVE MEMBERS REGISTERED, FULL GATES, CROSS-CONFIRMED.
   Member 0: A2 = 0.0018792 (pin to 7.0e-11), RMS 5.8e-9, closure
   1.5e-8, gamma = 0.57718, Om2 = 3.20082; 112 x 42 confirmation
   PASS (re-solve 1.8e-9; drift gamma 9.0e-6, Om2 6.7e-6, A2
   1.4e-8). Member 1: A2 = 0.0046979 (pin to 1.1e-10), RMS 5.6e-10,
   closure 4.8e-9, gamma = 0.57599, Om2 = 3.27294; 112 x 42
   confirmation PASS at machine grade (re-solve 1.2e-12; drift gamma
   8.8e-6, Om2 1.6e-5, A2 4.7e-10). The benign-tail observation:
   wsNyq ~ 5e-6 at 96 x 36 on both members collapses to ~1e-9 at
   112 x 42 -- grid-scale content, not incipient aliasing, and the
   trigger level of 1e-8 is over-sensitive by design; the
   confirmations it forces are bonus verification.

3. THE MEMBER-GRADE RATE MEASUREMENT (the session's central physics
   record). Approaching the third pin by the commission's own arc
   instrument (the a2-pinned solve exhibits the registered
   near-singularity: 64 objective-monotone rounds stalling at field
   RMS 3.6e-5), the branch's amplitude growth per unit arclength is
   MEASURED AT FULL MEMBER GRADE:
       dA2/ds ~ 6.5e-4 at A2 ~ 0.0048
       dA2/ds ~ 7.8e-5 at A2 ~ 0.0062
       dA2/ds ~ 6.8e-5 at A2 ~ 0.0063, still gently decaying
   with gamma FROZEN at 0.5746-0.5745, Om2 climbing ~3e-4 per step
   (3.343 at A2 = 0.0063), min z' healthy (+0.547), and the control
   (vi) W0-margin declining slowly (1.243 -> 1.167 -> 1.093; the
   operative resonance is 1:1, Om2 -> Om1, still distant). The
   arclength goes almost entirely into Om2 and field reorganization.
   An eightfold rate collapse INSIDE what the 64 x 24 record called
   the mild regime. At the measured decaying rate the third shared
   pin is 500-1000 arc steps from the head of the march; the walk is
   CI-scale and its checkpoint is exported (see ARTIFACTS).

4. PROBE-94 (the bounded discriminating experiment; procedure = the
   registered FND-143 deviation verbatim, 64 -> 96 direction).
   (a) The 64 x 24 member at the third shared pin regenerates
   INSTANTLY by stage 2's own code (RMS 6.4e-15): gamma = 0.569652,
   Om2 = 3.52702.
   (b) Its interpolation image at 96 x 36 carries field RMS 8.2e-2 --
   larger than any steepened member the scout measured.
   (c) The arc-anchored native re-solve is a 51-round fight through
   the same circulation the march's pinned solve showed, then SNAPS
   to machine grade: RMS 2.7e-10, closure 9.5e-9 (PASSES the
   halt-grade bar). THE BRANCH EXISTS NATIVELY AT THE PIN
   NEIGHBORHOOD: A2 = 0.0094647, gamma = 0.56950, Om2 = 3.53805.
   (d) The 64-grid member is DISPLACED ALONG THE BRANCH, not
   fabricated: drift is single-coordinate -- A2 +7.34e-3 relative
   (OUT of the 5e-3 gate) with gamma 2.62e-4 and Om2 3.13e-3
   in-gate. An order milder than the 2-7% multi-coordinate steepened
   contamination. The strict gate verdict is (b): the 64-grid member
   does not re-solve within the gates. The interpretive record is
   the displacement.
   (e) THE THIRD TELL, UNADJUDICATED: the landed native state
   carries wsNyq = 2.6e-3 at 96 x 36 (five hundredfold the mild
   members) and its own interpolation image at 112 x 42 carries
   field RMS 5.0e-1. The bars' tail trigger armed; the owed 112 x 42
   confirmation ran BUDGET-BOXED PARTIAL: 5.0e-1 -> 2.0e-4 over six
   rounds (capped-lsmr descent then f32-J exact GN), still
   descending, NO VERDICT. If it eventually confirms, the
   displacement finding is citable as stated; if it fails, the
   exhaustion is PROGRESSIVE ACROSS GRIDS -- each grid runs out a
   little further along the branch -- which would be the strongest
   form of the FND-143 method lesson. THE CONFIRMATION IS OWED and
   resumes from the exported checkpoint. SESSION-2 UPDATE
   (2026-08-21): fed to round 38 of a raised 80-round cap under the
   alternating schedule (capped-lsmr descent with f32-J exact GN;
   eight pure exact-GN rounds had circulated 2.0e-4 -> 2.6e-4 before
   the alternation broke it at round 10). Floor trajectory 1.1e-4 ->
   1.8e-5, contracting ~7-8% per pair WITHOUT decay; exact-GN bounces
   shrinking 1.2e-4 -> 3.4e-5 -- the pre-snap signature the 96-grid
   fight showed before its five-round, five-order collapse. STILL NO
   VERDICT; the uncapped finish belongs to CI.

5. WHAT THIS SESSION IS AND IS NOT ALLOWED TO CONCLUDE. The
   chartered binary (branch reaches R2 = 0.09396 or asymptotes) is
   NOT answered: outcome is RATES-REGISTERED under bars delta 6. The
   coherent but UNGRANTED reading of the assembled evidence: the
   64 x 24 contamination boundary sits near A2 ~ 0.006 (displacement
   grade) rather than gamma = 0.558 (fabrication grade); the native
   branch continues past the third pin but at collapsed and
   decaying dA2/ds; and R2 sits 10x beyond the third pin, so the
   asymptote scenario LEADS -- with the q = 3/2 rationalization the
   standing named suspect -- pending the owed confirmation and a
   CI-scale walk. Sigma_wave remains NOT re-priced; the FND-139
   rider stands; the FND-135 box is not superseded.

## THE INSTRUMENT RECORD (all annotated at their sites)

Fixes: the degenerate level-1 seeding of the first pinned solve (the
|c2| pin row is subgradient at the symmetry point; stage 2's own k2
injection replicated); the pin gate (|A2 - pin| < 1e-8; the field-RMS
bar does not see pin rows); ramp members discharging owed
confirmations; the f32-Jacobian store for every 112 x 42 build (three
OOM incidents mapped: f64 J + normal matrix + cache coexistence, the
ladder/cache eviction rule, the f32-LM normal matrix at n = 14114).
Scheduling forks: foreground ~300 s chunks with a 100 s margin
(background processes die at the tool-call boundary); rounds cap
28 -> 80 -> 160 (a cap is a budget, not a bar); the measured-crawl
escalation (exact-GN, then alternating with capped-lsmr descent,
invoked from the f64 band on a <25%-per-3-rounds trigger).
INCIDENT KEPT ON THE RECORD: the waypoint-tolerance episode
(3e-8/5e-8 acceptance for approach seeds, A2 0.0059-0.00628). Its
motivating hypothesis -- that the observed dA2/ds collapse was
solver-level error hiding in the slow subspace -- was FALSIFIED by
member-grade re-measurement after a full re-anchor: the collapse is
real. The tolerance is retired anyway; rate measurements cite
full-bar states only.

## ARTIFACTS

- analysis/native96_march_ckpt.pkl -- the march head (A2 = 0.0063277,
  member-grade, tangent pair included); resumes the approach/walk.
- analysis/probe94_ckpt.pkl -- PROBE-94 state incl. the landed 96
  member, the 64 member, and the 112 x 42 confirmation mid-solve
  (round 6, RMS 2.0e-4).
- Restore either to /tmp/<n96|p94>_ckpt.pkl and rerun the benchmark
  with --budget to resume.

## SESSION 3 ADDENDUM (2026-08-21): THE CONFIRMATION ADJUDICATED

The owed 112 x 42 confirmation of the PROBE-94 landed state COMPLETED
at round 58 and FAILED ON THE GATES, converged.

The snap came as read: floor contraction accelerated from the ~7-8%
per pair of session 2 to 15% -> 27% -> 30% -> 37% -> 57% per pair over
rounds 42-52, exact-GN stopped bouncing and DESCENDED for the first
time in the fight at round 53 (1.5e-6 -> 1.2e-6), and the solve
collapsed five orders in nine rounds to land at:

    RMS 1.86e-9 (bar 1e-8, PASSED)   closure 6.2e-07 (halt bar 1e-6, passed)
    A2 = 0.0093937   Om2 = 3.516758   gamma = 0.563755
    min z' = +0.4332  min sin th = 0.5699  W0-margin = 0.909  wsNyq = 8.0e-4

Against the 96 x 36 landed state, every drift gate FAILED, and the
failure is MULTI-coordinate:

    gamma  1.01e-2  OUT (gate 5e-3; 2x)
    Om2    6.02e-3  OUT
    A2     7.51e-3  OUT

VERDICT (instrument-written): the 96-grid landed state DOES NOT
confirm -- PROGRESSIVE resolution exhaustion across grids. The
pattern: 64 -> 96 drifted in A2 ALONE (7.34e-3, gamma and Om2
in-gate); 96 -> 112 drifts in ALL THREE. Refinement made it worse.
Corroborating and independent of the drift gates: wsNyq fell only
2.6e-3 -> 8.0e-4 (3x) under 1.4x refinement per axis, where genuine
grid-scale content collapses like the mild members' did (5e-6 ->
1e-9). The third-pin neighborhood state is not grid-converged at any
resolution tested; the third pin is NOT a usable anchor.

W0-margin note, standing: 1.243 -> 1.167 -> 1.093 across the march,
0.909 at the 112 x 42 landing -- it has CROSSED BELOW ONE. Whether
that is resonance approach, a fold, or chart conditioning is exactly
what the smallest-singular-value diagnostic (external review, step 3;
adopted into the queue) is for. Not adjudicated here.

INSTRUMENT LESSON KEPT: the pre-snap signature (shrinking exact-GN
bounces, accelerating floor contraction) predicted the snap
CORRECTLY and predicted NOTHING about the landing. It forecasts
convergence, never correctness. It is a scheduling signal, not
evidence.

Scheduling reversal, measured this session: setsid-detached runs
SURVIVE the tool-call boundary (the registered note said otherwise);
per-round checkpointing added; the no-verdict guard kept an 80-round
budget from adjudicating as a bar at round 42; two container OOM
reaps (rounds 42 and 57, silent, tracebackless) cost one round each
against the per-round save. A detached stage-D run is
MEMORY-EXCLUSIVE while live.

Registered as FND-145; FND-144's displacement reading amended at its
site. analysis/probe94_ckpt.pkl updated to the adjudicated round-58
state (conf_verdict, conf_metrics, and the round-40..58 floor
trajectory included).
