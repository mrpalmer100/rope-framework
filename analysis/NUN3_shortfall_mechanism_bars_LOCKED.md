# COMMISSION NUN3 -- BARS, LOCKED BEFORE COMPUTING

*2026-08-13. Candidate claim FND-103. The mechanism commission
FND-102 chartered: predict the binding shortfall BLIND from
registered structure. Author-directed session.*

## The two-number blind target (from FND-102, fixed)

T1: b/b_CS at SU(6) k=2 = 0.865(33).
T2: asymptotic 1/N coefficient ratio = 0.64(10) (measured 1.28(19)
    against derived 2).
Acceptance per candidate: BOTH numbers within 2 sigma, derived with
zero data contact and zero fitted parameters. One number in, one
out: PARTIAL, displayed, not adopted. Neither: MISS, registered.
A candidate whose required input is not derivable from registered
structure: BLOCKED, with the missing structure named (not estimated,
not fitted).

## The three candidate classes (from FND-101/102, fixed; no others
## may be added mid-session)

C1 ORIENTATION WEIGHTING: AYIN's v_B = 2/(N-1) counts two exchange
   orientations at equal weight. The candidate: only the symmetric
   (binding-coherent) combination contributes; v = 1/(N-1).
   Predictions computable exactly: b/b_CS = 1/2 at all N, coefficient
   ratio 1/2. NOTE AT LOCK: this lands between AYIN's statistics A
   and B only asymptotically for T2; T1 is a clean parameter-free
   number either way.
C2 PARTIAL-OVERLAP SUPPRESSION: exchange amplitude proportional to
   the cross-section overlap fraction f of loosely bound tubes;
   b/b_CS = f. RULE: f must be DERIVED from registered tube-profile
   and binding structure with no self-consistency shortcut that
   inputs the measured binding. If no registered claim supplies the
   profile-overlap relation, C2 is BLOCKED and the missing carrier
   is named.
C3 SOFTENING-ON-EXCHANGE: the derived quartic (c4 = -T0/8, FND-040
   family) suppresses the exchange amplitude at the operative strain.
   RULE: the operative strain is the REGISTERED tube-strain domain
   x <= 0.04 (FND-052's licensed domain); the suppression is the
   quartic-to-quadratic ratio at that strain, computed, not chosen.

## Pre-committed verdict grammar

- Any candidate passing both targets: name it the surviving
  mechanism, Modeled, and charter its promotion path.
- All candidates MISS or BLOCKED: register the elimination at full
  volume (Modeled elimination claim; misses displayed with sigma);
  the shortfall joins the corpus's named unexplained numbers with its
  wall shaped, and the next instrument class is the author's call.
- No candidate may be adjusted after its number is computed; no new
  candidate may be introduced after this lock.

## Display items (no verdict)

- The A-vs-B bracket: AYIN's statistic A gives b/b_CS = (N-1)/(2N)
  and coefficient ratio 1/2; B gives 1 and 1. Where the measured pair
  (0.865, 0.64) sits relative to the bracket is displayed as
  structure for the next commission.
- The crossover reading: the source paper's own bound-state vs
  weakly-interacting-pair transition (l_c sqrt(sigma) ~ sqrt(N)) is
  displayed as a caveat on T2's cleanliness, NOT used to soften any
  verdict (AYIN's rule: the corpus does not lean on data ambiguity).

## House

Bars locked before computing. Em dashes forbidden. Stale tripwires
honored. Zero data contact in every derivation branch.
