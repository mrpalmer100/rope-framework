# COMMISSION TAU-IDENT (NUN-GRV21) -- BARS LOCKED (2026-08-16)

Locked before computing. Chartered on the author's challenge to
GRV-127: the demoted Lambda_nat ~ 1e37 / chi ~ 1e-37 result was set
aside on registry grounds (object transplant), never adjudicated on
physics grounds. Either the landing was coincidence or it is the
answer; this session decides which, without letting the landing
steer the derivation.

## B0 -- CLEAN-ROOM ORDERING (the anti-temptation bar)
The derivation legs run FIRST, from registered text and registered
numbers only. The comparison to the demoted transplant values
([2.6, 4.5]e-17 J; 1e37; 1e-37) is a SEPARATE final leg, opened
only after the derived quantities are fixed. No intermediate step
may reference the transplant numbers.

## B1 -- THE IDENTITY QUESTION (reading, verbatim)
Does registered text give the vacuum strand a COARSE-level frame
torsion (winding referenced to the strand radius r = d_c/2)?
Registered inputs: FND-083 (two-level winding, pitch pairs, guided
path); FND-086 (pitch p in [a_f, lambda/4], worst case p = a_f);
FND-091 (kappa = (pi/p) sin 2psi at the derived angles, values
2.962/a_f and 2.751/a_f; slenderness r_s/a_f <= 0.355); GRV-112
(|tau_1| = 4.1888/a_f). Outcomes: COARSE-TORSION-FOUND /
DISTINCT-CERTIFIED (no coarse winding registered; GRV-127 upheld).

## B2 -- THE FINE-ROUTE COMPUTATION (the session's real work)
Whatever B1 returns, compute what the REGISTERED fine structure
implies for lambda directly -- the computation GRV-113 gated and
nobody has run with the SHIN numbers now on the books:
- per-sub-strand (G I_p)_f from the GRV-073 chain mirrored at fine
  level: E_f = k_f/(pi r_s^2), G_f = E_f/(2(1+nu)) at the SAME
  registered nu, I_p = pi r_s^4/2, with k_f/T0_f = 8.091 (DERIVED,
  EM-RECON-032) and T0_f = T0/n_sub (redistribution, FND-083);
- times the registered |tau_1| = 4.1888/a_f;
- summed over the n_sub sub-strands of one coarse strand at
  COHERENT handedness (the maximizing case -- any intra-strand
  handedness mixing only lowers it, so the result is an UPPER
  BOUND);
- r_s eliminated via the registered slenderness CEILING
  r_s <= 0.355 a_f (upper bound again);
- a_f eliminated via its registered CEILING a_f <= lambda_PeV/4
  (FND-086 over-resolution at the 1.400 PeV FND-083 ceiling).
Pre-committed outcome classes: FINE-BOUND-DERIVED (an upper bound
on lambda and hence Lambda_nat in registered numbers, with every
cancellation exhibited) / FINE-BLOCKED-AT (named input).

## B3 -- THE COMPARISON LEG (last)
Only after B2 is fixed: state the relation to the demoted transplant
band; adjudicate the COINCIDENCE question (same object? same class?
accident?); state what survives of, or replaces, the exclusion and
the chi prediction -- as BOUNDS with named gates, never as the
resurrected point values unless B1 returned COARSE-TORSION-FOUND.

## B4 -- REFUSALS
- No grant proposed here; if B1 finds the identification is
  grant-class, it goes to the author's desk as a charter, not a
  result.
- The m/(m, n_sub) suspension (FND-110) is not touched; a_f's
  ceiling is used AS a ceiling only.
- Condition 4 unchanged; no LARES verdict; chi statements are
  demands/bounds, not measurements.
- Intra-strand handedness coherence is TAKEN as the bounding case,
  not asserted as fact; the assumption is on the face.
- All riders inherited (k/T0 = 2 via FND-114; nu from GRV-073's
  chain; R_0 = 1 from GRV-126; T-branch closure from GRV-125's
  surviving reading).
