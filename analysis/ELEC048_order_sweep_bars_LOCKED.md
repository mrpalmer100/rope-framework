# ELEC-048 — The order sweep, the ledger collision, and the route's second death: locked bars

## Commission
ELEC-047 left one survivor: the pre-correlated-medium postulate, needing
persistent order over >= 12.6 w (~498 strands). This sweep asks whether ANY
registered mechanism supplies it. Discovered during preparation, and governing
everything below: the corpus already contains an HBAR campaign (HBAR-001..010,
v2.5.0, cut BEFORE the ELEC-044..047 arc) that superseded the reconnection
route by a standing-wave derivation -- and ELEC-044..047 did not cite it.
Also discovered: a TWO-LEDGER discrepancy in w (HBAR-005: 0.0578 fm from the
registered vacuum density 5.67e18; ELEC-040/044-047: 0.287 fm from nuclear
density). Both must be resolved in the open, per the registry-repair precedent.

## Locked bars
B1 (the collision, filed first). The cross-reference failure is recorded as a
   process fault of the ELEC-044..047 sessions (this auditor's), with
   annotations filed on all four claims. The CONTENT is then assessed: do the
   arcs contradict or confirm? (Assessment criteria: HBAR-001 asserted
   supersession by argument; ELEC-045/047 killed the reconnection route by
   derivation and census. Confirmation = independent kills of the same route.)

B2 (the ledger). Recompute the arc's three headline numbers (W1/hbar, n_t
   demand, R_req and the recruitment shortfall) at BOTH registered w values.
   The verdict of ELEC-047 must be tested for robustness: does the chain's
   death survive the ledger choice? Report both columns; neither is promoted
   until the w discrepancy is adjudicated (named as an open item, not decided
   here).

B3 (THE SWEEP). Each registered candidate mechanism is evaluated for the
   persistent correlation length it supplies, in units of w:
   (a) weave reservoir / substrate modes (FND-STRAND-007/008): the bath is
       GAPPED (omega_min = 1, kt = 0.64 registered); correlation length
       xi = sqrt(kt)/omega_min spacings, computed from the engine's own
       dispersion by direct correlation-function measurement, not just the
       formula.
   (b) GRV-035 horizon percolation: criticality (divergent xi) exists only at
       connectivity collapse; the ordinary vacuum's distance from threshold is
       stated from the registered f_c = 0.309 vs the percolation point.
   (c) HBAR-002's inter-strand coupling: supplies coherence CAPABILITY at all
       scales (c_perp = c/2) -- but for the reconnection route the question is
       whether OCCUPIED coherent modes of extent >= 12.6 w are generically
       present at an event site within the event duration ~2 w/c. Evaluate:
       can c_perp = c/2 transport build 12.6 w of correlation in 2 w/c? (No,
       by inspection; compute the reachable radius.) Can a PRE-EXISTING gapped
       thermal bath hold it? (Governed by (a).)
   (d) HBAR-007's equation of state: registered UNSTABLE (1e12 fine-tuning);
       disqualified as stated, no recomputation.
   PASS/FAIL per candidate: supplies xi >= 12.6 w persistent, yes or no.

B4 (the verdict). If no candidate passes B3, the pre-correlated-medium escape
   is registered FAILED for the reconnection route, the route is dead at BOTH
   joints (recruitment, ELEC-047; pre-correlation, here), and the corpus's
   quantum-action frontier is formally consolidated onto the standing-wave
   branch (HBAR campaign), whose own open problem is SCALE SELECTION
   (HBAR-002's negative), not coherence. If any candidate passes, the
   postulate survives with that mechanism named and the next computation is
   its occupation statistics.

B5 (honesty). The sweep is over REGISTERED mechanisms; absence from the
   registry is not absence from nature. The verdict binds the corpus's
   internal ledger only, and says so.
