# THE n_q COMMISSION: CLOSED AT CHARTERING (2026-08-09)
# Resolution by registry archaeology; no bars needed, no computation
# beyond one verification rerun. A self-catch against FND-MATTER-065.

## What happened
The commission was chartered to attack the "n_q 1.65x below the snap
band" naked tension registered yesterday in FND-MATTER-065. The
mandatory pre-charter step -- reading the neighboring registrations --
found the tension had been adjudicated DEAD ten claims earlier:

- FND-MATTER-046 (the h-demand session) proved the snap band and the
  n_q value are ONE EXPRESSION with shared inputs (routes agree to
  3e-4/2e-5), voided the shortfall as a stale-target comparison, and
  reclassified R7 from landing zone to TRACKING IDENTITY with the
  standing rule: regenerated per mesh point, NEVER compared across
  points.
- FND-MATTER-041's face carries the correction: "[the n_q shortfall
  row is also VOID]".

The ZPE re-audit (FND-MATTER-065) copied MATTER041's code path --
including the frozen NQ_BAND constant -- without honoring the void
annotations on that claim's face. Its C4 "naked tension" repeated the
exact error 046 corrected. Verified this session: matter046_h_demand.py
reruns clean; the regenerated band equals the value bracket at 2e-5.

## Under Branch B specifically
n_q tracks a T0 h; the band tracks the same inputs. Both carry the
granted (1-s) factor together, so at ANY share the comparison is a
tautology: the identity deepens rather than tensions. The snap
action's sub-quantum character moves by 1.03x only.

## Corrections applied
- FND-MATTER-065 face amended (self-correction at full volume):
  C4 tension VOID; catch #1 redundant (true but criticizes an
  already-voided sentence); catch #2 (l_q slack space) SURVIVES;
  the rigidity item was also mis-listed (voided by MATTER045).
- The Branch-B open coherence queue REVISES to four live items:
  R1 quantum-area anchor 2.0x (the Branch-B flip), l_q vs registered
  2.48x, lattice 4.9x, Sigma-route 7.0x.
- MATTER046's honesty clause re-inherited: with n_q an identity, the
  scale sector remains internally unconfrontable; falsification rests
  on the external judges (PVLAS-class discriminator, J1713).

## Error-class ledger
FIFTH instance of the fail-to-search class (FND-021 lineage), and the
first committed by the re-audit session itself: a claim's face
corrections were bypassed by copying its code. The tripwire that
caught it is the same one that caught the EM-RECON-014 ID collision --
read the neighboring sector's registrations before building on them --
firing at the very next commission's chartering, which is the system
working at minimum cost: the error lived one session and died before
any bars were locked against it.

## What the forward queue actually is
The n_q item leaves the queue. The sharpest LIVE items under Branch B:
- Sigma-route 7.0x and lattice 4.9x (T0 anchor spread -- these two
  disagree with the M-point AND with each other at 1.4x; a
  provenance-and-spread commission on the T0 anchor family is the
  natural next go).
- l_q vs registered 2.48x (interrogates the registered 1.39e-15 m's
  own provenance).
- R1 anchor 2.0x (the Branch-B flip; smallest, likely absorbed if the
  anchor family session moves anything).
Each is a separate go-decision. No commission opened here.
