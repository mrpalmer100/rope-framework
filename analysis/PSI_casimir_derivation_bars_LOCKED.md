# Commission PSI — why strand count follows the Casimir: locked bars

*Locked 2026-08-11. CHI left the recruitment rule n_D ~ C_D as the sector's
sharpest open question. CONTAMINATION: all relevant data (Casimir scaling to
<=5%, constant 9/4 profile ratio, rho_R = 1, lambda-universality) is in
context; every agreement below is a RETRODICTION and must be labeled so. The
forward content is (i) the premise ledger -- which registered claims carry
the derivation and which assumption is genuinely new -- and (ii) the
functional FORM the derivation forces on Casimir-scaling VIOLATIONS, which
future precision data can refuse.*

## The derivation target
Derive n_D/n_f = C_D/C_f from registered claims plus a minimal enumerated
assumption set, or register the obstruction.

## Locked bars
B1 THE PREMISE LEDGER, every step cited and graded registered/new:
   P1 LINEARITY: the mesh responds linearly to sources at tube strains.
      (Registered for EM fields far below critical, NU S1(3); its status AT
      TUBE STRAINS is NOT registered and must be treated as the derivation's
      live assumption -- and then MEASURED, see B3.)
   P2 SUPERPOSITION => FIELD ~ CHARGE: in a linear medium the strain field
      is the source's Green function times its charge; the squared color
      charge of a representation is its quadratic Casimir, so the
      ensemble-averaged energy density scales as u_D ~ C_D pointwise with a
      charge-independent PROFILE SHAPE. (This is the framework's transcription
      of Gaussian dominance; the transcription itself is the new content.)
   P3 u ~ strain^2: the registered quadratic energy calibration
      (EM-RECON-014 class); its extension from the EM to the chromo sector
      is an enumerated assumption, not a registered fact.
   P4 nu = u/T0: registered (ELEC-053, re-verified by TAU).
   CONCLUSION to check: n_D ~ C_D, same width, same lambda -- with each of
   the three RETRODICTIONS (constant profile ratio 9/4; rho_R = 1;
   lambda-universality across representations) labeled as retrodiction.
B2 THE RELOCATION, stated exactly: if the chain closes, the recruitment
   rule is not fundamental -- it is LINEARITY wearing group theory, and the
   open question moves from "why Casimir?" to "why is the mesh linear at
   tube strains?", which is quantitative, not mysterious.
B3 THE BOUND, Conjecture grade, arithmetic pre-committed: if the leading
   nonlinearity is analytic (relative correction ~ strain^2 = u/Sigma_vac),
   then Casimir violations grow linearly in C: delta_D ~ (u_f/Sigma_vac)
   (C_D/C_f - 1). Bali's bound delta <= 5% at C_D/C_f = 6 then forces
   u_f/Sigma_vac <= 1%, i.e. kappa_pack = Sigma_vac/Sigma_eff >= ~100
   (>= ~500 at the continuum ~1% reading). This inverts PHI's open factor
   into a LOWER bound -- registered at Conjecture grade because the
   analytic-leading-order form is assumed, and the form itself is the
   claim's falsifiable prediction: CS violations, when finally resolved,
   must be POSITIVE-DEFINITE in sign displacement and LINEAR in the
   Casimir, or the bound (and P1's measurement) falls.
B4 THE CONSEQUENCE LEDGER, named not computed: kappa_pack >= 100 moves
   every vacuum-facing consumer (Sigma_vac >= 3.6e37, the fence, kappa_0,
   the M-point chain including the m_e-consistency solve) -- a re-solve
   commission is REQUIRED before any of those numbers are quoted again,
   and until it runs the corpus quotes Sigma_eff only.
B5 HONESTY: the loop must be stated plainly -- linearity is assumed to
   derive CS, then CS precision is used to measure linearity's quality;
   this is self-consistency, not circularity, ONLY because the same
   linearity parameter is over-constrained (one parameter, seven
   representations); say exactly that, and grade the whole result on the
   weakest premise (the chromo extension of P3).
