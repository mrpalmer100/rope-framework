# COMMISSION ANSATZ -- THE COMPOSITE-STATE SELF-CONSISTENCY SESSION -- BARS (LOCKED)

*Locked 2026-08-18 before computing. The audit gap named on FND-135's
face: the composite numbers are exact averages of a STATED state
(superposed rigid rotations, Omega_2 fixed by the co-moving balance),
not of a verified solution. This session tests the state against the
equations of motion at the order the audit flagged,
(R_2/R_1)^2 = 0.174, and prices the consequence for Sigma_wave.*

## THE TEST, PRECISELY

The stated motion is fully specified: Cdot = Omega_1 (zhat x C),
ddot = Omega_1 (zhat x d) + Omega_2 s (t_1 x d), t_1 co-rotating.
Its exact material acceleration follows by one more time derivative
and is pointwise computable on the phase torus. The force the fibre
supplies at kb = 0 is EXACTLY T times the arclength derivative of the
unit tangent (tension constant at the registered 3/2). The state is a
solution iff mu rddot = T d(that)/ds pointwise. SCOPE: the clean test
runs at kb = 0; the kb-at-bound corners inherit the clean verdict
plus a bending-force gap that is NAMED, not computed (the composite
bending force density is not registered machinery).

## THE LEGS

1. SURFACE CONTROLS. (a) The level-1 limit (d -> 0) must balance
   exactly at the registered Omega_1 (halt on fail). (b) The
   fibre-direction solve: the registered composite tangent must lie
   in the span of the position surface's (a, b) derivatives to 1e-8,
   and the arclength derivative of the actual tangent must reproduce
   the registered composite curvature vector to 1e-6 (these controls
   validate the numerical D_s operator before it is trusted).

2. THE METRIC LEG. Does the stated motion preserve local arclength?
   Compute the stretch rate (D_s R) . (D_s v) over the torus; report
   max and RMS in units of Omega_2. A nonzero value means the ansatz
   oscillates the stretch and the k_f channel is implicated at the
   flagged order. OPEN before the run.

3. THE RESIDUAL LEG. Res(a, b) = mu rddot - T D_s(that), evaluated at
   the build's Omega_2, both handedness signs. Report max and RMS
   normalized by the level-2 centripetal scale mu Omega_2^2 R_2, and
   the decomposition of the residual by frequency content in b
   (which harmonics carry it).

4. THE REFIT DISPLAY. The residual-minimizing Omega_2 (Omega_1 pinned
   by the exact level-1 limit; a both-free cross-check run and
   displayed) and the KE/Sigma_wave corners AT the refit -- DISPLAYED,
   NOT ADOPTED (a least-squares refit is itself a modeling choice and
   adopting it would replace one ansatz with another by fiat).

## PRE-REGISTERED OUTCOME SHEET

- max residual < 5% of the centripetal scale AND metric drift < 5%
  of Omega_2 -> ANSATZ-ADEQUATE at the flagged order: FND-135's
  annotation is strengthened to "verified to O(5%)"; the box stands.
- Either measure >= 5% -> ANSATZ-GAPPED: the gap is quantified with
  its harmonic content; Sigma_wave's box gains a NAMED RIDER (value
  conditional on the stated ansatz, gap size on the face); the refit
  corners are DISPLAYED for the author. No registered number moves.
- Refit corners land OUTSIDE the registered box -> CONFLICT: reported
  to the author's desk with the offending corner named; nothing
  adjusted.
- Leg-1 control failure -> HALT.

## CLEAN ROOM

The registered Sigma_wave box appears in leg 4's display only. No
derive-point family value appears. T_fibre enters at its registered
value and is not re-priced (tripwire). Bars not edited after lock;
deviations recorded, not smoothed; verdict prose written AFTER the
run (FND-137's lesson).
