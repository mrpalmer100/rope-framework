# NUC-IMPORT BARS — the nuclear contact scale confronts the core survival threshold (LOCKED before any number moves)

Date: 2026-08-09. Commission: EM-RECON-017's designated settler #1.

## The identification chain, committed here from registrations before confrontation

The survival condition (EM-RECON-017, inherited sealed): per-crossing
contact energy E_x ≡ Ac·σ0 must satisfy E_x/(T0·a) > 2/C, threshold band
[0.40, 3.00] over the admissible standoff.

The registered nuclear quantity: e_b = 4.716 MeV per strong contact bond
(NUC-007, calibrated once from He-4; the NUC sector's single constant).
The registered mesh quantities: T0·a = 2.6065e-14 J = 0.1627 MeV (the
M-point, FND-MATTER-044; the corpus's one spent calibration).

LINK 1 (bond vs barrier): e_b is a BOND energy (net minimum of overlap
attraction minus core cost), while Ac is the repulsive contact amplitude.
At a contact equilibrium of the registered form class, the repulsive
component at the minimum is O(1)× the bond depth. CLASSIFICATION: an O(1)
conversion, direction-neutral, carried as a factor-3 band, NOT a value.

LINK 2 (bundle vs strand): e_b is a BUNDLE-level bond (nucleon knots are
tubes of n_t constituent strands); E_x is per SINGLE strand crossing.
Conversion requires the bundle contact multiplicity m_b (strand pairs in
contact per bundle bond): E_x = e_b·(L1 factor)/m_b. m_b depends on the
constituent width w — THE SAME unregistered quantity the QGATE campaign's
scale branch already bottlenecks on (QGATE-005: n_t = f_c(D/w)²,
unconfirmed and unfalsified). CLASSIFICATION: UNREGISTERED. Bounds only:
m_b ≥ 1 (some pair touches); m_b ≤ n_t with n_t ≤ ~500 admissible
(the ELEC-046-shifted demand); intermediate estimates may be DISPLAYED
under the numerology guard but not adopted.

## Bars

1. The survival band [0.40, 3.00] and the sealed numbers above may not be
   revisited after the confrontation begins.
2. Permitted outcomes: survival derived / failure derived / BOUNDED
   REDUCTION (the confrontation returns a two-sided bound; if it straddles
   the threshold, the block is registered at its exact link with its
   bounds, and no side is chosen).
3. Estimates for m_b (surface counting, patch geometry) are displayed with
   their assumptions visible and NOT adopted; any clustering of displayed
   points near the threshold is reported under the numerology guard as an
   observation, not evidence.
4. If the outcome is a bounded reduction, the convergence with the QGATE
   bottleneck (both questions waiting on w) is registered as a structural
   finding, and the standing tripwire extends: any future claim fixing w
   must be fed through BOTH censuses.
5. Nothing re-fitted; spend stays at ONE.
