# COMMISSION NUN -- THE 1/SQRT(A) DILUTION: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number in this commission was computed.

## Charter

NUC-027's consolidated demand: derive, blind, the 1/sqrt(A) dilution of
NUC-021's fixed cross-sublattice cost. One derivation covering three
registered misses (NUC-019 asymmetry magnitude, NUC-020 asymmetry exponents,
NUC-027 pairing scaling). "Blind" per the sealed-target discipline: every
channel's scaling exponent is DERIVED before it is compared with -1/2.

## Bars

B1 (channel enumeration, closed at lock): the registered delocalization
    channels through which a fixed local cost could dilute are enumerated
    NOW and no channel may be added after results are seen:
      D1 classical smearing: the misplaced label spread fractionally over
         M foreign-sublattice sites (bond energy bilinear in occupations).
      D2 defect-band delocalization: the misplaced label as a Bloch state
         hopping on its foreign sublattice (hopping element = the
         reconnection amplitude, value UNREGISTERED; only the EXPONENT may
         be used, and only if it is rate-independent).
      D3 elastic screening: the ambient labels polarize around the defect.
      D4 coherent collective sharing: the cost enters through an amplitude
         summed coherently over N participants (condensate-blocking).
    Each channel's exponent is derived symbolically or numerically BEFORE
    the target exponent is written next to it.

B2 (the geometric no-go, stated as a bar): any energy that is a sum of
    local terms over a compact 3D droplet expands in the geometric exponents
    A^1 (volume), A^(2/3) (surface), A^(1/3) (curvature), A^0. The
    commission must state whether 1/2 is a member of this set and draw the
    consequence honestly, whichever way it cuts.

B3 (the equivalence, derived before confrontation): the measured staggering
    Delta(A) = c_s A^(-1/2) (NUC-027: 24/sqrt(A) form preferred at
    chi-squared 42 vs 6326) is to be converted into the scaling of the
    energy component that CARRIES the staggering, via the discrete
    derivative E(N) - E(N-1). The conversion is arithmetic and is locked as
    the confrontation instrument.

B4 (pricing confrontation): if any surviving channel implies a smooth
    energy component c sqrt(A-ish) present in ALL nuclei, its coefficient
    is PRICED from NUC-027's measured staggering and then confronted with
    absorbability: project the priced term onto span{A, A^(2/3), A^(1/3), 1}
    over A in [16, 250]. Verdict grammar, locked:
      ABSORBED  if max |residual| < 3 MeV (inside SEMF-level scatter);
      EXCLUDED  if max |residual| > 10 MeV (a term that size cannot hide);
      TENSION   otherwise.
    No re-scoping of these numbers after the projection is seen.

B5 (adverse outcomes pre-authorized): acceptable outcomes include: the
    dilution derives; the dilution derives only conditional on an
    unregistered object (named, priced, left on the author's desk per the
    grants rule); no registered channel produces it (Failed-and-kept).
    Rescues and bar-shopping refused by rule.

B6 (house): claims opened and read verbatim before use (done at charter:
    NUC-019, NUC-020, NUC-021, NUC-026, NUC-027); no em dashes in file
    content; benchmark under benchmarks/nuclear/; verify before re-zip.
