# PRED-003-ETA bars — LOCKED before computation (2026-08-02)

Commission (PRED-003-LOCK's next-order): determine eta, the per-strand-to-bulk
normalization between OPT-006's impedance and the coarse-grained director stiffness.

A CORRECTION OWED FIRST, found in reconnaissance and recorded before any algebra so
it cannot be softened: LOCK's note called eta "the one pure number left in the chain,
and the last obstacle to lambda". That is WRONG. Combining CONST's definition
(e^2/(4 pi eps0) = 2 lambda J a) with DICT's reduction (l_q = g a) under the
enslavement gives lambda = g^2/(4 pi eta): TWO pure numbers remain, eta and g (the
winding's source length in units of a). The session registers the correction and
addresses both.

Premises, stated as premises:
- P1: the one-metric photon theorem (Derived) — the gapless transverse sector has
  exactly ONE wave operator with coefficients (mu, T) — applies to the photon sector
  both OPT-006 and the director coarse-graining describe.
- P2: the symbol T in OPT-006's derivation and in FND-001's endpoint force balance
  refers to the same per-strand tension (the corpus's uniform usage; flagged, since a
  hidden volume-fraction convention would shift eta by a pure number).
- P3: the registered scale inputs used for any numeric statement are the corpus's own:
  T0 in {1203, 1700} N (lattice-anchored / Sigma-route), a <= 1e-16 m (the Lorentz
  bound, an UPPER bound), alpha and hbar c measured.

Rules fixed in advance:
- R1: NO g MAY BE SELECTED. Candidate normalizations may be TESTED against registered
  bounds and killed or bounded, never chosen to fit.
- R2: Numerology guard: any numeric coincidence (e.g. an energy landing near a known
  particle scale) is filed as observation-with-flags, not leaned on — especially since
  a is an upper bound, making derived energies upper bounds.
- R3: The kappa NAME COLLISION found in reconnaissance (ELEC-021's electron-functional
  kappa = alpha hbar c versus the micromechanics locking modulus kappa) is registered
  as a hygiene finding regardless of anything else.
- R4: The kappa-independence sweep LOCK ordered is executed: machine-scan the registry
  for treatments of the locking kappa as an independent variable outside the
  PRED-003 lineage; report the count whichever way it falls.

Bars:
- B1 (the correction): lambda = g^2/(4 pi eta) derived symbolically; LOCK's
  "last obstacle" line corrected on the books.
- B2 (eta determined, conditionally): under P1 + P2, both K and sqrt(T mu) are the
  SAME sector's impedance-defining stiffness, and the one-metric theorem admits no
  second coefficient — hence K = T identically, eta = 1, and the enslavement
  sharpens to EXACT: kappa = 2T/a, l_lock = a/2, J = T a/2. Conditional on P1-P2,
  said so.
- B3 (numeric consequences under P3, all conditional): kappa and J evaluated at the
  registered scale sets; any independent check identified or its absence declared
  (expected: kappa has no independent registration — the exact enslavement is a
  PREDICTION awaiting one). R2 applies to whatever J lands near.
- B4 (the g bound — the session's quantitative target): with eta = 1,
  alpha = g^2 T a^2/(4 pi hbar c), so measured alpha + registered T + the Lorentz
  bound a <= 1e-16 m force g >= sqrt(4 pi alpha hbar c/(a^2 T)) — a LOWER BOUND on
  the source length from numbers already on the books. Computed for both scale sets.
- B5 (candidate kill, rule R1): the naive topological normalization g = 2 pi (source
  strength = the winding's own circulation) tested against the B4 bound; killed or
  survives, reported straight.
- B6 (hygiene, rules R3-R4): the name collision registered; the kappa sweep run and
  reported.
- B7 (lambda status): lambda = g^2/(4 pi) under eta = 1, bounded below by B4;
  g's determination named as the dictionary's true residual. No tier motion.
