# Commission VAV -- the N computation: the everything-brick turns out to be the registry's oldest residual wearing four names, the composition table dies whole, ETA's numbers become determinations, and the g-mechanism gets its target sheet

Date: 2026-08-11. Bars: analysis/VAV_n_computation_bars_LOCKED.md
(locked after the registry reading and before computation, with the
reading's reshaping of the commission stated on their face).
Benchmark: benchmarks/foundations/vav_n_computation.py.

## V1 -- the identification theorem

eta = 1 is already registered (PRED-003-ETA, conditional on P1/P2), so
FND-043's composite collapses: **N = 2g^2**, with g = l_q/a the
winding's source length -- ETA's own named "true residual." Machine-
checked at both floors: N = 1.37e4 / 2.33e4, g = 82.6 / 108.0,
lambda = g^2/(4 pi) = 543 / 928 slaved, A = rho l_q with
rho = 1/sqrt(2 pi^2 alpha) = 2.6348 re-verified as an identity
(ELEC-083's vacuity, standing). The ledger of convergence: the L1 area
selection (GRV-093), the vacuum packing (FND-042), the locking
softness (FND-043), the amplitude selection (ELEC-054), and lambda
(ETA) are ONE question -- what sets g -- asked from five directions
across two months and answered by none of them.

## V2 -- the composition kill and the adjudications

Under the enslavement l_lock = a/2, DICT's candidate table
{a, sqrt(l_lock a), l_lock} = {1, 0.71, 0.5} x a **dies whole** against
the determined g of order 1e2 -- factor 120-220 on every row. The
surviving composition is l_q = g a with g mesoscopic and
mechanism-less. HE's confrontation is adjudicated: the soft-channel
branch (eta >> 1) is DEAD by ETA's theorem (conditional P1/P2); the
"reading dies at number level" branch is the one that fired, and it
fired on the whole table at once. FND-043's drift cross-link (the
sole T1's second job) inherits a named premise -- g fixed under
drift -- annotated, not re-adjudicated.

## V3 -- the floor determinations

ETA's bound-era numbers, updated now that a is determined per reading:

| Quantity | kappa=50 | kappa=250 |
|---|---|---|
| g (was >= 13-16) | 82.6 determined | 108.0 determined |
| kappa_lock = 2T0/a (prediction) | 1.96e20 J/m^2 | 5.74e20 J/m^2 |
| lambda = g^2/(4 pi) | 543 | 928 |
| A/a | 217.6 | 284.6 |

And the catch: **J per link = T0 a/2 = K_me/2 = 81.4 keV is the
identity J = m_e c^2/(2L)** -- ETA's flagged "MeV-adjacency"
(0.375-0.531 MeV) was the calibration wearing per-link units, inflated
4.6-6.5x by reading a at the Lorentz bound. The observation dissolves
as an identity: the phantom/stale-comparison family's sixth catch,
found exactly where ETA's own flags said to look.

## V4 -- the mechanism target sheet (specified, not hunted)

Per the maximal guard, no candidate formulas were tested. The sheet
any g-mechanism must satisfy:

1. Produce g = 82.6 (5% floor) or 108.0 (continuum) -- equivalently,
   produce the kappa_pack^(1/6) scaling or fix kappa_pack outright.
   The FND-042 inversion makes the mechanism responsible for the
   vacuum packing; there is no version of solving g that does not
   also measure the vacuum.
2. Pass the ELEC-082/CONST drift filters as registered.
3. Deliver its number blind (the MATTER055/ELEC-083 standard).

The defect-log route is NAMED as the one registered structure with the
right shape -- E = pi K ln(R/a) + E_core, with the cutoff (0.18a) and
core constant (5.448K) already DERIVED in the registered benchmark --
and its required exponent is now a number: **4.41-4.68 (bare) or
6.13-6.40 (derived cutoff), in units of pi K.** An O(1) target for
machinery the corpus already owns. Computing whether the winding's
defect energetics deliver it is a well-posed commission with a
pre-committable bar.

## Guard disclosure (full volume)

During scoping, 0.18 exp(E_core/K) = 41.8 was noticed to land 2.8%
from the superseded kappa=1 ratio (43.0). Refused on three grounds:
scan-product; unregistered exponent convention (E_core/K where the
registered energy is pi K ln); and a dead comparison point -- against
the live values it misses by 2.0-2.6x. Displayed so the refusal is
auditable.

## Verdict

The N computation is DONE in the only sense tonight could do it
honestly: N is 2g^2 exactly, every scale slaved to g is determined at
both floors, every dead branch is adjudicated dead, and the one live
unknown has a target sheet with an O(1) exponent and named machinery.
The corpus's five hardest open questions are now one commission with
a number on it.

## Next bricks, ranked

1. **The defect-exponent computation**: run the registered defect/core
   machinery on the winding's energetics and test the 4.4-6.4 target
   blind (bar pre-committed to the kappa-reading bracket). This is the
   g-mechanism's first real shot, and it is falsifiable on its face.
2. The correlated-coefficients relation (unchanged).
3. The kappa rename (queued since ETA; v3.17.0 housekeeping).
