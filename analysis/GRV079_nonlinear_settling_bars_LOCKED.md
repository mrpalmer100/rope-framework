# GRV-079 bars — LOCKED before computation (2026-08-02)

Commission (GRV-078's P1''): does a generic STRONG disturbance find the static
basin? Linear order bought ringdown; tonight probes settling beyond it.

Model commitment, fixed in advance (the session's one nonlinearity):
- The corpus-native nonlinearity is EXHAUSTION: tension is finite and load
  depletes it (GRV-034's grammar; GRV-077's transverse load). Stand-in law:
  T_eff(x, t) = T(x) max(1 - e_loc/e_x, f_floor), with e_loc the local
  perturbation energy density (lightly smoothed), e_x the exhaustion scale, and
  f_floor = 0.01 a numerical floor keeping the system hyperbolic (stated, not
  hidden). PREMISE P-FORM: only the SIGN (load softens) and the EXISTENCE of a
  finite e_x are corpus-anchored; the linear ramp is a stand-in and is named as
  such. Nothing tonight may lean on the ramp's details.

Rules fixed in advance:
- R1: the weak-amplitude limit with the nonlinearity ON must reproduce GRV-078's
  relaxation (decay factor > 1e3), or the instrument is not trusted.
- R2: regimes are adjudicated by the measured peak of e_loc/e_x, not by input
  amplitude labels.
- R3: "settled" means BOTH: shell energy decayed by > 100x from its peak AND
  final max |u_t| < 1e-3 of its run peak (no persistent oscillating structure).
- R4: in the exhaustion-crossing regime, the claim is NOT that the interior is
  described (the floor is numerical); the claim is about the EXTERIOR of the
  exhausted region: an outer shell must still empty (> 100x) while the
  exhausted-region footprint is reported as an observation.
- R5: any run that fails to settle is registered at full strength as a bound on
  P1'', not reframed.

Bars:
- B1: model commitment as above; instrument check per R1.
- B2 (sub-exhaustion sweep): amplitudes with measured peak e_loc/e_x up to ~0.5;
  every run must settle per R3 -- P1'' in the reachable sub-exhaustion regime.
- B3 (the crossing): an amplitude with measured peak e_loc/e_x > 1; report the
  exhausted footprint (where T_eff hit the floor) and adjudicate per R4: the
  exterior outside it still relaxes to static.
- B4: verdict: expected P1'' -> DISCHARGED-IN-REGIME -- every probed disturbance
  ends in a static exterior, either the original one (ringdown) or one outside a
  larger exhausted core (collapse-flavored) -- with P-FORM named and the interior
  untouched.
