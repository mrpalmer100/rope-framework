# ROPE-MODE-004 locked bars — topology-specific spectral fingerprint

The central three-dimensional field is retained as the source of ordinary bound states. The rope is introduced only as a tubular perturbation. A certified Hopf-linked geometry is compared against rope-off, matched unlinked, and spherically averaged controls.

Parameters locked for the first run:

- central coupling `alpha = 12.0`
- central softening `epsilon = 0.30`
- rope coupling `beta = 0.50`
- tube width `sigma = 0.16`
- fixed spatial spacing `h = 0.25`
- box half-widths `3.0` and `4.0`
- first 12 eigenmodes

Success requires all bars:

1. Linked and unlinked topology controls are numerically certified.
2. Outer-shell probability is below 1% for every reported mode and control.
3. Excitation gaps change by less than 3% under domain enlargement.
4. Rope-on shifts exceed a minimal measurable threshold.
5. Linked-minus-unlinked shifts exceed three times the estimated domain uncertainty for at least two excited modes.
6. The topology contrast preserves sign and approximate magnitude across domains.
