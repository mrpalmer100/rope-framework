# COMMISSION FRAME-DRAG-RATIO (NUN-GRV13) -- RESULTS (2026-08-16)

Bars: analysis/NUNGRV13_framedrag_ratio_bars_LOCKED.md (locked
first; grant conditions quoted in B1; clean-room held -- derivation
legs ran before the comparison leg opened). Benchmark:
benchmarks/gravity/framedrag_ratio_nungrv13.py (sympy-exact; the
day's third prose-layer catch corrected to the machine on the face).

VERDICT: RATIO-REPRODUCED-IN-FORM / ONE-PARAMETER-AMPLITUDE
(B2's second outcome, the grant's own framing landing on GRV-062's
target).

## WHAT DERIVES, exactly and with zero freedom

    ratio = Lambda x J sin^2(theta) / (M c),     r-FREE

- LINEAR IN J: forced by beta_J = 1 (GRV-105, pinned, clean-room);
  no second slot exists.
- sin^2(theta): exact, from the axial-dipole vacuum solution
  A_phi = C_d J sin(theta)/r^2 (vector-harmonic check passed) times
  the metric weight -- the same angular form Lense-Thirring has.
- DIPOLE-LED: the axial pattern carries no scalar monopole
  (parity; GRV-020 realized on the ansatz).
- PARITY: J -> -J flips the drag sense.
- r-INDEPENDENT: g_0phi ~ 1/r against h_00 ~ 1/r divides out
  COMPLETELY -- stronger than the session's own prose expected
  (third catch, corrected to the machine). The framework's ratio
  loses r exactly as GR's loses r and G.

Frame dragging therefore EXISTS in this framework, with the correct
shape, the correct scaling, the correct parity, and the correct
radial cancellation, as a structural consequence of: rotation IS
twist (the grant), twist sources dipole-led (the Derived lemma),
and the shift rides the granted L_C3.

## THE ONE PARAMETER, and why it does not cancel

Lambda = C_d c^3 / (2 C_m): ONE dimensionless constant.
Lense-Thirring is EXACTLY Lambda = 1. The monopole rides the strain
sector (C_m, calibrated through inverse-measured G, GRV-005/006);
the dipole rides the granted L_C3 (C_d ~ lambda, magnitude
UNDERIVED, GRV-110). No registered identity connects them. The
cancellation that makes the GR ratio G-free is, in this framework,
the physical statement Lambda = 1 -- which is precisely the
"one-parameter measurement" GRV-115 fixed as the sector's binding
framing. GRV-062's fork resolves to its honest third prong: the
framework neither reproduces Lense-Thirring parameter-free nor dies
-- it PREDICTS THE FORM and OFFERS THE AMPLITUDE for measurement.

## WHAT A MEASUREMENT WOULD MEAN (framing only, per condition 4)
A LARES-class determination measures Lambda. Lambda = 1: the
granted coupling sits at the GR point and lambda acquires its first
gravitational pin. Lambda != 1 at significance: the L_C3 grant's
magnitude is measured off-GR -- a discovery-class statement in
either direction. NO kill-test language; NO numbers computed; the
confrontation itself needs its own bars and lambda's independent
bound per condition 4, unchanged.

## THE DYNAMO'S CONTRIBUTION, honestly stated
Composition discipline only, as chartered: the same
rotation-sources-circulation grammar (source -> dipole -> field ->
ratio) ran here with gravitational-sector claims in every leg and
zero EM constants imported.

## REFUSALS HONORED
No LARES numbers; no kill-test framing; lambda never estimated;
GRV-113's cap untouched; EM constants excluded; GR target quoted
from GRV-062's registered reduction, not re-derived.
