# THE FINE-CHANNEL DISPERSION LINE, STAGE 1 (PE9) -- RESULTS (2026-08-16)

Bars: analysis/PE9_bloch_L_stage1_bars_LOCKED.md.

VERDICT: RIGIDITY-DEMAND-DERIVED + R-GAP-FOUND + BLOCH-L-CHARTERED.

## 1. The rigidity demand (derived structure, falsifier-shaped)
The corpus registers a coarse longitudinal channel floor at
sqrt(2) c (FND-114), and the coarse strand IS the wound bundle. A
winding whose axial response is spring-compliant (wire bending and
torsion dominated) transmits axial stiffness BELOW the
stretch-projection value; the effective coarse channel would then
sit below its own registered floor. THEREFORE, as a necessary
condition already implied by registered structure: the granted
fiber's winding is STRETCH-DOMINATED in its axial response. This
is registered falsifier-shaped: the Bloch-L computation must return
axial transmission at or above the static projection, or the fine
structure conflicts with the corpus's own floor -- an internal kill
condition that fires before any target comparison.

## 2. The R-gap (named)
The registered winding data (pitch, angles via the kappa and tau
forms) do not fix the winding RADIUS without a parametrization
choice the registered forms leave open -- checked under both
natural angle conventions, which disagree. R is a named missing
registration. Consequence honored tonight: no spring-compliance
number was computed (it would smuggle an invented R), and Bloch-L's
input list carries R explicitly (readable from the SHIN
construction if any claim fixes the parametrization; else it is
the commission's first declared input question).

## 3. COMMISSION BLOCH-L (chartered; bars fixed here)
Instrument: FND-089's triply-controlled Bloch machinery (it has
interrogated this wound medium's transverse sector and passed its
controls). Build: the LONGITUDINAL branch at the registered
angles/pitch, granted rod inputs (kb saturated, tripwire noted),
mu_f = T0_f/c^2 (forced), R per (2). Output: c_L,f at long
wavelength. Pre-registered sheet (speed form; targets unsealed only
here): c_L,f = 2.844 c -> r_s = 0.2496 a_f and k/T0 = 2 DERIVES;
any other value -> its exact consequence via r_s/a_f = 0.7099
c/c_L,f lands in the CONFLICT or FALSIFIER region of the FND-122
sheet; below the projection value -> the rigidity demand (1) fires
first as an internal inconsistency. Controls inherited from
FND-089's triple set; the resonant regime (FND-085) must be
verified avoided (p << lambda at the reading point).

## STANDING
The trap is unchanged and now guarded on a second side: even before
Bloch-L reads a speed, its build must clear the rigidity demand.
Riders: KBSAT (tripwire), coherence, k/T0 = 2, condition 4.
