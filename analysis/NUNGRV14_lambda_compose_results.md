# COMMISSION LAMBDA-COMPOSE (NUN-GRV14) -- RESULTS (2026-08-16)

Bars: analysis/NUNGRV14_lambda_compose_bars_LOCKED.md (locked first;
B0 carries the named handoff correction). Benchmark:
benchmarks/gravity/lambda_compose_nungrv14.py (sympy-exact; same-chi
identity verified at verdict level before composing).

VERDICT: COMPOSED-DEMAND-REGISTERED (B2's first outcome).
NUMERICALLY-CLOSED not reached, exactly as the bars expected, with
the gates named.

## B0 -- THE NAMED CORRECTION, first
The v3.26.37/38 handoffs called the lambda derivation "owed." FALSE:
GRV-111 discharged GRV-110 condition 1 on 2026-08-15 (NOT-PINNED,
structure located), and GRV-112/113 carried the chain to
lambda = chi (G I_p)|tau| with chi <= 2.49e-19. The operator's
session-selection prose failed where the registry did not; the
correction is named here and in the v3.26.39 handoff. The search bar
has now caught the corpus's own operator -- its fourth payout, and
its most instructive.

## THE COMPOSITION, exact
    Lambda = chi x Lambda_nat,
    Lambda_nat = kappa_conv (G I_p) |tau| c^3 / (2 C_m)
Every leg registered: lambda = chi (G I_p)|tau| (GRV-111/112);
chi <= 2.49e-19 (GRV-113); Lambda = C_d c^3/(2 C_m) with C_d ~
lambda (GRV-120); kappa_conv the bookkeeping normalization between
the two registered conventions (constant, not freedom).

## THE THREE STRUCTURAL STATEMENTS, at full volume per bars

1. FRAME DRAGGING IS A CHIRALITY METER. In this framework the
   Lense-Thirring amplitude is DIRECTLY PROPORTIONAL to the net
   chirality fraction of the vacuum weave: Lambda = chi Lambda_nat,
   exact factorization, monotone. A frame-dragging measurement IS a
   measurement of the vacuum's parity imbalance.

2. THE DEFAULT VACUUM PREDICTS ZERO. chi = 0 (any parity-symmetric
   ensemble, GRV-112's registered default) gives Lambda = 0 EXACTLY:
   no frame dragging at all. The framework's unadorned vacuum does
   not drag frames. This is the sector's sharpest internal statement
   to date and is stated without softening.

3. THE PINCER, registered for the day the fine scale pins:
   GR-strength dragging (Lambda = 1, the GR point GRV-120
   registered) plus the polarimetric cap (chi <= 2.49e-19) DEMAND
       Lambda_nat >= 1/2.49e-19 ~ 4.0e18.
   Either the natural-scale amplitude of the granted coupling is at
   least ~4e18 (a statement about fine-scale (G I_p)|tau| c^3/(2 C_m),
   currently unregistered and refused per the FND-091 kb precedent),
   or this framework's frame dragging is suppressed below GR
   strength. NO observational verdict is issued (condition 4
   unchanged); the demand is internal structure, armed and waiting.

## WHAT THIS DOES TO THE SECTOR'S QUESTION
Yesterday: "measure Lambda." Today, sharper: Lambda is not merely a
free amplitude -- it is chi x Lambda_nat, where chi is capped by
polarimetry the corpus already holds and Lambda_nat is a fine-scale
mechanical quantity with a named floor. The one-parameter
measurement has become a TWO-SIDED SQUEEZE: polarimetry pushes chi
down; Lense-Thirring (when confronted under its own bars) pushes
Lambda up; and the fine-scale mechanics must eventually declare
whether it can pay the ~4e18. The framework has, for the first
time, a location where THREE independent measurement classes
(polarimetry, frame dragging, fine-scale mechanics) meet on one
constant.

## GATES (named): fine-scale G I_p; |tau| absolute; kappa_conv;
   C_m calibration -- all inside Lambda_nat, symbolic throughout.

## REFUSALS HONORED
Condition 4 untouched (no LARES numbers, no kill language, GR point
quoted not adjudicated); no fine-scale numerics invented; GRV-113's
cap and GRV-112's parity structure untouched; same-chi verified
before composition.
