# Commission VAV -- the N computation: locked bars

*Locked 2026-08-11, after registry reading (ETA, ELEC-054, ELEC-083,
the defect-core benchmark) and before any computation. The reading
changed the commission's shape and the bars say so plainly: eta = 1 is
ALREADY registered (PRED-003-ETA, conditional on P1/P2), so N = 2g^2
and the everything-brick IS the registry's already-named g-mechanism
residual. Tonight adjudicates, determines, and specifies; it does not
hunt.*

## Scope, in order

- V1 (the identification theorem): register that FND-043's N, ETA's g,
  ELEC-054's amplitude selection, and ELEC-083's rho are ONE unknown --
  the corpus asked the same question from four directions across two
  months. Machine-check N = 2g^2 and the slaving of A and lambda.
- V2 (HE's confrontation adjudicated): with eta = 1 by theorem, the
  soft-channel branch (eta >> 1) is DEAD; evaluate whether DICT's
  composition table {a, sqrt(l_lock a), l_lock} survives at number
  level under l_lock = a/2. If all candidates are O(a) against a
  determined g of order 1e2, the table dies as a composition and the
  drift-selection cross-link (FND-043's "second job") inherits a named
  premise (g fixed under drift) -- annotate, do not re-adjudicate the
  channel assignments.
- V3 (floor determinations): update ETA's bound-era numbers to the
  FND-040 floors, where a is DETERMINED per reading: g, lambda, A/a,
  kappa_lock = 2T0/a (the enslavement's external test), and J per
  link. Any identity found among them is reported as an identity.
- V4 (the mechanism, specified not hunted): NO candidate-formula scan,
  per the ELEC-083 maximal guard and the MATTER055 chance-level
  lesson. The deliverable is the sharpened target specification any
  g-mechanism must meet, including what the FND-042 inversion makes it
  responsible for. The defect-log route (E = pi K ln(R/a) + E_core,
  cutoff and core constant DERIVED in the registered benchmark) may be
  NAMED as the one registered structure with the right shape, with its
  required exponent COMPUTED as a target -- computing what a mechanism
  must deliver is specification; proposing that it does is hunting,
  and is out of scope.

## Honesty clauses

1. Guard disclosure at full volume: any numeric landing generated
   during tonight's reading or scoping is displayed, labeled
   scan-product, and refused -- including landings against superseded
   points, which are doubly dead.
2. All eta = 1 consequences remain conditional on ETA's P1/P2 and are
   so marked; everything kappa_pack-dependent remains conditional on
   FND-037 at the FND-040 floors.
3. No tier motion; lambda and the g-mechanism remain OPEN; nothing
   about hbar is claimed.
4. One computation pass; identities proven, not asserted.
