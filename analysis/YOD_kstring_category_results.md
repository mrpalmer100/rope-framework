# Commission YOD -- what a k-string IS: BUNDLE, by physics and by ontology. The FND-047 flag re-prices from "measured contradiction" to "uncomputed obligation," and the softening's one qualitative bundle prediction is what every dataset shows

Date: 2026-08-11. Posed by the author. Bars:
analysis/YOD_kstring_category_bars_LOCKED.md.
Benchmark: benchmarks/foundations/yod_kstring_category.py.

## Y1 -- standard physics: the asymptotic k-string is an N-ality object

Gluons carry no N-ality, so at long distance any source is screened
down to the lowest representation of its N-ality class; the stable
k-string is characterized by its Z_N flux, not by the representation
of the source that made it (survey statement in e.g. arXiv:1708.08821
and the Lucini-Teper k-string literature; SU(3) special case:
sigma_2 = sigma_1 by conjugacy). A k-string is k units of fundamental
flux in a bound configuration -- and sigma_k < k sigma_1 in the data
under BOTH candidate laws (sub-additivity table in the benchmark:
every (N, k) row, both laws), i.e. the tubes BIND.

## Y2 -- the corpus's ontology: charge is an additive integer

Charge = winding = linking number, Derived and topologically conserved
(GG-006); the Coulomb field is rope-count flux geometry; winding
charge is a circulation (PRED-003-CHAIN). Integers add. A source of
N-ality k IS k unit windings; the ontology contains no "single heavier
charge" primitive. Y1 and Y2 agree independently: **BUNDLE.**

## Y3 -- the consequence for the FND-047 flag

The SU(6) sine-law measurement is a TUBE-BINDING observable -- how
much energy k bundled windings save against k free tubes -- not the
single-source Casimir-recruitment/softening observable that PSI/BET/
CHET predict for. The flag's "walks into a measured contradiction"
stake was a category error: what the N-universal branch actually
inherits is an OBLIGATION -- compute the mesh's tube-tube binding
relation from the derived quartic and confront sine-vs-Casimir -- not
an immediate conflict. And the one qualitative bundle prediction the
derived softening already makes (overlapping strain costs less than
the sum, so parallel windings ATTRACT and sigma_k < k sigma_1) is
exactly what every dataset shows, under both laws. A sanity gate, not
evidence; the magnitude is uncomputed and is the named next-order
(THE BUNDLE-BINDING RELATION: from the -1/8, the binding of k
parallel unit windings; its large-N and k-dependence confronts
sine-vs-Casimir -- a genuine opportunity, since the two laws differ
at O(1/N^2) and the mesh quartic makes a definite functional form).

## Y4 -- untouched

The SU(3) single-source pin tests intermediate-distance
higher-representation potentials before screening (Bali's r <= 1 fm
window) -- valid as registered; the decision table stands. The
N-universality scope question remains OPEN and remains the author's
call, but its stakes re-price: NEITHER branch now walks into a
resolved contradiction. N-universal buys an obligation with a
qualitative check already passing; SU(3)-scoped remains safe and
narrower.

## Ledger

FND-048 registered (Modeled: a category adjudication with a numeric
sanity gate). FND-047's flag, FND-037's scope pointer,
KNOWN_LIMITATIONS, and Prediction 20's scope paragraph all updated
same-session to the re-priced stakes.

## Next bricks, ranked

1. v3.17.0 (FND-041..048; the arc now ends on a question turned into
   an opportunity).
2. The bundle-binding relation (the new frontier brick: k parallel
   windings under the derived quartic -- sine-vs-Casimir as the
   confrontation, with the qualitative gate already passed).
3. The ambient/collective g-charter (unchanged).
