# QB-030 bars — LOCKED before computation (2026-08-02)

Commission (S3, the campaign's closing computation): the end-to-end QB-027 Bell
experiment rerun with the PRODUCED ribbon -- the nucleated pair and its measured
holonomy statistics -- replacing the ribbon-by-fiat. Either outcome closes the
production question.

Model commitments, fixed in advance:
- ANALYZERS: QB-027's engine devices verbatim (the measured theta-grid weight
  table, leakage included; no analytic response law anywhere in the loop).
- THE PRODUCED RIBBON enters through two measured properties and one stated
  transport model:
  (i) exact base anticorrelation (QB-028, conservation);
  (ii) a per-trial holonomy error delta drawn from an EMPIRICAL BANK: interior
      phase-difference samples of the pinned field at pair scale (d = 45), from a
      fresh QB-029-style run (thousands of samples, not a parametric Gaussian);
  (iii) TRANSPORT MODEL (premise P1, carried from QB-029): the holonomy error
      coherently rotates the transmitted frame: fB = cos(delta) (-A x) +
      sin(delta) e_perp, with e_perp a random unit vector perpendicular to the
      transmitted frame. Stated as the session's one modeling commitment.
- CONTROLS, both required: the PERFECT ribbon (delta = 0) must reproduce QB-027's
  CHSH = 2.66 within 0.03; the SEVERED control must sit below the classical bound.

The analytic expectation, computed and locked before the run: with the measured
variance var ~ 0.50, <cos delta> ~ 0.78, and to first order S_nuc ~ <cos delta> x
2.66 ~ 2.07 -- a MARGINAL violation is the live expectation, and the trial count
must resolve it: M >= 200000 per setting pair, giving sigma(S) ~ 0.005.

Decision rule, fixed in advance (3 sigma against the classical bound 2):
- S_nuc > 2 + 3 sigma: THE PRODUCED RIBBON VIOLATES. The production question
  closes affirmatively at model level: the medium produces a pair whose measured
  imperfections still beat local realism. No promotion beyond Modeled; premises
  P1 (transport model) and the engine-parameter caveat carried.
- |S_nuc - 2| <= 3 sigma: MARGINAL, registered as such, with the temperature
  scaling reported (V depends on the engine's arbitrary T = 0.4; V(T) -> 1 as
  T -> 0, so the crossing temperature is the honest deliverable).
- S_nuc < 2 - 3 sigma: the produced ribbon FAILS at these parameters -- the
  honest negative, registered at full strength, with the same V(T) analysis
  showing what temperature would be needed.
Whichever branch: the severed control and the perfect-ribbon reference bracket
the result, and the V(T) curve is computed either way (it is one line of
arithmetic on the QB-029 lattice sum, not a new simulation).

Bars:
- B1: analyzer calibration reproduced (table within 0.03 of half-angle; leakage
  visibility stated).
- B2: empirical delta bank built (>= 2000 samples); its <cos delta> within 10% of
  QB-029's exp(-var/2).
- B3: perfect-ribbon reference within 0.03 of QB-027's 2.66; severed control
  below 2.
- B4: the nucleated-ribbon run at M >= 200000 per pair; no-signaling in the trial
  data (< 0.015); decision rule applied mechanically.
- B5: V(T) crossing computed from the lattice sum: the engine temperature below
  which the produced ribbon's predicted S exceeds 2 (and the temperature for
  QB-027-level 2.66 x 0.97-ish, reported for context).
- B6: verdict and propagation; no tier motion; the QB-027 premise ledger updated
  (ribbon: from BY-FIAT to PRODUCED-AT-MEASURED-VISIBILITY on whichever branch).
