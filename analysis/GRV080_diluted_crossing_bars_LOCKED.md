# GRV-080 bars — LOCKED before computation (2026-08-02)

Commission (GRV-079's named decider): rerun the exhaustion-crossing disturbance
with geometric flux dilution -- the spherically-reduced operator
mu u_tt = (1/r^2) d_r(r^2 T_eff d_r u) -- to split GRV-079's negative between
instrument (1D pulses never dilute) and physics (the interior).

Model commitments, carried and fixed:
- Identical exhaustion law and sink rule to GRV-079 (T_eff = T max(1 - e/e_x,
  0.01); floored cells absorb as a dynamically formed inner boundary), with
  P-FORM carried verbatim. The ONLY change is the geometry factor r^2 in the
  operator and in the energy measure. One knob turns; whatever changes is the
  geometry's doing.

Pre-committed expectations (the adjudication logic):
- The outgoing half of a crossing pulse dilutes as ~1/r^2 in energy density and
  should fall below exhaustion within the domain; the ingoing half CONCENTRATES,
  exhausts near the inner region, and is absorbed there -- which is collapse
  grammar, not failure: the energy joins the core.
- If, with dilution, the crossing disturbance settles to a static exterior with
  the floored footprint CONFINED to the inner region, then GRV-079's negative is
  attributed to the 1D instrument for the propagation part, and the physics
  residue narrows to the interior sink model alone.
- If it still fails to settle, the negative is physics-flavored and P1'' remains
  open at the crossing; registered at full strength per R5 (carried).

Bars:
- B1 (instrument): the spherical energy identity by machine -- on shell,
  d_t(r^2 e) = d_r(r^2 T u_r u_t) exactly; the weak limit with the nonlinearity
  ON relaxes (decay > 1e3).
- B2 (the crossing, adjudicated): a pulse with measured peak e/e_x >= 3 must
  show (a) the outgoing branch's peak load falling below e_x before the outer
  boundary, (b) the cumulative floored footprint confined to r < 6, (c) the
  outer shell (r = 12-30, r^2-weighted) emptied by > 100x, (d) residual motion
  < 1e-3 of run peak.
- B3 (the split): the SAME amplitude in GRV-079's 1D operator fails to settle
  (cited from its registered run; not rerun) -- with B2 passing, the 1D
  pathology is attributed to geometry, and the physics residue is the interior
  sink model plus P2'.
- B4 (verdict): expected P1'' -> DISCHARGED-IN-DILUTED-GEOMETRY: sub-exhaustion
  AND crossing-class disturbances both end in a static exterior (ringdown and
  collapse-flavored respectively); residues named; no tier motion.
