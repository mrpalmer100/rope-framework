# GRV-092 bars — LOCKED before computing (2026-08-03)

Commission (the residue GRV-091 named; the L1 frontier at the horizon's
door): the quantum of the ring. Does the mechanism's snap energy match the
ring quantum -- is the whisper emitted in units of hbar omega?

The computation, specified before running:
- The snap energy at depth sigma: e_bit(sigma) = beta_phys N(sigma) h with
  N = Sigma a^3/(chi sigma) (GRV-088/089/090 chain).
- The LOCAL ring frequency: the observed pitch is omega_inf = 0.23 kappa
  (GRV-040/091, geometric); locally it is blueshifted by the Tolman factor,
  omega_loc(sigma) = omega_inf/alpha(sigma) = 0.23 c/sigma.
- THE CANDIDATE FOURTH CANCELLATION, pre-named: the snap ACTION
  A* = e_bit(sigma)/omega_loc(sigma) -- if sigma cancels, the mechanism
  carries a universal action constant, independent of depth AND (since kappa
  appears in neither factor) of the black hole's mass: a medium constant of
  dimension action, secreted by the horizon chain.

Bars:
- B1: the cancellation checked by machine (sympy): A* must be independent of
  sigma and kappa exactly, with the closed form A* = (beta_phys/0.23)
  Sigma a^3 h/(chi c). If sigma does NOT cancel, the session registers that
  the snap action is depth-dependent and the quantization question is
  ill-posed at this level -- a negative kept per R5.
- B2: evaluation on both forks against hbar (the corpus's registered
  1.0546e-34 J s), with the chi = 1-3 bracket: report n_q = A*/hbar, the
  QUANTA PER SNAP.
- B3 (interpretation grammar, fixed in advance so the number cannot be
  spun):
  - If n_q is within one order of 1: the mechanism emits AT the quantum --
    an emergent hbar candidate, escalate to the HBAR sector immediately.
  - If n_q << 1: each snap is a SUB-QUANTUM coherent pulse; the whisper is
    then CLASSICAL gravitational radiation built from many snaps, quantized
    emission occurring only at the thinned rate (snap rate x n_q) -- a sharp
    structural statement (the corpus's horizon radiates waves, not
    gravitons, at leading order) and a falsifiable divergence from Hawking
    (whose emission is quantized at hbar omega by construction).
  - If n_q >> 1: each snap is a multi-quantum burst; report likewise.
- B4: the fork lever: n_q on F-Lor versus F-Sak (expected to differ by the
  a^3-driven orders) -- if large, register the ratio as the fork's THIRD
  internal discriminator. And the residue stated honestly: A* not equal to
  hbar convicts NOTHING tonight, because the corpus has never derived that
  gravitational emission is hbar-quantized -- the session's product is the
  RATIO, its universality, and the sharpened question: why does the medium's
  own action constant sit where it sits relative to hbar? That question IS
  the L1 frontier, now equipped with a number.

Rules: R1 no new physics constants introduced; R2 beta_phys = 35.4 and the
0.23 are the registered values, brackets carried; R3 failures at full
strength.
