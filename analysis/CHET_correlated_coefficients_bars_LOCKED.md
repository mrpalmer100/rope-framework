# Commission CHET -- the correlated-coefficients relation: locked bars

*Locked 2026-08-11, before any group-theoretic evaluation. ALEPH's
named next-order: compute the quartic-Casimir component's coefficient
from the same -1/8 that BET derived, giving the refined forward
prediction its second number. ALEPH's W2 asserted the framework's
quadratic nonlinearity GENERATES quartic-Casimir structure under
non-commuting charges; that assertion is itself under test tonight and
may be corrected -- the arc's precedent is that prosecuting one's own
refinements is how the corpus stays honest.*

## The computation, pre-named

Within the registered quadratic-nonlinearity ansatz (BET: energy
density u = Sigma(x/2 - x^2/8), x = strain^2), evaluate the quartic
term's color structure for a static source/singlet-channel pair in
representation R:

- C1 (mean field): the static strain profile g'_a(r) = T^a f(r). Is
  the quartic term central (a Casimir number) on the irrep? Verify
  with explicit SU(3) matrices (fundamental and adjoint at minimum;
  sextet if cheap).
- C2 (ordering-allowing): the pair-symmetrized four-charge correlator
  contracted isotropically. Evaluate S(R) = (1/3)[T.T T.T +
  T^aT^bT^aT^b + T^aT^bT^bT^a] against the identity T^aT^bT^aT^b =
  C2(C2 - C_A/2), machine-checked per representation. Determine what
  survives in the RATIO V_R/C_R and in DIFFERENCES across
  representations.
- C3 (the d^abcd question, adjudicated not assumed): does any
  contraction available to the ansatz at leading order produce a
  genuinely quartic-Casimir (d^abcd-class) component? If not, state
  at what order/structure it could first arise, without computing it.

## Verdict grammar (pre-committed)

- CORRELATED PAIR: both components present at leading order with a
  derived ratio -- register the ratio as ALEPH's second number.
- PURE-LINEAR: the leading-order violation is exactly BET's C2-linear
  form with the quartic-Casimir component ZERO at this order --
  register as a SHARPENING of ALEPH's refined prediction (the
  long-distance violation is predicted C2-linear-dominant with
  suppressed d^abcd), file a correction pointer against ALEPH's W2
  folding-in language, and restate the kill conditions accordingly.
- INDETERMINATE: the ansatz does not fix the structure -- register as
  such per the GRV-006 style.

## The pin (either non-indeterminate branch)

Whatever the structure, derive and register the kappa_pack PIN: the
inversion by which a resolved long-distance violation magnitude
measures epsilon_f and hence kappa_pack exactly (from BET's
delta_D - delta_f = -(eps_f/2)(C_D/C_f - 1)), with forward numeric
predictions per representation pair evaluated at both FND-040 floor
readings for lattice confrontation.

## Honesty clauses

1. All group identities machine-verified with explicit matrices; no
   identity used unverified.
2. No spatial-profile freedom exploited to manufacture structure: the
   isotropic contraction is the ansatz's own; anisotropic profiles are
   out of scope and named so.
3. Magnitudes stay conditional on FND-037 at the FND-040 floors and
   are so marked.
4. One evaluation pass; corrections to ALEPH filed same-session if the
   PURE-LINEAR branch fires.
