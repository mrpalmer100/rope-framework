# COMMISSION MEM -- THE m_b(n) CONFRONTATION: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number is computed. Chartered from
FND-078's re-pointed decider (m_b has the shortest dependency path to
n_b). The registered contact-multiplicity models were always functions
of a strand count; this commission supplies the counts the hierarchy
now provides and runs the confrontation both ways.

## Registered inputs, closed at lock

- MULTIPLICITY MODELS (FND-029's registered set, functional form, not
  modifiable): m_b(n) in {1, sqrt(n), n^(2/3), n} for single-pair,
  surface-line, contact-patch, full-section, with EM-RECON-018's
  registered plausibility commentary carried (full-section
  geometrically implausible for touching parallel cylinders; the
  commentary travels with any full-section cell).
- THE CONFRONTATION CHAIN (FND-029, EM-RECON-030, unchanged): raw
  bundle ratio 29.0; per-pair ratio 29.0/(m_b L1); survival iff
  > [0.395, 0.460]; L1 = 1 headline with the factor-3 band displayed.
  Survival ceiling: m_b < 63-73 (L1 band ~21-220).
- BUNDLE IDENTITY, enumerated at lock, BOTH evaluated:
  I-CONST: the touching object is the tube CONSTITUENT, n = n_b from
    TAV's integer windows -- (kappa50, f_c): 5-9; (kappa50, hex) and
    (kappa250, f_c): 14-27; (kappa250, hex): 40-81.
  I-TUBE: the touching object is the tube itself, n = n_t in [47, 198]
    (QGATE-004's census, tube-internal, survives SHIN), with the
    collective-hbar demand n_t = 111 marked as a point, not adopted.
- THE AT-THRESHOLD DEMAND (conditional tier): QOPH's displayed
  convergence (matter AT its survival threshold). If imposed, the
  per-pair ratio sits INSIDE [0.395, 0.460], pinning
  m_b = 29.0/(0.460 L1) .. 29.0/(0.395 L1) = [63.0, 73.4] at L1 = 1.
  This demand is CONDITIONAL-DISPLAY: QOPH refused to adopt the
  convergence and MEM inherits the refusal. Any cell it selects is
  reported as conditional, never adopted.

## The questions

Q1 SURVIVAL MATRIX: for every (identity x model x window) cell, the
   per-pair ratio and the survival verdict at L1 = 1 with the band.
Q2 THE PIN: which cells does the at-threshold demand select, i.e.
   where does m_b(n) = [63.0, 73.4] intersect a registered window?
   Inversions computed per model; intersections named.
Q3 THE DISCRIMINATION STATEMENT: what the pin would decide (floor
   reading, packing, n_b or n_t value) IF the conditional demand were
   ever adopted, stated as a conditional chain with every premise
   listed -- the deliverable is a well-posed target for the author,
   not an adoption.

## Guard disclosures

G1: pre-lock scoping noticed the at-threshold pin may intersect the
    (kappa250, hex) constituent window and the n_t census near the
    111 mark. Disclosed; the enumeration was closed before any
    intersection was computed, and no cell may be promoted because it
    intersects something interesting.
G2: disclosed-target blind integrity as in RESH: the ceiling and band
    are in context; protection is zero free numbers and a closed
    enumeration.

## Verdict grammar, pre-committed

- SURVIVES-UNPINNED: plausible cells (per the registered commentary)
  sit below the ceiling; the at-threshold demand selects nothing.
- PINNED-CONDITIONAL: the at-threshold demand intersects at least one
  registered window; the selected cells are named with their full
  conditionality chain. This does NOT determine n_b; it converts the
  determination into a listed set of premises plus one number.
- EXCLUDED: every plausible cell violates the ceiling.

## Adverse outcomes pre-authorized

All three registerable without rescue. The FND-070 resemblance rule
binds Q2/Q3: intersection is arithmetic, adoption is the author's.

## Deliverables

benchmarks/foundations/mem_mb_confrontation.py;
analysis/MEM_mb_confrontation_results.md; claim via tools/add_claim.py;
annotations to FND-078, FND-077, FND-029, EM-RECON-018, QGATE-004,
FND-074.
