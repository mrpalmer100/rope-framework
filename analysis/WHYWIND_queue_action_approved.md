# QUEUE ACTION -- APPROVED (author, 2026-08-29) + OPUS 5 QUEUE-HEAD
# HANDOFF PACKAGE
# Approved in the handoff-review session that granted WHYWIND-A..F
# and the FND-152/153 hardened rider (six grant records alongside
# this file). NOT EXECUTED here: this session holds session
# artifacts only (no repo, no solver modules), and its ~4 GB /
# worker-reap container class is precisely the regime the hardware
# note names as the binding constraint.

## THE APPROVED ACTION
Full stage-3 resolution replication of the Q-SWEEP columns moves
to the QUEUE HEAD, packaged for the Opus 5 handoff, under the
hardware condition: the 4/3 deep refusal (WHYWIND-F) is retried
ONCE on a machine where 144x54+ is feasible BEFORE being treated
as state-intrinsic. Bundled at the author's standing instruction:
the q = 5/4 fourth-cell column, which adjudicates BOTH the
committed dispersion prediction (FLAT,
WHYWIND_dispersion_results.md) and -- independently, since the
prediction was derived from the lattice, not from deep-end tangent
content -- the grid question of WHYWIND-D/E/F.

## PREREQUISITES FOR THE EXECUTING SESSION (must all hold)
1. HARDWARE: memory sufficient for 144x54 (and preferably 144x72)
   f64 factorizations without the disk-backed workaround; no
   reap-window regime. The probe42 instrument ledger (phase-C
   disk-backed factorization, per-dtype resumable Jacobian,
   bounded f64 endgame) remains available as fallback but its need
   signals the wrong machine.
2. REPO: full working tree (the instruments import
   benchmarks/foundations/qsweep_stage1 and traverse96_scout).
3. REGISTRY STATE: the six WHYWIND grants inserted and the
   FND-152/153 hardened rider applied (grant records in this
   package) BEFORE the replication runs -- the new work must cite
   registered claim numbers, not drafts.
4. CHARTER: a fresh bars-locked charter for the replication,
   written before computation per house rule. The stage-3 runbook
   (analysis/WHYWIND_stage3_runbook.md) is the operator base; its
   credential line (F17 >= 0.20) refused at 96x36 and needs
   re-derivation for the larger grids.

## WORK ORDER (queue head, in order)
1. Retry p42|4/3|deep gating at 144x42 on capable hardware (the
   WHYWIND-F refusal, one retry). Checkpoint:
   analysis/whywind_probe42_ckpt.pkl. If it gates: run the P42
   lines the probe charter locked. If it refuses again:
   state-intrinsic fragility registers per the WHYWIND-F clause.
2. Full stage-3 resolution replication of the Q-SWEEP columns at
   144x54+ (both cells), confronting the WHYWIND-D cross-grid
   record and the WHYWIND-E band decomposition: does the 4/3
   deep-end n = 17 content persist as the phi band opens, or
   dissolve (artifact call)?
3. The q = 5/4 column (charter to be locked before the march):
   adjudicates the committed FLAT prediction and, via its own
   grid ladder, the grid question out-of-sample.

## STATE CARRIED IN THIS PACKAGE
- analysis/qsweep_stage2c_ckpt.pkl (24 retained gated states, both
  profiles -- the WHYWIND-C/E input states)
- analysis/whywind_stage3_ckpt.pkl (eight full-bar members,
  96x36 / 96x54)
- analysis/whywind_probe42_ckpt.pkl (144x42 partial state incl.
  the refused deep attempts)
- All WHYWIND bars/results/runbook docs and the five instruments.

## STANDING
The q = 5/4 FLAT prediction stands committed. Registry untouched
by this session; the working session inserts the grants first.
Grants reserved to the author; failures kept.
