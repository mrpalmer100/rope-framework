# Commission PHI — bounding the packing factor: locked bars

*Locked 2026-08-10, before attempting to read 1702.03454's tables. UPSILON's
BROKEN verdict rests on profile-SHAPE constancy at short separations
(proceedings-grade). This session seeks the actual adjoint-vs-fundamental
WIDTH numbers, and any registered ceiling on kappa_pack.*

## Locked bars
B1 THE DATA: the Lisbon finite-T paper (1702.03454) computes widths for
   quark-antiquark (fundamental) and quark-adjoint tubes with the Lagrangian
   density weighting — convertible by the ELEC-086 mapping R_eq = sqrt(2 w^2)
   for BOTH, so the RATIO rho_R = R_adj/R_fund is convention-free (the
   sqrt(2) cancels). Use the LOWEST available temperature below Tc and the
   largest separation with finite errors; the temperature caveat goes on the
   face. If the tables cannot be read tonight, register ARMED with this
   conversion, the OMICRON pattern.
B2 THE OBSERVABLE, fixed: kappa_rel = kappa_adj/kappa_fund =
   (sigma_adj/sigma_fund)/rho_R^2 = (9/4)/rho_R^2, the RELATIVE packing.
   Pre-committed readings: rho_R = 1.5 -> kappa_rel = 1 (packing rigid,
   UPSILON's wound HEALS, vacuum-packing restored at this data's precision);
   rho_R = 1.0 -> kappa_rel = 2.25 (compression confirmed with numbers,
   UPSILON's verdict HARDENS); intermediate -> partial compression,
   quantified. Bands: HEALS if rho_R within 10% of 1.5; HARDENS if within
   10% of 1.0; PARTIAL otherwise.
B3 THE ABSOLUTE CEILING, from registered claims only: the strand hard core
   d_c = 1.87e-19 m (HBAR-005) caps areal density at close packing
   ~ 2/(sqrt(3) d_c^2); with vacuum areal density 3/a^2 this gives
   kappa_pack <= (a/d_c)^2 * 2/(3 sqrt(3)) — compute at both registered a
   (Lorentz bound 1e-16; M-point 6e-17) and state plainly how weak it is.
B4 HONESTY: kappa_rel bounds RELATIVE packing only; the fundamental tube's
   own kappa_fund remains unmeasured by any rho_R. State what WOULD measure
   it (a direct strand-count observable, or a framework relation tying
   compression energy to the FND-029 E_x channel) and whether tonight's
   data contains it (expected: no).
