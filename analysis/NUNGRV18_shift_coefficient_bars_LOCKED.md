# THE SHIFT-COEFFICIENT READING (NUN-GRV18) -- BARS LOCKED (2026-08-16)

Locked before the adjudicating read. GRV-124's fork-deciding session.

## B1 -- THE VERBATIM FINDING (declared pre-adjudication)
GRV-068 registers the vector operator WITH COEFFICIENTS:
(T/2)|k u|^2 + (gamma/2)|k phi|^2 + (kappa/2)|phi - (i k x u)/2|^2,
kappa = 0 exact (GRV-067 Goldstone). T = tension (T0 = 1.203e3 N
recovered at GRV-124); gamma = gamma_grav = 4.21e-4 J/m (GRV-073).
GRV-070: the source/metric overlap residue R_0 ~ (a+b)(c+d) is
UNDERIVED (registered as such).

## B2 -- OUTCOME GRAMMAR (exhaustive)
- FORK-RESOLVED-(A): both registered branch stiffnesses land the
  T0-class order (within the ~7-order T/gamma spread), Lambda_nat
  >> 6e18 for any O(1) residue; the GRV-122 attribution is EXCLUDED
  at registered coefficients; birefringence and frame dragging
  DECOUPLE. The named escape must be priced: a residue fine-tuned
  to ~1e-18 near GRV-070's null lines (a = -b or c = -d) could
  lower Lambda_nat into the window -- state whether any registered
  mechanism supplies it.
- FORK-RESOLVED-(B): a registered coefficient sits ~9+ orders
  STIFFER in the squared combination (see B3 correction), landing
  the window. (Expected unreachable.)
- STILL-BLOCKED: coefficients registered ambiguously.

## B3 -- NAMED CORRECTION owed to GRV-124's face (house rule)
GRV-124's fork wording said branch (B) needs stiffness "~18 orders
SOFTER." WRONG DIRECTION: Lambda_nat ~ 1/stiffness^2, so lowering
Lambda_nat from ~1.5e37 to ~6e18 needs the stiffness combination
~2.5e18 LARGER (~9 orders stiffer linearly), or equivalently a
residue suppression ~4e-19. The registered numbers in GRV-124 are
untouched; the direction word on its face is corrected here by
name. (Operator prose failure; the registry's numbers were right.)

## B4 -- REFUSALS
Condition 4 unchanged; no LARES verdict; residue values not
invented; gamma-vs-T branch assignment for the metric shift left as
GRV-070 left it (underived), with BOTH branches priced so the fork
verdict is branch-assignment-independent.
