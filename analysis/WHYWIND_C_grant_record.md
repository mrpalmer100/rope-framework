# GRANT RECORD -- WHYWIND-C (author's grant, 2026-08-29)
# Granted in the handoff-review session. REGISTRATION NOT YET TAKEN:
# this session holds only the session artifacts (no claims.yaml, no
# tools/add_claim.py). The working session executes the insert via
# the canonical tool in a verified process-quiet window. Nothing is
# registered until then.

## DECISION
WHYWIND-C GRANTED as drafted in WHYWIND_draft_registration.md.
NOTE FOR THE INSERTING SESSION: if WHYWIND-D and -E are granted
(records pending on this desk), add forward cross-references to
their claim numbers at insert time -- the resolution flag raised
here is the same question those claims adjudicate.

## CLAIM TEXT (for the canonical inserter)
Brick 2a (mode-resolved sector rotation on the retained 2c
states): SEL-OPEN, evidence-bearing. The j = n selection rule's
prediction is contradicted (kappa = 0 at all derived resonant
lines) while total pt share reproduces FND-152/153. The winding
motion is identified: baseline = the level-1/level-2 carrier beat
ladder (n = k, m = -k(N2-N1)) on both cells; through the 4/3
collapse the motion migrates to the single high harmonic
+-(n=17, m=19) at 74 pct -- with n at phi-Nyquist-1, raising the
resolution flag adjudicated (unsuccessfully) by stage 3.
Evidence: analysis/WHYWIND_rotation_bars_LOCKED.md,
analysis/WHYWIND_rotation_results.md,
benchmarks/foundations/whywind_rotation.py.

## STANDING
Author's grants so far: WHYWIND-A, -B (separate records), -C.
WHYWIND-D..F, the FND-152/153 rider, and the queue action remain
on the desk.
