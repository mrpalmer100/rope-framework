# COMMISSION EM-RECON-035 -- THE WINDOW-NARROWING COMMISSION -- BARS LOCKED (2026-08-15)

Locked BEFORE any constraint is assembled. Charter: bound the
idealization ratio r_w/xi of EM-RECON-034 from REGISTERED STRUCTURE
ONLY, from outside the indication, and test the razor-thin edge
[0.0500, ~0.0502] against the assembled bound. Consumption bar carried
over verbatim: EM-RECON-034's INDICATED may not be consumed; this
commission may kill it, contain it, or reframe it, but never cite it
as evidence for any input.

## QUESTION
What does the registry already refuse to allow r_w/xi to be -- where
r_w is the capped core radius of the bound-mode source and xi is the
mode's transverse decay length -- and does the refused region contain,
exclude, or straddle the indicated edge? PRIOR QUESTION, gating the
first (found at reading, fixed here before assembly): are r_w and xi
each ONE registered object across the closure's two targets, or does
the registered structure assign per-sector values? EM-RECON-009's own
target definitions use two denominators (nuclear spacing-to-RANGE;
chemical bond-to-HEALING-LENGTH) that differ by roughly four orders of
magnitude in physical units, while the numerator is a rope-level
radius on FND-110's ladder. If numerator and denominator do not scale
together, a single-ratio window is ill-posed and the honest output is
a REFRAME, not a bound.

## METHOD (B1)
Symbol inspection and registered-value assembly only. Every input
carries its provenance and a SOFTNESS GRADE fixed here:
- HARD: registered as derived or adjudicated (e.g. w = a/sqrt3 as a
  relation; the hard-core existence from FND-MATTER hard-core d_c).
- MEDIUM: registered as a modeled relation with named free inputs
  (e.g. R ~ a sqrt(N/4pi), FND-MATTER-002, N and a free).
- SOFT: consistency-window numbers only (e.g. N ~ 1e12 at a ~ 1e-16
  m, explicitly a window not a pin).
No SOFT input may, alone, exclude or contain the edge; SOFT inputs
may only be DISPLAYED with their softness stated. New numerics
limited to arithmetic on registered numbers; no integral, no fit, no
re-run of the closure.

## OUTCOMES, pre-committed (B2)
- NARROWED: registered structure yields a band for a well-posed
  single ratio, graded HARD/MEDIUM; verdict then CONTAINS, EXCLUDES,
  or STRADDLES the indicated edge. EXCLUDES converts EM-RECON-034's
  INDICATED to FAIL by outside evidence (registered on both faces).
- REFRAMED: the registered structure assigns per-sector ratios (the
  prior question answers "not one object"); the single-ratio window
  is registered ill-posed; EM-RECON-034's existing d0/xi curve is
  re-read PER TARGET against each sector's assembled ratio, with no
  new computation (the curve exists; reading it at another abscissa
  is not consumption). Each per-sector reading carries the softness
  grade of its inputs.
- TOO-LOOSE: the assembly yields no constraint at MEDIUM or better
  that bears on the edge. Registered as a verified absence; the
  indication stands exactly as registered, neither strengthened nor
  weakened.
- Mixed outcomes are admissible (e.g. REFRAMED with one sector
  NARROWED and the other TOO-LOOSE) and reported per sector.

## REFUSALS (B3)
- No consumption of the indication, in any direction, including as a
  prior on any input.
- No pinning of n_rs, N, a, or xi; no touching FND-MATTER-003's
  obstruction or FND-110's ladder.
- No re-run of the closure benchmark; the registered curve is read,
  not recomputed.
- No promotion of a REFRAMED per-sector reading into a pass or fail
  of EM-RECON-034's registered verdict, which was rendered under its
  own locked grammar and stands as history either way; consequences
  are registered as annotations naming their grammar difference.

## DELIVERABLE (B4)
One registered claim (EM-RECON-035) with the prior-question ruling,
the constraint table with provenance and softness grades, the verdict
per B2, annotations to EM-RECON-034 (and EM-RECON-008 if its ledger
moves), CHANGELOG, verify_corpus --quick green, re-zip.

## FAILURE MODES NAMED IN ADVANCE
- Laundering a SOFT number (N ~ 1e12) into a kill or a confirmation.
- Treating the reframe as a rescue: if per-sector reading goes badly
  for the closure, it is registered going badly.
- Deriving under cover of assembling (the standing discipline).
- Forgetting the day's lesson: every cited claim's VERDICT read
  verbatim, not its title.
