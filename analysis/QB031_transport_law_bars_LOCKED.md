# QB-031 bars — LOCKED before computation (2026-08-02)

Commission: derive P1 -- the frame transport law -- from two-strand geometry. P1 has
been the production campaign's one posited link (QB-029, QB-030).

The derivation route, stated before computing:
- GRV-020 (Derived): the two-strand rope's internal symmetry is G = R_slide x
  SO(2)_rot with ONE Goldstone, the azimuth. Parallel transport of a frame along
  the strand can only accumulate action generated by the medium's internal
  generators: slide acts trivially on frame ORIENTATION, so the holonomy group on
  frames is generated by SO(2)_rot alone -- rotation about the LOCAL TRANSPORT
  AXIS. No other generator exists to rotate about. This is the transport-law
  theorem: hol = R_axis(Delta), forced.
- The angle: Delta = the accumulated interior azimuth difference. Its
  deterministic part (the helical screw tau_0 L) is common calibration; its
  FLUCTUATION is exactly the delta QB-029 measured from the pinned field. What
  QB-029 posited as P1's noise variable is thereby identified, not assumed.
- The axis: the transport axis is the strand tangent, whose orientation relative
  to the frame vector is set by SOURCE GEOMETRY. New, weaker premise P1'
  (named): the source orientation is isotropic over trials, so the axis is
  uniformly distributed relative to the frame.

The algebra to verify symbolically (before the rerun):
- Uniform-axis average of a rotation by delta: <R_n(delta)>_n = ((1 + 2 cos
  delta)/3) I. Hence the DERIVED effective visibility is V' = <(1 + 2 cos
  delta)/3> = (1 + 2<cos delta>)/3 -- with QB-029's <cos delta> = 0.78, V' =
  0.853, ABOVE the 0.78 QB-030's model used.
- QB-030's model (rotation always perpendicular to the frame) is the WORST CASE
  of the derived law: restricting n perp f gives <R> f = cos(delta) f. QB-030's
  CHSH = 2.039 is therefore a LOWER BOUND under the derived transport.

Rules fixed in advance:
- R1: the rerun uses the derived per-trial map exactly -- full Rodrigues rotation
  of the transmitted frame by delta about a uniformly random axis -- with
  everything else (analyzers, bank, trial counts, no-signaling check) held at
  QB-030's values. Same-run closed-form self-consistency is the reference, per
  QB-030's environment flag.
- R2: first-order expectation locked: S_derived ~ V' x S_perfect ~ 2.33.
  Deviations beyond 10% from first order are investigated, not absorbed.
- R3: no tier motion. P1 moves from POSITED to DERIVED-GIVEN-P1' (the isotropy
  premise is geometric and weaker, and it is named, not hidden). QB-030's number
  is reclassified, not erased: it stands as the worst-case bound.

Bars:
- B1: the transport theorem stated with its GRV-020 dependency; slide triviality
  and screw-calibration points recorded.
- B2: the axis-average identity verified symbolically (sympy), both for uniform n
  (the derived law) and for n perp f (QB-030's worst case).
- B3: the empirical bank rebuilt (same recipe as QB-030); <cos delta> within 10%
  of QB-029.
- B4: the experiment rerun under the derived law at M = 200000 per pair; perfect
  and severed brackets rerun; no-signaling < 0.015; the result compared to the
  locked first-order expectation (rule R2).
- B5: propagation: QB-029's and QB-030's P1 annotations updated; the campaign's
  premise ledger closes with P1' as the sole remaining geometric premise.
