# FND-150..153 INTERPRETATION RE-PRICING -- CHARTER (LOCKED
# 2026-09-01, per the author: accuracy-first, both criteria scored;
# approved "let's follow your recommendation")

## Scope
Re-price the INTERPRETATION content of FND-150..153 against the
post-Q54 evidence. Rates, D bounds, and all measurement claims
UNTOUCHED. No re-adjudication of any column verdict. No new solves.

## Evidence table (FIXED)
E1. FND-163: Q54-COLLAPSE; the chartered FLAT prediction falsified.
E2. The registered hypothesis split at 5/4 (WHYWIND dispersion doc,
    section P): naive proximity predicted COLLAPSE (0.12 pct from
    the n=4, m=10, j=2.5 line); the j=n refinement predicted FLAT
    (nearest j=n line +33 pct). FND-163 vindicates naive proximity
    and FALSIFIES the j=n law on the cell built to split them.
E3. Q54-GRID-BOUND: the n=17 deep tangent object grid-bound in 2/2
    tested collapse cells (B36 0.9332 -> B54 0.0011).
E4. FND-160/161/162: the P42/S3R conviction lineage.
E5. The four registered column verdicts: 3/2 COLLAPSE, 4/3
    COLLAPSE, 5/3 FLAT, 5/4 COLLAPSE.
E6. The registered R2 failure (dispersion doc): 5/3 (FLAT) sits
    1.37 pct from its line, CLOSER than 3/2 (COLLAPSE) at 1.87 pct
    -- naive proximity's registered ordering miss.
E7. The registered R1 window (2 pct) and line lattice convention:
    integer-harmonic lines Om1 + n*Om2 = j*Om1, i.e.
    Om2/Om1 = (j-1)/n; order as tabulated in the dispersion doc;
    half-integer j lines exist where m is not a level-1 harmonic.

## Criterion C1 -- naive proximity (LOCKED grid, 12 variants)
Anchors: (i) the registered anchor set (the dispersion doc's
measured deepest-member placements for 3/2, 4/3, 5/3; the
registered interpolation for 5/4), (ii) measured deep-end anchors
uniformly (each cell's deepest registered member, 5/4 from the Q54
profile). Distances: (a) plain relative |d|, (b) order-weighted
k*|d|. Line sets: order k <= 3, <= 4, <= 5. 2 x 2 x 3 = 12
variants, one formula each, computed once.
SCORE per variant: does the FLAT cell (5/3) rank FARTHEST of the
four from its nearest line? n(C1) of 12.

## Criterion C2 -- the j = n (self-degenerate) law (LOCKED)
Per cell: nearest j = n line distance at the same two anchor sets;
the law retrodicts COLLAPSE iff within the registered 2 pct window.
Score against E5: n(C2) of 4 per anchor set (report both).

## Verdict forms (LOCKED)
R-SUPERSEDE: n(C1) >= 10 -> proximity robustly orders all four;
  FND-150..153 interpretation demoted to a proximity-based
  successor (draft claim for grant).
R-SPLIT: otherwise, if n(C1) >= 3 OR n(C2) >= 3 on either anchor
  set -> both mechanisms partial: harden FND-150..153 riders to
  name both criteria and E2's falsification of the j=n law; the
  deliverable is a FIFTH-CELL advance prediction chosen where the
  surviving variants disagree, plus the brick-2 coupling
  matrix-element commission named as the resolution path.
R-HOLD: n(C1) <= 2 AND n(C2) <= 2 on both anchor sets ->
  FND-150..153 stand with an E3 caveat rider only.
ALL forms: mandatory E3 rider on any surviving collapse-side
interpretation (grid-bound deep tangent, 2/2 tested; 54-grid
profile replication named as the open test).

## Deliverables
The 12-variant table + both C2 tables (one script, logged);
verdict; rider/claim texts as drafts for the author's grant;
analysis/REPRICE_results.md.

## AMENDMENT A1 (pre-computation, 2026-09-01): the dispersion doc's
tabulated "order" column is not mechanically reproducible (no single
formula fits the three rows). The C1 line-set axis is therefore
defined mechanically: lines Om2/Om1 = (j-1)/n with j = m/N1
(m any positive integer), 0 < (j-1)/n <= 1.25; richness axis
n_max IN {2, 4, 6}. Distance variants: plain |d| and n-weighted
n*|d| (coupling-order proxy). Recorded before any number was
computed.
