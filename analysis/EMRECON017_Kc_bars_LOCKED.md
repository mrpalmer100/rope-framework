# EM-RECON-017 BARS — K_c derived, not argued (LOCKED before computing)

Date: 2026-08-09. Commission: EM-RECON-013's flagged edge ("K_c ~ O(k) is
argued... not derived"), run on operator GO.

## The stakes, sharpened at charter time (before any computation)

EM-RECON-013: k_eff = k·K_c/(k+K_c); core exists iff k_eff > T0. Its note
already recorded that K_c = k requires k > 2T0. FND-027 has since
adjudicated k = 2T0 EXACTLY. Therefore at the registered branch:
  K_c = k  →  k_eff = T0 exactly  →  c4_eff = 0: MARGINAL, NO CORE.
**Survival strictly requires K_c > k.** The old "O(k)" argument, even if
correct, is INSUFFICIENT — a discovery made at chartering and recorded
here before the derivation runs.

## Bars

1. **The survival threshold is K_c/k > 1 STRICTLY**, committed here.
   K_c/k = 1 is registered as marginal (core vanishes), not survival.
2. **Registered inputs only**: P-VOL kinematics (width² = w0²/(1+ε)),
   the finite contact form Ac/(1+(d/σ)⁴) (FND-KIN-005/FND-STRAND-001),
   the cubic weave geometry (crossing density 2/a per strand length),
   k = 2T0 (FND-027), coverage-threshold placement (FND-MATTER-004).
3. **Permitted outcomes, all at full strength**: (a) K_c/k derived > 1 —
   core survives, "argued" upgraded to "derived"; (b) derived < 1 or
   marginal — CORE CONDITION FAILS, registered adversely against
   EM-RECON-009/013 with consequences traced; (c) reduction — K_c derived
   IN FORM with survival reducing to a named, currently-unregistered
   material ratio (e.g. Ac/(k·a·…)), registered as a precise open number,
   not a rescue.
4. **No invented calibrations**: if a material ratio is missing, it is
   NAMED, its threshold value computed, and left open. Assigning it a
   value that lands survival is refused by name.
5. **The assembly question displayed**: whether bulk over-density strain
   localizes in contact zones or distributes uniformly changes K_c by
   ~1/φ²; the selection must be derived from the threshold placement, not
   chosen.
6. Nothing re-fitted; spend stays at ONE.
