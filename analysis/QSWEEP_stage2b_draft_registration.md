# STAGE 2b -- DRAFT REGISTRATION (NOT REGISTERED)
# Prepared 2026-08-26 (FND-151 suggested). AWAITING THE AUTHOR.

TITLE: E-DIRECTION: the winding velocity is elevated 2.7x in the
collapse region of the collapsing branch and unchanged on the
flat branch (double ratio Q = 2.515 against the pre-registered
line of 2); on this data the amplitude collapse coincides with
arclength rotating into the direction sector

STATEMENT (draft): From the stage-1 record's surviving gated
states only (zero new solves; statistic and thresholds locked in
advance), the pt-velocity V = RMS_nodes(delta pt mod 2pi)/delta
s(free sector) measures: matched ramp interval V(4/3) = 0.00498
vs V(5/3) = 0.00446 (ratio 1.12: signature-absent while healthy
-- the control); anchor-region within-branch ratios 2.704 (q =
4/3, where dA2/ds had collapsed 5x) vs 1.075 (q = 5/3, rate
flat); double ratio Q = 2.515 >= 2: E-DIRECTION fires. Read with
FND-150 (the winding field is load-bearing), the collapse
coincides, on this data, with arclength rotating from the
amplitude sector into the direction sector: dA2/ds falls as
d(pt)/ds rises, and S1-SPLIT's rationalization-selectivity reads
as cell-dependent winding demand. SCOPE: two intervals per
branch (anchor intervals at different A2 by data survival);
verdict "on this data"; the FND-147 interpretive freeze is NOT
lifted; the quantitative account (does V's rise conserve the
arclength the rate loses?) requires the chartered profile
experiment.

EVIDENCE: analysis/QSWEEP_stage2b_results.md;
analysis/QSWEEP_stage2b_charter_LOCKED.md; the stage-1 checkpoint
(analysis/qsweep_stage1_ckpt.pkl) as the sole data source.

AUTHOR DECISIONS REQUESTED: (1) grant / amend / decline; (2)
charter the profile experiment (re-march both branches at ds =
0.02-0.08 with per-step state retention; pre-registered ledger
test: is |dx/ds|^2 apportionment between sectors conserved as
the rate falls?); (3) whether FND-151 registers now or waits to
merge with the profile result.
