# COMMISSION TSADE3 -- RESULTS (FND-109, 2026-08-13): CODIFICATION -- THE LATTICE IS THE REGULATOR, NOW BY RULE

*Bars: analysis/TSADE3_zeropoint_cutoff_bars_LOCKED.md. Census
executed over every zero-point/Casimir title hit in the registry.*

## The census

Nine families face-read (GRV-004, GRV-021, FND-MATTER-008/014/016/
055/056, FND-034/035; the remaining title hits are consumers of
these, not independent ledgers). Findings, uniform across all nine:

1. **Provenance:** every registered zero-point ledger predates
   GRANT-SUBSTRUCTURE-TIGHT (2026-08-12). Each was built on the
   ontology in which the strand is the finest object; there were no
   sub-strand modes for any integral to include, and no post-grant
   claim has extended any integral downward.
2. **Cutoff parameterization:** every family's mode content is
   regulated at COARSE lengths -- and one family already says the
   convention aloud: GRV-021, verbatim, "FINITE at every stiffness
   with NO imported UV cutoff -- the lattice is the regulator."
   GRV-004's theorem runs on local network mode structure; the
   FND-MATTER mass ledger's band is cutoff-set by the coarse weave
   (FND-MATTER-055) with knot/bend/contact costs on the coarse host;
   the dilute-Casimir suppression is pi (r/a)^2 coarse geometry
   (FND-MATTER-056); the packing-pressure arc (FND-034/035) is
   strand-contact physics.

No family is ambiguous; no family reaches fine scales. Neither the
SPLIT nor the AUTHOR-DECISION branch has anything to bite.

## The codified convention (per the pre-committed grammar)

**ZP-CONV (registered rule):** the corpus's zero-point ledger
integrates CARRIED (strand-level) modes only; the coarse strand
lattice is the regulator (GRV-021's registered statement, promoted
from one family's finding to the registry-wide convention it has in
fact always been). Sub-strand relative modes -- which the
over-resolution condition places strictly outside the carried band
-- are OUTSIDE the ledger.

**TRIPWIRE (registered with the convention):** any future claim
that integrates fine (sub-strand) modes into a zero-point quantity
must declare the departure from ZP-CONV on its face and price the
n_sub-dependent additive piece it thereby buys, including where that
piece goes. Silent fine-mode integration is a bar violation.

## Effect

SHIN8's Q5 CLOSES: with ZP-CONV registered, the zero-point item
upgrades from CONDITIONAL to INVARIANT, and the refinement-invariance
audit's verdict becomes SIX OF SIX, NO BREAK. EM-RECON-033 is
annotated accordingly. The substructure arc is now fully protected:
every load-bearing coarse quantity audited invariant, the fine
constant refinement-proof, and the one located hole filled by
codifying what the registry had been doing all along.
