# FND-MATTER-060 — the derived lever cashed in: all three knots inside the bar, uniform overshoot, and the ZPE band restated

Date: 2026-08-09. Commission: the propagation brick following FND-MATTER-059.
Benchmark: `benchmarks/foundations/matter060_lever_propagation.py`.

## Bars (locked before computing)

1. Nothing re-fitted: λ = 6π(r/a)² applied as derived; all registered inputs
   inherited unchanged; spend stays at ONE.
2. Per-knot confrontation with the inherited factor-2 bar against each
   knot's own demanded λ (the 1.51× demand spread is intrinsic; per-knot
   landings are the honest resolution).
3. The compliance refinement (059's lead) may not be invoked to rescue any
   miss.
4. Permitted outcomes: all inside / tension at exact scope; sign structure
   noted as data either way.
5. The band restatement is bookkeeping, not physics.

## (A) Per-knot confrontation

| knot | demanded λ | derived/demanded | predicted lever (reg. ~25%) | verdict |
|---|---|---|---|---|
| ring | 1.361e-5 | **1.22×** | 29.0% | INSIDE |
| trefoil | 9.029e-6 | **1.84×** | 38.1% | INSIDE |
| 5_1 | 1.203e-5 | **1.38×** | 31.6% | INSIDE |

**All three knots inside the 2.00× bar.** Worst residual 1.84× (trefoil).

The sign structure is data: the derived λ sits ABOVE every knot's demand —
a uniform overshoot, not a scatter. That is consistent in sign with 059's
named compliance refinement (a thin inclusion deforms strands rather than
terminating them, recovering mode content and strictly reducing λ), and
inconsistent with a random miss. The refinement was not invoked (bar 3);
the consistency is recorded as a pointer, not a rescue.

## (B) The ZPE band, restated

The scale campaign carried the zero-point lever as an unfixed ~25% with a
factor 2–3 propagated band ("the ZPE bar") through the M-point comparisons,
the l_q/a window, and the closure error bars. With the lever derived, the
honest new half-width is the worst-knot residual: **1.84×**, aggregate
landing 1.44×. Downstream consumers can re-run against the derived lever
with unchanged inputs; only their error model narrows.

## Grade and scope

Modeled. The lever's promotion (059) is corroborated at per-knot resolution:
the sector's first derived lever survives its first propagation confrontation
with every knot inside the bar and a physically-interpretable residual
structure. The trefoil's 1.84× is the tightest squeeze and the natural test
article for the compliance computation when it is run.

## Not claimed

An exact λ; the compliance correction's value; any lepton mass (PM-004
stands); any change to registered inputs; any new parameter.

## Next brick, named

The compliance correction, computed rather than gestured at: the mode
content recovered when ambient strands deform around (rather than terminate
on) a thin inclusion, as a derived reduction factor on λ. Prediction
pre-installed by the sign structure above: the correction must move all
three knots DOWN together, and if it lands the ring near 1.0× it must land
the trefoil near 1.5× (the demand spread is intrinsic and cannot be beaten
by a universal λ — a correction that "fixes" all three exactly would be
over-fitted on its face).
