# COMMISSION BET6 -- BARS LOCKED BEFORE COMPUTING

**Locked:** 2026-08-11, before any arithmetic.
**Charter:** go after w -- but audit the NAME before attempting the NUMBER.

---

## 0. Why this is a census and not a determination

FND-029 blocks on "the constituent width w" and calls it UNREGISTERED.
At least three registered objects wear the name w:

  (W1) EM-RECON-018's MESH STRAND width, w/a = 0.362, DERIVED from the
       coverage threshold 3 pi w^2/(4 a^2) = f_c at f_c = 0.309.
  (W2) QGATE-004's FLUX-TUBE CONSTITUENT width in n_t = f_c (D/w)^2,
       whose demanded point is w ~ 1.5e-3 fm.
  (W3) ELEC-050's w in [0.0395, 0.0565] fm, propagated from the lattice
       flux-tube width band.

EM-RECON-018 POSTDATES FND-029, so FND-029's "unregistered" may be
stale. This is the kappa-rename class (HANDOFF author's-desk item 4),
appearing a second time on a different symbol, and now load-bearing for
the localised sign (FND-064), matter stability, and the hbar branch.

**Determining a number for the wrong object would be worse than
determining nothing.** The census runs first.

## 1. Hypothesis under test

**H-COLLISION:** W1, W2, W3 are not all the same object. If W1 is the
object FND-029's m_b actually needs, then w is already registered and
BOTH censuses execute today without new physics.

## 2. Pre-committed verdict classes

- **V-ONE-OBJECT** -- all three are the same w; a single value follows;
  both censuses execute.
- **V-COLLISION** -- two or more distinct objects share the name; each
  census is re-pointed at its correct object and the registry owes a
  rename.
- **V-UNDECIDABLE** -- the definitions in the registry are not sharp
  enough to tell; reported as such, no number quoted.

## 3. Bars

**B1 (DEFINITIONS BEFORE NUMBERS).** Each of W1, W2, W3 must be stated
as a DEFINITION (what physical length, measured between what) from its
own claim, before any value is computed or compared. Numerical
proximity is explicitly NOT evidence of identity -- HANDOFF section 0's
resemblance rule, which this arc has already violated twice.

**B2 (THE a DEPENDENCY MUST BE EXHIBITED).** W1 is registered as a
RATIO w/a, so any value depends on which a is used. The registry
carries more than one: the Lorentz bound a <= 1e-16 m (FND-MATTER-005,
used by NUCQ-003/ELEC-050) and FND-040's re-solved pair
(a = 1.63e-17 m, and 9.53e-18 m at the second kappa reading). Every
quoted w must carry its a. A w quoted without its a is refused.

**B3 (NO IDENTIFICATION ON PROXIMITY).** If two objects land within an
order of each other, that is REPORTED and explicitly REFUSED as
evidence of identity unless a definitional argument is given. This bar
exists because the arc's own guard disclosures have twice been the
interesting part.

**B4 (NO CENSUS EXECUTED ON AN UNRESOLVED NAME).** If the verdict is
V-COLLISION or V-UNDECIDABLE, this commission may NOT run the FND-029
survival census or the QGATE tube census. Re-pointing is allowed;
executing is not.

**B5 (STALE-VALUE SWEEP, mandatory).** Every claim quoted must have its
bracketed annotations read before its face value is used. The arc has
failed this twice; it is now an explicit bar with a written check.

**B6 (NO NUMBER INVENTED).** No w may be supplied by this commission
from outside the registry.

**B7 (ANTI-RESCUE, carried).** Fourth consecutive commission in this
chain. No magnitude claimed against FND-047's k-string data.

## 4. What would make this a failure

V-UNDECIDABLE -- legitimate, reportable, and would itself convert the
"measure w" programme into "define w first," which is a result.
