# GR54 -- 54-GRID PROFILE REPLICATION (CHARTER, LOCKED 2026-09-01;
# authorized by the author: "Let's charter the 54-grid profile
# replication")

## Question
Is the q = 5/4 collapse PHENOMENOLOGY grid-robust? The FND-163
discriminators fired on the 144x36 profile; every line-based
mechanism has since fallen (re-pricing 0/12 and 3/4-falsified;
brick-2 linear coupling null), the deep tangent's n=17 content is
grid-bound in 2/2 tested cells, and the n=17 object appears in the
36-grid linear response. The remaining suspect is the grid itself.

## Protocol (locked)
1. Seed pair: phi-continue profile points s0 and s1 to 144x54 by
   the S3R rule (phi-only FFT zero-pad, a2 pin at each member's own
   A2, SJ-CREDENTIALED instrument, stage-1 full bars, 60-round
   budget each). The rider's gated 54 members (s10, s11
   continuations) stand as far-end anchors, display only.
2. Arc profile at 144x54: the registered stage-2c protocol
   verbatim from the 54 seed pair -- ds = 0.08, BUDGET = 12
   points, stage-1 full gates per point, C3-credentialed
   measurement formulas (rate, V_pt, f_dir; dpt wraps on the
   54-grid pt sector, N = 144*54). Fresh '144x54:arc:n2=5' pattern
   measured at the gated s1 continuation (faults 9/10/11 rules).
3. SEAL: per-point rate/V_pt/f_dir recorded, never printed; only
   display-class A2 surfaces. The discriminators are computed ONCE
   after the profile completes or refuses at budget (refusal at
   cum >= 60 recorded; verdict reads the profile as-is).
4. Discriminators, verbatim FND-153-era lines: r(logV_pt,
   -logRate) vs 0.8; f_dir rise (mean last 3 - mean first 3) vs
   +0.15. Correlation and rise are scale-free, so ds = 0.08
   verbatim is comparable across grids; absolute rates are NOT
   compared (dimension differs), only the two locked statistics.

## Verdict forms (LOCKED)
GR-ROBUST:   >= 8 gated points AND both lines fire at 54
             -> the collapse is physical; the mechanism hunt goes
             to second order (carrier-mediated commission ripens).
GR-ARTIFACT: >= 8 gated points AND neither line fires at 54
             -> the 36-grid collapse phenomenology does not
             replicate; FND-163 gains a grid-scope rider (verdict
             itself untouched -- it is a 144x36 statement by
             charter); the largest re-pricing in the corpus
             follows as its own commission.
GR-MIXED:    >= 8 gated points, lines split -> recorded; the
             second-order commission decides priority.
GR-OPEN:     < 8 gated points -> recorded with the refusal trace.

## Extension clause (contingent, not run by default)
A 5/3 flat-cell control profile at 54 runs only on the author's
explicit extension.

## Inputs / cost
36-grid profile states (registered); the rider protocol machinery;
no other new inputs. Estimated 2-4+ sessions: two 54-grid seed
solves (~1 session each by the rider's precedent), then 12 arc
points at 54-grid cost.

## Deliverables
Driver benchmarks/foundations/gr54_profile.py; durable
analysis/gr54_ckpt.pkl per gated point; results
analysis/GR54_results.md; claim drafts for the author's grant.
