# PRED-003-LOCK bars — LOCKED before computation (2026-08-02)

Commission (PRED-003-DICT's decider): determine which registered modulus curves the
on-site locking potential, thereby selecting l_q among {a, sqrt(l_lock a), l_lock}.

Pre-computation excavation findings (rope_microscopic_mechanics.docx read in full),
recorded now so the bars adjudicate them rather than absorb them:
- THE PROVENANCE MYSTERY RESOLVES: the paper derives the continuum stiffness
  K = T^2/(kappa a) (scalar) and K = 2 T^2/(kappa a) (DIRECTOR, two-mode) — the P6
  expression "alpha ~ 2T^2/(kappa a)" IS the director-field EM stiffness coefficient,
  registered and Derived via FND-001/FND-002 with benchmark. What was never registered
  is the stiffness-to-alpha closure, exactly as PRED-003-CONST found.
- THE MODEL HAS NO ON-SITE LOCKING: the registered microscopic EM model is the pure
  lattice XY model, E = J Sum (1 - cos(dtheta)) over BONDS, J = T^2/kappa. The
  "on-site locking potential" of DICT's decision question does not exist in the
  registered model. The corpus's own FND-KIN-003 states it: "the pure XY vortex has
  no width knob (its core is one plaquette)".
- A JOINT CONSTRAINT NOBODY HAD COMBINED: the photon sector carries TWO registered
  stiffness statements — OPT-006's Derived medium impedance Z = T/c, and the paper's
  coarse-grained director stiffness K = 2T^2/(kappa a). If both describe the same
  photon sector (premises below), matching impedances ENSLAVES kappa to the other
  primitives.

Premises, stated as premises:
- P1: the theta-field (torsion/orientation Goldstone) IS the photon sector
  (registered: torsion waves are light, EM-RECON-012 lineage).
- P2: OPT-006's impedance is that same sector's (it underwrites the derived optics).
- P3: any strand-count normalization between per-strand T and bulk K is a pure O(1)
  number eta (unresolved; flagged; exponents are eta-independent).
- P4: l_q is a defect-scale length of the registered model (carried from DICT).
- P5: G ~ 1/(Ta) remains ASSUMED (PRED-003's own standing caveat).

Rules fixed in advance:
- R1: All findings — including any change to the sole T1's registered ratio —
  reported at full volume. No tier motion either direction.
- R2: The eta ambiguity may not be resolved by choosing a value; only exponent-level
  (eta-independent) conclusions may be asserted.
- R3: Confrontation numbers recomputed under any updated ratio must use the
  registered PRED-003-CONF/META data verbatim, nothing new retrieved.

Bars:
- B1 (provenance resolution, machine-checked): the paper text contains the
  2T^2/(kappa a) director coefficient; FND-002 is Derived with benchmark; the
  "registry-underived" verdict of PRED-003-CONST is REFINED: the stiffness is
  derived, the closure was the unregistered step (and DICT supplied its form).
- B2 (the no-on-site theorem): the registered model's energy is bond-only (machine
  check on the paper text: XY bond sum, no on-site term); FND-KIN-003's registered
  one-plaquette-core statement quoted; hence the registered model contains exactly
  ONE defect length, a. DICT's candidates 2 and 3 require structure the registered
  model does not contain.
- B3 (the enslavement): under P1-P3, Z_theta = K/c = eta T/c forces
  kappa = 2T/(eta a), hence l_lock = T/kappa = eta a/2 — PROPORTIONAL TO a for any
  pure eta. The DICT candidate table COLLAPSES: all three candidates give
  alpha ~ T a^2 (triple (1, 2), kappa eliminated as an independent channel), and
  CONST's closure reduces to the same form under the enslavement (consistency
  verified symbolically — the "middle candidate" and "candidate 1" merge).
- B4 (the ratio, updated): with primitives reduced to (T, a) and G ~ 1/(Ta):
  TENSION channel gives d ln alpha/d ln G = -1; SPACING channel gives -2. The
  registered -2 is thereby REASSIGNED from the tension channel to the spacing
  channel; the tension-channel value is -1. Verified symbolically.
- B5 (confrontation update, rule R3): under ratio -1 (tension) recompute from the
  registered data: the forward tension (expected ~same sigma, null-vs-null), the
  inverse commitment Gdot/G = -(alphadot/alpha), the 3-sigma refutation threshold,
  and the J1713 implication. Report whether the prediction's testability survives.
- B6 (propagation): PRED-003, CONST, CHAIN, DICT annotated; the paper's P6
  correction queue updated; premises P1-P5 carried into the claim text verbatim in
  substance.
