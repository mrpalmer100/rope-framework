# COMMISSION KAF2 -- THE FUSION CONFRONTATION: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number is computed. Chartered from
FND-077's named next-order (the bundle-binding derivation against PHI's
calibration point). Extends KAF (FND-052); KAF's registered law is NOT
modifiable here.

## The question

Can PHI's measured point (kappa_rel, delta_fusion) = (2.25, +0.125)
(FND-035) decide the bundle count n_b, and which registered mechanism
owns the point?

## Registered inputs, closed at lock

- PHI's definitions, from the claim face: kappa_rel = (C_A/C_F)/rho_R^2
  with rho_R = 1.0 within errors and C_A/C_F = 9/4;
  delta_fusion = sigma_adj/(2 sigma_f) - 1 = +0.125 measured.
- FND-037's Derived linear response: energy density and tension
  proportional to the Casimir, sigma_D/sigma_f = C_D/C_f.
- KAF's registered overlap family (FND-052): E(f, k, x) =
  f e(k^2 x) + k (1 - f) e(x), e(x) = T0 (x/2 - x^2/8), both quartic
  signs carried, registered strain domain x <= 0.04, minimum over f
  taken when the configuration is free.
- The adjoint reading enumerated, CLOSED AT LOCK:
  R-FREE: the two unit windings minimize E over f (KAF's original
    reading; prediction delta = 0 at registered strains).
  R-COHERENT: the adjoint source forces f = 1 (same source, no
    separation before screening); prediction delta = e(4x)/(2 e(x)) - 1.
  R-RECRUIT: FND-037's charge-level response; delta = C_A/(2 C_F) - 1,
    no strain, no f, no bundle quantity.

## Guard disclosures (pre-lock, displayed so refusal is auditable)

G1: scoping noticed the candidate identity delta = kappa_rel/2 - 1
    (exact if rho_R = 1), which would make the point ONE measurement in
    two coordinates. To be machine-checked first; if confirmed, every
    downstream statement must treat the point as one degree of freedom.
G2: scoping noticed R-RECRUIT lands on the point exactly. The
    confrontation is run anyway for all three readings, and R-RECRUIT's
    hit, if confirmed, is labeled a RETRODICTION (the same lattice
    record underlies FND-037's ledger), not a prediction.

## Q1 -- the identity audit
Machine-check delta = kappa_rel/2 - 1 from PHI's registered definitions.

## Q2 -- the three-reading confrontation
Each locked reading's delta against the measured +0.125, exclusion
factors printed, both quartic signs for the strain-based readings.

## Q3 -- the discriminator verdict
Does ANY surviving reading contain n_b? If no surviving reading does,
FND-077's named decider route is adjudicated EMPTY and successor
n_b-carrying observables are NAMED (not chartered): the bundle contact
multiplicity m_b (EM-RECON-018's machinery), the strand-engine
penetration-barrier spectrum as a function of bundle size
(FND-STRAND-004's registered instrument), and any lattice observable
resolving constituent substructure. Chartering is the author's.

## Verdict grammar, pre-committed

- POINT-CONSUMED: G1 identity confirmed AND a registered n_b-free
  reading hits the point within the data precision. The point cannot
  decide n_b; FND-077's route is adjudicated empty; any strain-based
  reading missing beyond L1 is registered as excluded FOR THE ADJOINT.
- DISCRIMINATOR-LIVE: the identity fails, or a reading carrying n_b
  survives confrontation. The route stands.
- ALL-MISS: every reading misses beyond L1. New physics demanded;
  goes to the desk.

## Adverse outcomes pre-authorized

POINT-CONSUMED is an adverse outcome for FND-077's hope and is
registerable without rescue. Failures kept.

## Deliverables

benchmarks/foundations/kaf2_fusion_confrontation.py;
analysis/KAF2_fusion_confrontation_results.md; claim via
tools/add_claim.py; annotations to FND-077, FND-052, FND-035, FND-037.
