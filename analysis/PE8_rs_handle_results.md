# THE r_s HANDLE SESSION (PE8) -- RESULTS (2026-08-16)

Bars: analysis/PE8_rs_handle_bars_LOCKED.md (clean-room held; the
derive-point entered only at the comparison).

VERDICT: NO-REGISTERED-HANDLE tonight -- the question "is r_s =
0.2496 a_f" cannot be answered from the current registry -- but the
exhaustion delivered a provenance correction, an exact equivalence
that converts the question into a speed, and the handle inventory
in final form.

## LEG A -- provenance: the 0.355 ceiling retires
FND-091's slenderness ceiling descended from the kb bound via the
STRING reading; under the granted rod identity + KBSAT, kb is
r_s-independent and that derivation is SUPERSEDED. The standing
registered ceiling is the EXISTENCE one: r_s <= 0.3529 a_f
(k_f/T0_f >= 4.046). Superseded-not-erased; recorded here.

## LEG B -- the sweep: none pins tonight
Packing/clearance: no registered contact rule (grant-class if ever
wanted). Zero-point: FND-109 is a convention, not a length
(readable, no pin). Density: mu_f = T0_f/c^2 is FORCED (the SHIN
transverse-c invariant) but rho_f is unregistered. Dispersion: no
registered fine longitudinal measurement.

## LEG C -- THE SPEED EQUIVALENCE (exact; the session's keeper)
For the granted rod, r_s cancels from the longitudinal speed:
    c_L,f = c sqrt(k_f/T0_f)        [exact]
so under KBSAT the r_s question converts EXACTLY:
    r_s / a_f = 0.7099 c / c_L,f
"IS r_s = 0.2496 a_f" IS "IS THE FINE LONGITUDINAL SPEED 2.844 c."
And the ladder identity from the FND-117 factorization:
    c_L,f / c_L,coarse = sqrt(gamma_1 gamma_2) = 2.0115  [exact]
with the coarse anchor registered (sqrt(2) c, FND-114's channel
floor). The three forms are ONE question: is r_s = 0.2496 a_f
<=> is c_L,f = 2.844 c <=> is the coarse->fine stiffness mapping
exact. Any dispersion-shaped session or bound on the fine
longitudinal channel now lands on the sheet directly through the
speed form -- the instrument class most likely to arrive first.

## STANDING
The trap holds, unanswered and fully armed, now attackable in three
equivalent registered forms. Riders: KBSAT (tripwire armed),
coherence, k/T0 = 2, condition 4 unchanged.
