# Commission TET -- the lattice confrontation: locked bars

*Locked 2026-08-11, before any literature is read. CHET's pin meets
data: kappa_pack = (C_D/C_f - 1)/(2|delta_D - delta_f|), forward
predictions delta_A - delta_F = -1.25% (kappa = 50) / -0.25%
(kappa = 250), sign NEGATIVE (higher representations under-bind),
regime = the confining/intermediate region (BET's tube-strain
expansion), NOT the short-distance Coulomb regime (T3 firewall, where
the Anzai-Kiyo-Sumino quartic lives).*

## The search plan, pre-committed

Sweep for lattice determinations of Casimir scaling of the static
potential across representations (adjoint, sextet, higher; k-strings)
with quantified precision in the confining regime. Primary known
anchors to verify and quantify: Bali's Casimir-scaling study (SU(3),
multiple representations); k-string tension literature (Casimir vs
sine law); any modern (2010s-2020s) percent-level updates. 3-6
searches; original sources preferred over reviews.

## Admissibility rules

- A1: the measurement must be in the CONFINING regime (intermediate r,
  before adjoint screening flattens the potential). Short-distance
  data is T3-firewalled and inadmissible for the pin.
- A2: the quantity must map to delta_D - delta_f (relative deviation
  of V_D/V_f from C_D/C_f, or tension ratios for k-strings with the
  Casimir prediction as reference).
- A3: precision must be quantified in the source (a stated bound or
  error), not inferred by us.
- A4: adjoint string breaking is a real regime limit: if the admissible
  window (confining but unscreened) is too narrow for a clean
  determination, that registers under ILL-POSED-IN-PART, not as a
  rescue of the prediction.

## The inversion arithmetic, pre-committed

- A confirmed scaling bound |delta_D - delta_f| <= b in the admissible
  regime implies kappa_pack >= (C_D/C_f - 1)/(2b). For the adjoint
  pair (ratio 2.25): b = 2% -> floor 31; b = 1% -> floor 62.5;
  b = 0.5% -> floor 125; b = 0.25% -> floor 250.
- A resolved NONZERO negative deviation d measures kappa_pack =
  1.25/(2|d|) exactly (adjoint pair) and must land >= 50 for the
  FND-040 5% floor to survive; a resolved POSITIVE deviation
  (over-binding) at long distance KILLS the BET sign (Derived), which
  is registered at full volume if found.

## Verdict grammar (pre-committed)

- EXCLUDES: an admissible bound b < 1.25%/... tight enough to exclude
  a floor reading -> the excluded reading is registered dead at the
  data's confidence; if b < 0.25% both readings die and FND-037's
  tower takes a registered wound.
- TIGHTENS: an admissible bound implies a kappa_pack floor ABOVE 50 ->
  the data-derived floor supersedes the CS-bound floor; numbers
  re-solved (a, T0, g, all downstream) with pointers.
- CONSISTENT-AND-ARMED: existing precision brackets neither reading ->
  the pin stands with the required precision registered as a demand.
- ILL-POSED(-IN-PART): screening forecloses the admissible window for
  a rep class -> regime clause filed on the pin; k-strings or other
  unscreened probes inherit the pin if admissible.

## Honesty clauses

1. Every number cited to its source; no precision asserted beyond what
   the paper states. Conflicting determinations reported, not
   averaged away.
2. The sign convention is checked per source before use (deviation
   definitions differ); a sign error here would falsely kill or
   falsely save a Derived claim.
3. If the literature attributes observed deviations to known lattice
   artifacts or mixing, that attribution is carried, not stripped.
4. One sweep; findings registered as found.
