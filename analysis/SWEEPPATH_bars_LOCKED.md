# COMMISSION SWEEP-PATH -- THE FINE-WEAVE PATH-FACTOR TIGHTENING -- BARS (LOCKED)

*Locked 2026-08-18 before computing. The follow-up FND-135 flagged and
the handoff ordered first: the fine-weave coverage leg carried the
reading-B path factor; the load-bearing reading (A, by the BLOCH-L
isotropy control) gives a smaller one. Direction pre-disclosed by
FND-135: coverage DOWN, packing headroom UNCHANGED (the headroom ratio
carries no path factor). This is a TIGHTENING session unless the legs
say otherwise.*

## SCOPE

IN: recompute the coverage bracket f_{c,f} under reading A, with
SHIN8's own blindness exclusions inherited verbatim (B0: no r_f, no
gravity, no conduction twist, no coarse radius); sweep the registry
for every live quote of the reading-B factor and disposition each;
confirm the compensation identity is untouched (it is swap-invariant
-- that is WHY the original sweep missed the path factor, and the leg
must say so rather than silently re-pass it).

OUT, named: SHIN8's Sigma leg (a_f cancels; no path factor); the
handedness leg; the energy bill's own two-level bracket (already
superseded in value by FND-135; its 2.409 is history, disposition =
annotate-only).

## THE LEGS

1. THE IDENTITY CONTROL: 2/(sin^2 psi_1 sin^2 psi_2) recomputed;
   must land on the registered fine ratio to the SHIN8 tolerance.
   Swap-invariance stated on the face.
2. THE COVERAGE RECOMPUTE: SHIN8's leg-1 arithmetic verbatim, path
   factor replaced by the reading-A value, both ceiling counts.
   Both SHIN8 assertions (f < 10%, packing headroom < 1) re-checked.
3. THE QUOTE SWEEP: every live (non-history) occurrence of the
   reading-B factor in the registry, each with a disposition
   (recompute / annotate / already-superseded).

## PRE-REGISTERED OUTCOME SHEET

- Coverage falls by exactly the factor path_A/path_B and both SHIN8
  assertions still pass -> TIGHTENED: the coverage bracket is
  re-registered at the reading-A value with the correction attached by
  name; FND-115's text stands (history).
- Either assertion fails under reading A -> BREAK: report, do not
  adjust, hand to the author.
- The identity control fails -> HALT (the input layer is wrong and
  nothing downstream is trustworthy).

## CLEAN ROOM

No derive-point family value appears. Registered text is not edited;
corrections attach by name. Bars not edited after lock; deviations
recorded, not smoothed.
