# WHY-WINDING RE-PRICING (option b) -- RESOLVED-BAND f_dir: BARS
# (LOCKED 2026-08-29, before any banded number is computed)
# Authorized by the author ("Let's do c": this commission first, then
# the 144x42 confound-free probe). Addresses the registered doubt of
# WHYWIND_stage3_results.md against the INTERPRETATION of
# FND-150..153's deep-end f_dir. Scope: interpretation only; no
# registered claim's NUMBERS change here.

## OBJECTS
The retained 2c states (analysis/qsweep_stage2c_ckpt.pkl, read-only),
both cells, the 12 profile tangents each, alignment as credentialed
(profile point i = arc pair states[i+1], states[i+2]).

## LOCKED DEFINITIONS
- Identity of record (verified in brick 2a): the registered f_dir_i
  equals ||t_pt||^2, the pt-sector share of the unit tangent's
  squared arclength.
- BAND SPLIT, fixed here: RESOLVED band = pt Fourier content with
  |n_phi| <= 12 (two-thirds of the 144x36 phi-Nyquist 18; the beat
  ladder lives at |n| <= 5, the suspect mode at 17). NYQUIST band =
  |n_phi| >= 13. Bands partition the pt content exactly
  (Parseval): f_dir_i = f_res_i + f_nyq_i.
- Profile statistics: rise = last-point minus first-point value,
  matching the FND-152 statistic's form.

## LOCKED VERDICT LINES
  RP-HOLDS: f_res rise over the 4/3 profile >= +0.15 (the FND-152
      registered line applied to the resolved band alone) AND the
      5/3 f_res rise <= +0.05.
  RP-FALLS: f_res rise over the 4/3 profile < +0.05 (the collapse's
      winding demand is Nyquist-band content).
  Between: RP-PARTIAL. In all cases the f_nyq trajectories are
      reported. No salvage; no post-hoc bands.

## CONSEQUENCES (in advance)
  RP-HOLDS: FND-150..153's mechanism sentence survives re-pricing
      unamended; the stage-3 doubt attaches only to the mode
      identity, not the demand.
  RP-FALLS: an amendment to the FND-152/153 interpretation is
      DRAFTED for the author (the demand is grid-band content;
      interpretive weight moves to the 144x42 probe).
  RP-PARTIAL: both riders drafted; the 144x42 probe adjudicates.
Grants reserved to the author. Failures kept.
