# COMMISSION TAV2 -- THE BUNDLE-CONTACT SCALING MEASUREMENT: RESULTS

Executed 2026-08-12 under analysis/TAV2_bundle_contact_bars_LOCKED.md.
Benchmark: benchmarks/foundations/tav2_bundle_contact.py.

## Verdict (locked grammar): LATERAL-EXCLUDED + AXIAL-UNRESOLVED

Neither the discharge branch nor the conviction branch fires cleanly.
The measurement replaced one commentary with two facts and one open
fit, and the bars are applied exactly as written.

## G-LAT (the priced picture): full-section is EXCLUDED by measurement

Fitted exponents p = 0.310 (hex) and 0.257 (f_c), half-sweep stable.
Both sit BELOW the LINE band [0.40, 0.60]: UNCLASSIFIED under the
locked grammar, kept without re-binning. The registered commentary is
vindicated harder than it was priced -- lateral bundle-bundle contact
at the TAV gaps engages a SUB-LINE population (the contact zone is
one facing column, and its energy saturates in depth). Full-section
lateral contact is excluded at both packings by the engine's own
energetics. The n_b readout set is EMPTY laterally.

## G-AX (the bound-web candidate): UNSTABLE, no classification, kept

Fitted p = 1.226 (hex) and 1.189 (f_c), but the half-sweep stability
check FAILS at both packings (1.262 vs 1.132; 1.218 vs 1.113, drifts
0.13 and 0.105 against the 0.10 bar). The drift is monotone DOWNWARD
with n -- consistent with edge-dominated super-linearity relaxing
toward p = 1 -- but the locked sweep ends before the fit settles, and
extending the sweep after seeing the numbers is refused as
bar-shopping. UNSTABLE is registered and kept. In consequence:

- The DISCHARGE branch does not fire: Chain A's conditional is NOT
  converted to a measured geometry premise today.
- The CONVICTION branch does not fire either: conviction requires a
  classified non-full-section result, and an unstable fit trending
  toward the full-section band is not that. Manufacturing a kill from
  an unresolved fit would be the mirror image of a rescue.

## Displayed and not leaned on (two items, per the resemblance rule)

1. THE MULTIPLICITY-PER-CONSTITUENT: at G-AX the effective engaged-
   pair count runs m_eff ~ 4n (hex) / ~3n (f_c) -- each facing
   constituent engages several opposite ends at these gaps. MEM's
   Chain A inversion identified m_b with n one-to-one. If the engine's
   multiplicity were adopted, the pin m_b = [63.0, 73.4] would invert
   to n ~ 16-24, OUTSIDE every registered window -- the tension clause
   fires: registered as a TENSION on the face, not resolved, not
   adopted. Whether MEM's m_b counts pairs (engine currency) or
   engaged constituents (in which currency the engine gives
   full-section m = n exactly and the tension dissolves) is a
   DEFINITION question owed to MEM's locked bars, flagged for the
   author.
2. The sampled pin entry at n = 19 (G-AX hex) is the same tension in
   sampled form and carries no independent weight.

## What survived, what is owed

- TAV's BUNDLE-CONSISTENT grade: untouched. MEM's survival pass:
  untouched. GRANT-THRESHOLD: untouched, falsifiers unchanged.
- Chain A's full-section conditional: STILL OPEN, but now bracketed by
  measurement -- excluded laterally, unresolved axially, and carrying
  a new named definition question (pair-vs-constituent multiplicity).
- OWED to a successor commission, with fresh bars: (i) the asymptotic
  G-AX exponent on a widened sweep (the fit's settling point is the
  cheap decisive number); (ii) the MEM multiplicity-currency audit
  (open MEM's bars and read the m_b definition verbatim before any
  inversion is quoted again).
