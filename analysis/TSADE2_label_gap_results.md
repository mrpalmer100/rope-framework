# COMMISSION TSADE2 -- THE LABEL GAP ON POST-SHIN LENGTHS: RESULTS

Executed 2026-08-13 under analysis/TSADE2_label_gap_bars_LOCKED.md.
Benchmark: benchmarks/foundations/tsade2_label_gap_rerun.py. Bar
1.4 PeV (m = 1), conventions and grammar exactly as locked.

## Verdict (locked grammar): ASSIGNMENT-SPLIT

- L-STRAND: UNDERIVABLE. Every available coarse candidate misses,
  exactly as TSADE found (G1 0.16 MeV, G5 58 MeV, G3 1.06 TeV, G2
  20.7 GeV; G4 clears but remains killed-unregistered per FND-058).
  Nothing post-SHIN changed the coarse table.
- L-SUB: CONDITIONALLY-DERIVED. The window-edge lower bounds G6/G7
  land at 0.891 PeV = (4/2pi) x bar exactly -- AT-BAND, inside
  FND-060's O(1) structural factor, foreseen and disclosed at lock
  (guard G-B). The consistency reading G6' then closes the distance:
  the substructure was adopted TO carry the m x 1.4 PeV photon, and
  by FND-060's ceiling theorem the fine mesh can do so only if
  hbar c / a_f >= m x 1.4 PeV up to the same O(1) -- so any
  excitation whose minimum size is the sub-strand scale, including
  a label excitation under L-SUB, costs at least the carried photon
  energy up to that O(1). The gap is inherited from the carrier's
  own consistency, not from a new scale.

## What the split means for the desk

The GRANT-N2-GAP decision is TRANSFORMED, not made. The question is
no longer "grant a new unregistered scale or leave four disturbances
open." It is now the assignment question plus one condition:

- IF the label rides the SUB-STRAND (L-SUB, the colour-like reading:
  the coarse strand's label constituted by its constituents), the
  gap is CONDITIONALLY DERIVED from already-paid machinery --
  FND-087's substructure plus FND-060's ceiling -- at zero new
  scales. GRANT-N2-GAP dissolves into an ASSIGNMENT STATEMENT (a
  far cheaper act: no new primitive, no new scale, only a reading of
  where GRANT-N2's label lives, natural under the substructure) plus
  the named condition below.
- IF the label rides the COARSE STRAND (L-STRAND), the gap remains
  underivable and GRANT-N2-GAP stands as the only route at its full
  FND-056 price (one new unregistered scale).

## The named condition and its certificates (what would firm L-SUB)

CONDITION: FND-060's ceiling theorem transfers to the fine mesh (the
theorem was proved for the registered coarse transverse class; its
Gershgorin/periodicity structure is generic, but the transfer has not
been exhibited on the wound fine geometry).
CERTIFICATES, either sufficient:
- C1 (internal, one session): a fine-level ceiling check on the SHIN
  engine -- the corpus already owns the instrument that verified the
  wound carrier's Bloch structure (FND-089); the same instrument can
  exhibit the band ceiling at the fine spacing.
- C2 (external, free): any confirmed photon above 2.2 PeV forces
  m >= pi/2, at which point the WINDOW-EDGE bound G6 itself clears
  the 1.4 PeV bar with no O(1) appeal (0.637 m >= 1). LHAASO
  continues to observe in this range; the certificate may arrive on
  its own.

## Honest edges

- G6/G7 are lower bounds by construction (a_f <= lambda/4 is the
  registered statement); the true fine spacing can only be smaller
  and the gap larger. Nothing here can push the gap DOWN.
- G8 (fine locking energy) is deep sub-eV across any n_sub window --
  informative that the LOCKING channel is not the gap mechanism;
  the gap under L-SUB is a BAND-CEILING statement, not a locking
  one. Reported per the full-table rule.
- This commission adopts nothing: the assignment is GRANT-N2's to
  clarify and the author's to rule; the four FND-056 disturbances
  remain formally open until either the assignment ruling plus C1/C2
  or GRANT-N2-GAP closes them.

## On the author's desk (replacing the prior GRANT-N2-GAP item)

Rule the assignment: (a) ASSIGN-SUB (label rides the sub-strand;
cheap, colour-like, gap conditionally derived; C1 session or C2 data
then firms it), or (b) ASSIGN-STRAND (label rides the coarse strand;
GRANT-N2-GAP at full price is then the only route), or (c) hold the
assignment open (four disturbances stay open; the k-string
discriminator content of GRANT-N2 is untouched either way).
