# HBAR-011 bars — LOCKED before computation (2026-08-02)

Commission: repair HBAR-010's dependency defect (ELEC-064: its argument opens from the
retired relation hbar ~ w^2). Re-derive the cosmological constraint from the SURVIVING
standing-wave form S = pi T A^2 / (2c) (ELEC-054), which the hbar-sector retirement left
untouched, and commit specific instruments in place of "any medium-coupled observable".

Rules fixed in advance:
- R1: The derivation may use ONLY the surviving form. Any appearance of hbar ~ w^2,
  hbar c = N^2 T w^2, or the strand spacing w as the carrier is a failure of the session.
- R2: No third branch may be invented to rescue tier. The tier verdict rule is locked:
  if the restored exclusion yields a constraint shared with standard physics in
  confirmation, the verdict is CONSTRAINT (T3 ceiling), whatever the arithmetic shows.
- R3: All instrument values must be the corpus's already-registered data
  (PRED-003-CONF / PRED-003-META): Yb+ E3/E2 PTB alpha-dot/alpha = 1.0(1.1)e-18 /yr;
  quasar many-multiplet |delta alpha / alpha| <~ 1e-5 over z ~ 0.5-2. No new retrievals.
- R4: The ELEC-054 openness (no mechanism selects A) must be carried into the claim as a
  branch condition, not absorbed. If the comoving-amplitude branch cannot be shown forced,
  the result is a trichotomy, and the claim must say so.

Bars:
- B1 (derivation): From S = pi T A^2/(2c) with T a strand property, show d ln hbar =
  2 d ln A exactly, with L absent (it cancelled in ELEC-054) and w absent (R1). Machine
  check: symbolic/numeric independence of the reconstructed hbar from L and w.
- B2 (comoving branch): If A dilates with the scale factor (A ~ a), compute
  alpha(z)/alpha_0 = (1+z)^2 and the exclusion factor against |delta alpha/alpha| = 1e-5
  at z = 0.5, 1, 3. Report whichever way it falls.
- B3 (rigidity inversion): |delta ln A| < (1/2)|delta alpha/alpha| < 5e-6 over the
  quasar range, and the per-year bound |d ln A/dt| < (1/2) x (2-sigma clock bound).
  Both numbers computed, not quoted.
- B4 (dependency hygiene): benchmark asserts by inspection of its own source that no
  retired relation is used; forward_check acknowledged for HBAR-006 lineage.
- B5 (tier verdict): apply R2 mechanically. Expected outcome stated now so it cannot
  drift: CONSTRAINT with committed instruments, T4 -> T3 ceiling, NOT a return to T1/T2.
