# GRV-082 bars — LOCKED before computation (2026-08-02)

Commission (GRV-081's named successor): the reservoir's temperature -- the
whisper's provenance. Can the corpus say WHY the near-horizon emission has the
temperature it has, rather than that it has one?

The derivation chain, stated before computing (each link's source named):
- L1 (GRV-081, measured): the ratchet deposits a definite energy per broken
  crossing, order the threshold scale.
- L2 (P-ENT, premise): a crossing is a two-state object (intact/broken --
  GRV-037's bit), so the reservoir's temperature is set by energy-per-bit:
  T_res ~ e_bit up to the two-state O(1). Named, not derived.
- L3 (P-TH, premise from registered grammar): the local breaking threshold is
  set by the pressing budget per crossing, and the pressing is Rindler-class
  (GRV-038 with GRV-077's derived load): e_th(s) ~ K c^2/s in proper distance.
  Named; the proportionality of threshold to pressing is grammar, not theorem.
- CONSEQUENCE if L1-L3 hold: T_res(s) ~ c^2/s ~ a_proper -- the UNRUH FORM,
  with every O(1) free: the whisper's temperature profile has the acceleration
  shape because the bit-cost inherits the pressing profile.

The numeric leg (doubling as the queued bit-cost sweep):
- L1 is testable: rerun GRV-081's crossing at e_th in {0.01, 0.02, 0.04} and
  measure energy-per-broken-crossing. The chain requires e_bit to TRACK e_th.
- Bar: linearity -- e_bit/e_th constant across the sweep to 25% (the ratchet's
  order-one, not a precision claim). If e_bit does not track e_th, L1's
  'threshold scale' reading fails and the chain is registered broken at L1.

Rules fixed in advance:
- R1: the sweep uses GRV-081's engine verbatim except e_th; entrance-exam
  conservation is spot-checked at the extreme e_th (bar 1%).
- R2: no thermodynamic claims beyond the profile's SHAPE: no flux, no spectrum,
  no graybody, no comparison to Hawking's coefficient. The shape and its
  provenance chain are tonight's entire claim.
- R3: premises P-ENT and P-TH are carried visibly; the claim is
  Unruh-CLASS-conditional, and says so in its title.

Bars:
- B1: conservation spot-check per R1.
- B2: the bit-cost sweep; linearity per the 25% bar.
- B3: the chain assembled: T_res(s) ~ a_proper, conditional on P-ENT and P-TH,
  with each link's provenance printed.
- B4: propagation: the whisper lineage (GRV-049..053) gains a temperature
  provenance; what tonight does NOT do (flux, spectrum, coefficient) is listed;
  next-orders named.
