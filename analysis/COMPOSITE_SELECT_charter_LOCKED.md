# COMMISSION COMPOSITE-SELECT -- CHARTER (DRAFT, NOT LOCKED)
# THE LEVEL-2 AMPLITUDE: WHAT SELECTS IT, WHETHER THE FAMILY REACHES
# IT, AND THE RATIONAL CELL AS A CONFOUND -- THE COMPOSITE ARC
# REFRAMED AFTER GR54
# Drafted 2026-09-02 for the author's desk, against corpus v3.29.0 plus
# the post-release state (S3R, Q54-COLLAPSE and its GRID-BOUND rider,
# REPRICE R-SPLIT, BRICK2 B2-OPEN, GR54 IN PROGRESS). Locks ONLY on the
# author's word, and NOT before GR54 has rendered its verdict (see
# GATING). Executes strategic item 4 of the 2026-09-02 read.

## WHERE THE ARC STANDS (the honest sentence, before the question)
Since 17 Aug the composite build has produced: the two-frequency
torus state EXISTS at machine precision (FND-142); it is PIN-UNREACHED
-- the family gates to A2 ~ 0.005-0.011 against the registered
R2 = 0.09396, an order of magnitude short, and steepens toward a
singular parametrization (FND-142/143); the amplitude-growth collapse
along the aligned branch is rationalization-selective (FND-147:
4/3 collapses, 5/3 flat, 3/2 collapses; post-release: 5/4 collapses
and the author's FLAT prediction of record was falsified, FND-163);
every line-based mechanism for WHY is dead or floor-bound (dispersion
proximity 0/12, the j = n law falsified, first-order coupling null --
REPRICE, BRICK2); the deep-end n = 17 tangent object is a 144x36
discretization artifact in both collapse cells (S3R, Q54 rider); and
whether the collapse PHENOMENOLOGY is grid-robust is GR54's question,
running now. Two things the arc has never asked: what SELECTS the
level-2 amplitude (R2 was inherited from the static SHIN hierarchy
that KBSAT convicted, FND-131), and whether the rational cell q is a
confound in its own right (the physical ratio is 1.492; 3/2 is 0.5
percent away; every other cell run is 10-17 percent away and om2 is
strongly q-dependent, FND-147). The Sigma_wave deferral (hold-with-
rider vs retreat to the level-1-exact 2.598) has stood since FND-139;
the price DISPLAY at the last converged member is 2.62 T0
(TRUESTATE stage 2), a number that barely moves across the reachable
family.

## GATING (binding)
This charter LOCKS only after GR54 has rendered one of its four
verdicts, and its Leg B protocol is fixed by that verdict:
  GR-ROBUST:   Leg B rates are measured at the 36-density with one
               54-density spot-check cell (7/5 or 8/5, author's
               pick at lock).
  GR-ARTIFACT: Leg B rates are measured at TWO refinements per cell
               (36 and 54 densities) with a convergence bar (D
               within 20 percent across refinements, else NO CALL
               at that cell); the FND-150..163 grid-scope rider is
               inherited on every rate cited.
  GR-MIXED:    as GR-ARTIFACT.
  GR-OPEN:     Leg B is HELD; Legs A and C run (they consume gated
               members and energies, not rates).
No Leg B number is produced before the gate is read.

## THE QUESTION
  QA  SELECTION. Along the gated two-frequency family (the retained
      members of TRUESTATE, Q-SWEEP, Q54 and GR54, all cells), does
      any REGISTERED principle select a level-2 amplitude A2*, and
      does A2* lie inside the range the family actually reaches?
  QB  THE CELL. Is the collapse factor D(q) a smooth function of the
      ratio value q (so the physical branch at 1.492 is read by
      interpolation), or a function of the cell's DENOMINATOR (so
      it is a commensurability artifact of the rationalized
      instrument, and the physical quasi-periodic branch is read
      from the high-denominator limit)?
  QC  THE PRICE. With QA and QB in hand, what is Sigma_wave at its
      honest size, and what happens to the zero-point band's upper
      edge (Prediction 32, 0.779) and the kb ceiling downstream?
      The DECISION stays the author's; this commission puts the
      numbers on the desk with the retreat and hold options both
      priced.

## WHAT IS GIVEN (registered inputs; no others enter a build leg)
- The torus instrument and its credentialed sparse re-instrument
  (TRUESTATE stage 2; SPARSE-J SJ-CREDENTIALED, faults 1-11 on the
  ledger, J memos keyed on (x, pattern), rung budgets protocol-
  faithful), reused byte-identically.
- The retained gated states: analysis/qsweep_stage1_ckpt.pkl,
  qsweep_stage2c_ckpt.pkl, q54_stage1/profile/rider_ckpt.pkl,
  s3r_ckpt.pkl, gr54_ckpt.pkl (read-only).
- The registered pins: R1 (level-1 amplitude, exact), arc-mean
  tension <T> = 3/2 (tripwire quantity), gauge; R2 = 0.09396 as the
  STATIC-PROVENANCE value (FND-125/126 at worst-case pitch, the
  level-2 angle psi_2 = 59.4444 deg from FND-088's fourth-moment
  isotropy on a STATIC helix ensemble).
- The physical ratio 1.492 (TRUESTATE stage-1 bars: "q = 3/2, the
  nearest small rational to the registered 1.492 -- INSTRUMENT
  CHOICE").
- The NATIVE per-cell protocol (Q-SWEEP stage-1 bars: P1 level-1
  recovery, P2 ramp to the registered waypoints, P3 rate
  measurement; validation v1-v5 including Amendment 1's LM ladder
  and the (v5) control), and the stage-2c arc protocol (ds = 0.08,
  full-state retention, sealed per-point measurements).
- The energy machinery: FND-135's closed-form two-level kinetic
  energy; energy_bill.py's bookkeeping; the Sigma_wave bracket
  [3.222, 4.313] T0 per strand per axial length and the level-1-
  exact 2.598 (FND-134/135); the FND-139 rider.
- FND-088's moment conditions (second and fourth), FND-137's
  sixth-moment result, FND-132's material-speed identity, and
  FND-130's level-2 speed lock v_2 = sqrt(3/2 + 17.926 kb) c.

## WHAT IS NOT ASSUMED
No new solver, no new operator, no new pin. R2 is not a target in
QA; it is one of the numbers QA's answer is compared against. No
per-cell tuning of ladders, guards or budgets (the Q54 lesson: the
protocol-faithful rung budget, no hard nominal cap). No tangent-band
statistic of any kind is cited (S3R/Q54 rider: the n = 17 object is
grid-bound; nothing in this commission reads phi-band content). The
FLAT/COLLAPSE prediction game is over: no cell outcome is predicted
by the author or the session before it is measured; QB's verdict is
about the FUNCTION D(q), not about any one cell.

## SCOPE (hard boundary)
QA runs on EXISTING gated members plus at most the members Leg B
gates; no new deep marches are chartered for QA. QB is a rate-and-
A2_max measurement per cell under the registered protocol; it does
not re-adjudicate FND-163 or GR54. QC prices; it does not decide.
The anti-aligned branch and its Om1/2 resonance (FND-142) are OUT
(their own charter, the degenerate-perturbation session). The
second-order carrier-mediated coupling commission (BRICK2's F2) is
OUT; it ripens only on GR-ROBUST and is not this charter.

## DESIGN COMMITMENTS (locked at lock)
D1. THE CANDIDATE SELECTION PRINCIPLES (QA), closed list, each a
    functional of registered objects with zero new constants, each
    evaluated on every gated member as a function of A2 and cell,
    each yielding a stationary point or a crossing A2* or provably
    none:
      S-a ISOTROPY ON THE WAVE: the time-averaged tangent moment
          tensor of the gated two-frequency state (second AND fourth
          moments, FND-088's own conditions); A2* is where the
          fourth-moment deviator crosses zero. The static R2 is what
          this condition gives on the static helix; on the wave it
          may give something else. This is the principle that
          produced R2 in the first place, re-asked on the object
          that actually exists.
      S-b ENERGY: Sigma_wave(A2) per strand per axial length at
          fixed <T> = 3/2 and fixed level-1 sector (FND-135's
          machinery evaluated on gated states, not on the ansatz);
          A2* is a stationary point along the family if one exists,
          else NONE with the monotone direction recorded.
      S-c LORENTZ SATURATION AT LEVEL 2: the level-2 material orbital
          speed v_m2(A2, om2) on the gated state; A2* is where it
          reaches c (the level-1 identity of FND-132 extended one
          level). If the family never reaches c, NONE with the
          approach recorded.
    A principle that selects a DIFFERENT A2* in different cells at
    matched grid is reported as cell-dependent (and is then QB's
    business, not a selection).
D2. THE REACHABLE RANGE. Per cell, A2_max(q) = the largest full-bar
    A2 the registered protocol reaches within its charter budget
    (the collapse read as a fold, not as a rate): from the existing
    records for 4/3, 3/2, 5/3, 5/4, and from Leg B for the new cells.
    A2* is compared with A2_max at the same cell and grid.
D3. THE RATIONAL LADDER (QB). Cells at matched grid density (48
    s-points per sqrt 3, the registered density), approaching 1.492
    from both sides:
      below:  7/5 (1.400, N1 = 5), 10/7 (1.4286, N1 = 7),
              13/9 (1.4444, N1 = 9)
      above:  8/5 (1.600, N1 = 5), 11/7 (1.5714, N1 = 7),
              14/9 (1.5556, N1 = 9)
    plus the four existing cells as data. Statistics per cell: D(q)
    exactly as Q-SWEEP stage 1 defined it (rate at A2 = 0.0048 over
    rate at 0.0063, nearest-point pairing, full-bar states only),
    A2_max(q), and om2 at matched A2. N1 = 9 cells (n ~ 47k) are
    CONDITIONAL on a measured memory spike (SPARSE-J S3 precedent)
    landing under 3.0 GB peak; if it does not, the ladder closes at
    N1 = 7 and says so.
D4. THE ORDER TEST. D(q) is regressed against q (value) and against
    the denominator N1 (order) over all cells, no functional form
    beyond monotone-vs-non-monotone; the verdict is rank-based (the
    REPRICE precedent: rank statistics, no thresholds that could be
    shopped). Locked statistic: Spearman rho of D against q, and of
    D against N1, over the cells that produce a D from full-bar
    states.
D5. THE PRICE (QC). Sigma_wave computed by FND-135's closed form on
    (i) the selected member A2* if QA selects, (ii) the deepest gated
    member at the cell nearest 1.492, (iii) the level-1-exact
    retreat 2.598; and the consequence on Prediction 32's upper edge
    and on the kb ceiling (FND-141's 0.09332 chain) written for each
    of the three, as a table for the desk. No grant is made in the
    table.
D6. GR54 INHERITANCE. Every rate cited carries the grid-scope status
    GR54 assigns; every A2_max cited carries its grid.

## VALIDATION BARS (halt-grade; before any Leg A/B number is
## interpreted)
v1. INSTRUMENT: the sparse re-instrument reproduces one retained
    144x36 member per cell (4/3, 5/3, 5/4) at the C2 credential
    (RMS <= 1e-9, |dA2|/A2 <= 1e-6) before any new cell is opened.
v2. NEW-CELL CONTROL (the Q-SWEEP v2 and v5 bars): native level-1
    recovery on each new cell at the registered grades, and the LM
    ladder + guard + sub-pin protocol reproducing the registered
    q = 3/2 first waypoint at 96x36 before any new-cell ramp is
    interpreted.
v3. CLOSURE halt-grade (< 1e-6) on every accepted member on every
    cell (FND-143's promotion).
v4. FRESH PATTERN per cell at the injected rung state (faults 9/10/11
    binding: pattern keyed grid + pin_mode + cell; never measured at
    the degenerate level-1 state).
v5. ENERGY CONTROL: FND-135's closed form evaluated on the registered
    q = 3/2 member reproduces the stage-2 display Sigma(kb = 0) =
    2.62 T0 at A2 = 0.0108 to 1e-4.
v6. MOMENT CONTROL: the time-averaged tangent moments of the level-1
    sector alone reproduce FND-088's second-moment identity (1/3)
    to 1e-9 on every gated member (the level-1 sector is exact and
    shared; if this fails the averaging is wrong).
v7. CLEAN ROOM: R2 = 0.09396 appears in no QA file except as the
    comparison value loaded at QA's consequence step.

## THE LEGS
Leg 0  Controls v1-v7 (v1, v3, v4 on existing states; v2 per new
       cell as it opens).
Leg A  Selection: S-a, S-b, S-c evaluated on every gated member in
       the archive; A2* or NONE per principle per cell; A2_max per
       cell from the records.
Leg B  The rational ladder, under the GATING protocol; D(q),
       A2_max(q), om2(q); the order test (D4).
Leg C  The price table (D5); GR54 inheritance stamped.
Leg D  Results doc; the desk memo for the Sigma_wave decision with
       hold and retreat both priced; no draft claim before the
       author reads.

## PRE-REGISTERED INTERPRETATION RULES (locked before results;
## anything else is NO CALL)

QA (per principle; then a joint line):
  SEL-INSIDE: the principle yields A2* with A2* <= A2_max at the
      cell nearest 1.492 (and at every clean cell within 20
      percent). The wave state has a self-consistent level-2
      amplitude the family REACHES.
  SEL-OUTSIDE: A2* > A2_max at every cell that produces an A2_max.
      The principle selects an amplitude the family does not reach
      under the registered protocol.
  SEL-NONE: no stationary point or crossing on the family; the
      monotone direction recorded.
  Joint: COMP-SELF-CONSISTENT if at least one principle returns
      SEL-INSIDE and no principle returns a CONTRADICTING SEL-INSIDE
      (two principles selecting A2* values differing by more than
      20 percent are a contradiction, reported, no selection
      granted). Consequence: R2 is RETIRED as the level-2 amplitude
      of record (kept as the static-provenance value), the composite
      is priced at A2*, and Prediction 32's upper edge closes at the
      QC number.
  COMP-PIN-UNREACHABLE: every principle that selects returns
      SEL-OUTSIDE. The two-level rotating-wave state does not exist
      at its own selected amplitude under the registered protocol.
      Registered as a FAILURE of the two-level composite reading at
      full weight; Sigma_wave RETREATS to the level-1-exact 2.598
      with a rider; Prediction 32's upper edge is re-priced to the
      level-1 value (its two-sided band collapses to the lower
      edge plus a rider) -- the largest re-pricing the wave reading
      can take short of KBSAT's own tripwire.
  COMP-NO-SELECTION: every principle returns SEL-NONE. The level-2
      amplitude is a free parameter of the class; R2 stays the only
      registered value, flagged static-provenance; the composite
      stays bracketed; QC prices the bracket on the reachable
      family.

QB (exactly one):
  RAT-SMOOTH: |rho(D, q)| >= 0.8 and |rho(D, N1)| < 0.5 over the
      cells with a D. The collapse is a property of the ratio
      VALUE; the physical branch at 1.492 is read by interpolation
      between the bracketing cells and its D is registered with the
      interpolation on its face.
  RAT-RESONANT: |rho(D, N1)| >= 0.8 and |rho(D, q)| < 0.5. The
      collapse tracks the cell's commensurability order, not the
      physics: it is an artifact of the rationalized instrument.
      Consequence: FND-147/151/152/153/163's collapse statements
      gain a CELL-SCOPE rider (they describe rational cells, not
      the quasi-periodic state), the physical branch is read from
      the highest-denominator cells, and the anti-aliased continuum
      of the state becomes the next instrument question.
  RAT-MIXED: both correlations in [0.5, 0.8) or both above 0.8. No
      separation on this ladder; reported with both numbers; the
      next-order is a longer ladder (N1 = 11, 13) if memory allows.
  RAT-OPEN: fewer than six cells produce a full-bar D (GR-OPEN,
      memory refusals, or gate failures). NO CALL; the cells that
      did produce a D are recorded.

QC: a TABLE, not a verdict. Three rows (A2*, deepest gated near
1.492, retreat 2.598), each with Sigma_wave, the Prediction 32 upper
edge, and the kb ceiling consequence, each stamped with its
grid-scope status. The author's decision line follows the table.

Anything else (a control fails after a cell has opened; a member
gates at closure above 1e-6; a D formed from any non-full-bar state):
NO CALL at that cell; report and stop there.

## NO-RESCUE RULE
No fourth selection principle after Leg A numbers exist. No cell
added or removed from the ladder after Leg B opens except by the
memory-spike clause. No re-choice of the D definition, the matched
targets 0.0048/0.0063, or the rank statistics. No deep march to
"reach" an A2* that the protocol did not reach on its own budget; an
unreached A2* is SEL-OUTSIDE, full stop. Failures kept.

## SCHEDULING (budgets, not bars)
Leg A is post-processing of the archive: one session. Leg B is six
new cells, each a stage-1-class native run (level-1 recovery, three
waypoints, the rate march) on the sparse instrument -- N1 = 5 cells
(n ~ 26k) inside the 144x54 credential; N1 = 7 (n ~ 36k) expected
under 2 GB by the phi-major bandwidth rule; N1 = 9 conditional on
the spike. Expect one to two sessions per cell, detached,
memory-exclusive, checkpointed per member to
analysis/composite_select_ckpt.pkl; the ladder is expected to span
six to ten sessions and may be cut at N1 = 7 by the author at any
close-out with the verdict rendered on the cells in hand (RAT-OPEN
if fewer than six). Leg C is an afternoon. Results to
analysis/COMPOSITE_SELECT_results.md; the desk memo to
analysis/SIGMA_WAVE_decision_memo.md; this text to
analysis/COMPOSITE_SELECT_charter_LOCKED.md on the author's lock.

## STANDING
Registered claims untouched until the author grants. Grants reserved
to the author. This charter does not lock before GR54 renders; its
GATING section is the only part that may be edited between drafting
and lock, and only to record which GR54 verdict was read. Adjacent
commissions disclosed: CASIMIR-PLATE consumes the level-2 bracket as
a bracket (D7 there) and is unaffected by QA/QB; TRIPLE-DUTY tests
level 1 only; PEV-IDENT and FINE-GATE read a_f, which nothing here
moves. The degenerate-perturbation session on the Om1/2 root and the
second-order coupling commission remain separately charterable and
are not folded in.

DRAFT, NOT LOCKED. Lock line, the GR54 verdict read, and amendments,
if any, go below this line with their reasons.

## GATING AMENDMENT AND LOCK (2026-09-05, the only edit permitted between
## draft and lock; records which verdicts were read)
GR54 rendered GR-ARTIFACT (2026-09-02), RESCINDED by GR54-X's GR-DISPLACED
(2026-09-03, FND-164): the 5/4 collapse is physical and ~0.0003 deeper in
A2 at 144x54, sharper. GR54-A rendered A-ABSENT for 4/3 (FND-165): no
collapse to A2 0.0055 at 54, status beyond open. Neither verdict is a
name in the GATING table; the protocol is fixed here by their content:
  Leg B rates are measured at TWO refinements per cell (36 and 54
  densities; the GR-ARTIFACT/MIXED branch) with the 20 pct D convergence
  bar, PLUS a 20-POINT profile budget per cell at 54 (GR54-X lesson: a
  12-point 54 march would have called 5/4 an artifact), and every
  collapse call uses the DISC corrected rule (f_dir rise alone, C1).
  The FND-150..165 grid-scope riders are inherited on every rate cited.
  A2_max at 54 is reported with the span it was reached in.
LOCKED on the author's word ("run the next brick"), before any Leg A
number is computed. Leg A (existing members) and Leg 0 controls run in
the session; Leg B (six new cells at two densities) is a local-execution
item, driver to be built after Leg A; Leg C prices last.

## LEG B EXECUTION NOTE (2026-09-05): benchmarks/foundations/composite_legB.py
## built and smoke-tested through the 7/5 level-1 check (PASS: theta dev 0,
## Om1 rel 0, T = 3/2 exact) and the 240x36 a2 pattern build; the first
## solve at n = 17.3k was OOM-killed in the 3.7 GB session container.
## Leg B is a LOCAL-EXECUTION item (16 GB): queued after CASIMIR-PLATE and
## ANTI-ALIGNED Q1. Memory: 240x36 ~ C4 class (2.7 GB); 336x54 ~ 5 GB;
## 432x54 (N1 = 9) ~ 8 GB estimated -- within the laptop, so the D3
## conditional on N1 = 9 is expected to clear; measured peaks recorded by
## the run.
