# Commission OMEGA — the vacuum-facing re-solve under kappa_pack >= 100: locked bars

*Locked 2026-08-11. PSI froze every vacuum-facing number pending this
commission. The freeze lifts only for quantities this session re-solves.*

## Locked bars
B1 THE CLASSIFICATION, before computing: read the face of every frozen
   consumer (the fence / GRV-004 obligation; kappa_0 EM-RECON-027/029; the
   Schwinger separation and birefringence, QGATE-009/018 items; the
   proton/vacuum ratio; the M-point FND-MATTER-044 and its m_e-consistency
   chain; the Lorentz bound FND-MATTER-005; the d_c calibration HBAR-005)
   and classify each as RESCALES-CLEANLY (depends on Sigma_vac alone through
   a stated formula), MIXED (built assuming Sigma_vac = Sigma_eff -- these
   BREAK and need re-derivation, not rescaling), or UNTOUCHED (Sigma_eff- or
   tube-facing).
B2 THE RESCALINGS, computed at BOTH bound readings (kappa = 120 and 600),
   with the direction-of-coherence noted: rescalings that STRENGTHEN
   registered conclusions (more linearity margin, more invisibility) are
   consistency dividends of PSI's own logic and should be said to be such.
B3 THE COLLISION TEST, the commission's spine: determine whether any
   registered chain CAPS kappa_pack from above. If a cap k_max exists and
   k_max < 120, the corpus holds a CONTRADICTION between PSI's floor and
   that chain -- register it at full prominence, kill nothing unilaterally,
   and state which premise in each chain would have to yield (PSI's
   nonlinearity form; the chain's Sigma_vac = Sigma_eff assumption; or a
   deeper registered claim). If the M-point/m_e chain merely RE-SOLVES to a
   new (a, T0) pair inside the Lorentz bound, compute the pair and its
   consequences for the electron sector's registered numbers.
B4 THE VERDICT LEDGER: for every frozen quantity, one of RE-SOLVED (new
   value quoted, both kappa readings), STILL-FROZEN (needs its own
   commission, obstruction named), or RETIRED (the quantity was an artifact
   of the Sigma_vac = Sigma_eff conflation). The freeze lifts only on
   RE-SOLVED items.
B5 HONESTY: everything downstream of PSI's bound inherits its Conjecture
   grade -- no re-solved number may be quoted at a higher grade than the
   bound that produced it, and the ledger must carry "conditional on
   FND-037's nonlinearity form" on every line.
