# Commission PI — the de-convolution: locked bars

*Locked 2026-08-10, before extracting the widening-fit parameters from the
1302.3633 source. OMICRON located the Lisbon excess as quantum widening of the
TOTAL width and named the de-convolution as next-order: recover the INTRINSIC
width at the verdict-bearing distances and re-run the like-for-like
confrontation there, removing OMICRON's load-bearing interpretive step.*

## The exact lever, stated before the numbers
Second moments ADD under convolution: if the measured profile is the classical
(intrinsic) profile convolved with the quantum-vibration distribution, then
  w2_total(R) = w2_intr + w2_widening(R)
exactly, with no ansatz approximation. The subtraction inherits error only
from the two measured terms.

## Locked bars
B1 THE WIDENING LAW from the paper's own Fig. 11 fit (the "solid line"):
   extract the fitted form and parameter values from the source (expected
   leading-order Nambu-Goto: w2_widening = (1/(2 pi sigma)) ln(R/R0) with
   sqrt(sigma) = 0.44 GeV and R0 fitted). If the source does not contain the
   fitted constants, they are re-fit HERE to the reconstructed w2(R) points
   with the fit form and included points fixed in this bar: the paper's log
   law, all four R with finite errors, weighted by the MC-propagated errors.
B2 THE SUBTRACTION: w2_intr(R) = w2_total(R) - w2_widening(R) at every R;
   R_eq_intr = sqrt(2 w2_intr); errors propagated from both terms.
B3 THE CONSISTENCY CHECK, pre-committed: the de-convolved intrinsic width
   must be R-INDEPENDENT within errors (that is what "intrinsic" means).
   Report the drift; if the de-convolved values still trend with R beyond
   1 sigma, the widening model is inadequate and the confrontation below is
   VOID -- reported as such, not massaged.
B4 THE CONFRONTATION, same bands as XI/OMICRON (reference 0.402-0.407 fm),
   now at the verdict-bearing distances R = 6a and 8a where OMICRON could
   only say TENSION-with-an-interpretation. CONFIRMED here discharges
   OMICRON's load-bearing interpretive step by computation; TENSION or worse
   here means the intrinsic-width reading fails and OMICRON's skeptic
   reading (TENSION) becomes the registered verdict.
B5 HONESTY: the widening law is leading-order effective string theory; its
   R0 is a fitted constant; sigma is the paper's adopted 0.44 GeV. All three
   go on the face. The R = 4a point, being least widened, doubles as a
   near-anchor: the subtraction there must move it by less than its own
   error, or the widening law is over-subtracting -- a second pre-committed
   sanity gate.
