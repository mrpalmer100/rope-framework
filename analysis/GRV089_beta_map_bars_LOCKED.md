# GRV-089 bars — LOCKED before computing (2026-08-03)

Commission (GRV-088's suspect ledger, item (i), the prime suspect): beta's
parameter-dependence map. The bit-cost multiple beta = 15.67 entered the
Hawking-form coefficient linearly while measured at ONE arbitrary drive depth,
with its drive sensitivity disclosed the night it was registered (GRV-082:
fixed-amplitude deep point ran 55 percent hot). Tonight: map it, find the
engine's own physical operating point, and re-evaluate the coefficient at the
value the physics actually uses. Promotion or demotion, decided by measurement.

Definitions, fixed in advance:
- Dimensionless drive r_d = e_peak/e_th (peak smoothed energy density of the
  launch pulse over the breaking threshold). GRV-082's matched-drive sweep ran
  at r_d ~ 3.5; its exact three-digit collapse is the r_d-fixed,
  e_th-varied cut of tonight's map.
- beta(r_d, e_th) = reservoir total / broken-crossing total, GRV-081's engine
  verbatim, 50000 steps, crossing scenario (r_c = 8).

Bars:
- B1 (universality in e_th, extending GRV-082): at each fixed r_d in the grid,
  beta across e_th in {0.01, 0.02, 0.04} must collapse to within 15 percent.
  If it does, beta is a FUNCTION OF DIMENSIONLESS DRIVE ALONE and the map is
  one-dimensional; if not, the suspect is convicted outright (a bit-cost that
  depends on the threshold's absolute value is an engine artifact).
- B2 (the drive map): beta(r_d) for r_d spanning marginal to deep
  (approximately 1.5, 2.5, 3.5, 6, 10); monotonicity reported; the marginal
  and deep limits characterized. No fit form is imposed in advance.
- B3 (THE PHYSICAL OPERATING POINT, the session's teeth): the real shell does
  not choose its drive -- accretion does. GRV-081's finding: infalling waves
  blueshift past threshold and are captured MARGINALLY near the capture
  radius. Instrument the accretion scenario (sub-threshold launch, blueshift
  capture) and measure the breaking-rate-weighted distribution of e_s/e_th in
  the firing region: its mean IS the operating drive r_d*. beta_phys =
  beta(r_d*), read off the B2 map.
- B4 (the re-evaluation): GRV-088's coefficient bracket recomputed with
  beta_phys in place of 15.67; the tension's magnitude updated on the record,
  whichever direction it moves. Verdict grammar fixed in advance:
  - beta is PROMOTED to a physical function if B1 collapses and B3 yields a
    definite operating point: the coefficient then uses beta_phys, and suspect
    (i) is adjudicated (the 15.67 was the right function at the wrong point).
  - beta is DEMOTED to artifact if B1 fails.
  - The tension may worsen. Rule R5 (carried): the outcome registers at full
    strength either way; suspects (ii)-(iv) remain on the table regardless.

Rules: R1 engine parameters otherwise untouched (E_TH sweep only through the
registered mechanism); R2 no evaluation of h, K, n_x, nu; R3 the physical
claim is about THIS engine's operating point -- the real medium's r_d* is the
engine-to-physics mapping question, named if it becomes load-bearing.
