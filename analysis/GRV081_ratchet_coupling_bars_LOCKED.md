# GRV-081 bars — LOCKED before computation (2026-08-02)

Commission (specified from both ends by GRV-080): the energy-budgeted exhaustion
instrument -- GRV-037's reconnection ratchet coupled to the wave sector. The
interior model the crossing negative demands and the energy ledger the audit
demands, one construction.

Model commitments, fixed in advance:
- Fields: the wave (u, v) on the diluted-geometry background (GRV-080's operator
  verbatim), plus the intact-crossing fraction s(r, t) in [0, 1] and the
  reservoir density w(r, t) (energy absorbed by reconnection).
- Stiffness carried by intact crossings: T_eff = T s (tension exhaustion IS
  crossing loss -- GRV-034's grammar with GRV-037's object).
- THE RATCHET: ds/dt = -(Gamma/e_b) s relu(e_loc - e_th) -- load above threshold
  breaks crossings, irreversibly (s never recovers; the ratchet is one-way at
  low temperature per GRV-037's measured metastability).
- THE LEDGER, exact by construction: every joule the wave loses -- both the
  kinetic energy removed by reconnection drag and the elastic energy released
  when a loaded crossing breaks (the -T ds u_r^2/2 piece, the precise term whose
  neglect made the old instrument pump) -- is deposited into w. Nothing else
  moves energy between sectors.

Rules fixed in advance:
- R1 (the entrance exam): with reflecting walls, TOTAL energy (wave + reservoir,
  r^2-weighted) must be conserved to < 1% through a full crossing event. The old
  instrument failed exactly this; the new one does not run physics until it
  passes.
- R2: parameters (e_th, Gamma, e_b) are order-one stand-ins; only the STRUCTURE
  (threshold, irreversibility, exact ledger) is corpus-anchored. Nothing may
  lean on parameter values.
- R3: settled = residual motion < 1e-3 of run peak AND outer-shell energy
  decayed > 100x.
- R4: failures register at full strength (carried from GRV-079/080).

Bars:
- B1: the entrance exam per R1.
- B2 (sub-threshold): a below-threshold pulse must leave s untouched
  (min s > 0.999) and ring down per R3 -- the ratchet does not fire on ordinary
  waves.
- B3 (the crossing, at last): a super-threshold pulse must (a) break crossings
  along a LOCALIZED footprint, (b) deposit its energy in the reservoir, (c)
  settle per R3 to a STATIC configuration -- an enlarged exhausted region plus a
  quiescent exterior; and (d) the footprint must be PERMANENT (s never
  recovers): collapse as recorded structure.
- B4 (the bookkeeping bonus, measured not imposed): the reservoir total versus
  the integrated crossing loss -- the energy-per-broken-crossing ratio reported;
  the swallowed-energy-to-broken-count proportionality is the GRV-037 entropy
  hook and is REPORTED as an observation, with no thermodynamic claims tonight.
- B5: verdict: expected P1'' -> DISCHARGED-WITH-INTERIOR-MODEL -- every
  disturbance, sub- or super-threshold, ends static (ringdown or collapse) --
  with R2's parameter honesty carried and the ratchet-side physics (what w does
  next: temperature, whisper) named as the successor question.
