# Commission HE -- the area-selection derivation attempt: locked bars

*Locked 2026-08-11, before any registry reading or computation beyond
what FND-041/042 already put on the record. This is the L1 row's first
well-posed derivation target (GRV-093), attempted with FND-042's
metrology stakes: an external fix of l_q measures kappa_pack.*

## The question, decomposed in advance

"What selects the quantum area l_q^2/(4 pi alpha) over the cell
cross-section?" R1 is ONE equation in (T0, l_q) given hbar, so it
DEFINES l_q from T0 and cannot select anything alone. Selection
therefore requires an INDEPENDENT registered fixing of l_q (or of the
area). The commission decomposes into:

- E1 (provenance audit): how did l_q enter the registry? Trace the
  PRED-003 / HBAR-005 lineage. Adjudicate: was l_q ever given physical
  content independent of R1, or has it always been R1's definition
  wearing a length's name? Registry reading only.
- E2 (candidate selection principles, pre-named; the sweep may not
  invent others):
  - S1 invariance: the action quantum must be built from
    mesh-point-invariant quantities. Evaluate what the registry's
    invariants (the pinned K = T0 a; the fork-invariant quantum area)
    can and cannot fix. A principle that fails to select must be
    reported as failing.
  - S2 medium-expressibility: FND-042's lower-edge premise. Already
    known to bound, not select; include for completeness and say so.
  - S3 structure-tie: any REGISTERED claim tying l_q to a physical
    structure length (electron ring, core thickness, knot geometry).
    Only registered ties count; resemblance is not a tie.
- E3 verdict grammar (pre-committed): DERIVED (a principle forces the
  area; l_q externally fixed; kappa_pack MEASURED via FND-042's
  inversion) / NARROWED (selection reduces to one named missing
  derivation, registered as the dependency) / UNDERDETERMINED (current
  commitments cannot select; registered as such per the GRV-006 style)
  / FAILED (a principle selects the WRONG area; registered
  Failed-and-kept).
- E4 the numerology guard, hardened: any numeric landing found during
  E1-E2 (including anything in the r_e family adjacent to FND-041's
  flagged 3-percent coincidence) is reported at WHISPER grade with the
  look-elsewhere caveat at full volume, may not steer the derivation,
  and may not be promoted this session regardless of how well it lands.
  If a whisper lands, its kappa_pack readout may be COMPUTED and
  DISPLAYED as a conditional illustration, clearly labeled
  not-a-measurement.

## Honesty clauses

1. No rescue by re-choosing: R1's form, the alpha chain, and HBAR-005's
   calibration stay exactly as registered.
2. UNDERDETERMINED is a first-class outcome, not a failure to be
   avoided: it would convert the L1 question into a single named
   dependency with a metrology payoff attached, which is progress.
3. One pass per candidate principle; no scanning for principles that
   land.
4. The commission may not close FND-MATTER-003, may not claim hbar,
   and may not move any tier.
