# COMMISSION LAMBDA-COMPOSE (NUN-GRV14) -- BARS LOCKED (2026-08-16)

Locked BEFORE any computation.

## B0 -- NAMED HANDOFF CORRECTION (house rule: corrections are named,
     never silent)
The v3.26.37 and v3.26.38 HANDOFF and release-notes texts stated the
lambda derivation (GRV-110 condition 1) was "owed" / "the sector's
highest-leverage session." FALSE: GRV-111 (NUN-GRV7) DISCHARGED
condition 1 on 2026-08-15 -- NOT-PINNED, with the structure located
-- and GRV-112/113 carried the chain to lambda = chi (G I_p) |tau|
with chi <= 2.49e-19. The operator's session-selection prose failed
where the registry did not (the standing asymmetry, again). This
correction is carried here and in the v3.26.39 HANDOFF. The session
that IS new: composing the discharged chain with GRV-120's Lambda.

## THE QUESTION (B1)
GRV-120: ratio = Lambda x J sin^2(theta)/(Mc), Lense-Thirring at
Lambda = 1, Lambda = C_d c^3/(2 C_m), C_d ~ lambda.
GRV-111/112/113: lambda = chi (G I_p) |tau|, chi <= 2.49e-19,
chi = 0 default under any parity-symmetric vacuum ensemble.
COMPOSE THEM: what does the corpus's own lambda chain say Lambda is,
and what does GR-strength frame dragging (Lambda = 1) DEMAND of the
registered structure?

## PRE-COMMITTED OUTCOMES (B2, exhaustive)
- COMPOSED-DEMAND-REGISTERED: the composition yields
  Lambda = chi x Lambda_nat with Lambda_nat the natural-scale value
  at chi = 1; the demand for GR-strength dragging is then
  chi_required = 1/Lambda_nat, and with the registered cap this
  becomes Lambda_nat >= 1/(2.49e-19) ~ 4.0e18 -- registered as an
  INTERNAL STRUCTURAL DEMAND on the (unregistered) fine-scale
  quantity, with NO observational verdict issued.
- NUMERICALLY-CLOSED: only reachable if fine-scale (G I_p)|tau| is
  registered in usable units. (Expected unreachable: GRV-113's own
  face says "fine-scale G I_p unregistered per the FND-091 kb
  precedent." If unreachable, the gate is named, not filled.)
- COMPOSITION-BLOCKED-AT (named leg): a registered mismatch between
  GRV-120's C_d slot and GRV-111's lambda (e.g. different operator
  normalization) blocks the algebra; register the mismatch.

## STRUCTURAL CONSEQUENCES TO STATE EITHER WAY (B3)
1. THE CHIRALITY PRICE, composed: GRV-112 already registered that
   nonzero lambda prices as "the vacuum must be globally chiral."
   Composed with GRV-120: GR-STRENGTH FRAME DRAGGING, in this
   framework, REQUIRES A CHIRAL VACUUM WEAVE (chi != 0), at a
   fraction fixed by 1/Lambda_nat. State at full volume.
2. THE PARITY-SYMMETRIC DEFAULT: chi = 0 gives Lambda = 0 -- ZERO
   frame dragging. The framework's DEFAULT vacuum predicts no
   Lense-Thirring at all. State at full volume; this is the
   sector's sharpest internal statement to date and must not be
   softened.
3. The tension ledger: the two registered numbers (chi <= 2.49e-19;
   Lambda = 1 required for GR) pincer Lambda_nat >= ~4e18. Whether
   that magnitude is plausible for fine-scale (G I_p)|tau| c^3/(2 C_m)
   is EXPLICITLY not adjudicated here (fine-scale inputs
   unregistered); the demand is registered for the day the fine
   scale pins.

## REFUSALS (B4)
- Condition 4 UNCHANGED: no LARES/GP-B confrontation, no kill-test
  language, no observational verdict. "Lambda = 1 required for GR"
  is quoted as the GR point GRV-120 registered, not as a measured
  fact adjudicated here.
- No fine-scale numerics invented; the FND-091 kb precedent binds.
- No touching GRV-113's cap, GRV-112's parity structure, or the
  grant text.
- chi's channel identity: GRV-113 capped the MESH-CHIRALITY channel;
  GRV-112's lambda = chi (G I_p)|tau| IS that channel (same chi) --
  verify same-object at verdict level before composing (B1 of the
  benchmark); if they are different chis, outcome 3 fires.

## MACHINE CONTENT PLANNED (B5)
sympy, benchmarks/gravity/lambda_compose_nungrv14.py: the algebra of
the composition, the inversion chi_required = 1/Lambda_nat, the cap
propagation, monotonicity, and the chi = 0 null (Lambda = 0 exactly).
