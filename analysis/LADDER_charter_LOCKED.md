# COMMISSION LADDER -- CHARTER (DRAFT, NOT LOCKED)
# THE SHARED MODE LADDER: THE ASYMMETRY TERM'S SHAPE, SCALE AND
# COEFFICIENT, THE PAIRING COEFFICIENT BY A SECOND ROUTE, AND THE
# NUN LOOPHOLE CLOSED
# Drafted 2026-09-02 for the author's desk, against corpus v3.29.0 plus
# the post-release state. Locks ONLY on the author's word, and BEFORE
# any ladder is built. Executes the nuclear sector's standing open items
# as NUC-030 left them: "the asymmetry SHAPE (linear vs quadratic),
# state counting, untouched and untouchable by this mechanism; the
# asymmetry MAGNITUDE (NUC-019's factor 1.5-2.7 band); and v0's
# derivation", plus NUC-028's honest edge: "the 1D-strand-path
# delocalization variant was not enumerated and is a future charter".

## WHERE THE SECTOR STANDS (read before the question; corrects a loose
## sentence in the 2026-09-02 strategic read)
The 1/sqrt(A) pairing dilution is no longer an open derivation. NUN
(NUC-028) proved it unreachable by local counting (the exponent 1/2 is
not on the geometric lattice {1, 2/3, 1/3, 0}); SAMEKH (NUC-029) found
the unique realization, R4 pure off-diagonal hybridization with the
volume-normalized mediator amplitude v0/sqrt(N); the author adopted the
channel separation (NUC-030) with v0 = 16.97 MeV sealed as a target;
and the chain v0 -> p -> g -> arriving spectrum CLOSED end-to-end under
GRANT-THRESHOLD (FND-071/072/074/081, p_eff = [1.30e-3, 2.67e-3]
inside the sealed band). What that chain did NOT do is build the
collective object the hybridization lives in. The asymmetry term, by
contrast, is still where NUC-020 left it: the label model gives
|N - Z| A^(-1/3) (linear, surface-diluted, NUC-029's exponent hit is
against the MODEL's own slopes), while nature gives 23 (N - Z)^2 / A.
NUC-022 showed a shared ladder gives the quadratic form and half the
magnitude, and NUC-023 bounded that insight to DIFFERENCE quantities.
The asymmetry energy is a difference at fixed A. It is licensed.

## THE QUESTION
Build the shared collective mode ladder ONCE, from registered rope
objects, and ask three things of it:

  Q1  THE ASYMMETRY TERM. Filling the ladder with the four
      spin-isospin labels, does the energy DIFFERENCE between (N, Z)
      and (A/2, A/2) at fixed A come out quadratic in (N - Z),
      scaling as A^(-1), with a coefficient a_A that the mass table
      recognizes?
  Q2  THE PAIRING COEFFICIENT BY A SECOND ROUTE. Does the same
      ladder, under the ADOPTED R4 hybridization (NUC-030), hand back
      a mediator scale that lands v0 inside the sealed band, from the
      ladder's own level structure rather than from the branching
      chain? Two independent chains hitting one sealed number is the
      strongest object this registry builds; one hitting and one
      missing is the sharpest diagnostic it has.
  Q3  THE NUN LOOPHOLE. Does the un-enumerated 1D strand-path
      delocalization variant produce the -1/2 exponent LOCALLY? If it
      does, R4's non-locality was bought unnecessarily and NUC-030
      returns to adjudication; if it lands on the geometric lattice,
      NUN's theorem is complete and the adoption is strengthened.

## GRADE CEILING (stated first, so no session oversells)
The ladder is FILLED. Filling presumes (a) mode quantization -- one
quantum per mode -- which is QGATE's open programme, entering here
exactly as NUC-029 already lets it enter R4 ("the volume normalization
presumes mode quantization"), with hbar entering ONCE through the
registered conversion and nowhere else; and (b) an exclusion rule --
two per level per ladder -- which is the registered spin-mode
saturation proposal at CONJECTURE grade (KNOWN_LIMITATIONS: Pauli is
incomplete). Every result of this commission is therefore
Modeled-CONDITIONAL on those two named premises, with the grade
ceiling written on every registration. The one-fence circularity ban
is respected in the only way available: the quantization is not
derived here and is not claimed; it is CONSUMED, labelled, and its
falsifier (QGATE fails -> every result here returns to adjudication,
as NUC-030 already carries) is inherited verbatim.

## WHAT IS GIVEN (registered inputs; no others enter a build leg)
- The mode-overlap binding (NUC-004): a nucleon bundle carries a
  discrete transverse mode ladder; the Yukawa form derives from it.
- The bundle radius and saturation geometry (NUC-017): R_bundle =
  1.013 fm at the confinement reading; the confining sphere for A
  nucleons R = (3A / 4 pi rho)^(1/3) at the registered rho.
- The bundle wave speed from registered mechanics (mu = T/c^2, the
  registered T and the bundle's registered tension reading), used to
  convert the ladder's wavenumbers to frequencies. NO Fermi-gas
  formula: NUC-022's E_F = 38.4 MeV was a quadratic-dispersion
  textbook ladder and is a CONTROL here, not an input.
- The four spin-isospin labels (NUC-008/021): protons and neutrons on
  separate ladders.
- The registered mode-quantization conversion (QGATE; FND-071's
  t = hbar Gamma lineage) for the energy per level.
- The label model's own asymmetry channel as adopted: the diagonal
  cross-sublattice cost (NUC-021) diluting as A^(-1/3) (NUC-029/030).
- The R4 hybridization form (NUC-029): the odd nucleon coupled to
  N = A/2 mediator pair configurations at amplitude v0/sqrt(N).
- The sealed targets, loaded ONLY in Leg 5: a_A = 23 MeV (SEMF,
  with the standard kinetic/interaction apportionment displayed);
  the table's asymmetry coefficient by A-bin (from the AME table
  already in analysis/, the NUC-026/027 machinery); v0 = 16.97 MeV
  with its band as converted in FND-071.
- NUC-028's four enumerated channels and NUN's exponent lattice, for
  Q3.

## WHAT IS NOT ASSUMED
No Fermi-gas kinetic energy, no hbar^2 k^2 / 2m, no nucleon mass in
the ladder. The ladder's DISPERSION is derived from the bundle's
registered wave mechanics (linear at leading order unless the
registered rod terms say otherwise; whatever it is, it is derived and
then used, not chosen). No apportionment between "kinetic" and
"interaction" halves is imposed: the ladder term and the label term
are both computed and the table decides their sum. No new coupling.
a_A, 23, and v0 = 16.97 appear in no Leg 1-4 file.

## SCOPE (hard boundary)
Difference quantities only (NUC-023's criterion): the asymmetry energy
at fixed A, and the odd-even staggering. The ABSOLUTE binding is out
of scope by NUC-010/023's standing refutation and is not touched. No
re-pricing of NUC-005/018's volume, surface or Coulomb terms. No
re-adjudication of GRANT-THRESHOLD or the p -> g -> spectrum chain;
Q2 is a second route to the same sealed number, not a re-run of the
first. Shell structure is displayed where the ladder produces it
(NUC-022 saw it) and is not fitted.

## DESIGN COMMITMENTS (locked at lock)
D1. THE LADDER, BUILT ONCE. Transverse bundle modes confined to the
    sphere of radius R(A) at the registered saturation density;
    wavenumbers from the confinement geometry; frequencies from the
    registered bundle wave speed; energy per level from the
    registered quantization conversion. The dispersion relation is
    DERIVED in Leg 1 and recorded before any filling. The same ladder
    object serves Q1 and Q2; no per-question retuning.
D2. FILLING RULE. Two ladders (p, n); two nucleons per level per
    ladder (the four labels); lowest levels first. The rule is locked
    here as the registered conjecture and its grade travels with
    every number.
D3. THE ASYMMETRY DIFFERENCE. Delta_E(A, N - Z) = E_ladder(N, Z) -
    E_ladder(A/2, A/2) at fixed A, on the A-ladder A in {16, 40, 56,
    80, 120, 140, 208} and the excess ladder N - Z in {2, 4, ..., up
    to the physical drip} at each A. The shape exponent q in
    Delta_E ~ (N - Z)^q and the mass exponent m in Delta_E ~ A^m are
    extracted by locked log-log fits over the physically populated
    range (the NUC-020 lesson: never one A, never one excess).
D4. THE COMBINATION RULE, fixed before any number. The corpus holds
    two asymmetry channels after NUC-030: the label term (linear,
    A^(-1/3), registered) and the ladder term (this commission). The
    TOTAL asymmetry deficit is their SUM, computed with no weight, no
    double counting, and confronted as a sum. If the sum's shape or
    scaling is worse than the ladder term alone, that is a finding
    against the label term's asymmetry content (its cost belongs
    elsewhere), reported, not fixed.
D5. THE PAIRING ROUTE (Q2). Under R4 verbatim (NUC-029's realization,
    reused byte-identically), the mediator amplitude's scale is read
    from the ladder: v0_ladder = the R4 form evaluated with the
    ladder's top-level spacing as the mode stiffness, no other input.
    The staggering exponent is re-measured from the ladder-built R4
    (it must return -1/2; that is a control, bar v4), and v0_ladder
    is carried as a symbol until Leg 5.
D6. THE NUN COMPLETION (Q3). The 1D strand-path delocalization
    variant is defined at lock as: the odd nucleon's cost spread
    along a single connected strand path of length ell(A) through
    the bundle lattice, with ell ~ A^(1/3) (chord), ~ A^(2/3) (surface
    walk) and ~ A (space-filling path) each computed; the cost
    exponent for each path class is derived exactly as NUN derived
    its four channels (same instrument, benchmarks/nuclear/
    nun_dilution.py, reused). The classification is closed at lock
    at these three path classes.
D7. SHELL DISPLAY. Level-degeneracy-driven steps in Delta_E (magic-
    number structure) are displayed against the known closures and
    NOT used in any bar or fit.

## VALIDATION BARS (halt-grade; every one before Leg 5)
v1. CONTROL, THE TEXTBOOK LADDER: with the dispersion REPLACED by the
    quadratic Fermi-gas form and the standard conversion, the
    instrument must reproduce NUC-022 (median a_A 17.4 MeV, continuum
    E_F/3 = 12.8 MeV at E_F = 38.4 MeV) to 1 percent. This calibrates
    the filling and the difference extraction, not the physics.
v2. CONTROL, THE DERIVED DISPERSION: the ladder's dispersion agrees
    with the registered bundle wave mechanics (NUC-004's mode
    structure at the registered tension) to 1e-6 at three
    wavenumbers.
v3. DIFFERENCE EXACTNESS: Delta_E(A, 0) = 0 to machine precision at
    every A; the absolute E_ladder is never reported as a binding
    quantity.
v4. R4 REPRODUCTION: the ladder-built R4 returns the staggering
    exponent -0.50 +/- 0.02 (NUC-029's measured value) before any
    v0_ladder is formed.
v5. NUN REPRODUCTION: nun_dilution.py's four channel exponents
    (0, 0 with A^(-2/3) remainder, 0, -1/2) reproduced exactly from
    the reused instrument before the three path classes run.
v6. CLEAN ROOM: "23", "16.97", "12.8", "38.4" and the table appear in
    no Leg 1-4 file except v1's control (grep-verified, recorded).

## THE LEGS
Leg 0  Controls v1-v6.
Leg 1  THE LADDER: dispersion derived; levels enumerated on the
       A-ladder; the object frozen and checkpointed.
Leg 2  Q1: Delta_E(A, N - Z) on the grid; exponents q and m; the
       ladder coefficient a_A,ladder(A) in symbol units; shell display.
Leg 3  Q2: the ladder-built R4; exponent control; v0_ladder (symbol).
Leg 4  Q3: the three path classes; cost exponents; placement on the
       {1, 2/3, 1/3, 0} lattice or off it.
Leg 5  CONSEQUENCE LEG -- targets loaded ONCE. Numbers attach: the
       quantization conversion's hbar; a_A = 23 MeV and the table's
       A-binned asymmetry coefficients; v0 = 16.97 MeV and its band.
       The combination sum (D4) confronted; v0_ladder confronted;
       Q3 adjudicated.

## PRE-REGISTERED INTERPRETATION RULES (locked before results;
## anything else is NO CALL)
Bars on Q1 (all measured on the locked grid, physically populated
range only):
  S1  q in [1.8, 2.2]        (quadratic in N - Z)
  S2  m in [-1.15, -0.85]    (falls as 1/A)
  S3  the combination sum's a_A(A), A-binned, within 25 percent of
      the table's coefficient in every bin from A = 40 up (NUC-024's
      own tolerance class; 25 percent is where "emerges" was priced).

  ASYM-DERIVED: S1, S2, S3 all pass. The asymmetry term is a
      registrable Modeled-CONDITIONAL result of the shared ladder,
      shape, scale and coefficient, with the apportionment between
      ladder and label channels DISPLAYED as the corpus's own
      version of the kinetic/interaction split. NUC-005's declared-
      omission list loses its last item. The two premises' falsifiers
      are inherited on the face.
  ASYM-FORM: S1 and S2 pass, S3 fails by a factor R recorded, R
      monotone or not in A recorded. The FORM derives; the
      coefficient does not. If R is A-flat, the miss is a scale (a
      named next-order on the conversion or the bundle speed); if R
      runs with A, the combination rule is wrong and the finding is
      against D4's sum (reported; not re-weighted).
  ASYM-SHAPE-FAIL: S1 fails with all controls green. The rope
      ladder, filled under the registered rules, does NOT give
      Fermi-sea quadratic filling. This sharpens the one fence: the
      quantum layer the sector triangulates is not "a ladder plus
      occupancy", and FND-BOUND-001's target moves. Failed and kept.
  ASYM-SCALE-FAIL: S1 passes, S2 fails. The ladder's level density
      does not grow with the system the way nature's does; the
      derived dispersion or the confinement geometry is the
      defendant, named, not tuned.

Bars on Q2:
  P1  v0_ladder inside the FND-071 conversion band of the sealed
      v0 (the band as registered, not widened).
  PAIR-TWO-ROUTES: P1 passes. Two independent chains (branching
      ratio, FND-081; the ladder, this commission) hit one sealed
      number; the R4 adoption is crowned by its second witness.
  PAIR-ROUTE-SPLIT: P1 fails by a factor recorded. The ladder's
      mediator scale is not the branching chain's; since FND-081
      already closes on data, the ladder's reading of "mode
      stiffness" is the defendant. Reported; NUC-030 does NOT return
      to adjudication on this leg alone (its falsifier is the
      branching chain's, already met).

Bars on Q3:
  NUN-CLOSED: all three path classes land on {1, 2/3, 1/3, 0}
      (exponents within 0.03 of a lattice value). NUN's theorem is
      complete over the enumeration it named as owed; R4's
      non-locality is necessary.
  NUN-LOOPHOLE: any path class returns -0.50 +/- 0.03. A LOCAL
      channel produces root-extensivity; NUC-030's adoption RETURNS
      TO ADJUDICATION (the grant bought something a registered
      local mechanism supplies), with the path class named.
  NUN-OFF-LATTICE: a path class lands off the lattice and off -1/2.
      Reported as a new exponent with its mechanism; no verdict on
      R4.

Anything else (a control fails after Leg 1 began; the dispersion
cannot be derived from registered mechanics without a new constant;
an exponent fit fails its own residual bar): NO CALL; report and
stop.

## NO-RESCUE RULE
One ladder, all questions. No change to the dispersion, the filling
rule, the combination rule, the A-grid, or the path-class list after
a Leg 2 number exists. No coefficient introduced to move a_A; a
result that needs one is a fit and is reported as ASYM-FORM with R.
The v1 control's textbook ladder is never substituted for the derived
one in any bar.

## SCHEDULING (budgets, not bars)
Ladder enumeration and filling: minutes per A on the laptop. The
path-class exponents reuse nun_dilution.py's instrument at M up to
256 as registered. Two sessions expected: Legs 0-2, then Legs 3-5
with the results doc. No SPARSE-J solves, no supercell eigenwork.
Durable state to analysis/ladder_ckpt.npz; results to
analysis/LADDER_results.md; this text to
analysis/LADDER_charter_LOCKED.md on the author's lock.

## STANDING
Registered claims untouched until the author grants. Grants reserved
to the author. No claim is drafted before Leg 5 has run ONCE.
Adjacent commissions disclosed: CASIMIR-PLATE (F3) and this
commission both consume a per-mode energy assignment; they consume
DIFFERENT registered conversions (the wave state's action form there,
the QGATE mode-quantization conversion here) and neither's verdict
feeds the other's bars. TRIPLE-DUTY is independent. The three may run
in any order.

DRAFT, NOT LOCKED. Lock line and amendments, if any, go below this line
with their reasons.

LOCKED 2026-09-05 on the author's word ("Let's open Ladder"), before the
dispersion is derived and before any filling. Note at lock: the 3/2
composite states are absent from this release (BRICK2/FINE-GATE
precedent); this commission does not need them.
AMENDMENT (Leg 0, 2026-09-05): v1 reproduces NUC-022 bit-exactly ONLY on a
cubic box of side 2R(A) with two nucleons per (nx, ny, nz) state (m =
939.0 MeV); NUC-022's text says "a sphere of radius R". The spherical
Dirichlet ladder (2(2l+1) per level) gives E(40,4) = 1587 and a median
a_A of 32 MeV. The calibrated instrument for this commission is the one
NUC-022 actually ran (the box); the discrepancy is recorded for NUC-022's
face. The DERIVED dispersion runs on the same box and, as a display, on
the sphere.
LEG 1 DISPERSION (derived before any filling): the bundle's transverse
modes at mu = T/c^2 propagate at c; NUC-004's mode-overlap binding is the
evanescent tail of a mode with a mass gap kappa = 1/L (L = 1.4 fm, the
registered INPUT range). Hence E(k) = hbar c sqrt(k^2 + kappa^2); the gap
hbar c kappa cancels in every difference quantity; the massless limit
E = hbar c k is carried alongside as the "linear at leading order" case.
No Fermi-gas form enters. Both are filled on the calibrated box.
