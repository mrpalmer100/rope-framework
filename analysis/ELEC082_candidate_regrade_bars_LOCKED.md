# ELEC-082 bars — LOCKED before computation (2026-08-02)

Commission: PRED-003-XCHAIN's cheapest named next-order. ELEC-054 closed with five
candidates and no selection, graded only on magnitude (A/w ~ 60-71) and on whether
hbar was smuggled in. The corpus now owns three derived boundary conditions
(HBAR-011; PRED-003-XCHAIN, E1 branch):
  BC1  expansion decoupling: |d ln A/dt| < 1.6e-18 per year;
  BC2  tension channel:  A ~ T^(-3/2)  (equivalently hbar ~ T^(-2));
  BC3  spacing channel:  A ~ a^(1/2).
Re-grade the five dead candidates against them.

Rules fixed in advance:
- R1: The candidate list is CLOSED — the five of ELEC-054, exactly as registered.
  No candidate may be added, none reinterpreted beyond its own stated mechanism.
- R2: Every kill is CONDITIONAL and must say so: BC2/BC3 hold on the E1 branch
  (coupling drift-inert) of the two-chain corpus. A future derivation of p != 0
  reopens whatever it reopens; the claim must state this in its own text.
- R3: Exponent grading is exact match or fail. No "close" exponents.
- R4: If a candidate is not a mechanism (a bound), it is graded NOT-A-MECHANISM,
  not pass or fail.
- R5: Any class-level consequence (a whole family of mechanisms excluded) must be
  derived, stated, and its effect on ELEC-054's registered payoff structure
  reported straight, favourable or not.

Bars:
- B1: Each candidate's implied A-scaling in (T, a) derived from its OWN stated
  mechanism, symbolically, with the derivation shown (w = a/sqrt(3) exact, ELEC-053;
  HBAR-007's |C| = T w^2/2 where the anharmonic candidate needs it).
- B2: Grade M1-M5 against BC2 and BC3 mechanically per R3/R4.
- B3: Grade against BC1 where the candidate ties A to a length that comoves if the
  medium does.
- B4: The class question: does ANY mechanism of the form A = (pure number) x w
  survive BC3? Expected: no (exponent 1 vs 1/2). If confirmed, the consequence for
  ELEC-054's payoff structure (a pure-number mechanism would select Sigma) must be
  registered whichever way it cuts.
- B5: For any candidate with free structure (M3 thermal imports an energy scale),
  the conditions are CONVERTED into a specification on that structure, not into a
  pass. State exactly what the import must do to survive.
