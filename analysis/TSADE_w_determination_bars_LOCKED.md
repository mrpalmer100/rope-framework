# COMMISSION TSADE -- THE w DETERMINATION: BARS, LOCKED BEFORE COMPUTING

Locked 2026-08-12, before any number in this commission was computed.

## Charter

Three sectors wait on the constituent width w (the hbar branch via
QGATE-004, matter stability via EM-RECON-017/018/030 and FND-029, the
pairing/reconnection chain via FND-072). FND-066's V-COLLISION rule
governs: the NAME is fixed before the number is chased. Claims read
verbatim at charter: FND-066, ELEC-050, QGATE-004/005, FND-040,
EM-RECON-030, FND-067/068, FND-072, FND-029.

## Q1 (the name, first): the collision resolved at lock

- W1, the vacuum-mesh strand: RATIO ALREADY DERIVED, w/a = 0.6272
  (EM-RECON-030). Not open; blocked only on a's value.
- W2/W3, the flux-tube constituent: the open object all three censuses
  actually need.
- The identification W1 = W2/W3 (one strand kind everywhere) is
  ONE-MEDIUM: found not-false by FND-067, unprosecuted, and shown by
  ELEC-050's B1 to ALREADY underwrite the corpus's T0 chain
  (Sigma = T_tube/(pi R^2) is the one-medium declaration). Every
  determination below that uses it is CONDITIONAL ON ONE-MEDIUM, stated
  on the face, never silently.

## Q2 (candidate determinations, closed at lock)

- D-a (one-medium inversion): w = 0.6272 a, hence a = w/0.6272 with w
  from ELEC-050's registered lattice band [0.0395, 0.0565] fm.
- D-b (direct): ELEC-050's band itself, as the tube constituent.
No other candidate may be added after results are seen.

## Q3 (the standing tripwire, executed in locked order)

The candidate band feeds through the three censuses IN THIS ORDER, each
verdict recorded before the next runs:
  C1 the Lorentz bound: the inverted a must satisfy a <= 1e-16 m
     (FND-MATTER-005's registered bound), else D-a dies.
  C2 the hbar census: QGATE-004 under FND-068's corrected reading,
     n_t = pi R^2/a^2, confronted with the collective-hbar demand
     n_t = 111, R carried across the published lattice band
     [0.35, 0.5] fm. Intersection reported; if non-empty it NARROWS
     the w band and the narrowing is adopted as the census's output.
  C3 the reconnection window: FND-072's demanded per-pair contrast
     g in [0.082, 0.265] confronted with matter-stability survival
     g >= [0.395, 0.460] (EM-RECON-030). Both constrain the SAME
     per-pair ratio. If disjoint, the gap factor is computed and
     judged against the independent L1-class O(1) conversion bands
     (factor 3 each, direction-neutral, per FND-029's registration);
     a gap coverable by conversions is registered as NAMED TENSION,
     not resolved; a gap beyond them FIRES the NUC-030 falsifier.

## The resemblance rule (FND-070's, applied at lock)

Any numerical adjacency the computation surfaces (e.g. between the
inverted a and the M-point value, or between one-medium w at the
Lorentz bound and the lattice band edge) is DISPLAYED and REFUSED as
evidence absent a dependency path. Identification requires the path,
not the proximity. This bar exists because the electron-axis arc cost a
campaign to learn it.

## Conflicts reported, not resolved

If the inverted a excludes other registered a readings (FND-040's
re-solved values), the exclusion is REGISTERED AS A CONFLICT on the
relevant claims and left for adjudication. This commission does not
pick between a readings.

## Verdict grammar (locked)

  DETERMINED                  w fixed unconditionally (not expected);
  CONDITIONALLY-DETERMINED    w fixed as a band conditional on
                              one-medium, all censuses non-empty;
  UNDERSPECIFIED              a census cannot be posed;
  KILLED                      a census empties the band, or C3's gap
                              exceeds the conversion allowance.

## House

Adverse outcomes pre-authorized; no rescue, no bar-shopping; no em
dashes in file content; benchmark under benchmarks/foundations/;
verify before re-zip.
