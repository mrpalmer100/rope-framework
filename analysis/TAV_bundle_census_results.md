# COMMISSION TAV -- THE BUNDLE CENSUS: RESULTS

Executed 2026-08-12 under analysis/TAV_bundle_census_bars_LOCKED.md.
Benchmark: benchmarks/foundations/tav_bundle_census.py.

## Verdict (locked grammar): BUNDLE-CONSISTENT, carrying the G1 demotion

The tube constituent CAN be a bundle of vacuum strands: every census
passes, and the count confrontation's declared natural cell balances
the tube's strand budget to 10 percent, independent of the tube radius
and of the floor reading.

## B1 -- geometry (integer windows, all four cells non-empty)

| floor | phi = f_c | phi = hex |
|---|---|---|
| kappa 50 | n_b = 5..9 | n_b = 14..27 |
| kappa 250 | n_b = 14..27 | n_b = 40..81 |

## B2 -- solidity

Internal gap 0.799 sigma (f_c) / 0.050 sigma (hex), both below the
1.5-sigma crossover: a bundle at either packing is on the solid side,
consistent with the lattice reading it as one width-w_c object.

## B3 -- the count confrontation, and the cancellation

G1 CONFIRMED: FND-068's coverage convention gives
(pi/4)(w_vac/a)^2 = 0.30896 vs f_c = 0.30900 (1.3e-4 relative), so the
ratio n_struct_variant / (n_t x n_b) reduces IDENTICALLY to
(families)/phi -- no R, no a, no floor. The four locked cells:

    (all-family,  f_c):  9.71   beyond L1
    (all-family,  hex):  3.31   beyond L1
    (axial,       f_c):  3.24   beyond L1
    (axial,       hex):  1.103  CONSISTENT  <- declared natural cell

The natural cell -- axial strands (constituents of an axial tube),
close-packed (a solid bundle) -- was declared at lock, before any
number. Its reading: NUCQ-003's axial-family threading count equals
(constituent count) x (strands per constituent) up to exactly the
bundle's void fraction, 1/phi_hex = 1.103. The unaccounted ~9 percent
is numerically the interstitial share; displayed, not adopted.

## The demotion, applied as pre-committed

The cancellation runs through the SHARED registered convention: the
tube-internal packing f_c (QGATE-004) and the vacuum coverage f_c
(FND-068) are the same registered number, which is what removes R, a,
and the floor from the ratio. By the FND-067 precedent this is a
CONSISTENCY-THAT-COULD-HAVE-FAILED -- w_vac/a could have been anything
and was derived independently of NUCQ-003's count -- and not
independent evidence. What it establishes: the bundle hypothesis is
arithmetically coherent with every registered count at once. What it
does not establish: that the hypothesis is true.

## What the census does NOT determine

n_b. The windows are wide (5-27 at kappa 50 across packings) and no
registered constraint picks an integer. NAMED ACQUISITION, unchanged
from the bars: an independent n_b route -- the super-additivity
calibration point (PHI: delta_fusion = +12.5 percent at
kappa_rel = 2.25) or the bundle-binding relation owed under FND-050's
N-universality grant. Either would convert a viable picture into a
falsifiable one.

## Scope fence honoured

No T0 chain was touched; the NUCQ-002 coincidence flag stands exactly
where SHIN left it. One observation is FILED for that flag's future
owner without being used here: post-conviction, the additivity route
and the calibration route differ at the floors, and a bundle hierarchy
inserts a factor (n_b) between "strands" and "constituents" that any
resolution of that flag must now route through.
