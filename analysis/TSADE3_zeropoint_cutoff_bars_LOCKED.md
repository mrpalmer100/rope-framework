# COMMISSION TSADE3 -- BARS, LOCKED BEFORE THE CENSUS VERDICT

*2026-08-13. Candidate claim FND-109. The definitional claim
EM-RECON-033 located: WHICH modes does the corpus's zero-point
ledger integrate? Closes SHIN8's Q5 if the carried-modes-only
convention is codifiable.*

## The question

Adopt-or-discover the ledger convention: carried (strand-level)
modes only, or a geometric cutoff that would sweep in sub-strand
relative modes (n_sub-dependent additive zero-point).

## Method (fixed)

CENSUS BY PROVENANCE AND CUTOFF: enumerate the registered zero-point
and Casimir entry points (the FND-MATTER spectroscopy and mass-ledger
arc; GRV-004/021; the FND-034/035 packing-pressure arc; any other
title hit). For each family, identify (a) its registration date
relative to GRANT-SUBSTRUCTURE-TIGHT (2026-08-12) and (b) the length
scale parameterizing its mode content or cutoff (strand spacing a,
strand radius/diameter, knot geometry -- all coarse -- versus any
sub-strand length). Spot-read is required only where a family's
cutoff length is not identifiable from its registered face.

## Pre-committed verdict grammar

- CODIFICATION: if every registered family's mode content is
  parameterized by coarse lengths (equivalently: every ledger was
  built on the pre-grant ontology where the strand is the finest
  object, and no post-grant claim extended any integral to fine
  scales), then carried-modes-only is registered as the CODIFIED
  CONVENTION -- a description of universal registered practice
  promoted to a named rule, not a new physical postulate. Effect:
  SHIN8's Q5 closes to INVARIANT; any FUTURE claim wishing to
  integrate fine modes must say so explicitly and price the
  n_sub-dependence it thereby buys (tripwire registered with the
  convention).
- SPLIT: if any registered family integrates to a fine scale, the
  n_sub-dependent piece is real THERE; the family is named and the
  break EM-RECON-033 could not find is registered where it lives.
- AUTHOR-DECISION: if any family's cutoff is genuinely ambiguous on
  its registered face, the ambiguity is named and the convention
  choice goes to the author rather than being resolved by fiat here.

## House

Bars locked before the census verdict. Em dashes forbidden. The
census may not skip a family because its answer looks obvious.
