# Commission GIMEL -- the whisper-pricing re-audit under the moved M-point: locked bars

*Locked 2026-08-11, before any computation. Owed since FND-038 (OMEGA);
owner of the tower's sole registered refusal, l_q/a = 158 (271) outside
the 1-100 window at both FND-040 floor readings.*

## Frozen inputs (no new fitted numbers; m_e untouched)

- K_me = T0*a = 2.6065e-14 J -- the spent calibration, re-used not re-fit.
- Sigma_eff = 3.61e35 J/m^3 (ELEC-081, the measured stiffness).
- The FND-040 floors, conditional on FND-037: kappa_pack >= 50 (5% CS
  bound) and >= 250 (continuum), giving (a, T0) = (1.63e-17 m, 1599 J/m)
  and (9.53e-18 m, 2734 J/m).
- All registered relations as registered; in particular R1's quantum-area
  form as it appears in the corpus and in MATTER044's own benchmark.

## The questions, in order

- Q1 (the rescaling question, named open by OMEGA): does l_q rescale with
  the mesh? Adjudicated by REGISTRY INSPECTION ONLY: if the registered
  form of l_q is a function of T0 (as MATTER044's benchmark computed it,
  l_q^2 = 4 pi alpha hbar c / T0), then l_q rescales by its own
  registered definition and any fixed-l_q reading (as carried in
  BET's strain check, l_q held at 2.58e-15 m = the kappa=1 value) is a
  stale-value comparison, to be corrected with a bracketed pointer per
  the MATTER045 precedent. If the registry instead fixes l_q
  independently of T0, the strain stands as computed.
- Q2 (the re-priced table): recompute the full MATTER044 pairwise table
  at BOTH floor readings. ZPE bar = 3 applies to comparisons between
  INDEPENDENT determinations of the same quantity; identity
  de-duplication enforced as in MATTER044. A comparison whose basis was
  superseded by OMEGA (the card's Sigma_eff-normalized values) may be
  reclassified only by an argument from registered claims, stated in the
  results, never silently dropped.
- Q3 (the window provenance audit): what is the 1-100 window's grade?
  Trace its origin in the registry (the GRV-093 arc's pre-drafted
  reconciliation grammar). The window's grade CAPS the grade of anything
  derived from it.
- Q4 (the cap): if Q1 says l_q rescales and the window is retained at
  its found grade, compute the kappa_pack value at which l_q/a exits the
  window, and confront it with both floors. If the cap falls below 50,
  the collision OMEGA searched for and did not find has been found, and
  it must be registered at full volume as a contradiction between the
  FND-037 tower and the window. If the cap falls between the two floor
  readings, the confrontation is registered as excluding one reading and
  not the other, at the window's grade.

## Success, failure, and honesty clauses

1. Failure is reportable: if the strain survives Q1 at the registered
   form, it stands as the tower's refusal, sharpened not absorbed.
2. n_q invariance must be re-verified exactly (it tracks K_me*h, which
   is pinned); any deviation is a computation error, not physics.
3. Nothing is promoted. A cap inherited from the window carries the
   window's grade, stated on the face of the claim.
4. The 4.62 decomposition (1.67 x 2.77) is re-examined: state plainly
   what survives of it under the moved M-point and what was
   kappa=1-specific.
5. No scanning: one run per floor reading, numbers reported as computed.
6. GRV-074 rigidity row stays excluded (dissolved by MATTER045 as a
   category error; not resurrected here).
