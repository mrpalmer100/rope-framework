# FND-STRAND-029 — the Hong-Ou-Mandel pin: the boundary bounded from both sides

Date: 2026-08-04. Commission: the roadmap's Phase C. Method: the QB
campaign's g2 pin, applied to the deepest sub-Bell item on the standard
list. The HOM dip — two indistinguishable quanta entering a 50/50
splitter from opposite ports, with coincidences vanishing — is measured
in the laboratory at visibilities above 90%. This session derives what
BOTH of the framework's current routes predict, pins the gap
quantitatively, and names (without adopting) the shape of any escape.

## Route 1 — classical wave interference: the 50% cap, derived

Two independent classical packets, amplitudes E1, E2, random relative
phase theta, meet at a 50/50 splitter. Output intensities:
I_c = S + X cos theta, I_d = S - X cos theta, with S = (E1^2 + E2^2)/2
and X = E1 E2. Linear-response detectors (the FND-STRAND-027 Mandel
regime) give coincidences C proportional to <I_c I_d>:

  C = S^2 - X^2 <cos^2 theta> = S^2 - X^2/2  (uniform theta).

The no-interference baseline (large delay) is C0 = S^2. Visibility:

  V = (C0 - C)/C0 = X^2 / (2 S^2) <= 1/2,

with equality iff E1 = E2 (AM-GM). THE CLASSICAL WAVE CAP IS 50%,
derived inside the framework's own detector theory, attained only at
matched amplitudes, and degraded by any imbalance — the textbook bound,
now a registered closed form.

## Route 2 — per-quantum granular delivery: NO dip at all

Under Grant 3 as stated, two quanta are delivered INDEPENDENTLY: each
exits c or d with probability 1/2, so the coincidence probability is
1/2 — identical to the distinguishable-photons baseline. V = 0. The
granular route, which handed the corpus Grangier-Roger, Fock counting,
and the eraser-free delayed choice for free, predicts NO Hong-Ou-Mandel
dip whatsoever. This is stated at full volume: the grant's
per-quantum independence, which is exactly what recovered the classical
limit so cleanly in 025/027, is exactly what fails here.

## The pin

  Framework, granular route:   V = 0
  Framework, wave route:       V <= 1/2  (= 1/2 only at E1 = E2)
  Quantum mechanics:           V = 1
  Experiment:                  V > 0.9 routinely

The gap is a FACTOR-OF-TWO-AT-BEST shortfall with a derived ceiling —
the exact analog of the g2 >= 1 pin, and like it, a boundary the corpus
registers as a measurement of its own jurisdiction rather than hides.
HOM visibility beyond 50% requires TWO-QUANTUM EXCHANGE STRUCTURE:
the interfering object is a two-particle amplitude (the exchange term
between "both transmitted" and "both reflected"), which neither
independent classical waves nor independently delivered quanta possess.

## The escape, named and NOT adopted

The shape any escape must take is now exact: a JOINT-DELIVERY rule for
indistinguishable quanta — the two-quantum analog of Grant 3, under
which quanta sharing a channel are delivered as correlated bookkeeping
rather than independently (the bosonic exchange term as a delivery
rule). The echo of Grant 1 is noted for what it is — "the pair is one
thing" was the corpus's first joint-object sentence — but adopting a
fourth grant is an authorial decision with an austerity price, and
tonight's session PINS, it does not purchase. Falsifier structure for
any future adoption is already visible: joint delivery must reproduce
V = 1 at perfect indistinguishability, degrade with distinguishability
exactly as the measured HOM dip narrows with delay, and NOT disturb the
single-quantum consequences 025-028 derived.

## Status, scope, ledger

- The 50% cap and the V = 0 granular result are closed forms; the
  benchmark executes both (Monte Carlo classical fields with amplitude
  sweep; per-quantum sampling; the pin table).
- Status Modeled (a boundary claim, like the g2 pin). The classical cap
  arithmetic is exact; the claim's content is the registered boundary.
- HOM joins the funneling step and CHSH on the surveyed-edge ledger:
  three fences, each with a derived location, each with its escape's
  shape specified. The eraser remains behind QB-003.
- Absolute scale untouched (FND-MATTER-003).
