# GRANT RECORD -- WHYWIND-E + FND-152/153 RIDER (author's grant,
# 2026-08-29)
# Granted in the handoff-review session after full dig-in.
# REGISTRATION NOT YET TAKEN: this session holds only the session
# artifacts (no claims.yaml, no tools/add_claim.py). The working
# session executes the insert AND the rider application via the
# canonical tooling in a verified process-quiet window. Nothing is
# registered until then.

## DECISION
1. WHYWIND-E GRANTED as drafted in
   WHYWIND_draft_registration_addendum.md. Status: Derived (the
   band decomposition is exact/Parseval on the registered
   tangents; the identity f_dir = ||t_pt||^2 verified in brick 2a).
2. The FND-152/153 AMENDMENT RIDER GRANTED in its HARDENED form
   (per WHYWIND_probe42_results.md): rider text as drafted in
   WHYWIND_reprice_results.md, with the OPEN clause pointing at
   the 144x42 refusal (WHYWIND-F material) rather than at a
   pending probe. Applied APPEND-ONLY to FND-152 and FND-153 per
   house rule (NUC-005 convention). Rates and D bounds untouched.

## CLAIM TEXT -- WHYWIND-E (for the canonical inserter)
Re-pricing (option b), RP-FALLS under locked bars (resolved band
|n_phi| <= 12 of phi-Nyquist 18, fixed before computation): the
registered FND-152 f_dir rise (+0.378) is carried ENTIRELY by
Nyquist-band pt content -- f_nyq 0.004 -> 0.486 across the 4/3
profile while the resolved-band share FALLS 0.106 -> 0.002 (rise
-0.104 vs the RP-FALLS line < +0.05). The q = 5/3 control's
Nyquist band is 0.0000 at every profile point; the flat cell's
winding motion is fully resolved beat ladder. The deep 4/3 tangent
decomposes ~49 pct Nyquist-band phi content / ~51 pct amplitude /
~0.2 pct resolved winding. RP-FALLS relocates the measured "sector
rotation" to unresolved-band content; it does NOT prove artifact
(a physical n = 17 mode also lives in the band). Status: Derived.
Amendment rider applied to FND-152/153 (hardened form; append-only).
Evidence: analysis/WHYWIND_reprice_bars_LOCKED.md,
analysis/WHYWIND_reprice_results.md,
analysis/WHYWIND_reprice_run.txt.

## RIDER TEXT (hardened form; append to FND-152 and FND-153)
"Re-pricing 2026-08-29 (RP-FALLS, WHYWIND_reprice_results.md): the
f_dir rise registered here is carried entirely by pt content with
|n_phi| >= 13 on the 36-point phi grid (resolved-band share FALLS
0.106 -> 0.002 across the same span; the q = 5/3 control's Nyquist
band is 0.0000 throughout). The mechanism sentence 'the collapsing
cell demands winding' is therefore a statement about band-edge
content whose physical vs discretization character is OPEN: the
144x42 confound-free probe (WHYWIND_probe42_results.md) could not
adjudicate -- the 4/3 deep member refused full-bar gating on that
grid while its sibling and both 5/3 controls gated cleanly -- and
resolution is deferred to hardware where 144x54+ is feasible. The
measured rates and D bounds are untouched by this rider."

## STANDING
Author's grants so far: WHYWIND-A..E (separate records) and the
FND-152/153 hardened rider. WHYWIND-F and the queue action remain
on the desk.
