# ZPE BAR RE-AUDIT -- BARS LOCKED (2026-08-09, before any computation)

Trigger: the lever-provenance audit (LEVER_provenance_audit_results.md)
dissolved the 25% zero-point share; the 2-3x "ZPE bar" was priced off it.

## Scope (committed)
Both branches computed, NOTHING adopted in-session (grants are Mark's):
  BRANCH A -- lambda fully open: share s in [0, 1) with no constraint.
  BRANCH B -- MATTER056's pi(r/a)^2 as the standing prediction: share
    s in [0.064, 0.093] (FND-MATTER-057's computed per-knot levers,
    reused as-is; nothing recomputed).
Consumers enumerated FROM THE REGISTRY before any number (below).
The MATTER041 inverted pipeline re-run under both branches, full
coherence table, same code path (constants unchanged, only the T0
band edited per branch).

## Consumers enumerated (pre-committed list; no additions after numbers)
C1. l_q/a vs the 1-100 reconciliation window (MATTER040/041 step 4).
C2. T0 coherence factors vs the anchor table (MATTER041), with the
    "inside ZPE bar" tag re-evaluated at each branch's band width.
C3. l_q vs registered 1.39e-15 m (factor 2.40, previously excused by
    the ZPE bar).
C4. n_q vs the snap band [1.1e-4, 4.6e-4], including a CHECK of
    MATTER041's "recoverable within the same ZPE band" sentence --
    the direction of that recovery is verified, not assumed.
C5. The 4.6 blind-mass whisper's bar (outside 2-3 previously).
C6. FND-029's nuclear import (T0 a = 0.1627 MeV enters E_x/(T0 a)).
C7. Commission E's calibration-conditionality note (qualitative:
    does the branch change its cube-root sensitivity statement).

## Verdict grammar (pre-drafted)
- A consumer FLIPS if its registered tag or in/out verdict changes
  under a branch. Flips are named on the claim's face.
- Branch A's expected adverse shape: the bar is UNBOUNDED above
  (s -> 1 sends T0 -> 0); registered as-is if it comes out that way.
- Branch B's expected adverse shape: the band collapses ~[1.03, 1.05],
  so factors previously excused at <= 3 become naked tensions.
- If MATTER041's n_q recovery sentence fails directional scrutiny,
  that is a CATCH, logged, not silently corrected.

## Rules
No new calibration; spend stays at one. No adoption. Superseded-not-
erased. Both branches reported at equal volume regardless of which
reads better.
