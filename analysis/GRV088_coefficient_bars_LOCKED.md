# GRV-088 bars — LOCKED before computing (2026-08-02)

Commission: the (K, h) evaluation -- convert the Hawking law-form's coefficient
into a number and set it against the lineage's committed 0.23. Pre-committed as
a PREDICTION-MEETS-PREDICTION test: agreement and disagreement register at
equal strength.

The derivation, recorded before evaluating:
- K DERIVED (closing GRV-038's last O(1) at the statics level): GRV-077 gives
  the transverse demand per volume e a_p/c^2; per crossing (density chi/a^3)
  the pressing is N(sigma) = e a^3/(chi sigma) with a_p = c^2/sigma -- THE
  PRESSING IS THE CELL'S WEIGHT: each crossing carries m_cell a_p. K is not a
  free O(1); it is Sigma a^3/chi in the profile's numerator (e = Sigma, the
  vacuum tension density, premise P-e named: the shell's cell energy is the
  ambient density, not the pile-up).
- THE COEFFICIENT, assembled: C = T_inf/(hbar kappa) =
  beta Sigma a^3 h /(m* chi hbar c), every factor sourced: beta = 15.67
  (GRV-082, measured -- WITH its disclosed drive-depth dependence), Sigma a^3 =
  3 T0 a (FND-017, T0 the fork-invariant), h = d_c = 1.87e-19 m (HBAR-005,
  flagged possibly convention-dressed per GRV-076), m* = 4-6 (GRV-086),
  chi = 1-3 (weave geometry bracket), spectral peak factor 1-2.8 (thermal
  peak vs T, bracket).
- Evaluation on BOTH forks (GRV-076): F-Lor (a = 1e-16, T0 = 1203-1700) and
  F-Sak (a = 1.26e-34, T0 unchanged on the degeneracy line).

The decision rule, fixed in advance:
- MET if |log10(C/0.23)| <= 1 with brackets included; otherwise TENSION,
  registered at full strength, with the suspect-link ledger opened on BOTH
  chains (a disagreement between two internal predictions means at least one
  chain carries an unearned link, and the mode-analysis side's premises go on
  the table alongside the mechanism side's).

Suspect links pre-listed (so the post-hoc ledger cannot be accused of
improvisation): mechanism side -- beta's engine-parameter provenance (measured
at arbitrary simulation numbers; its drive-depth sensitivity was disclosed at
registration), h's convention-dressing (the fm-audit GRV-076 queued), P-e;
lineage side -- GRV-040's mode identification behind 0.23.
