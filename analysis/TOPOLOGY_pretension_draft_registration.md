# PRETENSION BRICK -- DRAFT REGISTRATION (NOT REGISTERED)
# Prepared 2026-08-26 (FND-149 suggested). AWAITING THE AUTHOR.

TITLE: Uniform pretension rescales the weave but never rescues
membership: the vector transverse threshold z_c is
tension-independent within brackets (W2), falsifying the
spider-web prior; floppy modes are gapped at stiffness
proportional to tau (spectrum-level proof); at low tau dilution
cures the pristine lattice's leaky-slab anisotropy.

STATEMENT (draft): On pretensioned cubic networks (K_bond =
k b b^T + (T0/L)(I - b b^T), shared k = m = a = 1, single control
tau = T0/(k a)), the equatorial transverse channel propagates at
the pristine speed scale c_T = sqrt(tau) (measured 0.95/0.53/0.134
vs theory 1.0/0.548/0.141) with healthy relative speed down to
z = 3.5 at every tau in {1.0, 0.3, 0.02}, degrading at z = 3.0;
the breakdown bracket z_c in [3.0, 3.5] (tau = 0.3, 0.02) and
[3.0, 4.0] (tau = 1) is FLAT within resolution: verdict W2. The
committed W1 (spider-web) prior is falsified for uniform tension,
with the mechanism exhibited in the spectrum: sub-Maxwell networks
show NO floppy proliferation at any tau > 0 -- exactly 3 rigid-
translation null modes, the lowest genuine eigenvalue scaling as
tau (ratio 50 = 1/tau at z = 6, ~43 at z = 4) -- so tension gaps
every floppy direction with the SAME factor that sets c_T^2, and
the threshold in z cannot move. Uniform pretension rescales; it
never rescues. Additionally: at tau = 0.02 the pristine lattice is
a leaky-slab anisotropic medium (equatorial p = 4.06) and dilution
CURES it (p -> 1.44 at z = 5, 0.54 at z = 4, quasi-2D), a
dimensional-crossover regime reported as measured per the
charter's mixed-outcome clause.

SCOPE (recorded): heterogeneous tension and polarization-mixing
remain open (W1 could revive there); the classifier binary is
advisory at low tau (amendment ledger in the results doc).

EVIDENCE: analysis/TOPOLOGY_pretension_results.md;
analysis/pretension_brick_ckpt.pkl;
benchmarks/foundations/pretension_brick.py;
analysis/TOPOLOGY_pretension_charter_LOCKED.md.

AUTHOR DECISIONS REQUESTED: (1) grant / amend / decline; (2)
whether the W2-with-mechanism statement or a brackets-only
statement is registered; (3) whether heterogeneous tension gets a
fourth brick.
