# COMMISSION SWEEP-PATH -- RESULTS (2026-08-18)

Executed under analysis/SWEEPPATH_bars_LOCKED.md.
Benchmark: benchmarks/foundations/sweeppath_coverage.py.

**VERDICT: TIGHTENED.** The fine coverage bracket under the load-bearing
reading is **f_{c,f} <= [1.80%, 5.07%]** at the registered ceilings,
down from [2.15%, 6.08%] by exactly path_A/path_B = 0.8349. Packing
headroom [0.150, 0.252] is unchanged (the ratio carries no path
factor). Both of the fine-weave session's assertions pass with more
margin: the spacing-separated-fiber picture sharpens.

## THE LEGS

1. IDENTITY CONTROL: 2/(sin^2 psi_1 sin^2 psi_2) = 8.0912 against the
   registered 8.091 (2.7e-5). The invariance of this identity under the
   sin<->cos swap is stated on the face as the REASON the earlier
   convention sweep cleared the file while the convention-sensitive
   path factor six lines below went unread.
2. COVERAGE RECOMPUTE: fine-weave leg-1 arithmetic verbatim, path
   factor moved from the reading-B value 2.4091 to the reading-A value
   2.0114 (load-bearing by the BLOCH-L isotropy control). Coverage
   moves by exactly the ratio; nothing else moves.
3. QUOTE SWEEP: three live-or-adjacent occurrences of the reading-B
   factor, each dispositioned -- the fine-weave results line (history,
   stands, correction attaches by name), the energy bill's two-level
   bracket (already superseded in value by the composite build), and
   no other live quote in the registry.

## WHAT MOVES

The registered coverage value: [2.15%, 6.08%] -> [1.80%, 5.07%].
FND-115's text stands (history). The registry's path-factor rule
(any new text quoting a path factor states its reading) already
covers the going-forward discipline; this session pays the one
outstanding inherited quote.

## REFUSALS

The Sigma leg untouched (a_f cancels; no path factor). Handedness leg
untouched. No registered text edited. The pre-registered outcome
(coverage falls by exactly the ratio, both assertions pass) is the
outcome observed; no deviations to record.
