# Commission ZAYIN -- the defect-exponent computation: MISS, structural. The energy-budget defect-log class is excluded as the g-mechanism, and the demand it leaves is 2.2-3.2 electron rest energies

Date: 2026-08-11. VAV's rank-1 next brick, run exactly as the target
sheet specified: blind, one pass, bar pre-committed. Bars:
analysis/ZAYIN_defect_exponent_bars_LOCKED.md (the full 3x3 table and
both bar brackets locked before any cell was evaluated).
Benchmark: benchmarks/foundations/zayin_defect_exponent.py.

## The table

Nine cells -- budgets {m_e c^2 = 2 pi J, J, E_core} against
coefficients {pi K, 2 pi K, pi^2 K} with K = J (the registered lattice
identification), circularity rule enforced (anything containing l_q,
g, A, or hbar-through-l_q excluded on the face). Every cell displayed;
every cell a miss. Exponent range delivered: **x in [0.10, 2.00]**
against the bar at [4.41, 4.68] (bare) / [6.13, 6.40] (cutoff).

## The verdict: Failed-and-kept, and the failure is structural

The table's range lies entirely BELOW both bar segments -- the chance
hit rate for this table was exactly zero, which cuts both ways: no
cell could have hit by luck, and none did by physics. The largest
registered budget the winding owns (its own rest energy) exhausts the
single-vortex logarithm at g = 7.4 cells (bare) -- the electron's
energy runs out an order of magnitude before the source length the
alpha chain demands. **The mesoscopic length is not the radius at
which the winding's own energy exhausts its logarithm.** The
best-shaped registered structure, run blind with every registered
budget, cannot reach the target.

## The inverted demand (MATTER046 grammar), registered

For any future rescue, the budget a defect-log mechanism must derive
BLIND:

| Coefficient | bare | cutoff |
|---|---|---|
| pi K (single vortex) | 2.21-2.34 m_e c^2 | 3.06-3.20 m_e c^2 |
| 2 pi K (pair) | 4.41-4.68 m_e c^2 | 6.13-6.40 m_e c^2 |
| pi^2 K (3D line) | 6.93-7.35 m_e c^2 | 9.63-10.05 m_e c^2 |

No registered energy is any of these. The demand is specific enough to
kill most rescues on sight -- exactly what a demand is for.

## What the failure teaches (stated, not spun)

The g-mechanism, whatever it is, involves more energy than the defect
owns, or a structure that is not a single defect's field, or a
non-energetic selection principle. The registered lesson (MATTER055:
the mesoscopic gap is the shape of missing physics) survives its
best-motivated challenger and is now sharper: the missing physics is
not the defect sector's logarithm. BKT-class screening lengths remain
out of scope (no registered vacuum temperature; noted per the locked
clause, not evaluated).

## Ledger

- FND-045 registered **Failed** (Failed-and-kept count 33 -> 34) --
  the arc's first registered failure, arriving at brick 23, by a
  pre-committed bar the table could not reach.
- No rescue attempted; no O(1) argued; no cell added after numbers.
- The kappa_pack question is untouched: the mechanism that was
  responsible for it did not materialize, so the floors stand as
  FND-040 left them, ceiling-free per FND-042.

## Next bricks, ranked

1. The correlated-coefficients relation (ALEPH's second number) --
   now the only live registered route to an independent kappa_pack pin.
2. The g-mechanism's surviving candidate space, scoped honestly: an
   ambient/collective construction (the MATTER053 lesson's direction:
   the answer lives in the weave, not the knot) -- a scoping session,
   not a hunt, when the corpus wants it.
3. The kappa rename and v3.17.0 (FND-041..045: five commissions, one
   arc, one failure kept -- the release's natural shape).
