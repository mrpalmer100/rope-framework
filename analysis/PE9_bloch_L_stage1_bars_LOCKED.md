# THE FINE-CHANNEL DISPERSION LINE, STAGE 1 (PE9) -- BARS LOCKED (2026-08-16)

Locked before computing. FND-123's named attack: read c_L,f off the
wound structure. Stage 1 = what tonight's registry supports; Stage 2
(the full Bloch-L longitudinal branch) is CHARTERED, not run.

## B0 -- CLEAN-ROOM (inherited, sealed)
The target values (2.844 c; r_s = 0.2496 a_f; 8.091) are SEALED for
all of stage 1 and for the Bloch-L charter's derivation legs. They
appear only in the pre-registered outcome sheet.

## B1 -- CONVENTION FINDING, DECLARED
The registered winding data are the PITCH (p = a_f worst case) and
the ANGLES (via kappa = (pi/p) sin 2psi and tau = (2pi/p) cos^2
psi). Reverse-engineering the winding RADIUS R from these requires
a helix parametrization the registered forms do not fix (the
standard Frenet forms and the registered forms disagree under both
natural angle conventions). R IS UNREGISTERED -- a named gap. NO
numeric spring-compliance estimate may be computed tonight; any
such number would be invented through an unregistered R.

## B2 -- STAGE-1 DELIVERABLES (pre-committed)
1. THE RIGIDITY DEMAND, derived as structure: the coarse
   longitudinal channel floor (sqrt(2) c, registered) propagates
   THROUGH the winding; a spring-compliant winding (axial response
   dominated by wire bending/torsion) transmits below the
   stretch-projection value and would violate the registered floor.
   Therefore: THE WINDING MUST BE STRETCH-DOMINATED AXIALLY -- a
   necessary dynamical condition on the granted fiber, registered
   falsifier-shaped: Bloch-L must return transmission >= the
   static projection, or the fine structure conflicts with the
   corpus's own floor.
2. THE R-GAP registered: the winding radius as a named missing
   registration (readable if any SHIN claim fixes the
   parametrization; otherwise it enters Bloch-L's inputs
   explicitly).
3. COMMISSION BLOCH-L chartered with full bars: instrument
   FND-089's triply-controlled Bloch machinery; inputs = registered
   angles/pitch, granted rod (kb saturated), mu_f = T0_f/c^2
   (forced), R per (2); output = c_L,f; outcome sheet in speed
   form, three regions, none null; the rigidity demand (1) as the
   internal consistency check that fires FIRST.

## REFUSALS
No R invented; no spring number tonight; targets sealed; condition
4 unchanged; the KBSAT tripwire noted on every stage-2 input.
