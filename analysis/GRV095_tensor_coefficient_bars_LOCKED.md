# GRV-095 bars — LOCKED before computing (2026-08-03)

Commission (GRV-075's P3; the fork's decisive internal test): the tensor
coefficient in physical units -- and what it decides.

The instrument facts, established by reading before computing:
- GRV-021's zp_energy diagonalizes the dynamical matrix with on-site term
  normalized to 1 and hopping kt: lattice frequencies are in units of the gap,
  so the physical energy unit is hbar omega_0 = hbar c/(a sqrt(kt)), and the
  induced gradient coefficient carries hbar c/a^2 times the pure number zeta =
  chi_2/sqrt(kt) = 1.208 (GRV-075) times a DICTIONARY FACTOR D -- premise P3.
- GRV-024/025's absorption instrument is THREE-DIMENSIONAL with explicit
  one-metric coupling (tensor patterns xx, zz, xy, xz): the exact-D extraction
  is a well-posed future computation on that instrument; tonight D is BOUNDED.

The D-bracket, pre-committed with each factor enumerated:
- family/orientation counting (chains per volume, parallel-family selection):
  between 1/3 and 3;
- polarization/mode counting of the registered band: between 1 and 4;
- the one-metric normalization (GRV-029's dictionary is exact, so its
  normalization is fixed and O(1)): between 1/3 and 3.
- CONSERVATIVE COMBINED BRACKET, locked: D in [1e-2, 1e2]. The decisive test
  below must be robust to this ENTIRE range or it does not run.

Bars:
- B1: the unit reconstruction by machine (sympy): induced EH-candidate tension
  = zeta D hbar c/a^2, dimensionally a tension, with the required value
  c^4/(16 pi G) = 2.4e42 N (GRV-074).
- B2: the decisive comparison at BOTH fork branches across the ENTIRE
  D-bracket:
  - F-Lor (a = 1e-16 m): report the maximum achievable induced tension and
    the deficit in orders of magnitude. Pre-committed decision rule: if the
    deficit exceeds 10 orders at D = 1e2, the branch CANNOT supply gravity
    through the derived channel, unrescuably by P3.
  - F-Sak: solve for a across the D-bracket; report the range in Planck
    lengths.
- B3 (the fork verdict, grammar fixed in advance): the corpus's weak-field
  gravity IS the derived induced channel (GRV-025 Derived; GRV-026/028/029
  unconditional, matching observation). Therefore:
  - If B2's F-Lor deficit is unrescuable: F-Lor is a branch on which the
    corpus's OWN DERIVED GRAVITY cannot account for Newton's constant, and
    would require a second, unregistered gravitational mechanism. Verdict:
    THE FORK RESOLVES TO F-SAK BY DERIVATION AND ECONOMY -- adopted as the
    working branch, with F-Lor demoted to named-alternative-requiring-
    unbuilt-physics (not deleted; demoted). Conditions carried on the claim's
    face: (C1) no second gravity channel exists in the registry; (C2) the
    registered hbar is the induced formula's hbar.
- B4 (the cascade, enumerated): the fork-sensitive one-power quantities
  (GRV-094) are pinned to their F-Sak values -- Sigma, the Hawking-form C,
  the snap action, n_q (the whisper's classicality deepens to one quantum per
  ~1e22 snaps), and the PVLAS-class nonlinearity discriminator becomes a
  DEFINITE prediction. The a-refinement within sqrt(D) (factor <= 10) is the
  named residual, closed by the exact-D extraction on the 3D instrument.
