# FND-029 — the nuclear import returns a bounded reduction: matter stability and the ħ scale branch now wait on the same width

Date: 2026-08-09. Commission: EM-RECON-017's designated settler #1.
Bars: `analysis/NUCIMPORT_bars_LOCKED.md` (chain committed before any
number moved). Benchmark:
`benchmarks/foundations/nuc_import_kc_confrontation.py`.

## The confrontation

Registered inputs only: e_b = 4.716 MeV (NUC-007, the nuclear sector's
one calibrated bond energy) against T0·a = 0.1627 MeV (the M-point, the
corpus's one spent calibration). Raw bundle-level ratio: 29.0.

Through the committed chain — L1 the bond→barrier O(1) conversion
(factor-3 band, direction-neutral), L2 the bundle→strand multiplicity
m_b ∈ [1, 500] (bounds only; unregistered) — the per-crossing ratio is

    E_x/(T0·a) ∈ [0.019, 87]   vs survival band [0.40, 3.00].

**The bound straddles the threshold. Outcome: BOUNDED REDUCTION** (bar 2).
Neither survival nor failure may be claimed; the block sits at exactly
one link.

## The displayed estimates (assumptions visible; not adopted)

| multiplicity model | ratio | vs band |
|---|---|---|
| m_b = 1 (single pair) | 29.0 | above |
| m_b = √n_t ~ 22 (surface line) | 1.32 | INSIDE |
| m_b = n_t^⅔ ~ 63 (contact patch) | 0.46 | INSIDE |
| m_b = n_t = 498 (full section) | 0.06 | below |

The geometrically-motivated middle models land inside the band. The
numerology guard's verdict stands on the face: a four-order m_b range
crossing a one-order band makes proximity weak evidence by itself.
Reported as an observation, not adopted.

## The structural finding (bar 4)

The block is m_b, a function of the constituent width w — **the same
unregistered width on which QGATE-005's tube census, and with it the
corpus's entire ħ scale branch, already waits.** One number now carries
both of the framework's largest open verdicts: whether it owns its own
quantum of action, and whether its contact physics stabilizes its own
matter. The QGATE-005 standing tripwire extends accordingly: any future
claim fixing w feeds through BOTH censuses immediately — the tube census
(n_t vs the collective-ħ demand) and this one (m_b vs the survival band).

The named width routes remain those QGATE-005 registered, with the
vacuum-tension chain (now living on the Σ ≥ 5.1e35 prediction and the
VMB@CERN arbiter, per QGATE-007) still the most promising because it
connects w to an independently measurable quantity.

## Consequences

- EM-RECON-017's settler #1 has run: the reduction is now two-sided and
  quantified rather than open-ended. Settler #2 (the standoff refinement
  via f_c, narrowing C) remains available and would tighten the survival
  band itself.
- EM-RECON-009's core: conditional as before, now with the condition's
  missing number identified with an existing corpus bottleneck rather
  than a new one — the corpus's open-problem count did not grow.

## Not claimed

m_b; w; survival or failure; any nuclear re-interpretation; any new
parameter. Spend remains ONE.
