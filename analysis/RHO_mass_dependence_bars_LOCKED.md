# Commission RHO — the mass-dependence prediction: locked bars

*Locked 2026-08-10, before any literature search on quenched-vs-dynamical
string tensions. CONTAMINATION DISCLOSED UP FRONT: the three-point width axis
(quenched 0.369-0.391 fm from PI; 160 MeV 0.353 fm central from XI; physical
0.402-0.407 fm from ELEC-052/081) is already in this session's context, so no
prediction OF THE WIDTHS can be blind. The blind element is constructed
downstream: the framework's registered commitments FORCE the entire width
shift into one external quantity — the string tension ratio — whose lattice
value this session has NOT seen. That confrontation is the commission.*

## The derivation chain, from registered claims only
D1 R_eq = sqrt(T_tube/(pi Sigma))  — the registered uniform-cylinder
   identification (ELEC-052 B3 propagation; NUCQ-003 relation), so
   dR/R = (1/2)(dT_tube/T_tube - dSigma/Sigma).
D2 THE ONE-MEDIUM DECLARATION (ELEC-038, framework-level commitment): vacuum
   strands and matter ropes are the same kind of object — the vacuum medium
   and its stiffness Sigma are NOT altered by which hadrons exist in it.
   FND-017: T0 is a global Lagrange multiplier of the medium's configuration,
   not a local property tunable by sea content. JOINTLY: Sigma is
   QUARK-CONTENT-INDEPENDENT. This is the framework's commitment, stated
   here as load-bearing.
D3 THEREFORE the entire measured width shift must live in T_tube = the QCD
   string tension sigma: R_dyn/R_quench = sqrt(sigma_dyn/sigma_quench).

## The forced prediction (computed from the contaminated widths, stated as such)
P1 Width ratio physical-mass/quenched: 0.402-0.407 over 0.369-0.391
   => R ratio in [1.028, 1.103]
   => PREDICTION: sigma_dyn(physical)/sigma_quench in [1.06, 1.22],
      i.e. the string tension WITH sea quarks at physical masses is 6-22
      percent LARGER than quenched, at matched physical scale setting.
P2 Sign statement (the 1-bit core): sigma_dyn > sigma_quench. If the
   literature says equal or smaller, the prediction FAILS.

## Locked bars
B1 THE SEARCH, performed only after this lock: published lattice
   determinations of the string tension (or the intermediate-distance slope
   of the static potential, before string breaking) in (2+1)-flavor QCD at
   or near physical masses, in physical units via a sigma-INDEPENDENT scale
   (r0, r1, fpi, w0), compared against quenched SU(3) at matched scale
   convention. Scale-setting circularity is the known trap and each source's
   convention must be stated; sources that DEFINE the scale by sigma are
   unusable for the ratio and are excluded with that reason.
B2 THE CONFRONTATION BANDS, pre-committed on the ratio s = sigma_dyn/sigma_q:
   - SURVIVES: s in [1.06, 1.22] (central overlap) or errors overlapping it.
   - SIGN-ONLY: s > 1 but below 1.06 — the direction survives, the magnitude
     fails; the residual must then be attributed and Sigma-universality is
     WOUNDED at the corresponding percentage, quantified.
   - FAILED: s <= 1 within errors — the one-medium reading that makes Sigma
     quark-content-independent is CONTRADICTED by the width axis; register
     Failed-and-kept with the required dSigma/Sigma stated (the amount the
     vacuum stiffness would have to soften with sea content).
B3 HONESTY ITEMS carried whichever way it falls: (a) the widths are
   contaminated (seen), the sigma ratio is the blind element; (b) the
   160 MeV midpoint (0.353) sits BELOW the quenched bracket and, if taken at
   face value, already violates monotonicity in the quark mass — its ~50
   percent kappa systematic is the registered reason it does not enter P1,
   and this exclusion is declared HERE, before knowing whether it would help
   or hurt; (c) string breaking caps the comparison window: sigma must be
   compared at intermediate d (~0.4-0.8 fm), pre-breaking.
B4 A verifier encoding P1/P2 and the verdict, extending the brick series.
