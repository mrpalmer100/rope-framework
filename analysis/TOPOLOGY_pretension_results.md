# TOPOLOGY COMMISSION -- PRETENSION BRICK RESULTS (THIRD BRICK)
# Executed 2026-08-26 under analysis/TOPOLOGY_pretension_charter_
# LOCKED.md. Instrument: benchmarks/foundations/pretension_brick.py
# (four daylight amendments, each justified on the pristine
# calibration row alone, all z-blind, annotated in-file; the full
# amendment ledger is part of this record). State: /tmp/pret_ckpt
# .pkl -> analysis/pretension_brick_ckpt.pkl.

## THE QUESTION

Does pretension buy weave membership -- is the vector-sector
threshold z_c a function of tau = T0/(k a)? Committed prior: W1
(spider-web: tension stabilizes sub-isostatic networks, z_c falls
with tau). 

## HEADLINE: ** W2 WITHIN BRACKETS -- AND A REASON. UNIFORM
## PRETENSION RESCALES; IT NEVER RESCUES. **

The V2 map (equatorial transverse channel; p = steady amplitude
exponent, c = measured front speed; each column carries its own
pristine calibration):

  z      tau=1.0          tau=0.3          tau=0.02
  6 cal  p 1.08  c 0.95   p 1.69  c 0.53   p 4.06  c 0.134
  4.0    p 1.10  c 0.69   p 1.06  c 0.41   p 0.54  c 0.113
  3.5    p 1.23  c 0.61   p 1.15  c 0.37   p 1.38  c 0.085
  3.0    (not run)        p 2.08  c 0.32   p 3.05  c 0.058

  measured c_T references (pristine): 0.95 / 0.53 / 0.134 vs the
  sqrt(tau) theory 1.0 / 0.548 / 0.141 -- the speed instrument
  reads true at every tau.

BREAKDOWN BRACKETS: propagation survives with healthy relative
speed (c ~ 0.6-0.8 of the column's own c_T) down to z = 3.5 at
EVERY tau; degradation (p steepening to 2-3, c sagging) appears at
z = 3.0. z_c in [3.0, 3.5] at tau = 0.3 and 0.02; [3.0, 4.0] at
tau = 1. FLAT WITHIN BRACKETS: verdict form W2. The committed W1
prior is falsified for uniform tension.

WHY IT HAD TO BE SO (recognized in hindsight, then confirmed in
the spectrum): uniform tau multiplies the ENTIRE transverse
operator, so it rescales time (c_T = sqrt(tau)) while leaving the
dilution-disorder structure -- and therefore the localization
threshold in z -- untouched. W1 remains live only for
HETEROGENEOUS tension or polarization-mixing physics, which are
outside this charter and recorded as open.

## V3 FLOPPY-MODE WITNESS (the mechanism fingerprint, L = 13 core)

  z=6: null modes = 3 (rigid translations only) at both taus;
       lowest genuine eigenvalue 0.0581 (tau=1) -> 0.00116
       (tau=0.02): ratio 50 = 1/tau EXACTLY.
  z=4: null modes = 3 at both taus; 0.0315 -> 0.00074, ratio ~43.
  NO floppy proliferation below Maxwell at any tested tau: every
  sub-isostatic direction is gapped BY tension at stiffness
  proportional to tau -- the same tau that sets c_T^2 -- so the
  spectrum rescales uniformly and z_c cannot move. The theorem's
  microscopic form.

## THE SURPRISE FINDING (reported as measured; outside the binary)

At tau = 0.02 the PRISTINE lattice is not an isotropic 3D medium:
a z-polarized source rides the stiffness-1 longitudinal channel
along its own columns (c = 1) while the equatorial tension channel
runs at c_T = 0.14 -- a leaky-slab anisotropy that shows up as
p = 4.06. DILUTION CURES IT: breaking columns scatters the axial
drain back, p falling 4.06 -> 1.44 (z=5) -> 0.54 (z=4,
quasi-2D trapping). A dimensional-crossover regime that the
MEDIUM/LOCALIZING binary does not describe; per the charter's
mixed-outcome clause it is reported as these numbers, no form.

## INSTRUMENT LEDGER (all daylight, all pristine-row justified)

1. Annulus width (L 33 -> 45): a 4-bin annulus cannot carry a
   power-law fit (pristine tau=1 read 1.58; reads 1.08 at L=45).
2. Fixed-wavenumber drive omega = k0 sqrt(tau): a fixed frequency
   exits the transverse band as tau falls (evanescent, not
   localized).
3. Equatorial annulus |dz| <= 2: spherical shells over a 7:1
   anisotropic field produce meaningless exponents.
4. Differential classifier (each (z,tau) vs its pristine row):
   an absolute p ~ 1 bar presumes isotropy low tau lacks. NOTE
   honestly: the binary still misfires where dilution CURES
   anisotropy (p moves toward 1, AWAY from the pristine
   reference); the map is therefore read as brackets from p AND
   c/c_T jointly, and the classifier column is advisory.
The tau=1 rows above are restated under the final instrument.

## STANDING

Verdict recommendation W2 (flat z_c within brackets) with the
rescale-not-rescue mechanism; heterogeneous tension and
polarization mixing recorded as the open edges. Draft
registration: analysis/TOPOLOGY_pretension_draft_registration.md
(FND-149 suggested), AWAITING THE AUTHOR'S GRANT.
