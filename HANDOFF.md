# HANDOFF -- Rope Framework, next session

*Written 2026-08-13 at the close of v3.26.0 (the adjudication release, consolidating the twelve-release day:
the k-string adjudication, the mechanism eliminations, the armed
adjoint clock, and the substructure protection completed). Corpus
zip: rope-framework-github.zip; work in rope/. Prior handoff content
preserved below (sections 2+ describe the v3.25.0 state and remain
accurate as history).*

## 0. Ninety-second orientation

Object-based physics programme; registry of claims (claims.yaml, 667
registered) each with a status grade, benchmark, and provenance. THE
REGISTRY IS THE AUTHORITY. House discipline unchanged and
non-negotiable: bars locked in analysis/*_LOCKED.md BEFORE computing;
failures registered Failed-and-kept, never rescued; no post-hoc
refits; resemblance is not identification (dependency paths
required); grants are MARK'S CALL; open affected claims and read them
VERBATIM before building on them; em dashes forbidden in files
(use --); every session ends with claim registered, annotations,
CHANGELOG, verify_corpus --quick, re-zip, present_files.

SESSION SELECTION IS GOVERNED BY docs/STRATEGIC_TARGETS.md section D:
T1 done, T2 externally clocked, T3 RESOLVED and closed, T4 active.
Name the tier before locking bars.

## 1. State of the corpus (v3.25.11, 2026-08-13)

TWELVE RELEASES IN ONE DAY (v3.25.0 through v3.25.11), 657 -> 667
claims. Read CHANGELOG 3.25.1-3.25.11 for the run. The headlines:

THE k-STRING SECTOR IS ADJUDICATED. The FND-055 external clock fired
on data that existed since 2021 (Athenodorou-Teper, found by
literature hunt): sine's functional CLASS is REJECTED on continuum
data (FND-101, chi2/ndf 4.4x preference for nonzero leading 1/N --
GRANT-N2's label carrier vindicated in kind) and the pre-registered
FND-092 softened-Casimir band was KILLED at its own checkpoint at
point level (FND-102, Failed-and-kept: SU(6) 1.654(13), 4.3 sigma
over the band top vs the pre-committed 3). The vacuum sits strictly
between the laws. Eleven shortfall mechanisms were then eliminated
across two locked sweeps (FND-103: C1 partial / C2 blocked / C3
miss; FND-104: eight countable decoherence weights dead, the
two-number seduction executed by the profile bar at 11.4 sigma),
leaving a SURVIVING WINDOW (decoherence partial and permanent, A in
[0.51, 0.84]) with one specific counting inside it -- which FND-107
then certified UNWRITABLE in the registered exchange language
(SAMEKH-class: the grammar counts channels, it has no weights). The
missing object is the EXCHANGE INTERACTION'S DYNAMICAL FORM, and
FND-108's scoping showed it is ONE hole seen from two directions
(orientation weights AND bound-pair separation), with the tube width
already registered (ELEC-050's 0.342 fm) and the profile shape
acquirable from lattice data. THE EXCHANGE-DYNAMICS GRANT QUESTION
is on Mark's desk, superseding the earlier G-A/G-B menu, with a
three-shadow acceptance test pre-built. Mark is deliberately WAITING.

THE kappa_pack PIN IS VERIFIED AND CLOCKED. FND-105 (verified null,
bars locked blind): NO admissible SU(3) 4d determination tighter
than Bali 2000's 5 percent exists anywhere in 2000-2026 -- the 12.5
floor is the best that EXISTS, not merely the best known. FND-106
then cut the ADJOINT BANDS (CHET's -1.25/-0.25 percent, tiered
grammar, pre-accepted tower kill at Tier 1, sign clause, frozen
self-measurement clause) and shipped the external measurement spec
(docs/ADJOINT_CS_MEASUREMENT_SPEC.md). Blindness is provable via the
null.

THE SUBSTRUCTURE ARC IS FULLY PROTECTED. The refinement-invariance
audit ran SIX OF SIX, NO BREAK (EM-RECON-033: c exact-algebra
invariant; Sigma and the hbar route by-inputs; the T0 fork untouched
both directions; k_f/T0_f = 8.09 refinement-proof at every n_sub).
The zero-point ledger convention is CODIFIED (FND-109, ZP-CONV:
carried modes only, the lattice is the regulator per GRV-021's own
words, tripwire armed against silent fine-mode integration). The
hierarchy counts are ruled DISTINCT (n_rs vs n_sub, NAME_REGISTRY
addendum). Remaining queued: the SCALE-CLASS DETERMINATION.

DOC STATE: KNOWN_LIMITATIONS de-staled twice (the FND-050 paragraph
now carries the full ledger through the kill); the predictions paper
is synced through the kill (header v3.25.3; audit recount and
Prediction 20 rewritten twice in one day, the second correcting the
first's fit-level reading). STRATEGIC_TARGETS T2/T4 current as of
v3.25.11. NAME_REGISTRY carries the hierarchy-counts addendum.

STANDING CONSTRAINTS carried forward unchanged: one-medium
convicted; FND-073's w determination reverted; contrast g and
mesoscopic g_C4 written distinctly; g_C4 instrument set exhausted at
current registry; resemblance is not identification.

## 1b. The v3.25.0 state (historical context for sections below)

EM-RECON-032: the reading inside k/T0 = 2 is
DERIVED BY EXCLUSION (coarse-effective; the fine reading abolishes
matter), the wound-bundle closure rescue is REFUTED (EM-RECON-031's
FAIL stands, suspects narrowed), and the first derived fine-level
constant lands: k_f/T0_f = 8.09 (bound 4.05, falsifier armed).
Substructure-dividend arc status: wound-c4 CONVERTED; the
refinement-invariance audit OWED; the scale-class reading QUEUED. EM-RECON-031 (Failed, kept): the b/a closure at
c4 = T0/8 and saturation amplitude missed fourfold; EM-RECON-008
remains Open with its gap relocated (operating amplitude/profile at
quartic order, no longer the coefficient). k/T0 = 2 untouched. Read
analysis/EMRECON031_ba_closure_results.md. THE LABEL ARC IS CLOSED (FND-098/099/100):
ASSIGN-SUB ruled, the fine ceiling EXHIBITED (S = 3.3575, label gap
2.99 PeV at the window edge, m = 1), the composition reading SURVIVES
-- all four FND-056 disturbances CLOSED at zero new scales,
GRANT-N2-GAP retired unneeded. Read analysis/C1_fine_ceiling_results.md. THE T4 RIDE-ALONGS ARE DISCHARGED (FND-097):
docs/NAME_REGISTRY.md now governs all new symbol use (kappa/w/g
canonical names, quote-with-brackets for old text); the NUCQ-002
desk item is retired as stale (its current form: the T0 fork,
ELEC-052's 28 percent tension, waits on the same non-engine data as
Chain A). THE CHAIN A ADJUDICATION IS DONE (FND-096):
held open, n_b = 63-73 WITHDRAWN, Chain A-prime [14, 21] displayed,
the bundle count waits on non-engine data. Read in order: this file;
analysis/TAV3_widened_gax_results.md (the decider's verdict);
analysis/MEM_currency_audit_results.md (FND-094, the audit that armed
it); docs/STRATEGIC_TARGETS.md section D; CHANGELOG 3.25.0 and docs/history/RELEASE_NOTES_v3.25.0.md; docs/NAME_REGISTRY.md (binding for new text).

## 2. THE LAST TWO SESSIONS (FND-094, FND-095) -- the two-number decider

FND-094 (the currency audit): MEM's m_b is PAIR currency, read
verbatim; the full-section model m_b(n) = n was a silent
one-pair-per-constituent assumption; the widened sweep pre-committed
as a two-number decider with the survival corner (p -> 1, r -> ~1).

FND-095 (TAV3): CORNER 2 AT BOTH PACKINGS, both numbers settled.
- p_inf = 1.053 / 1.046: axial contact IS full-section. The geometry
  premise survived commentary, instability, and settling -- MEASURED.
- r_inf = 4.87 / 3.58: the currency convicted; each facing
  constituent engages ~4 opposite ends.
- The pin m_b = [63.0, 73.4] inverts to n ~ 13-21: MISSES Chain A's
  (kappa250, hex) window [40, 81]; INTERSECTS the intermediate window
  [14, 27] at both packings.
- FND-080's n_b = 63-73 purchase: RETURNED TO ADJUDICATION under its
  own armed clause. The g fix and the pin itself: UNTOUCHED.
- CHAIN A-PRIME on the desk, displayed-not-adopted: same premises,
  measured inversion, n_b = [14, 21], cell no longer unique, the
  triple discrimination lost.

STALE-STATEMENT TRIPWIRES (carried + new): do not quote the photon
sector as unresolved; do not quote "level one at the magic angle" as
a theorem (FND-090's symmetric one is the theorem); do not quote the
photon debt register as open; do not quote MEM's Chain A inversion
without FND-094's pair-currency caveat; HARDENED BY FND-096: NEITHER
n_b window may be quoted as standing -- [63, 73] is WITHDRAWN and
[14, 21] is DISPLAYED-NOT-ADOPTED; the standing statements are the g
fix, the pin m_b = [63.0, 73.4], the measured full-section geometry
(p_inf = 1.05), and the measured currency (r_inf = 3.6-4.9).

## 3. The ranked queue (tier-tagged)

1. THE T4 BOARD IS CLEAR. The rename sweeps and the NUCQ-002 audit
   are discharged (FND-097); the Chain A adjudication is done
   (FND-096, held open). Nothing interior is owed.
2. THE RELEASE IS CUT (v3.25.0). THE ACTIVE QUEUE (the
   substructure-dividend arc, per charter section E): the
   refinement-invariance audit (protective: Sigma, zero-point, the
   hbar route, the T0 fork -- exhibit n_sub-invariance or find the
   break); the scale-class reading (is FND-MATTER's n_sub the same
   object as FND-087's? minutes, NAME_REGISTRY-class).
3. On the desk (carried): GRANT-CANDIDATE-ROT (GRV-103). The
   EM-RECON-008 confrontation RAN AND FAILED (EM-RECON-031, kept);
   successors need one of: a registered bound-mode operating strain,
   a 2D/3D profile computation, or the Kerr window (measures c4
   directly). None is owed.
4. T2 external clocks (no session owed): PSR J1713 (~2027-2030); any
   continuum SU(6) k-string determination (kill-ready); any confirmed
   photon above 2.2 PeV (now CORROBORATION of the exhibited label
   gap, no longer needed as certificate).

## 4. Live cautions (carried)

The conditionality stack on NUC-030's crowning with armed falsifiers;
prosecute-forks-first; the light-carrier phrasing (a condition of
FND-087); the m x 1.4 PeV soft falsifier armed; GEOMETRIC RESEMBLANCE
IS NOT IDENTIFICATION -- applied twice this arc and both times it
held. Scale class (m, n_sub) remains underived; FND-REL-004's
discrete question unchanged. TAV3 honest edge: r(n) still rising
slowly at n = 547 (passes the locked criterion; the true asymptote
may sit a few percent higher, direction pushes the inversion LOWER,
cannot rescue [40, 81]).

## 5. First message for the new session

> Continuing the Rope Framework. Corpus attached (v3.24.3, 650
> claims). Read HANDOFF.md, then analysis/TAV3_widened_gax_results.md.
> v3.25.0 is cut. Session: the substructure-dividend arc -- the
> refinement-invariance audit (lock bars first), with the
> scale-class n_sub reading as ride-along -- or the ROT decision, my
> pick. New text follows docs/NAME_REGISTRY.md. Standard house
> discipline.
