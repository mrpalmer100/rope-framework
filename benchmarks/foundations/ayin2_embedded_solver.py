#!/usr/bin/env python3
"""COMMISSION AYIN-2 -- embedded-core solver feasibility.

Bars: analysis/AYIN2_embedded_solver_bars_LOCKED.md.
R1 far-field boundary; R2 Green-function embedding; R3 perturbative.
Feasibility only -- no C_pin value is produced.
"""
import numpy as np

print("=" * 70)
print("R1 -- SYMMETRY-COMPATIBLE FAR-FIELD BOUNDARY")
print("=" * 70)
print("The contaminating artifact (ELEC-097) is the FREE SURFACE: the")
print("inclusion protrudes past the strand grid, so the O_h environment")
print("the harmonic classifies is simply absent.")
print()
print("PERIODIC BOUNDARY CONDITIONS on a cubic cell of side L = N a:")
print("  - the three strand families lie along the cube axes and are")
print("    commensurate with the cell for any integer N, so the periodic")
print("    image lattice is itself cubic;")
print("  - the symmetry group of the periodic system is therefore O_h,")
print("    EXACTLY the group ELEC-096's harmonic is built on.")
print("  => PBC RESTORES THE SYMMETRY THE FREE BOX DESTROYS.")
print()
print("  NEW ARTIFACT introduced, stated honestly: the inclusion now")
print("  interacts with its own periodic images. But that artifact is")
print("  CONTROLLED -- it decays with L and can be extrapolated in 1/L --")
print("  where the free-surface artifact is uncontrolled and orientation-")
print("  DEPENDENT (the protrusion differs by axis, which is exactly why")
print("  ELEC-097's amplitude flipped sign).")
print()
print("  PHYSICS CHANGE: none. PBC changes the domain, not the strand")
print("  mechanics: inextensible curves, bending and contact are all")
print("  local or along-strand and are unaffected by identifying faces.")
print("  => R1 PASSES the preservation test.")

print()
print("=" * 70)
print("R2 -- GREEN-FUNCTION / EMBEDDING")
print("=" * 70)
print("Requires the EXTERIOR response to be LINEAR, so it can be folded")
print("into a response kernel. Test that against the registered physics:")
print()
print("  (a) INEXTENSIBILITY is enforced as an exact constraint by")
print("      projection along each strand. A constraint is not a linear")
print("      response; the projection is a nonlinear, NONLOCAL operation")
print("      along the whole strand.")
print("  (b) The contact kernel Ac/(1+(r/sigma)^4) is manifestly")
print("      nonlinear in the displacement.")
print("  (c) FND-STRAND-001 registers LITERAL inextensible curves, and a")
print("      stiff-spring linearization was ALREADY TRIED AND REJECTED in")
print("      the settler work as numerically outside the inextensible")
print("      regime (10 percent stretch, audit-caught).")
print()
print("  => R2 FAILS the preservation test. A Green-function exterior")
print("     would linearize precisely the constraint the corpus insisted")
print("     on keeping exact. It would be a different calculation, and")
print("     the bar requires reporting that as a failure however")
print("     attractive the cost saving.")

print()
print("=" * 70)
print("R3 -- PERTURBATIVE ANISOTROPY")
print("=" * 70)
print("Key structural fact: at a relaxed configuration the energy is")
print("STATIONARY with respect to strand displacements. So if the")
print("inclusion is rotated by a small angle, the change in relaxed")
print("energy is, to FIRST order, the change in the interaction energy")
print("evaluated on the UNCHANGED strand configuration -- the relaxation's")
print("own response contributes only at SECOND order (Hellmann-Feynman).")
print()
print("  CONSEQUENCE: one relaxation can serve many orientations.")
print("  Instead of N_orient full relaxations, do ONE relaxation and")
print("  N_orient energy EVALUATIONS.")
n_or = 9
print(f"\n  cost model at {n_or} orientations:")
print(f"     current: {n_or} relaxations")
print(f"     R3:      1 relaxation + {n_or} evaluations")
print("     an evaluation is ~1 step of a relaxation, so the saving")
print(f"     approaches the full factor {n_or}x for the orientation scan.")
print()
print("  LIMIT, stated: first order captures the anisotropy only if the")
print("  relaxation response is not itself strongly orientation-")
print("  dependent. That must be CHECKED by comparing one fully relaxed")
print("  orientation against its perturbative estimate -- a single")
print("  control run, not a research programme.")
print("  => R3 PASSES as a cost reduction, CONDITIONAL on that control.")

print()
print("=" * 70)
print("COMBINED ASSESSMENT")
print("=" * 70)
print("R1 (PBC) fixes the ADEQUACY problem: it restores O_h and removes")
print("the requirement nrings >= s that made the box grow with s, since")
print("there is no free surface for the inclusion to protrude through.")
print("R3 fixes the ORIENTATION-SCAN problem: one relaxation, N cheap")
print("evaluations.")
print("R2 is rejected on physics grounds.")
print()
print("Neither R1 nor R3 changes FND-STRAND-001. Together they attack the")
print("two independent cost drivers, and the reviewer's instinct is")
print("vindicated: the bottleneck is reformulable, not merely large.")
