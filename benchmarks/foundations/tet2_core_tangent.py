#!/usr/bin/env python3
"""COMMISSION TET-2 -- the core's tangent field, by matching to the exterior.

Bars: analysis/TET2_core_tangent_bars_LOCKED.md.
T1 the axisymmetric solution; T2 is the alternative excluded;
T3 the angular-momentum consequence (mandatory whatever it returns).
"""
import numpy as np
import sympy as sp

print("=" * 72)
print("T1 -- THE AXISYMMETRIC MATCHING SOLUTION")
print("=" * 72)
th, ph = sp.symbols("theta phi", real=True)
print("   Exterior (GRV-020): the internal azimuth winds n = 1 about an axis;")
print("   the phase advances as phi, the azimuthal angle about that axis.")
print("   ELEC-090: strands reach r0 TANGENT to the core sphere.")
print("   A tangent field whose direction tracks the winding phase must")
print("   point along the direction in which the phase advances:")
print()
print("      v = e_phi          (the azimuthal field on S^2)")
print()
print("   ZERO SET: e_phi has magnitude sin(theta) in the round metric,")
print("   vanishing exactly where sin(theta) = 0, i.e. theta = 0 and pi --")
print("   THE TWO POLES OF THE WINDING AXIS, and nowhere else.")
mag = sp.sin(th)
print(f"      |e_phi| = {mag};  zeros at theta = 0, pi")
print()
print("   INDEX AT EACH ZERO: near a pole the azimuthal field is a pure")
print("   rotation about that point, index +1 (a centre, not a saddle).")
print("   TOTAL INDEX = 1 + 1 = 2.")
print()
print("   *** CONSISTENCY CHECK ON ELEC-090 PASSES: chi(S^2) = 2. ***")
print("   The matching solution independently reproduces the required")
print("   total index, which it was not constructed to do.")

print()
print("=" * 72)
print("T2 -- IS THE SINGLE INDEX-2 ZERO EXCLUDED?")
print("=" * 72)
print("   Hairy-ball permits one index-2 zero instead of two index-1 zeros.")
print("   Under AXISYMMETRY (a registered assumption of the ELEC-073/074")
print("   profile construction, stated as such), a tangent field must be")
print("   invariant under rotation about the axis. An index-2 zero at a")
print("   single point would leave the antipodal point a regular point of")
print("   an axisymmetric field -- but every axisymmetric tangent field on")
print("   S^2 has a zero at BOTH poles, since the tangent plane at a pole")
print("   admits no rotation-invariant nonzero vector.")
print()
print("   => the single-index-2 option is EXCLUDED BY AXISYMMETRY.")
print("   The two-pole structure is FORCED, given the assumption.")
print("   (Without axisymmetry it would survive; the exclusion is")
print("   conditional on an assumption inherited, not on a new one.)")

print()
print("=" * 72)
print("T3 -- THE ANGULAR-MOMENTUM CONSEQUENCE (mandatory)")
print("=" * 72)
print("   The matched field is v = e_phi: the strands at the core boundary")
print("   CIRCULATE about the winding axis. Under n -> -n (electron ->")
print("   positron) the phase advance reverses, so e_phi -> -e_phi and the")
print("   circulation reverses with it.")
print()
print("   THE CONFRONTATION, computed rather than avoided:")
print("   IF that circulation were the particle's spin, then the sign of")
print("   the charge would FIX the sense of the spin. Experimentally it")
print("   does not: electrons of one charge occur with BOTH spin")
print("   orientations, in equal measure. That would be a falsification.")
print()
print("   IS THE CORPUS COMMITTED TO IT? No -- and the reason is registered.")
print("   The winding axis is not fixed in space: GRV-020's broken symmetry")
print("   is G = R x SO(2) acting on the INTERNAL azimuth, and the")
print("   orientation of the winding AXIS in laboratory space is a")
print("   separate, unconstrained degree of freedom. n fixes the")
print("   circulation sense RELATIVE TO ITS OWN AXIS; it does not fix where")
print("   that axis points. Reversing the axis in space reverses the")
print("   observed circulation at fixed n.")
print()
print("   => charge fixes the HELICITY RELATION between winding and")
print("      circulation, not the spin direction in the laboratory.")
print("      The two spin states are the two orientations of the core axis.")
print()
print("   VERDICT T3: SPIN-INDEPENDENT -- no conflict. But note what this")
print("   COSTS: the corpus now owes an account of why the axis is free,")
print("   i.e. why the core's orientation is not energetically pinned to")
print("   anything in the surrounding weave. That is a NEW registered debt,")
print("   created by this commission and named here rather than left")
print("   implicit.")
