"""GRV-058 -- THE VECTOR SECTOR BUILT: the nonlinearity does generate a shift,
and it vanishes for exactly the source that has been measured.

Bars locked in analysis/GRV058_vector_sector_bars_LOCKED.md BEFORE computing.
"""
import numpy as np
import sympy as sp

TESS = 2e-6        # non-axisymmetric fraction of Earth's potential (C22/S22 scale)
LARES_SYS = 0.05   # systematic on mu = 1.00


def main():
    T, c, eps = sp.symbols("T c epsilon", positive=True)
    Ut, Ux, dt, dx = sp.symbols("U_t U_x d_t d_x", real=True)

    print("B1 DOES A CROSS TERM APPEAR? Expand the EXACT strand energy density")
    print("   L = -T sqrt(1 + u_x^2 - u_t^2/c^2) about a background (U_t, U_x):")
    L = -T * sp.sqrt(1 + (Ux + eps * dx) ** 2 - (Ut + eps * dt) ** 2 / c ** 2)
    quad = sp.expand(sp.series(L, eps, 0, 3).removeO().coeff(eps ** 2))
    cross = sp.simplify(quad.coeff(dt).coeff(dx))
    print(f"   cross coefficient = {cross}")
    assert sp.simplify(cross.subs(Ut, 0)) == 0 and sp.simplify(cross.subs(Ux, 0)) == 0
    print("   NONZERO, and proportional to U_t * U_x. THE VECTOR SECTOR EXISTS:")
    print("   the nonlinearity generates the mixed derivative the linear operator")
    print("   could not carry -- exactly as gravitomagnetism arises in GR.")
    print("   It vanishes iff U_t = 0 or U_x = 0.\n")

    print("B2 IS THIS COMPATIBLE WITH FND-REL-002?")
    print("   YES, and the distinction is the point. That claim forbids a MATERIAL")
    print("   velocity -- strands have no material points to move. What generates")
    print("   the cross term here is a moving STRAIN PATTERN, which transports no")
    print("   material and needs none. The prohibition is evaded legitimately, not")
    print("   violated. GRV-056's obstruction was real at linear order and is")
    print("   genuinely absent at nonlinear order.\n")

    print("B3 THE CASE THAT MATTERS -- and it does not cooperate:")
    print("   Lense-Thirring is measured for a STEADILY ROTATING, NEARLY")
    print("   AXISYMMETRIC body. An axisymmetric mass in steady rotation has a")
    print("   STATIONARY potential: d_t psi = 0 EXACTLY, by symmetry. The mass")
    print("   distribution rotates; the FIELD does not change.")
    print("   So U_t = 0 for Earth to the extent that Earth is axisymmetric, and")
    print("   THE CROSS TERM VANISHES for precisely the configuration that has")
    print("   been measured.")
    print("   What little survives comes from the non-axisymmetric (tesseral)")
    print(f"   harmonics, ~{TESS:.0e} of the potential, which do rotate with the body.\n")

    print("B4 THE MAGNITUDE:")
    print(f"   generated shift / GR shift <~ {TESS:.0e}  (one factor of the")
    print("   non-axisymmetric fraction at minimum, before kinematic suppression)")
    print(f"   measured: mu = 1.00 +/- {LARES_SYS} (LARES/LAGEOS, systematic)")
    print(f"   => this mechanism predicts mu ~ 1e-6, which is indistinguishable")
    print(f"      from ZERO at that precision, and zero is excluded at "
          f"{1/LARES_SYS:.0f} sigma.")
    print("   Gravity Probe B excludes zero at 5.2 sigma independently; LARES-2 at")
    print("   roughly 500.\n")

    print("B5 THE VERDICT: THE FRAMEWORK IS IN SERIOUS TENSION WITH EXISTING DATA.")
    print("   The vector sector exists and is generated by the nonlinearity, which")
    print("   answers GRV-057's question in the affirmative. But the mechanism it")
    print("   supplies is proportional to the TIME DEPENDENCE of the background")
    print("   field, and stationary rotation has none. The framework can produce")
    print("   gravitomagnetism for collapse, mergers and radiation -- and NOT for")
    print("   the steadily spinning Earth, which is the only case anyone has")
    print("   measured.")
    print("   WHAT WOULD STILL RESCUE IT, stated without inventing anything:")
    print("     (R1) a SECOND nonlinear structure, absent from the exact")
    print("          transverse energy used here, that couples to angular momentum")
    print("          rather than to d_t of the field. The corpus's framed strands")
    print("          carry twist (FND-STRAND-002/003) and GRV-045 shows writhe is")
    print("          exchanged in reconnection, so a twist-density coupling is the")
    print("          honest candidate -- but GRV-056 already showed gyroscopic")
    print("          terms carry no spatial derivative, so it would have to enter")
    print("          differently and nobody has shown it can.")
    print("     (R2) the medium's response to a rotating source being stationary in")
    print("          a co-rotating sense while still tilting cones. That is what a")
    print("          shift IS, and the linear operator cannot carry it, so this")
    print("          route needs R1 anyway.")
    print("   NEITHER IS DEMONSTRATED. Until one is, the honest statement is that")
    print("   the framework's known mechanisms give mu ~ 1e-6 where measurement")
    print("   gives 1.00 +/- 0.05.")
    print("PASS: the vector sector is built, its mechanism identified, and its")
    print("      failure on the measured case reported rather than softened.")


if __name__ == "__main__":
    main()
